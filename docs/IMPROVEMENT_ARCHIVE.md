# IvritSuite Improvement Log — Archive

Append-only overflow from [`IMPROVEMENT_LOG.md`](IMPROVEMENT_LOG.md): `## Done` entries older than
the last ~5 sessions, moved here at close-out to keep the working ledger cheap to read. Same format
as the working ledger's Done section (newest-first). Nothing here is re-summarized — it is the raw
record.

- [x] 2026-08-20 | (S238 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-eudggg` → the SAME draft PR #181 (verified OPEN/unmerged at session start via the armed check-in — head was 0574f20, mergeable clean, no comments).** Drift check: `origin/main` still at `c124e76` (no outside-loop landings since S235). **SW v527→v528** (precached files changed: torah_trainer, hebrew_dictionary, flash_cards, index, resources, contact, privacy, terms, 404 + locales). **FM: no bump** — Hebrew_Font_Maker.html untouched this session (v5.15 stands). Scripts: `build-locales` ×2 (the shared retry key + 5 copy updates; 4,457 keys, 1 known debt row), `check-i18n` CLEAN (Check A no new, Check B clean), `update-sitemap` run last. **Gates: 0 asked** — the session's only gated-class work (the retry-model convergence) was pre-approved in S237; one NEW small gate-2 item queued for the next attended session (the generator's two `reload the page` registers — same family, outside the approved four).
- [x] 2026-08-20 | c982eef | index.html + resources.html + contact.html + privacy.html + terms.html + 404.html | **The chrome dark toggle and the AllTools gear reach the touch floor** — index's ⚙ Import/Export button (the suite's backup entry point) rendered 22px, under WCAG 2.5.8's 24px; the byte-identical `.dark-btn` rule sat at 27px on all six chrome pages. Both now carry the S102 shape; `.dark-btn` stays byte-identical across its six carriers (md5-verified before AND after). | verified: 6 pages × 1280/800 all ≥30px, no header clipping, page overflow 0, HE cell clean, 0 pageerrors
- [x] 2026-08-20 | 3b92405 | torah_trainer.html | **The audio bar + in-card picker reach the touch floor** — the S234–S237 additions all sat OUTSIDE the S195 `.tt-ctrl` group rule, so the class-scoped sweeps never measured them: `#ttCardParsha` 19px (S237's own micro-feature), the loop-pause select ~21px, the chant-speed chip ~18px, Stop-looping ~25px, collapse/play-pause 28px squares, rate sliders 16px native. All at the house shape now (30px controls, 24px slider operable boxes). | verified: mocked-Sefaria bar census expanded 30/30/30+24/24, collapsed 30×4, bar+page overflow 0; card select 30px across EN/HE × light/dark with paired tokens
- [x] 2026-08-20 | c8961c0 | torah_trainer.html + flash_cards.html (`locales/ui-strings.csv`) | **The corpus register completes the APPROVED retry-model convergence (2/2)** — torah's static-data fetch extracted into `loadStaticData()`; a failed corpus load now renders ↻ Try again in the empty card (mutually exclusive with the reading-failure button by construction), and the retry re-runs the load → repopulates the picker → hands off to `fetchAndRender`. Flash's two captions promised a page reload while Start has actually retried since 95ceddc — copy now says "press Start to try again" (EN+HE). | verified: corpus-blocked boot → ↻ (31px) → unblock → click → 930-word reading rendered, 71 picker options (mocked Sefaria); HE copy; second failure re-arms; clean control; flash caption + start-note truth + a real aria-disabled Start click refetching. 0 pageerrors every arm
- [x] 2026-08-20 | d49dbe2 | hebrew_dictionary.html (`locales/ui-strings.csv`) | **Both dictionary corpus failures offer an in-place ↻ Try again (APPROVED convergence 1/2)** — the word-list chain extracted into a callable `loadWordCorpus()` (the failure card's button re-runs it, restoring the loading bar + copy first); the emoji register's button mirrors the mode-toggle's own load path. One new shared key `shared.corpus.retry_button` mirrors torah's ↻ label; both register strings drop "reload the page". New `corpus-retry-btn` class carries the S102 30px floor — **this file never had `.btn-xs`** (the first draft assumed it did and rendered 19px; the re-verify caught it). | verified: blocked-then-unblocked recovery on both registers (13,081 words / 1,581 emojis render after the click), EN+HE, dark-arm token pairing, clean-load control, 0 pageerrors
- [x] 2026-08-22 | (S239 Pass N — mobile & touch-device, the FIRST-EVER run of the pass, target `hebrew_dictionary.html`. **6 of 7 arms clean with receipts; the overflow detector proven capable of failing; 2 would-be hover-only findings killed by grounding; 1 real finding fixed in-pass.**) | Full arm-by-arm receipts in the rotation row above. Contact sheet (8 shots) delivered to the maintainer per the pass's screenshot rule — scratchpad only, never committed
- [x] 2026-08-22 | b0a9a10 | hebrew_dictionary.html | **The search and root fields stop fighting the phone keyboard** — `#searchInput` and `#shoreshInput` take Hebrew, transliteration or a Hebrew root, and carried no typing hints at all, so a phone capitalised the first letter and autocorrected "shalom"/"ktav". Added `autocapitalize="off" autocorrect="off" spellcheck="false"`; the first two are mobile-only with zero desktop effect, the third converges on 4 existing in-repo precedents (FM, generator, index, torah). **Gate-4 call recorded: NOT gated** — the mobile-only pair changes nothing on desktop, and the spellcheck half is correct on every platform under a Hebrew search box rather than a phone-driven desktop compromise | verified 12 cells (desktop/800/iPhone 13 × light+dark × EN+HE): all 4 attributes correct in every cell, input width unchanged (271px desktop / 366px phone), typing "shalom" still filters 100 cards→4 on BOTH phone and desktop, root field still accepts כ-ת-ב, 0 pageerrors; control — untouched flash_cards `#shareCodeInput` still reads `autocapitalize=null, spellcheck=true`, proving the checker distinguishes
- [x] 2026-08-22 | 7163e5f | hebrew_blend_generator.html | **Two padding-only button classes reach the 30px touch floor** (slice 1 of the S238 generator cluster; `sub-floor touch target` pattern) — `.color-mode-btn` 20→30px, `.font-size-row .fs-reset` 21.5→30px, the latter a byte-identical base rule to torah's `.sreset` fixed in S193/S195, so convergence on a proven sibling | verified 8 cells (1280/800 × light+dark × EN+HE) **with every panel expanded, because a default-state run measured NOTHING at all — 2 of the 4 `.fs-reset` buttons live in an inactive mode**; 4 visible controls per cell all at exactly 30px, 3 mandatory guards clean (no label clipped in its own box incl. Hebrew, no clipping ancestor pushed into overflow, page width unchanged), 0 pageerrors
- [x] 2026-08-22 | d2ebed4 | classroom_dashboard.html | **The TTS speed slider gets a 24px operable box** (`sub-floor touch target`, 2nd fix this session — AT THE 2/PATTERN CAP) — the file already carries the fix as a rule but scoped `.size-row input[type=range]`, and this slider is not in a `.size-row`; took `#zoomSlider`'s exact inline shape so the drawer's two sliders now match | verified 8 cells with the drawer open and panels expanded: 24px in all 8, sibling still 24px, own row not pushed into overflow, 0 pageerrors
- [x] 2026-08-22 | 0dcade6 | hebrew_blend_generator.html + locales | **Both corpus failures offer an in-place Try again** — completes the S237-ratified retry convergence (S238 shipped the other four). GATE 2 ASKED AND APPROVED in-session | verified 12 cells on the real teacher path (Real Words on, corpus aborted then restored): button visible 79×30 EN / 68×30 HE, recovers 13,081 words, self-removes, no "reload the page" left, 0 pageerrors; notice path verified separately in both languages
- [x] 2026-08-22 | (S239 close-out) | branch/deploy note | Branch `claude/improveloop-mobile-support-pass-ifhznq` → **draft PR #192** (opened this session for the pass-N skill update; the S235–S238 branch had already landed). **DRIFT: large, and the ledger's own S239 pointer was STALE — it recorded sw v528 / FM 5.15 at `c124e76`, but `origin/main` was at `ba91468` with sw.js at v543 and FONT_MAKER_VERSION at 5.24.** So ~15 sw bumps and 9 FM releases landed outside the loop between S238 and now: FM canvas rulers + drag-out guides, a canvas grid with adjustable spacing and snap-to-grid, constant-on-screen-size control handles, a 10-finding adversarial audit round, the ײַ U+FB1F Yiddish addition, plus resources.html gaining the Hebrew Touch Typing Trainer. **The clone arrived SHALLOW (50 commits) — `git fetch --unshallow` was run before any date-based receipt, per CLAUDE.md's warning that `git log` dates and `update-sitemap.mjs` are both unreliable on a shallow clone.** sw.js **v543→v544** (precached files changed: hebrew_dictionary.html, hebrew_blend_generator.html, classroom_dashboard.html, locales/en.json, locales/he.json). FONT_MAKER_VERSION stays **5.24** — nothing this session touched the Font Maker. Scripts run: `check-i18n.js` (clean), `build-locales.js` (2 keys retailed, no new keys), `update-sitemap.mjs` (last step). **Gates: 1 asked, 1 answered, 0 deferred** — gate 2 approved extending the retry model; gate 1 was never asked because the micro-feature costs 2 iterations and only 1 remained, so it was budget-excluded rather than declined; gate 4 was reached once and answered in the negative (the dictionary input fix has no desktop-visible layout effect), recorded above. **Protocol note for the skill:** pass N's own definition worked as written, with one lesson worth carrying — twice this session a fix targeted controls that render 0×0 in the default state (`.fs-reset` in an inactive mode, `#rwRetryBtn` behind the Real Words toggle), and a naive verification would have reported success while measuring nothing. Expand panels and switch the owning mode ON before believing a measurement
- [x] 2026-08-20 | (S238 Pass A — recurring-pattern sweep, over `3b572b5..HEAD` = 60 commits / 2,975 insertions / 30 files — the S227–S237 haul PLUS the outside-loop Starting-Fonts/FM v5.14–5.15 partner surface no A had seen. **16 signature classes CLEAN with receipts over 1,553 added code lines; the pointer-mandated WIDENED touch census found the real haul — 10 sub-24 controls on 5 pages, two introduced by the loop's own S234/S237 features.**) | Full class-by-class receipts in the sweep-status entry; the haul and its census-method carries in the pattern-health line. 2 fix-iterations landed in-session (3b92405, c982eef); generator/dashboard/FM residue logged as candidates

- [x] 2026-08-20 | (S237 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-eudggg` → the SAME draft PR #181 (verified OPEN/unmerged at session start; head a05399f → this commit).** Drift check: `origin/main` unmoved at c124e76; sw was v526, FM 5.15. SW **v526→v527** (precached torah_trainer, hebrew_dictionary, hebrew_blend_generator, trope_tutor + both locale JSONs changed). **No FONT_MAKER_VERSION bump** (the glyph-name work is a localization fill, zero FM code). `check-i18n` clean; `build-locales` run twice (net +11 keys: micro-feature aria + the 19 glyph names − 9 dead lad_* rows); `update-sitemap.mjs` run last. **ATTENDED session — 4 decision gates asked and answered** (the user flagged that unattended runs had been deferring gates): gate 2 retry-model → converge all four (APPROVED, budget-spilled to S238 as the top approved candidate); gate 2 font-attr shortcut → drop it (SHIPPED 57411f5); gate 2 FM Hebrew names → author + ship (SHIPPED 07df7a7); gate 1 micro-feature → torah in-card parsha picker (SHIPPED 0fc5f73). The standing gate-2 queue is now EMPTY. Full 5/5 budget.

- [x] 2026-08-20 | 57411f5 | hebrew_dictionary.html + hebrew_blend_generator.html + torah_trainer.html + trope_tutor.html | **The font pickers' attribution line is display-only on all four carriers (gate-2 APPROVED: drop the shortcut)** — the 10px credit span's pointer-only onclick made a near-miss PICK A FONT and was keyboard-unreachable by construction; the onclick/data-link and the clickability-advertising :hover underline are gone, credit text stays. One-concern four-carrier convergence (the 0c93dde shape). | verified on all 4 pages (+ dictionary dark): 0 spans with handlers, instrumented window.open at 0 calls, a click on the credit line now predictably picks that font; 0 pageerrors

- [x] 2026-08-20 | 07df7a7 | `locales/ui-strings.csv` (Font Maker display) | **The 19 composed-form glyph names get their Hebrew (gate-2 APPROVED: author + ship)** — pasekh/komets alef, veys/khof/fey, the three digraphs, seven rafe forms, sin, holam(vav), shuruk, and the `mark` fallback. The nine dead `lad_*` rows (keyed under a scheme the live slugger never produces) were re-keyed in place with samples stripped from both language cells (precompName re-appends them); five Yiddish-orthography spellings carry REVIEW markers for the maintainer. No FM version bump (localization fill). | verified via the precompName path over all 27 composed entries: EN byte-identical (zero regression), HE fully authored + samples, 0 raw keys, 0 pageerrors

- [x] 2026-08-20 | 0fc5f73 | torah_trainer.html (`locales/ui-strings.csv`) | **MICRO-FEATURE (gate-1 chosen by the maintainer): the empty/failed reading card offers a real in-card parsha picker** — the second half of the S180 seed (Retry shipped S233). The card's <select> lists the 54 plain parsha options (language-invariant labels; holidays stay in the toolbar) and drives the REAL #parshaSelect on change so every save/sync/fetch path runs unchanged; hidden when the parshiyot corpus itself failed. empty_body drops the stale 'from the dropdown above'; 1 new aria key EN+HE. | verified fresh-profile: 8/8 failed-card cells EN/HE × light/dark × 1280/800; end-to-end 503-then-200 mock — picking Vayera in the card renders its 930-word reading; empty variant + corpus-failure guard; 0 pageerrors

- [x] 2026-08-20 | (S237 Pass M — aesthetics & visual design, 2nd-ever run, `hebrew_dictionary.html`; no pass-sourced code) | **Verdict: one of the suite's best-kept pages** — dark parity clean, card grid disciplined, hierarchy clear, RTL mirror excellent — across light/dark × 1280/800 EN + HE 1280, with a design-measurement census (type sizes, radii, shadows, gaps, button heights) and full-page screenshots (BEFORE set under scratchpad, delivered at close-out per the M screenshot rule; no aesthetics-track change shipped, so no AFTER set). **Findings: 2 fixable P4s logged** — the ≤800px header `.nav-btn`s wrap into two-line pills (Tour/Dark vs one-line Home; white-space:nowrap), and `.wc-transl` English glosses in the Hebrew UI render their terminal period at the visual START (bidi; the fix needs an alignment decision — dir=auto flips alignment, direction:ltr + text-align:end keeps it) — **plus 2 measurements** (six type sizes crowd the 11.5–12.8px band, FM-S225's shape; radius census 3/4/8-dominant with 5px ×4 + 6px ×6 strays). **No gate-3** — nothing dramatic to propose. 0 pageerrors in every cell.

- [x] 2026-08-20 | (S236 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-eudggg` → the SAME draft PR #181 (verified OPEN, unmerged, mergeable at session start; head 292b2dc → this commit).** Drift check: `origin/main` unmoved at c124e76 since the S235 close-out; sw was v525, FM 5.15 — nothing landed outside the loop. SW **v525→v526** (precached `classroom_dashboard.html`, `torah_trainer.html`, `trope_tutor.html` changed). **No FONT_MAKER_VERSION bump** (FM untouched). `check-i18n` clean (zero new/changed strings this session — no `build-locales` run needed); `update-sitemap.mjs` run last. **Decisions deferred to maintainer: none new** (unattended run; no gated work was selected — the standing gate-2 trio still waits for the next attended session). Honest early stop at 4/5 iterations: dashboard and torah both hit the 2/tool cap and every remaining open P3+ candidate is capped, gated, or informational.

- [x] 2026-08-20 | 3a7cee4 | classroom_dashboard.html | **Four touch targets reach the WCAG 2.5.8 24px floor** — the zoom-bar Reset (18px) and weather ↺ Refresh (19px) are class-less inline-styled buttons that predate `.btn-xs`, so the S193 class-scoped sweep couldn't see them (they now carry `.btn-xs` → 30px with their inline look preserved); the zoom slider's 16px operable strip → `height:24px` (the 3ae6891 native-range precedent); `.sub-section-hdr` 23px → `min-height:24px` (the S207 collapse-header precedent). | verified light/dark × 1280/800: all four shapes ≥24px, docOverflow 0, 0 pageerrors, zoom-bar element screenshot eyeballed clean

- [x] 2026-08-20 | 3ea69e5 | torah_trainer.html | **The looped practice verse is remembered across visits — S180 audio slice (b), closing the audio-ergonomics cluster.** `startLoop` records `settings.loopVerse` (rides the settings blob — no AllTools wiring); init re-arms it through the existing `_pendingLoopVerse` consumer with a new arm-only flag: armed, marked, scrolled into view (`flashVerse`), playback waits for ▶ — never trusting the autoplay policy to block `play()`, since a daily student can earn Chrome autoplay permission. The stored value is regex-guarded exactly like the `?v=` deep link (it flows into a querySelector; imports are untrusted); an explicit deep link wins; a stale verse or non-aliyah scope drops silently. | verified with the mocked-Sefaria harness (S233 precedent): 8/8 restore cells (EN/HE × light/dark × 1280/800) armed + paused with instrumented `HTMLMediaElement.play` at 0 calls + verse marked and in view; stale-drop, deeplink-wins, full-parsha-no-arm, and the click path still persisting + playing; 0 pageerrors

- [x] 2026-08-20 | 0c93dde | classroom_dashboard.html + torah_trainer.html + trope_tutor.html | **Drawer initial focus returns to the ✕ close on all three drawer pages** — S235's `#drawerDarkBtn` carries `.settings-close` for styling and sits first in `.settings-hbtns`, so every `openSettings`'s `querySelector('.settings-close')` had started focusing the 🌙 toggle instead of the ✕ — a regression vs the receipted S191/S213 opener→close contract, introduced by S235 and caught by S236's own Pass C. `:not(#drawerDarkBtn)` in the focus selector on all three pages. | verified 12/12 cells (3 pages × light/dark × 1280/800): focus lands on the ✕, 0/10 Tab escapes, Escape closes, focus restored to the opener; 0 pageerrors

- [x] 2026-08-20 | (S236 Pass C — accessibility, `classroom_dashboard.html`, its 2nd dedicated C (S135 → S236, ~101 sessions — the suite's longest C gap); no pass-sourced code) | **a11y-strong: 9 dimensions clean over the never-C'd surface** (the weekly Schedule Sync grid editor, the folder trees, the first-run card, the S235 in-drawer toggle): 150 visible controls × EN/HE → 0 nameless / 0 placeholder-only / 0 symbol-only (2 title-only — the one name-quality residue, logged); drawer contract exact (focus in → 0/30 Tab escapes → Escape → restore to `#gearBtn`); the week editor adds arrow-key cell roving (Right day+1, Down period+1) and the day-menu-absorbs-first-Escape contract, both verified live; the first-run card takes focus, cycles its 9 controls, Escape-dismisses; tour opens/focuses/restores; reduce 0 animating (control: 1); contrast 0 real misses in BOTH themes (the injected control chip fired at 1.66:1; both flagged texts refuted — spoiler-by-design + the correctly-hidden upload input); focus rings 5/5 in both themes under the corrected padded-clip + real-keyboard recipe; `aria-pressed` on the scheme + font groups with Reset correctly stateless; 0 pageerrors across every probe. **Findings: 3 fixable — 2 fixed in-session (0c93dde, 3a7cee4), 1 cap-blocked (the message-editor toolbar's title-only controls, queued no-gate) — plus 2 refutations and a ring-probe method record, all logged.**

- [x] 2026-08-20 | (S235 close-out) | branch/deploy note | **Branch/PR: FRESH branch `claude/improve-loop-eudggg` cut from latest `origin/main` (c124e76) → NEW draft PR #181** — PR #178 had merged via the recorded 2026-08-20 direct-to-main landing, so the S235 pointer's if-merged arm applied (the harness pre-designated the branch name). **Drift check (diffed against the S234 close-out's recorded position, per the standing lesson): `origin/main` b95e6c7 → c124e76** — PR #178's own landing (the S233–S234 loop branch, recorded in the ⚑ note) plus the two out-of-band feature landings (PR #179 mark fidelity, FM 5.13→5.14; PR #180 Starting Fonts, FM 5.14→5.15 + `starting-fonts/` + sw v522→v524), all already recorded in Done, and Pass B swept that surface (the `?start=` boot arm). SW **v524→v525** in the final commit (precached files changed: torah_trainer, classroom_dashboard, trope_tutor, locales/en+he.json). FM: **no bump** (no Font Maker change this session). Scripts run: build-locales (1 new key, EN+HE authored), check-i18n (**clean** — Check A clean + 14 baselined, Check B clean), update-sitemap as last step **after `git fetch --unshallow`** (the container clone was shallow, exactly the case the script refuses). **Decisions deferred to maintainer: none new** — no gated work was selected (unattended run; the 3 standing gate-2 items remain queued for the next attended session). Session ended at 4 iterations (1 pass + 3 fixes): torah hit the 2/tool cap, the drawer-coverage class hit the 2/pattern cap, and no ungated uncapped candidate remained — an honest early stop, not verification debt.
- [x] 2026-08-20 | d640748 | classroom_dashboard.html + trope_tutor.html | **Both sibling drawers converge on torah's in-drawer dark toggle — premise re-measured on each, not assumed**: with the drawer open, `elementFromPoint` at the dark button's center missed the button on both pages (and a first probe that measured mid-slide read `covered:false` — wait out the 0.3s translateX before geometry probes). Same synced chip in both drawer headers (icon + `aria-pressed`, `shared.darkmode.toggle_title`, no new strings). The dashboard's dark branch also gains `initColorPickers()` so the vowel swatches keep matching the theme when dark flips while the drawer is open — the invariant its reopen path already maintained (and CLAUDE.md's own picker rule). | verified: 34-check matrix (2 pages × EN/HE × 1280/800, toggle + reload-sync + toggle-back) + the swatch invariant `#0099bb`→`#33ccee` across an in-drawer toggle; 0 pageerrors
- [x] 2026-08-20 | cac7da1 | torah_trainer.html | **The settings drawer offers the dark-mode toggle it covers** — the S180 Pass-H P3 (the 380px drawer overlays all four header controls). Grounded split: **dark is the one covered control whose state the drawer's own contents depend on** (the vowel/trope color pickers read dark at call time), so the drawer header now carries a synced 🌙/☀️ chip beside ✕ (two-way via toggleDark()/initDark(), `aria-pressed` per the suite contract, reuses `shared.darkmode.toggle_title` — no new strings); **language/Tour/fullscreen stay one ✕ away by design** (a click away, not a dead end) — recorded in the ticked candidate so no future session re-derives the whole set. | verified: 22-check matrix EN/HE × 1280/800 — click toggles dark + both buttons sync, initDark syncs the drawer copy after reload, HE title localized, toggle-back clean, 0 pageerrors; full-drawer screenshots light+dark
- [x] 2026-08-20 | 01f650b | torah_trainer.html (`locales/ui-strings.csv`) | **The collapsed audio bar now hints at what it hides: a live chant-speed chip** — the S180 Pass-H audio slice (c), queued no-gate by the S234 pointer. The collapsed strip kept only the unlabeled ▾, ▶ and "Karaoke", so once a teacher collapsed the bar (a persisted choice) nothing said the speed sliders existed. The strip now carries a small live rate chip (e.g. `0.85×`) that expands the bar on click; `syncMiniRate()` is called from every rate writer **including the drawer slider and reset that can run while the bar is collapsed**. One new CSV key (`torah.audio.mini_rate_title`, EN+HE authored — the title/aria of a new control inside a queued no-gate fix, not a copy change). | verified: 36-check matrix EN/HE × 1280/800 — hidden expanded, visible collapsed with live value, follows drawer slider+reset while collapsed, click expands with `aria-expanded` sync, persisted-collapsed reload shows the saved rate, 0 pageerrors; bar screenshots light+dark EN+HE (the RTL `×0.85` digit reordering is byte-identical to how the existing `.val` readouts render in Hebrew — house-consistent, not a defect)
- [x] 2026-08-20 | (S235 Pass B — console & error audit, **20th run**, over the S224–S234 delta plus the PR #179/#180 outside-loop surface; no pass-sourced code) | **EVERY arm clean — and the run closed B's own S223 method gap: the load arm ran at 375px as well as 1280 for the first time.** 52 loads (13 pages × EN/HE × 1280+375): 0 pageerrors, 0 same-origin failures, 0 raw-key leakage, `dir` 52/52 (only noise: torah's documented environmental Sefaria route-abort ×4). 7/7 interactions clean (generator 38-blend sheet; flash-cards card screen; dictionary 5 results; dashboard drawer+dark; **the S233 torah retry card renders under interaction**; trope 7-choice drill; FM letter tile). **The never-swept `?start=` partner boot holds its designed offline fail-soft exactly as documented** — localized "couldn't be loaded — you may be offline" askModal with Retry / Continue-to-Font-Maker, 0 errors (the CDN happy path is untestable in-container: opentype.js is jsDelivr). Persistence delta spot-checks: torah loopPause through the S234 mirror ✓, trope drillScope (S233 micro-feature) ✓. **Probe discipline — FOUR probe defects killed before filing:** worksheet cells are `.blend-hebrew` (`.heb` is the picker tiles); flash-cards' card screen is `#screenCard`; a wizard-open assert on offline `?start=` reads false BECAUSE the fail-soft modal is the correct behavior; and a flex item blockifies `display:inline-block` → computed `block`, so visibility asserts must use `!== 'none'`/`checkVisibility()`, never display equality. | verified: three probe scripts over local HTTP, SW blocked, external origins aborted
- [x] 2026-08-20 | (maintainer-directed landing) | branch/deploy note | **PR #180 (OpenSiddur "Starting Fonts", the entry below) landed on main at the maintainer's in-session "push to main" direction** — main fast-forwarded 441e558 → 1bc400c (6 commits incl. the follow-up generated LINKS.md; ancestry checked before pushing, so no merge commit and no drift; GitHub auto-closed draft PR #180 as merged). **The "pages build and deployment" run 1132 for 1bc400c concluded `success`**, so the `?start=shlomo-semistam` / `?start=taamey-frank-clm` deep links, `starting-fonts/` + LINKS.md, FM v5.15, and SW v524 are live. (This container's proxy blocks both the Pyodide CDN and ivritsuite.com itself, so the PR's deferred live-site spot-checks — one real-browser TTF export per seed + `ttx -t name` — remain with the maintainer; the deploy receipt is the run conclusion, the house standard.) Drift guidance for the next session: origin/main moving 441e558 → 1bc400c (plus this ledger commit after it) is this session's own merged work, not an outside-loop landing — measure drift from **the post-landing main tip / sw.js v524 / FONT_MAKER_VERSION 5.15**, and cut any new branch fresh off origin/main rather than reusing the merged `claude/opensiddur-font-collab-zfmm0s`.

- [x] 2026-08-20 | 36379c6→1bc400c (plan-driven feature build, branch `claude/opensiddur-font-collab-zfmm0s`, landed on main via PR #180 — NOT an improveloop session) | Hebrew_Font_Maker.html, sw.js, starting-fonts/ (new), scripts/add_os_font.py (new), .claude/skills/addOSFont/ (new), CLAUDE.md | **OpenSiddur "Starting Fonts" collaboration (FM v5.15): `?start=<id>` deep links open a partner font pre-imported into a fresh, license-locked project.** Data layer: `starting-fonts/manifest.json` + per-font `<id>/{ttf, LICENSE.txt}`, populated ONLY by detection (`scripts/add_os_font.py` behind the `/addOSFont` skill; allowlist OFL-1.1/GPL+FE/Apache-2.0/CC0, bare-GPL and unknown refused with printed evidence; RFNs recorded for EVERY license — Taamey declares one under GPL — and any declared RFN forces a rename). Runtime: `init()` strips `?start` (keeps `?lang`); `osStartBoot` inside `maybeRestoreAutosave` after `_launchDecided` (deferral batons intact; the no-param tail is the verbatim extraction `_bootLaunchPrompt`); snapshot arbitration before any discard; every failure → Retry/Continue, never a dead end; `wizardOpen('osfont')` swaps in static `wizStepOs1–3` (gate/new byte-identical; chrome via the data-i18n attribute-swap; Esc/backdrop/Cancel → gate wizard; `wizardRearmTrap` mode branch); finish seeds `project.osFont` + `font.license='os-passthrough'` then headless `applyFontImport({outlines,marks,anchors:true})`. Lock (belt-and-braces): `licenseText()` dispatches on `osFont` BEFORE the license key → `osPassthroughText()` (upstream ©+RFN lines verbatim, Modified-work ©, provenance line, INFORMATIONAL IvritSuite line — never "must keep": OFL/GPL forbid added restrictions — GPL §2(a) change note, upstream text verbatim via `osFont.licenseText` with `oflBody()` slice + `osEnsureLicenseText()` fallbacks); independent partner branches in `buildFontSpec` AND `buildUfoFontInfo` (combined ©, "designer; modified by", NEW nameID 10 description, upstream license name/URL — fontTools `setupNameTable` maps `description`→ID 10 with zero Python edits, verified with system fontTools); `syncOsFontLock` (hidden locked `<option>`, read-only RFN field, full unlock else-branch); `onMetaChange` never reads the locked fields from the DOM; `migrateProject` re-asserts from `data.osFont` + heals orphaned `'os-passthrough'`→`'ofl'` (the L15666 falsy→ofl coercion would otherwise silently unlock); `saveProjectAs` keeps upstream RFNs + refuses colliding copy names (and its eviction pre-check uses the preserved RFN-derived name); blocking export RFN guard (no export-anyway) → `exportJumpToFontDetails()`. sw.js: `/starting-fonts/manifest.json` network-first (the cache-first catch-all + `ignoreSearch:true` would have pinned it and defeated `?v=` — measured, then fixed), so **an intake needs no VERSION bump**; SW v523→v524; FM 5.14→5.15 (one combined changelog entry); 39 new CSV keys EN+HE; CLAUDE.md gained the "Starting Fonts / partner onboarding" contract. Seeds (dogfooded through the skill): `shlomo-semistam` (OFL-1.1 — README-declared derivative of Ezra SIL SR with RFNs "SIL","Ezra","Shlomo"; stale embedded family recorded as `nameTableFamily`; LICENSE.txt transcribed verbatim from the distributor's own README license block) and `taamey-frank-clm` (GPL+FE, © Yoram Gnat, RFN 'Taamey Frank CLM'; staged LICENSE = folder declaration + the distribution's own GNU-GPL text). | verified: headless Playwright across ~20 boots, 0 pageerrors — plain visit byte-identical (gate wizard, 4 pickable licenses, zero starting-fonts requests); cold/dirty-autosave/unknown-id boot matrix; wizard walk-through incl. RFN inline block + Escape fallback; lock survives devtools tamper, `.hfm`-equivalent round-trip, recents, save-as; license-text identity for all 4 normal licenses (`licenseText()===cc0/ofl/mit/arrText()`); EN+HE×light/dark×1280/800 screenshots eyeballed (RTL sane); **fidelity line from the v5.14 entry run for real** (npm hb.wasm + the extracted `hfm_read_anchors` offline through the real fontTools, stubbed `loadPyodide`): Shlomo 913 combos — **705 non-residual @ max 0** (matches the Ezra lineage receipt), 208 residual by-design; Taamey 940 — 732 non-residual @ max 10 (**16 ayin below-mark combos at 9–10 units, every one listed in the user-facing fidelity report** — the surfacing behavior working as designed; noted against the ≤3 bar), 208 residual; SW test: disk-edited manifest served fresh with NO version bump, offline falls back to cached manifest+font; `check-i18n` clean (0 new; 14 pre-existing baselined). **Deferred to post-merge on the live site (container proxy blocks the Pyodide CDN):** one real-browser TTF export of each seed + `ttx -t name` spot-check (the name-table content is verified at the spec level and the fontTools mapping offline; only the end-to-end browser build run remains).

- [x] 2026-08-20 | (maintainer-directed landing) | branch/deploy note | **PR #179 (Imported-Font Mark Fidelity, the entry below) landed on main at the maintainer's in-session "push to main" direction** — main fast-forwarded f74cf85 → cb8438d (single commit; ancestry checked before pushing, so no merge commit and no drift; GitHub auto-closed draft PR #179 as merged). **The "pages build and deployment" run 1130 for cb8438d concluded `success`**, so FM v5.14, the 18 `fontmaker.fidelity.*` keys, and SW v523 are live. Drift guidance for the next session: origin/main moving f74cf85 → cb8438d is this session's own merged work, not an outside-loop landing — measure drift from **cb8438d / sw.js v523 / FONT_MAKER_VERSION 5.14**, and cut any new branch fresh off origin/main rather than reusing the merged `claude/imported-font-mark-fidelity-d73ah3`.

- [x] 2026-08-20 | cb8438d (plan-driven feature build, branch `claude/imported-font-mark-fidelity-d73ah3` — NOT an improveloop session) | Hebrew_Font_Maker.html | **Imported-Font Mark Fidelity (FM v5.14): imported marks attach at the source font's own GPOS MarkAnchor, and every import is MEASURED against the original bytes.** New `m.attachAnchor` read only by `markAttachOrigin()` (the single choke point — preview/QA/ghosts/export inherit it); `hfm_read_anchors` v2 walks mark/abvm/blwm-feature lookups only (Ezra SIL carries 20+ contextual-only variant lookups that skewed the old any-lookup walk), replaces first-wins class collapse with majority-wins per app key (**marks × Hebrew-letter bases** — the plan's raw mark-count metric mis-picked Ezra's 2-mark/1-base shin-dots class over holam's 1-mark/76-base class, and FRL's rafe class rides with Latin accents, so both refinements were forced by measurement), folds near-constant per-class deltas into MarkAnchor (stddev ≤ 3/axis) and reports non-constant ones as `notes[]` residuals. Post-import fidelity report: harfbuzz shapes every imported-mark × covered-letter pair against the ORIGINAL bytes; status line + details modal (per-key summary, residual list, >3-unit table, **Apply per-combo fixes** pinning flagged combos that map onto exportable non-center combined forms via the existing `pc.anchor` — hand-set overrides never touched); fail-soft when hb can't load. Lifecycle: re-trace (`finalizeFromRaw`), SVG upload, and editor-mode Edit-shape applies drop `attachAnchor`; upload-mode applies carry it; boldGen leaves it fixed (D-decision); deep-clone persistence keeps it in autosave/`.hfm` for free. Word-edge-halign trop columns are skipped-and-counted (deliberate app convention). CSV: 18 new `fontmaker.fidelity.*` keys + `import_anchors` copy + `changelog.v5_14` (EN+HE); CHANGELOG_ORDER/CUR_KEY advanced. CLAUDE.md gained the "Imported-font mark fidelity" subsection. SW v522→v523; FONT_MAKER_VERSION 5.13→5.14. **Known residual (by design, D2):** classes whose per-letter deltas genuinely vary (Ezra's sheva/hiriq/hataf x-tuning, rafe classes) keep raw MarkAnchor and are flagged with numbers — the one-anchor-per-key model can't express them, which is exactly what the report tells the teacher. **OpenSiddur Starting Fonts plan (not yet in-repo) must add this QA line:** *"[ ] `?start=` import shows the fidelity report; seed fonts' max deviation ≤ 3 units on non-flagged combos."* | verified: offline identity test (REAL extracted reader + system fontTools + harfbuzzjs 0.4.6): **Ezra SIL 705 non-residual combos max dev 0 · David Libre 406 @ max 1 · Frank Ruhl Libre 321 @ max 0**; in-browser e2e on the real page (Playwright, local opentype.js/hb.wasm in place of the CDN, Pyodide stubbed with the real reader's output): Ezra 913 combos measured, non-residual max 0, 17 residuals surfaced, modal/status/console verified; apply-fixes pinned exactly the flagged FB2A/FB2B in a perturbation arm, reproduced measured truth exactly, one undoable step, user-truth guard held; GPOS-less font byte-identical old behavior (bbox origin convention asserted); attachAnchor survives autosave round-trip + applyProjectData, drops on re-trace; EN/HE × light/dark × 1280/800 screenshots eyeballed; `check-i18n` clean; 0 pageerrors across all arms

- [x] 2026-08-20 | (S234 Pass F — cross-tool consistency, **20th run**; affordance: the failure-state model; no pass-sourced code) | **The FAILURE-STATE model across all 7 tools — re-derived per S196 and it agreed with the S233 pointer** (never F-swept; three written sources: the S214 corpus-failure convergence, CLAUDE.md's trope never-dead-end contract, FM's `ensurePyodide` retry-on-idle reset). **Census: the four S214 corpus registers share the model sentence VERBATIM; retry affordances uniform on 6 of 7 tools** (torah ↻ button + replayable audio, FM click-Export, generator Generate-refetches with wait/warn notices, trope stated-degrade + never-dead-end drills, dashboard interval/interaction self-heal, translit degrade ×4 uniform, forms carry the email alternative). **ONE live divergence receipted then fixed in-session** (see 95ceddc): flash-cards Start pushed the "still loading — try again in a second" keys on GENUINE failure with no retry ever fired — fetch count pinned at 1 across taps, happy-path control arm green. **Two candidate findings refuted at grounding** (stale-marker class, both before filing): the `worksheet.validation.pdf_failed` "alert" was CSV-context drift — the live `exportPDF` catch already uses `showPreviewNotice(warn, dismissible)`; the 275(c) legend-glyph candidate was shipped in a2fe1ca ("the trope legend explains itself"). | verified: static census over every `fail/unavailable/retry` register + live probes (blocked-corpus walks with request counters, control arm on the happy path), 0 pageerrors

- [x] 2026-08-20 | 95ceddc | flash_cards.html | **Start retries a failed corpus load instead of dead-ending on "still loading."** The S234 Pass-F divergence: a failed `hebrew_words`/`hebrew_emojis` fetch left Start claiming "still loading — try again in a second" forever (its own `fetchEmojiData` comment names Start as the intended retrier; the correct failed-copy keys already existed unused). Start now re-fetches once per tap — never stacking a mid-flight download, never looping offline — shows the real failed-load copy, and auto-starts the deck when a retry lands (the emoji wait's `.then` no longer blind-restarts into the lie; the words branch gained the same shape). Reuses 4 existing keys; zero new copy. | verified: 5-arm matrix EN/HE × light/dark × 1280/800 (down → honest copy; recover → ONE tap starts the deck) + a slow-network arm (mid-download tap: true "still loading", fetch count stays 1, deck starts after landing), 0 pageerrors

- [x] 2026-08-20 | 2ad18dd | torah_trainer.html | **The loop's own pause control joins it in the audio bar** — the S180 Pass-H audio-ergonomics slice (a): "Pause between repeats" only affects the verse loop yet lived gear → scroll → expand away from it, while the bar already mirrored both speed sliders. A loop-scoped mirror of the drawer select now shows beside ⏹ Stop loop only while a loop runs, two-way synced via the applyTtsRate idiom (a mid-loop change applies to the next repeat — the loop reads the value live at each verse end); hidden with the collapsed bar; existing keys only. | verified: EN/HE × light/dark × 1280/800 — boot seeding from settings, visibility with the real loop chokepoint, both mirror directions, persistence past the 300ms saveSettings debounce, collapse/expand/stop transitions; bar screenshots eyeballed in both themes; 0 pageerrors

- [x] 2026-08-20 | (S234 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-8viczu` → the SAME draft PR #178** — checked at session start per the pointer: open, draft, mergeable_state clean at head ce1245b, so no new branch and no new PR. **Drift check: `origin/main` sat at `b95e6c7` all session** (identical to the S233 close-out record — zero outside-loop landings); `sw.js` v521 confirmed on disk at start. **SW v521→v522** (precached files changed: `flash_cards.html`, `torah_trainer.html`). **FONT_MAKER_VERSION 5.13 unchanged — correct: no Font Maker edits at all this session.** Repo scripts: `check-i18n` clean (no NEW Check-A violations; no CSV keys added so no locale rebuild), `update-sitemap.mjs` run last (clone already unshallowed since S233). **Gates: 0 asked (unattended run) — deferred to the next attended session: gate 1 micro-feature (skipped, bench unchanged); gate 2 ×3: the FM 17 glyph-name keys (standing), the NEW reload-only-corpus retry-model convergence, and the NEW dictionary font-attr-span shape (drop the pointer-only shortcut vs restructure attributions).** Deploy: Pages runs only on merge to `main`; verify the "pages build and deployment" run concludes `success` after the maintainer merges PR #178.

- [x] 2026-08-20 | (S233 Pass I — first-load & empty-state, **19th run**, over the S222–S232 delta no I had seen; no pass-sourced code) | **Mechanical gates clean for the SEVENTH consecutive I — and this run added a phone-width gate no I ever had: 13 pages × EN/HE at 1280 AND again at 375, all four sweeps 26/26** (0 pageerrors, 0 same-origin failures, 0 raw keys, correct `lang`/`dir`, and at 375 **docWidth ≤ 375 on every page** — the reflow campaign holds on fresh profiles, and the S223 "width-gated ReferenceErrors survive desktop-only loads" class stays dead at phone width). **Nine targeted fresh-profile walks, ALL CLEAN**: the trope drill-scope select defaults `all` with 7 translated options in EN+HE; a **full 10-question drill under total audio failure completes** with skip toasts and the S228 missed-review rendering 5 correct rows (the never-dead-end contract, receipted fresh); the 3fc6bda folder-tree empty strings render the right noun-agreeing sentence on all four foldered lists EN+HE (dashboard presets are DEFAULT-seeded so its empty state never shows — by design); Font Maker at 375 clean-profile with the wizard gate up: docW=375 both languages; dashboard fresh shows the 3-starter first-run card and the week editor opens to an instructive empty state EN+HE ("create a couple in Presets first"); generator fresh Generate → 1 sheet; flash cards fresh start → card screen; dictionary fresh search → 4 cards; index fresh backup: the Automatic path downloads `August_20_2026_AllTools.ivrit` with the status announced (the manual Export fills the textarea — by design, a probe mislabel killed by grounding). The torah fresh failed-load walk **re-grounded the open S180 P3** (contradictory pick-a-parsha card + no retry) → fixed this session as 29ccba2. **Two probe-method lessons for the next I/D**: answering a drill question renders `#qNextBtn` INSIDE `#qFeedback` — a choices-only click loop stalls at the feedback screen forever (the S228 kill #7, re-learned by this session's own first probe); and the AllTools backup has TWO delivery paths — `exportAllSettings()` fills the manual textarea, `ivritSaveFile()` downloads — so a download assertion must target the auto path. | verified: 4 Playwright harnesses over local HTTP, fresh contexts (empty localStorage AND IndexedDB), external origins aborted

- [x] 2026-08-20 | 29ccba2 | torah_trainer.html (`locales/ui-strings.csv`) | **A failed reading load names the failure and offers ↻ Try again** — the S180 Pass-H P3 the S233 pointer queued no-gate, re-grounded fresh by this session's Pass I. The failure path rendered the pick-a-parsha card (instructing the teacher to do the thing they just did) under the red status, with no retry; and since **init auto-loads this week's parsha** (calendars fetch → Bereshit fallback), an offline fresh visit hit that contradiction from second zero. `renderEmptyReading` gains a `failed` variant — failure title/body + a `.btn` Try-again that re-runs `fetchAndRender()` on the unchanged selection — and each text element stamps its key as `dataset.i18nKey` (the `#ttStatus` idiom) with the applyI18n repair block resolving from the stamp, so a language switch keeps the on-screen variant. 3 new CSV keys, locales rebuilt (4385, 1 allowlisted debt). | verified: failure card + visible retry in EN-light-1280 / EN-dark-800 / HE-light-1280; retry while still down keeps the card, no crash; a **mocked Sefaria recovery renders the reading on retry in all three arms** (12 verse elements, status cleared); EN→HE with the card up re-localizes the failure copy without reverting to pick-a-parsha; 0 pageerrors everywhere

- [x] 2026-08-20 | 53efe64 | Hebrew_Font_Maker.html | **Composed-form names localize through the glyph-name table** — the S207 Pass-K P4, taken as the census it demanded plus the wiring it unblocks. Census first: **12 lookup slugs already translated** (dagesh, shin_dot/sin_dot, all 8 wide_*, shin), 17 missing. `precompName` now returns through `gName` (English unchanged on a missing key), so tiles/side-titles/toasts/QA labels localize exactly where translations exist; a name carrying an inline Hebrew sample — Ladino "Shin (שׁ)" — translates its prefix and re-appends the sample, so its slug cannot collide into the bare letter key `shin` and silently drop the sample from the ENGLISH label (the trap the naive wrap would have shipped). The two `pc.name || precompName(pc)` display bypasses collapse (pc.name is precompName's first branch) and the one pre-wrapped `gName(precompName())` tile unwraps. Consumers censused display-only before touching anything — export glyph naming never goes through precompName. The 17 missing keys (Yiddish/Ladino set + sin/holam_vav/shuruk/mark) need authored Hebrew → logged as the gate-2 candidate above. **No `FONT_MAKER_VERSION` bump — localization wiring, not a feature.** | verified: EN byte-identical on 9 representative names AND the rendered Specialized tiles; HE renders דגש / אלף רחבה-family tiles / שין (שׁ) sample kept; missing-key names unchanged; 0 pageerrors both languages

- [x] 2026-08-20 | (S233 close-out) | branch/deploy note | **Branch/PR: FRESH branch `claude/improve-loop-8viczu` cut from latest `origin/main` (b95e6c7), exactly as the S233 pointer directed** — PR #177 had merged via the recorded 2026-08-20 direct-to-main landing (`0a1c5e8` → `822b2c1`), so no branch continuation; a NEW draft PR opened at push. **Drift: NONE beyond the recorded landing** — diffed `origin/main` against the S232 close-out's records (the standing lesson): only 822b2c1 + the b95e6c7 ledger note; sw v520, FM 5.13 both as recorded. **Iterations: 3 of 5** — pass + 2 fixes; stopped honestly: every remaining open candidate is gate-2, torah-capped (the cluster rule's one slice was taken), stale (4 ticked this session with receipts: the S227 FM-320px originating line closed by S230's 6eb47bb+e01ce03; the S177 FM panels-key census fully discharged in the live file via 1d6c0c1; plus the two S233 fixes' own lines), or has no clean small fix (the font-attr nested-interactive shape). `sw.js` **v520 → v521**; precached files changed: torah_trainer.html, Hebrew_Font_Maker.html, locales/en.json, locales/he.json. **FONT_MAKER_VERSION NOT bumped and correctly so** — a localization wiring fix, not a feature. Scripts run: build-locales (3 keys, 4385), check-i18n (**clean**, 14 baselined), update-sitemap (last, after `git fetch --unshallow` — the clone was shallow and the script correctly refuses those). **Decisions deferred to maintainer (unattended run — no gates could be asked): gate 1: micro-feature SKIPPED this session (bench unchanged: torah ×4, dashboard ×4, FM ×3); gate 2: the NEW 17-key FM glyph-name Hebrew authoring candidate (top of Candidates); all previously-deferred gate-2 items unchanged.** Deploy runs on merge to `main`; Pages `success` verified after merge.

- [x] 2026-08-15 | (S217 close-out) | branch/deploy note | **The session was INTERRUPTED mid-flight and closed out late, on the maintainer's prompt** — the plain-language recap the protocol requires as the last thing printed was asked for by name ("Where is the simple term summary?") before this ledger write-up existed, so the recap was delivered first and these entries were written after it. Recorded because the *order* is the lesson: an interrupted loop leaves its memory unwritten, and everything below existed only in a conversation that a container reclaim would have destroyed. Branch: PR #170 was merged at the end of S216, so S217 cut a fresh branch **`claude/seo-improveloop-review-d25rtt`** off `origin/main` and opened **draft PR #172** exactly as the S216 pointer directed. `sw.js` **v503 → v504**, one bump covering all three commits under the once-per-session rule. **Budget: 3 of 5 iterations used** (1 pass + 2 fixes) — the interrupt cost the remaining 2, and **gate 1 (micro-feature) was reached but never asked**, so the Feature-seeds section still has no fresh top pick. A check-in that fired mid-interrupt found **two committed-but-unpushed commits** sitting in an ephemeral container and pushed them (`5621af8..30bd836`); PR #172's title and body still describe only the twitter tags and do not mention the two torah fixes the branch now also carries.

- [x] 2026-08-15 | (S217 Pass H — teacher walkthrough / paper-cuts, `torah_trainer.html`) | its **first dedicated H since S77 (~140 sessions, the stalest H target by a wide margin)** and the first walk over everything shipped since: trope colour coding, the S207 print band, the panel-collapse memory, the corpus-failure register, the karaoke layer | **The tool walked cleaner than the walk expected, and the pass's real output was four kills.** A full lesson prep — this week's parsha → aliyah → translation on → nikkud + trope colouring on → handout → print → copy → share — took **9 clicks end to end with 0 pageerrors in every context** (1280 light, 800 HE/dark, 390 phone, print media). Verified working: the colour legend, the geniza note, the print band re-stamping on every sheet, the Copy panel, share links. **Four would-be findings were KILLED by grounding, every one a probe defect rather than a product defect** — (1) "jump-to-this-week is silent on failure" (probe read `#ttReading`; `setStatus` writes a red in-viewport message to `#ttStatus` naming the exact cause); (2) "transliteration fails silently" (probe read `#ttStatus` and toasts; the real handling is `_translitDead` + a `.tt-notice-chip` in `.tt-ref-hdr` reading "No transliteration" with a tooltip naming both causes, and the column is **suppressed rather than printed empty**); (3) "translation is broken" (two compounding probe errors — the stub's version entries lacked `license`, so `isPermissiveLicense` **correctly** rejected them, which is the app's deliberate copyright safeguard, and the query used `.tt-en` where the real class is `.tt-translation`; after fixing both: 5 verses, correct English, correct licensed footer credit); (4) "speak buttons print on the student handout" (the `shown()` helper checked only the element's own computed display, and `display:none` on `.tt-verse-buttons` does not make children compute to `none` — re-checked with `el.checkVisibility()`, print is fully clean: 0 speak / 0 bulk / 0 controls / 0 header, 52 words + the print band). A fifth, "the handout prints dark backgrounds", died on `colorScheme: 'light'` — the probe context had inherited dark mode. **Real intake: 2 fixes shipped (below) + 1 P4 + 1 unresolved lead deliberately NOT filed as a defect.**

- [x] 2026-08-15 | bf14438 | torah_trainer.html | **The aliyah jump chips were the smallest touch target on the page a student actually uses.** `.tt-jump-chip` measured **26×22** — under WCAG 2.5.8's 24px floor on one axis and well under the suite's ratified **30px** bar. WCAG 2.5.8's inline exception does not cover them: they sit in their own nav row, not inside a sentence, which is exactly the distinction the S213 `.tip-icon` entry drew when it *did* claim the exemption — so this one takes the floor. Shipped `min-height:30px; min-width:30px; display:inline-flex; align-items:center; justify-content:center` with the existing padding kept, so the chips grow without the row re-flowing. **24/24 assertions post-fix; the stashed control reproduced 16/24** — the 8 failures are precisely the size assertions.

- [x] 2026-08-15 | 30bd836 | torah_trainer.html | **Picking a parsha made the whole page slide sideways on a phone — and this CLOSES ONE OF THE TWO CAUSES the standing S213 reflow candidate demanded be attributed before anything changed.** `.tt-ctrl` is a flex item, and a flex item's default `min-width:auto` refuses to shrink below its content, so `select#parshaSelect` — sized by its longest option — pushed `scrollWidth` past the viewport at 320/390px: two-dimensional scrolling, WCAG 1.4.10 (AA). Shipped `min-width:0` on `.tt-ctrl` plus `min-width:110px; max-width:100%` on `.tt-ctrl select`, so the select still reads as a select at any width but can no longer set the page's width. **29/29 across 320/390/800/1280 × light/dark × empty/loaded.** The verification assertion itself needed a fix mid-run — `sw <= 451` fails legitimately at 800/1280 where `scrollWidth === clientWidth`, corrected to `sw <= max(clientWidth, 451)`. The **second** cause, `div.hright`, is cross-tool and stays queued (see the upgraded candidate).

- [x] 2026-08-15 | (S216 close-out) | branch/deploy note | `/improveloop` → **PR #170 still open + unmerged** (`state: open, draft: true, merged: false, mergeable_state: clean`), so per protocol S216 **continued on `claude/improve-loop-k6dwi7` / PR #170** — no branch restart, no new PR; that PR now carries S210–S216 plus the user-directed panel-collapse work. **Drift check: NOTHING landed outside the loop** — `origin/main` unmoved at `e435c04` (the merge-base), `sw.js` v502 and `FONT_MAKER_VERSION` 5.12 exactly where the S215 close-out left them, so the whole 81-commit delta Pass L had to cover was loop work. **`sw.js` v502 → v503** (precached files changed: `torah_trainer.html`, `classroom_dashboard.html`, `privacy.html`, `terms.html`, `locales/en.json`, `locales/he.json`). **FONT_MAKER_VERSION NOT bumped** — the Font Maker was not touched at all this session. Scripts run: `check-i18n.js` (clean, no NEW violations — 14 baselined Check-A findings unchanged), `build-locales.js` (4,349 → **4,354** keys ×2 after 6 new rows: 3 for the dashboard print control, 3 for the torah FAQ twin), `update-sitemap.mjs` (last step; non-shallow clone confirmed — `git rev-parse --is-shallow-repository` = false). **Gates: 4 asked, 4 answered, 0 deferred** — the first session in the loop's recent history where the micro-feature seed was BUILT rather than skipped. **Decision carried to S217 with its answer already given:** the twitter:title/description parity fix for `contact`/`privacy`/`terms` was gate-approved but did not fit the budget (the micro-feature costs 2), so S217 should ship it without re-asking. **⚑ SUPERSEDED SAME SESSION — the maintainer answered "Push to main" after the close-out was written.** `origin/main` was fast-forwarded from `e435c04` to `562882d` (36 commits, 0 behind, `merge-base --is-ancestor` confirmed before pushing — no merge commit, no rewrite), which auto-closed **PR #170 as MERGED** and carried S210–S216 plus the panel-collapse work onto `main` in one landing. **Pages deploy run 31887568256 for `562882d` concluded `success`** (13:34:25Z → 13:35:05Z), so unlike every prior loop close-out this one has its deploy verified **in-session**. This is the second ledgered direct-to-main authorization (precedent: S78; and the S201–S203 landing recorded at `57a4541`). **S217 therefore starts a FRESH branch off `origin/main` at `562882d` and opens a NEW draft PR** — `claude/improve-loop-k6dwi7` and PR #170 are finished and must not be reused.

- [x] 2026-08-15 | (S216 Pass L — SEO & discoverability audit, **7th run**, over `98eb346..HEAD` = **81 commits / ~2,000 insertions / 14 files** since L's 6th run, including `terms.html`, a page L had never seen) | 5 arms, static-source-first per the pass definition | **Arm 1 — byte check first, widened the three S201 ways** (head SEO surface *extracted and diffed* per page, not marker-grepped; the VISIBLE twins; the CSV faq/seo/meta rows): **11 of 14 pages byte-identical**, so S201's verification carries forward whole. The 3 that moved were `hebrew_dictionary`+`index` (the S202 theme-copy landing L itself approved, already verified) and `terms.html` (new). **This arm's first cut printed a confident "0 pages changed of 0"** — `git ls-tree -- '*.html'` matches nothing at root — caught only because the denominator was printed. **Arm 2 — mechanical gates at HEAD, 95/99:** canonicals **12/12** (+ `404.html` `noindex, follow` and `i18n-test.html` `noindex` correctly still excluded), **36/36 ld+json blocks parse**, sitemap covers exactly the 12 indexable pages with exact canonical agreement, robots.txt names the sitemap and disallows nothing indexable, crawl graph **12/12 reachable from `/` by static `<a href>`**, exactly one `<h1>` on **14/14** pages, **24/24** og/twitter images resolve on disk, titles and descriptions all unique. **Arm 3 — forward claims-vs-UI returned a VACUOUS clean and was reported as such**: 83 FAQ/HowTo claims checked, **0 of them decidable** (these bodies are prose with no quoted control names), so the "0 unreferenced" was worth nothing until the denominator said so. **Arm 4/5 — the inverse direction, which is the shape S201's headline actually had, on the corrected corpus.** A first parity table said torah_trainer claimed both print and copy; a second said it claimed neither; the contradiction was the proof that the **`<head>` slice is the wrong corpus** — torah's is 88,641 chars because the entire `<style>` block lives there, scoring 62 `print` hits that are CSS comments and a `media="print"` font preload, and 7 `copy` hits that are `#ttCopyBulkPanel` selectors. On the right corpus (meta `content=` attributes + ld+json bodies): **9 ships-but-never-claims gaps across 12 pages × 2 capabilities**, of which one is substantive and became the session's headline. **Findings: 1 real hit (fixed, twice — see below), 2 gate-2 copy items (both approved and 1 shipped), 1 informational.**

- [x] 2026-08-15 | be6afe3 | torah_trainer.html (+ 3 CSV rows, rebuilt en/he.json) | **The visible FAQ grows the same claim its JSON-LD just did — completing `6fdf370`, which was half a fix.** The ledger's own parity rule (registered S190, sharpened S201) requires a structured-data claim and its VISIBLE on-page twin to move together in one commit, "or the fix creates a new mismatch"; torah_trainer carries a 7-question `<details>` FAQ mirroring its 7 JSON-LD Questions, so growing only the JSON-LD side to 8 **reopened the very pattern the previous commit closed**. Answer 1 gains the same closing sentence and a new q8/a8 pair carries the approved Copy-panel copy, wording taken from the JSON-LD so both sides assert the same thing. | **verified 13/13** over the served page in EN and HE: visible FAQ renders 8 entries against 8 JSON-LD Questions, the new twin resolves in both languages with no raw keys, 0 pageerrors.

- [x] 2026-08-15 | 2686af2 | classroom_dashboard.html (+ 3 CSV rows) | **MICRO-FEATURE — the weekly grid can finally reach paper** (gate-1 approved on the seed's **fifth** offer, after four skips; the ledger had recommended offering it once more with a strike option, and the maintainer chose *build* instead). The reference artifact a teacher wants is the week on the desk, and it was the one thing the dashboard could not print: the grid is drawn by a `position:fixed` editor that the S208 print treatment deliberately hides, so Ctrl+P in the editor printed the board behind it. A **Print week** button wears `body.swm-printing` for one print job and swaps which thing IS the page — board and chrome off, grid and a title line on, class colours opted back in with `print-color-adjust` (the flash-cards card-sheet precedent, S162). **Three defects the ARTIFACT caught that the style assertions could not**, all fixed here: (1) the row-delete × is a **grid ITEM**, so `display:none` dropped a cell from the flow and every following row slid one track left — the printed week arrived scrambled, times mid-row and classes under the wrong days; its track is collapsed to zero and it is hidden in place instead; (2) `body` is `min-height:100vh; display:flex`, and on paper **100vh IS one sheet**, so every print was two pages with a blank second; (3) the `afterprint` fallback timer was 4 s, short enough to undress the sheet mid-render where `print()` does not block — now 15 s, and the class is inert on screen so holding it longer costs nothing. **No state added** (no new localStorage key, nothing print-shaped in the settings blob), so no AllTools or `.ivrit` wiring is owed. 89 lines in one tool + 3 keys — inside the ~150 fence. | **verified 105/105** across EN/HE × light/dark × 1280/800 **plus a fresh profile** (empty localStorage AND IndexedDB): controls first reproduce the seed's premise (printing without the button still hides the editor), column and row alignment are asserted **geometrically**, and real PDFs are produced on **Letter AND A4** — each **one page**, each inking the fixture's three class colours. A control re-introducing the old `display:none` rule fails the alignment assertions in all four cells.

- [x] 2026-08-15 | 7fc8fa2 | privacy.html + terms.html | **The two short search snippets join the suite's band** (gate-2 approved). Ten of twelve indexable pages sit at **136–164** chars (median 153); privacy at **92** and terms at **99** were the only two under 110, leaving roughly a third of the snippet unused. Both rewrites describe only what the pages already contain — privacy's new sentence maps to its own §5 (Local Storage & Your Control Over Data), §2/§7 (no collection of education records or student PII) and §7 (FERPA); terms' maps to §§4, 5, 6, 7, 10. `og:description` takes the same text on both, which also closes a smaller divergence (each was carrying a third, shorter variant). **The titles were deliberately NOT touched** — 25 and 27 chars against a median of 51 looks like a miss under the pass definition's "~50–60", but the distribution says short brand/legal titles are the suite's own convention, so that arm was a checker-threshold artifact, not a defect. | **verified 12/12**: served-byte lengths 151 and 145, `og:description` exactly equal to `description`, both pages 0 pageerrors in EN/light/1280 and HE/dark/800 with the description correctly NOT following the UI language. Suite re-run: description band now **136–164 with nothing under 110**.

- [x] 2026-08-15 | 6fdf370 | torah_trainer.html | **The structured data admits the tool can copy and print** — Pass L's headline, and the same shape as the S201 dictionary theme filter. The page ships a full **Copy panel** (Options drawer: rich-text colour-preserving copy, bulk select, type-a-verse-range, per-part include toggles — 26 authored `torah.copy.*` keys) and designed printed sheets with a per-sheet reference + clause-key band, and its entire crawler-facing copy said **nothing** about either: measured on the right corpus (meta `content=` + ld+json bodies), the counts were **copy 0, clipboard 0, print 0, paste 0** across 818 chars of meta and 5,121 of ld+json — the largest ld+json on the site. Three **closed enumerations** were doing the omitting: `WebApplication.description`, the FAQ's "What is the Torah Trainer?" answer, and a five-step HowTo ending at "Project fullscreen" with no take-it-away step. Suite parity made it a divergence rather than a wish: the generator and trope tutor both claim print, the dictionary claims copy. Gate-2 approved at full scope; every noun in the new copy is grounded in a shipped control; meta/og/twitter descriptions deliberately untouched as outside the approval. | **verified 21/21**: 4 ld+json blocks parse from the served bytes, FAQ 7→8, HowTo 5→6 with positions 1..6 intact, and **all 12 claimed controls present live** in EN/HE × light/dark × 1280/800, 0 pageerrors. Control with one deliberately unshipped claim (`#ttExportToWordDoc`) failed in all four cells. **Incomplete as shipped — see `be6afe3`.**

- [x] 2026-08-15 | (S215 close-out) | branch/deploy note | `/improveloop` → **PR #170 still open + unmerged** (`state: open, draft: true, merged: false, mergeable_state: clean`, 0 reviews; both existing comments are the loop's own S213/S214 summaries), so per protocol S215 **continued on `claude/improve-loop-k6dwi7` and the same PR** — no branch restart, no new PR; the PR now carries S210–S215. **Drift check: CLEAN** — `origin/main` still at e435c04 and a strict ancestor of HEAD (nothing landed outside the loop since S214), `main`'s `sw.js` still v495 while the branch runs ahead, `FONT_MAKER_VERSION` unchanged at 5.12 on both. **`sw.js` v501→v502** in this commit — precached files changed: `Hebrew_Font_Maker.html` (the `.wiz-dot.active` token) and `locales/{en,he}.json` (the two rebuilt emoji rows); `CLAUDE.md` and `THIRD_PARTY_LICENSES.md` are not precached and do not themselves require the bump. **No `FONT_MAKER_VERSION` bump** — the FM change is a one-property colour-token swap, not a feature, which is the documented no-bump case. Scripts run: `build-locales.js` (4,349 keys both languages, 0 new keys, the same lone `worksheet.layout.title_default` untranslated), `check-i18n.js` (**Check A clean — no NEW violations**; 14 baselined; Check B clean), `update-sitemap.mjs` as the last step. **Gate 1 asked and ANSWERED: SKIP** — the dashboard weekly-grid print seed was offered a **fourth** time and the maintainer chose to spend all four slots on fixes; the seed stays queued, still never rejected. **Gate 2: none asked** — the four fixes are one already-approved wording model applied to two more rows, one missing factual attribution record, one documentation correction and one colour token. **One judgement call flagged:** the YouTube licence entry adds public-facing prose to a doc `terms.html` links to, and was shipped **ungated as conformance** (it supplies a missing attribution record rather than rewording anything the site already says) — the same call S214 made for reduced motion; say so if that boundary should move. **Deploy verification happens after the maintainer merges** — Pages builds on push to `main`, so nothing is live from this branch yet.

- [x] 2026-08-15 | (S215 Pass E — freshness & site health, **19th run**, over `b6ddebc..HEAD` = **83 commits / 2,545 insertions / 20 files** since S200, including a page E had never seen) | 10 dimensions, each printing its own denominator, then every hit ground before being called a finding | **9 CLEAN, 1 REAL HIT — and the headline is that the new page was already wired correctly everywhere it mattered.** `terms.html` landed out-of-band between E runs, and a new page is exactly what a precache list, a sitemap and a link graph forget: it passed **D1** (sitemap lastmod 12/12 exact against `git log -1` per file, on a verified non-shallow clone), **D3** (SW precache both directions — 26 entries, 0 listed-but-absent, 0 pages unlisted, `i18n-test.html` still correctly absent, locales 2/2), **D2** (421 static internal refs, 0 broken) and **D7** (18-tag apple-startup block byte-identical across 13/13 pages, 18/18 splash mirror, 0 orphan and 0 dangling PNGs). Also clean: **D8** (8 `?v=` cache-busted `data/` fetches, none stale against their file's last data commit), **D10** (**432/432** PocketTorah timing labels resolve case-exactly, 0 unreferenced on disk — Pages is case-sensitive, so one wrong case is a dead karaoke aliyah; identical to S200's count), **D5** (**all seven** `═══`-marked shared blocks single-sha per block: ivritsuite-fonts 9, uploader 7, app-toast JS 4, app-toast CSS 4, folder-tree JS 4, folder-tree CSS 3, panel-collapse 6, and the `.ivrit` engine's documented 3-identical-plus-adapted-index shape confirmed rather than assumed), **D6** and **D9**. **The hit is D4:** `THIRD_PARTY_LICENSES.md` had no YouTube entry — fixed this session (bc6d305). **Four of the ten dimensions first reported a hit and three of those four were CHECKER DEFECTS**, each ground to a distinct named cause and fixed in-probe rather than logged: D2's `(?:href|src)=` also matched a JS `src = 'imported'` assignment (the S200-logged script-region FP class, in a new shape); D4's host axis compared CSP hostnames against a file that records services by product name; D10 assumed `<base>-<n>.json` beside the manifest when the timings are `.txt` label files under `timings/` named by the manifest's own `labels` map (the S200 "wrong naming assumption" shape, recurring). **D5's was the instructive one: four of the seven shared blocks open with a MULTI-LINE comment**, so a same-line `*/` anchor matched nothing and the probe reported "0 carriers, single-sha" — a *vacuous* clean that would have banked four free passes. That is precisely the S214 blind-zero trap, and it is worth recording that the trap bites checkers as readily as it bites pages.

- [x] 2026-08-15 | c1642c2 | Hebrew_Font_Maker.html | **The new-font wizard's active step number becomes readable** (the S202 Pass-C P4, shipped exactly as that entry prescribed — "navy text on the gold dot, the reverse of `.wiz-dot.done`'s white-on-navy"). `.wiz-dot.active` painted `#fff` on `--gold`: **2.75:1 light / 2.14:1 dark**, and at 11.5px bold the label is not WCAG "large text", so the bar is 4.5:1 — it missed in both themes, and because the dot never inverts there was no theme in which it read better. One property: `color: var(--navy)` → **5.38:1 light / 6.92:1 dark**, one value serving both themes because `--navy` is among the few tokens the dark block does not override. Verified on the live wizard across **8 cells** (EN/HE × light/dark × 1280/800), walking all three steps so every dot is measured in each of its three states and reading the colours the browser actually paints rather than the tokens: **32/32**, worst active 5.38/6.92, done and idle dots unchanged at 4.69/5.02, 0 pageerrors. **Control with the fix stashed: 24/32**, the 8 failures being exactly the active-dot assertion at 2.75/2.14 — reproducing the S202 measurement live, so the clean is a clean that could have failed. No `FONT_MAKER_VERSION` bump: a token swap, not a feature.

- [x] 2026-08-15 | ee3a93d | CLAUDE.md | **The reduced-motion section learns `terms.html`, the two block shapes, and how to verify** (Pass E's documentation-accuracy dimension). Three staleness items, all things a future change would trip over. (1) The exemption note named only `privacy.html` as the zero-animation page; `terms.html` is the second, and both are now marked as what they are — a **blind** zero that proves nothing, not a clean one. (2) The suite now has **two** block shapes and the doc described neither: 9 of the 11 carriers open with the universal neutralizer (adopted on the four biggest tools at S214) and `404.html` + `trope_tutor.html` still enumerate; recorded which to prefer and why (at (0,0,0) the universal rule loses to every class selector, so static cues still win, and it cannot silently miss a later declaration), plus why the duration is `0.001ms` and never `0`. (3) The rule said *carry* a block; it now says **measure** — S214 found 1,286 elements still animating on four pages that all carried one, so grepping for the block is not verification, and the reduce-off control is required for the same reason. Every figure was measured this session, not recalled: 11 carriers, 9 universal / 2 enumerated, privacy + terms at zero motion. **The two enumerated pages were measured live and are complete today** — 0 animating under `reduce`, controls at 2 and 67 proving the probe fires — so they are recorded as correct, not as pending work. That measurement **killed a candidate before it was logged**: the static scan's "these two lack the universal rule" reads like a defect and is not one.

- [x] 2026-08-15 | bc6d305 | THIRD_PARTY_LICENSES.md | **YouTube was the one embedded third-party service with no licence entry** (Pass E's D4, the session's only real hit, and a gap the new page is what exposed). `terms.html` publicly enumerates **eight** third-party services and links to `THIRD_PARTY_LICENSES.md` as the detailed record; seven had an entry, and `git log -S YouTube` shows YouTube never has, going back to the file's creation. It is a real runtime dependency, not a theoretical one: `classroom_dashboard.html` allowlists `www.youtube.com` + `www.youtube-nocookie.com` in its CSP `frame-src`, and `applyVideoEmbed` points the video panel's iframe at `https://www.youtube.com/embed/<id>` the moment a teacher pastes a URL. Adds a `### YouTube` entry in the same shape as its five siblings (service / terms / "Used by" note), placed with the other dashboard services and before the form services, recording the ToS + API Services Terms + Google privacy policy, that nothing loads until a teacher supplies a URL, and that CSP allows the `-nocookie` origin while the shipped code emits the standard one. Verified both axes: the terms cross-check goes **7/8 → 8/8** and the CSP-host axis **2 unlicensed of 21 → 0**. Documentation only — no page, no precached file, no behaviour change; shipped ungated as conformance (flagged in the close-out).

- [x] 2026-08-15 | 03b5bec | locales/ui-strings.csv + locales/{en,he}.json (hebrew_dictionary + flash_cards surfaces) | **The last two corpus-load failures join the approved register** (the S214 Pass-A P3, top of the candidate list, and explicitly ungated by the S214 pointer because it applies an already-approved model rather than deciding anything). S214 converged the four **word**-corpus messages; the two **emoji**-corpus messages sat one step outside that sweep and kept the terse `"Failed to load emoji data."` — which tells a teacher on a filtered school network nothing they can act on. Both now say what their four siblings say: *"Couldn't load emoji. Check your internet connection, or ask your school's IT to allow ivritsuite.com — then reload the page."* **Two CSV rows, no code change** — both call sites already report through their render chokepoint (`renderEmojiCatTree` / `syncOtherSubUI`), which is what makes the notice survive a re-render and a language switch. HE recombines the already-reviewed `dictionary.loading.failed` sentence with the emoji noun; both HE cells REVIEW-marked. 4,349 keys before and after. Verified with `data/hebrew_emojis.json` answered **502** (not aborted — S183) and `serviceWorkers:'block'` (S165: otherwise the SW serves the corpus from `DATA_CACHE` and no failure path runs at all): **36/36** across both tools × EN/HE/HE-dark. Layout measured at its real rendered box after locating the two ancestors that hide it (`emojiOpts`, `otherModeSection`) — 698×45, no overflow at 1280 or 800 — rather than reporting the 0×0 a force-unhide first gave.

- [x] 2026-08-13 | (S204 close-out) | branch/deploy note | `/improveloop all fixes` → S204 ran on the designated branch **`claude/improveloop-all-fixes-jdfkm4`**, the protocol's *merged* case: the S201–S203 branch's PR #166 landed direct-to-main (recorded + deploy-verified in 57a4541), no open loop PR existed, and the designated branch sat exactly at `origin/main` (57a4541) — **a new draft PR opens at this close-out**. **Drift check:** origin/main moved 8d61e8f→57a4541 entirely via the loop's own S201–S203 landing + its two post-landing record commits (sitemap refresh 98ea2db, ledger record 57a4541); `sw.js` v485 and FM 5.12 both matched the ledger — no unexplained outside-loop landings. **Pass run: — (skipped on explicit user direction — the invocation was `/improveloop all fixes`, the whole budget to fixes; K/H/G stay stalest and H-on-flash_cards stays the standing recommendation).** **Gates: none asked** — gate 1 was pre-answered SKIP by the invocation itself; no gate-2 surface was touched (the one EN wording delta, 'No stacking marks'→'No Stacked marks' in the QA empty state, is a wiring consequence of reusing the authored tab keys, logged in the commit). **SW v485→v486** (precached files changed: Hebrew_Font_Maker.html, hebrew_blend_generator.html, classroom_dashboard.html, flash_cards.html, torah_trainer.html). **FM: no FONT_MAKER_VERSION bump** — both FM commits are fixes (touch-target floor + i18n wiring), not features, per the pure-fixes-don't-bump convention. Scripts: check-i18n clean ×2 (post-iter-4 and at close-out), build-locales run (no CSV changes — verified the 21 qa keys already compiled), update-sitemap after `git fetch --unshallow` (shallow-clone guard honoured). Decisions deferred to maintainer: none.
- [x] 2026-08-13 | 8c9d580 | torah_trainer.html | **The nikkud underline's intended 2px actually applies — longhand quartet** (the queued S181 Pass G P3). The `!important` SHORTHAND was resetting `text-decoration-thickness` to auto and beating the plain longhand that followed, so the nikkud line grew with type size until it equalled the 4px trope clause line at handout 'xl' — collapsing the two-tier print hierarchy. Longhands each keep the post-S60 model's !important; text-underline-offset untouched (never part of the shorthand reset). | verified: light+dark × 1280/800 × screen+print emulation, real classes on a 40px fixture (where auto ≈ 4px): nik thickness exactly 2px, trope word line 4px, 0 pageerrors
- [x] 2026-08-13 | 670ba01 | Hebrew_Font_Maker.html | **The QA modal + traced readout speak Hebrew — 18 authored keys wired** (families (a)+(e) of the S179 haul; family (b) canvas.* remains). qaRenderTabs/Summary/Grid/BuildCheck/PairControl swapped their hardcoded English template literals for the 17 unused fontmaker.qa.* keys; updateTracedReadout uses fontmaker.panels.traced_readout. Fix-shape notes honoured: ✓-clause emphasis preserved by first-sentence split, {kind} reuses the tab_* keys already serving as kind nouns, {count} keeps its <b> via the param; an open QA modal joined applyI18n's re-render list (read-only, reuses qaResult). | verified: EN+HE cells, QA opened for real via the wizard path, all 18 keys resolve both languages, no key echo / no [i18n] missing warnings, live setLang('he') re-renders the open modal, 0 pageerrors; check-i18n clean
- [x] 2026-08-13 | 02b93ca | torah_trainer.html | **transition:all backlog, torah arm CLOSED — and the census lesson: bare-duration shorthands are implicit `all`** (`transition:0.2s` — 3 of the 8 real sites, invisible to a literal-'all' grep). 7 sites colors/opacity-only → explicit lists (.hbtn, .tt-speak family, .tt-jump-chip, .btn, .radio-group label, .vowel-scheme-btn, .slider track); the 8th is the one REAL motion in the whole backlog so far — the toggle-switch knob's `translateX(16px)` — scoped to `transform 0.2s` and added to the reduced-motion block (snap, .tt-word precedent). | verified: EN/light/1280 + HE/dark/800 + a reduced-motion cell: computed lists exact on all 9 selectors, knob transform-only 0.2s normally and 0s under reduce, toggles still flip, 0 pageerrors
- [x] 2026-08-13 | 3489f6f | hebrew_blend_generator.html + classroom_dashboard.html + flash_cards.html | **transition:all backlog, generator arm CLOSED** — all 9 declaration sites adjudicated colors/opacity-only (tiles ×4, .btn, .quick-start-chip, .blend-type-btn, .rw-drill-btn, .ivrit-drop) → explicit lists, zero reduced-motion additions needed. The shared `.ivrit-*` block's `.ivrit-drop` rule was updated in **all three carriers in lockstep** so the block's rules stay identical (the convention-shared-rules invariant — a one-carrier fix would read as drift to the next Pass F). | verified: both matrix cells × 3 carriers: computed transition-property exact everywhere (no all, no geometry), letter-tile click still selects, 0 pageerrors
- [x] 2026-08-13 | 6029786 | Hebrew_Font_Maker.html | **The FM slider arm ships — sliders/select/checkbox rows at the 24px floor** (the S203-deferred pointer-queue item). .side-slider + .zoom-bar #zoomRange + .inline sliders get height:24px (3ae6891 one-property shape); #drawSmoothingSel and the .chk rows get min-height:24px (387a6a1 shape). Vertical .vslider-col deliberately untouched (its own case). | verified: 4 matrix cells, wizard-driven into Draw mode on a fresh profile: zoomRange/side-slider/smoothing/5 chk rows/.inline (#bgStrength) all 24.0px, element screenshot shows native track/thumb visuals unchanged, 0 horizontal overflow, 0 pageerrors
- [x] 2026-08-13 | (S203 close-out) | branch/deploy note | `/improveloop` → S203 **continued on `claude/improve-loop-e0844w` and draft PR #166** (open + unmerged at session start, so no branch restart — protocol followed). Drift check: `origin/main` unmoved at 8d61e8f; the only outside-loop landing since the S202 close-out is the already-ledgered maintainer-requested v5.12 contour-ID box on this same branch (0600e97+53587a3, SW v483→v484, FM 5.11→5.12) — swept as part of this session's Pass A delta. SW **v484→v485** in the close-out commit (precached files changed: Hebrew_Font_Maker.html ×2 fixes, hebrew_blend_generator.html, classroom_dashboard.html, hebrew_dictionary.html). **FONT_MAKER_VERSION not bumped — deliberate:** both FM changes are repairs (keyboard reachability + touch floor), not features, per the S202 precedent. Scripts: check-i18n clean start-to-finish (0 new strings all session); build-locales not needed (0 CSV changes); update-sitemap run last. **Gate 1 asked and answered: SKIP** (the maintainer chose four fix iterations over the weekly-grid-print lead — the seed keeps its queue spot, still never rejected); no gate-2 items arose. **Ledger hygiene: two stale unchecked candidates discovered already fixed** (torah `#settingsBackdrop` → 915526d S198; torah dark selected-verse → a01087b S199) — markers corrected rather than re-fixed; S198's Done block archived (5 sessions retained). **LANDED DIRECT TO `main` BY EXPLICIT IN-SESSION MAINTAINER AUTHORIZATION** ("Push to main", 2026-08-13, after the close-out summary was delivered) — the branch-protocol carve-out, same as S78, S191–S193, S195 and S200. `claude/improve-loop-e0844w` **fast-forwarded** `main` from `8d61e8f` to `98ea2db` with all **19** commits (S201's 6 + S202's 5 + the maintainer-requested v5.12 pair + S203's 6): `git merge-base --is-ancestor` confirmed the fast-forward BEFORE pushing, so no merge commit and no drift, and **PR #166 auto-closed as merged by the push** (the session was auto-unsubscribed from its activity; the standing hourly check-in trigger was deleted — its job is complete). **Pages deploy VERIFIED: the `pages build and deployment` run for `98ea2db` (31735801985) concluded `success`** (started 2026-08-13T19:25:57Z) — the one close-out step a branch-and-PR session cannot do in-session. So **S201's, S202's and S203's fixes, the torah drawer-memory micro-feature, and Font Maker v5.12's contour-ID box are live on ivritsuite.com**, and `sw.js` **v485** is the version that will refresh returning users.
- [x] 2026-08-13 | (S203 Pass A — recurring-pattern sweep over `2771b39..HEAD`: 56 commits, +441/−95 across 12 code files — the never-A-swept S194–S202 fixes plus the outside-loop v5.12 feature) | 9 classes | **8 clean, 1 hit, hit fixed in-session (1410e7b).** Clean with receipts: **unescaped-input/unsafe-parse** (7 added `innerHTML` writes, every interpolation I18n.t/`_pT` trusted-CSV, static app data, loop integers, or `esc()`-wrapped; **0 new `JSON.parse`/`Object.assign` in the whole delta** — streak 13); **localStorage-vs-AllTools** (1 added `setItem`, on the pre-existing FM-exempt `hebrewFontMaker_inputMode`; a1320a0's new `panelsCollapsed` field rides the registered `hebrewTorahTrainer_settings` blob — streak 13); **destructive-bulk** (0 added loops writing stored per-item data — streak 13); **untrusted-shape-on-read** (the delta's one new blob-field read, `applyPanelStates`, is the reference consult shape for the class: `typeof map === 'object'` + `Object.prototype.hasOwnProperty.call` + `!!` coercion); **invalid-SVG-geometry** (the delta's one new `<rect>` emitter, v5.12's `contourSelBoxSVG`, is negative-proof by construction — width/height = max−min+2·pad ≥ 14 over one node array, empty-contour guarded — and ran live under the v5.12 19-check battery); **animation-outside-its-reduced-motion-block** (all 6 added transition/animation lines ARE reduced-motion neutralizers — S199's own fixes; v5.12's `.ct-row.sel` outline declares no motion); **JSON-LD ↔ visible parity** (the post-S201 byte delta on head/FAQ/ld+json surfaces is exactly the gate-2-approved theme haul on all three surfaces — nothing new drifted); **wired-then-clobbered** (0 added English-literal setters on bound elements); plus **both S201/S202 pattern-class fixes re-verified holding** (3a33b1a's `.wiz-modal .tbtn.ghost` rule intact, untouched by 0600e97; 0d4373a enumerates "theme" on meta/OG/JSON-LD *and* the visible FAQ twin). **HIT: sub-floor touch target, 2nd sweep** — the FM Edit-points contours rows (`.ct-name` ~21px, `.ct-eye`/`.ct-del` ~19–20px, the destructive ✕ in the mis-tap zone), a carrier the S193 census (scoped to `.btn-xs`) never covered.
- [x] 2026-08-13 | b9091ff | Hebrew_Font_Maker.html | **The two license-help "?" badges become real buttons** (top candidate, S202 Pass C — WCAG 2.1.1 on the new-teacher path). S92-style `<a onclick>`→`<button type="button">` at both sites (wizard step-1 + Font Details), explicit `aria-label` (a bare ? glyph is otherwise the ACCNAME — name-from-content beats `title`), reusing `fontmaker.panels.license_help_title` — 0 new keys; `.fld-help` gains border:none/padding:0/font-family:inherit so the circle renders byte-identical. | verified: 13-check battery, EN/light/1280 + HE/dark/800 — live Tab walks reach both badges (the S202 lesson: walk, don't read tabIndex), Enter opens the licenses help at both sites, HE aria-label localizes, geometry 15×15/50%/0-border, 0 pageerrors.
- [x] 2026-08-13 | 1410e7b | Hebrew_Font_Maker.html | **Contours-panel row controls reach the 30px touch floor** (the Pass A hit, closed in-session like S193's founding sweep). min-height:30px on `.ct-name`; 30×30 + inline-flex centering on `.ct-eye`/`.ct-del` — S193's exact fix shape (97a16b7). | verified: 16-check battery, both cells — 9/9 fixture controls at 30px (icon pair 30×30), 0 horizontal overflow in the panel, eye-toggle and row-select still work, 0 pageerrors.
- [x] 2026-08-13 | 4a52f53 | hebrew_blend_generator.html + classroom_dashboard.html | **Range sliders get a 24px operable box** — the next slider-slice arm after 3ae6891: generator `#zoomSlider` (visible on load) and the dashboard's `.size-row` drawer sliders had no height rule at all, i.e. the ~16px Chromium native default (**baseline measured in-run with an unstyled probe input — the dashboard arm was the queue's last unmeasured one**). FM's arm stays queued (tool cap). | verified: 18-check battery, both cells per file — both arms 24px, zoom-bar row 28px and drawer rows ≤30px (no layout jump), keyboard steps still drive zoom/header-size (the HE cell steps the mirrored direction — correct RTL).
- [x] 2026-08-13 | 62c0642 | hebrew_dictionary.html | **The letter-position tiles' `transition:all` becomes an explicit color list** (`border-color/background/color 0.12s`) — the transition:all backlog resolved per-selector as prescribed: `.lpt`'s only animated states (:hover border-color; .selected background/border-color/color) are color, so the selector provably needs no reduced-motion neutralization. The dictionary's arm (1 selector) is closed; generator 23 / dashboard 24 / torah 16 remain. | verified: 10-check battery, both cells — computed transition-property is exactly the 3 color props (no all/motion), tile click still selects, 0 pageerrors.
- [x] 2026-08-13 | 0600e97 | Hebrew_Font_Maker.html + sw.js | **MAINTAINER REQUEST, between S202 and S203 — outside any loop budget (charges no pass, iteration, or variety-governor slot):** the maintainer asked (with a screenshot of a 14-contour Gimel) for a box around the contour a clicked point belongs to, to make contours identifiable in **Edit points**. Shipped as **Font Maker v5.12**: `contourSelBoxSVG()` draws a dashed `NODE_TINTS[ci]`-coloured, `pointer-events:none` box around the full bbox of every contour holding a selected point (under the node markers, suppressed in `_penMode` and during active marquee drags, skipped for hidden contours — whose selection is already pruned), and `contoursPanelHTML` outlines the matching `.ct-row` in the same tint (`--ct-tint` + a direction-neutral `outline`, so RTL mirrors for free) with `aria-current` on its name button. Works for click, Ctrl/Shift-click, marquee, and list-row select; multi-contour selections get one box per contour. Editing aid only — never exported or persisted, mirroring the tint-overlay contract. **19-check battery** (EN/light/1280 + HE/dark/800): no-selection = no box/row-mark; click → exact tint + exact span (`fx`-computed, not a guessed threshold); box+row follow the selection across contours; background click clears; hide-contour drops both; 0 pageerrors. Screenshot receipts. `FONT_MAKER_VERSION` 5.11→5.12 + changelog entry (a feature, unlike S202's CSS repair); **SW v483→v484**; check-i18n clean (no new strings — the box and outline are visual; `aria-current` is attribute-only).
- [x] 2026-08-13 | (S202 close-out) | branch/deploy note | `/improveloop` → S202 **continued on `claude/improve-loop-e0844w` and draft PR #166** — still open and unmerged at session start (state: open, draft, mergeable_state: clean, head `b0e536e` = local HEAD), so per the branch protocol **no new branch and no new PR**: S201's 6 commits plus this session's 5 accumulate on the same PR. **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `8d61e8f` (PR #166's base), `FONT_MAKER_VERSION` unmoved at **5.11**, `sw.js` on main still v481 (the branch's v482 is S201's own bump). **SW v482→v483** in the final commit; precached files changed: `hebrew_dictionary.html`, `index.html`, `Hebrew_Font_Maker.html`, `torah_trainer.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** the FM change is a pure CSS contrast repair (one rescoped rule), not a feature. `check-i18n.js` clean after every fix (Check A clean + the same 14 baselined; Check B clean); `build-locales.js` run for the 2 changed CSV rows (4272 keys); `update-sitemap.mjs` run **last** on a clone verified unshallow. **Gates: 2 asked, 2 answered, 0 deferred.** Gate 1 early (the ratified ordering): **the maintainer chose the torah persist-drawer-panels seed** — the first gate-1 build since S198, ending the two-session skip streak. Gate 2 needed no new question: the theme haul shipped under S201's standing approval, the ghost fix is CSS, and the pass's one copy-adjacent find (.wiz-dot) was logged as a P4 candidate instead of asked mid-budget. Deploy `success` is verified after merge, not in-session; the send_later check-in loop on PR #166 stays armed.
- [x] 2026-08-13 | (S202 Pass C — accessibility, one tool: `Hebrew_Font_Maker.html`, its 3rd dedicated C: S21 → S125 → S202) | **Target re-derived per the S196 rule** (the per-tool dedicated-C history, not pointer memory): FM was the stalest C tool in the pointer's preferred set (S125, 2026-07-21) AND carried the largest never-C-audited surface anywhere — the entire v3→v5.11 arc including the whole 2026-08 draw-mode suite and the onboarding wizard (dashboard, S135 + the never-C'd weekly grid, is the runner-up and next C's natural lead). **Structurally excellent, with counts:** wizard gate opens with focus in `#wizName`, Escape provably inert in gate mode (by design) and closing in New mode, **0/25 tab-trap escapes**; **112 visible draw-mode controls → 0 nameless / 0 title-only / 0 placeholder-only / 0 symbol-only in EN and HE** (the S125+S166 naming discipline held through the draw build); **both tab rows (wtab steps + ctab categories) are correct APG roving tablists** — ArrowRight moves focus AND selection; wizard radiogroups arrow-navigate with `:focus-visible` plates; draw tools keyboard-activate (Space), sliders arrow-adjust with live readouts and the **RTL arrow flip correct in Hebrew**; D/L/V/S/E/X shortcuts verified; the `(pointer:coarse),(max-width:820px)` arm gives tools+tiles 44px; 0 draw-panel contrast misses in either theme; **0 pageerrors across every probe.** **Findings: 4. Fixed in-session (1, the headline):** three ghost buttons at 1.5:1 on the light modal, including the gate's only exit (3a33b1a). **Logged:** the 2 mouse-only `.fld-help` license helps (P3 — S203's first fix), `.wiz-dot.active` white-on-gold 2.75/2.14 (P4, aria-hidden duplicate), the Move-tool selection count unannounced (P4, pointer-driven sibling of torah's), plus the FM slider/select/checkbox census appended to the standing range-slider candidate. **Two automated signals refuted before logging:** the scanner's "8 keyboard-unreachable tabs" (they are roving tablists — proven in source AND by arrow-walk), and the "`l` shortcut does nothing" miss (my own earlier probe arm had arrowed workMode to trace, so the draw-only handler was correctly inert — assert preconditions per arm). Footer anchors at 14–15px re-confirmed as inline-text-exempt, not logged.
- [x] 2026-08-13 | 0d4373a | hebrew_dictionary.html + index.html + locales/ui-strings.csv | **The theme filter joins every public copy enumeration — all 12 gate-2-approved sites, insertion-only** (S201 Pass L headline; JSON-LD↔visible parity pattern). Inserted "theme" (HE "נושא", the app's own word per `dictionary.themes.panel_title`) into the dictionary meta/og:/twitter:/WebApplication/HowTo descriptions, the JSON-LD FAQ a_lookup answer, the static FAQ + HowTo-step2 fallbacks (+ CSV EN/HE), the index ItemList entry, and the homepage card (+ CSV EN/HE). **Both live parity mismatches closed in one commit** — the FAQ pair and the HowTo pair each now enumerate theme on both sides (re-grepped post-edit), and the static FAQ fallback is again byte-identical to its CSV `en` cell. Meta description landed at exactly the predicted 156 chars. Verified with the S201 passL extractor (byte-diff shows only the intended insertions; 34/34 JSON-LD blocks parse; canonicals/OG/lengths clean), build-locales (4272 keys) + check-i18n clean, and EN/HE × light/dark × 1280/800 headless renders showing the new copy with 0 pageerrors — the dictionary FAQ/HowTo proven with the `<details>` expanded (innerText of a closed details hides its content; the first probe's "missing" copy was that artifact, not a render failure).
- [x] 2026-08-13 | 3a33b1a | Hebrew_Font_Maker.html | **The wizard's ghost buttons become readable on the light modal — 1.5:1 → 14.8:1** (S202 Pass C headline; `theme-flipping-token-on-a-fixed-colour-plate`, the S193 mirror image — here a fixed navy-toolbar token on the theme-flipping modal plate). `.tbtn.ghost` is gold-light for the navy header, but the wizard reuses it on the modal surface, so **"Open a project ▾" — the boot gate's ONLY exit for a returning teacher — plus Back and Cancel rendered at 1.5:1 on every light-mode fresh boot** (screenshot receipt shows a visibly near-blank button). The file already carried the repair for exactly this situation (`.tour-card .tbtn.ghost`), so the fix is that one line rescoped: `.wiz-modal .tbtn.ghost { color: var(--text); border-color: var(--border); }`. Verified all three buttons at **14.81:1 light / 11.98:1 dark**, ghost transparency intact, the Escape-closes-New-mode contract unchanged, 0 pageerrors, before/after screenshots.
- [x] 2026-08-13 | a1320a0 (**MICRO-FEATURE — gate-1 chosen on its FIRST offer; 2 iterations; S180 Pass H seed intake**) | torah_trainer.html | **The settings drawer remembers which panels are open.** Every session repeated the same scroll-and-expand; now the 9 panels persist as `settings.panelsCollapsed` inside the existing `hebrewTorahTrainer_settings` blob — **zero new storage keys** (AllTools backup carries it automatically; the `karaokeBarCollapsed` precedent). A panel is identified by the language-independent tail of its title's `data-i18n` key; the map stays `null` until the first toggle; **a key missing from the map keeps that panel's markup default** (a future panel keeps its own); hostile imported shapes are type-checked away. Saved from the single toggle path (mouse + Enter) and both Expand/Collapse-all buttons; applied at boot and on Reset (returns live to captured markup defaults). 36 net lines, one tool — well inside the ~150-line fence. **Stricter micro-feature battery, 16 checks + the reset contract:** fresh profile = markup defaults with nothing stored + aria 9/9; mouse and keyboard toggle→reload round-trips; EN-store→HE-boot key stability; expand-all persistence; reset → live markup defaults with `panelsCollapsed:null` in the re-saved blob (the blob re-persists via the pre-existing lastPos-on-render write — so the assertion is the FIELD's value, not key absence); AllTools-shaped import applies on boot; garbage-map tolerance (string map ignored, defaults hold); en/light/1280 + he/dark/800; 0 pageerrors.

- [x] 2026-08-13 | (S201 close-out) | branch/deploy note | `/improveloop` → S201 ran on the designated branch **`claude/improve-loop-e0844w`**, cut fresh at `origin/main` (`8d61e8f`) — the protocol's *merged* case: S200 landed direct-to-main by in-session authorization and **PR #165 auto-closed as merged**, so there was no open PR to continue and a **new draft PR** opens at this close-out. **Drift check: nothing landed outside the loop** — `origin/main`'s only motion since the S200 close-out is S200's own post-close-out landing-record commit (`8d61e8f`), `sw.js` unmoved at **v481**, `FONT_MAKER_VERSION` unmoved at **5.11**. **The clone arrived SHALLOW** (4th recorded occurrence — S175, S187, S199, S201); `git fetch --unshallow` ran before any date-derived work, per the standing rule. **SW v481→v482** in the final commit; precached files changed: `trope_tutor.html`, `torah_trainer.html`, `hebrew_dictionary.html`, `hebrew_blend_generator.html`, `locales/en.json`, `locales/he.json`. **No `FONT_MAKER_VERSION` bump — the Font Maker was untouched.** `check-i18n.js` **clean after every fix** (Check A clean + the same 14 baselined; Check B clean). `build-locales.js` run once (2 new CSV keys — the gate-approved FAQ pair; 4272 keys). `update-sitemap.mjs` run **last**, after unshallow. **Gates: 3 asked, 3 ANSWERED, 0 deferred** — gate 1 early at the post-pass batch (**skip the micro-feature, all 5 on fixes** — second consecutive skip-by-answer); gate-2a (trope print-chart FAQ) **approved as drafted and SHIPPED in-session** (da22334); gate-2b (the dictionary theme-copy haul, found by the pass's last arm after the budget was spent) **approved — all 12 sites, wording locked, ships as S202's first fix**. Deploy `success` is verified after merge, not in-session.

- [x] 2026-08-13 | (S201 Pass L — SEO & discoverability audit, 6th run) | repo-wide | **The S190 method held end-to-end, and its one blind spot is where every finding was.** Byte check FIRST (`9578bad..HEAD`): the full SEO surface — title/desc/canonical/robots-meta/OG/Twitter/**JSON-LD block text** — EXTRACTED per page at both commits and diffed (stronger than a marker-grep: an edit inside an ld+json block cannot hide), **0 changes on 13/13 pages**; extended for the first time to the **visible twins** (0 changed FAQ/`<details>`/seo lines across all 9 FAQ-bearing pages) and the **CSV** (0 changed faq/seo rows) — so every copy dimension S190 verified carried forward without re-adjudication. Mechanical at HEAD, all clean: canonicals 11/11 (home bare-root; 404/i18n-test correctly excluded and canonical-free), **34/34 JSON-LD blocks parse**, sitemap↔canonical exact 11/11 both ways, robots.txt intent + sitemap pointer, **crawl graph 11/11 from the homepage via static `<a href>` with script regions blanked** (the S200 lesson applied — and JS-built links correctly don't count), one `<h1>` ×13/13, **0 `<img>` missing alt**, og:image/twitter:image files resolve after `?v=` strip; titles 27–61 / descriptions 92–164, all unique; contact/privacy twitter:title absence re-observed as the standing ledgered P4, not re-logged. **Claims-vs-UI over the S191–S200 delta: clean** — the three capability changes since S190 (trope print-chart cb79a15, FM tablet welcome v5.10, FM header Tour v5.11) contradict no claim; the print chart was claimed nowhere, and per the gate-2 approval its FAQ shipped in-session (da22334). **Then the surface the S190 pattern line explicitly named unswept — index ItemList, WebApplication featureLists (none exist: all 7 blocks lack the property), generator/flash-cards/torah/trope FAQ+HowTo, checked at last under the NARROWED convention — produced the headline: the dictionary's theme filter (launched 2026-07-23) is missing from 12 public-copy sites, including 2 LIVE JSON-LD↔visible parity mismatches in opposite directions** (the JSON-LD FAQ answer omits theme while its visible CSV twin has it; the visible HowTo step2 omits it while its JSON-LD twin has it — plus a third state in the static fallback). generator/flash-cards/torah/trope claims: clean under the narrowed convention. Gate-2 asked at close-out: **approved, all 12 sites — S202's first fix.** Method: "unchanged since the last verified run" only inherits verification for what that run actually verified; and a parity byte check must cover head copy + visible twins + CSV, or a single-surface check calls a three-state divergence clean.

- [x] 2026-08-13 | da22334 | trope_tutor.html + locales/ui-strings.csv | **The printable chart earns its FAQ entry (gate-2 approved as drafted).** Pass L found the S198 print-whole-chart feature claimed nowhere — legitimate teacher-search surface ("printable trope chart") left dark, and trope_tutor is the tool the S200 pointer said only a pass or a seed could serve. Added the 7th FAQ pair to BOTH sides in one commit with identical wording — the visible `<details>` and the JSON-LD FAQPage twin — so parity cannot drift by construction, plus `trope.footer.faq_q7`/`faq_a7` with Hebrew reusing the tutor's own vocabulary (טבלת עזר from the button title, משפחות הטעמים from the family-chips aria). Verified headless en/he × light/dark × 1280/800: 7/7 FAQ entries render, q7/a7 translate, the row is visible when expanded, 0 pageerrors; the FAQPage block JSON-parses with 7 questions; build-locales 4272 keys; check-i18n clean.

- [x] 2026-08-13 | 8b6b6e9 | torah_trainer.html | **The vowel picker's example glyphs speak Hebrew to screen readers** — the queued S200 one-liner. `initColorPickers` built its example nikkud glyph as a bare span while the identically-shaped line in `initTropeColorPickers` already set `ex.lang = 'he'`, so a reader hit 13 Hebrew glyphs with an English voice in the vowel list but not in the trope list one panel down (CLAUDE.md rule 8 — mark at the builder so it survives re-render). Verified light/1280 + dark/800: 13/13 vowel + 6/6 trope rows carry `lang="he"` after a forced rebuild of both pickers, 0 pageerrors.

- [x] 2026-08-13 | 387a6a1 | hebrew_dictionary.html | **The 57 filter checkboxes reach the 24px touch floor** — the next queued slice of the S178 Pass C haul, and the shared-rule shape the haul itself named first. `.cb-label` rows took their height from `line-height` alone (0.75rem × 1.6 = 19.2px) with a 13×13 default checkbox; **39 of 57 rendered under WCAG 2.5.8's 24px**. One rule fixes the class: `min-height:24px` on the row (the whole label is the target) + a 16×16 input. Verified light/1280 + dark/800: 39 under-24 → **0** (min exactly 24.0), 47 under-16 inputs → 0; horizontal overflow 0 at 1280/800/420; no row clips its container; 0 pageerrors.

- [x] 2026-08-13 | 3ae6891 | hebrew_dictionary.html + hebrew_blend_generator.html | **The `.font-size-row` sliders' 4px operable strip reaches 24px** — the haul's "worst single finding in the file", plus its copied twin found by grepping the rule suite-wide before fixing: both pages carry the byte-identical `.font-size-row input[type=range]` rule with an explicit `height:4px`, so the entire operable element box was a 4px strip while the native thumb overflowed it and LOOKED grabbable — a new sub-shape of the touch-target class (explicit tiny height, not padding-only sizing). One value per carrier: 4px → 24px; native accent-color rendering keeps track and thumb visually identical (element screenshots compared before/after). Verified light/1280 + dark/800 on both pages: all 6 covered sliders (dictionary font-size + TTS rate; generator traceSpacing + traceSize + font-size + TTS rate) at 24px, rows stay flex-centred, the size readout still follows a programmatic drag (2.00rem→2.78rem), 0 pageerrors. Measured-not-fixed: generator `#zoomSlider` at 16px native — logged as the next slice.

- [x] 2026-08-13 | (S200 close-out) | branch/deploy note | `/improveloop` → S200 **continued on `claude/improve-loop-v2ydt5` and draft PR #165** — still **open and unmerged** at session start (`state: open, draft, mergeable_state: clean`, head `a9cec4a` = local HEAD), so per the branch protocol **no new branch and no new PR**: S199's 6 commits plus this session's 6 accumulate on the same PR. **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `979b77b` (the same FM step-button commit S199 already attributed to the maintainer), `FONT_MAKER_VERSION` unmoved at **5.11**, and `sw.js` on `main` still v479 (the branch's **v480** is S199's own bump). **Clone arrived UNSHALLOW** — `git rev-parse --is-shallow-repository` = false, checked before any date-derived work rather than assumed (the S199/S187/S175 hazard; this is the first session in a while where the check passed on arrival, because S199 unshallowed this same container). **SW v480→v481** in the final commit; precached files changed: `flash_cards.html`, `torah_trainer.html`, `hebrew_dictionary.html`. **`CLAUDE.md` is NOT precached**, so the doc fix alone would not have required a bump. **No `FONT_MAKER_VERSION` bump — the Font Maker was not touched this session.** `check-i18n.js` **clean after every fix** (Check A clean + the same 14 baselined; Check B clean) — and the two i18n fixes *removed* English from the Hebrew UI without adding a single CSV key, so **`build-locales.js` was correctly not run: zero keys added all session** (every key the fixes reference was already authored and translated). `update-sitemap.mjs` run **last**. **Gates: 1 asked, 0 deferred.** Gate 1 was offered **early, at the post-pass batch point** — the explicit correction the S199 pointer asked for, since S199's miss was purely ordering. **Answer: skip the micro-feature, spend all 5 on fixes.** **⚑ The gate could not be asked in the shape the pointer specified, and that is itself a finding: the pointer said "offer a flash-cards seed first", but the ledger holds NO open flash-cards seed** — the only two ever logged were struck by the maintainer at S55 with a standing do-not-re-add. The offer was made honestly from the seeds that exist (dashboard weekly-grid print, two torah seeds) with that gap stated. Gate 2 was never reached: **zero user-facing copy changed all session** — both i18n fixes route to strings the maintainer already approved, the touch-target fix is CSS, and the doc fix is not user-facing. Deploy `success` is verified after merge, not in-session. **⚑ LANDED DIRECT TO `main` BY EXPLICIT IN-SESSION MAINTAINER AUTHORIZATION** ("Push to main", 2026-08-13, after the close-out summary was delivered) — the branch-protocol carve-out, same as S78, S191–S193 and S195. `claude/improve-loop-v2ydt5` **fast-forwarded** `main` from `979b77b` to `e0c35b6` with all **12** commits (S199's 6 + S200's 6): `git merge-base --is-ancestor` confirmed the fast-forward BEFORE pushing, so no merge commit and no drift, and **PR #165 auto-closed as merged by the push** (the session was auto-unsubscribed from its activity). **Pages deploy VERIFIED: the `pages build and deployment` run for `e0c35b6` concluded `success`** (started 2026-08-13T02:16:33Z) — the one close-out step a branch-and-PR session cannot do in-session, and the reason this carve-out is worth taking when it is offered. So **S199's and S200's eight fixes are live on ivritsuite.com**, and `sw.js` **v481** is the version that will refresh returning users.

- [x] 2026-08-13 | (S200 Pass E — freshness & site health, 18th run) | repo-wide | **10 dimensions, 9 CLEAN, 1 hit — and the clean count is the story: E's mechanical half has now been clean for two consecutive runs, so the run's value came from three dimensions built PAST it** (the S199 lesson applied to a different pass). Delta since E last ran (`d33dd94`, S188): **65 commits, ~1,039 insertions across 19 files**. **The seven S188 dimensions, all clean:** sitemap `lastmod` 11/11 exact against `git log -1 --format=%cs` per file; **399 internal refs, 0 broken**; SW precache both directions (25 `CORE_ASSETS`, 12/12 production pages, `i18n-test.html` correctly absent, locales 2/2 on disk and precached); licences vs **13 LOADED third-party hosts** + 13 bundled font files, all covered; CLAUDE.md shared-block carrier counts **9/7/4/4/4/4 exact** with all three `═══`-marked blocks single-sha; reduced-motion 11/13 with `privacy.html` **and** `i18n-test.html` re-validated as zero-animation exemptions; static integrity (robots/CNAME/.nojekyll/manifest parse, 3 manifest icons resolve, the 18-tag apple-startup block **byte-identical across all 12 production pages**, 18/18 splash mirror, 0 orphans, 0 dangling). **The licence dimension was cleared by a BYTE CHECK before it was audited by hand** (the S190 method, first applied to E): `git diff -U0 d33dd94..HEAD -- '*.html'` changed **0 Content-Security-Policy lines** across all 65 commits — and since CLAUDE.md's rule 3 makes a CSP edit mandatory for any new loaded origin, that one command proves no new dependency could have entered, in one line rather than 13 host lookups. **NEW dimension D8 — `data/` cache-buster freshness (never checked by any E run):** `sw.js`'s `DATA_CACHE` matches EXACT URLs, so a corpus that changes without its `?v=` bumped strands returning users on the old copy permanently and invisibly. 12 data literals across 6 files; all files present; **0 live staleness**. **NEW dimension D10 — corpus coverage integrity:** the PocketTorah manifest declares **432 per-aliyah timing filenames** whose upstream casing is inconsistent (`Noach-1.txt` beside `lech-lecha-1.txt`), and GitHub Pages is case-sensitive, so a single wrong-case label is a 404 and a silently dead karaoke aliyah — **432/432 exact matches, 0 case mismatches, 0 orphans, 0 entries declaring `missing`**, and all **47** pocket keys the Trope Tutor's index references resolve. **NEW dimension D9 — CLAUDE.md countable claims: the pass's ONE hit** (the Font Maker line count, fixed below); every other claim exact. **Two false-positive classes killed, on top of S188's four and S175's three:** (1) **HTML-building code inside `<script>` is tag-shaped text** — a whole-tag `href` scan reported 3 "broken refs" that were a bundled minified QR library and two `resources.html` template/concat strings; script regions must be blanked for a link audit, exactly as `check-i18n` already does. (2) **`git log -1` on a file reports its last COMMIT, and a rename is not a content change** — D8's only apparent hit (`hebrew_emojis.json` at `?v=1`, file dated 2026-05-07 vs the literal dated 2026-05-03) dissolved on grounding: the file's entire history is ONE commit, `2a36c19` "Organize data and docs into folders", a pure move — and `DATA_CACHE` did not exist until 2026-07-04, two months later, so no user could ever have held a stale copy at that URL. **A third checker bug was caught by its own contradiction:** D10's first version assumed `<audioBase>-<n>.txt` and reported 204 missing *and* 258 orphan simultaneously — impossible for a healthy corpus, and the tell that the manifest carries an explicit `labels` map. Assert the property the data actually declares, not the one the filename convention suggests.

- [x] 2026-08-13 | c6c6c04 | flash_cards.html | **The vowel group headers stop saying the colour in English** — `authored-but-unreferenced i18n key family` (S179 Pass A, part d), and **the flash-cards variety debt the S199 pointer called urgent: this tool had gone SEVEN consecutive sessions untouched** (last S192). The `flashcards.vowelgroup.header` template was wired and translated, but its `{color}` param was fed the raw English `g.colorHtml`, so a Hebrew UI read **"צליל AH – Aqua"** for all seven groups — half translated, on the setup screen a teacher sees first. The nine `flashcards.vowelgroup.color_*` keys already existed, fully translated, referenced nowhere. **The interesting half is why this was not a one-line key swap:** the English label carries a teaching mnemonic (`<u>A</u>qua` = "A for AH"), so naively switching to the key would have silently dropped the underline from every English header — a regression traded for a fix. **The generator had already solved exactly this** (`vowelColorLabel`: use the translation only when it actually differs from the English word), so the fix is a convergence, not an invention; `colorName` is always one word, so lowercasing IS the whole slugify its `i18nName` would do. `color_navy`/`color_orange` stay unreferenced **on purpose** — they belong to the TaL AM scheme, which hides group headers entirely. Verified he/en × light/dark × 1280/800: 7/7 headers Hebrew in HE, and the **control arm confirms all 7 English headers keep their `<u>` in every cell**; a live EN→HE→EN switch re-renders through `applyI18n` (which already called `initVowels`) and restores `<u>` 7/7. 0 pageerrors in all 8 cells.

- [x] 2026-08-13 | 849e312 | torah_trainer.html | **The colour-picker rows stop naming vowels and tropes in English** — same pattern (S179 Pass A, part c), **and this page's last consumer of the class**. Both pickers wrote `def.name` straight from the English defs table, so a Hebrew UI showed "Kamatz / H. Patach / Sof Pasuk clause" down both lists **and** the colour input announced half in each language (`"צבע עבור Kamatz"`). The 13 `torah.glyphname.vowel_*` keys were unreferenced; the 6 `torah.glyphname.trope_*` keys were wired **for the legend only** — though their own CSV `context` column reads **"trope-family legend + picker label"**, so the picker was always the intended second consumer, and fixing one list while leaving the other would have shipped a half-translated panel. **The mapping is the trap:** slugifying the display name works for 10 of 13 vowels and fails on the three hataf rows ("H. Patach" → `h_patach`, key is `hpatach`), so slugs are mapped from `def.key` explicitly — the same shape as the generator's `VOWEL_SUB_KEY`, which carries a comment saying precisely why. No new copy. Verified he/en × light/dark × 1280/800: Hebrew reaches **0 Latin characters** across 13 vowel + 6 trope labels and all 13 aria-labels; **control arm: English byte-identical in every cell**; the TaL AM scheme's different row order (Tzere before Segol, Vav Holam before Holam) translates in place; live EN→HE→EN round-trips. 0 pageerrors.

- [x] 2026-08-13 | f4b5751 | hebrew_dictionary.html | **The filter All/None buttons reach the 30px touch floor** — `sub-floor touch target on a shared small-button class` (S193), **a new carrier beyond `.btn-xs`**, and the slice the S178 Pass C haul itself names as the shared rule to do first. `.pos-all-btn` took its entire height from `padding:3px 0`, so 10 of the 12 buttons it renders — Expand/Collapse all menus, emoji All/None, part-of-speech All/None, Clear theme, Save as Word List — measured **20–22px**. That is under the suite's ratified 30px floor **and under WCAG 2.5.8's 24px minimum**, which is the sharper framing: this was not polish below a house preference, it was a conformance failure on a tool used on school tablets. Fix is the house copy, not an invention (flash_cards' S102 `.btn-xs` declarations); **the inline-flex is the load-bearing half** — it turns `min-height` into a taller box rather than taller text. **Baseline measured BEFORE editing**, as the pattern definition requires: 40 of 48 rendered instances under 30px, all 40 also under 24px, range 20–33px → **48/48 at 30–33px, 0 under either bar**. Both mandatory guards hold — horizontal page overflow still 0 in every cell, no target overflows a clipping ancestor — plus a third guard, that the taller box does not clip its own label, checked in **English and Hebrew** (Hebrew labels are the longer ones). 0 pageerrors.

- [x] 2026-08-13 | 323cd25 | CLAUDE.md | **Stop re-fixing the Font Maker line count every few weeks** — Pass E's one hit across 10 dimensions, and a **second-order fix rather than a data correction**. CLAUDE.md called `Hebrew_Font_Maker.html` "~13,900 lines"; it is **15,691**. The tell is in the blame: the 13,900 figure was itself written by a **previous Pass E** on 2026-07-24 (`c2169fb`, "docs: fix two drifted counts in CLAUDE.md (Pass E)"), so the same claim has now drifted twice in three weeks and re-typing the number is a treadmill — the file grows about a thousand lines a month. The entry now records both figures **with their dates** and says outright that any number there is a dated snapshot, so the sentence's actual job ("this file is huge; locate by pattern, never by remembered line number") survives the next thousand lines. Every other countable claim in CLAUDE.md was audited in the same dimension and is exact: 12 HTML pages, 26 TROPES entries, 13 `VOWEL_COLOR_DEFS`, `HEB_FONTS` 8 Block / 5 Cursive, shared-block carriers 9/7/4/4/4/4.
- [x] 2026-08-12 | (S199 close-out) | branch/deploy note | `/improveloop` → S199 ran on the designated branch **`claude/improve-loop-v2ydt5`**, the protocol's *merged* case: **PR #164 merged 2026-08-12T16:31:43Z**, so there was no open PR to continue and a **new draft PR** opens at this close-out. The branch was already sitting exactly at `origin/main` (`979b77b`), so no fresh cut was needed. **Drift check: ONE thing landed outside the loop** — `979b77b` *"Font Maker: the step buttons sit centred under the canvas"*, committed AFTER S198's close-out and sitemap commits and merged along with them. It moves the "Next: Trace →" / "Next: Nikkud →" / "Apply alignment…" buttons out of `.ws-under` into `#stageBelow` (they had been rendering far-right and below the fold, y=1268 at 1280px and y=2399 at 800px), and adds the missing v5.10/v5.11 CSV changelog keys so the Hebrew About tab no longer stops at v5.9. It folded into the unreleased **v5.11** rather than minting v5.12, and left `sw.js` at **v479** — both calls verified correct here by reading the files, not by trusting the message: `main` was on v476 before S198's bump, so v479 already covers those file edits. **`sw.js` v479 and `FONT_MAKER_VERSION` 5.11 both read from the files at session start.** **The clone arrived SHALLOW again** (the S188 hazard): `git rev-parse --is-shallow-repository` = true, so `git fetch --unshallow` ran BEFORE any date-based work and before `update-sitemap.mjs`. **`sw.js` v479→v480** in this final commit — precached files changed: `torah_trainer.html`, `index.html`, `404.html`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `hebrew_dictionary.html`. **`FONT_MAKER_VERSION` deliberately NOT bumped** — no Font Maker code was touched this session (the FM work in the drift check is the maintainer's, already carried by 5.11). Scripts run: `check-i18n.js` (clean, 14 baselined) after every fix, `update-sitemap.mjs` as the last step. **Decisions deferred to maintainer: gate 1 (micro-feature) was NOT offered this session** — the pass plus four fixes consumed the full 5-iteration budget, and a micro-feature costs 2, so it could not have been built even if chosen; the recommended seed is queued in the pointer for S200 instead. **No gate-2 work arose** — all four fixes reuse existing strings/tokens and change no user-facing copy.

- [x] 2026-08-12 | (S199 Pass B — console & error audit, 17th run) | 12 pages + 7 tools | **Every mechanical arm came back clean, and the one finding came from a deliberately-built third arm past them.** Delta since B's last run (`ae3c7cd`, S187): **~700 lines across 13 files** — torah_trainer +282, Font Maker +170, trope_tutor +113, dashboard +99, dictionary +72. **Arm 1 (load): 12 pages × EN/HE = 24 runs, `serviceWorkers:'block'`, local HTTP → 22/24 clean**, and the 2 flags are both torah_trainer's blocked-external artifacts, each grounded to a *handled* path before being dismissed (`fetchSefariaText`'s `console.error` sits in a catch that calls `setStatus(reading_load_failed)`; the `[hebrew-translit]` lines are the app's own two-CDN fallback `console.warn`s). **⚑ S187's recorded blind spot is CLOSED** — it warned that Playwright's own SW-block warning pre-fills the warning channel on 24/24 runs so a real warning would hide in plain sight; filtering it at source is exactly what turned 24/24-flagged into 22/24-clean. **Arm 2 (interaction): 7 tools, and its first version was VACUOUS** — steps guarded with `if (window.X)` silently no-opped because `generateBlends`/`startSession` do not exist (the real names are `generate`/`startPractice`), so "clean" meant "nothing ran". Re-run with a **hard receipt per step**: generator 1 sheet built + queue drained, flash-cards a real card face (`כְשֻׂ / Tap to flip / Khsoo`) with `#startBtn` gone, dictionary 9 results, dashboard 3 collapsed panels → 0, torah drawer open/close, **trope_tutor's brand-new S198 print-chart assembling 5 → 31 cards on `beforeprint` and restoring to 5 on `afterprint`**, FM tour card text. All 7 clean. **Arm 3 (past the gates): unhandled promise rejections — 0 suite-wide** on both the happy path and with each tool's `data/` corpus blocked; the same arm's failure-path column is what produced the hit. **1 finding, fixed this session** (torah_trainer's clobbered data-load message). **Two candidate findings were killed by grounding before either was logged:** (1) FM's `#stageBelow` reported empty in 4 of 5 work modes — `renderControls` early-returns on `!project` and my wizard-dismissal never created one; driving the REAL wizard to a created project showed the modes are conditional by design (the Next buttons need an image or contours) and `#stageBelow` is correctly centred (cx 640 = canvas cx, w 620), the anchors row being a deliberate 2-column layout inside it. (2) The generator's tour card "surviving" `tourEnd()` — its engine hides `#tourCard` with `hidden` rather than removing it, and my selector matched a different tour element. **Method note: my own failure-path detector produced a false negative** — a body-text regex for `fail|error|couldn't|…` missed both real messages because trope_tutor's uses a CURLY apostrophe ("couldn’t") and says "unavailable"; reading each tool's own `[role=status]` live region instead is what exposed the torah clobber. Measure the element, not the prose.

- [x] 2026-08-12 | b5bd9c0 | torah_trainer.html | **The "data files failed to load" message stops being overwritten a moment later** — Pass B's one finding, and a **NEW registered pattern** (`error-status-clobbered-by-a-later-routine-write`). When init's fetch of `parshiyot.json` / `pockettorah/*.json` fails — a school network blocking the origin, a partial deploy, a stale cache — the catch sets a diagnostic naming the missing files. init then continues to `fetchAndRender()`, which resolves no reading and, in its `if (!r)` branch, **overwrote that line with "Pick a parsha to start."** So the only message explaining the failure was destroyed inside the same init, and what remained instructed the teacher to pick from a list the failure had just emptied: the picker holds only the 17 local `HOLIDAY_READINGS`, with all 54 parshiyot gone. **Detected by reading `#ttStatus` under a blocked corpus, not by reading the code** — the code looks correct at both sites in isolation. Fix uses existing state (`!parshiyotData.length` is reachable only on that failure) and **two keys that already existed**, so no new copy and no CSV change; the catch also now passes its key to `setStatus` so a language switch re-resolves the line. Verified: blocked corpus → diagnostic holds in EN **and** HE with error styling and the key set; **CONTROL with the corpus present and no reading forced → still "Pick a parsha to start."**, so the normal path is untouched; language switch re-localizes the held message; light/dark × 1280/800 all 0 pageerrors.

- [x] 2026-08-12 | 071e326 | index.html | **The My Fonts note's link stops vanishing in dark mode** (`theme-flipping-token-on-a-fixed-colour-plate` / the UA-default-anchor sub-detector), closing the queued **"5 dormant anchors"** item. `.ie-note { color: var(--muted) }` does not reach an `<a>` inside it — the UA's own link rule wins — so the "Hebrew Font Maker" link kept UA blue `#0000EE`: **9.4:1 on light but 1.63:1 on the dark modal plate** at 12px. **Measured with the gear modal actually opened, not force-revealed**, in both themes and both viewports. Same one-line remedy as S194's three FM hint links, reusing the idiom `index.html` already applies to its footer links (`--gold-text`); underline left to the UA so the link is not signalled by colour alone. Now **6.22:1 light / 7.16:1 dark**. **The queued count reconciles exactly:** the 5 dormant anchors S193 found un-rendered = the 3 FM hint links (fixed S194) + these 2. **1 REFUTED and deliberately not fixed:** `hebrew_dictionary`'s `.skip-results` reports 1.81:1 in dark, but only while it is the clipped 1×1px offscreen skip link — its one visible state is `:focus`, which sets `background:var(--navy); color:#fff`. Recorded so the next sweep does not re-chase it (the same clipped-skip-link false positive S195 hit).

- [x] 2026-08-12 | 6fcf9e4 | 404.html + hebrew_blend_generator.html + classroom_dashboard.html + hebrew_dictionary.html | **The four `transform` transitions that escaped their own reduced-motion blocks** — the **first GENERAL sweep** of `animation-outside-its-reduced-motion-block` (S198 registered the pattern but censused only the `.settings-backdrop` drawer shape). Measured the **symptom** rather than parsing CSS: under `reducedMotion:'reduce'`, which elements still compute a non-zero transition/animation, across all 12 pages. **Triage mattered more than the count, and two thresholds had to be corrected before the number meant anything:** raw duration flags ~108 elements, but **`0.00001s` IS the neutralizer idiom** (`0.01ms !important`) — that is coverage, not a hit — and a 0.15s hover **colour** fade is not motion. Splitting transitioned *property* from duration leaves the unambiguous class: explicit `transform`, **4 pages / 4 selectors / 16 elements** — `404 .home-btn` (hover lifts `translateY(-2px)`), `generator .vg-arrow` (collapse chevron rotates −90°), `dashboard .day-item` ("today" cue scales to 1.02), `dictionary #fsBtn` (hover scales to 1.08). Each page already HAD a block; each simply missed these. **The generator's case is a convergence, not a novelty:** `hebrew_dictionary`'s block already lists `.vg-arrow`, and the generator — where that arrow originates — did not. Only the transition is dropped, so the end state still applies, instantly. **Keyframe animations were already neutralized on every page (0 suite-wide)**, which is why this class stayed invisible. **404 needed `!important` for a real reason, not for consistency:** `.home-btn`'s own transition is declared LATER in the same file at equal specificity and a media query adds none, so the first attempt parsed fine and **changed nothing** — caught only by re-measuring. The generator's `.vg-arrow` carries its transition as an **inline** style from `initVowels`, which `!important` also covers. Verified: all four 0s under reduce across light/dark × 1280/800, 0 pageerrors, **plus a CONTROL at `no-preference` confirming all four keep their original transitions** so ordinary motion is unchanged; detector re-run suite-wide → zero explicit `transform` transitions survive.

- [x] 2026-08-12 | a01087b | torah_trainer.html | **A selected verse's metadata reaches 4.5:1 in dark** — the queued P4, and the last actionable item in that contrast queue. Bulk-selecting a verse paints it with a gold tint compositing over the dark plate to `rgb(54,50,43)`; `.tt-verse-num` and `.tt-rowlabel` stay `--muted` (`#8a94a8`) at 11.2px / 9.92px → **4.18:1** against the 4.5:1 small text needs. **It fails only once a verse is SELECTED**, which is exactly why an unselected-state check clears it — those sit at 5.58:1 on the untinted plate. Uses the same lighter grey the generator already applies to small muted text on a tinted dark plate (`.vowel-chip-sub`, `#adb6c9`, S196), scoped `body.dark .tt-bulk-selected` so light is untouched. **Measured on REAL rendered verses rather than injected markup — Sefaria mocked at the route so the actual render path runs** (3 verses, 0 pageerrors). `.tt-rowlabel` only renders with the interlinear rows on, so the first measurement saw just `.tt-verse-num` and briefly looked like a scope correction; turning translation on confirmed **both** share the defect and both are fixed. Dark **4.18 → 6.26:1** for both selectors. Controls: light unchanged at 5.25:1, and **unselected verses unchanged** at 5.58:1 dark / 5.81:1 light at 800px.
- [x] 2026-08-12 | (S198 close-out) | branch/deploy note | `/improveloop` → S198 **continued on `claude/improve-loop-ah681q` and draft PR #164** (still open + unmerged at session start — the protocol's no-restart case; the PR now carries S196 + S197 + S198). **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `cb7c39e`, `sw.js` **v478** and `FONT_MAKER_VERSION` **5.10** both verified **by reading the files**, not by trusting S197's close-out. **The clone arrived SHALLOW** (`is-shallow-repository` = true) — `git fetch --unshallow` was started at session open and confirmed complete **before** `update-sitemap.mjs` ran, per the standing S191/S188 rule. **SW v478→v479**; precached files changed: `Hebrew_Font_Maker.html`, `trope_tutor.html`, `torah_trainer.html`, `locales/en.json`, `locales/he.json`. **`FONT_MAKER_VERSION` 5.10→5.11 WITH a changelog entry** — the header ❓ Tour button is a new visible control, i.e. a feature, not a defect fix; one combined bump for the session's single FM change. Scripts: `build-locales.js` run (**+5 CSV keys**: 3 fontmaker toolbar tour keys, 2 trope print keys; 4263→4268), `check-i18n.js` **clean** on both gates (Check A clean + the same 14 baselined; Check B clean), `update-sitemap.mjs` run **LAST** on the unshallowed clone. **Both decision gates asked and answered; nothing deferred to the maintainer.** **Protocol divergence flagged for the skill: none.** Deploy `success` is verified **after merge**, not in-session.
- [x] 2026-08-12 | 915526d | torah_trainer.html | **The settings backdrop honors reduce-motion like the drawer it travels with** (the queued P4; a binding CLAUDE.md rule). The reduced-motion block explicitly neutralized `.settings-modal` — the drawer snaps — but the backdrop travelling with it kept `transition:opacity 0.25s`, so a reader who asked the OS for less motion still got a fade. **Instance, not a class, and the census is what proved it:** of the **three** carriers of this drawer, `classroom_dashboard` (one line, grouped with `.fr-modal`/`.panel-title::after`) and `trope_tutor` (its own line) both already neutralize the backdrop; torah_trainer was the only one that did not. Converged onto the dashboard's idiom — grouped with `.settings-modal` on the same line, so the pair cannot drift apart again. | verified **12 cells** (3 carriers × reduce on/off × light/dark): with reduce on, backdrop **and** modal compute `0s`; with reduce off the backdrop is still `0.25s`. **That control arm is what makes the result mean anything** — a probe reading `none` in both states would be measuring nothing.
- [x] 2026-08-12 | cb79a15 (**MICRO-FEATURE — gate-1 chosen on its 4th offer; 2 iterations; S195 Pass G seed intake**) | trope_tutor.html | **Print the whole trope chart, not just the family you are on.** The Learn view studies one clause family at a time, so Ctrl+P yielded 5–6 of the 26 marks and a teacher wanting the full reference had to run **six separate print jobs**, with whichever family they happened to be on as the only one on the sheet. Print now assembles all six families into a paper-only `#tuPrintSheet`. **The design decision that matters: the sheet is built by walking every family through the REAL renderer and harvesting its cards, not by duplicating `renderLearn`'s ~95-line card builder** — so the paper can never drift from the screen, and the diff stayed at **71 lines** instead of ~170. Two invariants make reusing the live renderer safe: **`_chartPrintBuild` suppresses `renderLearn`'s "family visited" progress write** (without it, printing a reference sheet would silently mark all six families studied in a student's mastery record), and a `finally` block restores the teacher's family even if a card builder throws. Wired to both a **🖨️ Print chart** button and `beforeprint`, so plain Ctrl+P gets the full chart too; `afterprint` clears it so the next job re-reads the live language and tradition. **This SUBSUMES the queued S195 Pass G finding that interactive chrome printed onto the chart** — the chips, family note and "Next family →" are navigation for a single-family view the paper no longer has, so print hides the whole live view rather than maintaining a hide-list. | verified **on the artifact, not just the DOM** (CLAUDE.md's export rule): PDFs rendered at **Letter and A4, from dark and light**, all carry **6/6 family headings and 26/26 marks** with **zero screen chrome** in the extracted text and **no blank pages** (13–15 pp; per-page text lengths printed, so a stranded page could not hide). Plus **6 matrix cells** (light/dark × 1280/800 × EN/HE) asserting 26 cards under 6 headings, live view + current family restored, and the mastery record **byte-identical**; a fresh-profile arm holding families-studied at 1 through both the button and the `beforeprint`/`afterprint` path; and a regression arm holding the tour at 6/6 with `nextFamily` still working beside the new sibling button. **No new persisted state → no AllTools or preset wiring required.**
- [x] 2026-08-12 | 99c4a80 | Hebrew_Font_Maker.html | **The guided tour gets the header button every other tool has** (S198 Pass F's one laggard; **gate-2 asked, and the maintainer's follow-up question changed the fix**). Six of seven tools carry an identical header `#tourBtn`; the Font Maker's tour was reachable **only from inside the help modal**, so a returning teacher had to guess it was behind "Help". **The maintainer pushed back with "what about the wizard — doesn't that override that?", and grounding it split the answer in two:** the wizard does **not** cover the gap (its `#wizTour` "Show me around after setup" is **unchecked by default** and reset on every open, and the wizard only opens when there is **no restorable autosave** — "Continue" never shows it, so the returning teacher is never offered the tour at all), but it **does** override the *pulse* (on a fresh profile the wizard sits open over this very bar). So the button ships **without** the six tools' first-visit pulse — which also avoids a real trap: this page writes `hebrewFontMaker_tourDone` on tour **completion**, not on tour **start** like the other six, so a pulse gated on it would return every visit until a teacher finished all seven steps. Uses the suite's unanimous label/title (`❓ Tour` / "Take a quick tour") plus an `aria-label` stripping the emoji, matching the neighbouring Help button's own local idiom. **FONT_MAKER_VERSION 5.10→5.11 + changelog entry** (new visible control = feature). | verified 6 matrix cells (light/dark × 1280/800 × EN/HE): visible and in-viewport, correct label/title/aria in both languages, **sits before Help in LTR and after it in RTL**, `animation:none`, tour starts, Escape ends it, focus returns to the button; plus a fresh-profile cell (wizard open, still no pulse) and a **control arm** confirming trope_tutor and torah_trainer still pulse. 0 failures, 0 pageerrors.
- [x] 2026-08-12 | (S198 Pass F — cross-tool consistency, 17th run) | all 7 tools | **Affordance: the GUIDED TOUR — never swept in 16 prior F runs**, and picked because CLAUDE.md documents the seven engines as drifting per-file copies with a standing extraction rule, while S197 had just proved the drift real by fixing a broken step counter in two of them. Eight-dimension contract fixed **before** measuring, taken from CLAUDE.md's own wording: entry / card semantics / focus-in and focus-return / arrow keys / Escape / resize-safety / read-only walk / spotlight-in-viewport. **Result: the most CONSISTENT affordance the loop has ever swept — 6 of 7 tools identical on every dimension.** All seven expose `role="dialog"` + `aria-labelledby`, move focus into the card and return it to the opener, navigate by arrow key, end on Escape, reposition on resize, and mutate **nothing but their own seen-flag** across a full walk. **One laggard, on one dimension:** Font Maker had no header entry (fixed, 99c4a80). **Two weak arms were re-measured properly rather than reported as passes** — the first run's scroll check read `scrollY` on step 1 only (unchanged at 1280×900 because the target was already in view, i.e. vacuous), so it was replaced by walking **every** step at **two viewport heights** and asking whether the spotlighted target is inside the viewport band: **clean 14/14 cells, 0 off-screen targets and 0 off-screen cards**; and the pulse arm was re-run as fresh-profile-vs-flag-set, showing all six carriers converge on the identical `pulse-attn`/`attnPulse`/flag-gate idiom. **No new pattern registered for the tour itself** — a "one carrier missing a shared affordance" detector would just duplicate Pass F's own remit — but the session's *other* fix supplied a genuinely mechanical one, registered below (`animation-outside-its-reduced-motion-block`).
- [x] 2026-08-12 | (S197 close-out) | branch/deploy note | `/improveloop` → S197 **continued on `claude/improve-loop-ah681q` and draft PR #164** (open + unmerged at session start, so the branch/PR protocol's continue-don't-restart case — no fresh cut, no second PR). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `cb7c39e`, exactly where S196 left it and matching PR #164's base sha; `sw.js` v477 and `FONT_MAKER_VERSION` 5.9 both verified by reading the files, not by trusting S196's note. **This session: SW v477→v478** (precached files changed: `torah_trainer.html`, `hebrew_dictionary.html`, `Hebrew_Font_Maker.html`, and **both locale JSONs** — `/locales/en.json` and `/locales/he.json` are in `CORE_ASSETS`, so a CSV-key session always bumps). **FONT_MAKER_VERSION 5.9→5.10** — one combined bump + one About-tab changelog entry covering BOTH Font Maker changes (the tablet welcome + the Trace-step copy), per the one-bump-per-release rule; **5.10 not 6.0** because the v4 line ran to v4.31, so two-digit minors are this file's established convention. Scripts run: `check-i18n.js` (**both gates clean** — Check A clean + the 14 permanent baselined findings; Check B clean, the single empty-`he` cell `worksheet.layout.title_default` being pre-existing debt, not this session's), `build-locales.js` (**4 CSV keys added**, 4259→4263), `update-sitemap.mjs` as the last step. **Gates: BOTH ASKED AND ANSWERED — the session was attended, unlike S196.** Gate 1 → maintainer chose the **Font Maker mobile-warning seed** over the dashboard weekly-grid print seed and the trope-chart print seed. Gate 2 was asked **twice**: (a) the FM tour's Trace-step copy → *add a draw variant*; (b) the tablet-tier copy → maintainer chose the **shorter, no-caveat** wording over the longer version that kept the "fine-tuning is easier on a desktop" caveat. **Nothing deferred to the maintainer.** **No protocol divergence to flag** — the skill and the ledger agreed throughout. Deploy lands with the PR merge.
- [x] 2026-08-12 | 7d12b62 | Hebrew_Font_Maker.html | **The guided tour stops calling a drawing a picture** (gate-2 approved). In a draw project the Trace step read "Tracing turns your **picture** into a real font outline" — but a drawn letter has no picture. **Step 2 already shipped a `bodyKeyDraw` variant for exactly this reason**, so both the mechanism and the precedent existed and step 3 was simply missed when Draw mode landed; the fix is one new key (`fontmaker.tour.step_3_body_draw`, EN+HE) plus one line. Carries this release's combined `FONT_MAKER_VERSION` 5.9→5.10 bump + changelog entry. **Verified 6/6 with the upload arm as the CONTROL** — draw says "drawing", upload still says "picture", in EN and HE, light and dark, 1280 and 800. A change that flipped both would have been a regression, and only an arm that can fail proves the gate works. 0 pageerrors.
- [x] 2026-08-12 | 9bcecda (**MICRO-FEATURE — gate-1 chosen; 2 iterations; S180 seed intake**) | Hebrew_Font_Maker.html | **A tablet is told what it's good at instead of being told to go away.** The boot warning fires on (`≤820px` ∧ `pointer:coarse`). Landscape iPads already cleared that bar — but a **portrait iPad is 768px**, so the device the entire v5.8/v5.9 Draw-mode build was aimed at was met with "Best on a computer… much easier on a desktop or laptop… it may feel cramped" over a "← Go back" button. Split into two tiers: **phones (≤560px) keep the existing warning verbatim**; **tablet-sized touch devices (561–820px)** get drawing-first copy and a primary **"✍️ Start drawing"**. **The feature needed no new state:** `wizardOpen()` already preselects the source tile from the saved `hebrewFontMaker_inputMode` pref, so writing `'draw'` to that **existing** key IS the mechanism — no new flag, no second launch path to keep in sync with `maybeRestoreAutosave`, and no AllTools wiring (the key is already in the Font Maker's documented local-only set). The title swaps by **rewriting its `data-i18n` attribute**, not its `textContent`, because `applyStaticI18n` re-reads that attribute on every language change and a bare write would snap back to the phone copy. **Verified 12 cells + 4 targeted arms, each asserting its own emulation took effect** (a cell that lost `pointer:coarse` reports VACUOUS rather than passing by accident — 0 vacuous): phone 390/560 unchanged both themes; tablet 600/768 both themes; 1024px and narrow-desktop-with-a-fine-pointer still show nothing; the dismissed flag still suppresses both tiers; "Start drawing" → pref persisted → wizard opens with Drawing preselected → Create yields `inputMode="draw"`, `workMode="draw"`; EN→HE re-translates the tablet title and keeps its key. 0 failures, 0 pageerrors.
- [x] 2026-08-12 | 27554b4 | torah_trainer.html + hebrew_dictionary.html | **The tour step counter stops promising steps it will never show** — S197 Pass I's headline and a **NEW registered pattern** (`content-dependent-tour-step-miscounts-the-tour`). A step whose content never arrived resolves to null and is skipped **silently by design**; the counter was built from the raw array index, so it **jumped and over-promised**: torah with no reading `"Step 3 of 6" → "Step 6 of 6"`, dictionary with its corpus blocked `"Step 4 of 7" → "Step 6 of 7"`. Both are real school scenarios — a network that blocks the corpus (the case f72e0f8 already wrote a teacher-facing message for), a Sefaria outage, or simply no parsha picked yet. **Fix: count by PRESENCE of each step's target, never visibility.** Absent means the content isn't there at all; merely-hidden means a collapsed panel that opens fine once the tour reaches it. Presence is also the **only side-effect-free probe available** — testing visibility would mean running every step's `reveal()`, which in the dictionary opens the sidebar and expands panels the tour hasn't reached (the exact artifact this pass's own first probe hit). The step being shown always counts itself, so torah's `_tourHoldForReading` (parked on a step whose target hasn't rendered) still numbers correctly. **torah `1,2,3,6 of 6` → `1,2,3,4 of 4`; dictionary `1,2,3,4,6,7 of 7` → `1..6 of 6`; both unchanged when the content loads.** Verified 12/12 matrix cells (light/dark × 1280/800 × three states): no jump, stable total, walk ends exactly at that total, 0 pageerrors, 0 vacuous runs; the 5 untouched tours unchanged.
- [x] 2026-08-12 | (S197 Pass I — first-load & empty-state, 16th run) | all 12 pages | **The mechanical gates were clean for the fourth consecutive I, so the entire value came from past them — and the thing that paid was auditing the ONBOARDING itself.** Gates: 12 pages × light/dark = **24/24 clean** (0 pageerror, 0 failed local requests, 0 raw i18n keys in text OR in `title`/`aria-label`/`placeholder`, correct fresh-context OS-aware dark on all 12 dark runs, one `h1` each); **click-everything = 539 buttons across 8 fresh profiles, 0 throws** (up from S186's 333 — the selector now includes `[role="button"]`). The unswept delta was large: **80 commits / 1,547 added HTML+JS lines since S186's close-out `a994996`**, of which Font Maker was 1,164 (stamp tool, brush-picker grid, per-style dials, actual-size preview, trace-from-drawing, Letter Metrics relocation). **New probe, and the reusable result of this run: walk every tool's REAL guided tour on a fresh profile and compare steps actually shown against `TOUR_STEPS.length`.** Pass I's remit includes "does onboarding copy match current UI?", and a step whose `target()` no longer resolves is skipped *silently by design*, so a stale or truncated tour is invisible from outside. **1 real class found (2 carriers), 1 gate-2 copy finding, and 2 of my own probe results thrown out before either was logged.** The FM wizard→Draw first-run path was audited end-to-end and is **clean**: wizard auto-opens, Drawing tile → canvas visible, tour auto-starts, all 7 steps show, `bodyKeyDraw` correctly substituted for step 2, toolbar (Draw/Line/Move/Stamp/Erase) present, 0 suspicious labels, 0 pageerrors.
- [x] 2026-08-12 | (S196 close-out) | branch/deploy note | `/improveloop` → S196 ran on the designated branch **`claude/improve-loop-ah681q`**, cut fresh off latest `origin/main` — the protocol's *merged* case, not the continue case: S194+S195 were landed direct-to-main by maintainer authorization and PR #163 closed as merged, so there was no open PR to continue and a **new draft PR** opens at this close-out. **Drift check: NOTHING landed outside the loop** — `origin/main` sat exactly at `cb7c39e` (the loop's own S194–S195 landing-record commit), `sw.js` unmoved at **v476**, `FONT_MAKER_VERSION` unmoved at **5.9**. **The clone arrived SHALLOW** (`git rev-parse --is-shallow-repository` = true — S191's trap re-checked rather than assumed, and this time it fired), so `git fetch --unshallow` ran before any date-based receipt; 1959 commits after. **SW v476→v477**, precached files changed: `Hebrew_Font_Maker.html`, `js/i18n.js`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `torah_trainer.html`. **No `FONT_MAKER_VERSION` bump — the Font Maker change is a pure performance fix, not a feature** (CLAUDE.md: fixes/token swaps don't bump). `check-i18n.js` **clean** (Check A clean + the same 14 baselined; Check B clean) — zero user-facing strings were added or changed all session. `build-locales.js` **not run — zero CSV keys added** (every change is CSS or JS-internal). `update-sitemap.mjs` run **last**. **Decisions deferred to maintainer (the session was non-interactive, so both gates took their documented unattended defaults): gate 1 — micro-feature SKIPPED this session** (no seed built; the queue is unchanged and the strongest dual-audience seeds are still on offer); **gate 2 — not reached**, because all four fixes were CSS/performance with no copy, title, or JSON-LD change. Deploy `success` is verified after merge, not in-session.
- [x] 2026-08-12 | dbeea8a | torah_trainer.html | **The click-a-word hint toast clears the karaoke bar at tablet and phone widths.** Closes the latent P4 S190 measured but deliberately did not bundle into that session's feature commit. `.tt-hint-toast` carried a hardcoded `bottom:96px` while the sticky bar it must clear is **not** 96px — it wraps to **77px at 1280, 106.5px at 800, 167.3px at 420** (S190's numbers reproduced exactly), so the toast sat ON the bar precisely at classroom-tablet and phone widths. Fix reuses the mechanism already in the file for `#ttBulkDock`, which has the identical problem and was fixed this way in S190: ride the live `--tt-audio-h` a `ResizeObserver` publishes, scoped to `body.tt-karaoke-active` so 96px stays the no-karaoke resting offset (verified unchanged). **Two harness failures preceded the real measurement, both caught rather than shipped:** (1) forcing only the body class renders a **0px** bar — `barVisible` false, a vacuous cell — the app pairs `.open` on the bar WITH the body class, and the harness now asserts visibility; (2) the bar is `position:sticky`, so in a fixture with no reading loaded the page is too short for it to pin to the viewport edge at 1280/800 — those cells verify the *mechanism* (toast bottom now tracks the live height: 823/793/733 vs a constant 804) and are reported as **not valid overlap cells** rather than counted as passes. Overlap itself is verified in the 2 cells where the bar genuinely pinned (420 light+dark): **71.3px → 0.3px**, the residue being `--tt-audio-h`'s integer `offsetHeight` against a 167.3px box — the dock's identical formula carries the same sub-pixel residue, so it is deliberately left alone. `pageerror == 0` in all cells.
- [x] 2026-08-12 | 0a4871d | hebrew_blend_generator.html + classroom_dashboard.html | **Muted small text reaches 4.5:1 in dark mode in two more places** — the bundled P4 S194 logged with "bundle them the next time either file is open for contrast work". (1) The generator's **vowel-group sub-labels** at 11.5px on the coloured group plates: worst **3.28:1** (Default, OH/yellow) and **3.38:1** (TaL AM, EY/gold). (2) The dashboard's **`.emoji-set-btn` "None"** at 16px on `#2a3349`: **4.13:1**. Both take the tool's existing idiom — a lighter literal with a comment naming the floor, exactly like the generator's `.rw-drill-btn` — at `#adb6c9`, **sized against BOTH vowel schemes** because TaL AM re-groups the chips onto different plates and the Default reading alone does not cover it. The generator's sub-label moves from an inline style to a `.vowel-chip-sub` class, since an inline colour cannot be theme-conditional; light mode is untouched (the class still resolves `var(--muted)`). **⚑ METHOD — the first three readings were WRONG and the LEDGER WAS RIGHT.** `.vowel-group-section` carries `transition: background 0.15s`, so every measurement taken right after `toggleDark()` samples a plate still mid-fade. That inflated the generator's worst to 4.09:1, made `#99a2b8` look sufficient (4.88:1) when it is not (3.91:1), and manufactured a convincing **phantom bug** — "toggleDark never applies the dark vowel plates", all 7 groups apparently stuck on light values — **refuted before it was logged** by reading the inline style attribute (already dark) against the computed value (still light), then re-reading after 400ms. Verified on **settled, boot-themed** pages: 8 generator cells (1280/800 × light/dark × Default/TaL AM), 13 sub-labels found in each (**count asserted**, so the selector change cannot pass vacuously), 0 under 4.5:1, worst **4.91** / **5.07**; light unchanged at 4.62 / 4.50; dashboard 4.13 → **4.92:1** dark, 4.69:1 light unchanged; `pageerror == 0` in all 10 cells.
- [x] 2026-08-12 | 32b09be | js/i18n.js (all 12 pages) | **The shared language switcher reaches the 30px touch floor**, closing the last known member of the touch-target census S193–S195 worked through (`.btn-xs` suite-wide, `.sreset`, then torah_trainer's four `<select>`s). Left for its own iteration because it is styled in the shared runtime and renders on every page at once. Measured first: **24/24 cells (12 pages × light/dark) at 26.2px** against the ratified 30px floor (S102) and WCAG 2.5.8's 24px, its height coming from padding alone — the census pattern's exact shape. `min-height` + `box-sizing` rather than more padding (matching S195's torah_trainer fix) so the box is pinned regardless of inherited font-size across 12 different headers. Verified 24 cells plus a **36-cell reflow guard at 1280/800/420**: height 26.2 → **30.0px** in all 24, none left under the floor; the `::after` caret stays **exactly centred (offset 0 in every cell)** — it is positioned off the wrapper so it follows the taller box, which `appearance:none` made the specific risk worth measuring; the option label stays vertically centred (**pixel-centroid within ±0.78px** of the box centre, tolerance 1.5px, every cell with ink so the check is not vacuous); **0 new wraps and 0 new horizontal overflow** across all 36 reflow cells, the only height changing anywhere being the switcher's own row at **+3.8px**; `pageerror == 0`.
- [x] 2026-08-12 | 2e09c29 | Hebrew_Font_Maker.html | **The styled draw preview repaints once per frame, not once per input sample** — S196 Pass D's headline, and a **NEW registered pattern** (`per-sample-repaint-of-an-O(n)-live-preview`). Draw mode's live preview for the four styled pens rebuilt the ENTIRE stroke path from every accumulated point on **every accepted `pointermove` sample**, making one stroke O(n²) in its own length. The function's own comment says "each move batch" — the call sits **inside** the per-sample loop, so the code was already more expensive than its documented contract. Measured @4× CPU, last-10% mean per sample / frames over the 16.7 ms budget, as stroke length grows to the 2048-pt cap: **round 0.56→1.92 ms (0→4 frames)**, **chisel 1.9→10.98 (0→3)**, **brush 2.4→13.93 (0→28)**, **hollow 2.55→34.35 (0→408)**. The pain does not wait for the cap: **hollow is already over budget on 153 samples at n=1200** — one slow sweeping stroke on a classroom tablet. Fix is the tool's own idiom, used twice already — `boldGenSet` (itself **the S85 Pass D fix for exactly this shape**, reintroduced by draw mode 111 sessions later) and `nodeStatusCoords`: coalesce to one repaint per animation frame, no snapshot, the frame re-reads `g.pts`. The round pen's incremental append is **deliberately left alone** — measured, it never blows the budget. Verified @4×, light/dark × 1280/800, with **paced** input so rAF actually runs (a synchronous dispatch loop starves rAF and would fake a 100× win): `_drawLivePathD` invocations **884 → 76** (one per frame) with the instrumentation wrap **asserting it intercepted** rather than assuming, accepted points **884 unchanged**, **10/10 cells byte-identical** on both committed stroke JSON and rendered path `d`, preview still animating at **75 distinct live states** (same as baseline), wall clock **2.6–3.8× faster** for styled pens with round unchanged as a **1.00× negative control**, a real trusted-mouse stroke (`page.mouse`) committing correctly in both themes, and `pageerror == 0` everywhere.
- [x] 2026-08-12 | (S196 Pass D — performance snapshot, one tool) | Hebrew_Font_Maker.html | **Pass D on the stalest D tool in the loop by a wide margin — last dedicated D was S85 (2026-07-16, 111 sessions), against dashboard S117, generator S128, trope_tutor ~S130, dictionary ~S141, flash_cards S170, torah_trainer S184+S185.** Everything from v4.x to **v5.9** — the onboarding wizard, Draw mode, the chisel nib, the partial eraser — shipped *after* that only profile, and Draw mode is the highest-risk unprofiled surface in the suite (pointer-driven canvas work). **⚑ The pass was picked against the ledger's own pointer.** S195's `**Next session**` prose said "Stalest passes now: I (2026-08-07) — the only pass older than 2026-08-08", but the rotation table's own **D row reads 2026-08-06** — a day older. The table is the authority on state and the pointer only breaks *ties*; this was not a tie. S195's own method note ("re-derive a number even when the ledger is the one that wrote it") applied to S195's pointer. **1 hit, everything else clean, with receipts.** HIT: the styled-preview quadratic (2e09c29). CLEAN: **cold load** — @1× 0 blocks >200 ms (top longtask 130 ms, DCL 303 ms, FCP 176 ms); @4× 2 blocks (478, 222 ms) on a 15,630-line single-file app, logged as inherent-boot, not a defect. **Partial eraser FLAT** — 0.9× first-to-last ratio over 600 samples, so the mask-path string append is not quadratic in practice. **Whole-stroke eraser** fine (hit-test against 100 committed strokes: first-10% 0.35 ms, *falling* to 0.05 ms as strokes are consumed). **Render pipeline** fast @4×: renderStage 4.5 ms, renderControls 19.8, renderGrids 32.4, renderLetterGrid 7.4. Growth-curve harness (4 styles × 6 stroke lengths @4×) is the receipt that separated "tail-only at the cap" from "already costing frames at n=1200" — it is what set the fix's priority.

- [x] 2026-08-10 | (S191 close-out) | branch/deploy note | `/improveloop` → S191 ran on the designated branch **`claude/improve-loop-agk18i`**, cut fresh off latest `origin/main` — the protocol's *merged* case, not the continue case: PR #160 was merged when the maintainer authorized the S187–S190 direct-to-main landing (recorded in `d8be953`), so reusing it was not an option. **Drift check, measured against the baseline that same entry prescribed (`9578bad` / `sw.js` v470 / FM 5.9), NOT against the stale `2fec8ce` figure:** `origin/main` had advanced to `07e4631` with **two outside-loop landings** — `5ec50e5` (dashboard: `settings.timeFmt` was only ever read by the big clock, so the schedule Now/Next, both previews, the week-grid cell aria-label, the duplicate-time warning and the Shabbat candle/havdalah lines all ignored the 12-hr setting; fixed with new `clockOpts()`/`fmtClock()` helpers and memo keys that include `timeFmt`) and `07e4631` (a one-line sitemap `<lastmod>` refresh). `sw.js` **v470→v471** came from that dashboard commit; `FONT_MAKER_VERSION` **5.9, unchanged**. This session: `sw.js` **v471→v472** in this final commit; precached files changed: `torah_trainer.html`, `hebrew_dictionary.html`, `trope_tutor.html`, `classroom_dashboard.html`, `locales/en.json`, `locales/he.json`. **No FM bump — the Font Maker was not touched at all** (first such session in five). Scripts run: `build-locales.js` (1 key re-worded, 4259 keys, the single remaining `he` gap is the pre-existing `worksheet.layout.title_default`), `check-i18n.js` (**Check A clean + 14 baselined, Check B clean** — re-run at close), `update-sitemap.mjs` **after `git fetch --unshallow`** (the container clones shallow, and S188's own guard now refuses rather than publishing inflated dates — the guard fired for real this session and was cleared the documented way). **Gates:** both asked in one batch at the post-pass point, both answered — see the Decisions section in this entry's siblings. **Deploy NOT verified in-session** — Pages builds on merge to `main`.
- [x] 2026-08-10 | 1e28b1d | torah_trainer.html + trope_tutor.html + classroom_dashboard.html | **The font a teacher has already chosen stops being the hardest one to read** (Pass C contrast; the maintainer's gate-2 pick for the last iteration). `.font-opt.active .font-opt-preview` renders the SELECTED option's Hebrew sample in raw `var(--gold)` on that option's own gold-tinted plate — **measured 2.35:1 (2.39:1 on the dashboard) against the 4.5:1 that 21.6/22.4px non-bold text needs**. Pointed the light-mode rule at **`--gold-text`**, the token S63 added for exactly this gold-on-light case; dark is untouched because its own `body.dark` rule (`--gold-light`, already 10.23:1) still wins. **4 sites across 3 carriers** (the dashboard has both a Hebrew and an English picker). Measured after: **5.31 / 5.31 / 5.40 / 5.40:1** light, 10.23:1 dark unchanged, 0 pageerrors. The selected state stays obvious — it is also carried by the gold border and the tinted background, so the cue never rested on the glyph colour alone. | verified: alpha-compositing contrast probe over `.font-opt.active .font-opt-preview` in all 3 files × light/dark, before AND after; screenshot of the picker confirming selected-vs-unselected still reads at a glance
- [x] 2026-08-10 | f72e0f8 | hebrew_dictionary.html + locales | **The blocked-corpus message talks to a teacher instead of a developer** (the top open candidate, gate-2 approved verbatim at S190 and deferred there for budget only — shipped with **no gate call at all**, exactly as the S190 pointer instructed). `dictionary.loading.failed` read *"Failed to load. Open via a local server (not file://)."* — setup instructions for someone running the file off their own disk, sitting on the one path a **school-network block** actually reaches. Nobody on ivritsuite.com is on `file://`, so the sentence was useless and faintly alarming. Replaced in EN + HE with the two things that can help (check the connection; ask IT to allow the domain), plus the English literal that stands in when the failure lands before the deferred `js/i18n.js` has run. | verified: corpus fetch aborted with **`serviceWorkers:'block'`** (without it the worker serves `/data/` from `DATA_CACHE`, `page.route` never sees the request and the failure path silently never runs — the S165 trap); **8/8 cells** of EN/HE × light/dark × 1280/800 show the new text in the right language with `data-i18n` re-pointed for the language-switch heal, 0 pageerrors; `check-i18n` clean
- [x] 2026-08-10 | a500b9b | torah_trainer.html | **The bulk dock stops vanishing out from under a keyboard user's own Clear** (Pass C, focus management; the S190 micro-feature's second defect). Clearing empties `ttBulkSelected`, which hid the dock — **including the button just activated** — so focus fell to `<body>` and the next Tab restarted at the top of the document: precisely the long scroll back to your place the dock was built to prevent, delivered to the one user who can least afford it. The dock now stays up (reading "0 selected") while it holds focus and retires from a `focusout` listener the moment focus leaves. **The `:focus-visible` gate is the load-bearing part:** the first draft used plain `contains(document.activeElement)` and **measurably left an empty dock sitting there after every MOUSE Clear** (Chromium focuses a button on click), i.e. it traded a keyboard fix for a mouse regression — the probe caught it and the code comment now states the corrected claim. | verified: 5 properties × light/dark × 1280/800 — keyboard Clear keeps focus on the button with the dock up; Tab away retires it and lands on the next real control; mouse Clear hides it immediately as before; the panel's Clear and leaving bulk mode still hide it at once; 0 pageerrors in all 4 cells
- [x] 2026-08-10 | 6e3a57c | torah_trainer.html | **The bulk dock's Clear button stops being an empty box** (Pass C's headline finding — **P2, on surface that shipped last session**). The dock is a navy plate; its Clear is `.btn-secondary`, whose colour is `var(--text)` — **navy in light mode**. Navy on navy measured **1:1**: the word "Clear" was not dim, it was *absent*, and the screenshot shows an empty outlined rectangle. **Dark mode was fine** (because `--text` inverts there), so the theme most teachers actually use was the broken one — and that asymmetry is why a dark-only or theme-agnostic check would have missed it. Scoped the suite's existing navy-plate idiom to the dock (literal `#fff` on translucent white, exactly what the audio bar's buttons ten lines below and the dashboard's `.settings-close` already use) rather than inventing a third treatment; the ID selector out-specifies `body.dark .btn-secondary:hover` with no `!important`. | verified: measured in all **8 cells** of light/dark × 1280/800 × EN/HE — Clear **10.21:1** light / **13.47:1** dark, Copy All 5.38/6.92, the count 9.9/13.42, nothing under 4.5:1 anywhere, 0 pageerrors; before/after screenshots
- [x] 2026-08-10 | (S191 Pass C — accessibility, one tool) | torah_trainer.html | **Its 4th dedicated C (S18 → S72 → S146 → S191) and the first over the three interactive surfaces added since: the S190 sticky bulk dock, the S188 type-a-range verse picker, and the copy bar. 7 findings, 4 fixed this session, and the two loudest *automated* signals were both harness artifacts refuted before anything was logged.** Full detail in the Candidates block above. **CLEAN dimensions, with receipts:** accessible names — **124 visible interactive controls** enumerated with every progressive-disclosure surface proven open (reading injected, copy bar open, bulk mode on, 2 verses selected, drawer open with all panels expanded: 44 of its 85 controls visible) → **0 nameless / 0 title-only / 0 placeholder-only / 0 symbol-only, in EN and HE**; focus indicators — **20/20 representative controls paint a ring in both themes**, pixel-diff verified; tab order — **64 stops** in a sensible region order (skip link → header → controls → copy bar → dock → reading → footer), no invisible stop lacking an indicator; drawer contract — opener → close button, **0/30 Tab escapes**, Escape closes, focus restored to `#gearBtn`; the dock does not steal focus when it appears; reduced motion — every motion-carrying transition already neutralized; **0 pageerrors across every probe run.** **Two false alarms, both instructive:** (1) a style-read focus check reported **35 of 64 stops with no focus indicator** — Chromium computes `outline: <color> auto 0px` for its own UA ring, so `outlineWidth` is worthless as a detector; the pixel diff refuted it 20/20. (2) six "invisible (0×0) tab stops" are the toggle-switch inputs, which are ringed via the existing `input:focus-visible + .slider` rule. | verified: 6 headless probes over local HTTP with `serviceWorkers:'block'` — name enumeration (EN+HE × light/dark), full Tab walk (both themes), pixel-diff focus ring (both themes), alpha-composited contrast (both themes), touch-target measurement, modal/dock focus contract

- [x] 2026-08-08 | (S190 close-out) | branch/deploy note | `/improveloop` → S190 **continued on the S187/S188/S189 branch `claude/improve-loop-pudobq` → the same draft PR #160**, per the branch rule (the PR was still open + unmerged at session start: `state open, draft true, merged false, mergeable_state clean`, 0 check runs as always for this repo). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `2fec8ce` (HEAD 14 ahead / 0 behind, merge-base == origin/main), `sw.js` **v469** exactly as S189 left it, `FONT_MAKER_VERSION` **5.9** unchanged. `sw.js` **v469→v470** in this final commit; precached files changed: `Hebrew_Font_Maker.html`, `torah_trainer.html`, `classroom_dashboard.html`, `hebrew_dictionary.html`, `locales/en.json`, `locales/he.json`. **FONT_MAKER_VERSION deliberately NOT bumped:** the FM work was an i18n wiring fix plus 3 captions and one JSON-LD claim — no new capability, and the rule is one combined bump per *feature* release. `node scripts/build-locales.js` ran (4,256 → **4,259 keys**, 3 added; the only untranslated key remains the pre-existing `worksheet.layout.title_default`); `node scripts/check-i18n.js` **clean** (Check A clean + the same 14 baselined, Check B clean); `node scripts/update-sitemap.mjs` ran **last** (deep clone verified — `git rev-parse --is-shallow-repository` = false, so the S188 guard passes rather than trips). Ledger hygiene: Done had reached 6 sessions, so the **S185 block moved to `docs/IMPROVEMENT_ARCHIVE.md`**. **Decisions deferred to the maintainer: one** — the dictionary `file://` copy rewrite was gate-2 APPROVED with exact EN+HE wording, then deferred purely for budget (5/5 spent on pass + micro-feature + 2 fixes); it is recorded verbatim in Candidates and needs **no re-gating**. **Deploy: VERIFIED — the maintainer authorized a direct push to `main` in-session (2026-08-09, the 2nd ledgered direct-to-main authorization after S78).** `main` was **fast-forwarded** `2fec8ce → 9578bad` (clean FF, 18 commits, no merge commit; `check-i18n` re-run green as a pre-flight), which GitHub itself recorded as merging **PR #160** (`merged_at 2026-08-09T01:55:03Z`, `merged_by Hbleiberg`) — so S187–S190 are LIVE. "pages build and deployment" run **31289240014** for `9578bad` concluded **`success`**. **Note for S191's drift check:** `origin/main` now sits at `9578bad`, 18 commits past the `2fec8ce` baseline above — that jump is *this loop's own merged work*, NOT outside-loop landings; measure S191 drift from `9578bad` / `sw.js` **v470** / `FONT_MAKER_VERSION` **5.9**. The branch `claude/improve-loop-pudobq` is now merged, so S191 cuts a **fresh branch off `origin/main`** and opens a **new** draft PR (never reuse #160).
- [x] 2026-08-08 | 3237da8 | Hebrew_Font_Maker.html + classroom_dashboard.html + hebrew_dictionary.html + locales | **Three FAQ claims re-synced with the tools they describe — and their VISIBLE twins with them.** Pass L's own findings, all gate-2 approved. FM: the HowTo enumerated "one per image, a full sheet, or an existing font file" — 3 of the 4 sources the onboarding wizard offers, omitting **Drawing mode** entirely, under a step still named "Upload your letters" when one of those sources involves no upload; the FAQ answer likewise described only uploading or scanning. Dashboard: "assign a preset and an end time to each period" is exactly the single-day format the tool now calls "your existing schedule" and offers to migrate into the **weekly grid**, which went unmentioned (the Now/Next + countdown half is still true and kept verbatim). Dictionary: the filter enumeration omitted **Browse by Theme**, shipped since July. **The scope call:** every claim has a visible FAQ twin carrying the identical stale wording in EN *and* HE, so JSON-LD was not diverging from the page — the page had fallen behind the tool; changing one half would have created a fresh JSON-LD↔visible mismatch, so 3 JSON-LD claims + 3 localized strings moved together. Verified: all blocks still parse after hand-editing (35 total, unchanged count), each claim names the shipped affordance, the old wording is gone, each JSON-LD answer agrees with its visible twin, all 3 HE twins really translated, and the visible answer renders the new string with 0 pageerror in en/light/1280 + he/dark/800 on all three pages; full static L audit re-run clean.
- [x] 2026-08-08 | 23dc799 | torah_trainer.html | **MICRO-FEATURE (gate-1 chosen): the verse count and Copy All now follow the teacher down the page.** The S180 Pass-H seed. On a full parsha the copy panel sits at the top of a ~12,500 px page, so a teacher selecting verses partway down had **no visible confirmation** and a ~9,000 px scroll back to Copy All. A compact dock pins the count + Copy All + Clear to the bottom of the viewport whenever Select-verses is on **and** ≥1 verse is selected; both buttons delegate to the existing `copyAllSelected()`/`clearBulkVerses()`. **Derived state only** — it hangs off `updateCopyTextarea()`, which every selection path *and* `applyI18n` already funnel through, so the count and its language stay right with no second listener, no new setting, nothing for AllTools or `.ivrit`, and all 4 strings reuse already-translated keys. **Two things the first cut got wrong, both caught by measuring:** (1) clearance over the sticky karaoke bar was a 96px constant copied from `.tt-hint-toast` — that bar wraps to **77/107/167px** at 1280/800/420, so the dock overlapped it at exactly the width a classroom tablet uses; a `ResizeObserver` now publishes its live height as `--tt-audio-h` (covering collapse + reflow, reading 0 while hidden), with 96px surviving only as a fallback; (2) `.btn-xs` is **22px** tall — fine in a dense settings row, not for the one control reached one-handed on a tablet — so the dock's buttons are now 34px. Hidden in fullscreen and in print. Verified against a 132-verse fixture recreating the seed: 27 checks × en/light/1280 + he/dark/800, plus 5 on a fresh profile (empty localStorage AND IndexedDB) — including proof the scenario is still real (the panel's own Copy All measured at `top:-8632` while the dock sits in-viewport), the dock's Copy All putting exactly the panel's preview text on the clipboard, no karaoke overlap at either width with the offset tracking the bar's measured height, keyboard focus on both buttons, the count re-localizing on a live switch, Clear and exiting bulk mode both retiring the dock and its page padding, no persisted dock field, 0 pageerror. **68 lines, one tool.**
- [x] 2026-08-08 | 3fbba1f | Hebrew_Font_Maker.html + locales | **The Font Maker's upload panels finally speak Hebrew.** The top open candidate, queued three sessions and taken early exactly as the S189 pointer instructed. Nine strings in the "Add Letters" panels and the Align stage were English literals inside **template literals** — the documented Check-A blind spot, so the gate never saw them. **8 sites were pure wiring against keys already authored AND translated** (the `authored-but-unreferenced` pattern, 11th instance): the candidate's 6 plus two neighbours in the same two blocks (`pick_then_box`, `autosplit_by_name`) that were the same shape — taking them now rather than leaving a second visit. Wired through the file's existing `_pT(key, fallback)` helper so a pre-`I18n.ready` render still shows English, and `applyI18n`'s `renderControls()` + `renderStage()` already re-localize every site on a live switch — no new wiring. **3 Align-stage captions had no keys at all** (gate-2 approved): the imported-SVG and loaded-from-font captions and the "No image yet" placeholder, their HE reusing the "יובא מ-SVG" phrasing three already-translated siblings established, following the `esc(_pT(...))` idiom the neighbouring stage captions use. `combined_drop_svg` **gained back its `<b>`/`<code>` emphasis in both languages** — the authored row was plain text while the shipped English rendered markup, so wiring it as-is would have silently flattened the `id="uni05D0"` instruction. `letter_drop`/`markctl_drop` deliberately **not** wired (their `{kind}` needs noun values nobody authored — logged as a candidate rather than invented). Verified against a **PRE-FIX copy of the page served side by side**: all 12 EN renderings byte-identical (the property a wiring fix must have), all 12 HE now the CSV translation, a live EN switch restoring English in place without a reload, the markup surviving the CSV round-trip, no raw `fontmaker.panels.*` on screen, 0 pageerror + 0 missing-key in he/dark/800 and he/light/1280.
- [x] 2026-08-08 | (S190 Pass L — SEO & discoverability, 5th run) | all 13 root pages | **Structurally clean for the 5th consecutive run; all 3 findings came from the one dimension a byte check cannot clear, and all 3 shipped.** Full detail in the Candidates block above. Receipts: byte check 0 changed SEO-copy lines across 3,538 insertions in 7 files; canonicals 11/11; sitemap↔canonical exact 11/11 both ways; 35 JSON-LD blocks parse; og images resolve on disk; 1 `<h1>` per page on 13/13; 0 `<img>` without `alt`; crawl-graph 11/11 from the homepage with 0 pageerror. **2 would-be findings killed by grounding** (deliberate shorter X copy on 9 pages; 404's canonical already fixed in S175) — both converted into regression guards. **⚑ The retired `JSON-LD ↔ visible-content parity` pattern is UN-RETIRED by this run** (see Pattern health) — L owns its re-checks and L found real staleness.

- [x] 2026-08-02 | (S181 close-out) | branch/deploy note | Continued on `claude/ultracode-effort-cicysb` → **draft PR #146** (open, mergeable, 0 comments/reviews/checks throughout — checked 3× during the session). **Drift check CLEAN**: `origin/main` still at `1dcd2d7`, 0 behind; sw.js v449 and FONT_MAKER_VERSION 5.6 exactly as S180 left them — nothing landed out of band. **SW v449→v450** (torah_trainer.html edited). **FONT_MAKER_VERSION untouched** — the Font Maker was not edited. Ran `check-i18n` (clean, 0 new violations; no new UI strings this session so `build-locales` not needed) and `update-sitemap.mjs` last. **Stopped at 3 of 5 iterations, deliberately:** every one of Pass G's ~24 findings is in one tool, the variety governor caps that tool at 2 iterations/session, and the gate went unanswered — so there was no authorization either to exceed the cap or to build a micro-feature, and starting another tool's fix risked leaving the repo mid-change. **Decisions deferred to maintainer (gate went unanswered):** (1) gate 1 — micro-feature SKIPPED this session (11 seeds still queued); (2) gate 2 — the transliteration empty-column fix left unbuilt because collapsing a column the teacher switched ON is a default-behaviour change; logged as a candidate; (3) which of the 5 remaining P2 print defects to take next, and whether to override the 2-per-tool cap to take more than two.

- [x] 2026-07-31 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** right after the S178 close-out, so `claude/effort-estimation-mjp6vd` was **fast-forwarded** onto `main` (verified strict fast-forward first — `git merge-base --is-ancestor`, clean tree, 11 ahead / 0 behind, no merge commit). `18852d4..50603f7`. Lands **S177 + S178 together** (passes K + C; 8 fixes; SW **v442→v444**; FM unchanged 5.6). **PR #144 auto-closed as merged**; the session unsubscribed and **deleted the pending hourly check-in trigger** rather than letting it fire against a merged PR. **Deploy verified in-session: `pages build and deployment` run 30622341556 for `50603f7` concluded `success`** (10:05:46Z). Next loop session cuts a FRESH `claude/*` branch off latest `origin/main` per the branch protocol — never reuse #144.
- [x] 2026-07-31 | (S178 close-out) | branch/deploy note | "/improveloop" → S178 **continues on branch `claude/effort-estimation-mjp6vd`, adding to the still-open draft PR #144** (open/draft/mergeable-clean, 0 check runs — no CI on this repo), per the previous-PR-open rule. **Drift check: none** — origin/main unmoved at 18852d4; sw.js v443 + FM 5.6 matched; clone still full-history from S177's unshallow. **SW v443→v444** (precached changed: `hebrew_dictionary.html`, `classroom_dashboard.html`, `flash_cards.html`, `hebrew_blend_generator.html`). **FM untouched** — no bump question. Scripts: `check-i18n` clean after every commit; no CSV keys added (0 build-locales needed); `update-sitemap.mjs` last. **Session note: the Pass C workflow died once when the session paused and was resumed with resumeFromRunId (no cached results — full re-run); two S178 commits were re-stamped for committer identity per the stop hook (5fee473→2cc6c23, 3d8a4da→4441f67).** **All 4 gate questions asked AND answered (4th consecutive zero-deferral session): gate 1 = spend on fixes (no H); no-ink key APPROVED; bulk-copy a11y names APPROVED with proposed Hebrew; skip link APPROVED with proposed Hebrew** — three approvals are recorded on their candidate lines, ready to build without re-asking.
- [x] 2026-07-31 | 2582737 | hebrew_dictionary.html | **13 nikkud color pickers + the TTS speed slider get accessible names** (S178 Pass C; the `nameless-adjacent-text-labeled` pattern, S135-precedent fix). Pickers named via the same translated `glyphVowelName` the row shows (re-localized — initColorPickers re-runs in applyI18n); slider via `aria-labelledby` at its existing translated caption (0 new copy). Verified EN+HE: 13/13 aria==visible label, Hebrew names in HE, 0 pageerror.
- [x] 2026-07-31 | f66f9d5 | hebrew_dictionary.html | **The Word Lists manager is readable in dark mode — a P2 BLOCKER fixed** (S178 Pass C): `.wm-mini` ↑/↓/✕ had no color rule → UA-black on #161c2a = **1.23:1, functionally invisible**; now `var(--text)` = 13.32. Also `.wm-root-btn` white-on-gold (2.14 dark/2.75 light) → navy (6.92/5.38, matching its own hover), and a new **`--danger-text` token** (#a93226 light/#ff8a75 dark, print block covered) replacing literal #c0392b at the Delete button, wlSetStatus errors, and #exportErr (was 2.82 dark, 4.14 marginal light → 6.67/5.05). All ratios MEASURED post-fix in both themes on the live modal with a seeded list; 0 pageerror.
- [x] 2026-07-31 | 4441f67 | flash_cards.html + hebrew_blend_generator.html + classroom_dashboard.html | **Folder-tree buttons and the Learner Ladder close meet minimum touch-target size** (the S167 P4): `.ft-move-btn` (19px) + `.ft-newfolder` (22px) get `min-block-size:24px` in the byte-identical 3-carrier block (re-hashed identical post-edit: e26407b5); the ladder ✕ (29.4×22.8) gets the 30px suite bar inline. Measured post-fix on all carriers at 1280+800: every box ≥24, ladder 30×30. **Recorded call: ft buttons target the 24px WCAG floor, not the 30px bar — dense tree rows; trivial to raise if wanted.**
- [x] 2026-07-31 | 2cc6c23 | classroom_dashboard.html | **Dashboard tip icons stop announcing stale English in a Hebrew UI** (the S170 P4's pure-wiring part; S177 grounding held exactly): wire() captured the tooltip PROSE into aria-label once at parse time and never re-synced. Converged on the 4-carrier `shared.tip.aria_more_info` + `data-i18n-aria-label` pattern (prose stays via aria-describedby). Verified EN/HE × 1280/800 × light/dark + a LIVE setLang switch receipt (labels follow to מידע נוסף); 0 pageerror. **Test lesson re-learned: I18n.setLang is async without a promise — poll documentElement.lang, don't await it.**
- [x] 2026-07-31 | (S178 Pass C — dictionary, first-ever) | hebrew_dictionary.html | **Audit summary in the rotation row.** Same-session fixes: f66f9d5 (contrast cluster incl. the 1.23:1 blocker) + 2582737 (nameless controls). Logged for S179+: the touch-target cluster (15 findings, worst = the 4px slider hit-strip), bulk-mode a11y (names APPROVED), the skip link (APPROVED), 3 segmented groups' aria-pressed, #fsBtn reduced-motion one-liner.

- [x] 2026-07-31 | (S177 close-out) | branch/deploy note | "/improveloop" (ultracode session) → S177 ran on the **designated fresh branch `claude/effort-estimation-mjp6vd`** cut from latest `origin/main` (18852d4), per the pointer's fresh-branch requirement (PR #143 merged). **Draft PR opened at close-out.** **Drift check: none** — origin/main at 18852d4 (= the recorded ledger commit), sw.js v442 + FM 5.6 matched. **Clone arrived SHALLOW again (50 commits) — unshallowed before anything else** (the S175 lesson, now twice-proven: do this at session start). **SW v442→v443** (precached changed: `Hebrew_Font_Maker.html`, `torah_trainer.html`, `locales/en.json`, `locales/he.json`). **FM version NOT bumped** — both FM iterations are localization wiring/copy, not features (S172/S173 precedent). Scripts: `build-locales` ×4, `check-i18n` clean after every commit, `update-sitemap.mjs` last. **Pass K ran as a 5-agent parallel workflow** (4 probes + census + runtime sweep, ~693k tokens) — first workflow-orchestrated pass; every load-bearing claim was re-verified inline before acting. **All 4 gate-2 questions asked AND answered at the post-pass batch (all recommendations accepted); gate 1 skipped correctly (seed queue empty — Pass H still the only intake). Nothing deferred.**
- [x] 2026-07-31 | 86788ad | torah_trainer.html + locales | **Torah side-by-side column headers speak Hebrew** (S177 Pass K find; gate-approved: new col keys, wording kept). `torah.reading.col_english`/`col_translit` ("English"/"Transliteration", HE אנגלית/תעתיק reusing the row-label Hebrew) wired into `renderSideBySide`'s h3s via the file's own `esc(I18n.t(...))` idiom; pinned עִבְרִית untouched. Verified by synthetic-verse fixture (Sefaria is route-blocked): EN/HE × 1280/800, headers correct all 4 cells, 0 pageerror.
- [x] 2026-07-31 | 20ff25e | locales/ui-strings.csv | **19 authored-duplicate rows + `family_name_default` deleted** (gate-approved both). Every deleted key re-verified referenced NOWHERE suite-wide including via the `_pT`/`_cgT` dynamic prefixes before deletion; 4103→4083 keys; FM+index loaded EN/HE post-delete with 0 missing-key warnings. The maintainer chose to keep "My Hebrew Hand" as the English default (font name-table safety), so its localization key went with the duplicates. Future censuses stop re-deriving these 20 dismissals.
- [x] 2026-07-31 | c2a2192 | Hebrew_Font_Maker.html + locales | **The metrics stage's width/left/right drag annotations speak Hebrew** (S177 Pass K find; gate-approved incl. the proposed רוחב/שמאל/ימין). 3 new `metric_label_*` keys rendered as on-stage SVG `<text>`; the stage stays pinned LTR. Receipt: `metricHandlesSVG()` output contains all 3 labels per language, 0 pageerror.
- [x] 2026-07-31 | 49e9147 | Hebrew_Font_Maker.html + locales | **Trace, punctuation and stray-label panels speak Hebrew — census slice A, 14 keys** (trace_hint/info/under_font/under_svg/svg_outline_hint, punct_on/off_hint, autodetect_btn, reset_side_positions, apply_alignment, add_glyph_sample, custom_empty [+ `dir="auto"`/`text-align:start` replacing its hardcoded ltr/left pin], letter_progress, eng_progress). **Six CSV rows had their `<b>`/`<strong>`/`&amp;` markup restored EN+HE (markup-restore instances 15–20)** — the live English carried emphasis the CSV rows had lost. Receipts: builder-level (renderTraceUnder/renderCustomGlyphsGrid/read-only `_selectItem`+`_setWorkMode('trace')` jump) + visible DOM, EN byte-identical / HE Hebrew at every reachable site, EN/HE × 1280/800 + dark, 0 pageerror, gate clean after every commit.
- [x] 2026-07-31 | 5b1db81 | docs/IMPROVEMENT_LOG.md | **S177 Pass K logged** (rotation row + census correction + 2 new candidates) — the pass's own commit, before any fix. Headline: the burndown arithmetic was stale on every component (33 true-unwired, not ~46; 19 duplicates, not 8); runtime sweep 24/24 clean with a sanity-proven collector.

- [x] 2026-07-31 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** right after the S176 close-out, so `claude/improve-loop-flfnp9` was **fast-forwarded** onto `main` (verified a strict fast-forward first — `git merge-base --is-ancestor origin/main HEAD`, clean tree, 28 ahead / 0 behind, so no merge commit). `88eb53a..5623af3`. This lands **four loop sessions at once: S173 + S174 + S175 + S176** (passes H, L, E, F; the Save-as-Word-List prefill micro-feature; the dictionary/generator confirm guards; the 28 trope names; six Font Maker localization slices; SW **v438→v442**). **Draft PR #143 auto-closed as merged** by the push and the session unsubscribed from it — do not reopen; the next loop session cuts a FRESH branch off `origin/main` per the branch protocol. Deploy: the `pages build and deployment` run for `5623af3` was watched to conclusion (see the close-out summary) — this is the first in-session deploy verification in a while, possible only because the landing was direct-to-main.
- [x] 2026-07-30 | (S176 close-out) | branch/deploy note | "rerun" → S176 **continues on branch `claude/improve-loop-flfnp9`, adding to the still-open draft PR #143** (now S173–S176). **Drift check: none** — `origin/main` unmoved at 88eb53a; sw.js v441 + FM 5.6 matched. **SW v441→v442** (precached changed: `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `hebrew_blend_generator.html`, `locales/en.json`, `locales/he.json`, `sw.js`). **FM version NOT bumped** — localization + confirm-guards, no feature. Scripts: `build-locales` ×4, `check-i18n` clean after every iteration, `update-sitemap` last. **All 3 gate questions asked AND ANSWERED (approved)** — both Pass-F divergences fixed in-session, the 28 trope names authored. **Zero deferrals for the second session running.**
- [x] 2026-07-30 | (S176 Pass F — destructive-reset consistency) | 8 surfaces | **18 of 20 destructive controls properly guarded** (confirm/askModal/udDo-undo) — an unusually consistent affordance; detail in the rotation row. The 2 divergences (dictionary color Reset; generator word-list Clear) were gate-approved and fixed the same session (06cc69b, 7c72894). View-state resets correctly stay one-click.
- [x] 2026-07-30 | 792f6c8 | Hebrew_Font_Maker.html + locales | **The wide-form panel speaks Hebrew** (authored-but-unreferenced, 11th instance): 9 keys incl. both Back buttons and the donor-placement hint whose bolded {name} lands at BOTH occurrences via I18n's global substitute; `wide_auto_hint` markup restored EN+HE (14th restore). `wide_need_trace` = authored-duplicate. Receipts on stubbed traced + untraced wide forms, EN+HE.
- [x] 2026-07-30 | 42a40a5 | Hebrew_Font_Maker.html + locales | **The trop panel's 28 missing Hebrew mark names authored** (gate-approved) — 0 missing-key warnings with the panel enabled (was 28); the `referenced-but-unauthored` founding instance closed. Detail on the candidate line above.
- [x] 2026-07-30 | 7c72894 | hebrew_blend_generator.html + locales | **The word-list Clear asks before wiping a non-empty import** (S176 Pass F divergence 2, gate-approved): the pasted/imported list persists nowhere, so a mis-click meant re-importing; empty clears stay one-click. Dialog receipts EN+HE (no-dialog-when-empty, dismiss-preserves, accept-clears).
- [x] 2026-07-30 | 06cc69b | hebrew_dictionary.html + locales | **The vowel-color Reset gets the confirm the other four carriers have** (S176 Pass F divergence 1, gate-approved): new `dictionary.advanced.color_reset_confirm` with generic wording (no scheme mention — the dictionary has no Default/TaL AM). Dialog receipts EN+HE.
- [x] 2026-07-30 | c660d21 | docs/IMPROVEMENT_LOG.md | **S176 Pass F logged** (rotation row + the survey table) — the pass's own commit.

- [x] 2026-07-29 | (S171 close-out) | branch/deploy note | "/improveloop" → S171 **continues on branch `claude/improve-loop-j3sj8w`, adding to the still-open draft PR #142** (open / draft / mergeable-clean, **0 check runs — this repo has no CI**, 0 review threads), per the protocol's "previous PR still open → same branch, no new PR". **Drift check: none** — `origin/main` still at `a011825`, HEAD 5 ahead / 0 behind, `sw.js` v435, FM 5.6, exactly as S170 left them; nothing landed outside the loop. Ran **Pass I (first-load & empty-state)**. **Iters: 1 pass + 4 fixes = 5 (full budget).** `sw.js` **v435→v436** (precached: flash_cards, index, classroom_dashboard, hebrew_blend_generator; note v435 was S170's unmerged bump on this same branch — the once-per-session rule is per session, not per branch). **No `FONT_MAKER_VERSION` bump** — the Font Maker was probed by the pass and found clean; no code touched. Scripts: `check-i18n` clean after every commit; **`build-locales` run — 2 new CSV keys** (the first new keys in five sessions); `update-sitemap.mjs` last. **All FOUR gates asked AND answered** (second attended session running): gate 1 → *skip the micro-feature, spend the budget on fixes* (**seed queue still empty; Pass H still not run** — fifth session carrying that note); gate 2a → *add 2 keys, author EN + Hebrew* (the ladder modal); gate 2b → *convert the geolocation alert to an inline message*; gate 2c → *the language-switcher `aria-label="Language"` is deliberate → won't-fix* — **REVERSED out-of-band the same day: the maintainer asked for more detail, the write-up surfaced that this attribute is consumed only by AT users (the group the "keep it recognisable" argument does not serve), and they then asked for it to follow the language. Shipped in e66add4.** **Nothing deferred to the maintainer.** **Protocol note:** no divergence found between this skill and the ledger; the rotation table was followed (I was stalest at S161).
- [x] 2026-07-29 | (S171 Pass I — first-load & empty-state) | 12 pages + 8 tools | **Pass I — 14th run, and the first over the S162–S170 surface, most importantly the out-of-band Learner Ladder and printable progress report that no first-run audit had ever seen.** **S161's gates: 24/24 cells clean** (12 pages × light/1280 + dark/800 — 0 pageerror, 0 raw i18n keys in text OR in `title`/`aria-label`/`placeholder`, 0 failed local requests). So the pass went past those gates in two directions. **(1) A new gate — click everything a new teacher can click.** In a fresh context, every visible enabled button in the default first-run state was clicked one at a time with print/navigation/native dialogs neutralised and `confirm` forced false: **8 tools, 280 buttons, 0 pageerror, 0 throws.** **(2) Empty-state quality**, verified rather than assumed: flash_cards shows "No saved profiles yet.", the active-profile select is disabled reading "— no profiles —", the report/weakness buttons are *hidden inside a hidden detail panel* rather than left as live dead ends, `readProfiles()` is safe on an absent store, and the ladder builds 12 levels with exactly 1 unlocked once a profile exists. **3 findings, all fixed this session** (ladder modal copy 4358208, geolocation alert 95c679d, generator missing-key warnings 05a1f29) plus 1 logged. **Two candidate findings were killed before they could be believed, both my own harness:** the Learner Ladder appearing to render "0 nodes" for a new user (it correctly opens the profile chooser first and returns — `#ladderList` does not exist yet), and **6 Font Maker pageerrors that were caused by my teardown removing `[id$=Overlay]` nodes between clicks** — re-run non-destructively (close via the app's own `aCloseModal`, never remove a node), Font Maker's first run is **clean**. **That is the second consecutive session where a harness artifact nearly became a finding.**
- [x] 2026-07-29 | 41629a7 | flash_cards.html + index.html | **A damaged backup hid a teacher's saved results — the S170 Pass-D leftover, closed.** An imported `.ivrit`/AllTools blob is hand-editable text; a truncated or edited one can carry a `null` result, a `results` that is a string or object, or a result whose `cards` is not an array. Each threw in `renderProfileHistory` / `collectMissedCards` / `analyzeWeaknesses` / `_reportEnter`, and the visible outcome was the worst kind: **the Results History rendered empty while the data was still in storage**, so it read as data loss. **Measured pre-fix: 5 of 8 shapes throw**, including a file holding one perfectly good result beside one `null`. **FIXED** by sanitising at the **read chokepoint** (`readProfiles()`) rather than patching four renderers — that heals a stored bad blob and an imported one in one place, and costs one pass per changed blob thanks to S170's memoization. **Deliberately non-destructive: sanitising never writes**, so a teacher's file is not rewritten merely because it was read, and only unreadable entries are skipped. `mergeProfilesBlob` also sanitises the **incoming** side (its dedupe reads `r.savedAt` off every incoming result, so one `null` would abandon the import halfway), and **index.html's `mergeFlashCardProfiles` — the second carrier — sanitises both sides**, since it can also be handed an already-corrupt store. Both use `ivritSafeAssign`, never a bare `Object.assign`, on the untrusted source. Verified light/1280 + dark/800, **22 assertions per cell, all passing**; negative control 5-of-8-throw → 0.
- [x] 2026-07-29 | 4358208 | flash_cards.html (+ locales) | **The Learning Path asked a brand-new learner to "Save results to…" before any results existed** (S171 Pass I). Because progress needs an identity, the ladder asks for a name first — but it reused the post-drill dialog, so the first thing a new user saw was a modal titled **"Save results to…"** with a **"Save"** button. `chooseProfileModal` now takes an optional `{titleKey, confirmKey}`; **the save flow's wording is the default, so that entrance is behaviourally byte-identical**, and only the ladder passes its own framing: **"Who's practicing?" / "Start"** (`מי מתרגל?` / `התחילו`). **2 new keys, EN + HE authored at the maintainer's gate-2 direction**, following the file's plural-imperative style (`status_next` is already "Start here" / "התחילו כאן") and the house American spelling ("practicing", 44:0 in the CSV). Verified EN + HE × light/1280 + dark/800: each entrance shows its own title and confirm label, the two are never the same string, `role=dialog` + initial focus in the name field unchanged, Escape still closes, and creating a profile from the ladder entrance still builds the 12-level map with exactly one level unlocked. **Method note now in the commit: `ui-strings.csv` is CRLF, and a Python text-mode read/rewrite silently converted all 4,061 lines to LF** — caught on the diffstat and redone in binary.
- [x] 2026-07-29 | 95c679d | classroom_dashboard.html | **Declining the location prompt threw up a blocking dialog** (S171 Pass I — found by clicking through as a new teacher). `alert("Could not get location: …")`; on school-managed devices geolocation is denied **by policy**, so this is a routine first-run path and CLAUDE.md's inline-validation convention says an alert is the wrong vehicle. **FIXED:** both geolocation alerts (denied, and API absent) now report **inline** under the Current Location button, reusing the existing `.schedule-validation`/`.sched-warn` styling so **no new CSS**, with `role="status"` + `aria-live="polite"` so the refusal is announced and not merely seen. The note clears on a fresh attempt and when the teacher types a city instead, so a stale refusal can't sit beside a location that now works. **0 new keys.** `setLocNote` deliberately remembers the **key**, not the rendered string, and `applyI18n` re-renders a note still on screen — otherwise this would have added a fresh instance of the ledger's **pre-ready-i18n / never-re-rendered** pattern, frozen in whichever language was active. Verified light/1280 + dark/800, **12 assertions each, all passing**, including a direct assertion on Playwright's `dialog` event that **no native dialog fires at any point**.
- [x] 2026-07-29 | 05a1f29 | hebrew_blend_generator.html | **Every generator load logged 6 missing-key warnings — a self-inflicted blind spot** (S171 Pass I, from the console on a clean first load): 5 × `[i18n] missing key: worksheet.glyphname.<x>_sf` in English and 1 × the **empty** key `worksheet.glyphname.` in Hebrew. The lock grid built its sofit entries with `name: glyphNameSofit(l.name, true)` — already localized — and the shared row builder ran that output through `glyphName()` **again**, looking up a key derived from a finished label ("Chaf sf" → `chaf_sf`; in Hebrew the localized name has no ASCII left, so the slug is empty). **Nothing was visibly wrong, and that is why it survived: `i18nName`'s idempotency fallback returns the string it was handed, so the double call hid behind its own safety net.** **FIXED:** the label travels in its own `display` field and the builder prefers it, so `glyphNameSofit` runs exactly once per tile. **The value is for the next audit — 6 permanent warnings per load are 6 places a genuinely missing key could hide, and a console sweep is exactly how passes B and I find those.** Verified EN + HE × light/1280 + dark/800: warnings 5/5/1/1 → **0**, and the visible labels are **byte-identical** to the pre-fix run.

- [x] 2026-07-29 | (S170 close-out) | branch/deploy note | "/improveloop" → S170 ran on a **fresh branch `claude/improve-loop-j3sj8w`** cut off latest `origin/main`, because S169's draft PR #141 was closed by the maintainer's authorized fast-forward of S167–S169 onto `main` — a merged/landed PR cannot carry new work. **Drift check: none** — HEAD was byte-identical to `origin/main` at `a011825`, `sw.js` v434, FM 5.6, exactly as the direct-to-main note recorded (the local `main` ref was stale at `eb5c5d6`; `git fetch` corrected it, and *that* is the check — never compare against an unfetched local ref). Ran **Pass D (performance)** on **`flash_cards.html`**. **Iters: 1 pass + 4 fixes = 5 (full budget).** `sw.js` **v434→v435** (precached: flash_cards, classroom_dashboard, torah_trainer, trope_tutor). **No `FONT_MAKER_VERSION` bump** — the Font Maker was not touched; its one finding (the custom-glyph modal) has no keys yet and was logged, not changed. Scripts: `check-i18n` clean after every commit; **`build-locales` not needed — 0 new CSV keys for a fourth straight session** (every i18n fix this session reused an already-authored, already-translated key); `update-sitemap.mjs` last. **All three gates were ASKED AND ANSWERED — the first attended session in four.** Gate 1 → *"No, spend the budget on fixes"* (so the micro-feature is skipped again and **the seed queue is still empty; Pass H still has not run** — the deadlock the last three pointers flagged is unchanged and only H breaks it). Gate 1b (which two Flash Cards fixes, under the 2-per-tool cap) → *"Perf + the self-marking contrast"*, i.e. the recommended pair; the third Flash Cards finding (malformed-import hardening) was logged as the **top P2 for S171**. Gate 2 → *"Reuse existing keys only"*, which is exactly what iterations 4 and 5 did and what kept the Font Maker modal out of scope. **Nothing deferred to the maintainer.** **Protocol note for the skill:** the ledger's rotation table is the authority and it was followed (D was stalest); no divergence found between skill and ledger this session.
- [x] 2026-07-29 | (S170 Pass D — performance, one tool) | flash_cards.html | **Pass D — `flash_cards.html`, the stalest D tool in the loop by a wide margin (last profiled S75, 95 sessions ago) and since then the recipient of ~2,000 lines of out-of-band work no D had ever seen: the Learner Ladder, the printable progress report, and the cut-out card sheet.** **ONE LARGE HIT, and it is the shape worth remembering: the tool got slower the more a teacher used it.** `readProfiles()` re-ran a reviver `ivritSafeParse` of the **entire** saved-profiles blob on **every call**, and one render of the Teacher Tools panel calls it ~2× per profile (`getActiveProfile` + `buildItemRow`, plus the history / ladder / weakness paths) — instrumented at **9 calls for 1 student, 17 for 5, 23 for 8, 47 for 20**. Measured `renderProfiles()` at 1× CPU: **32 ms (1 student) → 408 ms (5) → 2,409 ms (8 at the 50-drill cap) → 10,602 ms (20)**, with **98% of it inside the parse**; `renderLadderCard` 187 ms, `openLadderMap` 188 ms, `collectMissedCards` 86 ms at the 8-student size. Fixed 6ff3a0a. **CLEAN, with counts:** boot @1× max **67–71 ms** (well under the 200 ms bar; @4× 217–272 ms, throttled-only, matching the suite's parked pattern); `generateDeck(200)` **0.9–15.6 ms** across all six modes; `renderCurrentCard` **0.7 ms avg / 2.7 ms max** over 40 samples and `flipCard` ~0 ms; a real 12-card UI walk reached the results screen with **0 longtasks >200 ms** at 1×; `printCardSheet` at 200 cards + duplex built **46 pages / 401 cells in 4.7 ms**; `_reportEnter` 3–5 ms at ordinary profile sizes. **Two method notes.** (1) **A synchronous loop cannot drive an async UI:** the first drill harness clicked ✓Yes 200 times and reported "flip 0 ms, 0 cards played" — `markCorrect` schedules `autoAdvance` on a 220 ms timer, so nothing advanced while `showSkippedAgain` re-queued cards forever. The numbers were meaningless until the walk awaited each transition. (2) **Verify a suspicious zero before reporting it:** `_reportEnter` looked like it built nothing (`0 nodes`) — the host is `#fcReportSheet`, not `#fcReport`. A harness typo, not a defect, and it would have shipped as a false finding.
- [x] 2026-07-29 | 6ff3a0a | flash_cards.html | **Teacher Tools froze for seconds as a class's saved history grew** (S170 Pass D; the session's headline). Memoized the parse on the exact stored string: a different raw string — another tab wrote, or a write failed — misses the cache and re-parses, so a stale profile can never be served. **Every one of the 21 `readProfiles()` call sites was audited for the aliasing hazard this introduces** (a caller mutating the shared object without writing): all of them write immediately, with no early return between mutation and write — `deleteProfile` and `renameProfile` both bail *before* touching the object. `writeProfiles` adopts the object as the cache **only after a successful `setItem`** and **drops the cache on failure**, which preserves today's truthful behaviour on a quota error: the teacher keeps seeing the state that actually reached the disk, not the mutation that was lost. **renderProfiles 408→35 ms (5 students), 2,409→119 ms (8), 10,602→301 ms (20); renderLadderCard 187→0.1 ms, openLadderMap 188→0.7 ms, collectMissedCards 86→0.1 ms.** The residual 301 ms at 20 students is **one** cold parse of a 4.8 MB blob, not 47 of them. Verified with **23 assertions × light/dark × 1280/800 (92 passes, 0 failures)**: profile create / rename-with-merge / delete, result save + single-result delete, ladder record, **external-tab write**, **failed write**, import merge + replace, and a full `.ivrit` round-trip; pageerror 0 in every cell.
- [x] 2026-07-29 | 935483f | flash_cards.html | **The two buttons a child taps on every single card failed AA in light mode** (S169's designated top candidate for S170, closed). `#markYes` "✓ Yes" measured **3.55:1** and `#markNo` "✗ No" **4.42:1** against 4.5 — and sweeping *every* state for text painted in `--good`/`--bad` found **two more sites of the same class that S169 had not**: the ✓/✗ marks on the results screen, same tokens on the same soft fills. Retuned the two light tokens rather than adding scoped overrides, since both failures are the token landing on its own `-soft` companion and every other role improves under the same shift. **Solved numerically** for the smallest same-hue, same-saturation lightness drop clearing 4.5 *with margin* (4.51 was rejected as too thin): `--good` #2f8a4d→**#287542** (4.65 on `--good-soft`), `--bad` #b2363a→**#ad3538** (4.61 on `--bad-soft`, 6.28 on white). The badge role paints **white text ON** these tokens, which rises 5.51→5.66 and 6.15→6.28 — measured, not assumed. Verified by re-sweeping every text node carrying either token across **six states** (flipped card, setup, setup+profile, weaknesses, ladder map, results) × light/dark × 1280/800: **10 sites, 4 failing before, 0 after**; **dark mode was measured too and already passes at 4.77/4.95/5.09**, so its tokens are untouched. Screenshots confirm both buttons still read as red/green.
- [x] 2026-07-29 | ba73f0a | classroom_dashboard.html | **The vowel colour-picker rows in the settings drawer stayed English in a Hebrew UI — all 13 of them, label and screen-reader name alike.** The `dashboard.glyphname.*` family (13 keys) was authored and fully translated but **referenced nowhere in the file** (`grep -c glyphname` → 0, as it also is in flash_cards, while the generator, dictionary and torah trainer all use theirs). Same "authored, translated, never wired" shape as S166's `fontmaker.ad.*` and S167's letter/vowel names — and the same shape as the P3 candidate this closes, since the English lives as a `name` value inside the `VOWEL_COLOR_DEFS` object literal, which Check A cannot see. Mapped off the stable `def.key` rather than slugifying the English name, because **"H. Patach" / "H. Kamatz" / "H. Segol" do not slugify to their `hataf_*` keys** and because `def.key` is identical across both colour schemes; any miss falls back to the English name, so an unmapped entry degrades to today's behaviour, never to a raw key. `applyI18n` now rebuilds the picker so a live language switch is picked up, alongside the font selectors it already rebuilds. **0 new copy.** Verified light/dark × 1280/800: **13/13 rows Hebrew under both Default and TaL AM**, `label` always === `aria-label`, 0 raw keys on screen, reverts to English on switching back, **custom colour overrides preserved through the rebuild**, and the picker still writes overrides afterwards. **Negative control on the pre-fix file: 0 of 13 localized.**
- [x] 2026-07-29 | 7aff49f | torah_trainer.html + trope_tutor.html | **Two more controls whose translations existed but were never referenced** — one line each, 0 new copy, same class as `ba73f0a` so they share its iteration. **torah_trainer:** the "Pause between repeats" audio row was the lone un-wired row in a drawer where every sibling localizes; the text went into its **own `<span data-i18n>`** rather than `data-i18n` on the `<label>`, because the label also holds the tooltip span a `textContent` write would destroy — the shape every other label in that file already uses — and its tooltip had a translated key too (`loop_pause_tip`), wired at the same time. **trope_tutor:** the "↺ Reset all Trope Tutor settings" button in the About panel, whose `confirm()` was *already* localized via `trope.confirm.reset_all` — only the button face was missed. Verified light/1280 + dark/800 on both: each localizes, each reverts, the torah tooltip localizes with its `?` icon and the `label[for]`↔`select` association intact (4 options), the trope button keeps its `onclick`; pageerror 0 in all four cells.
- [x] 2026-07-29 | (S170 — measured i18n sweep, 8 pages) | 7 tools | **Widened check-i18n's documented blind spot by measuring instead of grepping, and the method change is the finding.** The logged P3 asked for a grep sweep of English hidden in JS **object literals**. Grepping produced **1,858 raw candidates**, and sharpening it (drop `ld+json`, hex codepoints, code-ish keys) only got to **1,474** — still dominated by legitimately-English families (glyph names with their own keys, `ashk`/`seph` trope pronunciations, font names, Hebrew examples). **That is the same untrustworthy-detector wall S166 hit with the orphaned-key census.** Replaced it with a measured detector: **set the UI to Hebrew, open every collapsed panel, and look for English left in the control chrome** (buttons, labels, options, summaries, panel titles + `title`/`aria-label`/`placeholder`/`data-tip`), with a principled exemption list for the CLAUDE.md content boundary. It finds object literals, template literals and plain-argument strings alike, and it answers the question that actually matters — *does English reach a Hebrew user?* **Result: 15 (flash_cards) + 3 (generator) + 8 (dashboard) + 14 (torah) + 2 (trope) + 30 (font-maker) + dictionary, of which 4 were fixed this session and the rest are logged as candidates below.** Worth carrying: **two whole authored+translated `glyphname` families are unreferenced** (`dashboard.glyphname.*` 13 keys — fixed here; `flashcards.glyphname.*` 45 keys — still open, held off by the per-tool cap).

- [x] 2026-07-29 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "push to main"** at the end of the S169 working day, so `claude/improve-loop-u4k2pf` was **fast-forwarded** onto `main` (16 commits, 0 behind, clean tree — no merge commit needed). This covers **S167 + S168 + S169** in one landing, and closes **draft PR #141** by landing its head. Per the loop's branch protocol this supersedes the `claude/*`-branch rule **only on explicit in-session authorization** (precedents: S78, and the S166 direct push recorded above) — the default for future sessions remains a `claude/*` branch feeding a draft PR. Versions landing: `sw.js` **v434** (from v431 across the three sessions), `FONT_MAKER_VERSION` unchanged at **5.6**. Pages deploys on push to `main`, so this is the first time in the three sessions that deploy `success` was verifiable in-session rather than after a merge.
- [x] 2026-07-29 | (S169 close-out) | branch/deploy note | "rerun" → S169 **continues on branch `claude/improve-loop-u4k2pf`, adding to the still-open draft PR #141** (now carrying S167 + S168 + S169; open/draft/mergeable-clean, 0 review comments, no CI on this repo). **Drift check: none** — 0 behind `origin/main`, `sw.js` v433, FM 5.6 exactly as S168 left them. Ran **Pass G (print & export fidelity)** on **`Hebrew_Font_Maker.html`**, stalest since S159. **Iters: 1 pass + 4 fixes = 5 (full budget).** `sw.js` **v433→v434** (precached: Hebrew_Font_Maker, generator, flash_cards, dashboard, torah_trainer). **No `FONT_MAKER_VERSION` bump** — the template change is a print fix, not a feature. Scripts: `check-i18n` clean after every commit; **`build-locales` not needed — 0 new CSV keys** (third session running); `update-sitemap.mjs` last. **Gate 1 and gate 2 both went unanswered again → unattended fallback for the third consecutive session:** micro-feature skipped (**seed queue still empty; Pass H still not run**), and reuse-only on copy — though no fix this session needed new copy, so gate 2 cost nothing. **Both recorded as deferred.** **Tool-pick note for future G runs:** `classroom_dashboard.html` is formally the only tool Pass G has never covered (tally: torah 6, dictionary 6, flash_cards 5, generator 4, trope 3, FM 1, dashboard 0) — but it has **zero `@media print` blocks and no `window.print()`**, and its only export path is the shared `.ivrit` engine, which has its own round-trip checks. Recording that judgement so a future session need not re-derive it.
- [x] 2026-07-29 | (S169 Pass G — print & export fidelity, one tool) | Hebrew_Font_Maker.html | **Pass G — the Font Maker's printable templates, the sheets a child actually writes on. 2 of 5 templates were wasting a sheet of paper each; everything else measured clean.** Target chosen as the highest-consequence export surface with only **one** prior G run ever. **Method note worth reusing:** `html2canvas` (and `jsPDF`) are CDN-blocked in the harness, so rather than stubbing the exporter the harness builds **the same page DOM `buildTemplatePageHTML` emits, at `TPL_PAGE` size, and lets the real browser rasterize it** — that is the artifact a student holds, and it is stronger evidence than a stub. **HIT:** last-page fullness measured per template — `Vowels (Nikkud)` 17 items in a 4×4 grid → **page 2 held 1 box (6% full)**; `Punctuation & More` 27 items in 5×5 → **page 2 held 2 boxes (8%)**. The stale comment `// 16 → 1 page` on the nikkud spec shows the cause: the grid was tuned for 16 marks, a 17th was added later, nobody re-tuned. **Fixed 51fa7d5.** **CLEAN, with counts:** exact **8.5:11 Letter ratio** on all five; **white paper and light-blue ink in BOTH themes** — the path hardcodes `#fff` and `TPL_INK`, so dark-mode ink bleed is impossible by construction, now confirmed empirically and by eye on the rendered PNGs; **0 clipped cells** (the `headReserve:56` assumption holds — measured header is 49px); correct cell counts; 0 pageerror. Printed-output English (title, "Write one glyph per box in DARK ink…", "page 1 of 2") is **correct by CLAUDE.md's scope boundary**, not a finding — checked rather than assumed. **Two limits stated rather than papered over:** the jsPDF page-assembly path produced no artifact offline, and that PDF is hardcoded `format:'letter'` so A4 users get a scaled Letter page. Both logged as candidates.
- [x] 2026-07-29 | 4552d6b | flash_cards.html | **Muted text failed AA on the tinted surfaces the token was never tuned against** (S167 Pass C finding, closed S169). `.ladder-card-pitch` 4.38 (light, cream ladder card), `#cardMeta` 4.11 (light, tinted card screen), `.fc-hint` 4.03 (dark, navy card face) — each below 4.5, each on a *different* background, while all three passed on the surface its comment cited. **FIXED** by retuning both tokens to clear AA on the **worst** surface they land on, **solved numerically** (smallest same-hue shift clearing every measured background) rather than guessed: light `#68758e`→`#626e85` (5.14 / 4.84 / 4.55), dark `#838b9f`→`#8d94a7` (5.77 / 5.97 / 4.54). Retuned the token itself rather than adding a scoped `--muted-text`, because the shift is only 6–8% in the same hue and the comments already document this token by its ratios — unlike `--gold-text`, which exists because gold needs a drastically different value for text than for borders. Verified by sweeping **every** directly-owned text node (not just the three sites) across setup + card + flipped-card × both themes × two viewports: **1440 measurements**, setup (258 nodes) and card (50) clean throughout. **That widened sweep found 2 pre-existing misses no earlier pass could have seen, because none had flipped a card** — logged as the top S170 candidate.
- [x] 2026-07-29 | b98ada6 | flash_cards.html | **The Weaknesses view's four tabs announced identically** (`class-only-selected-state`; the last of the three groups S167's Pass C found). Fixed in `paintTabs()`, which already loops the tabs to repaint, so one line covers the initial paint and every switch; the file's own `initPillAria()` cannot help here because these tabs are built and torn down with the overlay. **This waited two full sessions on the variety governor** — both times because flash cards had already spent its iteration slots. Verified light+dark × 1280/800 with a seeded profile carrying missed cards: exactly one tab `aria-pressed="true"` at every step, always agreeing with `.active`, correct through all four clicks. Negative control 4/4 fail pre-fix (`aria-pressed` null on every tab).
- [x] 2026-07-29 | 2322027 | generator + dashboard + torah_trainer | **The Default / TaL AM vowel-scheme buttons never said which scheme was live** (`class-only-selected-state`). `syncSchemeButtons()` now mirrors `.active` to `aria-pressed`, plus the pre-JS state in the static markup. **The S168 candidate overstated the scope — it said all four scheme tools; only three had the gap.** flash_cards routes its segmented pills through a generic `initPillAria()` that already covered them; the hand-rolled duplicate added there was **reverted**, because a second mechanism doing the same job diverges from that file's idiom for no gain. **The negative control is what caught it:** 6 of 8 runs failed pre-fix while flash_cards passed both before *and* after — the fingerprint of "already covered" rather than "broken". Also repaired mid-edit: a regex glued `aria-pressed="false"` to the next attribute on all four TalAm buttons, which would have merged them into one malformed attribute; caught before verification.
- [x] 2026-07-29 | 51fa7d5 | Hebrew_Font_Maker.html | **Two printable templates wasted a sheet of paper each** (S169 Pass G). Re-tuned the grids to restore the evident single-page intent — nikkud 4×4→**4×5**, punct 5×5→**5×6** — after measuring three candidate grids for each. **Stated plainly, this makes the boxes smaller:** nikkud 179×227→**179×179** (now exactly the Hebrew Letters template's cell, so a child writing a vowel gets the same room as writing a letter) and punct 140×179→**140×147** (still taller than the shipped trop cell, 140×124). Both stay larger than the smallest box the tool already ships. Verified by producing and visually inspecting the rendered sheets: all 17 vowels and all 27 punctuation marks now on one page, no clipping, white paper in both themes.

- [x] 2026-07-29 | (S168 close-out) | branch/deploy note | "rerun" → S168 **continues on branch `claude/improve-loop-u4k2pf`, adding to the still-open draft PR #141** (S167's; open/draft/mergeable-clean, 0 review comments, no CI on this repo → no branch restart, no new PR). **Drift check: none** — branch was 5 ahead / 0 behind `origin/main`, `sw.js` **v432** and `FM 5.6` exactly as S167 left them; nothing landed outside the loop. Ran **Pass A (recurring-pattern sweep)**, stalest since **S158**, **plus the first-ever A2** retired-pattern spot check (~12 A passes had run without one, so the "every 6th A" clause was long overdue). **Iters: 1 pass (A+A2) + 4 fixes = 5 (full budget).** `sw.js` **v432→v433** (precached: index, generator, flash_cards, dashboard, dictionary). **No `FONT_MAKER_VERSION` bump** — the Font Maker was not touched. Scripts: `check-i18n` clean (14 baselined) after every commit; **`build-locales` not needed — 0 new CSV keys all session**; `update-sitemap.mjs` last. **Gates: both asked, NEITHER answered → protocol's unattended fallback applied.** Gate 1 → **skip the micro-feature** (seed queue still empty; Pass H **not** run, so it stays empty — this is now the third session with no intake). Gate 2 → **reuse-only, no new copy**, which is why `#dashEditor`'s accessible name is still deferred. **Both recorded as decisions deferred to the maintainer.** Deploy runs on merge to `main`; Pages `success` is verified after merge.
- [x] 2026-07-29 | (S168 Pass A — recurring-pattern sweep + first-ever A2) | suite-wide | **Pass A over the S158→S167 surface — the longest un-swept delta in a while (52 commits, ~1,900 insertions across 8 tool files, dominated by `flash_cards.html` +900: the out-of-band Learner Ladder, progress report and card sheet). 2 classes hit, 8 clean, and 3 of my own harness results had to be thrown out before they could be believed.** **Method note — the clone is shallow (55 commits, oldest = the S160 close-out), so the diff-based delta only reaches S161–S167.** S158–S160 were covered instead by **current-state** detection, which is the right instrument for most of these classes anyway (a bare `JSON.parse` or an unregistered key is a property of the code as it stands, not of a diff). **HITS:** (1) **`unescaped-input / unsafe-parse` — 3 sites, first break in an 11-sweep clean streak** (fixed a81d94b, see below); (2) **`dead-feature-guard` — 1 site** (fixed 766f723). **CLEAN, with counts:** `localStorage-vs-AllTools` — **41 keys written across 13 files**, every one either registered in all three AllTools functions (21) or in the documented exempt set (9 one-time flags, 5 per-device prefs/caches, and the Font Maker's 4 deliberately-local keys) → **streak 12**; `destructive-bulk` — 27 new loop lines in the delta, **0** write or clear persisted state → **streak 12**; `ivrit-gather-gap` — all four `IVRIT_CFG` carriers checked, `apply()`/`gather()` aligned (the apparent "gaps" are the documented AllTools-bundle alias keys, e.g. `data.presets || data.flashCardPresets`); `modal-focus-trap` — **resolved at runtime, clean** (see the harness note); `symbol-only accessible name` — 0, no new icon buttons in the delta. **A2 (first ever): all four checked classes stay retired** — `slider-commit` 35 range inputs / 0 offenders; `shadowed-global` 0 duplicate `esc`/`applyI18n`/`showAppToast`/`ivritSafeParse` declarations across 8 files; `listener/interval accumulation` — flash_cards' 2:1 `setInterval`:`clearInterval` ratio is the guarded single-timer pattern (the clear runs before either assignment); `falsy-zero` — 3 suspect restore lines, **all provably unreachable with 0** (`wordsMinLen`/`wordsMaxLen` are clamped to 1–20 at the input handler, and the time-limit pills offer 30/60/120/180, no zero). **Three harness self-corrections, all caught by the standing "verify a clean result as hard as a positive one" rule:** (a) a 5-hit `localStorage` GAP list was **entirely false** — 4 were the Font Maker's documented exemption and the 5th, `hebrewDashboard_holidayCache`, is **`sessionStorage`**; my constant-indirection heuristic matched `setItem(KEY` without binding the storage object; (b) the focus-trap sweep first reported **"no visible aria-modal element" on all 5 dialogs**, because it filtered on `offsetParent !== null` and **`offsetParent` is `null` for `position: fixed`** — which these drawers are; (c) re-run, it then reported the dashboard's **student picker "LEAKS after 1 tab"**, which was also wrong: two dialogs were visible at once and the probe latched onto `frModal` (the first-run card) while focus sat correctly inside the picker. Isolated, the picker holds focus through 6 tabs. **A defect in the projected student overlay was nearly reported twice over, from the same harness, in one pass.**
- [x] 2026-07-29 | e5eb4f4 | hebrew_dictionary.html | **The vowel filter's seven group headers said "Aqua" in a Hebrew UI** (S168, executing the S167 blind-spot candidate). The headers passed `group.sound` and `group.colorHtml` into the localized template **raw**, so Hebrew read "צליל AH – Aqua" — sentence translated, colour name English. The generator maps both through `i18nName()`/`vowelColorLabel()`; the dictionary was the lone laggard, and its `applyI18n` **already** called `refreshDictVowelGroupColors()` ("re-localize the vowel-group Sound headers") with nothing localized to re-render. **FIXED:** ported the generator's two helpers verbatim (both fall back to the English source on a missing key, so a gap shows the word, never a raw key). **0 new keys, 0 new copy** — `worksheet.vowels.color.*` / `.sound.*` already existed. **The wider sweep was a NEGATIVE result worth recording:** 702 candidate values across 8 files, and every other one is source data a localized lookup already maps (`glyphName`/`glyphVowelName`), content under the `headerLang`/`dowLang` toggles (day names, weather text), or not UI at all (font families, hex, codepoints) — **the S167 Font-Maker case was isolated, not systemic**, so that candidate closes. Verified light+dark × 1280/800 × en→he→en. **The first negative control PASSED and was therefore wrong** — the template was already Hebrew pre-fix, so "contains Hebrew" proved nothing; retightened to require the translated colour words *and* the absence of the English ones, then 4/4 fail pre-fix.
- [x] 2026-07-29 | 766f723 | hebrew_blend_generator.html | **A vowel-scheme save that could never run** (S168 Pass A — `dead-feature-guard`). `setVowelScheme` ended with `if (typeof saveSettings === 'function') saveSettings();` and the generator defines `saveSettings` **zero** times — a copy of flash cards' version, where it exists and is called unconditionally. **FIXED** by replacing it with a comment rather than a silent deletion: the dead line misled in both directions, implying a persistence the generator never had, and a future reader adding `saveSettings()` would not have known this call was waiting. No user-visible change. **It does expose a real inconsistency, logged rather than fixed because it is a behaviour change (gate 2, unanswered):** TaL AM survives a reload in flash cards and reverts to Default in the generator, which has no standing-preference store for it. Verified light+dark × 1280/800 that the switcher still re-groups the picker (6 groups), re-orders the colour defs, lands in `getSettings()` and round-trips through `applySettings()`.
- [x] 2026-07-29 | 83ed5c8 | generator + flash_cards + dashboard (+ index unchanged) | **The shared `.ivrit` Automatic/Manual Input buttons never announced which was on** (`class-only-selected-state`). **The interesting part is that this was drift, not an omission:** `index.html`'s copy of the shared engine **already** carried the correct `aria-pressed` wiring and the other three carriers had been left behind — so the fix was to propagate the known-good copy, not invent one. `setIvritMode` is byte-identical across all four again (**sha 1755507c**), which the shared-block convention requires; the static markup got the matching `aria-pressed` so the state is right before the engine's first call. Verified on all four carriers, light+dark × 1280/800: exactly one button reads `aria-pressed="true"` at rest, it follows `setIvritMode` both ways, and it always agrees with `.active` (48 assertions). **Negative control fails 12 of 16 — precisely the three lagging carriers × four runs, with `index.html` still passing, which is the signature of drift rather than a missing feature.**
- [x] 2026-07-29 | a81d94b | index.html + flash_cards.html | **Three import paths had drifted off the safe JSON helpers** (S168 Pass A — `unescaped-input / unsafe-parse`, **first break in an 11-sweep clean streak on this consequence-critical class**). (1) `index.html` **`lsJSON()`** used a bare `JSON.parse` and **backs 26 call sites** — every key in `exportAllSettings` and in the AllTools `IVRIT_CFG.gather()`, all of which the import path writes; (2) `useFontEverywhere`'s `setIn` did a bare-parse read-modify-write over three import-written settings blobs; (3) flash_cards' **`mergeLadder`** used a bare `Object.assign` on its single-sided branch, reached from `mergeProfilesBlob` — an import path. **On severity, accurately: none was a live exploit** — `Object.prototype` stayed clean in every pre-fix run, because `JSON.parse` defines `__proto__` as an own property rather than assigning it. That is precisely the scenario the rule exists for ("so a future missed parse-site still can't pollute `Object.prototype`"); this restores the defence in depth. Verified by seeding a poisoned blob and driving the real import paths, light+dark × 1280/800: the hostile keys are stripped, neighbouring real data survives, the font-everywhere write-back still lands. **Negative control fails 8/8 and shows the two distinct failure shapes** — `Object.assign` leaks `constructor` as an own property while `__proto__` goes through the setter.

- [x] 2026-07-24 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "We can push to main now"** → fast-forwarded `origin/main` from `62f6c5b` to the **S152–S156** loop work (`3d325f0`; strict linear FF — `merge-base --is-ancestor` verified before pushing, all 13 commit hashes preserved, no rebase/squash). **3rd ledgered direct-to-main authorization** (after S78 and the S145–S151 one). Draft **PR #134 auto-closed as merged** by the push and the session auto-unsubscribed from its activity — so the "verify deploy after merge" line in the S152–S155 close-outs is discharged here rather than on a separate merge. `sw.js` at **v413** (precached edits across S152–S156: generator, dictionary, dashboard, flash_cards, torah_trainer, trope_tutor, Hebrew_Font_Maker, contact, privacy); FONT_MAKER_VERSION unchanged at 5.5 throughout. Loop sessions normally feed a draft PR (the S76 practice); this supersedes that by explicit direction and does **not** change the default for S157.
- [x] 2026-07-24 | 9e95399 | hebrew_blend_generator.html | **Real Words transliteration restyle actually restyles the sheet** (S156 — the S155 P2, properly fixed). Full diagnosis in the closed Candidate above: wrong renderer **plus** a stale shallow-copy cache. Extracted `rerenderCurrentSheet()` from `onQrAnswerKeyToggle` (the pre-existing deterministic re-render that already routed Real Words correctly) and taught `rebuildTranslitDisplay` to refresh `rwRenderCache.items`/`.words`. **Verified** with a stubbed transliterator so an offline style switch is observable: Real Words × light/dark × 1280/800px = 4 runs — **30/30 translit cells restyled**, answer-key page kept (2→2), words unchanged, 0 pageerrors / 0 `[translit]` warnings; standard-blends control stable across the same matrix; **QR answer-key toggle unregressed in both modes** (the extraction's risk surface). Residual (matching / word-search / crossword caches) logged as a P3 rather than claimed.
- [x] 2026-07-24 | (S156 close-out) | branch/deploy note | S156 ran **Pass K** (i18n / localization audit, stalest since S145). **CLEAN on every dimension:** `check-i18n` Check A clean (no new hardcoded literals; 14 baselined) and Check B clean (no CSV debt, no untranslated-attribute backlog). The pass's real work — probing **Check A's documented blind spot** (English built inside JS template literals or passed as plain function arguments, which the static gate cannot see) — was run over the K-unswept surface `cd0987b..HEAD` (S146–S155, 138 added HTML lines) across four shapes: template-literal `title`/`aria-label`/`placeholder`/`alt`, English as a plain arg to `showAppToast`/`showToast`/`showHintToast`/`announce`/`alert`, bare-literal `textContent`/`innerHTML`, and bare-literal `setAttribute` of a visible attribute — **all four clean**, and the two highest-risk shapes re-run **suite-wide** were clean too (range coverage sanity-checked against the known S152/S154 additions). Then fixed the **S155 P2** (above). **Branch/PR:** continued on `claude/improve-loop-7fnz9n` / draft PR #134 (still open + unmerged); the PR now carries **S152–S156**. **Drift check: clean** — `origin/main` still @ 62f6c5b, branch 11 ahead / 0 behind, `sw.js` v412 + FONT_MAKER 5.5 matching the S155 close-out. `sw.js` **v412→v413** (precached: `hebrew_blend_generator.html`). **FONT_MAKER_VERSION unchanged** — Font Maker untouched. Scripts: `check-i18n.js` clean (0 new CSV keys), `update-sitemap.mjs` last. **Decisions deferred:** none — no gate tripped (a re-render correctness fix, no copy or default-behaviour change); gate 1 still has nothing to offer (Feature seeds empty since S151).
- [x] 2026-07-24 | 0f60983 | hebrew_dictionary.html (+ generator/dashboard/flash_cards marker re-true, + CLAUDE.md) | **The dictionary's preferred toast path was dead, so it always blocked with `alert()`** (S155 Pass F — transient-feedback affordance across all 7 tools). `_noHebVoiceNotice()` reads `if (typeof showAppToast === 'function') showAppToast(m, 6000); else … alert(m);` — but the dictionary was **the one carrier of that function that didn't carry the shared app-toast block**, so `showAppToast` was never defined, the preferred branch was permanently dead, and every teacher who tapped 🔊 without a Hebrew TTS voice got a **modal alert that blocks the page**. The two sibling carriers (generator, flash_cards) have **byte-identical** notice code (sha 14b857c9; the dictionary's differs only by its own `announce(m)` line) and showed a non-blocking toast — same code, different outcome, purely because one carrier lacked the dependency. **Fix (Pass F convergence on the best existing implementation):** added the shared app-toast JS + CSS blocks to the dictionary verbatim; since that makes a **4th carrier**, the marker comment's carrier list was re-trued in all four so the blocks stay byte-identical (**JS `fcf5e4c4`, CSS `b8210cc4`** across all four). CLAUDE.md updated (4 carriers + shas, the placement rule for a page with no `.ivrit` engine, and the re-true requirement). **Rest of the affordance is healthy:** all 7 tools + `resources.html` expose transient feedback carrying `role="status"`, and every animated toast is covered by its page's reduced-motion block; differing durations (2600–8000 ms) and torah/trope's click-to-dismiss are deliberate. **Verified:** dictionary × EN/HE × light/dark × 1280/800px = 8 runs — toast created with `role="status"`, fades to opacity 1, correct localized text both languages, and **no dialog fires** (previously always did); all 4 carriers regression-tested; check-i18n clean.
- [x] 2026-07-24 | (attempted, reverted) | hebrew_blend_generator.html | **Real Words translit-restyle no-op — fix attempted and cleanly reverted** (S155 Pass F dead-guard sweep). Confirmed the bug empirically with a DOM-sentinel test, wrote the minimal fix, then found via an **isolating test on untouched code** that `renderWorksheet(false)` drops the Real Words `.answer-key-sheet` page — so the fix would have swapped a silent no-op for silently deleting the teacher's answer key. Reverted; re-logged as a **P2 candidate** with the full diagnosis and the reason the naive fix is wrong (see Candidates). A clean revert + better intelligence, per the when-a-fix-fails rule.

## Done (archived — S139 and earlier)

- [x] 2026-07-24 | (S155 close-out) | branch/deploy note | S155 ran **Pass F** (cross-tool consistency, stalest since S144) in its **primary form — one UX affordance across all seven tools** (S144 had run F as the shared-block byte-identity audit, and S154 re-verified that, so a UX-affordance comparison was the non-redundant angle). Affordance: **transient user feedback (toasts / status messages)**. Inventory: shared `app-toast` (generator/dashboard/flash_cards), torah `showHintToast`, trope `showToast`, FM `status()` → `#statusMsg`, resources `#fontToast`, index none-by-design. **All carry `role="status"`** and all animated ones are reduced-motion-covered — the affordance is in good shape. **1 real divergence found + fixed** (the dictionary dead-toast, above) and **1 P2 bug surfaced + logged** by the follow-on dead-guard sweep. **New pattern registered: `dead-feature-guard`** — see Pattern health. **Branch/PR:** continued on `claude/improve-loop-7fnz9n` / draft PR #134 (still open + unmerged); the PR now carries **S152–S155**. **Drift check: clean** — `origin/main` still @ 62f6c5b, branch 9 ahead / 0 behind, `sw.js` v411 + FONT_MAKER 5.5 matching the S154 close-out. `sw.js` **v411→v412** (precached: dictionary + the 3 marker-comment re-trues). **FONT_MAKER_VERSION unchanged** — Font Maker untouched. Scripts: `check-i18n.js` clean (0 new CSV keys — the toast reuses the existing `shared.audio.no_hebrew_voice`), `update-sitemap.mjs` last. **Decisions deferred:** none — no gate was tripped (the toast fix restores the behaviour the code already declared and matches the sibling tools, which CLAUDE.md's "no new `alert()`-driven" rule explicitly points toward); gate 1 still has nothing to offer (Feature seeds empty).
- [x] 2026-07-24 | 3718f35 | torah_trainer.html + locales/ui-strings.csv | **Custom-range verse fields get real accessible names** (S154 — closing the S146 Pass C candidate; gate-2 + Hebrew approved). `#ttCustomVEnd` had only `placeholder="to"` as its name (placeholder-as-name is a known anti-pattern and the text vanishes once the user types), and `#ttCustomVStart` borrowed the shared "Verses:" `for=` label, which doesn't say which end of the range it is. **Fix:** `aria-label` + `data-i18n-aria-label` on both, **2 new CSV keys** — `torah.custom.verse_from_aria` "First verse"/פסוק ראשון and `torah.custom.verse_to_aria` "Last verse"/פסוק אחרון (screen-reader-only; visible "from"/"to" placeholders + "Verses:" label unchanged). This unblocks what **S152 had re-scoped as blocked** — every zero-new-key route was a lateral move (the placeholder keys just restate the weak text and would *override* the start field's stronger label; `aria-labelledby` names both fields identically; the visually-hidden-span route needs an `.sr-only` utility the suite doesn't have). **Verified via the real AX tree** (not just the attribute), with the picker opened first since hidden nodes are absent from the AX tree: EN "First verse"/"Last verse", HE פסוק ראשון/פסוק אחרון × light/dark × 1280/800px = 8 runs, 0 pageerrors. `build-locales` run; check-i18n clean, Check B still no debt.
- [x] 2026-07-24 | 3d903cd | torah_trainer.html | **"HEBREW" row label dropped when Hebrew is the only row** (S154 — closing the S148 Pass G candidate; gate-2 approved, since it changes the default on-screen appearance). The interlinear renderer stamped the per-verse `.tt-rowlabel` unconditionally; the label exists to tell rows apart, so with translit **and** translation off it names nothing and just indents every verse by 42px of dead space — on screen and in print alike. (S148's handout fix hid it via CSS, but only for the handout.) **Fix:** emit the label only when ≥1 other text row is shown — 2 lines in the one renderer that has row labels (side-by-side and bible-page never had them). **Verified** with a fulfilled Sefaria-shaped response across all four toggle combinations × light/dark × 1280/800px = 16 render assertions, 0 pageerrors: Hebrew-only → 0 labels, +translit → 2/verse, +translation → 2/verse, all three → 3/verse, Hebrew text always present. (The first assertion pass was wrong, not the code — it ignored the unlabelled per-verse header row; corrected before drawing any conclusion.)

- [x] 2026-07-24 | (S154 close-out) | branch/deploy note | S154 ran **Pass E** (freshness & site-health, stalest since S143 — 14th run). **7/7 dimensions CLEAN, zero findings** — the cleanest E on record (S143 was 6/7). (1) SW precache **forward**: all 25 `CORE_ASSETS` resolve on disk. (2) SW precache **reverse**: every app-shell file listed; `i18n-test.html` correctly omitted as a non-production harness. (3) **Internal links: 372 local refs, 0 unresolved.** (4) **sitemap `lastmod` vs git: 11/11 exact.** (5) **Licenses: 13 *loaded* external origins + 5 bundled fonts, all documented** (jsDelivr→Pyodide/HarfBuzz/opentype, cdnjs→html2canvas/jsPDF, Google Fonts, gtag, esm.sh, hCaptcha, Web3Forms, Hebcal, Open-Meteo, Nominatim, files.readme.io→Sefaria logo). (6) robots.txt / CNAME / .nojekyll / sitemap.xml / manifest all present; manifest parses and every icon resolves. (7) **Doc accuracy — all CLAUDE.md counts still true**: FM 13,896 ≈ "~13,900"; 13 `hebrewBlender_lang` carriers; shared-block carriers 9/7/3/4; 26 TROPES; 12 production pages. Also **re-verified the sha-claimed byte-identity** my S152 tooltip edit could have disturbed: `ivritsuite-fonts` 9/9 (8830cd5e), `app-toast` 3/3 (**0c889973**, matching the documented sha), folder-tree JS 4/4 (63679d04) + CSS 3/3 (**7298803b**, matching the documented sha) — all intact. **Method note:** the first audit run produced **3 false-positive classes**, all caught before anything was logged — (a) the licenses check flagged all ~60 *outbound hyperlinks* in the resources directory as undocumented "deps" (they are `<a href>` links, not loaded resources) and matched by hostname when the doc documents by library **name**; (b) an `\b(href|src)=` regex matched **`data-rwsrc=`** (`src` inside `rwsrc`); (c) even with a `(?<![\w-])` lookbehind it matched a **JS statement** `src = 'imported';` inside a `<script>`. Fixes: restrict to genuinely loaded constructs with an origin→library-name map, and scan attributes in a **markup-only view**. Post-fix: 372 refs / 0 unresolved, 13 origins / 0 undocumented. **With E clean and the backlog blocked only on decisions, both open gate-2 candidates were asked and approved in one batch and shipped** (3718f35, 3d903cd), clearing the actionable backlog to zero. **Branch/PR:** continued on `claude/improve-loop-7fnz9n` / draft PR #134 (open + unmerged → protocol says continue); the PR now carries **S152 + S153 + S154**. **Drift check: clean** — `origin/main` still @ 62f6c5b, branch 6 ahead / 0 behind, `sw.js` v410 + FONT_MAKER 5.5 matching the S153 close-out. `sw.js` **v410→v411** (precached: `torah_trainer.html` + both `locales/*.json`). **FONT_MAKER_VERSION unchanged** — Font Maker untouched. Scripts: `build-locales.js` (2 new keys), `check-i18n.js` clean, `update-sitemap.mjs` last. **Decisions deferred:** none — both gates were asked and answered; gate 1 still has nothing to offer (Feature seeds empty).
- [x] 2026-07-24 | 735b110 | Hebrew_Font_Maker.html + contact.html | **Two over-length SEO strings trimmed to SERP-safe lengths** (S153 Pass L, gate-2 approved). FM `<title>` **66→58** ("Create Your Own Hebrew Fonts"→"Design Your Own Font") — at 66 the `| IvritSuite` brand suffix truncated in results; the "Free" keyword and brand suffix are kept and only the redundant second "Hebrew" is dropped. contact `<meta description>` **167→146** — dropped the trailing "help keep it running." so the payment methods (PayPal/Venmo/Zelle) survive truncation. **Scope held to what was approved:** FM's `og:title`/`twitter:title` and contact's `og:description` are separate, deliberately-tailored social copy under no length pressure → untouched (editing OG copy is its own gate-2 decision). Titles/descriptions stay English by design, so no CSV involvement. **Verified:** static re-audit (FM T=58, contact D=146, no other page's lengths disturbed) + headless FM/contact/privacy × light/dark × 1280/800px = 12 runs, 0 pageerrors.
- [x] 2026-07-24 | 1e88389 | privacy.html + contact.html | **og:image + `summary_large_image` card added** (S153 Pass L — the re-confirmed S142 candidate; gate-2 approved "Add to both pages"). Both chrome pages had og:type/site_name/title/description/url but **no og:image and no twitter:card**, so links to them shared as bare text while every tool page rendered a card — and privacy is the page schools actually circulate for FERPA review. Reuses the existing `og-card.png?v=3` (verified HTTP 200, 89,195 B) with the same width/height/alt trio the tool pages use. **Deliberately omits `twitter:title`/`twitter:description`** — X falls back to the `og:*` pair, so the card needs no copy of its own, keeping the change free of new unapproved social copy. **No CSP change** (meta tags are crawler-read, not page-fetched; CSP diff confirmed empty). **Verified:** static re-audit clears both findings with no new one introduced; headless × light/dark × 1280/800px = 12 runs, 0 pageerrors, og:image + twitter:card present in the parsed DOM.

- [x] 2026-07-24 | (S153 close-out) | branch/deploy note | S153 ran **Pass L** (SEO & discoverability, stalest since S142 — its 2nd run ever), an offline static-source audit of the 11 indexable pages. **Structurally CLEAN across every dimension:** canonicals all correct incl. the bare-root homepage; `404.html` (noindex,follow) + `i18n-test.html` (noindex) correctly excluded and absent from the sitemap; sitemap↔canonical an exact 11/11 match; **32 JSON-LD blocks all parse** and **135 FAQ/HowTo/ItemList/featureList claims all match visible content** (L owns this re-check since S142); exactly one `<h1>` per page (the S142 fix holding); crawl-graph — all 10 non-home indexable pages reachable from the homepage via static `<a href>`; every content `<img>` has alt; every og:image resolves on disk; og:url == canonical everywhere. **3 findings, all gate-2 copy, all asked and approved → all 3 fixed** (735b110, 1e88389). **Method note worth keeping:** the first audit run produced **3 false-positive classes** that were caught and eliminated before any candidate was logged — (a) a `content="…"` regex that stopped at the first apostrophe, truncating every value containing one (it reported trope_tutor's description as 40 chars when it is 153, and manufactured ~30 bogus "og:title != title" hits); (b) `<img>`/`<h1>` scans that matched tags quoted **inside JS comments** (dashboard + torah "missing alt"); (c) a strict OG **equality** test — the suite's `og:title` deliberately drops the `| IvritSuite` suffix (og:site_name carries it) and `twitter:*` is a shorter variant, so equality is the wrong test and S142's CLEAN verdict was right. Fixed the harness (quote-aware extraction, markup-only view) and re-ran before drawing conclusions. **Branch/PR:** continued on `claude/improve-loop-7fnz9n` / **draft PR #134** (open + unmerged at session start → protocol says continue, no new branch or PR); the PR now carries **both S152 and S153**. **Drift check: clean** — `origin/main` still @ 62f6c5b, branch 3 ahead / 0 behind, `sw.js` v409 + FONT_MAKER 5.5 matching the S152 close-out; no outside-loop landings. `sw.js` **v409→v410** (3 precached `<head>` edits: Hebrew_Font_Maker, contact, privacy). **FONT_MAKER_VERSION unchanged** — a `<title>` edit is not a Font Maker *feature*. Scripts: `check-i18n.js` clean, `update-sitemap.mjs` last. **Decisions deferred:** gate-1 micro-feature — still nothing to ask (Feature seeds remain empty); the torah custom-range `aria-label` P4 still needs approved Hebrew.
- [x] 2026-07-24 | 02693b6 | hebrew_blend_generator.html + hebrew_dictionary.html + torah_trainer.html + trope_tutor.html | **42 tooltip triggers exposed the raw i18n key as their accessible name** (S152 Pass B — the NEW `pre-ready-i18n` pattern). `bindTip` set `aria-label` via `I18n.t('shared.tip.aria_more_info')` at wire time, i.e. **before `I18n.ready` resolves**, so `t()` returned the key itself — and the runtime's `dictLoaded` warn-gate deliberately suppresses the `[i18n] miss` warning pre-ready, so nothing surfaced it. Nothing re-applied the attribute, so every `?` tooltip in 4 tools announced the literal string "shared.tip.aria_more_info" to screen readers, **in both EN and HE** (generator 31 / dictionary 5 / torah 5 / trope 1). The key itself was fine (translated in both locales) — purely a timing bug. **Fix:** converged on **flash_cards' already-correct pattern** (English fallback + `data-i18n-aria-label`, translated by the on-ready `applyStaticI18n` sweep), plus a guard that uses the live string when the dict is already loaded so torah's dynamically-bound שם ה׳ chip is localized too. torah/trope `bindTip` kept byte-identical (28-line diff empty, per CLAUDE.md). **Verified:** 5 tooltip carriers × light/dark × 1280/800px = 20 runs, 0 raw keys, 0 pageerrors, `aria-expanded` still toggles on click; EN "More information" / HE "מידע נוסף" on all 42; flash_cards unregressed; check-i18n clean.
- [x] 2026-07-24 | caaa0fc | classroom_dashboard.html + hebrew_dictionary.html | **Two visible counters rendered a raw i18n key instead of a count** (S152 Pass B, same `pre-ready-i18n` pattern). `updatePickerRosterCount()` (student-picker roster) and `updateExportState()` (Anki/Quizlet export count) build their label via `I18n.t()` during init, pre-`ready` — so each **replaced its perfectly good static fallback text** ("0 names" / "0 words selected for export.") with the literal `dashboard.picker.roster_count.other` / `dictionary.export.count.other`, in **both** languages, permanently (neither page's `applyI18n` re-rendered them). **Fix** per CLAUDE.md's `applyI18n` rule (static pass + a re-render of the page's JS-built dynamic content): registered both functions in their page's `applyI18n`. Both are read-only recomputes — they derive label and disabled state from current state and mutate none of it. **Verified:** light/dark × 1280/800px × EN/HE = 16 runs, 0 pageerrors, correct plurals ("0 names"/"3 names", "0 שמות"/"3 שמות"), live roster typing still updates; live EN→HE switch **with bulk mode on** now re-translates the count while preserving the selection (2→2) and bulk mode — a path that was previously stale-English too. Suite-wide raw-key scan (12 pages × EN/HE, text + 4 attributes) → **0**.

- [x] 2026-07-24 | (S152 close-out) | branch/deploy note | S152 ran **Pass B** (console & error audit, stalest since S141 — 13th run), the first B over the S142–S151 loop surface. **Error dimension CLEAN:** 24 load runs (12 pages × light/dark) + 12 HE-locale runs + 8 per-tool interactions = 0 pageerror / 0 real console / 0 `[i18n] miss` / 0 local failed requests (the only signals are the harness's own deliberately-aborted external origins on torah). **But B's added raw-key DOM scan found a 44-instance defect class that every existing gate is structurally blind to** — see the two fixes above and the new `pre-ready-i18n` Pattern-health line. Interaction fidelity was upgraded mid-pass after four probes proved hollow (selector misses, not defects): re-ran with real handles — AllTools modal open/close, the **S151 theme→Word-List micro-feature end-to-end** (colors → 22 words → all 22 selected → save modal opens), and a **real** torah reading driven by a fulfilled Sefaria-shaped response (21 words / 28 `lang="he"` spans / 19 trope-classed words / 52 `.nik` spans after enabling both colour layers). **Branch/PR:** `claude/improve-loop-7fnz9n`, draft PR (loop protocol; the S145–S151 direct-to-main authorization was one-time and is NOT carried forward). **Drift check: clean** — `origin/main` == branch tip @ 62f6c5b, no outside-loop landings since the S151 close-out; `sw.js` v408 and FONT_MAKER 5.5 both matched that close-out's note. `sw.js` **v408→v409** (5 precached files changed: generator, dictionary, torah_trainer, trope_tutor, dashboard). **FONT_MAKER_VERSION unchanged** — Font Maker was not touched this session. Scripts run: `check-i18n.js` (clean, no new violations; 0 new CSV keys were needed — both fixes reuse existing translated keys), `update-sitemap.mjs` (last step). **Decisions deferred to maintainer:** (1) gate-1 micro-feature — **nothing to ask**: the Feature-seeds list has zero open seeds (all shipped), and the protocol forbids inventing one; (2) gate-2 — the torah custom-range `aria-label` P4 needs **2 new CSV keys with maintainer-approved Hebrew** (every zero-new-key route was evaluated and is a lateral move — see the annotated Candidate), and the privacy/contact `og:image` P4 is OG copy; both left unchanged and logged rather than forced.
- [x] 2026-07-24 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main" in-session** → fast-forwarded `origin/main` from 2ab0197 to the S145–S151 loop work (linear FF, all 13 commit hashes preserved; the S145–S151 close-outs' "verify deploy after merge" now applies to this direct push instead). **2nd ledgered direct-to-main authorization** (after S78); loop sessions normally feed a draft PR (#133), which this supersedes by explicit direction. `sw.js` at v408 (dashboard + torah + dictionary precached edits across S145–S151); FONT_MAKER unchanged. GitHub Pages "pages build and deployment" runs on this push — deploy `success` verified after.
- [x] 2026-07-24 | df834b8 | hebrew_dictionary.html | **Micro-feature: one-click "Save as Word List" from Browse-by-Theme** (S151 Pass H finding, gate-1 approved). After Browse-by-Theme (e.g. colors → 22 words) there was no obvious save path — "Save as Word List" only appeared after expanding Advanced + enabling Bulk Copy Mode + Select-all-matching (3 buried steps). Added a theme-panel button (shown only while a theme is selected, toggled in onFilter) → `saveThemeAsWordList()` runs bulk mode + `selectAllMatching()` + `wlOpenSaveModal()`. Reuses `dictionary.wordlists.save_button` (0 new keys) + the registered `ivritSuite_wordLists` store (no new backup wiring) + the modal's `?wl=` Flash Cards/Generator deep-links. | verified headless fresh-profile (empty localStorage + IndexedDB) × desktop/800px × light/dark: button hidden→appears-on-theme→one-click saves the 22-word list + reveals deep-links→hides on clear, 0 pageerrors
- [x] 2026-07-24 | (S151 close-out) | branch/deploy note | S151 ran **Pass H** (teacher walkthrough / paper-cuts, stalest since S140) on **hebrew_dictionary.html** (stalest-H tool [S68], chosen to walk the never-H'd theme project; torah avoided per the S150 note). Role-played a themed-vocab-lesson prep (browse colors → save as Word List → use in flash cards/worksheet). **1 finding → 1 micro-feature shipped.** The gap: after Browse-by-Theme the "Save as Word List" path was 3 buried steps (expand Advanced + Bulk Copy Mode + Select-all-matching). **Gate 1 asked → maintainer approved "Build it this session"** → shipped a one-click theme-panel "Save as Word List" button (df834b8), reusing the existing key/store/deep-links (0 new keys/state). **Drift vs S150:** none — `origin/main` @2ab0197 (v403); PR #133 open+unmerged → S151 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart). **SW v407→v408** (hebrew_dictionary.html precached, edited). **FONT_MAKER unchanged.** Scripts: check-i18n (clean; no new keys → no build-locales), update-sitemap. **Iters:** 3/5 (1 pass + micro-feature costs 2). **Gates:** gate-1 asked → "Build it" (above); gate-2 not triggered (reused copy).
- [x] 2026-07-24 | 2c171b0 | torah_trainer.html | **Fetch failure shows the friendly empty card, not a blank reading pane** (S150 Pass I). torah_trainer auto-loads the current parsha on first visit; when that fetch fails (offline / Sefaria down) the catch only set an error status and left `#ttReading` blank — a dead pane for a first-time user. Fix: on failure, `renderEmptyReading()` (the existing "Pick a parsha to start" card) when nothing is displayed, so the error status has context; guarded on `currentVerses` so a refetch failure (translation toggle) never wipes an already-displayed reading. Reuses existing copy, 0 new keys. | verified headless (Sefaria aborted, light+dark): fetch-fail shows the card + specific error status (not blank), refetch keeps the reading, 0 pageerrors
- [x] 2026-07-24 | (S150 close-out) | branch/deploy note | S150 ran **Pass I** (first-load & empty-state, stalest since S139 — 12th run). **Comprehensively clean; 1 fix.** Fresh-context sweep (isolated storage = empty localStorage + IndexedDB) of all 12 pages × light/dark = **24 runs, 0 pageerrors / 0 `[i18n] miss` / 0 real console**. New-user paths verified: the **dictionary theme project** works for a brand-new user (14 theme counts populate, selecting a theme filters to N); generator + dictionary empty states instructive; flash_cards shows its config UI. **1 gap fixed:** torah_trainer auto-loads the current parsha on first visit, but a failed fetch (offline/Sefaria down) left `#ttReading` blank — now shows the friendly empty card + error status (2c171b0, guarded so a refetch never wipes a displayed reading). **Drift vs S149:** none — `origin/main` @2ab0197 (v403); PR #133 open+unmerged → S150 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart). **SW v406→v407** (torah_trainer.html precached, edited). **FONT_MAKER unchanged.** Scripts: check-i18n (clean), update-sitemap. **Gates:** none (reuses existing empty-state copy).
- [x] 2026-07-24 | (S149 close-out) | branch/deploy note | S149 ran **Pass D** (performance snapshot, stalest since S138) on **hebrew_dictionary.html** — first D over the dictionary theme project's new Browse-by-Theme filter (its last dedicated D was S105). **Perf-healthy; measure-only, 0 fixes.** Profiled cold-load + theme-filter + show-all at 1× and 4× CPU (corpus now 13,081 words). **@1× (real desktop): 0 longtasks >200 ms** — cold load 1376 ms wall (longtasks max 180 / sum 399); theme filter `onFilter()` 9 ms; show-all (13,081 filtered) 32 ms. The **new theme filter adds no regression** (fast + the results grid is paginated `filtered.slice(shown,shown+PAGE)`); search is already debounced (`onSearchInput`→`filterDebounced` 150 ms). **@4× throttle:** cold-load has 2 inherent >200 ms parse blocks (303/251) + a one-off show-all `onFilter` at 217 ms — throttled-only, inherent 3 MB-corpus parse, **parked** (consistent with the S105/S141-accepted dictionary cold-load). 0 pageerrors. **Drift vs S148:** none — `origin/main` @2ab0197 (v403); PR #133 open+unmerged → S149 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart). **No code change → no `sw.js` bump; ledger-only.** **FONT_MAKER unchanged.** Scripts: check-i18n (clean). **Gates:** none.
- [x] 2026-07-24 | fe8f658 | torah_trainer.html | **Handout drops the redundant "HEBREW" row label** (S148 Pass G). The Student Handout is billed as a "clean Hebrew-only printout" and forces translit/translation off, yet still stamped the per-verse `.tt-rowlabel` ("HEBREW") under every verse — a label that distinguishes nothing when only Hebrew is present. Fix: `body.tt-handout-print .tt-rowlabel { display:none !important }` in the handout print block (scoped to the handout, the only guaranteed Hebrew-only surface; the regular reading print keeps its labels — a teacher may print with translit/translation on). CSS-only, 0 new keys. | verified headless print-emulation: handout label gone, regular reading print + screen reading unaffected, 0 pageerrors
- [x] 2026-07-24 | (S148 close-out) | branch/deploy note | S148 ran **Pass G** (print & export fidelity, stalest since S137) on **torah_trainer.html** — first G over the S141 Student Handout + copy-export surface. **Print fidelity excellent; 1 fix.** Empirical audit (print-emulated the reading + handout at Letter/A4, light+dark, with Genesis 1:1–5 nikkud + te'amim + trope): dark mode reprints on light paper with dark ink (bodyBg #fff both themes), chrome hidden, side-by-side collapses to single column, verses `break-inside:avoid`, nikkud/te'amim crisp (no clipping), RTL correct, trope + nikkud-highlight both fall back to print underlines, legend prints with forced swatch colors, Letter+A4 PDFs generate. **The one blemish:** the handout ("clean Hebrew-only") stamped a redundant per-verse "HEBREW" row label → fixed fe8f658. Logged P4: the regular reading print shows the same label when Hebrew-only (default) — broader (screen too), deferred. **Drift vs S147:** none — `origin/main` @2ab0197 (v403); PR #133 open+unmerged → S148 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart). **SW v405→v406** (torah_trainer.html precached, edited). **FONT_MAKER unchanged.** Scripts: check-i18n (clean), update-sitemap. **Gates:** none (a print-CSS declutter; no copy/behavior change).
- [x] 2026-07-24 | (S147 close-out) | branch/deploy note | S147 ran **Pass A** (recurring-pattern sweep, stalest since S136). **CLEAN, 0 hits** across the 3 active consequence-critical classes over `43daf88..HEAD` — the major new surface since S136: the dictionary theme project (Phase 2 Browse-by-Theme UI + POS-aware), the trope music-staff (hand-rolled inline SVG via `createElementNS` + validated same-origin `trope_motifs.json`), the Torah feature batches (Student handout, per-verse + bulk copy, שם ה׳ detect), #132 solemn banner, my S145 dashboard holiday/Shabbat i18n, S146 torah a11y. **unescaped-input/unsafe-parse:** new innerHTML sites all escaped (dashboard `escapeHtmlDash`; generator answer-key `esc()`/`ctx.title` — confirmed by the in-code comment); 0 new `JSON.parse`; trope motifs trusted-fetch + structural-validation; trope SVG DOM-built. **localStorage-vs-AllTools:** the 1 new `setItem` writes an already-registered settings key; no new unregistered keys. **destructive-bulk:** 0 new overwrite loops (bulk-select = `Set` of verse refs). Streaks: localStorage 9→10, unescaped 5→6, destructive 9→10, all KEPT ACTIVE (consequence-critical). **0 fixes; ledger-only, no `sw.js` bump** (no precached file changed). **Drift vs S146:** none — `origin/main` @2ab0197 (v403); PR #133 open+unmerged → S147 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart, no new PR). **FONT_MAKER unchanged.** Scripts: check-i18n (clean). **Gates:** none. Top candidate `#ttCustomVEnd` (P4 a11y — needs new i18n keys) left for a future session.
- [x] 2026-07-24 | c1cd264 | torah_trainer.html | **6 settings-drawer controls given accessible names** (S146 Pass C — the S135 dashboard pattern, now on torah). The Show-cantillation / Show-nikkud / Color-code-nikkud / Color-code-trope checkboxes + the Chant/Read speed sliders sat in `<div class="form-row"><label>Text</label><label class="toggle"><input></label></div>` with the visible label lacking `for=` → nameless in the AX tree. Fix: `for=` on the 4 plain-text labels (also enlarges the click target); `id` on the inner text span + `aria-labelledby` on the input for the 2 tooltip-wrapped labels (excludes the tooltip "?" trigger from name + click). Reuses visible text, **0 new i18n keys**, names live across EN/HE. | verified headless light/dark/Hebrew: 0 nameless in the drawer, each resolves to its correct label (EN + HE הצגת ניקוד/שירה/הקראה…), 0 pageerrors
- [x] 2026-07-24 | 3a3fd85 | classroom_dashboard.html | **Jewish-calendar projected widgets wired through their staged i18n keys** (S145 Pass K — the Check-A template-literal blind spot). renderHolidayCountdown (today joyous 🎉 / solemn 🕯️ + N-days countdown) and renderShabbat (candle-lighting/havdalah) hardcoded English via template literals despite the CSV already carrying fully-translated keys (`dashboard.days.holiday_today`/`_solemn`/`holiday_countdown.one`/`.other`/`shabbat_candle`/`shabbat_havdalah`); `applyI18n` already re-rendered Shabbat "status strings via t()" + `shabbat_unavailable` was wired → governance (follow UI language) already shipped by the maintainer, only these lines left unwired (#132 flagged the banner for "a future i18n wiring"). Wired via `I18n.t()` ({title} stays escaped Hebcal content; en-US {date}/{time} stay content per CSV notes; `.one`/`.other` plural + heb-name span preserved) + added a cache-first `fetchHolidays()` re-render to `applyI18n`. No CSV/build change (keys pre-existed). | verified headless light+dark × desktop+800px: EN output byte-identical (no copy change), HE framing correct, live EN↔HE repaints, 0 raw keys leaked, 0 pageerrors
- [x] 2026-07-24 | c93d529 | hebrew_blend_generator.html + classroom_dashboard.html | **folder-tree CSS block re-unified to byte-identity** (S144 Pass F — the one confirmed drift). flash_cards carried the canonical i18n-rule-5 **logical** form (`padding-block:3px 5px; padding-inline:16px 5px` + `text-align:start`) while generator+dashboard held the stale physical form — the logical-CSS sweep had run per-page and the copy-to-carriers step was missed (drift predates the shallow-clone graft; sole pickaxe hit = the 9c8b0f3 import). Propagated flash_cards' 2 lines into both carriers → all three marker-to-marker spans re-hash to canonical `7298803b01ca7ae7`. **Verified:** LTR byte-equivalent (computed L=16/R=5/T=3/B=5), Hebrew UI now mirrors the folder indent + move-menu alignment on both pages (matching flash_cards' shipped behavior), real seeded folder render, 0 pageerrors — 38/38 checks across en+he × light+dark × 1280/800.
- [x] 2026-07-24 | c2647a1 | trope_tutor.html | **bindTip 2-line comment back-ported** (S144 Pass F, critic-caught — the audit's `identical:true` for the pair was wrong). torah_trainer's bindTip IIFE gained a mouse-only-hover rationale comment (9c8b0f3) its documented verbatim copy lacked — the only byte difference. Back-ported → 64-line diff empty, CLAUDE.md's "verbatim copy" claim true again. Comment-only; verified trope loads clean EN+HE (0 pageerrors), tooltip click-toggle works (aria-expanded + #tipFloat).
- [x] 2026-07-24 | 18029d5 | CLAUDE.md | **Shared-block sha-verified dates refreshed to 2026-07-24** (engine 3-way, app-toast JS+CSS ×3, fonts ×9, uploader ×7 — all re-verified; folder-tree JS ×4 + TROPES ×2 re-trued unchanged) + **resources.html reclassified** from "seventh font-selector tool" to store *consumer* (gallery reads `listUserFonts`; no picker/uploader) so the every-selector-carries-the-uploader rule can't read as a resources violation. Doc-only, not precached.
- [x] 2026-07-24 | c2169fb | CLAUDE.md | **Two drifted counts fixed** (S143 Pass E doc-accuracy). FM line-count `~13,400`→`~13,900` (`wc -l`=13,896 — the recurring Pass-E FM line-count drift, last bumped S89). i18n add-a-language IIFE locator `12 pages`→`13 root HTML files (the 12 production pages + the non-production i18n-test.html harness)` — `grep -l hebrewBlender_lang *.html`=13 since i18n-test.html (added out-of-band 2026-07-21) also carries the no-flash IIFE. The other three `12 pages`/`12 HTML` refs (lines 100/141/1598 = localized-pages / SEO / apple-touch-splash counts) correctly exclude the test harness (apple-touch carriers verified =12) → left as-is. CLAUDE.md not precached → no SW bump. Both fixes verified against on-disk ground truth.
- [x] 2026-07-24 | beab875 | privacy.html + contact.html | **Two `<h1>` per page → single `<h1>`** (S142 Pass L finding). Both chrome pages carried a header-brand `<h1>` **plus** the real `.page-title` `<h1>` in `<main>` (all 9 other pages had exactly 1) — a diluted main-heading signal + suite-convention divergence. **FIXED:** demoted each header-brand `<h1>` → `<div class="brand-title">` (CSS selector `header h1`→`header .brand-title`, base + responsive rules), leaving `.page-title` as the sole `<h1>`. Zero visible change (a `<div>` doesn't match `header p`, so it keeps the white Libre Baskerville brand). **Verified headless** (h1=1, brand `rgb(255,255,255)`/24px/block unchanged, tagline color intact, 0 pageerror) light+dark × desktop+800px.
- [x] 2026-07-24 | a3a423f (gate-2 approved: "Apply all 4 as drafted") | torah_trainer.html + trope_tutor.html | **Over-length SEO title/description → SERP-safe** (S142 Pass L finding). Both `<meta description>` ran ~300 chars (torah 300, trope 307 — truncated at ~155-160 in search); torah `<title>` was 89 chars (~60 shown). **FIXED:** torah title 89→59, desc 300→157; trope title 66→61, desc 307→153 — every substantive keyword preserved (parsha, transliteration, translation, vowel & trope color-coding, PocketTorah karaoke; te'amim, Ashkenazi/Sephardi names, chanted examples, drill), claims still match the UI, no stuffing; titles/descriptions stay English by design. **Verified:** new lengths, 0 duplicate titles/descriptions across the 11 indexable pages, both pages load 0 pageerror.
- [x] 2026-07-23 | (Dictionary theme project — user-directed, SHIPPED both phases) | branch/deploy note | The maintainer said "Lets do the dictionary project" → executed the greenlit 2-phase word-theme project on PR #129. **Phase 1 (f477788):** multi-agent pipeline (46 taggers / 14 adversarial reviewers / 33 miss-hunters ≈ 7.6M subagent tokens, 0 agent errors) tagged **1,392 of 13,081 entries** with a `themes:[]` field (14 kid-relevant themes); snapshot-diff proved no other field changed; `hebrew_words.json ?v=5→6` on dictionary + flash_cards + generator; **spot-check surface: `docs/theme_tagging_report.md`**. **Phase 2 (73db913):** Browse-by-Theme panel (opt-in union filter, live counts, Clear, stale-corpus auto-hide) + full state wiring (presets-free tool → lastState/AllTools ride-along + `?s=`) + 17 i18n keys (HE flagged REVIEW — pending maintainer confirm, per CSV precedent). **Verification:** 26/26 PASS across light/dark × 1280/800 + fresh-profile `?s=` round-trip + both other corpus consumers on `?v=6`; 0 pageerrors. **SW: v398→v399** (3 precached pages changed). `check-i18n` clean; `build-locales` (+17 keys); `update-sitemap` refreshed. Deploy verified after merge. _Follow-ups for the maintainer: skim `theme_tagging_report.md`, confirm the 17 REVIEW-flagged HE theme names._ **Refinement (user-directed, pushed to `main`, sw v399→v400):** the theme panel is now **POS-aware** — `updateThemeVisibility()` shows only the theme categories reachable from the currently-checked parts of speech and hides the whole panel when none are. Themes live on nouns (all 14) + color adjectives (colors only), so: Noun checked → all 14; Adjective-only → just Colors (badge = 15 adjective-colors); Noun+Adjective both off → panel hidden. Per-theme badge counts are POS-aware (= exact theme∩POS match), and a category that becomes unreachable auto-unchecks so it can't filter while hidden. Verified headless 26/26 (light+dark × the 4 POS scenarios + the auto-uncheck safety), 0 pageerrors.
- [x] 2026-07-23 | (S141 FM residual i18n — approved, shipped) | branch/deploy note | Maintainer asked what the deferred "FM residual" was, then approved the drafted Hebrew ("Go ahead" → "approve"). Wired it on PR #129 (4613f07): the **guides-panel labels** (7 labels; 7 keys pre-existed in the CSV unwired — wired the code to them, +2 net-new `guide_orient_*` keys) and the **4 harfbuzzGate shaping-check messages** (new `fontmaker.modals.shaping_fail_*` keys). This **fully retires the FM template-literal i18n blind-spot backlog** (S101/S124 candidates both DONE). Pure i18n → **no `FONT_MAKER_VERSION` bump**. **SW: v397→v398** (FM + locales precached). Verified headless EN+HE: 0 pageerror, 13/13 keys resolve + substitute, Guides panel renders Hebrew. `check-i18n` clean; `build-locales` re-run (net +6 keys after de-duping 7 pre-existing; 3958 total); `update-sitemap` refreshed. Deploy verified after merge.
- [x] 2026-07-23 | (S141 Hebrew wiring — approved drafts shipped) | branch/deploy note | Follow-through on decision #4 of the batch below: the maintainer reviewed the drafted Hebrew and replied **"Approve all"**, so the "I draft, you approve" items were wired on PR #129. **Trope staff caption (56a8226):** one sentence appended to `trope.learn.intro` (EN+HE), clearing the S140 candidate. **FM tooltips (4110ac1):** 11 template-literal sites localized via the S126 guarded pattern → 14 new `fontmaker.*` keys (EN + approved HE), clearing the S124 tooltip-backlog candidate in full and the S101 residual candidate **partially** (the 2 "Rounded…point" status sites done; guides-panel prose + 3 harfbuzzGate messages remain as a deferred residual for a follow-up). Pure i18n → **no `FONT_MAKER_VERSION` bump**. **SW: v396→v397** (trope_tutor + FM + locales precached). Verified headless (EN+HE, light+dark): both pages 0 pageerrors, all keys resolve + substitute, live tooltip renders as "שרטטו את Shin". `check-i18n` clean; `build-locales` re-run (+14 keys, 3952 total); `update-sitemap` refreshed. Deploy verified after merge.
- [x] 2026-07-23 | (S141 user-directed backlog-clear batch) | branch/deploy note | Immediately after the S141 Pass B close-out, the maintainer asked to "start clearing the backlog" and made 4 decisions via AskUserQuestion. Executed on the same branch/PR (#129): **(1) Dashboard focus rings (b0507c1)** — generic `button/a:focus-visible` gold-ring fallback (the S135 candidate's "0 rules" premise was stale; real gap was only generic buttons/links); verified deterministically light+dark. **(2) Flash Cards→Dictionary deep-link (9093907)** — empty-state link now `?wordlists=open` + a Dictionary param handler opens `wlOpenManager()` on load; no new strings; verified. **(3) Closed 9 parked-perf/limitation candidates** as accepted (measured-fine/parked — FC buildPool, dashboard/dict/FM/trope cold-load longtasks, generator exportPDF + Class-Set variance, torah renderText one-shot, FM kern-precomposed). **(4) Hebrew-gated i18n items (trope staff caption + ~15 FM tooltips): maintainer chose "I draft, you approve"** — those 3 candidates re-tagged UNBLOCKED; trope caption Hebrew drafted for sign-off, FM batch queued (NOT yet wired — awaiting approval). **SW: v395→v396** (dashboard + dictionary + locales precached). `check-i18n` clean; `build-locales` re-run (href-only CSV change); `update-sitemap` refreshed. No FM bump (no FM code yet). Deploy verified after merge.
_Older entries (S139 and earlier) archived to [`docs/IMPROVEMENT_ARCHIVE.md`](IMPROVEMENT_ARCHIVE.md)._

- [x] 2026-07-23 | (S139 close-out) | branch/deploy note | S139 ran **Pass I** (first-load & empty-state, stalest since S129) — **11th run; comprehensively CLEAN, 0 fixes, measure-only.** 12 production pages × light/dark = **24 fresh-context runs** (each an isolated Playwright context = empty localStorage AND IndexedDB): **0 pageerrors / 0 `[i18n] miss` / 0 real console errors** on load + a fresh-user interaction per delta tool. The only console output is torah_trainer's Sefaria/`hebrew-transliteration`-ESM external-fetch failures (harness route-aborts all external origins) — **gracefully handled**: fresh load shows a single visible `.tt-status.error` banner (4242-char body, not blank), the documented known-noise. **Delta surface verified fresh-user-safe** (S129 predates it): trope_tutor's outside-loop music-staff SVG renders on empty storage (MOTIFS=25 loaded, 5 staves on the default family, all-family cycle clean), and the S137 generator Name-the-Letter fix works fresh (`.ntl-ak-name` ×40, worksheet generates). **First-run affordances confirmed for a brand-new user:** generator instructive empty state ('✡︎ Ready to Generate…' + 5 quick-start recipes), dashboard STARTERS setup card visible (`hebrewDashboard_setupSeen`=null → shows), FM welcome modal present (`hebrewBlender_welcomeSeen`=null) + grids rendered — all 0 pageerror. **Iters 1/5** (measure-only; 0 open P1/P2/P3, all 17 open candidates P4-gated — no clean non-gated variety-appropriate win). **Drift:** none since S137 — `origin/main` still @ 67b3d85, PR #126 open/unmerged (branch 3 ahead); `sw.js` v389, FM 5.5. No precached file changed → **no SW bump / no sitemap / no i18n change**. Branch/PR: continues on `claude/improve-loop-8ges97` / draft **PR #126** (now S137 + S138 + S139). Deploy `success` verified after merge.
- [x] 2026-07-23 | (S138 close-out) | branch/deploy note | S138 ran **Pass D** (performance, stalest since S128) on **trope_tutor.html** — its **2nd dedicated D** (first S95), chosen to cover the outside-loop **music-staff-SVG drift** (the un-profiled 260a933 inline-SVG motif staves + 1541bec staff-highlight, the latter reverted to a **static staff** in 67b3d85). **Effectively CLEAN, measure-only, 0 fixes — the S95 baseline HOLDS and the drift introduced no regression.** Method: Playwright + CDP CPU-throttle (1×/4×), a longtask PerformanceObserver via `addInitScript`, light/dark × 1280/800. **Cold load** @1× ~1.1–1.4 s, boot longtasks max 76 ms @1× / 147 ms @4× (0 >200 — actually *better* than S95's ~385 ms @4×). **renderLearn with the new SVG staves** 0.9–2.7 ms/family @1× / ≤15.6 ms @4× (MOTIFS=25 loaded; 3–6 staves & 73–163 SVG nodes per family) — negligible vs S95's 21 ms/6-family. **20× family-cycle** 19–24 ms @1× / 112–114 ms @4×, DOM stable at 762 nodes (no accumulation — renderLearn clears `#tuCards`). **Drill** ≤11.8 ms @1× / ≤66.6 ms @4×. **Idle 3 s** = 0 rAF scheduled / 0 node delta (projector-stable); the `_clipTick` audio watcher is rAF-gated to active playback only (guarded self-terminate, torah `_verseEndStopAt` pattern) and `auditionMotif` is debug-only fire-and-forget Web Audio (no loop). 0 pageerrors all configs. **Iters 1/5** (measure-only; 0 open P1/P2/P3, all 17 open candidates P4-gated [parked-perf/needs-Hebrew/design-calls/low-value], the 1 GREENLIT seed is a fenced `data/` project — no clean non-gated variety-appropriate win). **Drift:** none since S137 — `origin/main` still @ 67b3d85, PR #126 open/unmerged (branch 2 ahead); `sw.js` v389, FM 5.5. No precached file changed → **no SW bump, no sitemap/i18n change** (measure-only). Branch/PR: continues on `claude/improve-loop-8ges97` / draft **PR #126** (now carries S137 + S138). Deploy `success` verified after merge.
- [x] 2026-07-23 | (S137 close-out) | branch/deploy note | S137 ran **Pass G** (print & export fidelity, stalest since S127) on **hebrew_blend_generator.html** (torah_trainer needs a synthetic fixture — Sefaria unreachable headless; flash_cards has no print surface). **1 real finding fixed, verified via produced artifacts.** Method: Playwright print-emulation across 11 worksheet-type configs (reading/inverse/fillvowels/gematria/bingo/nametheletter/tracing/matching + realwords reading/wordsearch/crossword) × light/dark, scanning for dark-bg ink-bleed + faint gold-on-white text, PLUS real `page.pdf()` at **Letter + A4** for reading/gematria+AK/bingo/ntl+AK. **Fix (d25777d):** the Name-the-Letter answer key rendered each letter-name answer with inline `color:var(--gold)` — the lone answer type using inline gold, so it escaped the print block's gold-darkening rule (`.gematria-value/.blend-answer/.rw-translation/.ws-wordbank-tn/.cw-clue-num → #6b4e12`) that every other answer value follows, printing ~40 faint gold answers (L≈150) per teacher answer-copy. Moved the color to a `.ntl-ak-name` class + added it to the print list; screen stays gold (L=150 light/171 dark), print darkens to #6b4e12 (L=80) both themes, faint-in-print 40→0, 0 pageerrors across light/dark × 1280/800. **Verified non-issues:** answer-key-banner navy (documented-intentional print re-light), bingo-card-header navy (literal `#1a2744`, identical both themes → intentional card design, not a dark bleed), frozen `#genFab` footer (child of `.sidebar` → hidden in print), pagination + A4/Letter sizing (identical page counts L/A4: reading 1, gematria 2, bingo 11, ntl 2 — fluid `.sheet`/`.worksheet` `width:100%`, no clipping). **Iters 2/5** (1 pass G + 1 fix; stopped — backlog gated [P4 parked/needs-Hebrew/design-calls], the one new P4 subtitle finding low-value/broad). **Drift:** PR #124 MERGED since S136 (was open); +4 outside-loop **Trope Tutor** commits landed (motif builder + `data/trope/trope_motifs.json`, music-staff SVG on Learn cards, staff-note highlight+debug mode, first-click fix + 2 shifted-aliyot correction) — fresh, un-swept surface; `sw.js` **v386→v388** (maintainer), FM **5.5** unchanged; this session **v388→v389** (generator HTML). Branch: fresh `claude/improve-loop-8ges97` off merged `main` → new draft PR at close-out. `check-i18n` clean (CSS-only, 0 new strings); `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-22 | (S136 close-out) | branch/deploy note | S136 ran **Pass A** (recurring-pattern sweep, stalest since S126) — **CLEAN, 0 hits** across all 3 active consequence-critical classes (unescaped-input/unsafe-parse, localStorage-vs-AllTools, destructive-bulk) over the delta `0ccaa7c..HEAD` = the never-A-swept surface since S126 (design-call batch c1591a8 + S127/S129 dictionary print/reorder + frozen-footer generator + S132 flash_cards + S135 dashboard a11y). **Detection:** delta-diff signature greps (`setItem` / `innerHTML`+`insertAdjacentHTML`+`outerHTML` / `JSON.parse`+`Object.assign` / bulk-write loops) all empty + eyeball of the two new render paths: dictionary `paint()` renders stored `w.word`/`w.translation` via `wlEl`'s `e.textContent` (verified helper; call sites pass only trusted style/lang/type/title props), flash_cards `announce()` writes `msg` via `s.textContent`, FM `labelStage` sets an inert `aria-label` from trusted `I18n.t`; `wlMoveWord` is a bounded 2-element swap on the already-registered `ivritSuite_wordLists` (0 new keys, not a bulk overwrite); frozen-footer generator is CSS + IntersectionObserver only. All 3 classes stay **ACTIVE** (consequence-critical, never auto-retire). **0 new candidates, 0 fixes.** **Iters:** 1 pass (A) = **1/5** (stopped — backlog fully gated: all open candidates P4 parked/needs-Hebrew [FM tooltips], both feature seeds gated [dictionary theme = `data/` project, torah handout = Sefaria-unverifiable]; the one non-gated-ish P4, the S135 dashboard `:focus-visible`, is deliberately parked — UA default works and a whole-file focus restyle risks regressions). **Drift:** none since S135 — `origin/main` still @ `1c25cab`, my branch 4 ahead / 0 behind, so **PR #124 still open/unmerged**; sw **v386**, FM **5.5**. **Branch:** continues on `claude/improve-loop-m5oupe` / draft **PR #124** (still open → no branch restart, no new PR). **No `sw.js` bump** (ledger-only, no precached file changed); no `FONT_MAKER_VERSION` bump; `check-i18n` N/A (no code); `update-sitemap` no-op (no content change). Deploy `success` verified after merge. (Pass A joins the plateau: B/E/F/K/A all clean since the design-call batch — but S135's dedicated C found 3 real fixes, so dedicated one-tool passes still deliver.)

- [x] 2026-07-22 | (S135 close-out) | branch/deploy note | S135 ran **Pass C** (accessibility, stalest since S125) — **first-ever dedicated C on classroom_dashboard.html** (S125 was Font Maker; picked the stalest NON-FM tool). **a11y-strong on structure, one systemic naming gap fixed.** Modals solid (settings drawer `role=dialog`+`aria-modal`+`_settingsTrapKey` trap + focus-in/restore + Escape + backdrop; pick-overlay / `ivritAskMode` / tour all keyboard-accessible), reduced-motion complete, UA focus outline intact. **Ground-truth AX-tree enumeration: 33/33 visible form controls nameless** — the toggle-switch/slider/color-picker rows kept their descriptive text in un-`for=`'d labels. **3 fixes → 0 nameless interactive controls:** (1) `nameToggleSwitches()` aria-labelledby for all 39 settings toggles (f9f3af5), (2) aria-labelledby on `#zoomSlider`/`#ttsRateSlider` (354e972), (3) `aria-label` on all 13 nikkud color pickers (f8e1edd) — **all reuse existing visible text → 0 new i18n keys**, names follow a language switch live (verified EN→HE). Verified headless light/dark × desktop/800px, `pageerror==0`, both vowel schemes. **Logged P4 parked:** no custom `:focus-visible` (UA default suffices; a whole-file focus restyle risks regressions), text inputs are placeholder-named (localized). **Iters:** 1 pass (C) + 3 fixes = **4/5** (stopped — 0 nameless reached, rest P4/parked). **Drift:** none — branch `claude/improve-loop-m5oupe` even with `origin/main` (HEAD `1c25cab` = S134 close-out; sw **v385**, FM **5.5**); the prior loop **PR #123 has MERGED** (no open PRs), so this is a correctly-fresh branch off latest `main`. **SW v385→v386** (classroom_dashboard.html precached, bumped once in the final commit). **No `FONT_MAKER_VERSION` bump** (no Font Maker change). `check-i18n` clean (no new strings — fixes reuse existing text/variables); no `build-locales` (0 CSV keys added); `update-sitemap` run (dashboard content changed). **Branch:** `claude/improve-loop-m5oupe` → new draft PR at close-out. Deploy `success` verified after merge.
- [x] 2026-07-22 | (S134 close-out) | branch/deploy note | S134 ran **Pass K** (i18n/localization audit, stalest since S124) — **Check A + B clean, 0 fixes**: the automated gate is green (14 baselined), and a JS-template-literal blind-spot probe over the recently-changed surface (the design-call batch + S131–133 + the merged frozen-footer generator work) found it fully localized — the frozen-footer `#genFab` button carries `data-i18n` + `data-i18n-title` + a guarded fallback; the one `.textContent='CARD'` hit is a `visibility:hidden`/`aria-hidden` print-alignment **spacer**, never visible (false positive). Broad re-sweep: **0 bare-English `alert`/`confirm` across all 6 non-FM tools** (S124 claim holds); dashboard TTS-drift surface has 0 bare-English chrome. The only i18n backlog left is the FM template-literal tooltips that need **new maintainer-authored Hebrew** — explicitly deferred in the design-call review, so no non-gated fix. **Iters 1/5.** **Drift:** none since S133 (main `e9234cc`; PR #123 unmerged). No sw bump / no code (ledger-only). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123. (4th consecutive clean pass — B/E/F/K.)
- [x] 2026-07-22 | (S133 close-out) | branch/deploy note | S133 ran **Pass F** (cross-tool consistency, stalest since S123) on the **share / shareable-config affordance** (first dedicated F sweep of it) — **CONSISTENT, 0 fixes**: generator + dictionary have **converged** `?s=` URL-share (identical pattern — `serialize`→`history.replaceState`→clipboard w/ execCommand fallback→button-flip-to-"copied"→`announce()` SR→1500 ms restore, both localized + keyboard-operable); flash_cards' paste-in share-code is the grandfathered coexistence the Shared-UX rule allows; FM shares a font file (a distinct affordance); dashboard/torah/trope carry **no** shareable-config state (device-local/content-driven) so no share affordance is appropriate — no laggard. `#shareNotice` confirmed live (auto-restore banner, L12379), not vestigial. Only a cosmetic icon/wording diff (📋 "Copy shareable link" vs 🔗 "Copy link") — a maintainer wording call, not a defect. **Iters 1/5**, stopped — no non-gated candidate (backlog all P4 gated; the one actionable-with-drafted-Hebrew item, FM tooltips, was explicitly deferred by the maintainer in the design-call review). **Drift:** none since S132 (main `e9234cc`; PR #123 unmerged). No sw bump / no code (ledger-only). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123.
- [x] 2026-07-22 | (S132 close-out) | branch/deploy note | S132 ran **Pass E** (freshness & site-health, stalest since S122) — **mechanical CLEAN**: `update-sitemap` no-op (all `<lastmod>` == git); SW `CORE_ASSETS` 25/25 resolve + all 12 production pages precached; **0 broken internal links** (12 pages scanned; the only regex hits were `data-rwsrc` attrs + JS template literals); `THIRD_PARTY_LICENSES.md` covers every CDN dep + all 5 bundled fonts (Frank Ruhl, Lakhish, Reuben, TzviScript, TzviScriptStrokeGuide, all used in resources.html); sitemap `<loc>` = 11 (all prod pages bar 404; i18n-test/404 correctly excluded); 3/3 manifest icons resolve. **1 fix (4af7b78):** completed the flash-cards a11y-tail candidate — `announce()` now sets `lang="he"` for Hebrew content so the card-face SR announcement is voiced in a Hebrew voice (verified headless: Hebrew face→`lang="he"`, translit→cleared, pageerror 0 light/dark × desktop/800px). **Iters:** 1 pass (E) + 1 fix = 2/5. **Drift:** none since S131 (main `e9234cc`; PR #123 unmerged). **SW v384→v385** (flash_cards.html precached). `check-i18n` clean (no new strings). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123. Deploy verified after merge.
- [x] 2026-07-22 | (S131 close-out) | branch/deploy note | S131 ran **Pass B** (console & error audit, stalest since S121) — **11th run, comprehensively CLEAN**: 24 load runs (12 pages × light/dark) + one interaction per tool (index gear→modal, generator `generate()`+frozen-footer button, dashboard drawer, flash_cards Start→flip, dictionary search, torah settings, trope drill, FM `.ltile`→Alef) = **0 pageerror / 0 `[i18n] miss` / 0 failed-local / 0 non-noise console** on load AND after interaction, including the drift surface (merged frozen-Generate-footer generator work + the out-of-band FM/flash_cards design-call batch). **0 new candidates, 0 fixes** — backlog fully gated (all open candidates P4: needs-Hebrew / parked-perf / design-calls; both feature seeds blocked — dictionary theme = `data/` project, torah handout = Sefaria-unverifiable; flash_cards+FM at the variety cap from the same-session batch). **Drift check:** PR #122 (S126–S130) + the user-directed frozen-Generate-footer work **merged to `origin/main`** since S130 (main `e9234cc`; sw `v383`, FM `5.5` unchanged). No new `sw.js` bump this pass (ledger-only; the design-call batch already bumped v383→v384). **Branch:** runs on directed `claude/improvement-loop-decisions-3p7c9z` / draft **PR #123** (diverges from the merged loop branch `claude/improve-loop-5xuuei`/#122 — a task-directive branch, flagged). `check-i18n` clean. Deploy verified after merge.
- [x] 2026-07-22 | c1591a8 (+ sw v383→v384 in the ledger/sw commit) | Hebrew_Font_Maker.html + flash_cards.html (+ locales) | **Maintainer design-call batch (out-of-band — a directed review of gated candidates, NOT a numbered rotation session; no discovery pass, session counter untouched).** Resolved 3 gated a11y/copy candidates + accepted 1 WON'T-FIX: (1) FM edit-canvas accessible names (`labelStage` aria-label on `#stage`/`#meStage`/`#adStage`, no role change); (2) FC screen-reader card announce (wired the idle `#a11yStatus` region in `renderCurrentCard`/`flipCard`); (3) FC per-card results auto-expand on mistakes; (4) FC practice-button rename ("This Drill's Mistakes" / "Weak Cards", EN+HE); (5) FM ccmp non-dagesh adjacency → accept-as-is (documented limitation). Dictionary theme-filter seed **greenlit + scoped** (deferred — Phase-1 corpus tagging is a `data/` project). Verified headless: EN+HE, light+dark, desktop+800px, `pageerror==0`, both results paths; `check-i18n` clean, `build-locales` run. | branch `claude/improvement-loop-decisions-3p7c9z`
- [x] 2026-07-21 | 37125a9 | hebrew_blend_generator.html (+ sw.js) | **Frozen "Generate" footer button (user-directed — "Let's do that floating generate button on the bottom, frozen cell style"; resolves the S130 Pass-H P4).** The primary `#generateBtn` sat at the bottom of the ~2693px controls sidebar with no always-visible variant. Added a **`position: fixed`** "⬡ Generate" footer pinned to the viewport bottom-left (sidebar width, 320px) on desktop, so the primary action is always one click away; the sidebar gets `padding-bottom` so no content hides behind it. An **IntersectionObserver** hides the frozen footer whenever the inline `#generateBtn` is on screen (the two never show at once). Desktop-only (mobile keeps the existing `.mobile-fab` bar); reuses `worksheet.actions.generate_mob`/`.generate` keys (**0 new keys**); no persistent state (no preset wiring); opacity transition neutralized in the reduced-motion block. **Verified headless (light/dark × 1280/800, mobile 600, `pageerror==0`):** visible at page-top, clicking it builds a worksheet, hides when the inline Generate scrolls into view + reappears on scroll-up, `display:none` on mobile. `sw.js` v379→**v380**. **Landed on branch `claude/improve-loop-5xuuei` / PR #122.**
- [x] 2026-07-21 | (S130 close-out) | branch/deploy note | "Rerun" → S130 **continues on branch `claude/improve-loop-5xuuei`, adding to the still-open draft PR #122** (S126–S129; open + unmerged, verified via the self-check-ins → no branch restart, no new PR). **Drift check: no outside-loop change** since S129 — branch head @ `96eba05`, `sw.js` **v379**, `FONT_MAKER_VERSION` **5.5**; PR #122 open/mergeable, no CI checks, 0 review comments. **Pass run: H** (teacher walkthrough / paper-cuts — stalest since S120; **hebrew_blend_generator.html, its 2nd H [last S56, 74 sessions ago = the stalest-H tool]**). **Measured mature/well-onboarded, 0 clean fixes.** Role-played a teacher building a worksheet end-to-end (headless, fresh context, light/dark): excellent empty state (clear instruction + 5 one-click ready-made starters + tour), sane defaults (2-letter, 40 blends, Letter), logical top-to-bottom selector flow with Generate at the natural end, **live-preview auto-updates after the first generate** (verified blendCount 40→20 re-render, no click), Real-Words has a built-in word bank (no dead-end), `pageerror==0`. **1 minor friction logged (P4, design-call):** the first-generate `#generateBtn` sits at the bottom of the ~2693px sidebar with no sticky variant — well-mitigated (starters + instruction + live-preview + selectors-end-at-Generate), and any fix (sticky/floating or empty-state Generate button) is a maintainer UI-design choice → **log, don't force**. Budget **1 pass = 1 of 5**; **stopped early** — tool mature, and the rest of the fix-backlog is gated (design-calls / needs-new-Hebrew-keys / parked-perf / unverifiable-headless) with no clean variety-appropriate win. **`sw.js` NOT bumped (stays v379) — no precached file changed** (ledger-only session). **No `FONT_MAKER_VERSION` bump.** `check-i18n` clean (no UI strings); no `build-locales`; `update-sitemap` no-op. Deploy `success` verified after the maintainer merges PR #122.
- [x] 2026-07-21 | (S130 Pass H — teacher walkthrough / paper-cuts, one tool) | hebrew_blend_generator.html (stalest pass, last H S120 flash_cards; generator's 2nd H, last S56) | **Pass H — generator mature/well-onboarded; 0 clean fixes, 1 P4 design-call logged.** Role-played a fresh teacher building a worksheet end-to-end (headless over local HTTP, light 1280 / dark, fresh context). **Excellent onboarding:** "Ready to Generate" empty state with a clear instruction + **5 one-click ready-made starters** (Kamatz & Patach drill, Class Bingo, Alef-Bet, Fill-in-the-Vowels, Numbers 0–20) + tour link; sane defaults; logical top→bottom selector flow (Mode → Layout → Letters → Vowels → Generate at the natural end). **Live-preview confirmed:** after the first generate, every tweak auto-updates (blendCount 40→20 → 20 cells, no click); Real-Words mode has a built-in word bank (generated "Hebrew Word Practice" immediately — no saved-list dead-end); `pageerror==0`. **1 friction (P4, design-call, logged):** first-generate `#generateBtn` at the bottom of a ~2693px sidebar, no sticky variant — well-mitigated, any fix is a UI-design choice for the maintainer. Since the S56 first-H the generator gained the chunked build (S61), print work (S30–S34/S116), and the `?wl=` deep-link (S121); the UX held up excellently. All 7 tools now H'd ≥1×: flash_cards S34/S120, dashboard S46/S107, generator S56/S130, dictionary S68, torah S77, trope S87, FM S97.
- [x] 2026-07-21 | (S129 close-out) | branch/deploy note | "Rerun" → S129 **continues on branch `claude/improve-loop-5xuuei`, adding to the still-open draft PR #122** (S126–S128; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** since S128 — branch head @ `581ae54`, `sw.js` **v378**, `FONT_MAKER_VERSION` **5.5**. **Pass run: I** (first-load & empty-state — stalest since S118; **comprehensively CLEAN, 10th run**). All 12 pages × light/dark = **24 fresh-context runs** (empty localStorage + IndexedDB): **0 pageerrors / 0 `[i18n] miss` / 0 real console errors**. Empty-state/new-user probes all pass — generator fresh Generate→1 sheet + `rwSource='lists'`→`#rwListEmptyState`; dashboard S119 message placeholder; flash_cards `startPractice`→card; **the S127 dictionary `beforeprint`/`afterprint` handlers are safe on a fresh load** (globals array-init); FM fresh `newProject`→grids; trope Learn view+cards. The one torah console error is Sefaria being unreachable through the proxy — **gracefully handled** (a prominent red `#ttStatus` error banner "Sefaria API may be unreachable", not a silent failure). **1 micro-feature (6ee266c, 2 iters)** = 1 pass + 1 micro-feature = **3 of 5**; stopped — the rest of the fix-backlog is gated, and this was the one clean/verifiable actionable win. `sw.js` v378→**v379** (hebrew_dictionary.html + locales/en.json + locales/he.json precached + changed). **No `FONT_MAKER_VERSION` bump** (dictionary, not FM). `check-i18n` clean (Check A+B); `build-locales` (2 new keys, EN+HE); `update-sitemap` run. Deploy `success` verified after the maintainer merges PR #122.
- [x] 2026-07-21 | 6ee266c | hebrew_dictionary.html (+ locales, sw.js) | **Reorder words within a saved Word List (S129 micro-feature — the S68 Pass-H "Word-List reorder" feature seed).** The Word List manager's "View words" expansion could add/remove words but not reorder them, so a teacher couldn't sequence a lesson's vocabulary. Added ↑/↓ buttons per word row (disabled at the ends) driven by a new **`wlMoveWord(id, word, dir)`** helper that swaps neighbours in `list.words`; the expansion re-paints in place from the live store order (a full `wlRefreshManager` would collapse it). **Reuses the existing `ivritSuite_wordLists` store** (already backed up via AllTools + the `.ivrit` engine) → 0 new persistence wiring, 0 new localStorage keys; **2 new i18n keys** (`move_up_title`/`move_down_title`) reuse the repo's established Hebrew for "Move up"/"Move down". **Strict verification (light/dark × desktop/800px, fresh empty-storage context, pageerror==0):** reorder both directions, ends disabled, expansion stays open, **order persists across a reload** (store-backed → `.ivrit`/AllTools covered), EN "Move up"/"Move down" → HE "הזז למעלה"/"הזז למטה".
- [x] 2026-07-21 | (S129 Pass I — first-load & empty-state) | site-wide (stalest pass, last I S118) | **Pass I — comprehensively CLEAN, 10th run** (first over the S118→S128 surface). All 12 pages × light/dark = 24 fresh-context runs (empty localStorage + IndexedDB) + one new-user interaction/tool: **0 pageerrors / 0 `[i18n] miss` / 0 real console errors** on load and after interaction. New-surface probes (S119 dashboard placeholder, S127 dictionary print `beforeprint`/`afterprint` — safe on fresh load, S126 trope fullscreen, FM tooltips, Basic-TTS notice) all handle absent-keys/fresh-context correctly; empty states instructive (generator `rwListEmptyState`, FM grids, trope Learn). The lone torah console error is Sefaria unreachable-through-proxy → gracefully handled with a prominent red error banner (verified via screenshot), not a silent failure. No new candidates; paired the clean pass with the top clean actionable seed (Word-List reorder, 6ee266c).
- [x] 2026-07-21 | (S128 close-out) | branch/deploy note | "Rerun" → S128 **continues on branch `claude/improve-loop-5xuuei`, adding to the still-open draft PR #122** (S126–S127; open + unmerged + `mergeable_state: clean`, verified via the S127 self-check-in → no branch restart, no new PR). **Drift check: no outside-loop change** since S127 — branch head @ `1b6737a`, `sw.js` **v378**, `FONT_MAKER_VERSION` **5.5**; PR #122 open/mergeable, no CI checks (repo has none), 0 review comments. **Pass run: D** (performance, one tool — stalest since S117; **hebrew_blend_generator.html**, its 3rd dedicated D — last S44 ~84 sessions ago, by far the stalest-profiled). **Measured effectively CLEAN, 0 fixes:** the long-parked **S44 Class-Set monolithic longtask** (whole 40-version build = 353 ms @1× / **1,305 ms @4×**) is confirmed **RESOLVED by the S61 chunked build** under the perf lens — a max-stress build (blendCount 100 × Class-Set 40 × answer key = 80 sheets / 33,040 DOM nodes, bigger than S44's 13,840) now streams at **~90–100 ms/chunk @4×** (occasional GC/layout variance to ~200–224 ms; **0 systematic >200 ms**), a typical 1,600-cell build has 0 longtasks >50 ms @1× AND @4×, and @1× everything is trivial. Boot has the standard **@4×-only** cold-load longtask (~225 ms), same parked category as every tool. `exportPDF` couldn't be profiled (its html2canvas/jspdf CDN is unreachable through the proxy — stays parked P4). Budget **1 pass = 1 of 5**; **stopped early** — build measured clean and the entire fix-candidate backlog is gated (design-calls / needs-new-Hebrew-keys / parked-perf / maintainer-preference / unverifiable-headless), with no clean non-dictionary win (a dictionary micro-feature would be a 3rd consecutive dictionary-heavy touch after S127 — against the variety governor). **`sw.js` NOT bumped (stays v378) — no precached file changed** (ledger-only session). **No `FONT_MAKER_VERSION` bump.** `check-i18n` clean (no UI strings); no `build-locales`; `update-sitemap` no-op (no page content changed). Deploy `success` verified after the maintainer merges PR #122.
- [x] 2026-07-21 | (S128 Pass D — performance, one tool) | hebrew_blend_generator.html (stalest pass, last D S117; generator's 3rd D, last S44) | **Pass D — generator worksheet build measured effectively CLEAN; the S61 chunking confirmed to have resolved the long-parked S44 Class-Set monolithic longtask; 0 fixes.** Headless over local HTTP, buffered `longtask` PerformanceObserver + CDP CPU throttle, @1× and @4×. **Cold-load boot:** @1× 2 longtasks max 99 ms / sum 182 ms (0 over 200); @4× max 225 ms (one, throttled-only — the standard parked cold-load category). **Class-Set worksheet build (heavy interaction):** version 0 syncs then one version per `setTimeout` tick (S61). A 40-version / 1,600-cell / 7,000-node build → **0 longtasks >50 ms** @1× AND @4×; the absolute-extreme 33,040-node build (blendCount 100 × 40 × answer key) → **~90–100 ms/chunk @4×** typical (variance spikes to ~200–224 ms, no systematic >200), vs the pre-S61 S44 single **1,305 ms @4×** freeze. `pageerror==0`. **Logged (informational, parked):** the @4×-extreme-config per-chunk variance ceiling (~200 ms on a 33k-node build) — throttled-only, extreme, user-initiated, contract-risky to split further (the strict Worksheet-Chunked-Build contract), same category as the parked cold-loads. `exportPDF` un-profilable headless (CDN unreachable). Generator now Pass-D'd 3× (S13/S44/S128), confirming S61 held.
- [x] 2026-07-21 | (S127 close-out) | branch/deploy note | "rerun loop" → S127 **continues on branch `claude/improve-loop-5xuuei`, adding to the still-open draft PR #122** (S126; open + unmerged + `mergeable_state: clean` → no branch restart, no new PR). **Drift check: no outside-loop change** since S126 — branch head @ `0ccaa7c`, `sw.js` **v377**, `FONT_MAKER_VERSION` **5.5**; PR #122 open/mergeable. **Pass run: G** (print & export fidelity, one tool — stalest since S116; run as a **6-config workflow: 3 modes × 2 themes, adversarially verified**). **Tool = hebrew_dictionary.html**, chosen over the pointer's suggestions: **torah_trainer** can't be audited headless (Sefaria unreachable through the proxy → no real reading loads; needs a synthetic-fixture G, logged as a deliberate future target) and **flash_cards has NO print surface at all** (no `@media print`, no `window.print`). The dictionary is hermetic (local `data/` corpus), has the suite's most elaborate print CSS, and never had a dedicated print-fidelity G. **6 findings, all CONFIRMED; 2 fixes:** pagination completeness (912d1ea, P2) + print-CSS fidelity — chrome-leaks + bg-off/dark legibility (9c8b0f3, 5×P3/P4). Budget **1 pass + 2 fixes = 3 of 5**; stopped — all Pass-G findings addressed, remaining cross-tool backlog design-gated. `sw.js` v377→**v378** (hebrew_dictionary.html precached + changed). **No `FONT_MAKER_VERSION` bump** (dictionary, not FM). `check-i18n` clean (no UI strings touched); no `build-locales` (0 keys); `update-sitemap` run. Deploy `success` verified after the maintainer merges PR #122.
- [x] 2026-07-21 | 9c8b0f3 | hebrew_dictionary.html (+ sw.js) | **Print @media fidelity — chrome leaks + background-off/dark legibility (S127 Pass G, 5×P3/P4).** The `@media print` block had five defects (all CONFIRMED by the audit's adversarial verify): (chrome leaking onto paper) `#shoreshControls` (root input/Look-up/browse chips) and per-card `.wc-copy`/`.wc-search`/`.wc-speak` action icons weren't in the print hide-list → added; (illegible under the print default "Background graphics: off", which drops all fills) `.b-len` length badge + `.ec-tag.cat` emoji category badge were light-gold text whose only channel was a navy fill → transparent bg + dark `--gold-text` + `--gold` border (legible with backgrounds on OR off), the `.shoresh-patterns-title` "Common Patterns" heading was white-on-navy → navy text + bottom border, and the `.ec-gender` masc/fem labels printed from dark theme kept low-contrast dark-mode colors → forced back to the legible light-theme values. **Verified headless (print emulation, words/emojis-dark/shoresh, pageerror==0):** chrome `display:none`; `.b-len`/`.ec-tag.cat` `rgb(135,104,29)`+border on transparent; `.ec-gender` `rgb(58,120,192)`/`rgb(201,70,143)`; shoresh title `rgb(26,39,68)`+border.
- [x] 2026-07-21 | 912d1ea | hebrew_dictionary.html (+ sw.js) | **Print all filtered results, not just the loaded page (S127 Pass G, P2).** Results are lazy-paginated (`PAGE=100` via `loadMore`), but `window.print()` snapshots the live DOM — so a filtered set >100 printed **silently truncated** to the loaded page (100 of **13,081** words / 100 of **1,581** emojis), and the print CSS hides the "Show more" button + result count + sidebar count, so a teacher's handout was silently incomplete with **no on-paper cue**. Added a `beforeprint` listener that materializes every filtered row before the browser paginates (reusing the existing `loadMore()` primitive → covers words + emojis; shoresh renders all roots already) and an `afterprint` listener that restores the lazy view (removes appended cards, resets `shown`, re-shows "Show more"). Covers the 🖨️ button AND Ctrl+P (the established generator/torah `beforeprint` pattern). **Verified headless (pageerror==0):** words 100→13,081 and emojis 100→1,581 expand on `beforeprint`, both restore to 100 on `afterprint`.
- [x] 2026-07-21 | (S127 Pass G — print & export fidelity, one tool) | hebrew_dictionary.html (stalest pass, last G S116 generator) | **Pass G — 6-config workflow (words/emojis/shoresh × light/dark), every finding adversarially verified; 6 CONFIRMED, all addressed by 2 fixes.** Ran a validated Playwright print-emulation harness per config + screenshot inspection + independent refutation of each finding (accounting for the headless caveats: no `beforeprint` handler existed → `emulateMedia('print')` faithful; "background graphics off" is the print default so lost bg tints are expected unless meaning is lost; `screenshot`≠print). **P2:** print pagination truncation (words+emojis; 912d1ea). **P3/P4:** shoresh `#shoreshControls` chrome leak, per-card `.wc-*` chrome leak, `.b-len`+`.ec-tag.cat` badge illegibility bg-off, `.shoresh-patterns-title` white-on-navy invisibility, `.ec-gender` dark-theme low-contrast (all 9c8b0f3). **Clean:** words/emojis card layout at Letter+A4 (0 overflow, break-inside honored, RTL correct), dark→light token reset, main chrome hide-list. **Deferred:** torah_trainer print G (Sefaria unreachable headless → needs a synthetic-fixture harness) logged as a future G target.
- [x] 2026-07-21 | (S126 close-out) | branch/deploy note | "/improveloop" → S126 **runs on a fresh branch `claude/improve-loop-5xuuei` + a new draft PR** (prior loop S123–S125 landed on `main` via authorized direct push → previous loop branch effectively merged, so per the branch protocol: fresh branch + new PR). **Drift check: substantial outside-loop TTS work** since S125 (`sw.js` v370→v376): the maintainer added **Advanced TTS (Kokoro Hebrew, client-side ONNX)** to generator/dictionary/flash_cards (4032210), iterated (7fd2d6c/196d31a/a4c2f35), then **fully removed it** (43b64e6), leaving only a **Basic-TTS "no Hebrew voice" notice** (349c2c9, new key `shared.audio.no_hebrew_voice`). Verified the removal is clean — CSP fully reverted (no stale `wasm-unsafe-eval`/Hugging Face), no orphaned CDN refs, Basic-TTS byte-identical across the 3 tools. `FONT_MAKER_VERSION` still **5.5**. **Pass run: A** (recurring-pattern sweep, 3 consequence-critical classes, stalest since S114; **CLEAN, 0 hits** over `fd0fd11..HEAD`). **1 fix (1984167) + 1 micro-feature (9a2a2c6, 2 iters)** = 1 pass + 1 fix + 1 micro-feature = **4 of 5**; stopped — micro-feature quota used, remaining fix-backlog design-gated/needs-Hebrew. `sw.js` v376→**v377** (Hebrew_Font_Maker.html + trope_tutor.html precached + changed). **No `FONT_MAKER_VERSION` bump** (pure i18n wiring, not a feature). `check-i18n` clean (Check A+B, 0 new); no `build-locales` (0 new keys); `update-sitemap` run. Deploy `success` verified after the maintainer merges the PR.
- [x] 2026-07-21 | 9a2a2c6 | trope_tutor.html (+ sw.js) | **Trope Tutor fullscreen "project" button (S126 micro-feature — S123 Pass-F Feature seed).** Trope Tutor was the **only projected tool without a fullscreen toggle** (the other 6 ship a `#fsBtn`), so a teacher presenting the Learn/Drill view on a SmartBoard had only native F11. Added a header `#fsBtn` icon-button (styled as the existing `.hbtn icon-btn`, matching dark/settings) using the **dictionary single-button model**: `toggleFullscreen()` + a `fullscreenchange` listener swapping glyph + title. **Reuses the `shared.fullscreen.*` keys (0 new keys)**, adds **no persistent state** (no `.ivrit`/AllTools wiring), no CSP change; `header .hright` is already print-hidden. **24-line diff, single tool.** **Strict verification (light/dark × desktop/800px, fresh empty-storage context, pageerror==0):** keyboard-reachable native button (36×30px), `requestFullscreen` fires, icon/title swap both directions (enter↔exit), EN "Present fullscreen" → HE "מצב מסך מלא" title + Hebrew aria after a language switch.
- [x] 2026-07-21 | 1984167 | Hebrew_Font_Maker.html (+ sw.js) | **FM: 9 template-literal tooltips localized (S126 Pass A recurring i18n blind-spot, P3).** Wired the guides-panel cluster (collapse/color/name/delete/style), the mark-edit delete-piece, custom-glyph delete, kern click-to-edit, and combined-sheet remove `title=` strings through their **existing (orphaned) i18n keys** — JS-built template-literal titles are the documented Check-A blind spot, so they showed English even in the Hebrew UI. Fixed with the guarded inline `${esc(window.I18n ? I18n.t('key') : 'fallback')}` form (`renderControls` rebuilds these panels without re-running `applyStaticI18n`, and `applyI18n` re-renders them on a language switch → inline is the correct vehicle, matching the node-editor convention). **0 new keys, no `FONT_MAKER_VERSION` bump** (pure i18n wiring). **Verified headless (light/dark × 1280/800, pageerror==0):** all 9 keys resolve EN+HE; the live guides panel shows English titles in EN and switches to Hebrew (כווץ / גרור…, מחק קו עזר זה, …) on a language switch.
- [x] 2026-07-21 | (S126 Pass A — recurring-pattern sweep) | site-wide (stalest pass, last A S114) | **Pass A — CLEAN, 0 hits across the 3 active consequence-critical classes** over `fd0fd11..HEAD` (S115 FM v5.5 export chain + S116–S125 loop changes + the substantial outside-loop **Advanced-TTS-added-then-removed / Basic-TTS** drift). **unescaped-input/unsafe-parse:** the dashboard Now/Next guard `esc()`-wraps every dynamic value (only trusted `I18n.t` raw); the message-surface `innerHTML` is pre-existing teacher-local contenteditable (the new `_dashTextEmpty` uses an inert detached `<template>` only to read `textContent`); FM's one new interpolation is a trusted anchor `Object.assign` clone; Basic-TTS uses `SpeechSynthesisUtterance(text)` (not an HTML sink) + localized strings; new fetches are same-origin `data/*.json`. **localStorage-vs-AllTools:** 0 new keys (TTS reuses the already-registered `hebrewDictionary_ttsRate`; `_heVoiceWarned` is a JS var). **destructive-bulk:** FM anchor emits are shallow clones, never mutating stored anchors. Also confirmed the Advanced-TTS removal cleanly reverted its CSP additions. Found the FM 9-tooltip pure-wiring fix (1984167) as the top actionable candidate; no new pattern registered.
- [x] 2026-07-20 | (S123–S125 landed) | branch/deploy note | **S123–S125 landed on `main` via maintainer-authorized direct push** ("push to main") — clean fast-forward `92b7cdb`→`5ce1fb3` (10 commits: S123 Pass-F fullscreen `#fsBtn` title i18n + flash_cards sole-word-list + torah scope tooltip, S124 Pass-K dictionary emoji-card + dashboard picker i18n, S125 Pass-C FM slider accessible names ×2, + 3 close-outs). **PR #120 auto-recorded merged** (`merged_by` Hbleiberg, 2026-07-20T23:01Z), session auto-unsubscribed. **Pages deploy for `5ce1fb3` concluded `success`** → the 7 fixes are **live on ivritsuite.com**. `sw.js` **v370**; `FONT_MAKER_VERSION` 5.5 (unchanged). A future loop session cuts a **fresh branch off latest `main` (5ce1fb3)** since PR #120 is merged.

- [x] 2026-07-20 | (S125 close-out) | branch/deploy note | "Pass C next" → S125 **continues on branch `claude/improve-loop-nfeveh`, adding to the still-open draft PR #120** (S123–S124; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** since S124 — branch head @ `a6bf9dc`, `sw.js` **v369**, `FONT_MAKER_VERSION` **5.5**. **Pass run: C** (accessibility, one tool — stalest since S113; user-directed). **First-ever dedicated C pass on Hebrew_Font_Maker** — audited **a11y-strong** (headless enumeration: only **3 nameless** controls among 247 visible; 0 weak icon buttons; modal focus-trap, focus-visible indicators, wrapping-label coordinate inputs all present). **2 fixes:** every FM range slider now carries an accessible name (f5d4080 `vSlider` helper + `#zoomRange`; 18d9a5d spacing/kern sliders) — the systemic gap was a shared helper rendering unassociated label spans. **Deferred (design-call):** the interactive editing-stage `role`/`aria-label` (nuanced pointer-driven canvas). Budget **1 pass + 2 fixes = 3/5**; stopped early — no remaining clean FM a11y fix (stage finding is design-gated; Pass C is one-tool). `sw.js` v369→**v370** (Hebrew_Font_Maker.html precached + changed). **No** `FONT_MAKER_VERSION` bump (pure a11y fix, per S101/S102 precedent). `check-i18n` clean; `update-sitemap` run (FM lastmod). Deploy verified after merge.
- [x] 2026-07-20 | 18d9a5d | Hebrew_Font_Maker.html (+ sw.js) | **FM spacing & kerning sliders labeled (S125 Pass C, a11y).** The letter/word/line-spacing sliders (`renderSpacingPanel`) + kern Gap/Auto-tightness sliders (`renderKerningSection`) rendered their label as adjacent text in a `.inline` `<div>`, unassociated with the input → nameless to screen readers. Added `aria-label` reusing each slider's existing `fontmaker.panels.*` label key (localized for free). No new keys, no FM version bump. Verified headless (rendered both panels) EN/HE × light/dark: all 5 expose localized names, pageerror==0.
- [x] 2026-07-20 | f5d4080 | Hebrew_Font_Maker.html (+ sw.js) | **FM range sliders get accessible names — systemic fix (S125 Pass C, a11y).** The shared `vSlider()` helper rendered its label as an unassociated `<span class="vslabel">`, so all 6 opacity sliders built through it were nameless; added `aria-label="${esc(label)}"` (label is already the localized `I18n.t` string). Also mirrored the `nodeZoomRange` pattern onto its sibling `#zoomRange` (reuse `fontmaker.node_editor.zoom_title`). Verified headless: FM visible nameless controls 3→0; localized names EN/HE × light/dark, pageerror==0.
- [x] 2026-07-20 | (S125 Pass C — accessibility, one tool) | Hebrew_Font_Maker.html (first-ever dedicated C on FM; stalest pass, last C S113 trope) | **Pass C — FM audited a11y-strong; 2 systemic slider-label fixes + a stage finding logged.** Headless keyboard/SR enumeration of **247 visible interactive controls**: only **3 nameless** (all range sliders) + **0 weak** icon-only buttons — prior sessions' tooltip/aria work paid off. Root cause of the sliders: the shared `vSlider()` helper + spacing/kern `.inline` divs + `#zoomRange` render labels as **unassociated adjacent text**. Fixed all (f5d4080 + 18d9a5d) reusing existing label keys → visible nameless **3→0** (+ the hidden vSlider/spacing/kern sliders). Also verified fine: modal focus-trap (`aOpenModal`/`_activeModal`), focus-visibility (`.ad-box:focus-visible` stroke, `.me-coord:focus-within` ring — every `outline:none` site is compensated), wrapping-`<label>` coordinate inputs, `#nodeEditToggle` dynamically labeled when shown (false positive). **Deferred:** interactive editing-stage `role`/`aria-label` (design-call, logged P3); hidden file inputs (standard label-trigger pattern).
- [x] 2026-07-20 | (S124 close-out) | branch/deploy note | "Pass K next" → S124 **continues on branch `claude/improve-loop-nfeveh`, adding to the still-open draft PR #120** (S123; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** since S123 — branch head @ `b1c30a0`, `sw.js` **v368**, `FONT_MAKER_VERSION` **5.5**. **Pass run: K** (i18n/localization audit — stalest since S112; user-directed). `check-i18n` Check A + B **clean**; the JS-built-English blind-spot sweep found the suite in excellent shape (alert/confirm/toast route through `I18n.t` — 0 bare-English; dashboard `.innerHTML` widget text correctly follows `headerLang`). **2 fixes:** dictionary emoji-card form (5dd4f94) + dashboard picker tooltips (9dc2662) — reused existing/orphaned keys (+2 new dict keys). **FM 24-tooltip backlog deferred** with precise intel (mechanism: `renderControls` re-renders without `applyStaticI18n` → needs inline guarded `I18n.t`, not `data-i18n-title` alone). Budget **1 pass + 2 fixes = 3/5**; stopped early — remaining K work is the FM backlog (own focused effort). `sw.js` v368→**v369** (precached: dictionary, dashboard, locales/en.json, he.json). **No** `FONT_MAKER_VERSION` bump (FM untouched). `check-i18n` clean, `build-locales` (2 new dict keys), `update-sitemap` run. Deploy verified after merge.
- [x] 2026-07-20 | 9dc2662 | classroom_dashboard.html (+ sw.js) | **Student-picker chip tooltips localized (S124 Pass K).** The picker chip `.title` hints ("Absent today — tap to include" / "Tap to mark absent for this session") were hardcoded English in `renderStudentPicker`; the keys `dashboard.picker.chip_absent`/`chip_present` already existed (translated) but were never wired. Wired both via `I18n.t()` (matching adjacent picker render code); `applyI18n` re-renders the picker → a live language switch re-localizes. No new keys. Verified headless (seeded roster, one absent → both title branches) EN/HE + switch, light/dark × 1280/800, pageerror==0.
- [x] 2026-07-20 | 5dd4f94 | hebrew_dictionary.html (+ locales, sw.js) | **Emoji-card gender forms + speak button localized (S124 Pass K).** The `addForm` template literal hardcoded gender labels ("♂ Masculine"/"♀ Feminine"/"…(only form)") + the 🔊 speak button title/aria "Hear pronunciation" — a consistency gap (the word *modal* localized these via `dictionary.modal.form_*`/`dictionary.card.hear_title`; the emoji *card* didn't). Reused those keys; added 2 keys for the sole-form variants; wired via `data-i18n*` + inline `I18n.t()` (renderEmojiResults isn't re-run by applyI18n). Verified headless EN/HE + switch, light/dark × 1280/800, pageerror==0.
- [x] 2026-07-20 | (S124 Pass K — i18n/localization audit) | site-wide (stalest pass, last K S112) | **Pass K — Check A + B clean; JS-built-English blind-spot sweep → suite in excellent shape; 2 fixes + FM backlog mapped (6th K run).** Both `check-i18n` gates clean. Systematic sweep of the documented blind spot (English UI strings built in JS template literals / plain args): (a) `setAttribute('aria-label'|'title'|'placeholder', …)` — clean (only flash_cards' correctly-wired `aria_more_info`); (b) `.title`/`.placeholder =` — only the dashboard picker (fixed); (c) `.textContent`/`.innerHTML` template literals — dashboard widget titles + Omer line correctly follow `headerLang` (projected content, not UI); (d) `alert`/`confirm`/`showAppToast`/`showToast`/`showHintToast` — **0 bare-English** across all 6 non-FM tools; (e) template-literal `title=`/`aria-label=` attributes — dictionary "Hear pronunciation" (fixed) + the **FM 24-tooltip backlog** (logged, ~9 pure-wiring orphaned keys + mechanism note). **2 fixes** (5dd4f94, 9dc2662).
- [x] 2026-07-20 | (S123 close-out) | branch/deploy note | "/improveloop" → S123 **runs on a fresh branch `claude/improve-loop-nfeveh` + new draft PR** (the S117–S122 loop work already landed on `main` via authorized direct push per the S117–S122 landed note; prior loop branch `claude/improve-loop-vghof6`/PR #119 is merged → fresh branch per protocol). **Drift check:** branch head == `origin/main` == `92b7cdb` (S122 close-out state); `sw.js` **v367**, `FONT_MAKER_VERSION` **5.5** — no un-ledgered drift. **SW v367→v368** (precached files changed: generator, dictionary, flash_cards, torah_trainer, locales/en.json, locales/he.json). **`FONT_MAKER_VERSION` NOT bumped** (no Font Maker change). Ran `check-i18n` (clean), `build-locales` (1 new torah key), `update-sitemap`. Deploy verified after merge. Budget **1 pass + 3 fixes = 4/5** — stopped early: the 2-per-session i18n pattern cap was reached and no clean non-i18n candidate remained.
- [x] 2026-07-20 | faedfb4 | torah_trainer.html (+ locales, sw.js) | **Torah reading-scope holiday tooltip localized (S123 Pass F recurring-shape sweep, P3 i18n).** `updateScopeSelectState` set `scopeSel.title` to a hardcoded English literal when a holiday/custom reading is active → Hebrew UI showed English on the disabled scope `<select>`. New key `torah.controls.scope_holiday_note` (EN+HE, terminology-anchored to the tool's `scope_aria`="היקף הקריאה" + `holiday_group_label`="קריאות התורה לחגים"), wired via `data-i18n-title`. Verified headless via the **real** `applyCustomRange`→`updateScopeSelectState` path: EN/HE resolve + re-resolve on live language switch, light/dark × 1280/800, pageerror==0.
- [x] 2026-07-20 | 8225e07 | flash_cards.html (+ sw.js) | **Words mode: default-select the sole word list (S123, S120 Pass-H P4 paper-cut).** A teacher with exactly one saved Word List hit `err_select_wordlist` on Start because the picker started unchecked. `renderWordListPicker` now auto-checks the sole list (one-list case only; runtime-only; multi-list left ambiguous; in-session deselect still holds). No new state/key — `selectedWordListIds` already persists via `selectedWordListNames`. Verified headless fresh-profile: 1 list → auto-select + Start reaches the card screen; 2 lists → neither selected; pageerror==0.
- [x] 2026-07-20 | d57108a | hebrew_blend_generator.html + hebrew_dictionary.html (+ sw.js) | **Fullscreen `#fsBtn` title localized on `fullscreenchange` (S123 Pass F, P3 i18n).** Both tools reset the button title to a hardcoded English literal, clobbering its localized `data-i18n-title` after a fullscreen round-trip → Hebrew UI showed English (EN was inconsistent too: static i18n resolves "Present fullscreen"). Converged to flash_cards' canonical `_syncFsBtn` (set the state key + resolve via `I18n.t`; reused existing keys, no CSV/build). Verified EN+HE × light/dark × 1280/800, pageerror==0.
- [x] 2026-07-20 | (S123 Pass F — cross-tool consistency) | site-wide (stalest pass, last F S111) | **Pass F — fullscreen-toggle affordance audit; dark-mode spot-checked CONSISTENT (10th F run).** **Dark-mode toggle** across all 12 pages: OS-aware no-flash IIFE, `aria-pressed` synced in toggle + init, correct accessible names (icon-only → `aria-label`; text-label `🌙 Dark` → visible text), icon/label swap, all localized via `shared.darkmode.*` = **fully CONSISTENT, no fix.** **Fullscreen toggle:** present in **6/7 tools** (trope_tutor has NONE → feature seed); two legitimate models — two-button enter+overlay-exit with header-hidden-in-fullscreen (dashboard/torah/FM/generator) vs single-button-stays-visible toggle (flash_cards, the `aria-pressed`-on-fullscreenchange canonical); accessible names all present. **Bug fixed (d57108a):** generator+dictionary hardcoded the `#fsBtn` title on `fullscreenchange`. **Recurring-shape mini-sweep** (hardcoded English `.title`/`aria-label`/`textContent` in JS, the Check-A blind spot) across the 7 tools: generator's remaining hits are intentional printed worksheet/answer-key/crossword output (baselined); found + fixed torah scope tooltip (faedfb4); logged dashboard student-picker chip titles (deferred — pattern cap); flash_cards `aria_more_info` confirmed correctly wired (false positive).
- [x] 2026-07-20 | (S117–S122 landed) | branch/deploy note | **S117–S122 landed on `main` via maintainer-authorized direct push** ("push to main") — clean fast-forward `e535f09`→`32adb6d` (13 commits: the S117 Now/Next guard b5d4261, S118 panel-collapse 969e293, S119 empty-placeholder 6e75907, S120 emoji-load fix 203a0b9, S121 dict→gen `?wl=` deep-link 1d43e48, S122 README drift 717c53b, + the six close-out/sitemap commits). **PR #119 auto-recorded merged/closed** (merged_by Hbleiberg, 21:18Z) + session auto-unsubscribed. 10th ledgered direct-push precedent (after S78/S105/S106–S110/S111–S113/S114–S115/S116). `sw.js` **v367** on `main`, FM **5.5**. Pages deploy for the landing verified `success` (see the close-out summary). S123 cuts a **fresh branch off latest `main`** since PR #119 is merged.
- [x] 2026-07-20 | (S122 close-out) | branch/deploy note | "rerun the loop" → S122 **continues on branch `claude/improve-loop-vghof6`, adding to the still-open draft PR #119** (S117–S121; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** (`origin/main` @ `e535f09`; `sw.js` **v367**, `FONT_MAKER_VERSION` **5.5**; PR #119 open/mergeable). **Pass run: E** (freshness & site health — stalest since S109; 11th run, **mechanical CLEAN**). **1 fix (717c53b, README drift).** Budget 1 pass + 1 fix = 2 of 5; **stopped early** (site-health clean, remaining fix-backlog design-gated/parked in variety-favorable tools). **`sw.js` NOT bumped (stays v367) — no precached file changed** (README.md + ledger only, neither precached). **No `FONT_MAKER_VERSION` bump.** `check-i18n` clean (README not scanned; no UI strings touched); no `build-locales` (0 keys); `update-sitemap` no-op (README isn't a sitemap page). Deploy `success` verified after the maintainer merges PR #119.
- [x] 2026-07-20 | 717c53b | README.md | **README drift fix (S122 Pass E site-health).** Two stale facts corrected: (1) the Font Maker line said **"(Currently v4.29.)"** but `FONT_MAKER_VERSION` is **5.5** (the tool went 4.x→5.x since that line was written) → updated to v5.5; (2) the `fonts/` files-table row listed only "(Frank Ruhl Libre, Reuben, TzviScript, TzviScript Stroke Guide)" — **missing Lakhish Bold**, bundled since S110 (`fonts/LakhishBold.ttf`, OFL 1.1 in THIRD_PARTY_LICENSES.md, offered in resources.html) → added. Doc-accuracy only (README is not precached → no `sw.js` bump, not user-facing UI → no i18n). Verified by inspection against the source of truth (`FONT_MAKER_VERSION`=5.5; `fonts/` = 5 `.ttf`, all licensed). Spot-checked other version-sensitive README claims — flash_cards present/fullscreen mode (`#fsBtn`) now exists, dashboard 13-font picker, 54 parshiyot, 13,081 entries all still accurate.
- [x] 2026-07-20 | (S122 Pass E — freshness & site health) | site-wide (stalest pass, last E S109) | **Pass E — mechanically CLEAN, 11th run** (first over the S109→S121 surface). **Sitemap:** `update-sitemap` no-op (all `<lastmod>` == git). **SW precache:** `CORE_ASSETS` all **25/25 resolve**, every root HTML precached bar the intentional `i18n-test.html` (12/13), **locales `/locales/{en,he}.json` precached** (no offline i18n gap). **Internal links:** Node scan of **410** local `href`/`src`/`url()` refs across 12 pages → **0 real broken** (5 regex false-positives from `data-rwsrc="all|lists|imported"` matching `src=`, per the S109 note; the S121 `?wl=` deep-link hrefs are JS-built so correctly skipped). **Licenses:** all **5** bundled fonts (Frank Ruhl Libre, Lakhish Bold, Reuben, TzviScript, TzviScript Stroke Guide) covered in THIRD_PARTY_LICENSES.md. **Docs:** every `docs/*.md` + `scripts/*.{js,mjs}` referenced by CLAUDE.md exists; CLAUDE.md carries **no** `?wl=` handshake claim to drift. **1 fix:** the README drift found here (717c53b). No other candidates.
- [x] 2026-07-20 | (S121 close-out) | branch/deploy note | "/improveloop" → S121 **continues on branch `claude/improve-loop-vghof6`, adding to the still-open draft PR #119** (S117–S120; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** (`origin/main` @ `e535f09`; `sw.js` **v366**, `FONT_MAKER_VERSION` **5.5**; PR #119 open/mergeable, verified via the S117 self-check-in). **Pass run: B** (console & error audit — stalest since S108; 10th run, **CLEAN**). **1 micro-feature (1d43e48), costs 2 iterations** (1 pass + 1 micro-feature = 3 of 5). `sw.js` v366→**v367** (hebrew_dictionary.html + hebrew_blend_generator.html + locales/en.json + locales/he.json precached + changed). **No `FONT_MAKER_VERSION` bump** (no FM change). `check-i18n` clean (Check A+B); `build-locales` run (1 new key `dictionary.wordlist.use_in_generator`, EN+HE); `update-sitemap` run. Deploy `success` verified after the maintainer merges PR #119.
- [x] 2026-07-20 | 1d43e48 | hebrew_dictionary.html + hebrew_blend_generator.html (+ locales, sw.js) | **Dictionary → Worksheet Generator `?wl=` deep-link (S121 micro-feature — the deferred second half of the S72 handshake).** A saved Word List could be carried into Flash Cards (S72 `?wl=`) but not into the Worksheet Generator. Now the Dictionary's Word List manager + save-success panel offer a **"Use in Worksheet Generator →"** link alongside "Use in Flash Cards →" (new key `dictionary.wordlist.use_in_generator`, EN+HE, RTL-mirrored arrow), emitting `hebrew_blend_generator.html?wl=<id>`. The generator consumes `?wl=<id>` in `restoreFromURL` (after the `?ak=` branch, before `?s=`): validates the id against the shared `ivritSuite_wordLists` store (object-key/Set only, never interpolated), sets `realWordsToggle` on + `selectedWordListIds`, `rwSource='lists'` (set before `toggleRealWordsUI` so its `setRwSource(rwSource)` skips the corpus load), then `generate()`; returns early so `?s=` is skipped. Invalid/absent id = silent no-op; a link only sets the live view, never clobbers saved presets. **Cross-tool handshake micro-feature (declared pair only), ~27 lines, no new dep/build/data.** **Strict verification (light/dark × desktop/800px, fresh profile w/ a seeded store):** generator consume → real-words on + `rwSource='lists'` + list selected + worksheet built (2 sheets / 6 rw-cards), no h-scroll @800px; invalid `?wl=` → real-words stays off; Dictionary manager shows BOTH deep-link buttons with `href=…?wl=wl_test` in EN ("Use in Worksheet Generator →") **and** HE ("שימוש במחולל דפי העבודה ←"); `check-i18n` clean; 0 pageerrors throughout.
- [x] 2026-07-20 | (S121 Pass B — console & error audit) | site-wide (stalest pass, last B S108) | **Pass B — CLEAN, 10th run** (first over the S108→S120 surface). Loaded all **12 pages × light/dark = 24 runs** over local HTTP + one basic interaction per tool (index gear→modal, generator Generate, dashboard settings, flash_cards Start→card, dictionary search, torah gear→drawer, trope drill tab, FM `.ltile`→Alef, resources filter, contact input): **0 pageerrors / 0 real console errors-or-warnings (external-abort noise filtered) / 0 failed-local requests / 0 `[i18n] miss`** on load AND after interaction. Interaction firing independently confirmed (5/6 guessed selectors + the FM `#letterGrid .ltile` corrected). Covered the S109–S120 loop surface (dashboard copy/perf/panels/placeholder, torah localize, resources license, FM i18n/v5.5, generator print, flash_cards emoji fix) + the S117–S120 branch changes. No new candidates.
- [x] 2026-07-20 | (S120 close-out) | branch/deploy note | "/improveloop" → S120 **continues on branch `claude/improve-loop-vghof6`, adding to the still-open draft PR #119** (S117–S119; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** (`origin/main` @ `e535f09`; `sw.js` **v365**, `FONT_MAKER_VERSION` **5.5**). **Pass run: H** (teacher walkthrough / paper-cuts — stalest since S107; **flash_cards.html**, the stalest H target [last H S34, 86 sessions ago] + non-dashboard per the S119 variety note). **1 fix (203a0b9).** Budget 1 pass + 1 fix = 2 of 5; **stopped early** — flash_cards is mature/well-built; remaining findings are copy/design-calls or minor (logged). `sw.js` v365→**v366** (flash_cards.html precached + changed). **No `FONT_MAKER_VERSION` bump** (flash_cards, not FM). `check-i18n` clean (0 new — the fix reuses the existing `err_emoji_loading` key); no `build-locales` (0 new keys); `update-sitemap` current. Deploy `success` verified after the maintainer merges PR #119.
- [x] 2026-07-20 | 203a0b9 | flash_cards.html (+ sw.js) | **Flash Cards: Start no longer errors when emoji data is still loading (S120 Pass H, P3 teacher-facing paper-cut).** Emoji mode fetches a **~450KB** `data/hebrew_emojis.json` when the teacher switches to it (`syncOtherSubUI` → `fetchEmojiData`); tapping **Start** before it finished (a real window on slow school wifi) showed a blocking "Emoji data still loading — try again in a second" `alert()` — a click-wait-click hiccup. Now `startPractice`'s emoji branch **waits for the in-flight fetch and retries once** with a **non-blocking toast** (reuses the existing `flashcards.start.err_emoji_loading` string — **0 new keys**), instead of erroring. `fetchEmojiData` now **caches its promise** (`_emojiFetchP`; short-circuits if `EMOJI_DATA` already set) so the 450KB file is **never fetched twice**, and **nulls it on `.catch`** so a genuine load failure still surfaces the original alert (no silent dead-end, no retry loop; `_emojiAwaitingStart` guards re-entrancy). **Verified headless (light/dark × desktop/800px):** Start-during-load → 0 alerts + toast + drill starts, **exactly 1 emoji fetch**; normal path unchanged; forced-failure state still alerts + doesn't start; letters-mode drill unregressed; `check-i18n` clean; 0 pageerrors.
- [x] 2026-07-20 | (S120 Pass H — teacher walkthrough) | flash_cards.html (stalest H target, last H S34) | **Pass H — flash_cards mature/well-built; 1 P3 paper-cut fixed (203a0b9) + candidates logged.** Role-played a fresh teacher building a lesson deck end-to-end (headless fresh-context, light/dark × 1280/800, + an Explore code-map of the 6601-line flow). **Core flow solid:** sane defaults (2-letter mode, 26 letters + 9 main vowels pre-selected, card-count 10), first-run Tour pulse, aleph-bet deck → flip → mark ✓/✗ → results all work, results score/accuracy dual-metric coherent (falsy-zero-guarded), `pageerror==0`. **Fixed:** the emoji-data-still-loading Start error (203a0b9). **Logged candidates (all copy/design/minor):** P4 per-card results panel ships collapsed (auto-expand on mistakes — borderline design); P4 words-mode no-default-list-selected; P4 two near-identical "practice the hard ones" buttons (mistakes vs weaknesses) + opaque "Other" pill + "Emoji" label over-promises (Animal+Food only). **Not logged** (intentional/struck): per-sub-mode gender defaults (maintainer-confirmed intentional S34); hands-free loop + student-picker (struck S55); `#cardMeta` "Card 1 of 10" is localized on render (not a leak). **No new Feature seeds** — the words-mode cross-tool authoring gap is the existing S34 "My Word Lists dead-ends to Dictionary" candidate.
- [x] 2026-07-20 | (S119 close-out) | branch/deploy note | "Let's do the dashboard empty text placeholder" → S119 **continues on branch `claude/improve-loop-vghof6`, adding to the still-open draft PR #119** (S117+S118; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** (`origin/main` @ `e535f09`; `sw.js` **v364**, `FONT_MAKER_VERSION` **5.5**). **Pass run: — (SKIPPED; user-directed micro-feature).** Per the protocol, an explicit user request skips the rotating pass → **Pass H stays stalest** (teacher walkthrough / paper-cuts, last S107). **1 micro-feature (6e75907), costs 2 iterations** (2 of 5). `sw.js` v364→**v365** (classroom_dashboard.html + locales/en.json + locales/he.json precached + changed). **No `FONT_MAKER_VERSION` bump** (dashboard feature, not FM). `check-i18n` clean (14 baselined, 0 new); `build-locales` run (1 new key `dashboard.text.empty_placeholder`, EN+HE); `update-sitemap` current. Deploy `success` verified after the maintainer merges PR #119.
- [x] 2026-07-20 | 6e75907 | classroom_dashboard.html (+ locales, sw.js) | **Dashboard empty-state placeholder for the class message surface (S119, user-directed micro-feature — S107 Pass-H seed).** When Dashboard Text is on but blank (e.g. right after a starter layout clears the intro text), the message surface was a bare top-border strip with no "click to add" affordance. Added `#dashTextDisplay:empty:not(.editing)::before { content: attr(placeholder) }` rendering **"Click to add a message…"** (new key `dashboard.text.empty_placeholder` EN+HE, via `data-i18n-placeholder` → the `placeholder` attribute); the empty strip is also **click-to-edit** (mouse/touch — the ✎ pencil still covers keyboard/SR). Hidden **while editing** and **in fullscreen/projected view** so students never see an edit hint. Collapses "effectively empty" content (lone `<br>`/empty `<div>` from contenteditable) to `''` in `sanitizeDashHTML` + `commitEditorToDisplay` so the placeholder reliably reappears after clearing — reset behavior unchanged. ~25-line dashboard-only diff, no new dep/build/data. **Strict micro-feature verification (light/dark × desktop/800px, fresh profile empty localStorage+IndexedDB):** placeholder shows when empty; hides with content / while editing / in fullscreen; click-to-edit enters in-place edit; clear→'' restores it (even `<div><br></div>`); language switch → Hebrew "לחצו כדי להוסיף הודעה…"; localStorage persistence round-trip holds; 0 pageerrors throughout.
- [x] 2026-07-20 | (S118 close-out) | branch/deploy note | "Rerun the loop" → S118 **continues on branch `claude/improve-loop-vghof6`, adding to the still-open draft PR #119** (S117; open + unmerged → no branch restart, no new PR per protocol). **Drift check: no outside-loop change** — `origin/main` @ `e535f09` (PR #119 unmerged; my branch carries only the S117 commits), `sw.js` **v363**, `FONT_MAKER_VERSION` **5.5**. **Pass run: I** (first-load & empty-state — stalest since S106; 9th run, comprehensively CLEAN). With the pass clean and the backlog design-gated, an **AskUserQuestion** put 3 design-calls to the user, who chose **collapse the dashboard power-user panels** and explicitly **kept flash_cards teacher panels collapsed + the torah Diaspora/Israel toggle buried** → both of those candidates marked 🚫 WON'T-FIX (do not re-log). **1 fix (969e293).** Budget 1 pass + 1 fix = 2 of 5; **stopped early** (user directed "Dashboard fix only"). `sw.js` v363→**v364** (classroom_dashboard.html precached + changed); **no** `FONT_MAKER_VERSION` bump (dashboard-only markup). `check-i18n` clean (0 new; the fix adds no UI strings — pure `class` change). `update-sitemap` run. Deploy `success` verified after the maintainer merges PR #119.
- [x] 2026-07-20 | 969e293 | classroom_dashboard.html (+ sw.js) | **Dashboard: ship the settings-drawer power-user panels collapsed (S118, user-directed).** The settings drawer opened with all 12 functional panels expanded → a teacher scrolled past the occasional-setup **Presets** + **Schedule Sync** panels (backup lives inside Presets) to reach daily controls. Added `collapsed` to `#presetsPanel` + `#schedulePanel`, matching the pre-existing `collapsed` About & FAQ panel, so daily controls sit higher; the panel-title click still expands them. Pure default change — no new copy, no persistence change (`openSettings` doesn't force-expand; collapse stays transient by design). Resolves the S107 Pass-H design-call candidate under explicit maintainer direction (S118 AskUserQuestion). Verified headless (light/dark × desktop/800px): both panels start collapsed with hidden bodies, all daily panels stay expanded, About & FAQ unchanged, title-toggle works, 0 pageerrors.
- [x] 2026-07-20 | (S118 Pass I — first-load & empty-state) | site-wide (stalest pass, last I S106) | **Pass I — comprehensively CLEAN (9th run; first over the S106→S117 surface).** Loaded all **12 pages** in a **fresh browser context** (empty localStorage + IndexedDB) × light/dark: **0 pageerrors / 0 `[i18n] miss` / 0 failed-local** across 24 runs. Empty-state / no-crash-on-absent-key probes on all 7 tools: FM fresh `newProject` renders grids; generator fresh Generate → 1 sheet **and** the `rwSource='lists'` picker shows its instructive `#rwListEmptyState`; flash_cards `startPractice` runs cold + `renderWordListPicker` shows `#wordsEmptyState`; dictionary "📚 Word Lists" opens the instructive "No word lists yet…" manager (S103); trope `renderLearn` cold; torah reading toolbar present. The **S117 Now/Next guard's else-branch** confirmed correct (nn hidden with no schedule). Onboarding copy accurate: the dictionary empty-state's "Bulk Copy Mode" reference still matches the current control (`dictionary.copy.bulk_mode` L886). **No new candidates.** With the pass clean + backlog design-gated, surfaced 3 design-calls to the user via AskUserQuestion → 1 fix (969e293) + 2 candidates closed WON'T-FIX.
- [x] 2026-07-19 | (S111–S113 landed) | branch/deploy note | **S111–S113 landed on `main` via maintainer-authorized direct push** ("Push to main") — clean fast-forward be4a6d9→f2d5307 (6 commits; 7th ledgered direct-push precedent after S78/S105/S106–S110). **PR #114 auto-recorded merged**, session auto-unsubscribed. **Pages deploy run for f2d5307 concluded `success`** (2026-07-19T22:47:38Z, ~29s) → S111 (FM "My IvritSuite Fonts" opener, FM v5.2), S112 (11 FM `status()` toasts localized via existing keys), S113 (trope_tutor `prefers-reduced-motion` completeness) are **live on ivritsuite.com**. `sw.js` v356. A future loop session cuts a **fresh branch off latest `main` (f2d5307)** since PR #114 is merged.
- [x] 2026-07-20 | (S117 close-out) | branch/deploy note | "/improveloop" → S117 on a **fresh branch `claude/improve-loop-vghof6` off latest `origin/main` `e535f09`** (0 divergence; PR #118 closed after S116+crossword landed on `main` → per the branch protocol a fresh branch + new **draft PR** at close-out). **Drift check: no un-ledgered outside-loop change** — `origin/main` @ `e535f09` (= the ledgered S116 close-out + the crossword data fix, both already in Done), `sw.js` **v362** (the crossword fix `856b934` bumped v361→v362 — it touched `data/` **and** the generator's `cleanRealWords`, already ledgered), `FONT_MAKER_VERSION` **5.5** (unchanged). **Pass run: D** (performance, one tool — stalest since S105). `sw.js` v362→**v363** (classroom_dashboard.html precached + changed); **no** `FONT_MAKER_VERSION` bump (dashboard-only JS, no FM change). Deploy `success` verified after the maintainer merges. `check-i18n` clean (14 baselined, 0 new; no new UI strings added — the fix reuses existing keys). `update-sitemap` refreshed classroom_dashboard.html's lastmod.
- [x] 2026-07-20 | b5d4261 | classroom_dashboard.html (+ sw.js) | **Dashboard Now/Next block: guard the per-second `innerHTML` rebuild (S117 Pass D, P4 all-day-projector churn).** `#nowNextBlock` (the on-dashboard "Now: X (5m left) │ Next: Y at 10:30" strip) rebuilt its `innerHTML` on **every clock tick** when Schedule Sync is active, though its content depends only on the active/next entries + **minute-granularity** `timeStr` — never the per-second tick (~21,600 needless subtree rebuilds/day on a projector). Added a `_lastNowNextKey` content-key guard that skips the rebuild when unchanged, **exactly mirroring the sibling `renderSchedulePreview`'s S14 `_lastPreviewKey` guard** in the same function; `applyI18n` nulls the new key alongside the other two so translated labels re-render on a language switch; the else-branch nulls it on hide so a re-show always rebuilds. **Verified headless (light/dark, desktop/800px):** live schedule → 20 stable per-second ticks = **0** rebuilds (MutationObserver), schedule content change → 1 rebuild (new preset shown), language switch → translated re-render ("Now"→"עכשיו"), 0 pageerrors.
- [x] 2026-07-20 | (S117 Pass D — performance, one tool) | classroom_dashboard.html (stalest-profiled: last dedicated D **S35**; the movable-panels feature was never D-profiled) | **Pass D — dashboard thoroughly CLEAN; 1 per-second-churn fix (b5d4261).** 3rd dedicated dashboard D (S13→S35→S117). Drove real cold-load + 4 interactions + a 40-cycle stability hammer over local HTTP, @1× and @4× CPU throttle. **Cold-load:** boot longtask **@1× max 195 ms (UNDER the 200 ms bar) / @4× 561 ms max, 796 ms sum** (one-time, throttled-only, desktop-fine — the parked cold-load candidate; its "render-blocking Google Fonts" half is now **STALE** — the maintainer's ~07-17 change made the stylesheet `media=print`-swap non-render-blocking, so the JS boot block is the sole cause). **Movable-panels feature** (never profiled before): button-driven `insertBefore` reparenting over 5 panels — enterLayoutEdit + 3 moves = 145 ms @4×, no >200 ms LT. **All interactions <200 ms even @4×** (openSettings 65, colorCode+full-rerender 31, darkToggle 153). **All-day-projector stability:** 40 open/close·dark·lang·re-render cycles → DOM 1104→1104 (Δ0), `setInterval` 3→3 (Δ0) — no listener/interval/DOM accumulation (retired listener-accum pattern re-confirmed dead under a perf lens); the found per-second nn-block rebuild was fixed (b5d4261). 0 pageerrors throughout.
- [x] 2026-07-20 | (S116 + crossword-fix landed) | branch/deploy note | **S116 + a user-reported crossword data fix landed on `main` via maintainer-authorized direct push** ("Push to main") — clean fast-forward `3ba31ef`→`c98f716` (5 commits: `07b6549` Pass-G banner, `62b8f6d` S116 close-out, `eaae474` sitemap, `856b934` crossword `רָקַד .`/`בָּתוּל m .` data repair + `cleanRealWordHeb` guard + corpus `?v=4→5` cache-bust across gen/dict/cards, `c98f716` sitemap; 9th ledgered direct-push precedent after S114–S115). PR #118 auto-closed on landing. `sw.js` v360→**v362** across the two landings (v360→v361 banner, v361→v362 crossword). Pages deploy `success` verified post-push. (The crossword fix is a dedicated data-correctness change, not a loop iteration — see the plan/PR; a separate pre-existing finding was logged: ~1,351 root/idiom entries use maqaf-separated notation with a floating dot, e.g. `ר־א־ש ׁ`/`ד־ל־י־ב`, which also render oddly as crossword answers — out of scope, flagged for follow-up.)
- [x] 2026-07-20 | (S116 close-out) | branch/deploy note | "/improveloop" → S116 on a **fresh branch `claude/improve-loop-01ra80` off latest `origin/main`** (S114+S115 landed on `main` via authorized direct push, PR #117 merged → per the branch protocol: fresh branch + new draft PR at close-out). **Drift check:** origin/main @ 3ba31ef, `sw.js` **v360**, FM **5.5** — all matching the S115 pointer, no un-ledgered outside-loop drift. Ran **Pass G** (2 iters = 1 pass + 1 fix, the standard Pass-G shape). `sw.js` **v360→v361** (hebrew_blend_generator.html precached + changed — the only precached file touched). **No `FONT_MAKER_VERSION` bump** (generator-only CSS fix, no FM change). `node scripts/check-i18n.js` clean (no new violations — the banner change is CSS color values + a comment, no UI strings); `build-locales` not run (0 CSV keys added); `node scripts/update-sitemap.mjs` run as the last step. Deploy `success` verified after the maintainer merges.
- [x] 2026-07-20 | 07b6549 | hebrew_blend_generator.html | **Dark-mode print answer-key banner relit to the light/PDF navy (S116 Pass G, P3 print consistency).** The `@media print` dark-reset block relights every `body.dark .X` surface to its LIGHT-mode appearance (white bg, navy ink) so a dark-mode `window.print()` matches the canonical output — but the answer-key banner alone relit to a beige (#f6efdd/#6b4e12) present in no other mode, so the same teacher answer key printed navy/gold from light mode + `exportPDF` (which always renders light) yet beige from dark mode. Relit it to the light navy/gold (#1a2744/#f0d080) like its sibling rules, + a comment to prevent a revert-to-beige. **Verified headless:** computed `.answer-key-banner` bg identical in light & dark print (`rgb(26,39,68)`); screen dark banner untouched (`rgb(13,18,32)` — the change is `@media print`-scoped); `pageerror==0`; a dark-print screenshot confirms the navy banner.
- [x] 2026-07-20 | (S116 Pass G — print & export fidelity) | hebrew_blend_generator.html (stalest pass, last G S104; last generator G S74) | **Pass G — worksheet print SUBSTANTIALLY CLEAN across 7 modes × light/dark × Letter/A4; 1 divergence fixed (07b6549).** Drove real `page.pdf`/print-media emulation at the true Letter (749px) & A4 (727px) printable widths across **blend·highlight·gematria·tracing·word-search·crossword·bingo** (fluid layout, **0 overflow either width**, `pageerror==0`, each visually inspected). Every S30–S32/S74 protection **HELD**: dark→light token reset (flawless in all modes incl. the grids), gematria write-on blanks as real borders + darkened `#6b4e12` answer values, word-search tinted cells → gold outline in the key, crossword black-blockers (deliberate per the L2053 comment), bingo 4-up dashed cuts + caller sheet, tracing model→fade→write-line with clean 4-page pagination. **Cross-tool print-reset sweep CLEAN** — torah/dictionary/dashboard/flash_cards print blocks all relight-to-light correctly (torah even forces `.tt-ref-hdr`→dark ink); the banner beige was a one-off, not a recurring pattern (no new health line). Chose the generator over the S115-pointer's suggested FM font-export target: the FM `.ttf` chain can't be produced headless (the CLAUDE.md recipe stubs Pyodide) and S115 already deep-verified it, whereas the generator worksheet is the student-held artifact and is fully headless-verifiable. **0 new candidates** (survey clean).
- [x] 2026-07-20 | (S114–S115 landed) | branch/deploy note | **S114–S115 landed on `main` via maintainer-authorized direct push** ("Push to main") — clean fast-forward fd0fd11→a7fad43 (5 commits; 8th ledgered direct-push precedent after S78/S105/S106–S110/S111–S113). **PR #117 auto-recorded merged**, session auto-unsubscribed. **Pages deploy run for a7fad43 concluded `success`** (2026-07-20T14:34:13Z, ~29s) → S114 (Pass A clean sweep + 2 FM status() toasts localized, c788498) and S115 (**FM v5.5 export-chain release, d05d5f1** — shin custom-dagesh via FB2C/FB2D + `ccmp_dagesh`, the feaLib lookupflag-leak reset, the resurrected shaping gate) are **live on ivritsuite.com**. `sw.js` v360, FM 5.5. (Aside: the prior day's 91c1035 Pages run shows `failure` — superseded by fd0fd11's `success`; the known transient class, no action.) A future loop session cuts a **fresh branch off latest `main`** since PR #117 is merged.
- [x] 2026-07-20 | (S115 close-out) | branch/deploy note | Maintainer reported "my custom Shin dagesh isn't being exported" → S115 **maintainer-directed fix on branch `claude/improve-loop-wc8stj`, adding to open draft PR #117** (S114; open + unmerged → no branch restart). **Rotating pass skipped (Pass G stays stalest)** — the session ran a full plan-mode investigation instead (2 exploration passes + a design pass, plan approved by the maintainer). **1 release commit (d05d5f1, FM v5.4→v5.5)** fixing a 3-bug chain (see the discovery + fix entries below). `sw.js` **v359→v360** (Hebrew_Font_Maker.html precached + changed). **`FONT_MAKER_VERSION` 5.4→5.5 + About changelog entry (EN+HE)** — user-visible export-correctness release, maintainer-requested. 6 new i18n keys (5 gate messages + changelog v5_5), EN+HE filled, `build-locales` run, `check-i18n` clean. `update-sitemap` run last. Deploy `success` verified after merge.
- [x] 2026-07-20 | (S115 DISCOVERY — P1-class, found while reproducing the reported bug in real font binaries) | Hebrew_Font_Maker.html | **Three-bug export chain, all shipping silently:** (1) **lookupflag leak** — feaLib (pyodide-bundled fontTools 4.51.0, verified byte-identical locally) carries `lookupflag UseMarkFilteringSet [dots]` OUT of the named `ccmp_shinsin` block into every following plain ccmp rule (the in-code comment believed the named block scoped it), hiding each rule's OWN mark → **בּ/כּ/פּ/תּ/וּ dagesh forms, וֹ FB4B, and every per-letter baked shape could never compose in ANY shaper** — custom dagesh shapes and the v5.4 cholamScale feature never actually shipped in exports; default-shape users saw no difference only because GPOS mark attachment renders equivalent ink. (2) **No composed shin+dagesh form** (no FB49/FB2C/FB2D) — typed שּׁ ligates shin+dot→FB2A ACROSS the dagesh (canonical order dagesh ccc21 < dots ccc24), orphaning the `pl_uni05E9_uni05BC` fallback → the reported custom-shin-dagesh loss. (3) **The export shaping gate NEVER ran** — harfbuzzjs 0.4.6's `hb.wasm` declares wasi/env imports and `hbjs.js` needs emscripten's `addFunction`, so `WebAssembly.instantiate(wasm, {})` threw unconditionally and `harfbuzzGate` returned `{skipped:true}` (console.warn only) on every export since it shipped — the v5.4 "verify the וֹ ligature" guard was verifying nothing. Bugs (1)+(3) compounded: the gate that would have caught (1) was itself dead.
- [x] 2026-07-20 | d05d5f1 | Hebrew_Font_Maker.html (+ locales, sw.js) | **FM v5.5: shin's custom dagesh survives export; dagesh ccmp made ordering-robust; shaping gate resurrected (S115, maintainer-directed + plan-approved).** (1) `lookupflag 0;` reset after `ccmp_shinsin`. (2) Exports bake real **U+FB2C/U+FB2D** glyphs (FB2A/FB2B contours + the letter's own dagesh via `markContoursFor` at `anchors.center`, `nikScale('05BC')`-sized; cmap'd; FB2A-style base anchors — above=holam slot, center skipped); ALL dagesh rules (5 precomposed forms + per-letter dagesh bakes + the 2 new second-stage FB2A/FB2B+dagesh rules) moved into ONE `lookup ccmp_dagesh { lookupflag UseMarkFilteringSet [uni05BC]; }` emitted after the plain rules — matches across canonically-earlier vowels (typed בָּ), fixing the bet-family sibling loss too; `seedAnchorsIfNeeded` added to both bake loops (a never-seeded anchor silently skipped bakes — reproduced live in the fixture). (3) `ensureHB` loads the `hb.js` emscripten glue (same CDN origin, no CSP change) then wraps the ready Module; 5 new localized gate probes (שּׁ/שּׂ→1 glyph, שָּׁ→2+positioned vowel, בּ→1, בָּ→2) + a שּׁ Spacing test chip. **Verified offline end-to-end** (CDN policy-blocked in-session): app's `PY_BUILDER` extracted verbatim + pinned fontTools **4.51.0** built real TTFs from in-page `buildFontSpec()` dumps; **uharfbuzz (HarfBuzz 8+) shaped 15 probes** — baseline reproduces all 3 bugs (שּׁ→[FB2A+global dagesh], בּ/וֹ never compose), fixed font: שּׁ→uniFB2C with the square test-dagesh **provably in the glyf contours** [4,14,4], שָּׁ→FB2C+vowel, בּ→FB31, בָּ→FB31+vowel, וֹ→FB4B restored, שֹׁ/דּ regressions unchanged, typed U+FB2C/D direct; **the real in-app `harfbuzzGate` (hbjs served locally, byte-identical to CDN) passes the fixed font and FAILS the baseline loudly**; UI matrix light/dark×1280/800: 0 pageerror, 0 i18n-miss, v5.5 changelog EN+HE(rtl).
- [x] 2026-07-20 | (S114 close-out) | branch/deploy note | "/improveloop" → S114 on a **fresh branch `claude/improve-loop-wc8stj` off latest `origin/main` (fd0fd11, 0 divergence)** — PR #114 merged (S111–S113), so per the branch protocol a fresh branch + new **draft PR** at close-out. **Drift check: substantial outside-loop landing** — `origin/main` f2d5307→**fd0fd11** (7 commits: **FM v5.3 + v5.4** — faithful שׁ/שׂ dot-stacking checks + shaping-check retry + vav-holam preview, combined-form mark-grid tiles replacing the redundant generic tiles (×4 commits), the stacking-UI no-false-overlap audit, a sitemap refresh, and the Holam-Haser-on-vav split); `sw.js` v356→**v358**; `FONT_MAKER_VERSION` 5.2→**5.4**. **Pass run: A** (recurring-pattern sweep, stalest since S103) — swept the 3 ACTIVE consequence-critical classes over `36161c0..fd0fd11` (that FM drift + S104–S113 loop) — **CLEAN, 0 hits** (streaks localStorage 8 / unescaped 4 / destructive 8; all KEPT ACTIVE). **1 fix (c788498):** with the security surface clean and the whole remaining fix-backlog maintainer-design-gated, an **AskUserQuestion** offered 3 directions and the user chose the FM template-literal i18n wiring. **Budget: 1 pass + 1 fix = 2 of 5; stopped early** (backlog otherwise all maintainer-design-calls). `sw.js` **v358→v359** (Hebrew_Font_Maker.html precached + changed). **No `FONT_MAKER_VERSION` bump** (pure i18n wiring, not a feature — S112 precedent). `check-i18n` clean; no `build-locales` (0 new keys); `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-20 | c788498 | Hebrew_Font_Maker.html | **Localize 2 template-literal `status()` toasts via existing plural keys (S114 — slices the S101 template-literal candidate; user-approved keep-or-improve call).** The auto-detect "Assigned N letter(s) from the sheet…" and SVG-import "Auto-assigned N letter(s) from named SVG elements…" toasts were hardcoded-English JS **template literals** (invisible to check-i18n Check A) → Hebrew-UI users saw English. Both target keys already existed (maintainer-authored `assigned_letters`/`autoassigned_svg` `.one`/`.other` plurals + the `box_others` fragment key for the conditional "Box any others by hand." suffix) → wired through `I18n.t()` with the English binary plural rule (`n===1?.one:.other`); **0 new keys, no build-locales**. The only EN change is proper "letter(s)"→"letters"/"letter" pluralization (the maintainer had flagged this "needs a keep-or-improve call"; the user approved "improve" via AskUserQuestion). Verified headless over local HTTP: **all 5 expression cases EN-correct** (Assigned 3 letters / 1 letter; Auto-assigned 5 letters + suffix / 1 letter + suffix / 27 letters no-suffix) AND **HE renders in Hebrew** with correct singular/plural (הוקצתה/הוקצו) + the `box_others` concat; check-i18n clean; light 1280 + dark 800 pageerror==0, 0 `[i18n] miss`.
- [x] 2026-07-20 | (S114 Pass A — recurring-pattern sweep) | site-wide (stalest pass, last A S103) | **Pass A — CLEAN, 0 hits** across the 3 ACTIVE consequence-critical classes over `36161c0..fd0fd11` (S104–S113 loop + the substantial **outside-loop FM v5.2→v5.4 drift**: combined-form mark-grid tiles, faithful שׁ/שׂ dot-stacking checks, the וֹ export shaping-guard, the Holam-Haser-on-vav split). **unescaped-input:** the new FM tile/stacking code interpolates only trusted static codepoints (`pc.target`), `esc()`-wrapped form `name`s (visible spans + inside `esc()`-wrapped `title` attrs), and trusted i18n strings; no new untrusted `JSON.parse`/`Object.assign` (streak →4). **localStorage-vs-AllTools:** 0 new keys — all new state is `project.*` (IndexedDB autosave, FM backup-exempt by design) (streak →8). **destructive-bulk:** the v5.4 migration seeds `cholamScale`/`holamScale` only `if (==null)` from a prior FB4B dot size (so existing saves look unchanged) and the `precomposed` backfill stays `=== undefined`-guarded — never clobbers a customized anchor/holam (streak →8). All 3 KEPT ACTIVE (consequence-critical carve-out). No new candidates logged.
- [x] 2026-07-19 | (S113 close-out) | branch/deploy note | "Rerun loop" → S113 **continues on branch `claude/improve-loop-lrlji3`, adding to the still-open draft PR #114** (S111+S112; open + unmerged → no branch restart, no new PR). **Drift check: no outside-loop change** (`origin/main` @ be4a6d9; `sw.js` v355, `FONT_MAKER_VERSION` 5.2). **Pass run: C** (accessibility, one tool, stalest since S102) — **trope_tutor.html**, its first-ever dedicated Pass C (variety-favorable, last touched S100). **a11y-strong / effectively CLEAN:** APG keyboard nav (roving tablist, radiogroup, aria-pressed); drill choices are real `<button>`s (aria-label=trope-name on symbol choices, aria-pressed two-tap melody), focus lands on #qPrompt after answering, #qFeedback role=status; settings drawer Escape-closes + restores focus to #gearBtn; all 5 form controls have `<label for>`; contrast clean both themes (muted 5.8 light / 5.6 dark; gold decorative/hover/preview only). **1 fix (d04b45c):** completed `prefers-reduced-motion` coverage (2 missed color transitions). **Budget: 1 pass + 1 fix = 2 of 5; stopped early** (tool a11y-clean; remaining backlog maintainer-design-calls + parked). `sw.js` **v355→v356** (trope_tutor.html precached + changed). No `FONT_MAKER_VERSION` bump. `check-i18n` clean (no strings touched); `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-19 | d04b45c | trope_tutor.html | **Complete `prefers-reduced-motion` coverage (S113 Pass C).** The reduced-motion block neutralized every *motion* transition (settings-modal slide, tour-spot, toast, toggle knob) and most hover-color transitions, but missed two color transitions — `.settings-close` (background 0.15s) and `.radio-group label` (0.15s) — so they still animated for reduced-motion users, contrary to CLAUDE.md's reduced-motion rule. Added both to the block's consolidated `transition:none` line (1-line CSS). Verified headless: under `reducedMotion:'reduce'` both compute `transition-duration:0s`; without it both still animate 0.15s (fix scoped correctly); pageerror==0.
- [x] 2026-07-19 | (S113 Pass C — accessibility, one tool) | trope_tutor.html (stalest pass, last C S102) | **Pass C — trope_tutor a11y-strong / effectively CLEAN; 1 completeness fix.** First-ever dedicated Pass C on the Trope Tutor (only prior dedicated-C tool: flash_cards S82). Audited keyboard/focus/aria/modals/reduced-motion/contrast: **keyboard** = APG roving tablist + radiogroup + aria-pressed (S51–55 work holds), drill choices real `<button>`s (symbol choices carry aria-label=trope-name since glyph shapes mean nothing to a SR; melody two-tap uses aria-pressed), focus moves to #qPrompt after answering, #qFeedback/#tuStatus role=status; **modals** = settings drawer Escape-closes + restores focus to #gearBtn (verified headless); **labels** = all 5 form controls have `<label for>` (a headless "no accessible name" flag was a false positive — the probe did not resolve `<label for>`); **contrast** = muted 5.81/6.15 light + 5.58 dark, gold only decorative/hover/preview (not body text) — clean; **reduced-motion** = 2 missed color transitions (fixed d04b45c). No candidates logged.
- [x] 2026-07-19 | (S112 close-out) | branch/deploy note | "Rerun loop" → S112 **continues on branch `claude/improve-loop-lrlji3`, adding to the still-open draft PR #114** (S111; open + unmerged → no branch restart, no new PR per protocol). **Drift check: no outside-loop change** (`origin/main` @ be4a6d9; my branch carries only the S111 commits; `sw.js` v354, `FONT_MAKER_VERSION` 5.2). **Pass run: K** (i18n/localization audit, stalest since S101) — Check A + B clean; the template-literal blind-spot probe found the recently-changed **non-FM surface clean** (torah/dictionary/dashboard/resources hits are content/data interpolation, not UI chrome) and surfaced the ledgered **FM orphaned-`status()` cluster**. **1 fix (b112c0b):** wired 11 orphaned FM status() toasts through I18n.t. **Budget: 1 pass + 1 fix = 2 of 5; stopped early** — remaining backlog is maintainer-design-calls + parked, and further FM i18n work would over-concentrate on one tool (FM was S111's focus too). `sw.js` **v354→v355** (Hebrew_Font_Maker.html precached + changed). **No `FONT_MAKER_VERSION` bump** (pure i18n wiring, not a feature). `check-i18n` clean; no `build-locales` (0 new keys); `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-19 | b112c0b | Hebrew_Font_Maker.html | **Localize 11 hardcoded FM `status()` toasts via existing keys (S112 Pass K — slices the S101 template-literal candidate).** 11 status() toasts were hardcoded-English JS **template literals** (invisible to check-i18n Check A, so a green gate was not proof) → Hebrew-UI users saw them in English. Each already had a matching `fontmaker.status.*` key with **EN+HE both filled** (orphaned since the i18n rollout) → pure wiring through `I18n.t()` (same pattern as the S106 torah fix); **0 new keys, no build-locales, EN byte-identical**. Wired: `mark_sheet_loaded` {label}; `loaded_image`/`area_selected`/`imported_svg`/`vector_selected` {name}; `preparing_letters` {i,total}; `drew_contour` {count} + `drew_contour_hole` fragment; and plural pairs `kept_points` / `copied_contours` {mod} / `pasted_contours` / `added_pairs` (count===1?.one:.other). Verified headless: **16/16 key+param cases EN byte-identical** to the old strings AND HE renders with params (no leftover {placeholder}); check-i18n clean; pageerror==0 (light 1280 + dark 800). ~6 sites left (2 "letter(s)", 2 "Rounded", guides prose).
- [x] 2026-07-19 | (S112 Pass K — i18n / localization audit) | site-wide (stalest pass, last K S101) | **Pass K — Check A + B clean; template-literal blind-spot probe → non-FM surface clean, FM orphaned-`status()` cluster sliced.** 5th K run. `check-i18n`: Check A clean (14 baselined, no new), Check B clean (0 attribute backlog left). Probed the Check-A-invisible template-literal pocket across the S102→S111 surface: the non-FM hits (torah holiday/ref `option` text, dictionary Word-List "name (n)" labels, glyph-name spans) are **content/data interpolation, not UI chrome** (correctly untranslated); the genuine cluster is the ledgered **FM `status()` toasts** — the CSV carries **129 `fontmaker.status.*` keys** with EN+HE filled but ~15 `status()` call sites were still hardcoded template literals (orphaned). Fixed a slice of 11 (b112c0b); logged the ~6 remainder on the candidate. No new candidates.
- [x] 2026-07-19 | (S111 close-out) | branch/deploy note | "/improveloop" → S111 on a **fresh branch `claude/improve-loop-lrlji3` off latest `origin/main` (be4a6d9, 0 divergence)** — PR #113 merged (S106–S110), so per the branch protocol a fresh branch + new **draft PR** at close-out. **Drift check: no outside-loop change** (`origin/main` @ be4a6d9 = the S106–S110 ledger-note commit; `sw.js` v353, `FONT_MAKER_VERSION` 5.1 — both match the S110 note). **Pass run: F** (cross-tool consistency, stalest since S100) — toast/status affordance **CONSISTENT** across all 7 tools (see Pass-F entry). **1 micro-feature (b006dbd):** FM "My IvritSuite Fonts" opener (the S97 Pass-H top seed). **Budget: 1 pass + 2 micro-feature = 3 of 5; stopped early** — the remaining backlog is all maintainer-design-calls, and an AskUserQuestion to pick a direction could not be delivered (session went non-interactive) → took the ledger's explicit top-pick seed autonomously (safe: draft PR, maintainer reviews before merge). `sw.js` **v353→v354** (Hebrew_Font_Maker.html precached + changed). `FONT_MAKER_VERSION` **5.1→5.2** (feature rule). `check-i18n` clean; `build-locales` run (new key); `update-sitemap` run. **Deploy `success` verified after merge.**
- [x] 2026-07-19 | b006dbd | Hebrew_Font_Maker.html (+ locales) | **FM "My IvritSuite Fonts" opener micro-feature (S111 — closes the S97 Pass-H top seed).** The shared-store manager (`openMyFonts` — save the current font for every IvritSuite tool + manage/delete) was reachable ONLY from the post-export success screen, so a teacher who had not just exported could not open it. Added a **"★ My IvritSuite Fonts…"** item to the **Save Project ▾** dropdown (`toggleSaveMenu`) that calls the existing `openMyFonts()`; +1 separator, +1 button, +1 i18n key `fontmaker.savemenu.my_fonts` (EN "★ My IvritSuite Fonts…" / HE "★ הגופנים שלי ב-IvritSuite…"). **No new state** (opens an existing window → no localStorage/AllTools/.ivrit wiring needed). FM v5.1→5.2 + About-tab changelog `<li>`. Verified headless over local HTTP on a **fresh profile** (empty localStorage + IndexedDB): item present (menu now 3 items), opens the window with the correct empty state, EN+HE (dir=rtl), light+dark × 1280/800, pageerror==0. ~8-line diff (well under the 150-line micro cap).
- [x] 2026-07-19 | (S111 Pass F — cross-tool consistency) | site-wide (stalest pass, last F S100) | **Pass F — toast/status-feedback affordance CONSISTENT across all 7 tools; 0 new small candidates.** 9th F run. generator/dashboard/flash_cards share `showAppToast` (`#appToast`); torah `showHintToast` (`ttHintToast`); trope `showToast` (`tuToast`); FM `status` (`#statusMsg`) — **all carry `role="status"`, all neutralize `@media (prefers-reduced-motion)`, all have a `body.dark` variant.** Also spot-checked the dark-toggle (visible-text "🌙 Dark" toggles need no aria-label; icon-only ones carry it — consistent), tour-launch buttons (all present + localized), and empty-state markup (context-specific by design; Pass I owns first-load). **The only gap is full toast *adoption*** — the dictionary has no toast and generator/dashboard/flash_cards still use `alert()` for ~17/13/16 sites — a **known large refactor** already tracked by CLAUDE.md's "Inline validation, never alert()" rule (opportunistic migration), NOT a small verified iteration. No small fix surfaced.
- [x] 2026-07-19 | (S106–S110 landed) | branch/deploy note | **S106–S110 landed on `main` via maintainer-authorized direct push** ("Push to main") — clean fast-forward 737a53f→c516210 (9 commits; 6th ledgered direct-push precedent after S78/S105). **PR #113 auto-recorded merged**, session auto-unsubscribed. **Pages deploy run for c516210 concluded `success`** (2026-07-19T21:14Z) → S106–S110 (torah status/karaoke i18n, dashboard skip-link + English-Font + starter copy + schedule-preset hint, Lakhish Bold OFL 1.1 license) are **live on ivritsuite.com**. `sw.js` v353. A future loop session cuts a **fresh branch off latest `main` (c516210)** since PR #113 is merged.
- [x] 2026-07-19 | (S110 close-out) | branch/deploy note | Maintainer supplied the Lakhish OFL license → S110 **continues on branch `claude/improve-loop-r9fb7r`, adding to draft PR #113** (S106–S109; open + unmerged + clean). **Drift check: no outside-loop change** (`origin/main` @ 737a53f; `sw.js` v352→**v353**, `FONT_MAKER_VERSION` 5.1). **Pass run: — (skipped; user-directed Lakhish license fix; Pass F stays stalest).** **1 fix (1bace42):** stated Lakhish Bold's SIL OFL 1.1 end-to-end (new license file + resources entry + doc). `sw.js` **v352→v353** (resources.html precached + changed); no `FONT_MAKER_VERSION` bump. `check-i18n` clean; `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-19 | 1bace42 | resources.html + THIRD_PARTY_LICENSES.md + fonts/LakhishBold-LICENSE.txt | **Lakhish Bold license stated (SIL OFL 1.1) end-to-end (S110, maintainer-confirmed — closes the S99 Pass-E legal call).** The gallery offered Lakhish for download + redistributed the binary with no stated license (siblings Reuben CC0 / TzviScript OFL name theirs). Maintainer confirmed OFL 1.1, © 2026 Baruch Sienna, Reserved Font Name "Lakhish". Wired the 3 sibling sites: new `fonts/LakhishBold-LICENSE.txt` (canonical OFL body + Lakhish header, byte-identical to TzviScript below the header); resources entry `license:'Other'→'OFL'` + `licenseFile` + `madeWithIvritSuite:true` (download now zips the real OFL text via the licenseFile path; the OFL gallery filter includes it); the THIRD_PARTY_LICENSES.md line states OFL 1.1 + file ref (mirrors TzviScript). Verified: resources loads clean (light/dark) pageerror==0; the produced download `LakhishBold.zip` = `.ttf` byte-identical to source + `LICENSE.txt` OFL w/ the Lakhish header, CRC-valid; check-i18n clean.
- [x] 2026-07-19 | (S109 close-out) | branch/deploy note | "Continue" → S109 **continues on branch `claude/improve-loop-r9fb7r`, adding to the still-open draft PR #113** (S106–S108; open + unmerged + clean → no branch restart, no new PR). **Drift check: no outside-loop change since S108** (`origin/main` @ 737a53f; `sw.js` v351→**v352**, `FONT_MAKER_VERSION` 5.1). Ran **Pass E (freshness & site-health, stalest since S99)** — mechanical checks **CLEAN** — then the maintainer picked **dashboard copy polish** (the S109 AskUserQuestion tool-stream closed; the user replied "dashboard copy polish"). **1 fix (74c8f76):** accurate 'Hebrew classroom' starter copy + a new-teacher schedule preset hint. `sw.js` **v351→v352** (classroom_dashboard + locales precached + changed); no `FONT_MAKER_VERSION` bump. `check-i18n` clean; `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-19 | 74c8f76 | classroom_dashboard.html (+ locales) | **Dashboard onboarding copy polish (S109, maintainer-directed — closes 2 S107 Pass-H candidates).** (1) The 'Hebrew classroom' starter said "Everything on — …" but doesn't enable the picker/video/dash-text (opt-in) → dropped the "Everything on —" prefix (EN+HE) so the desc accurately enumerates. (2) Schedule Sync soft dead-end: `renderScheduleValidation` now shows a muted `.sched-hint` ("Each block switches to a saved preset — create a couple in Presets first.") when `≤1 preset` AND `≥1 schedule row` — exactly when a new teacher has started a schedule but has nothing to assign; new key `dashboard.schedule.need_presets_hint` (EN+HE), `textContent` (XSS-safe), distinct `var(--muted)` style, and the validation container now shows for the hint alone. Verified: check-i18n clean; load matrix (light/dark×1280/800) pageerror==0; hint HIDDEN@0-rows / SHOWS@1-preset+row / HIDES@2-presets, renders in HE; starter drops "Everything on"/"הכול פעיל" in EN+HE.
- [x] 2026-07-19 | (S109 Pass E — freshness & site-health) | site-wide (stalest pass, last E S99) | **Pass E — mechanical site-health CLEAN.** 10th run; first over the S99→S108 surface. `update-sitemap` a no-op (all `<lastmod>` == git); **SW `CORE_ASSETS` 27/27 resolve** on disk; every root HTML precached except the intentional `i18n-test.html`; **0 broken internal links** (a Node scan of 344 local `href`/`src`/`url()` refs across all 12 pages — the only "hits" were JS template-literal/code false-positives: `data-rwsrc="all"`, SVG-builder `href="${url}"`, `@font-face` `url('${file}.woff2')`); all 5 bundled `fonts/*.ttf` named in `THIRD_PARTY_LICENSES.md`. Nothing to fix; the maintainer-directed dashboard copy polish (74c8f76) was the session's fix.
- [x] 2026-07-19 | (S108 close-out) | branch/deploy note | "Rerun" → S108 **continues on branch `claude/improve-loop-r9fb7r`, adding to the still-open draft PR #113** (S106+S107; open + unmerged + `mergeable_state` clean → no branch restart, no new PR). **Drift check: no outside-loop change since S107** (`origin/main` @ 737a53f; S106+S107 on the branch via PR #113, not merged; `sw.js` v351, `FONT_MAKER_VERSION` 5.1 — match the S107 close-out). Ran **Pass B (console & error audit, stalest since S98)** — **CLEAN, 0 fixes (discovery-only).** Selected **no fix**: the only clean-ish candidate (dashboard 'Everything on' P4) sits in the just-touched dashboard + carries a maintainer wording choice; every variety-favorable tool (trope S100 / FM S101 / flash_cards+generator S102) has only parked-perf / size-L / design-call candidates → no clean non-judgment win, so per the variety governor + "don't force marginal changes" this is a verification-only session (documented mature-loop outcome). **No precached file changed → no `sw.js` bump; no `FONT_MAKER_VERSION` bump.** `check-i18n` clean; `update-sitemap` no-op. Deploy `success` verified after merge.
- [x] 2026-07-19 | (S108 Pass B — console & error audit) | site-wide (stalest pass, last B S98) | **Pass B — CLEAN, 0 new candidates.** 9th run; first over the S98→S107 surface (S103 dashboard confirm, S105 dict rename, S106 torah i18n, S107 dashboard copy). Headless over local HTTP: 12 pages × light/dark = **24 runs, load + 1 interaction per tool** (index gear, generator Generate, dashboard settings, flash_cards start, dictionary search, torah settings, trope learn/drill, FM tile-click, resources) → **0 pageerrors, 0 `[i18n] missing key`, 0 failed local requests, 0 non-noise console errors/warnings.** An **unfiltered** distinct-message capture confirmed the only console output is the expected external-abort noise (Google-Fonts/gtag `net::ERR_FAILED` on every page + torah_trainer's documented offline Sefaria `fetchSefariaText` + esm.sh/jsdelivr hebrew-transliteration ESM artifacts — all route-aborted). Nothing to fix.
- [x] 2026-07-19 | (S107 close-out) | branch/deploy note | "Rerun" → S107 **continues on branch `claude/improve-loop-r9fb7r`, adding to the still-open draft PR #113** (S106; open + unmerged + `mergeable_state` clean → no branch restart, no new PR per the branch protocol). **Drift check: no outside-loop change since S106** (`origin/main` @ 737a53f; S106 on the branch via PR #113, not merged; `sw.js` v350, `FONT_MAKER_VERSION` 5.1 — match the S106 close-out). Ran **Pass H** (teacher walkthrough / paper-cuts, stalest since S97) on **classroom_dashboard** (its only prior H, S46, was a no-code audit) + **1 fix** (two dashboard copy paper-cuts db3221d). `sw.js` **v350→v351** (classroom_dashboard + locales precached + changed); **no `FONT_MAKER_VERSION` bump**. `check-i18n` clean (A+B); `update-sitemap` run. Deploy `success` verified after merge.
- [x] 2026-07-19 | db3221d | classroom_dashboard.html (+ locales) | **Two misleading dashboard copy labels fixed (S107 Pass H).** (1) First-run skip link "Skip — use New York for now" was **factually false** when a teacher typed a city — `frSkip()`→`frCaptureLocation()` keeps the typed location, so "Boston" gave Boston, not New York; neutralized to "Skip for now" (accurate in both cases). (2) The "Text Font" sub-section header in the Dashboard Text panel drives the **Latin/`engFont`** picker only (`#engFontOptions`/`ENG_PICKER`), while the Hebrew font sits in a separate panel — a Hebrew-typing teacher clicking it saw no change; relabeled **"English Font"** to parallel the existing "Hebrew Font" header. Both are i18n label changes (EN+HE for `dashboard.setup.skip` + `dashboard.text.sub_font`; build-locales run); no behavior change. Verified: check-i18n clean; load matrix (light/dark×1280/800) pageerror==0; both labels render correct in EN+HE, 0 "[i18n] missing key".
- [x] 2026-07-19 | (S107 Pass H — teacher walkthrough / paper-cuts) | classroom_dashboard.html (stalest pass, last H S97; dashboard's only prior H was the S46 no-code audit) | **Pass H — role-played a fresh teacher setting up today's class board; 2 copy paper-cuts fixed (db3221d), 3 candidates + 1 seed logged.** Headless fresh-context walkthrough + an Explore code-map. **Onboarding is strong** (Welcome modal sets location + 3 clear starter layouts; board is NOT empty behind it — first visit applies DEFAULT_PRESET with intro text; `setupSeen` shows once; schedule "until" auto-prefill repeats the last period length; validation covers unknown-preset/missing/dup/out-of-order; timer ~2 clicks; picker no-crash/no-alert; dark/lang/inputMode shared cross-tool). **Fixed:** the misleading "use New York" skip link + the ambiguous "Text Font"→"English Font" label. **Logged:** P3 Schedule-Sync soft dead-end for a 1-preset new teacher (explainer cites non-existent presets); P4 'Hebrew classroom' starter over-promises "Everything on"; P4 settings-drawer 12/13 panels expanded (design-call); Seed: `.dash-text-display:empty::before` placeholder (starter clears the intro text → bare strip).
- [x] 2026-07-19 | (S106 close-out) | branch/deploy note | "/improveloop" → S106 runs on a **fresh branch `claude/improve-loop-r9fb7r` off latest `origin/main` (737a53f, 0 divergence)**, new **draft PR** at close-out (PR #112 merged when S103–S105 were maintainer-direct-pushed to `main` → no open loop PR → fresh branch + new PR per the branch protocol). **Drift check: no outside-loop change since S105** (`origin/main` @ 737a53f = S105's own ledger-note commit; `sw.js` v349, `FONT_MAKER_VERSION` 5.1 — both match the S105 close-out). Ran **Pass I** (first-load & empty-state, stalest since S96) — CLEAN — + **1 fix** (torah_trainer i18n orphaned-key wiring 8f26ea9). `sw.js` **v349→v350** (torah_trainer precached + changed); **no `FONT_MAKER_VERSION` bump** (no FM feature). `check-i18n` clean (A+B); `update-sitemap` run. Deploy `success` verified after the maintainer merges.
- [x] 2026-07-19 | 8f26ea9 | torah_trainer.html | **Localize 11 hardcoded-English status/karaoke messages via existing keys (S106, Pass-K class — closes the localization half of the S77/S98 audio-unavailable candidate).** 11 `setStatus`/`setKaraokeStatus` sites still emitted hardcoded English (Check-A blind spots: template literals + plain function args): No-PocketTorah-manifest, Audio-basename-missing, Failed-to-load-timings, Loaded-N-timings, the word-count-drift note, Pick-a-parsha-to-chant, the Sefaria this-week / cant-determine-parsha messages, the translation-version fallback notes. Every one already had a matching `torah.status.*` key with **EN+HE both filled** (orphaned since the 07-12 rollout) → pure wiring through `I18n.t('torah.status.<key>', {params})`; **0 new keys, no build-locales**; EN byte-identical (English users unchanged, Hebrew users now get Hebrew). Verified: check-i18n clean; headless load matrix (light/dark×1280/800) pageerror==0; all 11 keys resolve EN+HE with correct {param} substitution + 0 "[i18n] missing key"; real path `goToCurrentParshah()` (Sefaria unreachable) renders localized `cant_determine_parsha` in `#ttStatus`.
- [x] 2026-07-19 | (S106 Pass I — first-load & empty-state) | site-wide (stalest pass, last I S96) | **Pass I — CLEAN, 0 new candidates.** 8th run; first over the S96→S105 surface (S98 dictionary Add-to-Word-List + dup-guard, S102 Print button, S103 Word Lists toolbar button + dashboard confirm, S104 resources zip). Headless over local HTTP, a **fresh browser context per run** (empty localStorage **+ IndexedDB**): 12 pages × light/dark × 1280/800 = **48 runs `pageerror==0`, 0 `[i18n] missing key`, 0 failed local requests**. Targeted empty-state interactions **8/8 OK**: the flagship S103 📚 Word Lists toolbar button opens an instructive empty manager ("No word lists yet…"); the S98 Add-to-Word-List action is correctly gated (`wlListCount()>0` → hidden for a fresh user, no dead button); dashboard/resources/FM/generator/flash-cards/trope all render instructively with no crash (generator fresh Generate → 1 worksheet). Nothing to fix.
- [x] 2026-07-19 | (S105 close-out) | branch/deploy note | "Rerun the loop" → S105 **continues on branch `claude/improve-loop-jhtu02`, adding to the still-open draft PR #112** (S103+S104; open + unmerged + `mergeable_state` clean → no branch restart, no new PR). **Drift check: no outside-loop change since S104** (`origin/main` still @ 7c09697; S103+S104 on the branch, not merged; `sw.js` **v348**, `FONT_MAKER_VERSION` **5.1** carried). Ran **Pass D (performance)** on **hebrew_dictionary** — the stalest-profiled tool (last dedicated D was the S14 multi-tool pass). **Effectively CLEAN, 0 fixes** — well-architected (paginated `PAGE=100` + debounced search/gematria); every real interaction <200 ms @1×; only a **517 ms cold-load boot longtask** (2.9 MB corpus parse+build, `data/` out of scope, progress-barred) and the **broadest** filter/search @4×-only (229/205 ms, ~all `Intl.Collator` on the 13k sort — a sort-branch hoist was **empirically ruled out at 0 ms**). Logged parked/informational, same category as the other tools' cold-load longtasks. Ran `check-i18n.js` (clean). **Iterations: 1 pass (D) + 1 maintainer-directed fix.** Pass D itself forced 0 fixes (discovery-only, precedent S45/S66/S67/S75); then, when the maintainer picked from the logged backlog, shipped the **dictionary Word-Lists styled inline rename + dup-name guard** (59ed584, the S68 balloon-risk item — replaces the raw `prompt()`, blocks renaming onto another list's name). `sw.js` **v348→v349** (hebrew_dictionary.html + locales precached); **no `FONT_MAKER_VERSION` bump** (FM untouched). Ran `build-locales.js` (2 new keys), `update-sitemap.mjs` (dictionary content changed). Remaining backlog: maintainer-flagged (torah Diaspora toggle, flash_cards card-face aria-live, Lakhish Bold license line) / other-pass (K) / parked-perf. **Landed on `main` via maintainer-authorized direct push** ("Push to main") — clean fast-forward `7c09697`→`381306d` (S103+S104+S105, 9 commits; 5th ledgered direct-push precedent after S78/S93/S94/S96–S97/S102); **draft PR #112 auto-recorded merged**, session auto-unsubscribed. **Pages deploy `success` verified** (run 29695790870, sha 381306d, 2026-07-19T16:54Z) — S103–S105 now live on ivritsuite.com.
- [x] 2026-07-19 | 59ed584 | hebrew_dictionary.html (+ locales) | **Word-Lists styled inline rename + dup-name guard (S105, maintainer-directed — closes the S68 Pass-H balloon-risk item).** The Word-List manager's Rename used a raw `prompt()` with **no duplicate-name check** (a list could be renamed onto another's name — a data-integrity asymmetry vs the S98 Save-flow block). Replaced with a **styled inline editor** rendered into the manager row (avoids nested-modal complexity in the single `#wordModal`): pre-filled input + Save/Cancel + aria-live status + Enter-to-commit, reusing the Save flow's exact dup guard (excludes self so an unchanged name is fine) + existing `enter_name`/`dup_name_suffix`/`name_placeholder`/`rename_prompt` keys; **2 new keys** (`rename_save`/`rename_cancel`, en+he). **No new state/localStorage** — mutates the existing `ivritSuite_wordLists` via the unchanged `wlRenameList` → no AllTools/`.ivrit` change. Delete's `confirm()` intentionally stays (destructive-action guard, §5). Escape closes the manager (the modal's capture-phase contract); Cancel + Enter cover the editor. **Verified** headless (light+dark × 1280/800): styled editor appears (**0 native prompts**), duplicate + empty blocked (store unchanged + error shown), Cancel preserves, unique rename + Enter-commit succeed, same-name allowed; `pageerror==0`; `check-i18n` clean.
- [x] 2026-07-19 | (S105 Pass D — performance) | hebrew_dictionary.html (stalest pass, last D S95; first dedicated D on the dictionary since S14) | **Pass D — effectively CLEAN, 0 fixes.** Empirically profiled cold-load + heavy interactions at 1× and 4× CPU throttle (CDP `setCPUThrottlingRate` + longtask observer). Corpus = **13,081 words** (2.9 MB). **Cold-load** 1036 ms @1× / 1170 ms @4× (one 517 ms @1× boot longtask = fetch+parse+build; progress-barred; `data/` out of scope). **Per-interaction, all fine @1×:** filter-all 70 ms, search 62 ms, one 100-card page 28 ms. **@4× only** the broadest filter/search cross 200 ms (229/205 ms), both debounced (150 ms); that cost is ~entirely `Intl.Collator.compare` × ~180k on the 13k sort (127 ms @4×; filter-only 4.4 ms). **Sort-branch hoist measured = 0 ms delta (ruled out); a collation-key precompute risks wrong Hebrew ordering (not clean).** Full-render-all (13,081 cards ~4 s @1×) is pathological (~130 "Load more" clicks) — pagination protects normal use. `pageerror==0` throughout. Emoji mode (457 KB, lighter) not separately deep-profiled. Logged the parked P4-informational finding.
- [x] 2026-07-19 | (S104 close-out) | branch/deploy note | "Run the loop again" → S104 **continues on branch `claude/improve-loop-jhtu02`, adding to the still-open draft PR #112** (S103; open + unmerged + mergeable_state clean → no branch restart, no new PR per the protocol). **Drift check: no outside-loop change since S103** (`origin/main` still @ 7c09697 = the S102 close-out; S103's work is on the branch, not merged; `sw.js` **v347**, `FONT_MAKER_VERSION` **5.1** carried from S103). Ran **Pass G (print & export fidelity)** on the **resources.html font-download-zip** — the first dedicated G on that export surface (the S104-pointer's flash_cards pick was wrong: flash_cards has **0 `@media print` + no printable/exportable student artifact**, only its `.ivrit` backup). Export verified end-to-end: all 4 fonts byte-accurate round-trip, zips CRC-valid, committed OFL/CC0 license files verbatim + Lakhish Bold's honest "License: Other" notice — **1 P4 fixed** (malformed DOS timestamp). `sw.js` **v347→v348** (resources.html precached); **no `FONT_MAKER_VERSION` bump** (FM untouched). Ran `check-i18n.js` (clean — no UI-string change), `update-sitemap.mjs` (resources content changed); archived S99 Done → ARCHIVE. **Iterations: 1 pass (G) + 1 fix = 2** — stopped early: after the pass fix, the backlog stays maintainer-flagged (torah Diaspora toggle, flash_cards card-face aria-live, Lakhish Bold license line) / other-pass (K) / parked-perf / balloon-risk (dictionary Rename dup-name needs a modal). Deploy verified after merge.
- [x] 2026-07-19 | d317e71 | resources.html | **Valid MS-DOS timestamp in the font-download zip (S104 Pass G, P4).** The dependency-free `makeZip` wrote a date field of `0` in every local-file-header + central-directory record → MS-DOS date (1980, **month 0, day 0**), not a representable calendar date (extractors tolerate it, but the download showed a malformed 1980-00-00 stamp). Set a fixed, valid, reproducible stamp — **1980-01-01 00:00** (`dosDate=0x21`, `dosTime=0`) via named constants used in both records. **Verified:** regenerated + re-extracted all 4 zips (`unzip -t` + Python `zipfile`, byte-identical `.ttf` round-trip, date now `(1980,1,1)`); end-to-end real `downloadFont()` click captured via Playwright download event across light/dark × 1280/800, `pageerror==0`; `check-i18n` clean.
- [x] 2026-07-19 | (S104 Pass G — print & export fidelity) | resources.html font-download-zip (stalest pass, last G S94) | **Pass G — export CLEAN + 1 P4.** First dedicated G on the resources font-gallery download export (`downloadFont`→`buildLicenseNotice`/committed `licenseFile`→`makeZip`→`.zip`). **Corrected the rotation pointer:** flash_cards (its suggested target) has **no print/export surface** (0 `@media print`, no `window.print`/PDF/CSV; only the `.ivrit` backup) — logged so future G runs don't re-pick it. Verified all 4 fonts (Reuben CC0, TzviScript + Stroke Guide OFL, outside-loop **Lakhish Bold**): **byte-accurate `.ttf` round-trip**, **CRC-valid zips** (`unzip -t` + Python `zipfile.testzip()` both pass), committed OFL/CC0 `LICENSE.txt` shipped verbatim, Lakhish Bold's generated notice is the honest "License: Other" (its actual license is the logged S99 P4 maintainer legal call — not invented here). One P4 fixed (DOS timestamp, d317e71). No other divergence.
- [x] 2026-07-19 | (S103 close-out) | branch/deploy note | "/improveloop" → S103 runs on a **fresh branch `claude/improve-loop-jhtu02` off latest `origin/main` (7c09697, 0 divergence)**, **draft PR #112** (PR #111 was auto-merged when S102 was direct-pushed to `main` → no open loop PR → fresh branch + new PR per the branch protocol). **Drift check: no outside-loop change since S102** (`origin/main` @ 7c09697 = the S102 close-out; `sw.js` **v345**, `FONT_MAKER_VERSION` **5.1** — both match S102's recorded values). `sw.js` **v345→v347** (precached: hebrew_dictionary.html + classroom_dashboard.html + locales/{en,he}.json + ui-strings.csv); **no `FONT_MAKER_VERSION` bump** (FM untouched this session). Ran `build-locales.js` (2 new keys total), `check-i18n.js` (clean), `update-sitemap.mjs` (dictionary + dashboard content changed). **Iterations: 1 pass (A) + 3 fixes** — the pass + top P3 (Word Lists top-level) closed at 2; then a **maintainer-directed 3rd fix** (dashboard `resetPanelLayout` confirm — the "log, don't force" P4, now explicitly authorized in-session). Other remaining backlog stays maintainer-flagged / other-pass (K) / parked-perf / balloon-risk.
- [x] 2026-07-19 | 62be610 | classroom_dashboard.html (+ locales) | **Confirm-gate `resetPanelLayout` (S103, P4 — maintainer-directed).** "↺ Restore default layout" discarded the teacher's custom movable-panel arrangement with no confirm/undo, unlike the suite's other reset buttons (`resetNikudColors`, reset-all-text-sizes). Added `if (!confirm(I18n.t('dashboard.layout.reset_confirm'))) return;` (canonical dashboard confirm pattern) + 1 new localized key (en+he, reusing existing "panel arrangement"/"default layout" terminology). The S93 Pass-A log said "log, don't force" (maintainer shipped no-confirm by choice); shipped now under explicit in-session maintainer direction. **Verified** headless (light+dark × 1280/800): dismiss preserves the sentinel custom layout, accept still resets to `DEFAULT_PANEL_LAYOUT`, dialog shows the localized message, `pageerror==0`; `check-i18n` clean.
- [x] 2026-07-19 | 439bb47 | hebrew_dictionary.html (+ locales) | **Word Lists top-level entry point (S103, P3 — closes the S68 Pass-H top candidate).** The flagship Word List flow was buried two collapse levels deep behind "Bulk Copy Mode"; added a **"📚 Word Lists" button** to the always-visible results `.toolbar` (next to 🖨️ Print, the S102 pattern) opening the existing `wlOpenManager()`. Reuses the `dictionary.wordlists.manager_button` label key + 1 new toolbar-title key (en+he); no new state/localStorage. Empty manager shows its instructive empty-state (not a dead-end). **Verified** headless over local HTTP (light+dark × 1280/800): button visible in `.toolbar`, click opens `#wordModal` with the empty-state, `pageerror==0`; `check-i18n` clean.
- [x] 2026-07-19 | (S103 Pass A — recurring-pattern sweep) | site-wide (stalest pass, last A S93) | **Pass A — CLEAN, 0 hits.** Swept the 3 active consequence-critical classes over the surface changed since S93's A sweep — the **outside-loop FM v5.0→v5.1** (precomposed shin/sin+ḥolam stacking, spacing-preview nikkud, Save-as-a-copy, help formatting, og-share), the **Resources font-gallery** additions (Lakhish Bold + mark-stripping preview), and the **S94–S102 loop changes** (`a22bfc0..HEAD`, 21 files). **unescaped-input/unsafe-parse:** the only new `innerHTML` sinks feeding user data (`meItem.name`/`lname`) are `esc()`-wrapped (S73 pattern); the rest are trusted static (`HELP_CONTENT` / i18n-CSV `buildAboutBody`, all values `esc()`'d) or clears; the 2 new `JSON.parse` are `JSON.stringify` clones (exempt); resources font-preview user text uses `textContent`. **localStorage-vs-AllTools:** 0 new keys anywhere (FM Save-as-copy reuses `recentProjects`; dictionary reuses `ivritSuite_wordLists`). **destructive-bulk:** FM precomposed reset is single-target + `udDo`-wrapped; the `data.precomposed` migration backfill is null-guarded (`=== undefined` only). Maintainer-suggested falsy-zero spot-check on the new FM coordinate code = clean (`!= null` / `Number.isFinite` throughout; retired, stays retired). Streaks: localStorage 6→7, unescaped-input 2→3, destructive-bulk 6→7 (all KEPT ACTIVE, consequence-critical).
- [x] 2026-07-19 | (S102 close-out) | branch/deploy note | "/improveloop" ("Rerun the loop") → S102 **continues on branch `claude/improve-loop-fhrbxq`, adding to the still-open draft PR #111** (S98–S101; not merged → no branch restart, no new PR). **Drift check: no outside-loop change since S101** (`origin/main` @ 33a1d3c; SW v344, FM 5.1). Ran **Pass C (accessibility)** — the stalest pass (last dedicated C S92); executed the two logged S100 Pass-F P3 a11y candidates + the S82 touch-target tail, scoped via **2 read-only Explore surveys**. **Full push: Pass C + 3 fixes.** 6e26a4d font-size control a11y convergence (generator + dictionary + torah — associate each existing visible label with its slider + reset aria; 0 new keys); 0e83432 flash_cards sub-30px touch targets → 30px min (`.tour-x`/`.btn-xs`, CSS-only); 1a37213 dictionary visible Print button (torah `printReading()` canonical, click-time guard via SR `announce()`; 3 new keys). **Deferred (logged):** the flash_cards card-face `aria-live` (S82 design call — survey mapped the path). `sw.js` **v344→v345** (generator + dictionary + torah + flash_cards + locales precached); **no `FONT_MAKER_VERSION` bump** (FM untouched). Ran `build-locales.js` (3 new keys → 3866), `check-i18n.js` (Check-A clean; advisory backlog + translation debt empty), `update-sitemap.mjs`; archived S97 Done → ARCHIVE. **Plan-mode note:** the `ExitPlanMode` gate hit the S98–S100 "permission stream closed" failure again (S101 was the exception); the read-only plan was finalized first, execution proceeded on the user's "Continue". **Maintainer-authorized direct push to `main`** ("Push to main after this" in-session) → the S102 commits land on `main`, GitHub auto-records draft PR #111 as merged; **4th ledgered direct-push precedent** (after S78/S93/S94/S96–S97) — the standing rule (loop work on a `claude/*` branch → draft PR) is unchanged, direct push stays the explicit-authorization exception. Pages deploys on push to `main`; deploy `success` verified this session.
- [x] 2026-07-19 | 1a37213 | hebrew_dictionary.html (+ locales) | **Visible Print button (S102 Pass C — closes the S100 Pass-F P3).** The dictionary had the suite's most elaborate `@media print` CSS but no button/`window.print()` (Ctrl+P only). Added a 🖨️ Print button to the results `.toolbar` (next to Copy link / Reset filters) on torah's `printReading()` canonical — a **click-time guard** (the dictionary has no visible toast → an empty search routes through the existing SR-only `announce()` instead of a blank print dialog); the toolbar is in the `@media print` `display:none` list so it never leaks into the printout. 3 new `dictionary.toolbar.print_button`/`_title`/`_empty` keys (en+he). Verified headless (light+dark): localizes EN→HE, `window.print()` fires with results, an empty search suppresses print + announces to `#a11yStatus`, 0 client rects under print media; check-i18n Check-A clean; pageerror=0.
- [x] 2026-07-19 | 0e83432 | flash_cards.html | **Sub-30px touch targets → 30px min (S102 Pass C, WCAG 2.5.5/2.5.8 — the S82 tail).** `.tour-x` (tour ✕) 26→30px + inline-flex centering, with its heading's reserved clearance (`.tour-card h4` `margin-inline-end`) bumped 26→30px in lockstep so the larger ✕ can't overlap the title; `.btn-xs` (~20px — color/emoji None·Main·All + `#fcSpeakBtn`) → `min-height`/`min-width:30px` + inline-flex centering (min-* over padding so the text buttons keep their content width). CSS-only, 0 new keys, no animation (reduced-motion untouched). Verified headless (light+dark, 1280+800): `.tour-x` renders 30×30 with no h4 overlap, `.btn-xs` floors at 30px while staying content-width; pageerror=0.
- [x] 2026-07-19 | 6e26a4d | generator + dictionary + torah | **Font-size control a11y convergence (S102 Pass C — closes the S100 Pass-F P3).** The 3 laggard font-size sliders had no accessible name + their ↺ resets no `aria-label`, vs the trope canonical. WCAG-correct fix — **associated each existing visible label** with its slider (`<span class="fs-label">`→`<label for="hebFontSizeSlider">` on generator/dictionary; `for=` added to torah's 3 sibling `<label>`s) so the accessible name == the visible text (2.5.3) + `aria-label`+`data-i18n-aria-label` on each reset, reusing each tool's existing title key (already annotated "aria-label only" in the CSV) → **0 new keys**, tag/attribute-only. All four tools' `applyI18n()` already call `I18n.applyStaticI18n()`, so it fills at load + live switch. Verified headless (light+dark): all 5 sliders report an associated label, resets expose a localized aria-label EN→HE; check-i18n Check-A clean; pageerror=0.
- [x] 2026-07-19 | (S102 Pass C — accessibility) | generator + dictionary + torah + flash_cards (stalest pass, last dedicated C S92) | **Pass C — form-control a11y convergence + touch targets.** Two read-only Explore surveys scoped the logged candidates against the trope canonical: (1) the font-size slider/reset accessible-name gap across the 3 laggards (survey found the WCAG-correct fix = associate the existing visible labels, and that the reset title keys were already authored as aria sources → 0 new keys); (2) the dictionary Print-button slot (no visible toast → click-time guard via `announce()`) + the flash_cards `.tour-x`/`.btn-xs` sub-30px targets (self-contained, one coupled `h4` margin). All three fixed this session (6e26a4d / 0e83432 / 1a37213). **Deferred:** the flash_cards card-face `aria-live` (S82 design call — survey mapped a concrete path: announce position on render + the pronounceable transliteration on flip). | run: 2026-07-19, S102
- [x] 2026-07-19 | (S101 close-out) | branch/deploy note | "/improveloop" ("Rerun the loop") → S101 **continues on branch `claude/improve-loop-fhrbxq`, adding to the still-open draft PR #111** (S98/S99/S100; not merged → no branch restart, no new PR). **Drift check: no outside-loop change since S100** (`origin/main` @ 33a1d3c, PR #111 open/draft/mergeable-clean, 0 review comments; SW v343, FM 5.1). Ran **Pass K (i18n / localization audit)** — Check-A clean (14 baselined); the **tooltip-attribute advisory backlog = exactly 7, all Hebrew_Font_Maker** static aria-labels (variety-favored: FM untouched since S97). **Full push: Pass K + 4 fixes = 5.** iter 2 **f257019** localized the 7 FM aria-labels (`data-i18n-aria-label` + 7 new `fontmaker.*_aria` keys) → **clears the whole advisory backlog to 0**; iter 3 **11e9880** wired FM `contoursPanelHTML()` to 14 pre-existing *orphaned* `fontmaker.node_editor.contour_*` keys (a Check-A template-literal blind-spot; 0 new keys — activated dead translations); iters 4–5 the non-FM blind-spot slices — **b049741** dashboard student-picker class-switcher `title`/`aria-label` (reused orphaned `dashboard.picker.switch_class`) + **20def99** dictionary emoji-card "View details for …" aria-label (reused orphaned `dictionary.card.aria_view_details`) — both 0 new keys. `sw.js` **v343→v344** (FM + dashboard + dictionary + locales precached); **no `FONT_MAKER_VERSION` bump** (i18n fixes, not a FM feature). Ran `build-locales.js` (7 new keys → 3863 total), `check-i18n.js` (Check-A clean, advisory backlog + translation debt empty), `update-sitemap.mjs`; archived S96 Done → ARCHIVE. **Plan-mode note:** unlike S98–S100 the `ExitPlanMode` gate succeeded normally this run. Deploy `success` verified after the maintainer merges.
- [x] 2026-07-19 | 20def99 | hebrew_dictionary.html | **Localize the emoji-card "View details for …" aria-label (S101 Pass K iter 5, Check-A blind-spot).** The emoji-grid card set its `aria-label` by concatenating English (`'View details for ' + e.english`) in a JS builder — invisible to Check A, English for Hebrew screen-reader users. Routed through `I18n.t('dictionary.card.aria_view_details', { word: e.english })` — the key already existed (orphaned, EN+HE) so **0 new keys**; the name goes through `I18n.t` param substitution + `setAttribute` (textContent-safe). Verified headless (light+dark): an emoji card's aria-label reads "View details for …" (EN) → "הצגת פרטים עבור …" after `setLang('he')`; pageerror=0.
- [x] 2026-07-19 | b049741 | classroom_dashboard.html | **Localize the student-picker class-switcher aria-label/title (S101 Pass K iter 4, Check-A blind-spot).** The picker card's "Switch class" `title`/`aria-label` was a hardcoded-English JS template literal (invisible to Check A). Wired both to `I18n.t('dashboard.picker.switch_class')` — the key already existed (orphaned, EN+HE) so **0 new keys**; the card re-renders via `applyI18n`. Verified headless (light+dark): `dashboard.picker.switch_class` resolves "Switch class" (EN) → "החלף כיתה" after `setLang('he')`; pageerror=0.
- [x] 2026-07-19 | 11e9880 | Hebrew_Font_Maker.html | **Localize the Contours panel — wire the orphaned `contour_*` keys (S101 Pass K iter 3, Check-A blind-spot).** `contoursPanelHTML()` rendered the node-editor Contours panel's labels/titles/aria (`Contour {n} — {n} pts`, Filled-shape / Hole-(counter) titles + aria, show/hide + delete titles/aria, the `Contours (n)` summary, hint, empty-state) as hardcoded-English template literals — a Check-A blind spot. Their matching `fontmaker.node_editor.contour_*` CSV keys **already existed + were already Hebrew-translated but never wired** (14 orphaned keys); replaced the literals with `I18n.t(...)` calls (interpolation preserved) → fixes the blind-spot **and** activates dead translations (**0 new keys**). Verified headless (light+dark) via an in-page fixture: the panel renders English → Hebrew via `setLang('he')` (`Contour 1 — 3 pts` → `קו מתאר 1 — 3 נק׳`, etc.); check-i18n Check-A stays clean; pageerror=0.
- [x] 2026-07-19 | f257019 | Hebrew_Font_Maker.html (+ locales) | **Localize the 7 remaining static aria-labels — clears the tooltip-attribute advisory backlog (S101 Pass K iter 2).** FM was the last tool with untranslated static `aria-label`s (the whole suite's remaining backlog): the ❓ Help button, the Workspace-step `.work-tabs[role=tablist]`, the Glyph-category `#catTabs`, the two `.ws-split` panel-resizer separators, the node-editor `#nodeBar[role=toolbar]`, and the `.npnl-mode[role=group]`. Added `data-i18n-aria-label` to each (keeping the English literal as the pre-JS fallback) + 7 new keys (`fontmaker.toolbar.help_aria`, `fontmaker.workspace.steps_aria`/`categories_aria`/`resize_left_aria`/`resize_right_aria`, `fontmaker.node_editor.toolbar_aria`/`mode_group_aria`, EN+HE) → FM's `applyI18n`→`I18n.applyStaticI18n()` fills them at load + live switch. **check-i18n's tooltip-attribute advisory backlog is now empty (0) across all 12 pages.** Verified headless (light+dark): the 7 elements' aria-label resolves EN on load → HE after `setLang('he')`; pageerror=0. | fixed: 2026-07-19, S101
- [x] 2026-07-19 | (S101 Pass K — i18n / localization audit) | site-wide (stalest pass, last K S91) | **Pass K — audit + burndown.** `check-i18n`: Check-A clean (14 baselined printed-output signatures); the tooltip-attribute **advisory backlog = exactly 7, all Hebrew_Font_Maker** static aria-labels (FM the only tool untouched in the last 2 sessions → variety-favored, and the Pass-K target). A Check-A blind-spot probe (template-literal / arg-passed English that escapes Check A) surfaced 3 clean slices: FM `contoursPanelHTML()` (matching `contour_*` keys already existed, unused), the dashboard picker-card `Switch class`, and the dictionary emoji-card `View details for …` — all fixed this session (f257019 / 11e9880 / b049741 / 20def99). **Residual blind-spot logged** (future K, size L): FM's ~17 `status()` toast strings + the guides-panel prose remain hardcoded-English template literals; the non-FM template-literal pocket is now cleared. | run: 2026-07-19, S101
- [x] 2026-07-19 | (S100 close-out) | branch/deploy note | "/improveloop" → S100 **continues on branch `claude/improve-loop-fhrbxq`, adding to the still-open draft PR #111** (S98/S99; not merged → no branch restart, no new PR). **Drift check: no outside-loop change since S99** (`origin/main` @ 33a1d3c, PR #111 0 review comments; SW v342, FM 5.1). Ran **Pass F (cross-tool consistency)** — surveyed the Hebrew font-picker via 2 read-only Explore agents; converged **2 font-picker a11y gaps**: (1) `aria-pressed` selected-state across all **6** pickers (Font Maker excluded — no `.font-opt` picker), onto the S86 pills canonical (the S90 "no canonical source" deferral is now resolved); (2) `lang="he"` on the preview glyph in the **4** laggards (dashboard/generator/dictionary/torah — trope/flash_cards already had it). **Stopped at 3** (Pass F + 2 convergences): dictionary/generator/dashboard/torah sit at the 2/tool cap; the cleaner single-laggard (**Print button → dictionary**) + **font-size a11y** (3-laggard) are logged as top F candidates for next session. `sw.js` **v342→v343** (all 6 picker HTML precached); **no `FONT_MAKER_VERSION` bump** (FM untouched); attribute-only → **no CSV/build-locales** (check-i18n Check-A clean). `update-sitemap.mjs` run; archived S95 Done → ARCHIVE. **Plan-mode interrupt again** (as S98/S99): approval gate failed in this non-interactive run; read-only survey ran first, fixes after plan-mode exit. Deploy `success` verified after merge.
- [x] 2026-07-19 | 90c0e48 | generator + dictionary + torah_trainer + dashboard | **Mark the font-preview glyph `lang="he"` on the 4 laggards (S100 Pass F).** The preview "אֶרֶץ" carried `lang="he"` in trope_tutor + flash_cards but not the other 4 → screen readers voiced the Hebrew sample in English. Added it to the 4 builders; dashboard's builder is shared with its English picker (cfg-driven), so the attribute is conditional (Hebrew picker only). Attribute-only. Verified headless (light+dark): the 4 laggards' previews now carry `lang="he"`, dashboard's English picker correctly 0, trope/flash unchanged; pageerror=0.
- [x] 2026-07-19 | 7c9b571 | all 6 font-picker tools | **Expose the selected font via `aria-pressed` across all 6 pickers (S100 Pass F).** The `.font-opt` buttons toggled only `.active`; converged onto the suite's `aria-pressed` selected-state idiom (S86 pills / generator toggles) — each builder + setter derives `aria-pressed` from the same active-condition. Covers both pickers on dashboard (shared cfg helper) + flash_cards (Heb `#fontGrid` + Eng `#engFontGrid`). Attribute-only, no `.active` behavior change. Verified headless (light+dark, all 6 incl. both dual pickers): `aria-pressed` matches `.active` (exactly 1 pressed); clicking a font moves it; pageerror=0.
- [x] 2026-07-19 | (S100 Pass F — cross-tool consistency) | Hebrew font-picker (6 tools; last F S90) | **Pass F — font-picker a11y convergence.** 2 read-only Explore agents surveyed the picker + alternative affordances. Found: `aria-pressed` absent on all 6 `.font-opt` pickers (S90's "no canonical" blocker resolved by the S86 pills) + `lang="he"` missing on 4 previews → both converged (7c9b571, 90c0e48). **Alternative affordances surveyed:** dark-mode/tour/gear buttons already converged; the cleanest untouched single-laggard = **Print button → dictionary** (dictionary has the elaborate print CSS but no button/`window.print()`, unlike generator + torah) — logged, deferred (feature-add on a cap-maxed tool); **font-size control a11y** (gen/dict/torah sliders lack accessible names + reset aria-labels vs the trope canonical) — logged. | run: 2026-07-19, S100

- [x] 2026-07-19 | (S99 close-out) | branch/deploy note | "/improveloop" → S99 **continues on branch `claude/improve-loop-fhrbxq`, adding to the still-open draft PR #111** (S98; not merged → no branch restart, no new PR, per the branch protocol). **Drift check: no outside-loop change since S98** (`origin/main` @ 33a1d3c; SW v341, FM 5.1 unchanged). Ran **Pass E (freshness & site-health) — CLEAN** (25 SW `CORE_ASSETS` resolve + every root HTML precached [only i18n-test.html excluded]; 11 sitemap `<loc>`→files + all `<lastmod>` == git; 0 broken internal links; splash/og/manifest assets resolve; `THIRD_PARTY_LICENSES.md` comprehensive; referenced scripts/data exist), then **3 Check-B i18n fixes; stopped at 4** (no clean caveat-free 5th on an untouched tool — FM's 7 remain, variety-disfavored). `sw.js` **v341→v342** (generator + dashboard + pwa.js + locales precached); **no `FONT_MAKER_VERSION` bump** (FM untouched). Ran `build-locales.js` (18 new keys), `check-i18n.js` (Check A clean; generator/dashboard/pwa.js dropped from Check-B), `update-sitemap.mjs`; archived S94 Done → ARCHIVE. **Plan-mode interrupt again** (as S98): the interactive approval gate failed in this non-interactive run; read-only Pass E ran first, fixes applied after plan-mode exit. Deploy `success` verified after the maintainer merges.
- [x] 2026-07-19 | 04348bb | pwa.js | **Drop the redundant static "Dismiss" aria-label literal (S99 Pass E iter 4, Check-B).** The install-banner close button carried `aria-label="Dismiss"` in the innerHTML template, immediately overwritten by `setAttribute('aria-label', t('pwa.aria.dismiss', 'Dismiss'))` before appendChild — already localized; the static literal was pure redundancy (pwa.js's last Check-B flag). Removed it (matches the banner's own `t()`-set aria-label); `pwa.aria.dismiss` already exists (EN+HE), no CSV change. Verified headless: forcing the Android banner, the close button reads "Dismiss" (EN) / "סגירה" after `setLang('he')`; pageerror=0.
- [x] 2026-07-19 | 9a4892a | classroom_dashboard.html (+ locales) | **Localize the in-place-editor "Finish editing" title (S99 Pass E iter 3, Check-B).** The ✓ Done button's label was localized but its `title="Finish editing"` had no `data-i18n-title` sibling; added one (`dashboard.editor.finish_editing`, EN+HE). Marked the student-roster placeholder `i18n-ignore` (entity-encoded mixed EN/HE example names a JS-set placeholder can't decode — illustrative content). Verified headless (light+dark): title resolves EN on load, → "סיום עריכה" via `setLang('he')`; pageerror=0.
- [x] 2026-07-19 | 6f1c5dc | hebrew_blend_generator.html (+ locales) | **Localize the activity-panel input aria-labels + reset titles (S99 Pass E iter 2, Check-B — clears the generator's whole backlog).** The bingo/word-search/crossword/matching/numbers/real-words-bingo/timer panels carried English `aria-label`s on their numeric inputs/selects with no sibling; added `data-i18n-aria-label` (17 new `worksheet.aria.*` keys, EN+HE) + wired the 2 remaining "Reset to default" titles to the existing `worksheet.common.reset_default` key (no new key). Transliteration-format placeholder → `i18n-ignore`. Verified headless (light+dark, 1280+800px): each resolves EN on load and switches to Hebrew via `setLang('he')`; pageerror=0.
- [x] 2026-07-19 | (S99 Pass E — freshness & site health) | site-wide (stalest pass, last E S89) | **Pass E — CLEAN.** Read-only mechanical audit: SW `CORE_ASSETS` (25) all resolve + every production page precached (only i18n-test.html excluded, intentional); 11 sitemap `<loc>` map to files + every `<lastmod>` == git commit date (update-sitemap a no-op); **0 broken internal `href`/`src`**; apple-touch splash (18 PNGs) + manifest icons + og/twitter images all resolve; `THIRD_PARTY_LICENSES.md` comprehensive & current (all CDN libs + APIs + fonts); README/CLAUDE-referenced scripts + data exist. **1 tiny nit logged** (not fixed — maintainer legal call): `fonts/LakhishBold.ttf` offered for download with no explicit license line. | run: 2026-07-19, S99

- [x] 2026-07-19 | (S98 close-out) | branch/deploy note | "/improveloop" → S98 runs on a **fresh branch `claude/improve-loop-fhrbxq` off latest `origin/main` (33a1d3c), 0 divergence**, with a **new draft PR** (S96+S97 were maintainer-authorized direct pushes to `main` → no open loop PR; per protocol → fresh branch + new PR). **Drift check: no outside-loop change since S97** (`origin/main` @ 33a1d3c = the S97 ledger record; SW v340, FM 5.1 both match the S97 note). Ran **Pass B (console/error audit) — effectively CLEAN**, then 2 fixes + the Add-to-Word-List micro-feature (full 5-iter budget: 1 pass + dup-guard + micro-feature[2] + torah toast). **NOTE — plan-mode interruption:** the session opened in plan mode and the interactive approval gate (ExitPlanMode/AskUserQuestion) failed repeatedly ("permission stream closed") in this non-interactive run; read-only work (ledger read, drift check, Pass B audit) ran first, and once plan mode was exited the fixes were applied/verified/committed normally. `sw.js` **v340→v341** (dictionary + torah HTML + locales precached changed); **no `FONT_MAKER_VERSION` bump** (Font Maker untouched). Ran `build-locales.js` (2 new keys), `check-i18n.js` (Check A clean — no new violations), `update-sitemap.mjs`. Deploy `success` verified after the maintainer merges.
- [x] 2026-07-19 | deb2e28 | torah_trainer.html | **Surface audio/chant-unavailable messages as a visible toast (S98 Pass B iter 5, P4).** The three no-audio early-returns — `chant_unavailable` ×2 (custom-range chant) + `aliyah_audio_unavailable` (`loadKaraoke`) — landed only on the buried `#optKaraokeStatus` / `#ttStatus` lines; each now *also* fires the visible `showHintToast` (role="status", auto-dismiss), reusing the existing keys (0 new i18n). Sidesteps the S93-feared "destination-change + sibling-i18n" entanglement by adding a toast beside the existing line. Verified headless (light+dark): forcing `loadKaraoke`'s no-audio branch shows the toast with the correct message; chant message displays; pageerror=0.
- [x] 2026-07-19 | 1e1e9c3 | hebrew_dictionary.html (+ locales) | **Per-word "Add to Word List" micro-feature (S98 — closes the S68 Feature seed).** Word-detail modal gains "➕ Add to Word List…" (guarded to `wlListCount() > 0`) → a small picker of existing lists → `wlMergeIntoList([wlWordFromDict(w)])` (deduped, 200-cap), reusing the already-registered `ivritSuite_wordLists` store (no new key, no AllTools change) + status strings; 2 new localized keys; picker's Hebrew word `lang="he"`; emoji modal excluded. Verified headless (light+dark, fresh profile = empty localStorage **and** IndexedDB, + 800px): button gating, picker flow, real dict word added 1→2, dedupe on re-add, correct `wlWordFromDict` shape, pageerror=0. 46-line diff (< 150 micro cap).
- [x] 2026-07-19 | a2c1159 | hebrew_dictionary.html | **Block duplicate Word List names at save time (S98 Pass B iter 2, P4 polish).** The "Save as new list" handler computed a `dup` flag but saved anyway and only appended a post-hoc note (two same-named lists in the manager); now guards *before* `wlAddList` — inline-warns via the styled `wlSetStatus` (reusing `dup_name_suffix`) + refocuses the name field. Zero new i18n. Verified headless (light+dark): duplicate blocked (list count stays 1, red warning), unique name saved (→2), pageerror=0.
- [x] 2026-07-19 | (S98 Pass B — console & error audit) | all 12 root pages (stalest pass, last B S88) | **Pass B — effectively CLEAN.** Headless over local HTTP, 12 pages × light/dark = **24 runs**, load + one interaction per tool: **`pageerror==0`, 0 failed local requests, 0 `[i18n] missing key`**. Only non-abort console output = **torah_trainer**'s documented offline artifacts (Sefaria `fetchSefariaText` "Failed to fetch" + the hebrew-transliteration esm.sh/jsdelivr ESM-CDN warnings — all external, route-aborted by the audit, gone over a real network). No regression across the S89→S97 surface; no new candidates.
- [x] 2026-07-18 | (S97 close-out) | branch/deploy note | "/improveloop" (continues after an interrupt) → S97 **continues on branch `claude/improve-loop-h22l25`, adding to the still-open draft PR #110** (S96; not merged → no branch restart, no new PR). **Drift check: no outside-loop change since S96** (`origin/main` still at `3a4ceee`; `sw.js` v339, FM 5.1 unchanged). `sw.js` **v339→v340** (precached files changed: Hebrew_Font_Maker.html, locales/en.json+he.json). **No `FONT_MAKER_VERSION` bump** (the FM change is a Help-copy correction, not a feature). `check-i18n` clean; `build-locales` rebuilt (0 new keys — edited existing `fontmaker.help.body_export`/`body_ivritsuite` cells); `update-sitemap.mjs` run last. **Stopped at 2 iterations (1 pass H + 1 fix)** — after the Pass-H P3 fix, the remaining backlog offered no clean variety-appropriate win (dashboard/flash-cards/index candidates are maintainer-deferred or soft; the generator Check-B aria remainder is niche P4 on a variety-disfavored tool touched S96), so no marginal work was forced. Variety: font-maker 1 (2/tool cap). **Post-close-out: maintainer directed "Push to main" → the S96+S97 commits land on `main` via an authorized direct push (clean fast-forward `3a4ceee`→`71dc0c4`; 3rd ledgered direct-push precedent after S78/S93); GitHub auto-records draft PR #110 as merged. The standing rule (loop work on a `claude/*` branch → draft PR) is unchanged — direct push stays the explicit-authorization exception. Deploy `success` verified on the resulting Pages run.**
- [x] 2026-07-18 | 2564beb | Hebrew_Font_Maker.html (+ locales) | **Fix stale Help copy pointing to a removed export-picker option (S97 Pass H, P3 — confusing copy blocking the core cross-tool payoff).** The Help `export` + `Use in IvritSuite` tabs told teachers to save their font for other tools by choosing **★ Use this Font in IvritSuite** "at the bottom of the export format picker" — but that picker only has TTF/WOFF2/TTF+WOFF2/UFO; the option was removed. A teacher following Help to get their font into the Generator/Flash Cards (the tool's key value prop) would hunt for a nonexistent option. Corrected both tabs to the real path — export → **Exported ✓** screen → **★ My IvritSuite Fonts** → **★ Save current font** — in the English `HELP_CONTENT` template + the Hebrew CSV (`body_export`/`body_ivritsuite`) + the mirrored English CSV source cells. | Verified headless: both Help tabs render corrected copy EN + HE (RTL), 0 residual "format picker" claims, `check-i18n` gate clean, `pageerror==0`. | fixed: 2026-07-18, S97
- [x] 2026-07-18 | (S97 Pass H — teacher walkthrough / paper-cuts) | Hebrew_Font_Maker.html (first-ever H on the Font Maker — the only never-H'd tool + freshest surface post-v5.1) | **Pass H — teacher walkthrough of the Font Maker ("turn my handwriting into a classroom font"); effectively CLEAN onboarding, 1 P3 fixed + 1 Feature seed.** Role-played a fresh teacher headless over HTTP: welcome modal + next-step hint ("Start by uploading and tracing your first letter"), 3 clear input methods (one-letter-per-image / one-image-all / existing-font-file), workflow tabs (Align/Trace/Place Nikkud/Trop), reference-glyph tracing guide, and the export guarded ("Trace at least one letter first" on an empty Export TTF — **not** a dead-end). **Found + fixed (2564beb, P3):** the Help misdirected teachers to a removed "★ Use this Font in IvritSuite" export-picker option for the core save-to-My-Fonts / use-in-other-tools payoff. **Logged 1 Feature seed:** the **My IvritSuite Fonts window is only reachable via the post-export success screen** (`openMyFonts` has no standalone toolbar/menu opener) — a teacher can't save/manage My Fonts without exporting first. **H history now complete for all 7 tools:** flash_cards S34, dashboard S46, generator S56, dictionary S68, torah S77, trope S87, **font-maker S97**. | run: 2026-07-18, S97
- [x] 2026-07-18 | (S96 close-out) | branch/deploy note | "/improveloop" → S96 runs on a **fresh branch `claude/improve-loop-h22l25` off latest `origin/main` (3a4ceee), 0 divergence** with a **new draft PR** (S95 loop branch `claude/improve-loop-jjtbjk` merged; the only newer commit on `main` = the improveloop skill's plain-language-recap doc, no app surface). **Drift check: no outside-loop app change since S95** (`sw.js` v338, FM 5.1 both match the S95 note). `sw.js` **v338→v339** (precached: Hebrew_Font_Maker.html, hebrew_blend_generator.html, torah_trainer.html, locales/en.json+he.json). **No `FONT_MAKER_VERSION` bump** — the FM change wired existing/new keys through `I18n.t()` (an i18n fix, not a feature). `check-i18n` clean; `build-locales` rebuilt for 8 new keys; `update-sitemap.mjs` refreshed the changed pages' `<lastmod>`. **Full 5-iteration push: 1 pass (I) + 2 fixes + 1 micro-feature (2 iters) = 5.** Variety: font-maker 1 + generator 1 + torah 1 (2/tool cap); i18n class 2 (at cap) + micro-feature 1. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-18 | 48e0e42 | torah_trainer.html (+ locales) | **Visible Print button (S96 micro-feature — closes the torah Print-button Feature seed).** The tool's core use case is printing readings for students, yet the only print affordance was Ctrl+P (undiscoverable; unmentioned by tour/How-To/FAQ). Added a `🖨️ Print` button to the always-visible `.tt-controls` toolbar → `printReading()`, which guards the empty state (`!currentVerses.length` → the existing `showHintToast` "Load a parsha first to print." instead of a blank print dialog) then `window.print()`. The print `@media` already hides `.tt-controls`, so the button never leaks into the printout. No persisted state (no AllTools/`.ivrit` wiring). 3 new keys `torah.controls.print_button`/`_title`/`_need_reading` (en+he). | Verified headless (micro-feature bar): button present + localized EN "🖨️ Print"/HE "🖨️ הדפסה" (RTL), empty-guard toast fires with 0 `print()` calls, `print()` fires once a reading is loaded, `.tt-controls` `display:none` under print media, fresh browser context, `pageerror==0` light/dark×1280/800. | shipped: 2026-07-18, S96
- [x] 2026-07-18 | ba640c0 | hebrew_blend_generator.html (+ locales) | **Localize the always-visible Layout-panel aria-labels (S96, P4 Check-B slice).** The Layout panel's 4 numeric/text inputs (Total Blends, Class Set versions, Title, Subtitle) carried English `aria-label`s with no `data-i18n-aria-label` sibling — Hebrew screen-reader users heard English on the first controls a fresh user sees. Total Blends reuses `worksheet.layout.total_blends`; added `class_set_versions_aria`/`title_aria`/`subtitle_aria` (en+he). The 17 activity-panel/timer aria-labels (bingo/word-search/crossword/matching/numbers) remain a follow-up Check-B slice. | Verified headless: all 4 accessible names EN unchanged → HE localized (סך הצירופים / גרסאות סט כיתתי / כותרת דף העבודה / כותרת משנה של דף העבודה); `check-i18n` gate clean; `pageerror==0`. | fixed: 2026-07-18, S96
- [x] 2026-07-18 | 394d1ea | Hebrew_Font_Maker.html (+ locales) | **Localize the mark-editor & auto-detect dynamic titles/scope notes (S96, P3 — closes the FM instructional-prose candidate).** The mark-editor modal title, its two `meScopeNote` `<b>`-laden prose blocks, the `Apply to current letter (<name>)` button, and the auto-detect modal title were hardcoded-English JS template literals (Check-A blind spot) → a Hebrew user saw English chrome. Wired all 5 sites through `I18n.t()`: `ad_title_sheet`/`markedit_scope_all`/`markedit_scope_letter` already existed (with Hebrew) but were never wired to their call sites; `markedit_title_named`/`markedit_apply_current_named` added. Preserved the `<b>` emphasis by adding the bold spans to the 2 scope-note CSV values (en+he), so English is unchanged and Hebrew bolds the parallel phrases; `esc(name)` preserved (name esc'd before the non-escaping `{name}` substitution). | Verified headless: EN scope notes/titles/button byte-identical to the old hardcoded strings (3 bold spans each), HE localized incl. RTL bold at dark/800; `check-i18n` gate clean; `pageerror==0`. | fixed: 2026-07-18, S96
- [x] 2026-07-18 | (S96 Pass I — first-load & empty-state) | all 12 root pages | **Pass I — first-load & empty-state, 7th run; first over the S87→S95 loop surface + the outside-loop FM Beta→v5.0→v5.1 / Resources font-gallery drift; effectively CLEAN.** Headless over local HTTP, a **fresh browser context per run** (empty localStorage **+ IndexedDB**), all 12 pages × light/dark × 1280/800 = **48 runs `pageerror==0`, 0 local-request failures, 0 `[i18n] miss`**. Every tool's empty state is instructive (FM welcome + "0/27 letters traced"; generator "Ready to Generate" + quick-start recipes; dashboard "Let's set up your dashboard" starter; flash cards setup + "Start Practicing"; dictionary "13,081 words"; trope Learn intro; torah auto-loads or shows an informative Sefaria error; resources 42 links). Demo/sample paths all produce output with 0 errors (generator recipe→worksheet, dashboard starter→applied, flash-cards deck→card face, trope drill→"Question 1 of 10"). Font-picker "My Fonts" reads the empty IndexedDB with no crash. Nothing to fix — worked the backlog for the remaining iterations. | run: 2026-07-18, S96
- [x] 2026-07-18 | (S95 close-out) | branch/deploy note | "/improveloop" → S95 runs on branch `claude/improve-loop-jjtbjk` (checked out on latest `origin/main` `f89f7b0`, 0 divergence — fresh start) with a **new draft PR** (prior loop branch `claude/improve-loop-y54736`/S94 merged to `main`). **Outside-loop drift since the S94 ledger (which recorded v330 / FM 4.30):** the live tree is **`sw.js` v337 / `FONT_MAKER_VERSION` 5.1** — the maintainer graduated the Font Maker **Beta→v5.0→v5.1** (nikkud in the spacing preview 00aceb5, dotted shin/sin+ḥolam stacking f89f7b0, help-window formatting 9845516, og-share image) + added Resources font-gallery entries (Lakhish Bold 642fe89, preview nikkud/te’amim strip da6500c). Heavy **Pass-A territory** → folded into the **next Pass A** (last ran S93); did NOT hijack this Pass-D session. This session `sw.js` **v337→v338** (precached changed: hebrew_dictionary.html, torah_trainer.html, locales/en.json+he.json). **No `FONT_MAKER_VERSION` bump** (no FM code touched). `check-i18n` clean; `build-locales` rebuilt for 5 new keys (4 dict wordlist-banner + 1 torah tour); `update-sitemap.mjs` run last (dictionary+torah → 2026-07-18). **4-iteration session: 1 pass (D) + 3 fixes** — stopped at 4 (no clean 5th under the variety governor: dict at the 2/tool cap, torah remaining = maintainer-calls / Pass-K-entangled, no clean cold-tool or trope-perf candidate). Variety: dict 2 + torah 1 + trope-tutor (Pass D, cap-exempt). Loop branch → draft PR; Pages deploys on merge — verify `success` after merge.
- [x] 2026-07-18 | df464f7 | hebrew_dictionary.html | **Sanitize tab/newline/CR in Anki TSV export fields (S94 Pass G candidate, P4 export-fidelity parity).** `exportAnki` tab-joined 6 fields under `#separator:tab`/`#html:false` with no escaping (while `exportQuizlet` uses `csvEscape`); a literal tab would split a column, a newline spill a row. Anki TSV can’t quote → collapse per-field `[\t\r\n]`→space. Latent today (bundled corpus has none), defensive parity for a future free-text word source. | Verified headless: a crafted word with embedded tabs + a newline exports as one well-formed 6-field row (was 8 fields across 2 rows); a normal word unchanged; headers intact; `pageerror==0`. | fixed: 2026-07-18, S95
- [x] 2026-07-18 | adae250 | torah_trainer.html (+ locales) | **Hold the tour’s reading steps until the reading loads (S77 Pass H, P3 — closes the candidate).** On a slow first load, opening the tour before the Sefaria fetch resolved made `_tourShow` permanently skip s4 (read/chant/loop) + s5 (Hebrew text) — a truncated tour missing the two key steps. Mark s4/s5 `waitForReading`; track an in-flight fetch (`_readingLoading`); hold on a still-loading reading step ("Loading the reading…", spotlighting `#ttReading`) instead of skipping; `renderText`/`renderEmptyReading`/the fetch-catch re-drive via `_tourOnReadingChanged` — show the step on arrival, skip cleanly on empty/failure (never a stuck tour). New key `torah.tour.reading_loading` (HE filled). | Verified headless (light/dark × desktop/800): reading-present = all 6 steps; slow-load holds on s4 then delivers s4/s5/s6; empty skips; failed fetch releases; Escape ends; `pageerror==0`. | fixed: 2026-07-18, S95
- [x] 2026-07-18 | 5112e65 | hebrew_dictionary.html (+ locales) | **Active-list banner for "Load as filter" (S68 Pass H, P3 — closes the live half).** Loading a saved Word List set a live-only `wlFilterOverride` whose view silently vanished on the next filter change, with no on-screen sign it was active. Added a persistent "Saved list: {name} · {n} words" banner + explicit ✕ Exit below the filter chips; hide on any real filter change (`onFilter`) or mode switch (`setDictMode`); re-localize on a live language switch (`applyI18n`). Name via `textContent` (XSS-safe); 4 new i18n keys (HE filled). No new storage keys (transient UI). | Verified headless (light/dark × desktop/800): load shows the banner; a filter change + the Exit button both hide it and clear the override; EN→HE preserves + re-localizes; `pageerror==0`. | fixed: 2026-07-18, S95
- [x] 2026-07-18 | (S95 Pass D — performance, one tool) | trope_tutor.html (first-ever dedicated D — the only never-D’d tool, coldest by loop-touch S87) | **Pass D — performance profile of the Trope Tutor.** Headless over local HTTP, 4× CPU throttle: cold load + the heavy paths (index fetch/parse, learn-card DOM build, drill build + 10-question session, audio clip engine). **Effectively CLEAN baseline:** desktop @1× = 0 longtasks (~176 ms load); only @4× does cold init fan into one-time throttled-only boot longtasks (max ~385 ms / ~1146 ms total across 7 tasks) — same P4-informational category as the parked dashboard/FM cold-load siblings (logged as a Candidate). All interactions well under the 200 ms bar even @4× (drill session 94 ms, `renderLearn` 6 families 21 ms, `startDrill` 1.7 ms). 0 pageerrors across light/dark × desktop/800px; console noise = external-abort only (0 same-origin failures). No actionable small-diff fix → parked.
- [x] 2026-07-18 | (S94 close-out) | branch/deploy note | "/improveloop" → S94 runs on a **fresh branch `claude/improve-loop-y54736` off latest `origin/main` (a22bfc0)** with a **new draft PR** (S93 landed on `main` via an authorized direct push → prior loop work merged; per the branch protocol a merged PR → fresh branch + new PR). **Outside-loop drift since S93 (v328 / FM 4.29):** one maintainer commit — `a22bfc0` "Font Maker: Save as a copy…" (duplicate a font into a variant) — moved `sw.js` **v328→v329** and `FONT_MAKER_VERSION` **4.29→4.30** (+9 filled en/he i18n keys), touching only `Hebrew_Font_Maker.html`. A project-duplication feature (Pass-A territory) that landed **after** S93's Pass A → **unswept**; noted for the next Pass A, did NOT hijack Pass G (not a print/export surface). This session `sw.js` **v329→v330** (precached files changed: hebrew_dictionary.html, torah_trainer.html, locales/en.json+he.json). **No `FONT_MAKER_VERSION` bump** (no FM change this session). `check-i18n` clean (no new violations); `build-locales` rebuilt for the 2 edited `torah.about.faq_a2/a3` cells (0 new keys); `update-sitemap.mjs` run last (dictionary+torah → 2026-07-18). **Full 5-iteration push: 1 pass (G) + 4 fixes = 5**, across dictionary (2: print + CSV BOM — both pass-found, cap-exempt) + torah (2: this-week chip + FAQ contrast) — within the 2/tool cap. **Landed via a maintainer-authorized direct push to `main`** ("Push to main" in-session; clean fast-forward `a22bfc0`→`a1b0aba`) — draft PR #105 auto-recorded as **merged**. **3rd ledgered direct-push-to-`main` precedent** (after S78, S93); the standing rule (loop work on a `claude/*` branch → draft PR) is unchanged — direct push stays the explicit-authorization exception. GitHub Pages deploys on push to `main` — deploy `success` verified on the resulting Pages run.
- [x] 2026-07-18 | f1cc90e | torah_trainer.html (+ locales) | **FAQ credit links → `--gold-text` for AA contrast (S72 Pass C G3 residual, P4 — closes the candidate).** The 2 About-FAQ inline links (PocketTorah/Sefaria, rendered via `data-i18n-html`) still used `color:var(--gold)` (~3.2:1 on the cream FAQ panel); swapped to `var(--gold-text)` in the markup fallbacks + the `torah.about.faq_a2`/`faq_a3` en+he cells of `ui-strings.csv`, then rebuilt locales (0 new keys). Dark unchanged (there `--gold-text`=`--gold`). | Verified headless: both links render `#7f5a13` at **5.88:1** on cream, the rendered `<a>` style carries gold-text (confirming the JSON path); `check-i18n`+`build-locales` clean; `pageerror==0`. | fixed: 2026-07-18, S94
- [x] 2026-07-18 | 7fd0ab3 | torah_trainer.html | **Surface "this week's parsha" as a toolbar chip (S77 Pass H, P3 paper-cut).** Jumping back to the current week was only reachable via gear→expand the collapsed "Reading schedule" panel→click; added a compact "Jump to this week's parsha" chip into the always-visible `.tt-controls` toolbar (next to `#parshaSelect`), reusing the existing `goToCurrentParshah()` handler + `torah.schedule.jump_this_week` key (0 new keys, no state). The panel button stays (with the Diaspora/Israel toggle). | Verified headless (1280/800 × light/dark): the chip is in `.tt-controls` (not a collapsed panel), visible, i18n-filled, wired, theme-adaptive; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-18, S94
- [x] 2026-07-18 | cc09372 | hebrew_dictionary.html | **Lead the Quizlet CSV export with a UTF-8 BOM (S94 Pass G, P3 export fidelity).** `exportQuizlet` wrote a BOM-less UTF-8 `.csv`; the on-screen hint tells users to open it in Word/Excel, where Windows-Excel mojibakes Hebrew without a BOM. Prepended `'﻿'` to the CSV only — the Anki `.txt` is untouched (a BOM would break its leading `#separator:tab`; Anki reads UTF-8 without one). | Verified headless by capturing both real downloads: CSV now starts `EF BB BF` then `Term,Definition` (Hebrew intact); Anki still starts `#separator:tab`, no BOM; `pageerror==0`. | fixed: 2026-07-18, S94
- [x] 2026-07-18 | 8dc3505 | hebrew_dictionary.html | **Add the first `@media print` block (S65 Pass G, P3 print fidelity — closes the candidate).** The dictionary had no print stylesheet, so dark-mode Ctrl+P prints near-white `--text` invisibly on white paper (or floods `--cream`/`--white` ink when backgrounds are on) and leaks the navy header, 300px sidebar, and fixed chrome (`#fsBtn`, sidebar toggles, `#tipFloat`, modals, tour overlay). Added a print block (kept last): force the light palette + white paper for both themes, hide chrome, unclip `.app/.main/.content` overflow, `break-inside:avoid` on cards. | Verified headless (print emulation, Letter+A4, light+dark): body prints white bg + navy ink and all chrome is `display:none` in both themes; screen unaffected (100 cards render at 1280/800 × light/dark); `pageerror==0`. | fixed: 2026-07-18, S94
- [x] 2026-07-18 | (S94 Pass G — print & export fidelity) | hebrew_dictionary.html (stalest print tool, last G S65) | **Pass G — print+export audit of the dictionary.** Headless over local HTTP (print emulation Letter+A4 × light/dark; drove the real Anki/Quizlet exports). **Found + fixed in-pass (cap-exempt):** no `@media print` block at all → dark-mode ink-bleed + chrome leak (8dc3505); Quizlet CSV lacked a UTF-8 BOM → Excel mojibake (cc09372). **Confirmed sound:** Anki/Quizlet builders preserve nikkud, Quizlet fields route through `csvEscape`, and word selections all originate from the clean bundled corpus (`hebrew_words.json`/`hebrew_emojis.json` — 0 embedded tab/newline). **Logged P4 (informational):** `exportAnki` doesn't escape tab/newline like Quizlet's `csvEscape` — a real parity gap but **not reachable** today (no free-text word entry); a future free-text source would make it reachable. | run: 2026-07-18, S94
- [x] 2026-07-17 | (S93 close-out) | branch/deploy note | "/improveloop" → S93 runs on a **fresh branch `claude/improve-loop-7ugf93` off latest `origin/main` (f76c4d0)** with a **new draft PR** (the prior loop PR #99 / `claude/new-session-2z7kfq` **merged** into main before this session — S89–S92 are now on main; per the branch protocol a merged PR → fresh branch + new PR). **Outside-loop drift since S92 (v323):** the maintainer landed **9 commits on 2026-07-17** — the **dashboard movable-widget-panels feature** (639fcf8 + review-fixes 53e50fc), Referrer-Policy meta on all pages (42c4825), non-render-blocking Google Fonts (bbc36b2), network-first `/locales/*.json` in sw.js (45d1bad), and the `/improveloop` skill files — moving `sw.js` **v323→v327**, FM unchanged at 4.29. This session: `sw.js` **v327→v328** (precached files changed: torah_trainer.html, hebrew_dictionary.html, Hebrew_Font_Maker.html). **No `FONT_MAKER_VERSION` bump** (the FM change was a pure dead-code prune, not a feature). check-i18n clean (no new violations; 0 CSV keys → no build-locales); `update-sitemap.mjs` run last. **Landed via a maintainer-authorized direct push to `main`** (`f76c4d0`→`006c0ac`, clean fast-forward, "push to main" in-session) rather than a manual PR merge — GitHub auto-recorded draft PR #103 as merged. **2nd ledgered direct-push-to-`main` precedent** (after S78); the standing rule (loop work on a `claude/*` branch → draft PR) is unchanged — direct push stays the explicit-authorization exception. Deploy `success` verified on the resulting Pages run.
- [x] 2026-07-17 | 707cd80 | Hebrew_Font_Maker.html | **Removed the inert `#addebug` auto-detect diagnostic scaffolding (S64 Pass A candidate; P4 cleanup).** The Firefox root cause was fixed by `.ad-box{display:inline!important}` (v4.11); the hash-gated `_adDebug()` gate + the ~55-line `#addebug` readout in `renderAdStage` (SVG geometry probes + `#adDbg` overlay) only ran with `#addebug` in the URL → 4.1 KB of dead code removed; `.ad-box` fix + v4.11 changelog stay. No FM version bump (pure cleanup). Verified headless (light+dark, desktop+800px): `_adDebug` undefined, `renderAdStage`/`_adWire` intact, `.ad-box` CSS present, pageerror==0.
- [x] 2026-07-17 | 2c670de | hebrew_dictionary.html | **Word-List save-status success color → `--gold-text` (gold-on-light contrast).** `wlSetStatus` painted success messages in raw `var(--gold)` (~2.5:1 on the light word-detail modal); swapped to `var(--gold-text)` (light #87681d ≈5.2:1; dark = `var(--gold)`, unchanged); error branch stays #c0392b. (The translit-credit link L967 is `data-i18n-html` → left for a Pass-K/CSV slice.) Verified headless (light+dark, desktop+800px): success rgb(135,104,29)=5.21:1 / gold=7.96:1; error #c0392b; pageerror==0.
- [x] 2026-07-17 | ed47aba | torah_trainer.html | **Custom-range picker seeds Book/Chapter from the current reading (S77 Pass H, P3).** `toggleCustomPicker` only toggled visibility, so `#ttCustomBook` always reset to Genesis; `prefillCustomPicker` now seeds book + chapter + the correct chapter max (`TORAH_BOOK_CHAPTERS`) from `settings.customRef` (custom scope) or the current parsha's book (`parshiyotData`); holiday/unknown → Genesis default. No strings/CSS. Verified headless (light+dark, desktop+800px): custom→Deuteronomy 3/max34, Vayikra→Leviticus/max27, holiday→Genesis/max50; pageerror==0.
- [x] 2026-07-17 | 307849b | torah_trainer.html | **Gold-on-light text/links → `--gold-text` (S72 Pass C G3, P3, contrast).** `.tt-aliyah-inline`, `.tt-bnum` verse-superscripts, the 2 PocketTorah/CC audio-credit links, and the 5 Related-Tools links (9 sites) swapped from `var(--gold)` (~2.5:1 on cream) → `var(--gold-text)`. Gold-on-navy, hover/active, and the decorative drop cap + Star stay `--gold`; the 2 About-FAQ `data-i18n-html` links left for a Pass-K slice. Verified headless (light+dark, desktop+800px): swapped sites 5.88:1 light / 7.96:1 dark, untouched navy audio-bar link 9.9:1, pageerror==0.
- [x] 2026-07-17 | (S93 Pass A — recurring-pattern sweep) | changed surface since S83 (dashboard movable-panels feature 639fcf8/53e50fc + S84–S92 loop changes + the 07-17 maintainer commits) | **Pass A — CLEAN, 0 hits across all 4 active classes.** Focus = the outside-loop **dashboard movable-widget-panels** feature: `panelLayout` rides in the AllTools-registered `hebrewDashboard_settings` blob (localStorage-vs-AllTools ✓); loaded via `ivritSafeParse` + a `normalizePanelLayout` validator at every ingress (unsafe-parse ✓); zone reorder is pure array-index splicing with `=== undefined` guards, no numeric `||`-restores (falsy-zero ✓); no untrusted `innerHTML` added (unescaped-input ✓); `resetPanelLayout` is a reversible view-pref below the destructive-bulk bar (logged P4, not a hit). **falsy-zero reaches 3 consecutive clean sweeps (S73/S83/S93) → RETIRED** (correctness-scoped, not consequence-critical); the 3 consequence-critical classes stay ACTIVE.
- [x] 2026-07-16 | (S92 close-out) | branch/deploy note | "rerun the loop" → S92 **continues on branch `claude/new-session-2z7kfq`, adding to the still-open draft PR #99** (S89–S91; not merged → no branch restart, no new PR). No outside-loop drift since S91 (`origin/main` still at #98; `sw.js` v322, FM 4.29 unchanged). `sw.js` **v322→v323** (precached files changed: index.html, resources.html, contact.html, privacy.html, 404.html, torah_trainer.html, Hebrew_Font_Maker.html). **No `FONT_MAKER_VERSION` bump** (FM change = a 1-line footer contrast token swap, not a feature). `check-i18n` clean — **0 new i18n strings anywhere** → no CSV/`build-locales`; `update-sitemap.mjs` refreshed the 11 `<lastmod>`. **Full 5-iteration push: 1 pass (C) + 4 fixes = 5**, across resources (2: keyboard blocker + lang/modal polish) + index (2: modal focus/dialog + landmark/aria) + a cross-page contrast convergence (1, also touching contact/privacy/404/torah/FM) — each dedicated tool within the 2/tool cap; a11y theme (the C pass) + one suite-wide contrast slice (the S63 pattern). GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | d94aa5e | index.html | **Home page gets a `main` landmark + mode-toggle `aria-pressed` (S92 Pass C, P3).** The landing content (`.hero`) was a bare div (no primary-content landmark/skip target) and the Auto/Manual `.ivrit` input-mode toggle exposed its selected state only via a CSS class. Added `role="main"` on `.hero`; added `aria-pressed` to both toggle buttons and synced it in `setIvritMode` alongside `.active`. | Verified headless (light+dark): exactly one main landmark; `aria-pressed` starts auto=true/manual=false and stays in sync with `.active` on toggle; `pageerror==0`; `check-i18n` clean. | fixed: 2026-07-16, S92
- [x] 2026-07-16 | 6de9915 | resources.html | **Hebrew previews get `lang="he"` + the two modals get dialog semantics (S92 Pass C, P3).** The Hebrew sample text (font-card preview lines + the preview-modal render/sizes + the preview input) rendered without `lang="he"` — a screen reader would voice it in English; added it at those chokepoints. Gave the Suggest + Font-Preview modals `role="dialog"`/`aria-modal`/`aria-labelledby` (referencing their existing `<h3>` titles). Attribute-only. | Verified headless (light+dark): 3 font-card preview lines + the 3 preview elements carry `lang="he"`; both modals expose `role=dialog`/`aria-modal`/valid `aria-labelledby`; `pageerror==0`; `check-i18n` clean. | fixed: 2026-07-16, S92
- [x] 2026-07-16 | b00428f | index + resources + contact + privacy + 404 + torah_trainer + Hebrew_Font_Maker | **Gold-on-light text converged onto `--gold-text` across the chrome pages (S92 Pass C, contrast — the S63 pattern).** The `--gold-text` token (light `#7f5a13` ~6.2:1 / dark `var(--gold)`) had reached the 6 tool pages but no chrome page, so their always-visible body/footer links + text used raw `var(--gold)` (~2.7:1 on cream/white, failing WCAG 1.4.3). Added the token + swapped the always-visible sites (all 5 chrome footers, contact+privacy body links, resources `.card-grade`/`.font-by`/`#sgStatus`/success-toast, privacy `.ferpa h2`, 404 tool-links) + the two laggard tool-page footers (torah_trainer, Font Maker). Gold-on-navy stars, gold borders, and gold backgrounds stay `var(--gold)`; the giant decorative `.code-404` numerals left as-is. Also clears the **S72 G3** torah gold-tail footer slice. | Verified headless (light+dark, all 7 pages): swapped text computes `rgb(127,90,19)` in light and gold in dark; `pageerror==0`; `check-i18n` clean. | fixed: 2026-07-16, S92
- [x] 2026-07-16 | 4c59f00 | index.html | **The AllTools gear modal becomes a proper focus-managed dialog (S92 Pass C, P2).** `#ieBackdrop` had no dialog semantics and no focus management: focus never entered the modal on open (parked on the trigger), Tab escaped to the non-inert page behind the backdrop, and focus wasn't restored on close. Added `role=dialog`/`aria-modal`/`aria-labelledby` (h3 gets an id), focus move-in to the close button on open, focus restore to the trigger on close, and a lightweight Tab focus-trap; the Escape handler now routes through `closeIEModal` so it restores focus too. | Verified headless (light+dark): role/aria-modal/aria-labelledby resolve, focus moves in on open + restores to the gear on close, Escape closes, Tab/Shift+Tab wrap within the dialog (8 focusables); `pageerror==0`; `check-i18n` clean. | fixed: 2026-07-16, S92
- [x] 2026-07-16 | 806d23a | resources.html (Pass C — accessibility) | **Pass C (accessibility), first-ever dedicated C on `resources.html` — the Fonts-view keyboard blocker fixed (P2).** The Fonts view's only controls — Preview / Download / Use — were built as `<a onclick>` with no `href`/`role`/`tabindex`/keydown, so they were **mouse-only** (not focusable or Enter/Space-operable). Switched the three to `<button type="button">` and widened the `.font-actions` CSS to cover buttons with an appearance reset (identical rendering); labels still route through `esc(I18n.t(...))`. **Audit (2 parallel Explore agents, verified against live code):** resources + index were the two never-C'd pages; reduced-motion / focus-rings / i18n-wiring already clean on both; the suite-wide gold-on-light contrast gap (chrome pages never got `--gold-text`) surfaced as the convergence above. **Logged remaining (P4):** resources Suggest-modal focus-management (move-in/restore/trap), view-toggle/filter/pill `aria-pressed`/`aria-selected`, the `target=_blank` new-tab cue, and index nested-link-in-card + hero-`h2` semantic nits. | Verified headless (light+dark): all three actions are native buttons (tabIndex 0), keyboard activation opens the preview modal, `pageerror==0`; `check-i18n` clean. | run: 2026-07-16, S92
- [x] 2026-07-16 | (S91 close-out) | branch/deploy note | "Rerun" → S91 **continues on branch `claude/new-session-2z7kfq`, adding to the still-open draft PR #99** (S89–S90; `mergeable_state: clean`, not merged → no branch restart, no new PR). No outside-loop drift since S90 (`origin/main` still at #98; `sw.js` v321, FM 4.29 unchanged). `sw.js` **v321→v322** (precached files changed: hebrew_blend_generator.html, flash_cards.html, hebrew_dictionary.html, Hebrew_Font_Maker.html, torah_trainer.html, index.html, classroom_dashboard.html, locales/en.json+he.json). No `FONT_MAKER_VERSION` bump (i18n wiring only). `check-i18n` clean; `build-locales` rebuilt for the 2 new `fontmaker.workspace.*` keys; `update-sitemap.mjs` a no-op. **Full 5-iteration push: 1 pass (K) + 4 fixes = 5**, across generator (2: Sound + ftRead) + flash_cards (2: Sound + ftRead) + Font Maker (2: Undo/Redo + Editing) + dictionary (1) + index (1) + dashboard (1) + torah (1) — 7 tools, each within the 2/tool cap; i18n theme + 1 security fix (ftRead). GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | 3fa7fbc | torah_trainer.html | **Settings-close button i18n (S81 Pass K, Check-B).** The settings drawer's ✕ close carried `title="Close"` + `aria-label="Close settings"` with no `data-i18n-*` siblings (torah's last 2 Check-B items). Added `data-i18n-title=torah.settings.close_title` + `data-i18n-aria-label=torah.settings.close_aria` (keys existed). | Verified headless: EN "Close"/"Close settings" → HE "סגירה"/"סגירת ההגדרות" on a live switch; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S91
- [x] 2026-07-16 | 59b8733 | shared folder-tree (index + dashboard + flash_cards + generator) | **`ftRead` parses import-derived data via `ivritSafeParse` (S79 P4 security).** `ftRead` read `localStorage['hebrew<Tool>_<thing>Folders']` (written from imported `.ivrit`/AllTools via `ftImportTree`) with a bare `JSON.parse` — shape-checked but didn't strip `__proto__`/`constructor`/`prototype`. Swapped `JSON.parse`→`ivritSafeParse` IDENTICALLY in all 4 carriers (kept byte-identical). Latent (nodes read for names/ids), aligns the last folder-tree parse site with the security convention. | Verified: `ftRead` byte-identical across all 4 (sha e2150bee…); headless — legit tree round-trips, a `__proto__`-laden payload is stripped with no `Object.prototype` pollution, garbage falls back; `pageerror==0`. | fixed: 2026-07-16, S91
- [x] 2026-07-16 | aa7b1c6 | Hebrew_Font_Maker.html (+ locales) | **"Editing:" work-current header i18n (S81 Pass K blind-spot).** `updateCurrentGlyphLabel` built the persistent "Editing: <glyph> — <name>" indicator as hardcoded English (template literal). Added 2 keys `fontmaker.workspace.editing`/`_glyph`, routed both sites through `I18n.t()` — `{glyph}` (trusted markup) + `{name}` (already `esc()`'d) render via the unescaped substitution; `window.I18n`-guarded fallback + `applyI18n` re-render. Split off (logged P3): the larger `meScopeNote` prose + modal titles. | Verified headless: EN "Editing: א — Alef" → HE "עריכה: א — Alef"; an injected `<b>` name renders escaped; `build-locales`+`check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S91
- [x] 2026-07-16 | 93a31eb | Hebrew_Font_Maker.html | **Undo/Redo `aria-label` i18n (S81 Pass K, Check-B).** The 2 toolbar + 2 node-editor Undo/Redo buttons carried `data-i18n-title` but hardcoded `aria-label="Undo"/"Redo"` (4 Check-B items). Added `data-i18n-aria-label=fontmaker.toolbar.undo_aria/redo_aria` (keys existed). | Verified headless: all 4 accessible names localize EN "Undo"/"Redo" → HE "בטל"/"בצע שוב"; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S91
- [x] 2026-07-16 | 77e99fa | hebrew_blend_generator.html + flash_cards.html + hebrew_dictionary.html (Pass K — i18n audit) | **Pass K (i18n / localization audit), 3rd run — localize the visible "Sound" vowel-group header.** The vowel-group color header rendered hardcoded English `${group.sound} Sound – ${group.colorHtml}` (a JS template literal, invisible to Check A) at 5 sites across 3 tools. Routed all 5 through the already-translated `worksheet.vowels.group_header` / `flashcards.vowelgroup.header` keys via `I18n.t(key,{sound,color})` (the unescaped `{color}` renders the trusted `colorHtml` `<u>` markup; dictionary reuses the worksheet key) — 0 new keys, no build-locales. `window.I18n`-guarded English fallback at init (the pickers build during parse, before the deferred `js/i18n.js`), and each page's `applyI18n` now re-renders the headers (generator `refreshVowelGroupColors` / flash_cards `initVowels` / dictionary `refreshDictVowelGroupColors`). **Audit:** `check-i18n` exits 0 (Check A clean; Check B empty-`he` clean bar the deliberate printed-output fallback); logged the remaining Check-B backlog (generator 24 / FM 7 / dashboard 2 / pwa 1) + a fresh FM instructional-prose cluster (the FM Undo/Redo + "Editing:" + torah close slices were fixed this session). | Verified headless (all 3, light+dark): 7 group headers each render EN "AH Sound – <u>A</u>qua" → HE "צליל AH – Aqua"; no raw `{sound}` leaks; `check-i18n` clean; `pageerror==0`. | run: 2026-07-16, S91
- [x] 2026-07-16 | (S90 close-out) | branch/deploy note | "Rerun 100" → S90 **continues on branch `claude/new-session-2z7kfq`, adding to the still-open draft PR #99** (S89; `mergeable_state: clean`, not merged → no branch restart, no new PR). No outside-loop drift since S89 (`origin/main` still at #98; `sw.js` v320, FM 4.29 unchanged). `sw.js` **v320→v321** (precached files changed: classroom_dashboard.html, hebrew_blend_generator.html, flash_cards.html, torah_trainer.html, hebrew_dictionary.html, locales/en.json+he.json). No `FONT_MAKER_VERSION` bump. `check-i18n` clean; `update-sitemap.mjs` a no-op (changed pages already `<lastmod>` 2026-07-16). **Full 5-iteration push: 1 pass (F) + 4 fixes = 5**, across dashboard (2: Pass F tour-pulse + ivritAskMode) + generator (1) + flash_cards (1) + torah_trainer (1) + dictionary (1) + locales (1) — within the 2/tool cap; pattern classes consistency 1 / a11y 2 (ivritAskMode + dict role=status) / print 1 / contrast 1 — none > 2. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | 458ef64 | hebrew_dictionary.html | **`#a11yStatus` gets `role="status"` (S90 P4, share-affordance consistency).** After S89 wired `announce()` into the generator + flash_cards share buttons, all three announce copy success via a hidden `#a11yStatus` live region — but the dictionary's carried `aria-live="polite"` without the `role="status"` the other two have. Added `role="status"` (one attribute, no copy). | Verified headless (light+dark): `#a11yStatus` has role=status + aria-live=polite and `announce()` still populates it; `pageerror==0`; `check-i18n` clean. | fixed: 2026-07-16, S90
- [x] 2026-07-16 | c4020e0 | locales (ui-strings.csv + en/he.json) | **Translit-credit link uses `--gold-text` (S82/S83 P4).** The "Powered by hebrew-transliteration" credit link still rendered `var(--gold)` (~2.5:1 on cream) from its `data-i18n-html` cells; swapped `var(--gold)` → `var(--gold-text)` (light `#7f5a13` / dark `var(--gold)`) in `flashcards.advanced.translit_credit` (en+he) + `dictionary.translit.credit` (he) and rebuilt locales. The dictionary en value + worksheet credit are plain text (no link) → untouched. | Verified headless: the rendered credit link computes `--gold-text` on flash_cards (en+he) + the dictionary HE link, light+dark; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S90
- [x] 2026-07-16 | db099e6 | torah_trainer.html | **Print the trope legend so colored readings keep a key (S84 Pass G P3).** A trope-colored reading prints its clause families as underlines, but the color→family legend was in the `@media print` chrome-hide list → colored underlines with no key. Removed `.tt-trope-legend` from that list + added a print-compact treatment (flat card, thin border, white bg, small font, break-inside:avoid; hide the interactive tutor link; `print-color-adjust:exact` on swatches). The legend stays `[hidden]` unless trope coloring is on + a reading is loaded → never prints spuriously; swatches read `--trope-*-bg`, re-resolved to light by the S89 beforeprint handler. | Verified headless (print emulation, light+dark): legend shows in print, tutor link hidden, shadow flattened, swatch bg a light-palette tint even from dark mode; screen unchanged; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S90
- [x] 2026-07-16 | 9fe5d43 | hebrew_blend_generator.html + classroom_dashboard.html + flash_cards.html | **`.ivrit`-engine Merge/Replace modal dialog semantics (S82/S89 P2, cross-tool — the last open P2).** The shared engine's `ivritAskMode()` (the confirm shown on every restore) was a bare fixed div — no role="dialog"/aria-modal, no Escape, no Tab focus-trap, no focus move-in/restore; completes the S89 flash_cards overlay work (the split-out cross-tool half). Added self-contained dialog semantics (no external helper, so the engine still depends only on IVRIT_CFG + standard IDs) **IDENTICALLY** to the 3 byte-identical copies; the deliberately-adapted index.html variant left as-is. | Verified: the engine block is byte-identical across all 3 after the change (sha 7dc8e620…); headless on all 3 pages (light+dark) — role=dialog, aria-modal, valid aria-labelledby, initial focus on Merge, Tab/Shift+Tab wrap, Escape closes + resolves null + restores focus, a Merge click still resolves "merge"; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S90
- [x] 2026-07-16 | 7053f7a | classroom_dashboard.html (Pass F — cross-tool consistency) | **Pass F (cross-tool consistency), 7th run — converge the tour-button pulse onto the shared `.pulse-attn`.** Survey: font-picker `.font-opt` `aria-pressed` = a 6-file enhancement-to-all with no canonical source (weak F fit, deferred); share buttons already converged post-S89. Lone laggard = the first-visit tour nudge: 5 tools share `.pulse-attn`/`@keyframes attnPulse` whose reduced-motion rule keeps a **static box-shadow fallback ring**; the dashboard used a bespoke `#tourBtn.tour-pulse`/`@keyframes tourPulse` whose reduced-motion rule killed the animation with **no** fallback ring. Replaced the bespoke CSS with the canonical (from the generator) incl. the fallback ring, swapped the 2 JS toggles → `pulse-attn`, and wired the static `#tourNext` `data-i18n="shared.tour.next"` (existing key; the live tour render already localizes it per-step). Left the intentional `.hbtn-label` inner-span `data-i18n` split. | Verified headless (light+dark × 1280/800): `#tourBtn` carries `.pulse-attn` with animation attnPulse on first visit; reduced-motion shows the static ring (== the generator canonical); `#tourNext` localizes EN/HE; `check-i18n` clean; `pageerror==0`. | run: 2026-07-16, S90
- [x] 2026-07-16 | (S89 close-out) | branch/deploy/reconcile note | New session on branch `claude/new-session-2z7kfq`, freshly based on `origin/main` — which already contains the **merged S85–S88 PR #97** and the **outside-loop Font Maker PR #98** → a **new draft PR** (PR #97 is merged, so S89 is fresh work off latest `main`, not a continuation). **Outside-loop reconcile (Pass E):** PR #98 (Font Maker v4.29 — curve-preserving import + Reduce points + Transform rotate/scale sliders) landed after the S88 close-out, moving `sw.js` **v318→v319** and `FONT_MAKER_VERSION` **4.28→4.29**; it changed only HTML + locales + sitemap + the SW version line (**no `CORE_ASSETS`/precache/`data/` change; the v4.29 About-tab changelog entry was already present**). This session `sw.js` **v319→v320** (precached files changed: torah_trainer.html, flash_cards.html, classroom_dashboard.html, hebrew_blend_generator.html); **no `FONT_MAKER_VERSION` bump** (no FM change). `check-i18n` clean; `update-sitemap.mjs` a no-op (all changed pages already `<lastmod>` 2026-07-16 from earlier same-day commits). **Full 5-iteration push: 1 pass (E) + 4 fixes = 5**, across torah_trainer (1) + flash_cards (2) + classroom_dashboard (1) + generator (1) — within the 2/tool cap, 4 tools; pattern classes a11y 2 (#24 + #21) / print 1 / attribution 1 — none > 2. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | ffa0e6e | hebrew_blend_generator.html + flash_cards.html | **Announce share/copy success to screen readers (S80 Pass F P3).** The generator’s `copyShareLink` and flash_cards’ `copyShareCode` confirmed a copy only by swapping the button text — a visual-only cue the dictionary already backs with an SR announcement. Added the dictionary’s tiny hidden `#a11yStatus` live region + one-line `announce()` helper to each page and called it after the clipboard write, reusing the existing `worksheet.share.copied` / `flashcards.share.copied` keys (no new copy). The code-vs-URL share *mechanisms* were left divergent (per the candidate) — only the a11y announce converged. | Verified headless (both pages, light+dark): `#a11yStatus` receives "✓ Copied!" on copy and the button still swaps; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S89
- [x] 2026-07-16 | 256a364 | classroom_dashboard.html | **Visible attribution for weather + location data (S79 Pass E P3).** The weather widget’s forecast (Open-Meteo, CC BY 4.0) and reverse-geocoded location name (Nominatim/OpenStreetMap, ODbL 1.0 — requires "© OpenStreetMap contributors") were shown with no on-screen credit. Added a tiny muted credit line inside `#weatherBlock` (after `.weather-city`, in the `renderWeather` success template) so it shows only when weather data is present and vanishes when the widget is hidden/unavailable — the license-correct behavior. Kept an English literal per CLAUDE.md’s scope rule (footer brand/attribution credits never translate; `i18n-ignore` noted). | Verified headless (light+dark × 1280/800): credit shows with seeded data (muted color adapts per theme), absent when hidden and on the no-data status path; `pageerror==0`. | fixed: 2026-07-16, S89
- [x] 2026-07-16 | 98be114 | flash_cards.html | **Keyboard dialog semantics for the JS-built overlays (S82 Pass C P2).** The profile-picker (`chooseProfileModal`), card-count (`askCardCount`), and weakness-viewer (`openWeaknessView`) overlays were bare fixed divs — no role="dialog"/`aria-modal`, no Escape, no Tab focus-trap, no focus move-in/restore (a keyboard/AT user could Tab out behind the modal). Added one in-file `fcModalize(ov, box, closeFn, titleEl, initialEl)` helper (role/aria-modal + `aria-labelledby` to the existing title, Escape-to-close, a Tab/Shift+Tab focus-trap, focus save + restore via a teardown observer; modeled on the tour opener) and wired all 3. No new copy → no CSV/build-locales. **Split off:** the 4th "overlay" (`ivritAskMode`) lives in the shared byte-identical `.ivrit` engine block → a cross-tool change, logged as its own P2 candidate. | Verified headless (light+dark × 1280/800, all 3): `role=dialog`, `aria-modal`, valid `aria-labelledby`, initial focus inside, Tab/Shift+Tab wrap, Escape closes + focus restores to opener; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S89
- [x] 2026-07-16 | 2e69d0f | torah_trainer.html | **Print nikkud/trope with the LIGHT palette from dark mode (S84 Pass G P2).** `applyNikkudColors()`/`applyTropeColors()` push the dark palette into inline `--nikv-*`/`--nikh-*`/`--trope-*-bg`/`--trope-*-line` body vars, and inline custom props beat the print stylesheet’s dark-token reset, so Letter/Underline nikkud + trope underlines printed washed-out from dark mode (e.g. tzere `#aaaadd` ~1.9:1 on white). Added a `_printForceLight` flag honored by `getNikudColor`/`getTropeColor`; the existing `beforeprint` now flips it on + re-resolves both palettes to light when `body.dark`, and a new `afterprint` restores the dark palette. Hex overrides still win; only the default palette flips; idempotent. | Verified headless (print emulation, light+dark × 1280/800): print resolves tzere → light `#7777aa` and trope katon-line → light `#3b82c4` and reverts on `afterprint`; light mode unaffected; `pageerror==0`. | fixed: 2026-07-16, S89
- [x] 2026-07-16 | 175d3c5 | README.md + CLAUDE.md (S89 Pass E — freshness/site-health) | **Pass E (freshness & site-health), 8th run — over the S79→S88 + outside-loop PR #98 surface; mechanical CLEAN, 2 doc-drift fixed.** SW `CORE_ASSETS` (**25** entries) resolve both directions (only i18n-test.html intentionally omitted; splash/og-card runtime-cached); sitemap 11 `<loc>` all map to files + every `<lastmod>` == git commit date (`update-sitemap.mjs` a no-op); all loaded external origins match the S79-documented set (**no new CDN/API** since — #98 added none); `THIRD_PARTY_LICENSES.md` still covers every dep. **Reconciled the outside-loop PR #98 drift** (`sw.js` v318→v319, `FONT_MAKER_VERSION` 4.28→4.29 — HTML+locales+sitemap+sw only, About changelog already done). **2 doc-drift fixed** (175d3c5): README FM **v4.26→v4.29** (trailed FONT_MAKER_VERSION), CLAUDE.md FM line-count **~12,900→~13,400** (actual 13,439). README/CLAUDE not precached → the doc fix itself needs no SW bump. | run: 2026-07-16, S89
- [x] 2026-07-16 | (S88 close-out) | branch/deploy note | "Rerun" → S88 **continues on branch `claude/new-session-ienue4`, adding to the still-open draft PR #97** (S85–S87; `mergeable_state: clean`, not merged → no branch restart, no new PR). No outside-loop drift since S87 (`sw.js` v317, FM 4.28 unchanged). `sw.js` **v317→v318** (precached files changed: classroom_dashboard.html, flash_cards.html, hebrew_blend_generator.html, locales/en.json+he.json). No `FONT_MAKER_VERSION` bump (no FM change). `check-i18n` clean; `update-sitemap.mjs` a no-op (all changed pages already `<lastmod>` 2026-07-16 from S86). **Full 5-iteration push (user-directed): 1 pass (B) + 4 fixes = 5**, across dashboard (1) + flash_cards (2) + generator (1) — within the 2/tool cap, 3 tools. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | 6b392d1 | hebrew_blend_generator.html | **Revealed self-check card backs print light in light mode (S74 P4).** Self-check "Reveal all" flips `.flip-card-back` into view; its base `background:var(--navy)` is dark even in light mode, so a light-mode reveal-then-print bled a solid dark block + gold text onto paper (the S30 print reset only relit `body.dark` backs). Added an unscoped `@media print` `.flip-card-back{background:#fff;color:#1a2744}` so revealed backs print white/dark-ink in both modes (`.rw-flip-back` uses `var(--cream)`, already light → unaffected). | Verified headless (print emulation): back stays navy on screen (light) / dark (dark) but prints white bg + navy ink in both; `pageerror==0`. | fixed: 2026-07-16, S88
- [x] 2026-07-16 | 0319d64 | flash_cards.html (+ locales) | **Accessible names for the nav + icon-only delete buttons (S82 P3).** The "Related Hebrew Tools" `<nav>` had a hardcoded English `aria-label`; the preset/profile delete ✕ buttons had no accessible name (announced "times"). Localized the nav via the existing `shared.nav.header` key (zero new keys), and gave both ✕ buttons an `aria-label` "Delete {name}" via a new `flashcards.actions.delete_aria` key. | Verified headless (EN+HE): nav = "Related Hebrew Tools" / "כלים עבריים נוספים"; preset ✕ = "Delete MyPreset", profile ✕ = "Delete TestProfile"; `build-locales` + `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S88
- [x] 2026-07-16 | 52c60c8 | flash_cards.html | **Announce the session score to screen readers (S82 P3).** The results-hero (the big %, fraction, streak shown after every drill) was a static region → made it `role="status" aria-live="polite"` so the score is announced when `showResults()` populates it. | Verified headless (light+dark): the hero is a polite live region holding the score text once populated; `pageerror==0`. | fixed: 2026-07-16, S88
- [x] 2026-07-16 | 1661e93 | classroom_dashboard.html | **Localize the "Restore default" slider-reset tooltips (S81 Check-B).** The 11 ↺ size-reset buttons carried a hardcoded `title="Restore default"` with no `data-i18n-title` — the bulk of the dashboard's Check-B advisory backlog. The key `dashboard.common.restore_default` ("Restore default" / "שחזר ברירת מחדל") already existed + was translated but was never wired; added `data-i18n-title` on all 11 (no CSV, no new keys). | Verified headless: all 11 titles localize — EN "Restore default" / HE "שחזר ברירת מחדל"; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S88
- [x] 2026-07-16 | (S88 Pass B — audit) | all 12 root pages (console & error audit, 7th run) | **Pass B (console & error audit), 7th run — first over the S79→S87 surface; CLEAN.** All 12 root pages × light/dark = 24 page-runs over local HTTP, load + one interaction per tool (index gear, generator letter-tile, dashboard drawer, flash_cards mode-pill, dictionary search, trope drill, FM letter-tile). **`pageerror==0` on every run; 0 local-request failures; 0 `[i18n] missing key` warnings.** Console-error counts equal the route-aborted external count on 11 of 12 pages; the only non-abort output is **torah_trainer**'s documented offline artifacts — the Sefaria API `Failed to fetch` (`fetchSefariaText`) + the hebrew-transliteration ESM-CDN load warnings (`esm.sh`/`jsdelivr`), all external + gone over a real network (as in S47/S57/S69/S78). No regression from the S79–S87 changes (S85 FM debounce, S86 dashboard weather heal / flash_cards pills, S87 trope/torah/index). No fix forced. | run: 2026-07-16, S88
- [x] 2026-07-16 | (S87 close-out) | branch/deploy note | "Rerun" → S87 **continues on branch `claude/new-session-ienue4`, adding to the still-open draft PR #97** (S85–S86; `mergeable_state: clean`, not merged → no branch restart, no new PR). No outside-loop drift since S86 (`sw.js` v316, FM 4.28 unchanged). `sw.js` **v316→v317** (precached files changed: trope_tutor.html, torah_trainer.html, index.html, locales/en.json+he.json). No `FONT_MAKER_VERSION` bump (no FM change). `check-i18n` clean; `update-sitemap.mjs` refreshed trope/torah/index `<lastmod>` to 2026-07-16. **Full 5-iteration push (user-directed): 1 pass (H) + 4 fixes = 5**, across trope_tutor (1) + torah_trainer (2) + index (1) — within the 2/tool cap, 3 tools. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | 9fafa40 | index.html (+ locales) | **AllTools modal close-button aria-label (a11y).** The Import/Export All Settings modal's ✕ close button was the only icon-only button on the home page with no accessible name — the "×" glyph (U+00D7) is announced as "times", not "Close". Added `aria-label` + `data-i18n-aria-label` with a new `home.alltools.modal_close_aria` key (Close / סגור), matching the S80 dashboard-gear labeling convention. | Verified headless (1280+800): accessible name "Close" (EN) / "סגור" (HE); `build-locales` + `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S87
- [x] 2026-07-16 | e8ccfb8 | torah_trainer.html | **Audio-collapse button `aria-expanded` (S72 P4 G4).** `#ttAudioCollapseBtn` conveyed expanded/collapsed only via its ▾ glyph + an aria-label swap; added `aria-expanded` synced in three places (static markup, settings-restore on bar open, `toggleAudioBarCollapse`). | Verified headless (light+dark): `aria-expanded` flips true→false→true and always equals `!collapsed`; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S87
- [x] 2026-07-16 | ee33666 | torah_trainer.html | **Resume-link gold-on-light contrast (S72 P3 G3, scoped).** `.tt-resume-go` (the interactive resume link on the cream chip, small bold text) used `color:var(--gold)` (#c9922a, ~2:1 on cream — WCAG-fail); swapped to the suite-wide `--gold-text` token (#7f5a13 light / var(--gold) dark). The unambiguous single-selector slice of the S72 G3 sweep; the broader "which small-text spots count" tail stays a maintainer design call. | Verified headless: computed rgb(127,90,19) = **5.88:1** vs cream (light, passes AA); **7.96:1** in dark (unchanged); `pageerror==0`. | fixed: 2026-07-16, S87
- [x] 2026-07-16 | d47e5ca | trope_tutor.html | **Pass H — localize the family-nav button text (P3 i18n leak).** `renderLearn()` set `#tuNextFamily`'s text to hardcoded English literals ('Next family →' / 'Ready? Try a drill →'), overwriting the button's `data-i18n` and leaving the Hebrew UI showing English on a core Learn control. Both locale keys already existed and were translated (`trope.learn.next_family`, `trope.learn.ready_drill` — authored but never wired), so routed the ternary through `I18n.t()` — no CSV / build-locales / new keys. `applyI18n` already re-renders Learn, so a live switch re-localizes too. | Verified headless (light+dark): EN shows English, HE shows "המשפחה הבאה →" / "מוכנים? נסו תרגול →" incl. on a live switch; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S87
- [x] 2026-07-16 | (S87 Pass H — audit) | trope_tutor.html (teacher walkthrough — first-ever H on the Trope Tutor) | **Pass H (teacher walkthrough), first-ever on `trope_tutor.html`; effectively CLEAN flow, 1 P3 i18n leak fixed.** Role-played a teacher preparing a lesson end-to-end (fresh profile, HTTP, light/dark × 1280/800): **Learn** (6 family chips, cards, `nextFamily` cycles all 6 families + hands off to Drill on the last, intro links to Torah Trainer) and **Drill** (start screen w/ types-summary + mastery grid + first-run tour pulse → full 10-question session → results 80–100% + mastery grid) both complete, `pageerror==0`. **Audio-failure degradation verified robust** (all MP3s env-blocked → all 4 toast kinds fired: clip-switched / audio-unavailable / new-question / skipped — the drill still reached results, **never dead-ended**). **1 fix same-session** (d47e5ca — the hardcoded-English family-nav button). A broad blind-spot scan for sibling hardcoded literals found none; mastery grid is well-labeled (per-trope, grouped by 6 families). No paper-cuts or Feature seeds beyond the leak — the tool is feature-complete for its scope. | run: 2026-07-16, S87
- [x] 2026-07-16 | (S86 close-out) | branch/deploy note | "Rerun" → S86 **continues on branch `claude/new-session-ienue4`, adding to the still-open draft PR #97** (S85; `mergeable_state: clean`, not merged → no branch restart, no new PR). No outside-loop drift since S85 (`sw.js` v315, FM 4.28 unchanged). `sw.js` **v315→v316** (precached files changed: classroom_dashboard.html, flash_cards.html, hebrew_blend_generator.html, locales/en.json+he.json). No `FONT_MAKER_VERSION` bump (no FM change). `check-i18n` clean; `update-sitemap.mjs` refreshed generator/flash_cards/dashboard `<lastmod>` to 2026-07-16. **Full 5-iteration push (user-directed): 1 pass (I) + 4 fixes = 5**, across dashboard (1) + flash_cards (2) + generator (1) — within the 2/tool cap, 3 tools; no recurring-pattern class exceeded 2. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | 2dd3dd8 | flash_cards.html (+ locales) | **Dead preset/profile empty-state copy removed (S76 P4).** `#presetEmpty`/`#profileEmpty` carried instructive copy but `renderPresets`/`renderProfiles` force-hid them ("folder tree shows its own empty state"), so the shared folder-tree's own message ("No saved presets yet.") was always what showed. The folder-tree is byte-identical shared, so surfacing the copy would just duplicate its message → removed the dead `<p>`s, their hide-code, and the 2 orphaned locale keys (rebuilt locales), aligning flash_cards with the other foldered lists. | Verified headless (light+dark, fresh empty profile): removed ids gone, folder-tree empty state still renders for both lists, `renderPresets`/`renderProfiles` run clean, `check-i18n` clean, `pageerror==0`. | fixed: 2026-07-16, S86
- [x] 2026-07-16 | 9640a11 | hebrew_blend_generator.html | **PDF-export `alert()` → inline notice (S74 P4, §5 convergence).** The `exportPDF` catch was the last blocking `alert()` in the worksheet flow; routed it through the in-file non-blocking `showPreviewNotice(...)` as a dismissible `notice-warn`, matching the tool's other error surfaces. | The real failure needs the CDN PDF libs, so the catch was exercised headless by stubbing `window.jspdf` to throw inside the try: no alert fires, `#previewNotice` shows the pdf_failed text as `notice-warn` with a ✕ dismiss, the button resets in `finally`, `pageerror==0` (light+dark). | fixed: 2026-07-16, S86
- [x] 2026-07-16 | 88cab3a | flash_cards.html | **Segmented-pill selection exposed to screen readers via `aria-pressed` (S82 P3 a11y).** ~18 `.pill-group`s conveyed the selected option only via `.active`; added toggle-button `aria-pressed` to every `.pill-group .pill` via one `initPillAria()` that seeds initial state + installs a class `MutationObserver`, tracking BOTH click and programmatic `.active` changes (preset restore, `?wl=` deep-link, mode switches). Pills are native `<button>`s (already keyboard-operable) and the groups are static markup → one init-time observer covers all. | Verified headless (light+dark): all 49 pills carry `aria-pressed`, exactly one active/group, and `aria-pressed === .active` holds on load, after a click, and after a programmatic change; `pageerror==0`. | fixed: 2026-07-16, S86
- [x] 2026-07-16 | e458161 | classroom_dashboard.html | **Pass I — heal weather status messages rendered before i18n is ready (P3 fresh-user i18n leak).** Pass I caught the raw key `dashboard.weather.unavailable` painted in the weather widget for a fresh user: `fetchWeather` renders its loading/not_found/unavailable messages via `I18n.t()`, which returns the raw key before `/locales` loads, and `applyI18n`→`renderWeather()` early-returns without a `weatherCache` so it never re-translated — the raw key persisted (most visibly on a projected dashboard, offline/slow load). Routed all three status messages through a `renderWeatherStatus()` helper tracked by `_weatherStatus`; `renderWeather()` now calls it when there's no cache, so the existing `applyI18n`→`renderWeather` path re-translates once ready (mirroring the Shabbat widget's self-heal). | Verified headless (light+dark): the pre-ready render reproduces the raw key, then `renderWeather()` heals it to "Weather unavailable" / "מזג אוויר לא זמין"; success path still renders temp+city; Pass I sweep now clean on all 12 pages; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-16, S86
- [x] 2026-07-16 | (S86 Pass I — audit) | all 12 root pages (first-load & empty-state, 6th run) | **Pass I (first-load & empty-state), 6th run — first over the S77→S85 surface.** Fresh browser context (empty localStorage + IndexedDB) per run, all 12 root pages × light/dark × 1280/800 = **48 runs**, over local HTTP. **`pageerror==0` everywhere;** brand-new user defaults to EN/`dir=ltr` with the switcher mounted; `lang="he"` present on all 7 Hebrew pages; OS-aware dark honored; no h-scroll; no dir/switcher issues. **1 fresh-user i18n leak found + fixed same session** (e458161 — the dashboard weather-unavailable raw key). Depth probes on the S79–S85 diffs (FM Generate Bold on an empty project, dictionary corrected tour copy, dashboard colorize on a fresh profile) all clean. | run: 2026-07-16, S86
- [x] 2026-07-16 | (S85 close-out) | branch/deploy/reconcile note | New session on branch `claude/new-session-ienue4`, freshly cut from merged `origin/main` (`4f52ee3`; the S79–S84 PR #94 is merged) → **new draft PR**. **Outside-loop reconcile:** 4 Font Maker commits (Generate Bold synthetic-weight generation + live preview, Style dropdown, deferred restore prompt) landed on `main` after the S84 close-out, moving `sw.js` **v310→v314** and `FONT_MAKER_VERSION` **4.26→4.28** — the S84 "SW at v310 / FM 4.26" note was stale. This session bumped `sw.js` **v314→v315** (precached files changed: Hebrew_Font_Maker.html, classroom_dashboard.html, hebrew_dictionary.html, locales/en.json+he.json); **no `FONT_MAKER_VERSION` bump** (the FM change is an internal responsiveness optimization, no user-facing behavior change — S64 precedent). `check-i18n` clean; `update-sitemap.mjs` refreshed dictionary/dashboard/FM `<lastmod>` to 2026-07-16. **Full 5-iteration push (user-directed): 1 pass (D) + 4 fixes = 5**, across Font Maker (1) + dashboard (2: gear aria-label + colorize esc) + dictionary (2: tour copy + colorize esc) — within the 2/tool cap, 3 tools; no recurring-pattern class exceeded 2. GitHub Pages deploys on merge to `main` — verify `success` after merge (this session is on a branch).
- [x] 2026-07-16 | af99b55 | hebrew_dictionary.html + classroom_dashboard.html | **`colorizeHebrew` esc() alignment (S79 P4 hygiene).** The generator/flash_cards/torah colorizers `esc()` every char/syllable before `innerHTML`; the dictionary (incl. its colour-disabled `return text` branch) and dashboard copies interpolated raw. Aligned both with the generator pattern (colour values stay raw — validated on import per the S17 nikud-override fix). Inputs trusted today → no live bug; closes a latent injection sink and ends the sibling divergence. `esc()` is a no-op on Hebrew letters+nikud → real-corpus output byte-identical. | Verified headless both themes: 0 HTML entities introduced on real Hebrew, colour spans intact, a metacharacter input now escapes (`<b>`→`&lt;b&gt;`), `pageerror==0`. | fixed: 2026-07-16, S85
- [x] 2026-07-16 | b711347 | hebrew_dictionary.html (+ locales) | **Stale tour copy fixed (S68 P3 copy-half).** The "Bulk Copy & Export" tour step claimed export "into the Worksheet Generator" (no such export exists — Anki/Quizlet download + copy/paste only) and never named Word Lists; rewrote `dictionary.tour.export_body` (en+he) to describe the real capability (export to Anki/Quizlet, or save as a Word List to reuse in other tools; Hebrew mirrors the wordlists `head_tip`). | Verified headless EN+HE: corrected copy renders, no Worksheet-Generator claim, Word Lists surfaced; `build-locales` + `check-i18n` clean; `pageerror==0`. **The buried-panel + "Bulk Copy Mode"-label half of S68 remains open** (maintainer UX call). | fixed: 2026-07-16, S85
- [x] 2026-07-16 | 2b11c02 | classroom_dashboard.html | **Header gear button aria-label (S80 P3).** `#gearBtn` was the only icon-only header button in the suite relying on `title` alone for its accessible name (least reliable per ACCNAME — not announced on touch / by several SR configs); added `aria-label` + `data-i18n-aria-label` reusing the existing `dashboard.header.settings_btn` key (no new locale string). | Verified headless: computed accessible name "Settings" (EN) / "הגדרות" (HE, dir=rtl) at 1280+800, `pageerror==0`; `check-i18n` clean (also clears one Check-B advisory item). | fixed: 2026-07-16, S80→S85
- [x] 2026-07-16 | 2f59215 | Hebrew_Font_Maker.html | **Pass D — Generate Bold warning-scan debounce (P3 drag responsiveness).** The Bold live preview re-ran a deep-clone + outline-offset + O(n²) self-intersection scan over EVERY traced letter on every strength-slider frame (~65 ms/frame @4× CPU over a full alphabet; no single block >200 ms, but janky on the Chromebook-class hardware the suite targets). Extracted the full-corpus scan into a debounced `boldGenWarnScan()` so each drag frame re-renders only the ≤5 preview glyphs; warnings settle ~130 ms after the slider stops; timer cleared on modal close. | Verified headless (1×/4×): per-frame render **67→24 ms @4× / 18→4 ms @1×**, preview byte-identical, settled warnings match a synchronous scan, `pageerror==0`. No `FONT_MAKER_VERSION` bump (internal opt, no user-facing change — S64 precedent). | fixed: 2026-07-16, S85
- [x] 2026-07-16 | (S85 Pass D — audit) | Hebrew_Font_Maker.html (performance — first-ever dedicated D) | **Pass D (performance), first-ever dedicated pass on Font Maker.** FM's heavy work is lazy CDN/WASM (surveyed-exempt), so the in-scope surface is its own JS — the render pipeline + the new **pure-JS** Generate Bold — never before profiled. Headless over **local HTTP** (the page's root-absolute `/js/i18n.js` needs a web root — file:// leaves `I18n` undefined and the render paths throw; documented env artifact), buffered-longtask + CPU-throttle rig, light/dark × 1280/800 × 1×/4×, seeded a full alphabet of 300+140-pt traced contours (which trigger the O(n²) self-intersection branch). **No in-scope block >200 ms:** heaviest Generate Bold render **~92 ms @4× / ~23 ms @1×**; `boldGenApply` ≤150 ms @4×; cold init one boot longtask **438 ms @4× / 104 ms @1×** (one-time, throttled-only — logged P4, same category as the dashboard cold-load). `pageerror==0` across the matrix. **1 P3 fixed same-session** (2f59215 — the redundant per-frame all-letters warning scan → debounced). | run: 2026-07-16, S85
- [x] 2026-07-15 | (S84 close-out) | branch/deploy note | S84 continues on the designated branch `claude/new-session-vhfp4g`, adding to the already-open **draft PR #94** (S79–S83; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v309→v310** (precached file changed: `torah_trainer.html`). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (CSS + comment only). `check-i18n` clean. `update-sitemap.mjs` refreshed `torah_trainer.html`'s `<lastmod>` to 2026-07-15. **Pass rotation:** ran the strictly-stalest **G** (print & export fidelity, last S74) — a **2nd-pass on torah_trainer** (the suite's most-printed tool, un-audited since S53; flash_cards — the only un-G'd tool — confirmed to have NO print/export surface). Iters: 1 pass (G) + 1 fix = 2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-15 | ce5ab3c | torah_trainer.html (+ sw.js) | **Pass G — 2 dark-mode/highlight print-fidelity gaps in the printed reading, both in `@media print`.** (1) **Dark-mode reference header near-invisible:** `body.dark .tt-ref-hdr` is light gold and the print token reset keeps `--gold-light` light, so the parsha/verse-range label — the only text identifying **which** reading the sheet is — printed ~1.4:1 gold on white; forced it to dark ink in print (`body.dark .tt-ref-hdr { color:var(--navy) }`). (2) **Nikkud Highlight mode dropped silently:** it's a `background-color` tint (`:638`) that the print dialog's default "background graphics off" strips, and the print block had no `.nik` rule → vowel color-coding vanished with no cue; converted Highlight to the same per-vowel underline as Underline mode (reusing the `--nik-c` chain), mirroring the S53 trope→underline print fallback. Also corrected the stale S53 rationale comment (claimed inline nikkud spans win — S60 replaced them with a stylesheet `!important` rule reading body vars). **Logged** the larger dark-palette-reprint P2 (Letter/Underline nikkud + trope underline print washed-out from dark mode — needs a `beforeprint` palette re-resolve). | Verified headless print-emulation (`emulateMedia('print')`, synthetic elements): dark `.tt-ref-hdr` → `rgb(26,39,68)` navy (not `#f0d080`); Highlight `.nik` → transparent bg + underline in the per-vowel color; Letter (color) / Underline / trope print behaviors unchanged; `check-i18n` clean; `pageerror==0`. | fixed: 2026-07-15, S84
- [x] 2026-07-15 | (S84 Pass G — audit) | torah_trainer.html (print & export fidelity, 2nd-pass) | **Pass G (print & export fidelity), 2nd-pass on torah_trainer (last S53) — the S53/S55 print treatment substantially HOLDS; 2 P2 print gaps fixed + 1 doc correction.** First confirmed **flash_cards has NO print/export surface** (no `@media print` / `window.print()` / PDF-file export — only `.ivrit` settings backups + a paste share-code + live projection), like dashboard/trope → the only un-G'd tool has no G surface, so this is a 2nd-pass on the stalest print tool. **CLEAN (held):** chrome-hide list, dark→light token reset (`body{background:#fff}` un-beaten), verse `break-inside:avoid`, the S53 trope→underline print fallback, side-by-side→single-column (Letter + A4), aliyah dividers (border not bg), RTL. **Fixed** (ce5ab3c): the dark-mode invisible reference header + the silently-dropped nikkud Highlight mode (both P2 — a misleading/incomplete printed artifact). **Logged:** the dark-mode washed-out nikkud/trope palettes P2 (pre-existing; `--nikv-*`/`--trope-*-line` hold dark pastels ~1.9:1 on white when printing from dark — needs a `beforeprint`/`afterprint` light re-resolve, its own session) + the print-hidden trope legend P3 (intentional S53 chrome-hide tradeoff). | run: 2026-07-15, S84
- [x] 2026-07-15 | (S83 close-out) | branch/deploy note | S83 continues on the designated branch `claude/new-session-vhfp4g`, adding to the already-open **draft PR #94** (S79–S82; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v308→v309** (precached file changed: `hebrew_blend_generator.html`). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (CSS-only color change). `check-i18n` clean. `update-sitemap.mjs` refreshed the generator's `<lastmod>` to 2026-07-15 (the clock ticked over — S79–S82 committed 07-14, S83 on 07-15). **Pass rotation:** ran the strictly-stalest **A** (recurring-pattern sweep, last S73) over the never-A-swept surface (FM v4.18→v4.26 + S74–S82) — **CLEAN, 0 hits**; per the S41 precedent paired the clean sweep with 1 bounded backlog fix. Iters: 1 pass (A) + 1 fix = 2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-15 | 449b587 | hebrew_blend_generator.html (+ sw.js) | **Pass A backlog fix — generator footer/nav gold-on-light contrast (completes S82's convergence).** The Pass A sweep was CLEAN, so per the S41 precedent this session pairs it with the bounded, variety-favored backlog twin S82 logged. The generator's footer credit links (Created by / Kaikki / CC / GitHub) + the 7 "Related Hebrew Tools" nav links used `var(--gold)` as text on light (~2.2:1, fails WCAG AA) — the **last remaining laggard** (flash_cards fixed S82; dictionary/torah/trope/resources already avoid it). Swapped the 12 `color:var(--gold);text-decoration:none` link spots → `--gold-text` (light `#7f5a13` / `var(--gold)` dark → **dark unchanged**), matching S53/S63/S71/S82. CSS-only — **no CSV, no build-locales**; other `var(--gold)` (gold-on-navy) untouched. The translit-credit rendered link (data-i18n-html CSV) still needs the CSV swap → logged. | Verified headless over local HTTP, all 4 runs (light/dark × 1280/800) `pageerror==0`: light footer/nav/kaikki now **6.22:1** (`rgb(127,90,19)`, was ~2.2:1); dark **unchanged** (`rgb(224,168,50)` = generator's `--gold`, 7.16:1); `check-i18n` clean. | fixed: 2026-07-15, S83
- [x] 2026-07-15 | (S83 Pass A — sweep) | FM v4.18→v4.26 + S74–S82 (never-A-swept surface) | **Pass A (recurring-pattern sweep), over the never-A-swept surface since S73 — CLEAN, 0 real hits across all 4 active classes** (2 read-only agents). Biggest new-code surface = the outside-loop **Font Maker v4.18→v4.26** feature work (zoom slider, copy/clear outline on Align, steadier handle rounding, straighten, round corners, guide thickness, fit margins, vowel+trop pairing, edit-points toggle). **falsy-zero CLEAN** — new FM geometry guards 0 (vowel-pair `dx` via `typeof==='number'`; guide-weight/zoom/fillet-radius via slider floors or default==0; near-miss `setVowelPairMode` `Math.abs(dx)||70` is an intentional re-seed, persisted value preserved by the guarded reader). **localStorage-vs-AllTools CLEAN** — 0 new keys (FM pairing is project-file data). **unescaped-input/unsafe-parse CLEAN** — new innerHTML sites interpolate only `I18n.t` locale strings / consts / `esc()`-ed glyph names (S81 index `.ivrit` = trusted locale); all new `JSON.parse` are clones or `ivritSafeParse`-gated. **destructive-bulk CLEAN** — new FM Align/round-corners/copy-outline/straighten are per-item, `udDo`-undoable, `askModal`-confirmed; `applyI18n` hooks read-only. Pattern-health streaks: falsy-zero →2; localStorage →5, unescaped →1, destructive →5 (the 3 consequence-critical stay ACTIVE). No retirements. | run: 2026-07-15, S83
- [x] 2026-07-14 | (S82 close-out) | branch/deploy note | S82 continues on the designated branch `claude/new-session-vhfp4g`, adding to the already-open **draft PR #94** (S79–S81; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v307→v308** (precached file changed: `flash_cards.html`). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (CSS-only color change — no UI strings / `data-i18n` touched). `check-i18n` clean. `update-sitemap.mjs` refreshed `flash_cards.html`'s `<lastmod>` to 2026-07-14. **Pass rotation:** ran the strictly-stalest **C** (accessibility, one tool, last S72) on **flash_cards** — its dedicated C was S8, the oldest of any tool. Iters: 1 pass (C) + 1 fix = 2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-14 | 9c10a65 | flash_cards.html (+ sw.js) | **Pass C — fix flash_cards' gold-on-light text contrast (WCAG 1.4.3).** `var(--gold)` (#c9a24a) used as **text on light surfaces** failed AA (~2.2–2.4:1): the footer "Created by"/CC/GitHub links, the **7 "Related Hebrew Tools" nav links**, the **streak stat value** (the score shown after every session), and a white-on-gold skip-reshow pill. Converged onto the existing readable-gold **`--gold-text`** token (light `#7f5a13` / `var(--gold)` in dark → **dark unchanged**), matching the S53/S63/S71 pattern; the skip pill went **navy-on-gold** (matching `.nav-btn.flip`/BETA badges). Pure CSS/inline-color change — **no CSV, no build-locales**; the other ~9 `var(--gold)` uses (gold-on-navy) left untouched. The footer/nav are per-page inline copies of shared chrome; flash_cards + generator are the two laggards → generator's twin logged. | Verified headless over local HTTP, all 4 runs (light/dark × 1280/800) `pageerror==0`: light footer/nav/streak now **6.22:1** (`rgb(127,90,19)`, was ~2.2:1), skip **5.97:1** (navy); dark **unchanged** (7.82:1 dark gold); `check-i18n` clean. | fixed: 2026-07-14, S82
- [x] 2026-07-14 | (S82 Pass C — audit) | flash_cards.html (keyboard/focus/ARIA + motion/contrast/targets/lang) | **Pass C (accessibility), flash_cards — first dedicated a11y pass since S8 (the oldest of any tool), over the S35/S47/S49/S71/S72 growth.** 2-agent audit. **CLEAN:** reduced-motion (global `*` neutralizer covers every animation), `lang="he"` (every Hebrew chokepoint), keyboard *operability* (card flip via global handler, `bindTileA11y` tiles, accordions w/ synced `aria-expanded`, accessible tooltips w/ Escape, the guided tour dialog+focus-save/restore, Present/fullscreen native-FS + `aria-pressed` sync), no `outline:none`, real `:focus-visible` rings. **Fixed** the bounded high-value slice: gold-on-light contrast (9c10a65). **Logged candidates:** **P2** overlay modals (5 JS builders) lack `role=dialog`/Escape/focus-trap/restore (shared-helper job); generator footer/nav contrast twin + the flash_cards translit-credit rendered link (data-i18n-html CSV, needs build-locales); ~18 segmented-pill groups expose selection only via `.active` (no `aria-pressed`); card/results no `aria-live`; profile-delete ✕ + nav `aria-label` (localize); `.tour-x` 26px / `.btn-xs` ~20px sub-30px touch targets. | run: 2026-07-14, S82
- [x] 2026-07-14 | (S81 close-out) | branch/deploy note | S81 continues on the designated branch `claude/new-session-vhfp4g`, adding to the already-open **draft PR #94** (S79–S80; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v306→v307** (precached file changed: `index.html`). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (reused existing `shared.ivrit.*` keys). `check-i18n` clean (the edits live in a script region + reuse existing keys → neither Check A nor Check B changes). `update-sitemap.mjs` refreshed `index.html`'s `/` `<lastmod>` to 2026-07-14. **Pass rotation:** ran the strictly-stalest **K** (i18n / localization audit, last 2026-07-12) — 2nd run. Iters: 1 pass (K) + 1 fix = 2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-14 | 6d4d1a1 | index.html (+ sw.js) | **Pass K — localize `index.html`'s `.ivrit` save/restore UI (a `check-i18n` blind-spot leak).** The home-page AllTools `.ivrit` backup UI was the last hardcoded-English copy of the shared `.ivrit` engine: the restore modal (`ivritAskMode` — title + Merge/Replace body + the 3 buttons) + the Saved/Restored status lines (`ivritSaveFile`/`ivritRestore`). Built via `innerHTML`/status-setter, so **invisible to `check-i18n`** (Check A blanks script regions) — a genuine English-in-Hebrew-UI leak on the most-hit page. Every other tool page already routes these through `I18n.t('shared.ivrit.*')`; ported index onto the flash_cards sibling **reusing the already-existing, already-translated `shared.ivrit.*` keys** (status_saved / status_restored / restore_dialog_title / restore_dialog_body / restore_merge_button / restore_replace_button / restore_cancel_button) — **no CSV, no build-locales, no new keys**. The body's `**bold**`/`\n` markdown is rendered inline; the values are trusted locale strings, so the port keeps index's no-`esc` convention (index defines no `esc` helper — not "fixing" its deliberately-adapted AllTools engine per CLAUDE.md). | Verified headless over local HTTP, index × light/dark × 1280/800 = **4 runs `pageerror==0`**: EN modal "Restore from .ivrit file" + Merge/Replace/Cancel + bold body + "Saved …"/"Restored from …" statuses; live `I18n.setLang('he')` → modal `שחזור מקובץ ‎.ivrit` + `מיזוג`/`החלפה`/`ביטול` + `נשמר …`/`שוחזר מ-…`; no raw `shared.ivrit.*` leaks; `check-i18n` clean. | fixed: 2026-07-14, S81
- [x] 2026-07-14 | (S81 Pass K — audit) | check-i18n + the template-literal blind spot | **Pass K (i18n / localization audit), 2nd run — first over the S72→S80 surface.** `check-i18n`: **Check A CLEAN** (no NEW hardcoded literals; 14 baselined printed-output items); **Check B** = 52 untranslated `title`/`aria-label`/`placeholder` attributes lacking a `data-i18n-*` sibling (generator 24, dashboard 13 mostly repeated "Restore default", Font Maker 11, torah 2, flash_cards 1, pwa.js 1); empty-`he` debt CLEAN. A 2nd agent probed the checker's blind spot (English in JS template literals / `innerHTML`) across generator/flash_cards/index → surfaced the **index `.ivrit` block** (fixed, 6d4d1a1) as the cleanest, highest-value slice (coldest tool, home page, existing keys). **Logged candidates:** the 52-attr Check-B backlog (bounded per-tool slices); the shared "Sound" group-header (generator/flash_cards, needs 1 new key); the 122 REVIEW-flagged Hebrew translation-quality rows. | run: 2026-07-14, S81
- [x] 2026-07-14 | (S80 close-out) | branch/deploy note | S80 continues on the designated branch `claude/new-session-vhfp4g`, adding to the already-open **draft PR #94** (S79; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v305→v306** (precached files changed: classroom_dashboard.html, hebrew_dictionary.html, torah_trainer.html, trope_tutor.html). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (reused existing `shared.fonts.*` keys). `check-i18n` clean (Check A no new); `update-sitemap.mjs` refreshed the 4 changed pages' `<lastmod>` to 2026-07-14. **Pass rotation:** ran the strictly-stalest **F** (cross-tool consistency, last S71) — 6th run. Iters: 1 pass (F) + 1 cross-tool fix = 2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-14 | d08f02a | classroom_dashboard.html + hebrew_dictionary.html + torah_trainer.html + trope_tutor.html (+ sw.js) | **Pass F — Hebrew font-picker i18n convergence (the S62-deferred affordance).** The picker's localization was split: `flash_cards` + `hebrew_blend_generator` route the section headers + `[DEFAULT]` badge through `I18n.t()`, while **`hebrew_dictionary`, `trope_tutor`, `torah_trainer` hardcoded them** (English "My Fonts / Print Fonts / Torah Fonts / Cursive / Script / [DEFAULT]" leaked into the Hebrew UI in 3 of 6 pickers), and `torah_trainer`'s "⬆ Upload your own font?" label was the only one of seven not localized. Converged the laggards onto flash_cards' pattern **reusing the already-existing `shared.fonts.*` keys** (`section_my_fonts`/`section_cursive`/`section_generic`{section}/`default_marker`/`upload_label`/`upload_title`) — **no CSV edit, no `build-locales`**: dictionary/trope/torah section-header ternary + `[DEFAULT]`→`default_marker`; dashboard `[DEFAULT]`→`default_marker` (its headers already localized); torah upload label localized to match the six siblings + **`initFontSelector()` added to torah's `applyI18n`** so the picker re-localizes on a live language switch (trope/dashboard/dictionary already did). Note: `[DEFAULT]` sits inside a single-quoted string within the template `${…}`, so the fix uses `' + esc/ivEsc(I18n.t(…)) + '` string-concat, not `${}` interpolation. | Verified headless over local HTTP, all 4 pages × light/dark × 1280/800 = **16 runs `pageerror==0`**: EN headers render "Print Fonts"/"Torah Fonts"/"Cursive / Script"/"Block Fonts" + badge `[DEFAULT]`; live `I18n.setLang('he')` re-localizes headers to Hebrew (`גופני …`, `כתב יד / סקריפט`) + badge `[ברירת מחדל]`; no raw `shared.fonts.*` key leaks; `check-i18n` clean. | fixed: 2026-07-14, S80
- [x] 2026-07-14 | (S80 Pass F — audit) | font-picker / tour-button / header-chrome affordances | **Pass F (cross-tool consistency), 6th run — surveyed 3 candidate affordances via 3 read-only agents; converged the most substantive/deferred one.** Chosen: **font-picker i18n** (dictionary/trope/torah hardcoded section headers + `[DEFAULT]`; torah upload label) — a real cross-tool split with existing keys, best impl = flash_cards/generator → fixed (d08f02a). Confirmed the shared `ivritsuite-fonts` + uploader blocks are still byte-identical (not the target); the `.font-opt` `aria-pressed` gap is uniform across all tools (enhancement-to-all, not a laggard). **Logged 3 candidates** for future passes: dashboard gear button `aria-label` (only icon-only header button using `title` alone — clean 1-line a11y); dashboard tour-pulse uses bespoke `.tour-pulse` vs the shared `.pulse-attn` (+ Font Maker has no header tour button — larger, separate); generator/flash_cards share-copy lacks the SR `announce()` that dictionary's copy has. | run: 2026-07-14, S80
- [x] 2026-07-14 | (S79 close-out) | branch/deploy note | S79 runs on the harness-designated branch `claude/new-session-vhfp4g`, based cleanly on the current `origin/main` tip (`5666f91`, the FM zoom-slider commit — 2 ahead / 0 behind at push, no stacking on merged history; S76–S78 already on `main` per the S78 note). Opens a **new draft PR**. **Docs-only session** — `README.md` / `CLAUDE.md` / `THIRD_PARTY_LICENSES.md` are **not** in `CORE_ASSETS`, so **no `sw.js` VERSION bump** (stays **v305**; consistent with the S48/S58/S70 docs-only Pass E precedent). No `FONT_MAKER_VERSION` bump; no locale rebuild; no `data/` edit. `update-sitemap.mjs` re-run = **no-op** (no sitemap-listed page changed; all 11 `<lastmod>` already git-current). **Pass rotation:** ran the strictly-stalest **E** (freshness & site-health, last S70) — 7th run. Iters: 1 pass (E) + 2 doc-freshness fixes = ~2. GitHub Pages deploy runs on merge to `main` — verify `success` after merge (this session is not on `main`).
- [x] 2026-07-14 | 2f89745 + c878a1a | README.md + CLAUDE.md + THIRD_PARTY_LICENSES.md | **Pass E doc-freshness reconcile — 2 drift fixes.** (1) **2f89745:** README FM version **v4.17→v4.26** (trailed `FONT_MAKER_VERSION`); CLAUDE.md FM line-count **~12,650→~12,900** (actual `wc -l` = 12,911, kept the "~" per the file's own drift caveat). (2) **c878a1a:** `THIRD_PARTY_LICENSES.md` gained a new **"Third-Party Web Services & APIs"** section documenting 5 previously-undocumented runtime services — **Hebcal** (dashboard calendar/Omer), **Open-Meteo** (dashboard weather + geocoding, data **CC BY 4.0**), **Nominatim/OpenStreetMap** (dashboard reverse-geocoding, data **ODbL 1.0** — requires "© OpenStreetMap contributors"), **Web3Forms** (contact/resources form backend + client script), **hCaptcha** (contact/resources bot protection) — each linked to its terms, with the CC BY / ODbL attribution obligations called out explicitly. No visible-UI attribution change (logged as a P3 candidate for a future dashboard session). | Verified: `grep` confirms README v4.26 + CLAUDE ~12,900; re-read of THIRD_PARTY_LICENSES confirms all 5 services present with license links + attribution notes; cross-checked against the audited CDN/API origins — 0 loaded-but-undocumented services remain, none documented-but-unused. Docs-only: no precached asset or sitemap page changed → no Playwright run needed; working tree held only the 4 intended files. | fixed: 2026-07-14, S79
- [x] 2026-07-14 | (S79 Pass E — audit) | site-health (root pages + sw.js + sitemap + docs) | **Pass E (freshness & site-health), 7th run — first over the S71→S78 + FM v4.18→v4.26 surface; mechanical CLEAN, doc-drift fixed.** Concrete audit (recon agents): **§1 SW `CORE_ASSETS`** — 25 entries both directions @v305 all resolve, every root HTML precached (`i18n-test.html` + splash/og-card intentionally runtime-cached) — CLEAN; **§3 sitemap** — 11 `<loc>` all map to files, every `<lastmod>` == git commit date (`update-sitemap` no-op), `404.html`/`i18n-test.html` correctly excluded — CLEAN; **§5 internal links** — every static href/asset/manifest/sw path resolves, 18 splash hrefs present — CLEAN. **§2 doc drift** (README FM version, CLAUDE line-count) + **§4 THIRD_PARTY_LICENSES** (5 undocumented runtime services, 2 with attribution duties) — FIXED (2f89745 + c878a1a). Also ran a **bonus off-rotation Pass A grep recon** (not a formal A run, doesn't advance A's rotation/streak): codebase defensively written, **no P1/P2**; logged 3 low-signal candidates (dashboard visible weather/geo attribution P3; `ftRead` plain-parse P4; dictionary/dashboard `colorizeHebrew` non-`esc()` P4). | run: 2026-07-14, S79
- [x] 2026-07-13 | (S78 close-out) | branch/deploy note | S78 developed on the designated branch `claude/new-session-s17scs` (draft PR #90). **The maintainer then authorized a direct push to `main`** (option B) after S78, so S76–S78 + the index CTA relabel land on `main` via **fast-forward** (main was a strict ancestor, 0 divergence) — PRs **#89** (`nv4854`) and **#90** (`s17scs`) are closed since their commits are now on main. `sw.js` VERSION **v300→v301** (precached file changed: `js/i18n.js`). No `FONT_MAKER_VERSION` bump; no locale rebuild (JS-only fix). Final pre-push regression sweep CLEAN (all 12 pages `pageerror==0` + console-clean; S77 torah 35/35; CTA 2/2). **Pass rotation:** ran the strictly-stalest **B** (console & error audit, last S69) — first B over the site-wide i18n rollout. Iters: 1 pass (B) + 1 fix = 2. **Verify the Pages "pages build and deployment" run on `main` concluded `success`.**
- [x] 2026-07-13 | 0f72e1d | js/i18n.js (shared runtime) | **Pass B — suite-wide pre-ready `[i18n] missing key` console flood, found + fixed.** Every tool page logged a burst of `[i18n] missing key: …` warnings on load (dictionary 567, Font Maker 377, resources 255, dashboard 122, generator 86, flash_cards 32, torah 10 — ~1000+/load across the suite). **Root cause:** pages call `I18n.t()` during init, before the deferred `/locales/*.json` fetch resolves, when `dict` is still `{}` — so every key looks "missing." Verified **all 239 unique flagged keys DO exist in en.json** (0 genuine gaps — 100% pre-ready timing); `applyI18n` re-renders on `I18n.ready` so the display self-heals, but the console was flooded (burying real errors). **Fix (~3 lines in the shared runtime):** gate the missing-key `console.warn` behind a new `dictLoaded` flag (set when the locale dict actually loads) — pre-ready the warning is suppressed, post-ready a genuinely-absent key still warns (and `applyI18n` re-exercises all init sites on ready, so real gaps are still caught). One change fixes all pages; **supersedes the per-page pre-ready gate** (the S76 trope_tutor `_i18nReady` pattern). | Verified headless over local HTTP: re-run Pass B across all 12 pages → **`[i18n] missing key` count: 0** (was 239 unique / ~1000+ total), `pageerror==0`, no non-env console output; a post-`ready` genuine miss (`I18n.t('genuinely.missing.key.xyz')`) STILL warns + returns the key (detection preserved). | fixed: 2026-07-13, S78
- [x] 2026-07-13 | (S78 Pass B — audit) | all 12 shell pages | **Pass B (console & error audit), 6th run — first over the site-wide i18n rollout + S70→S77; 1 suite-wide finding found + fixed, otherwise CLEAN.** Headless over local HTTP, all 12 pages × light-1280/dark-800, load capture of `pageerror` + `console.error`/`.warn` + failed local requests, env noise (route-aborted externals) filtered. **`pageerror==0` everywhere; the ONLY non-env output was the pre-ready `[i18n] missing key` flood** (239 unique keys, all verified present in en.json → pre-ready timing, not gaps) → fixed suite-wide in js/i18n.js (0f72e1d). No genuine missing keys, no crashes, no broken interactions. (`i18n-test.html` excluded — its `I18n.t('this.key.does.not.exist')` is an intentional harness miss.) | run: 2026-07-13, S78
- [x] 2026-07-13 | (S77 close-out) | branch/deploy note | S77 continues on the designated branch `claude/new-session-s17scs`, adding to the open **draft PR #90** (S76; `HEAD` ahead of `origin/main` → no branch restart, no new PR). `sw.js` VERSION **v298→v299** (precached files changed: `torah_trainer.html` + regenerated `locales/{en,he}.json`). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **H** (teacher walkthrough / paper-cuts, last S68) on **torah_trainer** (un-H'd + lesson-shaped; H had covered flash_cards/dashboard/generator/dictionary). Iters: 1 pass (H) + 2 fixes = 3. (The S72–S75 PR #89 on `nv4854` remains separate for the maintainer to reconcile.)
- [x] 2026-07-13 | 8aa6691 | torah_trainer.html (+ locales) | **Pass H teacher-walkthrough fixes — 1 P3 + 2 P4, the safe/unambiguous slice.** **P3 FIXED:** the scope dropdown (`#scopeSelect`) had only an aria-label while its `Parsha:` sibling shows a visible label — a teacher saw an unlabeled control the tour/How-To call "the Reading dropdown"; added a visible **"Reading:"** label (new `torah.controls.reading_label`, en `Reading:` / he `קריאה:`) mirroring the sibling, with the fuller `Reading scope` aria-label preserved. **2 P4 FIXED:** `#gearBtn` gained an `aria-label` (reusing `torah.header.settings_title`) matching its dark/fullscreen siblings (closes the gearBtn half of S72 G4); and the one hardcoded-English audio status `'No chantable audio for this reading.'` now routes through `I18n.t('torah.status.chant_unavailable')`. `check-i18n` clean. | Verified headless over local HTTP (**35/35**, EN+HE × light/dark × 1280/800): scope label reads `Reading:`/`קריאה:` (visible, `for=scopeSelect`, aria preserved), `#gearBtn` exposes `Settings`/`הגדרות`, no h-overflow @800px, `dir=rtl` in HE, `pageerror==0`; hardcoded literal gone (grep 0). | fixed: 2026-07-13, S77
- [x] 2026-07-13 | (S77 Pass H — audit) | torah_trainer.html (teacher walkthrough) | **Pass H (teacher walkthrough / paper-cuts), first-ever on torah_trainer — effectively CLEAN flow, rich friction inventory + 2 Feature seeds.** Role-played a teacher preparing a Torah-reading lesson end-to-end (fresh profile, headless over HTTP, light+dark, 1280+800): auto-loads this week's parsha, scope/display/audio/karaoke all work, settings persist (the debounced `saveSettings` verified), `pageerror==0`. **Highest-value gap → Feature seeds:** the core "print readings for students" use case has **no Print affordance** (Ctrl+P only) and **no student-handout mode**. **Logged P3s:** buried "Jump to this week" + Diaspora/Israel toggle; custom-range never prefills from the active reading; tour skips its 2 key steps on a slow first load. **Logged P4:** low-visibility audio-unavailable status; audio-collapse `aria-expanded`. **Fixed same session** (8aa6691): scope-dropdown label + gearBtn aria + one audio-status i18n. | run: 2026-07-13, S77
- [x] 2026-07-13 | (S76 close-out) | branch/deploy note | S76 runs on the **newly-assigned** designated branch `claude/new-session-s17scs`, which already contained all S72–S75 commits (same tip as the prior `claude/new-session-nv4854`) → a clean continuation, but the harness assigned a **different branch name** this session, so per the develop-on-designated-branch rule S76 pushes to `s17scs` and opens a **new draft PR**. The S72–S75 draft **PR #89** (on `nv4854`) is left for the maintainer to reconcile — not pushed to without permission. `sw.js` VERSION **v297→v298** (precached file changed: `trope_tutor.html`). No `FONT_MAKER_VERSION` bump; **no locale rebuild** (the fix reused existing CSV keys). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **I** (first-load & empty-state, last S67) — the **first-ever I over the 2026-07-12 site-wide i18n rollout**. Iters: 1 pass (I) + 1 fix (trope status i18n) = 2.
- [x] 2026-07-13 | 8845e7f | trope_tutor.html | **Pass I first-load fix — the trope index-load status messages were hardcoded English, so a Hebrew-UI user saw English (worst on a failed/offline load, whose error persists on screen).** All four `setStatus()` call sites (index-loading, index-unavailable, audio-loading, tap-to-hear) passed English literals — invisible to `check-i18n` (its documented plain-arg blind spot) — even though **matching `trope.status.*` keys with completed Hebrew already existed in the CSV, just unused** (added by the i18n rollout; the call sites were never wired). Wired all four to their keys via a new `setStatusKey(key, en, isError)` that keeps the English literal as a **pre-ready fallback** (`loadData` runs during `init`, before `/locales/*.json` loads → a `_i18nReady` gate avoids a raw-key flash / missing-key warn) and stores `_statusState` so `applyI18n` **re-translates a visible status** on `I18n.ready` and on live language switch. No CSV/locale change; `check-i18n` stays clean. | Verified headless over local HTTP (**19/19**): happy path clears status + Learn renders + `pageerror==0` (light/dark/stacked); HE + blocked index → Hebrew error copy; EN + blocked index then live `setLang('he')` → re-translates to Hebrew (applyI18n + `_statusState`); locale-JSON + index both blocked → English fallback literal, **no raw key, no crash**. | fixed: 2026-07-13, S76
- [x] 2026-07-13 | (S76 Pass I — audit) | all 12 root pages (fresh-profile) | **Pass I (first-load & empty-state), 5th run — first over the site-wide i18n rollout; effectively CLEAN, 1 i18n leak found + fixed.** Headless Chromium over local HTTP, a **fresh browser context per run** (empty localStorage = brand-new user), all 12 pages × desktop-1280/stacked-800 × light/dark = **48 runs: `pageerror==0`, 0 local console errors, 0 failed local requests.** Confirmed: absent-key safety solid (every `JSON.parse(getItem)` guarded — static recon + runtime agree, incl. the no-`||` `ivritSafeParse(getItem)` settings loaders in trope/torah); a brand-new user (no `hebrewBlender_lang`) defaults to English `dir=ltr` with the switcher mounted and **no raw `page.feature.x` keys leaking in rendered chrome**; first-run affordances fire (dashboard `frModal` + seeds settings/presets, Font Maker `welcomeOverlay`). **1 finding fixed** (8845e7f — trope_tutor status i18n, the one spot a Hebrew first-time/offline user saw English). **1 P4 logged** (flash_cards dead `#presetEmpty`/`#profileEmpty` copy, superseded by the folder-tree empty state). | run: 2026-07-13, S76
- [x] 2026-07-13 | (S75 close-out) | branch/deploy note | S75 continues on the designated branch `claude/new-session-nv4854`, adding to the already-open **draft PR #89** (S72–S74; open, not merged, `HEAD` 8 ahead of `origin/main` / 0 behind → no branch restart, no new PR). **No `sw.js` VERSION bump, no sitemap change** — measured-clean audit, **no product code changed** (docs-only ledger, not precached; like S66/S67/S69). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **D** (performance, last S66) — a **2nd-pass on `flash_cards.html`** (the only tool without a 2nd D; first + only D was S15). **Measured effectively CLEAN — no main-thread block >200 ms in any interaction; `pageerror==0` @1×+4×; 0 fixes** (the parked S15 `buildPool`×3 re-confirmed negligible, no regression). Iters: 1 pass (D) + 0 fixes (discovery-only). _(Reached via the user's "Rerun" after S74.)_
- [x] 2026-07-13 | (S75 Pass D — no code) | flash_cards.html (read-only profiling) | **Pass D (performance) — 2nd pass on `flash_cards` (first + only prior D was S15); measured effectively CLEAN.** Headless Chromium over local HTTP, `PerformanceObserver('longtask')` + `MutationObserver`, desktop 1× **and** 4× CPU throttle, card counts 10/20/100, `pageerror==0` throughout. **The S15 `buildPool`×3-per-card (letters) re-measured negligible, NO regression:** ~7.6 µs/card; `generateDeck(20)` **0.4 ms @1× / 1.55 ms @4×** (S15 logged 1.3–5.9 ms → at/below), `generateDeck(100)` ≤ **6.2 ms @4×** (~30× under the 200 ms bar). A `ctx`-hoist (base pools + `allVowelsList` once in `buildDeckContext`, like emoji already does) is mechanically clean but buys ~1–5 ms/deck → **stays parked P4.** **CLEAN with numbers:** cold load TTI 1410 ms @1× (load longtasks: @1× one 68 ms <200 ms; @4× one one-time 245 ms init — throttled-only P4); emoji `fetchEmojiData` 46/75 ms one-time (pool cached via `ctx.emojiPool`); words/all-dict `JSON.parse` 17/74 ms sync (<200 ms; the 2.9 MB load is async network wait, not main-thread; `resolveWordsPool` runs on setup, not the deck loop; the `?wl=` hook is a trivial gated localStorage read); **idle** — a running timer swaps ONE `#timerDisplay` text node/tick (5 mutations/3.6 s), present/fullscreen is pure CSS (**0 `requestAnimationFrame`, the only `setInterval` is the timer**), no per-tick DOM rebuild. Nothing to fix. | Verified via the instrumented 1×/4× profiles (deck-gen, cold load, idle, emoji/words paths). | run: 2026-07-13, S75
- [x] 2026-07-13 | (S74 close-out) | branch/deploy note | S74 continues on the designated branch `claude/new-session-nv4854`, adding to the already-open **draft PR #89** (S72–S73; open, not merged, `HEAD` 6 ahead of `origin/main` / 0 behind → no branch restart, no new PR). `sw.js` VERSION **v296→v297** (precached file changed: `hebrew_blend_generator.html`). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **G** (print & export fidelity, last S65) — a **2nd-pass** on the **worksheet generator** (stalest + most-changed printable tool; last G was S30–S32, before the S56 crossword-RTL / S61 chunked-build / i18n changes). Iters: 1 pass (G) + 1 fix (self-check print-chrome) = 2. _(Reached via the user's "Rerun" after S73.)_
- [x] 2026-07-13 | e57c06d | hebrew_blend_generator.html | **Pass G (print & export fidelity) — 2nd-pass on the generator; substantially CLEAN, 1 P3 fixed.** Headless print-emulation (all 8 modes × Letter/A4 × light/dark) + code-audit of the PDF path. **CLEAN (verified held over the S56/S61/i18n changes):** S30 dark-mode print reset (dark prints light — `body` bg white, Hebrew navy in both themes), S30 per-item + S31 whole-grid `break-inside:avoid`, S31 Letter/A4 `@page` + landscape (`_applyPageSizeStyle`), gold answer-ink `#6b4e12` on B/W, **gematria B6/B7 both now fixed**, S56 crossword RTL, no nikkud clipping; **the S61 chunked-build × print contract INTACT** (`beforeprint`→`flushWorksheetBuild` L8981 flushed 30/30 sheets, `exportPDF` flushes L8189 — no truncation on any print path); **the i18n boundary held** (`.worksheet{direction:ltr}` L717 + `headerLang` decoupled → a Hebrew UI prints an unchanged English-header LTR sheet); `pageerror==0` throughout. **P3 FIXED:** self-check flip-card mode appended `.score-bar`/`.self-check-controls`/`.timer-bar` into `#worksheet` but the print `display:none` list (L1989–1990) never hid them → a printed self-check sheet carried the score/controls/timer chrome above the flip cards, **and in dark mode the `.btn-xs` inside them bled dark `#1e2535` ink** (L357, unrelit by the S30 reset). Added the 3 classes to the print hide-list (one line) — removes the junk **and** the dark-ink bleed; flip-card fronts still print. | Verified headless over HTTP (light+dark): a real self-check worksheet renders the 3 bars visible on screen but `display:none` in print, no self-check `.btn-xs` contributes ink in print, worksheet content still prints, 0 pageerrors. | fixed: 2026-07-13, S74
- [x] 2026-07-13 | (S73 close-out) | branch/deploy note | S73 continues on the designated branch `claude/new-session-nv4854`, adding to the already-open **draft PR #89** (S72; open, not merged, `HEAD` 4 ahead of `origin/main` / 0 behind → no branch restart, no new PR — like S67–S71 on PR #86). `sw.js` VERSION **v295→v296** (precached file changed: `Hebrew_Font_Maker.html`). **No `FONT_MAKER_VERSION` bump** — internal security hardening, no user-facing change (matches the S64 UFO-guard precedent). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **A** (recurring-pattern sweep, last S64) over the large never-A-swept surface since S64 (the whole 2026-07-12 site-wide i18n rollout + Pass K + S68/S71/S72 diffs), 2 recon agents. Iters: 1 pass (A) + 1 fix (FM XSS hardening) = 2. _(Reached via the user's "Rerun" after S72.)_
- [x] 2026-07-13 | a87745e | Hebrew_Font_Maker.html | **Pass A (recurring-pattern sweep) — 4 of 5 active classes CLEAN over the i18n-rollout surface; unescaped-input/unsafe-parse found + fixed a P2 stored-XSS (+ a P3 folded in).** **CLEAN:** falsy-zero (the `substitute()` engine + `===1` plurals preserve a legal 0; every `|| N` a legal guard), localStorage-vs-AllTools (the one new key `hebrewBlender_lang` is a per-device UI pref, correctly export/import-exempt + erase-omitted like `hebrewBlender_darkMode`), destructive-bulk (the S72 `?wl=` deep-link is select/add-only; all `applyI18n()` hooks read-only), shadowed-global (one `esc`/`applyI18n` per file; FM `t()` + `pwa.js` `t()` intentional in-scope locals). **P2 stored-XSS (fixed):** a crafted `.hebrew-font-maker-project` import sets a letter/mark `.name` to `<img onerror=…>` (`migrateProject` L12375 preserves the imported name verbatim), which flowed **unescaped into `innerHTML`** across side-panel builders → executes (same-origin) when the victim opens that glyph's editing panel. Introduced by the i18n rollout (`gName()`). **The recon undercounted (5 sites); the end-to-end exploit test caught 5 MORE**: fixed **10 name sinks** — 5 `gName(...)` panel titles (metric/markctl/adjust/outline/trace) **+ 5 direct `${l.name}`/`${it.name}`** (crop-overlay `<text>`, letterDrop + markDrop drop-zones, "Region selected" hint, anchor/place panel title) — **+ 3 precomp glyph spans** (`precompComposed(pc)`, the P3), all one-token `esc()` wraps matching the already-correct siblings (`esc(gName(...))` 9567/9568, `esc(precompName(pc))` 8408). `status()` toasts (textContent) + canonical `LETTERS`/`NIKKUD_MARKS` grid names were already safe. | Verified headless over HTTP (light+dark): a malicious letter name imported via `applyProjectData` renders as **literal escaped text** in the align + trace panels — no injected `<img>`, `onerror` never fires; normal glyph titles still render; a **whole-file grep confirms zero remaining unescaped project-name innerHTML sinks**; 0 pageerrors. | fixed: 2026-07-13, S73
- [x] 2026-07-13 | (S72 close-out) | branch/deploy note | S72 runs on a **fresh** designated branch `claude/new-session-nv4854` that sat **exactly on `origin/main`** (`bb2b92a`, 0 ahead) at session start — the S66–S71 **draft PR #86 merged**, and the **2026-07-12 i18n rollout (PRs #87 full-site localization + #88 language-switcher `<select>`)** + the **first Pass K** landed on `main` too (SW **v280→v294**, all 12 pages localized, `check-i18n` gate + column-driven locale pipeline). No open PR at start → S72 opens a **new draft PR**. `sw.js` VERSION **v294→v295** (precached files changed: torah_trainer, hebrew_dictionary, flash_cards, locales/{en,he}.json). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **C** (accessibility, last S63) on **torah_trainer** (coldest by recent-touch variety, untouched in S70/S71; last dedicated C ~S18). Iters: 1 pass (C) + 2 fixes (G2 reduced-motion, G1 aria-label localize) + **1 micro-feature** (Dictionary→Flash Cards deep-link, 2 iters) = 5 (budget; micro-feature pulled by user direction). | done: 2026-07-13
- [x] 2026-07-13 | 471e765 + ecf9faf | torah_trainer.html (+ locales) | **Pass C (accessibility) — torah_trainer, 2nd dedicated pass (last S18); the tool is heavily hardened so NO P2 keyboard/focus blocker remained — 2 real fixes, 2 logged.** Recon confirmed already-solid: drawer focus-trap + focus-return + Escape, roving-tabindex reading surface, panel-title button semantics, labeled audio bar + region, tour dialog + key handling, `lang="he"` chokepoints, RTL drawer-flip via logical props. **Fixed:** (G2, 471e765, P4-but-free) the `@media (prefers-reduced-motion: reduce)` block missed the `.tt-word` **karaoke highlight** transition (L576) + the audio-collapse caret (L735) — added a one-line neutralizer, no new keys. (G1, ecf9faf, P3 — the genuine RTL-rollout regression) five `setAttribute('aria-label', …)` sites hardcoded **English** so a Hebrew-UI screen-reader user got English names after interacting (invisible to `check-i18n` — its documented plain-arg blind spot): play/pause (L3671), audio collapse (L3659), verse loop (L3034), and the two color pickers (L3991/L4039) now route through `I18n.t()` — 4 reuse existing keys, only `torah.settings.color_for_aria` is new. **Logged as Candidates:** G3 gold-on-light contrast sweep (P3, maintainer design call), G4 `#gearBtn` aria polish (P4). | Verified headless over local HTTP: G2 — `.tt-word`/caret compute `transition:none` under reduced-motion, keep 0.1s/0.2s otherwise, 0 pageerrors light+dark; G1 — all 7 keys resolve Hebrew + live-exercised collapse toggle + both color-picker builders emit Hebrew aria-labels (`{name}`/`{verseRef}` content preserved), 0 pageerrors light+dark; `check-i18n` clean, `build-locales` +1 key. | done: 2026-07-13, S72
- [x] 2026-07-13 | 908a83c | hebrew_dictionary.html + flash_cards.html (+ locales) | **MICRO-FEATURE: "Use in Flash Cards →" Word List deep-link (Dictionary → Flash Cards) — ships the top S68 Feature seed, closing the "save a Word List → drill it" handshake that dead-ended.** Emit: each Dictionary Word List manager row + the save-success modal state now offer a `<a href="flash_cards.html?wl=<encodeURIComponent(id)>">` built via the existing `wlEl()` DOM helper (href by property assignment, name via `textContent` — no `javascript:`-interpolation, no `esc()` gap; id = the store's stable key). Consume: a ~12-line `?wl=` restore hook at the end of Flash Cards' `DOMContentLoaded` validates the id against the shared `ivritSuite_wordLists` store (read via `ivritSafeParse`), selects it into Other→Words→My-Lists with the list pre-checked, and lands the teacher on setup — **no auto-start** (a link only sets the live view); a garbage id is a silent no-op that never mutates saved data. **No new localStorage key** (no AllTools/`getSettings` change); one new i18n key (`dictionary.wordlist.use_in_flashcards`). Generator half deferred (larger/entangled) → new Feature seed. ~20–25 lines, 2 files — within the micro-feature fence. | Verified headless over local HTTP (EN + Hebrew RTL, light + dark): emit link renders with correct encoded href + is XSS-safe against a crafted `<img onerror>` list name (rendered as literal text, no element, onerror never fired); save-success link hidden→reveals with `?wl=<newid>` after a real create; consume auto-selects (Other-mode pill active, Words sub-mode, list checkbox checked, setup screen active, no drill started); garbage `?wl=` no-op with store intact; fresh-profile (empty localStorage) clean on all 3 pages; 0 pageerrors throughout; `check-i18n` clean. | done: 2026-07-13, S72
- [x] 2026-07-12 | i18n rollout (pages 8–11/11 — FINAL) | index.html + resources.html + contact.html + privacy.html + 404.html + pwa.js | **DONE: the five static pages + the shared PWA install banner converted to full runtime i18n + Hebrew RTL — completing the whole-site rollout (all 12 pages).** Same reference template as the tool pages: deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher + `data-i18n*` static annotation + dynamic strings → `I18n.t()` + an `applyI18n()` re-render hook wired **inside** DOMContentLoaded (deferred i18n.js ⇒ `window.I18n` is undefined at inline-parse time) + physical→logical RTL CSS (no-ops in LTR, mirror in RTL). Footer brand/attribution credits (Created by / Word Data from / CC BY-NC-SA / GitHub) + resource-directory third-party content stay English, matching the suite convention. **`index.html`** (own commit): header/hero/8 tool cards/buttons + the **AllTools backup modal** — 50 static annotations + 31 dynamic sites (My-Fonts manager, export/import/erase alerts+confirms, the adapted `.ivrit` restore engine, bookmark tip, dark label) via `home.alltools.*`/`shared.ivrit.*`/`shared.fonts.*`; the "reload open tool pages" success `alert()` kept as a translated blocking instruction (added `home.alltools.restore_reload`). **`resources.html`**: 98 annotations + 36 dynamic (both filter bars, both empty states, FAQ/how-to, related-tools nav, the Suggest-a-Resource + font-preview modals, results/fonts counts, `CAT_BADGE_KEY` map, `Grades:`/`By {author}` prefixes, form validation). **`contact.html`**: web3forms form (labels/placeholders/validation via `contact.form.*`, `{email}`/`esc(message)` preserved) + donation tabs (`contact.donate.*`, PayPal/Venmo/Zelle brand-literal). **`privacy.html`**: 10 policy sections + FERPA; the §2–§5 multi-part list bodies (single `\n`-joined CSV keys) render via a new **`renderLegalBlocks()`** that zips `\n`-split segments onto each block's `p`/`li` slots, bailing to the English fallback markup on count-mismatch. **`404.html`**: hero title/subtitle (Hebrew-wordplay markup + the inline `(לֹא נִמְצָא)` span), quip, home button, tool links; the decorative Hebrew `דַּף לֹא נִמְצָא` heading + `4✡4` display left as-is. **`pwa.js`** install banner (Android/iOS text + aria-labels) reads through a guarded `t(key, fallback)` — English fallback when the dict hasn't loaded — keeping it self-contained + precached. **Translations**: filled Hebrew for all **268** `home/resources/contact/privacy` keys (1 delta-subagent, terminology locked to the tool pages' existing Hebrew — tool names, קלט אוטומטי/ידני, ניקוד/טעמים, ייצוא/ייבוא, לוח חכם) + the **11 `notfound.*`** (404, preserving the Hebrew-pun quip/subtitle) + the **4 `pwa.*`** + `flashcards.wordlist.untitled`; the whole CSV is now **0 untranslated except `worksheet.layout.title_default`** (the printed default worksheet title — deliberately English). `sw.js` **v287→v290** (v289 index, v290 the rest). | Verified headless (EN+HE, dir flips, switcher mounts, Hebrew chrome, live switch re-renders, **0 raw keys in rendered UI, 0 pageerrors**): index **8/8** (+ **6/6** structural + AllTools modal Hebrew), resources **9/9**, contact/privacy/404 **23/24** (the 1 "fail" a test artifact — 404's `<h1>` is the literal `IvritSuite` wordmark; its hero title/subtitle/button confirmed Hebrew in a follow-up). This closes PR #87's rollout: all tool pages + all static pages localized. | done: 2026-07-12
- [x] 2026-07-12 | i18n rollout (page 7/11) | Hebrew_Font_Maker.html | **DONE: Hebrew Font Maker — the suite's largest single-file app (~12.7k lines, ~1,270 keys) — converted to full runtime i18n + Hebrew RTL, with the glyph-edit stage coordinate system pinned LTR so tracing NEVER mirrors.** Seventh page of the full-site rollout, the deepest dynamic surface. Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher. **THE INVARIANT THAT HELD:** `.stage-wrap`/`#stage`/`#meStage`/`.ws-center` are pinned `direction:ltr` (`fx=x=>PADX+x*SC`, no x-flip) so a traced glyph's on-screen coordinates are identical EN vs HE — verified `#stage` computes `direction:ltr` in both languages and the stage never mirrors under `dir=rtl`. **Static chrome**: header, all panels, add-glyphs, footer, every modal, the guided tour. **Glyph/letter names → Hebrew** via a `gName()` helper (`fontmaker.glyphname.*`, English fallback). **Dynamic → `I18n.t()`** (mostly delegated to two focused subagents, then verified): the next-step hint (`nexthint_*`, sentinel-split to keep the clickable glyph link), step tabs + per-glyph tick aria (`tabs.*`), engine badge + `PYO_PHASE_LABEL` (resolved at call time), tile-status aria words (`ltStatusWord`), the `#adConfirm` "changed N boxes" banner, the shortcuts sheet (`shortcutGroups()`/footer via `help.shortcuts_grp_*`/`sc_*`), **68 `status()` toasts**, **all 41 `askModal()` dialogs** (titles/bodies/buttons via `fontmaker.modals.*`, dynamic error bodies + `<b>`-emphasis on interpolated names/counts preserved, `.one`/`.other` plurals), and **9 renderControls builder panels** (`renderMarkControls`, `renderNodeChrome`, `nodeEditPanelHTML`, `renderPrecompControls`, `renderEngAccentLeft`, `renderAnchor{Right,Under,StageBelow}`, `kernClassManagerHTML`). **About tab** now builds its changelog from the 46 `fontmaker.changelog.v*` keys (newest-first `CHANGELOG_ORDER` + `buildAboutBody()` intro/special-thanks/changelog) so version history is translatable; `HELP_CONTENT.about` kept only as the no-I18n fallback. **`applyI18n()`** (wired INSIDE `init()` after the deferred i18n.js — top-level `window.I18n` is undefined): `applyStaticI18n` + re-render read-only surfaces (`renderControls`/`renderGrids`/`updateTabs`/`renderNextStepHint`/`_setPyoPhase`) + retranslate an open Help/Shortcuts modal — **never `udDo`/`markDirty`** (undo/dirty contract) and never touches the stage geometry; a live switch preserves the open project + undo stack. Also clears a stale raw-key `#statusMsg` (newProject's init toast runs before the dict loads). **RTL CSS**: 24 physical→logical conversions for chrome (`.lt-dot` status badge → `inset-inline-start`, dropdown/close-button/`.tour-x`/`#fsExitBtn` anchors, `margin-inline-start:auto`, `text-align:start`) — all no-ops in LTR (EN byte-identical), mirror in RTL; the `.ws-split` column-resize delta is sign-flipped under `dir=rtl` (the grid mirrors via `grid-template-areas`); stage/coordinate-input LTR rules deliberately untouched. **Keys**: added `status.pyo_*`, 12 builder keys (`node_editor.sel_actions`/`point_type`/`segment_type`/`placing_points`/`points_selected.one·other`/`no_points_selected`, `panels.custom_mark`/`contours_count.one·other`/`edit_shape_btn`), split `xform_summary_sel` into `.one`/`.other`, removed one duplicate (`precomp_anchor_hint`→existing `precomp_where_hint`), and added the `v4_18` changelog key; final **3,692** keys, **0 `fontmaker.*` untranslated**. `FONT_MAKER_VERSION` **4.17→4.18** (+ matching changelog `<li>` + CSV key); `sw.js` **v287→v288**. **Known deferred gap:** the 5 upload drop-zones (`markDrop`/`letterDrop`/etc.) stay English — their existing keys flatten out the inline `browse` `onclick`; a suite-wide drop-zone pass (behavior-preserving) is the right home for them. | Verified: headless Playwright over local HTTP — **13/13** standard HE/EN (dir=rtl/lang=he, Hebrew header + 5 panel-titles, `#stage`+`.stage-wrap` `direction:ltr` in EN **and** HE, tour Hebrew, live switch keeps stage LTR, 0 pageerrors) + **8/8** About-tab (Hebrew intro w/ "גרסה 4.18", 47 changelog `<li>` from keys, v4.18 at top, EN shows 4.18) + Save-Project askModal fully Hebrew (title/body/3 buttons) + a refined visible-text leak scan (script/style excluded) = **0 raw `fontmaker.*` keys in rendered UI, 0 pageerrors, dir=rtl**. The leak scan caught + fixed the newProject init-toast raw-key (added a stale-key clear to `applyI18n`) and a duplicate precomp key. | done: 2026-07-12
- [x] 2026-07-12 | i18n rollout (page 6/11) | classroom_dashboard.html | **DONE: Classroom Dashboard UI chrome converted to full runtime i18n + Hebrew RTL — projected widget CONTENT stays independent of UI language.** Sixth page of the full-site rollout, and the largest dynamic surface so far. Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher + `.dir-arrow`/`.bidi-isolate` utils (CSP already `'self'`+hebcal/open-meteo/nominatim/youtube — no edit). **Static chrome** (~190 `data-i18n*` via an annotate script + a large manual pass): the entire settings drawer (13 panels: Location/Display/Date&Time/Weather/Timer/Picker/Day-of-Week/Omer/Dashboard-Text/Presets/Schedule-Sync/Hebrew-Settings/About), zoom bar, fullscreen strip + exit, first-run setup card, body timer/picker/weather controls, the class-management + font-upload controls, tooltips (text→child `<span>` + `data-i18n-tip`), radio/bare-text labels wrapped in spans, all placeholders, the related-tools nav (`shared.nav.*`), the `.ivrit` backup UI (`shared.ivrit.*`), and the About/FAQ markup restored into the CSV (`data-i18n-html`, marked `html`). Ambiguous cross-context strings (Display/Underline/None/Reset/Timer/Weather/…) were assigned by-hand to the right panel key. **Shared blocks re-unified byte-identical to flash_cards** (`.ivrit` engine, folder-tree JS, My-Fonts uploader — all now `I18n.t()`-based; app-toast + ivritsuite-fonts were already identical). **Dynamic → `I18n.t()`**: `TOUR_STEPS` (`titleKey`/`bodyKey`+`shared.tour.*`), 18 `alert`/`confirm`/`prompt` dialogs, `STARTERS` starter cards, the whole Schedule-Sync builder (rows, header, per-row move/remove, validation warnings, preview, saved-schedule/preset row builders), the on-board Now/Next block + countdown, the Student-Picker (progress, group messages, roster count/label, new-round), weather/Shabbat status states, timer "Time!", and the font-selector section headers (`shared.fonts.section_*`). **Content pin:** `hebDisplay()` sink now emits `lang="he" dir="rtl"`. **Widget content-language INDEPENDENT (per the locked decision):** column titles, weather description (WMO), dates, days, Omer, Shabbat candle-lighting/havdalah (en-US), and student-group card labels all stay under the existing `headerLang`/`dowLang`/`showOmerEnglish`/`showEngDOW` toggles + `toLocaleDateString('en-US',…)` — **NOT** coupled to `I18n.lang` (verified: with `headerLang='en'` the column title stays English even in a Hebrew UI). The default editable board message (`dashTextHTML`) is likewise user content — left as-is. **RTL:** settings drawer flips to the **left** edge in Hebrew (shared logical-props + `[dir=rtl] .settings-modal.open` specificity-tie fix); logical props for chrome (`margin-inline-start`, `text-align:start`). **`applyI18n()`** (defined + wired inside the DOMContentLoaded init, after the deferred i18n.js) re-runs `applyStaticI18n` + rebuilds every dynamic surface (font selectors, schedule rows/preview/UI, saved-schedules, presets, picker, date-time, days, weather, Omer, Shabbat) and re-renders the first-run starter cards when the modal is open (it auto-opens before `I18n.ready`). Added **2 new keys** (`dashboard.settings.drawer_aria`, `dashboard.backup.auto_explainer`); filled Hebrew for all **396** `dashboard.*` keys — **1 flagged `REVIEW:`** (`dashboard.days.ytt_size` "Y/T/T size" → "גודל א/ה/מ", the Yesterday/Today/Tomorrow initials). `sw.js` **v286→v287**. | Verified: headless Playwright over local HTTP — **16/16 HE/EN assertions PASS** (dir=rtl/lang=he, Hebrew header + 13 panel-titles, drawer opens on the **LEFT** edge in HE / RIGHT in EN, content pinned `lang=he dir=rtl`, **widget content follows `headerLang` not the UI language**, no English chrome leak in the drawer, live language switch preserves the in-progress schedule + flips dir, 0 pageerrors) + a **12/12** dynamic-path smoke run (schedule builder / picker / presets / tour render clean in both languages). Two HE screenshot passes caught + fixed: first-run starter cards showing raw keys (rendered pre-`ready` → re-render added to `applyI18n`), the Shabbat "unavailable" status showing its key (Shabbat re-render added to `applyI18n`), and 8 ambiguous drawer labels (Display/Display-size/Underline/None/Reset) the annotate-script had skipped. | done: 2026-07-12
- [x] 2026-07-12 | i18n rollout (page 5/11) | trope_tutor.html | **DONE: Trope Tutor UI chrome converted to full runtime i18n + Hebrew RTL — the taught Ashkenazi/Sephardi pronunciation NAMES stay as content.** Fifth page of the full-site rollout, right after its Torah twin (reused Torah's just-built patterns). Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher + `.dir-arrow`/`.bidi-isolate` utils (CSP deliberately `'self'` with **no** sefaria/esm — none added). **Static chrome** (~64 `data-i18n*` via an annotate script + a 28-item manual batch): header/skip-link, Learn/Drill tabs, the whole settings drawer (tradition Ashk/Seph, drill-type toggles, playback rate, Style, font/reset/upload), tour card, related-tools nav (`shared.nav.*`), and About/Learn-intro/FAQ whose `<a>`/`<strong>`/`<em>` markup was restored into the CSV (marked `html`). **Dynamic → `I18n.t()`**: `TOUR_STEPS` (`titleKey`/`bodyKey`+`shared.tour.*`), every `showToast` caller (`trope.toast.*` audio-failure fallbacks), the full drill flow (progress/Identify/Hear/Melody prompts, correct/incorrect, finish/next, results summary + `types_summary` with `type_*`, mastery-cell titles, min-one-type inline refusal, not-enough/index-not-loaded), and the Learn cards (family notes, per-trope notes with English fallback, `name_ashkenazi/sephardi_prefix`, rare-tag, see-in-parsha, play-example aria, visited aria). **The family chips + mastery-grid family labels** now resolve through `trope.glyphname.family_*` (was hardcoded English `fam.name` — the one genuine I18n leak, caught by an HE screenshot pass and fixed at both `renderFamilyChips` + `buildMasteryGrid`). **Content pin:** Hebrew is DOM-built (`createElement`/`textContent`, never `innerHTML`) — `renderMarkedWord` (example/question words) + `tropeGlyphSpan` glyph tiles already emit `lang="he" dir="rtl"`. **`applyI18n()`** (registered on DOMContentLoaded after the deferred i18n.js): `applyStaticI18n` + `initFontSelector` + re-render the Learn view (when visible) + `updateDrillStart`, but **NOT the active drill question** (an in-flight audio clip must not be disrupted). **RTL**: settings drawer flips to the **left** edge in Hebrew (the shared logical-props + `[dir=rtl] .settings-modal.open` specificity-tie fix); the `role="tablist"` roving arrow-key nav made direction-aware (`fwd = dir==='rtl' ? ArrowLeft : ArrowRight`); header wraps on narrow screens (54px @360 overflow → 0). Filled Hebrew for all **200** `trope.*` keys — **41 flagged `REVIEW:` in notes**: the `trope.glyphname.*.ashk`/`.seph` pronunciation-name spellings (e.g. Etnachta→אתנחתא, Kadma→קדמא). These are **JS-driven / unreferenced at runtime** — the running UI reads the `ashk`/`seph` fields straight from the `TROPES` taxonomy table (`tropeName()`), so the taught pronunciation names render as **content**, exactly per the resolved "names are the teaching payload" decision; the CSV translations sit in the dict for the fluent pass / future use. `sw.js` **v285→v286** (bumped in the infra commit). | Verified: headless Playwright over local HTTP — **18/18 HE assertions PASS** (dir=rtl/lang=he, Hebrew chrome + drawer + related-tools nav, drawer opens on the LEFT edge, 70 `lang="he"` content nodes pinned, Hebrew drill prompt, live switch retranslates, 0 pageerrors) + a follow-up **10/10 family-chip pass** (family chips + mastery labels now Hebrew in HE / English in EN, 0 pageerrors). An HE screenshot confirmed the drawer-flip + Hebrew chrome and caught the family-chip leak. | done: 2026-07-12
- [x] 2026-07-12 | i18n rollout (page 4/11) | torah_trainer.html | **DONE: Torah Trainer UI chrome converted to full runtime i18n + Hebrew RTL — the Hebrew reading stays content-locked RTL and untranslated.** Fourth page of the full-site rollout. Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher + `.dir-arrow`/`.bidi-isolate` utils (CSP already `'self'`+sefaria/esm/jsdelivr — no edit). **Static chrome** (~130 `data-i18n*`): every control, the whole settings drawer (Display/Schedule/Font/Vowel-color/Trope-color/Translit/Audio/Reset/About panels), audio bar, custom-range picker, tour card, fs-exit — including labels-with-nested-tooltips (text→child `<span data-i18n>` + `data-i18n-tip`), ambiguous labels mapped by `for=`/`onclick` context, the related-tools nav via shared `shared.nav.*`, and FAQ/how-to/speeds-intro/translation-footer whose `<a>`/`<strong>`/`<em>` markup was restored into the CSV (marked `html`). **Dynamic → `I18n.t()`**: tour (`titleKey`/`bodyKey`+`shared.tour.*`), hint toast, all reading buttons (Read/Chant/Loop, Read-all/Chant-all, Stop), empty state, resume chip, every `setStatus`/`setKaraokeStatus` message, translit-lib status, version footer, the scope-dropdown labels (`refreshScopeLabels`), and the render-time row labels (Hebrew/Translit/English) + aliyah markers. Trope-legend family chips rebuilt from the `torah.glyphname.trope_*` keys via a new **`buildTropeLegend()`**. **`applyI18n()`** (registered on DOMContentLoaded, after the deferred i18n.js): `applyStaticI18n` + rebuild legend/scope-labels/translation-footer, and re-render the reading — **but NEVER while audio is playing/looping** so karaoke `data-twi` word-timing can't desync. **RTL**: the settings drawer flips to the **left** edge in Hebrew (logical `inset-inline-end`/`border-inline-start` + an explicit `[dir=rtl] .settings-modal.open` rule to win back `translateX(0)` at the equal specificity the attr+class tie created); the reading's `direction:rtl` rules are deliberately left physical (reading stays RTL in any UI). Added 9 keys (skip-link, restore-default, looping-label, 3 row labels, 2 aliyah markers, reading-load error); filled Hebrew for all **262** `torah.*` keys (2 flagged `REVIEW:` in notes — `glyphname.trope_revia`/`trope_segol` clause-name spellings). `sw.js` **v284→v285**. | Verified: headless Playwright — **14/14 HE assertions PASS** (dir=rtl/lang=he @1280/800, Hebrew chrome incl. the drawer + related-tools nav, settings drawer opens on the LEFT edge in HE, live switch preserves scope selection + retranslates, 0 pageerrors); an HE screenshot pass caught + fixed 8 initially-missed strings (labels-with-tooltips, ambiguous "Style", scope map, drawer title, About&FAQ). Two footer logo/link credits (Sefaria/PocketTorah, external branding) intentionally left English. | done: 2026-07-12
- [x] 2026-07-12 | i18n rollout (page 3/11) | hebrew_blend_generator.html | **DONE: Worksheet Generator UI chrome converted to full runtime i18n + Hebrew RTL — printed output deliberately UNTOUCHED.** Third page of the full-site rollout (after the Flash Cards pilot + Dictionary). Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher; annotated all sidebar/footer/tour/modal chrome with `data-i18n*`; routed every dynamic string through `I18n.t()` (preset dialogs, folder-tree block now byte-identical to the reference, tour via `titleKey`/`bodyKey`, quick-starts, combos count, lock warnings, preview notices, share/truncate notices, validation alerts, empty-state guidance, self-check UI, real-words pool/import counts, translit status, dark-toggle label, and the shared `.ivrit` engine + font-uploader blocks — both re-verified byte-identical to flash_cards). **THE BOUNDARY HELD:** `renderWorksheet` + every mode builder + answer key + QR (`akPayload`/`appendQrToFooter`/`renderAnswerKeyView`) + tracing + the printed `wsMetaHTML` Name/Date/Class header were left as-is — a headless diff of **all 8 printed modes** (reading/inverse/fill-vowels/bingo/gematria/matching/tracing/name-the-letter) against `HEAD` in EN UI is **byte-identical**, and `headerLang` (printed header language) is confirmed decoupled from `I18n.lang`. **Init robustness:** the page inits at top-level *before* the deferred i18n.js, so top-level `I18n.t` sites (`updateCombosCount`, `initFontSelector`) are `window.I18n`-guarded and `renderPresets` is skipped until ready — `applyI18n()` (registered on `I18n.ready`, which always resolves) re-renders on load + live switch, preserving letter/vowel selections. **RTL:** logical props + `[dir=rtl]` overrides for toggle knobs/scaled toggles, sidebar edge-tabs (JS reposition + CSS mirror), flip-score badge; fixed the home-link doubled-arrow; **header now wraps on narrow screens** (360px overflow 97px→24px, was pre-existing). Content stays `dir=rtl lang=he` regardless of UI language (letter/vowel tiles pinned). **Two annotation gaps fixed:** the dark-toggle label was JS-set English (`🌙 Dark`/`☀️ Light`) → now `shared.darkmode.label_*`; and `shared.fullscreen.label`/`exit_label` were referenced but never created → added. Added **13 new keys** (11 `worksheet.*` empty-state/self-check/action + 2 `shared.fullscreen.*`); filled Hebrew for all **502** `worksheet.*` keys (`layout.title_default` intentionally left English — it's the printed default title; **9 flagged `REVIEW:` in notes** — `modes.tracing.label`/`type.tracing` (מעקב term) + the 7 `vowels.sound.*` phonetic labels kept Latin, both unreferenced picker-side). Glyph/vowel picker labels kept English to **match the reference** (flash_cards leaves them English; its `glyphname.*` keys are likewise unreferenced). `sw.js` **v283→v284**. | Verified: headless Playwright over local HTTP — **24/24 HE assertions PASS** (dir=rtl/lang=he @1280/800/360, Hebrew chrome, content pinned lang=he, no h-overflow @≥700, live switch preserves selection + retranslates + 0 errors, dark OK) + **boundary diff: 8/8 modes byte-identical to HEAD in EN, 0 pageerrors**. | done: 2026-07-12
- [x] 2026-07-11 | i18n rollout (page 2/11) | hebrew_dictionary.html | **DONE: Hebrew Dictionary converted to full runtime i18n + Hebrew RTL** (second page after the Flash Cards pilot; full-site rollout per the localization plan). Wired the deferred `/js/i18n.js` + no-flash language IIFE + header EN/עברית switcher; annotated all static chrome with `data-i18n*`; routed every dynamic string through `I18n.t()` (tour, result counts, filter chips, empty state, Anki/Quizlet export, Word Lists save/manager modals, share link, `announce()` a11y sink, word/emoji detail modals, shoresh family/groups/patterns, loading/translit status, mobile filters). **Content pinning:** `colorizeHebrew()` sink now wraps `dir="rtl" lang="he"`; letter/vowel/shoresh glyph tiles + word-list Hebrew pinned. **RTL:** physical→logical CSS across sidebar/handles/chips/cards; `.dir-arrow`/`.bidi-isolate` utilities; mirrored home + sidebar chevrons. **Glyph names → Hebrew** (per the resolved decision) via `glyphLetterName`/`glyphVowelName` over the `dictionary.glyphname.*` keys. **Falsy-zero safe** counts (no `||` collapse, `.one`/`.other` plurals). Added a canonical `esc()` + 6 new keys (`filters.vowel_main`, 4 `wordlist.err_*`, `shared.nav.link_flashcards`); filled Hebrew for all 354 `dictionary.*` keys (4 flagged `REVIEW:` for the fluent pass). `sw.js` **v282→v283**. **applyI18n()** re-render hook registered inside DOMContentLoaded (so the deferred i18n.js has loaded) — a live language switch repaints in place with filters/search/selection preserved. | Verified: headless Playwright over local HTTP (light+dark, 1280/800/360, EN+HE) — 28/28 assertions PASS: live switch preserves search state, content stays `dir=rtl` under both UIs, glyph names render Hebrew, no h-scroll @360/800, dir/lang persist across reload, 0 pageerrors, no CSP violations. | done: 2026-07-11
- [x] 2026-07-11 | (S71 close-out) | branch/deploy note | S71 continued on the designated branch `claude/new-session-4scgct`, adding to the already-open **draft PR #86** (S66–S70; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v279→v280** (precached files changed: flash_cards, hebrew_blend_generator, classroom_dashboard) — first SW bump since S68. No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **F** (cross-tool consistency, 5th run, last S62) — converged the empty-state "No word lists yet" link affordance. Iters: 1 pass (F) + 1 cross-tool fix = 2. _(Reached via the user's "Rerun" after S70.)_
- [x] 2026-07-11 | 2e61fa3 | flash_cards + hebrew_blend_generator + classroom_dashboard | **DONE: empty-state "No word lists yet" link a11y converged on `--gold-text` (S71 Pass F — clears the deferred S62 empty-state debt).** flash_cards' `#wordsEmptyState` inlined `<a style="color:var(--gold)">` — **2.6:1 on light** (WCAG 1.4.3 fail), no dark variant, no underline — while its **byte-identical** generator twin `#rwListEmptyState` had already fixed exactly this. Converged **both** onto the shared **`--gold-text`** token (the S63 suite-wide readable-gold standard): flash_cards gains `#wordsEmptyState a { color:var(--gold-text); text-decoration:underline }` + drops the inline gold (**2.6→6.22:1 light / 7.82:1 dark**); the generator's `#rwListEmptyState a` **token-ized** (`#7f5a13`+`body.dark` override → one `var(--gold-text)` rule; no visual change, 5.88/7.96:1); the **dead `.preset-empty` CSS** in the dashboard (presets long since moved to the folder tree; 1/1 file-wide occurrence, zero references) **deleted**. **Survey (3 agents-worth, read-only) confirmed no other laggard** — dictionary `renderDictEmptyState`/`.empty-btn`, torah `renderEmptyReading`/`.tt-empty`, and FM first-run affordances are already class-based/branded; trope has only a `:empty` auto-hide. So the S62/S68 empty-state consistency candidate is now **cleared**. +5/−9. | Verified: headless WCAG harness (light/dark × 1280/800) — both empty-state links measure ≥4.5:1 (flash **6.22/7.82**, gen **5.88/7.96**) with an underline non-color cue; `.preset-empty` gone from the dashboard CSSOM; 0 pageerrors. | done: 2026-07-11, S71
- [x] 2026-07-11 | (S70 close-out) | branch/deploy note | S70 continued on the designated branch `claude/new-session-4scgct`, adding to the already-open **draft PR #86** (S66–S69; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). **No `sw.js` VERSION bump** — the only changed files (README.md, CLAUDE.md) are **not precached** (like S48/S58). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **E** (freshness & site-health, 6th run, last S58) — mechanical **CLEAN**, only P4 doc-drift fixed. Iters: 1 pass (E) + 1 doc-freshness batch. Docs-only. _(Reached via the user's "Rerun" after S69.)_
- [x] 2026-07-11 | 5bac85c | README.md + CLAUDE.md | **Pass E (freshness & site-health, 6th run) — mechanical site-health CLEAN; 2 P4 doc-drift fixed.** Mechanical checks over the S59–S69 surface all **CLEAN**: `sw.js` CORE_ASSETS **both directions** (22 entries all exist on disk @ v279; every root `*.html` precached; only the documented og-card/zelle-qr/18-splash omissions); sitemap (11 `<loc>` all map to files, every `<lastmod>` == the file's `git log` commit date → `node scripts/update-sitemap.mjs` reported **"already current — no changes"**); THIRD_PARTY_LICENSES (no new dep since S58); robots/CNAME(=ivritsuite.com)/manifest coherent; **shared app-toast JS block re-verified byte-identical across the 3 tool pages** (sha `9944abcb3943`), `.ivrit`-engine claim accurate post-S59. **Doc-drift fixed (5bac85c):** README FM **v4.10→v4.17** (trailed `FONT_MAKER_VERSION` by the S62 4.14→4.15 + outside-loop 4.15→4.17), CLAUDE.md FM line-count **~12,550→~12,650** (actual 12,634 after the mark-editor relocation). No feature-list gaps — S59–S69 were refactors + a bug fix, no new user-facing feature. | Verified via the mechanical re-greps + the idempotent sitemap run + the shared-block sha. | run: 2026-07-11, S70
- [x] 2026-07-11 | (S69 close-out) | branch/deploy note | S69 continued on the designated branch `claude/new-session-4scgct`, adding to the already-open **draft PR #86** (S66–S68; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). **No `sw.js` VERSION bump** — CLEAN audit, no code changed (docs-only ledger, not precached). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **B** (console & error audit, 5th run, last S57) — **CLEAN**. Iters: 1 pass (B) + 0 fixes (discovery-only, like S37/S47/S57). _(Reached via the user's "Rerun" after S68.)_
- [x] 2026-07-11 | (S69 Pass B — no code) | all 12 root pages (headless console/error audit) | **Pass B (console & error audit, 5th run — first over the S58–S68 surface) — CLEAN: 24/24 page-runs, 0 pageerrors, 0 non-environmental console errors, 0 local request failures.** All 12 root pages × light-1280 + dark-800 over local HTTP; captured `console` error/warning + `pageerror` + `requestfailed` on load AND one representative interaction per tool — **interactions verified to actually fire**: dictionary → **shoresh mode active + 14-word family rendered** (exercises the S68 shoresh-select change), generator → worksheet built, flash_cards → deck started, FM → letter alef selected (relocated mark grid), trope → drill started; plus index gear, dashboard drawer, torah parsha-select. **Noise histogram = 100% route-aborted externals** — every page's console-error count equals its external `requestfailed` count (Google Fonts / gtag / Sefaria / PocketTorah MP3s / ESM CDN); torah's +2 are the documented offline "load data files"/Sefaria messages. **No regression** from the S58–S68 changes (shoresh-select, FM mark-editor relocation, trope SEO, S59 toast extraction, S60 torah decouple, S61 generator chunked build). Matches the 4 prior clean B runs. | Verified via the 24-run audit + a 5-page interaction-effect confirmation. | run: 2026-07-11, S69
- [x] 2026-07-11 | (S68 close-out) | branch/deploy note | S68 continued on the designated branch `claude/new-session-4scgct`, adding to the already-open **draft PR #86** (S66–S67; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v278→v279** (precached file changed: `hebrew_dictionary.html`) — the **first** S66-series session to ship tool code, so unlike the docs-only S66/S67 it **does** bump SW. No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **H** (teacher walkthrough, last S56) — **first-ever H on `hebrew_dictionary.html`**. Iters: 1 pass (H) + 1 fix = 2. _(Reached via the user's "Rerun" after S67; tool chosen for variety — steered off torah_trainer/S66 + Font Maker/warmest.)_
- [x] 2026-07-11 | 0de2408 | hebrew_dictionary.html | **DONE: shoresh-mode "Select all matching" saved the WRONG words (S68 Pass H — silently-wrong output).** `onFilter()` early-returns for shoresh mode (L2119) without recomputing `filtered`, but `selectAllMatching()` (L1312) populated `bulkSelected` from that **stale `filtered`** (the last Words-mode result set) — so a teacher viewing a root family and clicking "Select all matching" got unrelated words into their selection while the *visible* shoresh checkboxes were checked (the DOM showed the family; the saved/exported list held different words). **Repro:** root אבד family = 14 words, but selectAllMatching selected **63 stale "hand" search results, 0 in the family.** Fixed by stashing the rendered family in a new `shoreshFam` module var (set in `renderShoreshFamily`) + a `shoreshMode` branch in `selectAllMatching` that selects it, using the same `{w, text: wordBulkText(w)}` shape as the per-card path so save/export/Anki/Quizlet/Word-List all consume it unchanged. +7/−1. | Verified: headless (light/dark × 1280/800) — shoresh selectAllMatching now selects **14/14 family, 0 stale**; **regression clean** (Words mode 63→63, Emoji mode 52→52 unchanged); 0 pageerrors. | done: 2026-07-11, S68
- [x] 2026-07-11 | (S68 Pass H — no code beyond 0de2408) | hebrew_dictionary.html (teacher walkthrough) | **Pass H (teacher walkthrough) — first-ever on the dictionary.** Role-played a teacher building a lesson's vocabulary end-to-end (fresh profile, headless over HTTP, light-1280 + dark-800; "find themed words → save a Word List → carry into Flash Cards/Generator"). **Works:** one-box search (Hebrew/translit/English), the Word Lists create→name→save modal (name up front, save-new or add-to-existing), lists persist to the shared `ivritSuite_wordLists`, exports (Anki/Quizlet incl. the S65 emoji fix). **1 correctness bug found + fixed** (shoresh selectAllMatching, 0de2408 — the one cheap win). **Friction logged** — 3 P3 candidates (Word Lists buried 2 collapse levels + labeled "Bulk Copy Mode"; stale tour copy claiming a nonexistent Worksheet-Generator export + never mentioning Word Lists; save-success dead-end + "Load as filter" silently discarded on filter change) + 1 P4 (dup names / raw prompt-confirm). **4 Feature seeds** (H = the seed intake): cross-tool "Use in Flash Cards →" deep-link (**the biggest gap** — the dictionary never points onward though Flash Cards points back), per-card "Add to Word List", word theme/category filter, reorder-in-list / surface-at-top-level. **Struck-seeds rule honored** (none are the struck flash_cards ideas). 0 pageerrors throughout. | Verified via the empirical walkthrough + the fix before/after + a Words/Emoji regression pass. | run: 2026-07-11, S68
- [x] 2026-07-11 | (S67 close-out) | branch/deploy note | S67 continued on the designated branch `claude/new-session-4scgct`, adding to the already-open **draft PR #86** (S66; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). **No `sw.js` VERSION bump** — docs-only (ledger `docs/IMPROVEMENT_LOG.md` + `IMPROVEMENT_ARCHIVE.md`, not precached), like S45/S33/S58. No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **I** (first-load & empty-state, 4th run, last S55) — **CLEAN**. Iters: 1 pass (I) + 0 fixes (no P1, nothing found; discovery-only, like S33/S45/S55). _(This session was reached via the user's "Rerun" after S66; a read-only pre-audit agent found no unguarded first-load crash surface, and the empirical sweep confirmed it.)_
- [x] 2026-07-11 | (S67 Pass I — no code) | all 12 root pages (read-only fresh-profile sweep) | **Pass I (first-load & empty-state, 4th run — first over the S56–S66 surface incl. the outside-loop FM mark-editor relocation + trope SEO polish) — CLEAN.** Headless Playwright over local HTTP, each page in a **fresh browser context (empty localStorage + empty IndexedDB = brand-new user)**. **Sweep: 24/24 page-runs clean** (12 pages × light/dark) — **0 pageerrors**, OS-aware dark honored on every page (light→no `body.dark`, dark→`body.dark`), `lang="he"` present on all 7 Hebrew pages (generator 187 / dictionary 100 / trope 68 / FM 57 / flash_cards 44 / dashboard 14 / torah 6), first-run affordances fire (FM `#welcomeOverlay`, dashboard `#frStarters` starter card, tour pulses on the 6 pulse-tools), **no h-scroll @1280 or @800**. **3 depth-probes CLEAN:** (1) **FM mark-editor relocation** — on a truly empty project the changed nikkud/trop tile→Edit-shape path (`openMarkEdit(cat,cp,{global:true})`) opens `#markEditOverlay` (`.open`, `display:flex`, visible), `#meApplyCur` hidden, `meGlobal=true`, `meItem` seeded with default pieces from static tables (no null-deref — `newProject()` runs unconditionally at init so `project` is never null), Apply-to-all creates the custom store entry (0→1), trop path runs after enabling — 0 pageerrors; (2) **trope SEO** — 4 JSON-LD blocks (WebApplication/BreadcrumbList/FAQPage/**new HowTo**), 0 parse errors, JSON-LD↔DOM **parity all pass** (family chips ×13, tradition toggle, mastery grid, tour), head-only so no first-load render impact; (3) **confirmatory** — torah color-code-nikkud default renders (`--nikv-a=#0099bb`), generator presets empty-state (0), 0 errors. **Static:** S60/S61/S65/FM commits introduced **zero** new `localStorage.getItem` reads; every read in the touched files is fallback-guarded. Nothing to fix. | Verified via the 24-run sweep + 3 depth-probe scripts (incl. a direct `openMarkEdit` reachability test after the tile-grid selector proved DOM-gated). | run: 2026-07-11, S67
- [x] 2026-07-11 | (S66 close-out) | branch/deploy note | S66 = fresh **Pass D** work on the **new** designated branch `claude/new-session-4scgct`, which sat **exactly on `origin/main`** (`f6b2f65`, 0 commits ahead) at session start — **draft PR #82 (S62–S65) was merged**, and **4 outside-loop commits landed after** (Font Maker nikkud/trop mark-editor into the left column + tiles open the Edit-shape modal; trope-tutor SEO polish; sitemap refresh), bumping `sw.js` **v275→v278** and `FONT_MAKER_VERSION` **4.15→4.17** (reconciled here, same as the S41/S50/S58/S62 outside-loop notes). No open PR at start → S66 opens a **new draft PR**. **No `sw.js` VERSION bump** — the only changed files are the ledger (`docs/IMPROVEMENT_LOG.md` + `IMPROVEMENT_ARCHIVE.md`), which are **not precached** (like S45/S48/S58). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **D** (performance, last S54) — a **2nd-pass D on `torah_trainer`**, **effectively CLEAN**. Iters: 1 pass (D) + 0 fixes (no P1/P2, no cheap isolated win — the one-shot render block is large-refactor-only → logged P4). Discovery-only close-out (like S33/S45/S35-D).
- [x] 2026-07-11 | (S66 Pass D — no code) | torah_trainer.html (read-only profiling) | **Pass D (performance) — 2nd pass on `torah_trainer`; empirically CONFIRMED the S60 nikkud CSS-var decouple's no-rebuild win HELD.** Headless Playwright over local HTTP, injected a worst-case fixture (176 verses → **2,317 `.tt-word` / 4,636 `.nik` spans**, nikkud color + trope + translit + translation all on), across light/dark × 1280/800 × 1×/4× throttle, with a `MutationObserver` on `#ttReading` + a `longtask` `PerformanceObserver`. **S60 verification (the crux):** `toggleDark`, color-mode radios, `setVowelScheme`, and both color pickers = **0 reading mutations** and **0.1–2.7 ms JS** every scenario, vs. `renderText`/layout = **354–356 reading mutations / 26–194 ms** — definitive proof the color/mode/scheme/dark paths are pure `<body>` var+class swaps with **no reading rebuild**. **Idle CLEAN** (0 mutations / 3 s; no `setInterval`, rAF one-shot only). **No pageerrors, no h-scroll @800px.** Karaoke `onAudioTimeUpdate` re-confirmed by static analysis as binary-search + `idx`-unchanged early-out + O(1) DOM per word change (live audio env-blocked — remote PocketTorah MP3s route-aborted, as documented). **Two costs logged, no fix:** (1) worst-case full-parsha `renderText` is a one-shot content-change block (~128–194 ms @4×) — large-refactor-only → **new P4 candidate**; (2) changing body color vars restyles all 4,636 `.nik` spans (~0.1–0.5 s @1× / ~0.8–1.6 s @4× browser style-recalc longtask) — inherent to the CSS-var model, `saveSettings` is debounced, and still strictly cheaper than a rebuild → informational, no action. | Verified via the 4-scenario harness (airtight after fixing an async-`MutationObserver` read-timing bug in the harness). | run: 2026-07-11, S66
- [x] 2026-07-10 | (S65 close-out) | branch/deploy note | S65 continued on the designated branch `claude/ivrit-improvement-loop-x8vpn4`, adding to the already-open **draft PR #82** (S62–S64; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v274→v275** (precached file changed: hebrew_dictionary.html). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **G** (print & export fidelity — **first-ever on the dictionary's export surface**). Iters: 1 pass (G) + 1 fix = 2 (under budget — no P1/P2 remain).
- [x] 2026-07-10 | 6082a7e | hebrew_dictionary.html | **DONE: emoji selections now export to Anki/Quizlet/Word-List (S65 Pass G — first-ever G on the dictionary).** `exportSelectedWords()` filtered on `.w`, **silently dropping emoji-mode selections** (stored `{e,text}`, no `.w`) from all 3 export paths — yet `updateExportState` enables the export box + shows an "N words selected" count in emoji mode, so selecting emoji cards then hitting ⬇ Export for Anki/Quizlet or Save as Word List dead-ended with an empty result + the **misleading** inline error "Select words using Bulk Copy Mode first." Emoji cards carry the fields a word record needs (`hebrew_masculine`/`_feminine` with nikkud, `translit_*`, `english`), so `exportSelectedWords` now expands each selected emoji's visible Hebrew form(s) (masc/fem per the gender filter, via the existing `emojiVisibleForms`) into word-shaped records — flowing through the Anki/Quizlet/Word-List builders unchanged (`translitDisplayOf` + `wlWordFromDict` already consume the shape). One function; all 3 paths fixed. +13/−1. | Verified: headless (light + dark) 19 checks/run — Anki `.txt` (tab-sep, `#separator:tab` header) + Quizlet `.csv` (`Term,Definition`, comma-quoted, Hebrew in col 1) now carry a word row AND the emoji's Hebrew form with **nikkud round-tripping byte-for-byte**; `wlSelectedWords` includes the emoji entry + correct translit; **emoji-only selection now non-empty + no error (dead-end gone)**; word-only output unchanged (regression); `emojiGender='masc'` → exactly 1 row; empty selection still guards; 0 pageerrors. | done: 2026-07-10, S65
- [x] 2026-07-10 | (S65 Pass G — no code beyond 6082a7e) | hebrew_dictionary.html (read-only survey) | **Pass G (print & export fidelity) — first-ever on the dictionary's export surface.** 2 read-only surveys confirmed the dictionary is the only remaining tool with genuine un-audited human-facing export artifacts (flash_cards/dashboard/trope are screen/JSON-only, no printable deck). **Word-export builders audited CLEAN:** Bulk-Copy (`\n`-joined, `computeCopyText`), single-card copy, **Anki `.txt`** (TSV, `#html:false` so nikkud isn't escaped), **Quizlet `.csv`** (`csvEscape` quotes `" , \n`), Word Lists (denormalized store) — all preserve `w.word` nikkud + logical order + round-trip. **1 finding fixed** (Gap A, emoji export drop — 6082a7e). **Deferred:** Gap B (P4) — no `@media print`, so dark-mode Ctrl+P ink-bleeds; but the dictionary is a browse/export tool (infinite-scroll, routes printing through Anki/Quizlet/Worksheet-Generator), not a paper deliverable → logged, not fixed. | Verified via the 2 surveys + the empirical export harness (19 checks). | run: 2026-07-10, S65
- [x] 2026-07-10 | (S64 close-out) | branch/deploy note | S64 continued on the designated branch `claude/ivrit-improvement-loop-x8vpn4`, adding to the already-open **draft PR #82** (S62–S63; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v273→v274** (precached file changed: Hebrew_Font_Maker.html). **No `FONT_MAKER_VERSION` bump** — the fix is an unreachable-today internal hardening with zero user-facing change (nothing to changelog). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **A** (recurring-pattern sweep, over the S52→HEAD surface). Iters: 1 pass (A) + 1 fix = 2 (under budget — no P1/P2 remain).
- [x] 2026-07-10 | b9c1aa3 | Hebrew_Font_Maker.html | **DONE: UFO version fallback falsy-zero guard (S64 Pass A — the one sweep finding).** `buildUfoFontInfo` (L10899) used `spec.version || 1.0` — the **exact falsy-zero anti-pattern the S52 fix (d343b1b) removed elsewhere**; its two siblings (L10699 TTF path, L11347 version-bump) use a `Number.isFinite(...) ? ... : 1.0` guard with explicit "`||`-guard would promote a legal 0.x version to 1.x" comments. Normalized this site to match. **Unreachable with 0 today** (UFO export always bumps the version to ≥0.001 at L11347 before `buildUfoSpec`), so **no user-facing change** — internal consistency hardening that closes the latent gap the S52 sweep missed, one screen from its own guards. `spec.lineGap || 0` (L10902) left (legal `||0`, the `advanceWidth||0` class). +1/−1. | Verified: headless direct guard test (light/dark × 1280/800) — `buildUfoFontInfo(version:0)` → `versionMajor/Minor` **0/0** (was **1/0** pre-fix), `version:0.5` → **0/500**, `version:undefined` → **1/0** (fallback intact), 0 pageerrors; grep confirms no other numeric `.version \|\|` falsy-zero remains. | done: 2026-07-10, S64
- [x] 2026-07-10 | (S64 Pass A — no code beyond b9c1aa3) | all tool pages (read-only sweep) | **Pass A (recurring-pattern sweep, over the 26-commit S52→HEAD surface) — 6 of 7 active classes CLEAN; falsy-zero found 1 latent site (fixed b9c1aa3).** Swept via 3 agents across the generator chunked build, torah nikkud/trope CSS-var decouple, `showAppToast` shared block, `--gold-text` pill, and the never-loop-swept **FM v4.10–v4.15 outside-loop Firefox auto-detect-box debug code**. **CLEAN:** unescaped-input/unsafe-parse (all new sinks `esc()`/`textContent`; nikkud/trope now validated CSSOM vars; 0 new `JSON.parse`/`Object.assign`), localStorage-vs-AllTools (0 new keys), listener/interval (chunked-build timer single/self-chaining + `cancelWorksheetBuild` clears on re-entry; `beforeprint` once; color fns add no listeners), destructive-bulk (FM `adApply` is `udDo`-backed + avoids traced letters), JSON-LD parity (no `ld+json` changed), shadowed-global (every helper single top-level decl). **FM Firefox-box deep-dive CLEAN:** `#addebug` is a URL-hash gate (dynamically-built `#adDbg`, never visible in normal use); coords/labels/undo all safe; the `.ad-box display:inline !important` is the intentional Firefox fix. **Informational (not a finding):** obsolete debug scaffolding (`#adDbg` probes + `.ad-box` `!important`/focus rules) is inert cruft → logged P4; pre-existing FM `<tspan>`/`<image>` interpolations are self-sourced single-char/blob-URL (unchanged, safe). | Verified via the 3 cross-class sweeps + reachability traces. | run: 2026-07-10, S64
- [x] 2026-07-10 | (S63 close-out) | branch/deploy note | S63 continued on the designated branch `claude/ivrit-improvement-loop-x8vpn4`, adding to the already-open **draft PR #82** (S62; open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v272→v273** (precached files changed: hebrew_blend_generator, flash_cards, classroom_dashboard, hebrew_dictionary, torah_trainer, trope_tutor). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **C** (accessibility, 4th-ish — focused on the font-upload affordance across carriers). Iters: 1 pass (C) + 1 cross-tool fix = 2 (under budget — no P1/P2 remain).
- [x] 2026-07-10 | 5797ca0 | generator + flash_cards + dashboard + dictionary + torah + trope | **DONE: upload-pill gold-on-light contrast (the top S62 P3) — WCAG 1.4.3 fix + trope structural convergence.** The "⬆ Upload your own font?" pill used `color:var(--gold)` (`#c9922a` / flash `#c9a24a`) on light cream/paper — measured **~2.5–2.9:1**, failing 4.5:1. Adopted the theme-aware **`--gold-text`** token already proven in trope (`#7f5a13`, S53) + dictionary (`#87681d`, S40): light = readable dark gold, dark = `var(--gold)` (already-passing bright gold untouched). Added `--gold-text` (light `#7f5a13` / dark `var(--gold)`) to the 4 carriers lacking it (generator/flash/dashboard/torah); swapped all **6 inline pills** to `color:var(--gold-text)` (dictionary kept its `#87681d`; only the pill swap needed); **converged trope's divergent `.font-upload-label`** (centered plain link, no border/tooltip) onto the shared dashed-chip markup + `title`, keeping its hardened color, and removed the now-unused CSS rule — so all **7 pills are byte-identical inline chips** referencing `var(--gold-text)`. Pills sit **outside** the `═══` guarded uploader block (per-page markup). +16/−17. | Verified: headless WCAG harness, **24 configs** (6 pages × light/dark × 1280/800) — every pill measured **light 4.93–6.22:1 (was ~2.5–2.9) / dark 7.8–8.7:1**, each pill's computed color == resolved `--gold-text`, dashed border + `title` present, `onUpload(Eng)?FontPick` wiring intact, trope `.font-upload-label` fully gone, 0 pageerrors. | done: 2026-07-10, S63
- [x] 2026-07-10 | (S63 Pass C — no code beyond 5797ca0) | 7 font-upload carriers (read-only survey) | **Pass C (accessibility) — focused audit of the font-upload affordance across all carriers.** Surveyed the "⬆ Upload your own font?" pill markup + gold tokens across generator/flash_cards/dashboard/dictionary/torah/trope + index: found the shared inline pill's `var(--gold)` text failing WCAG 1.4.3 on light (~2.5–2.9:1) in 6 pills, and trope's `.font-upload-label` structurally divergent (already color-hardened via S53). `index.html`'s My-Fonts **manager** label uses `color:var(--text)` (dark navy — passes; gold only on ephemeral hover) → **out of scope**, left as-is (logged). Fixed the contrast + trope structure together in one cross-tool commit (5797ca0). | Verified via the cross-carrier survey + the WCAG contrast harness. | run: 2026-07-10, S63
- [x] 2026-07-10 | (S62 close-out) | branch/deploy note | S62 = fresh work on the **new** designated branch `claude/ivrit-improvement-loop-x8vpn4`. **Outside-loop reconciliation:** the branch carried **7 post-S61 Font Maker commits** (Firefox auto-detect-box debugging + one `sw.js` no-store tweak) that landed after the S61 close-out, bumping `sw.js` **v264→v271** and `FONT_MAKER_VERSION` **4.10→4.14** (only `Hebrew_Font_Maker.html` + `sw.js` changed) — reconciled here, same as the S41/S50/S58 outside-loop notes; the FM About changelog skips **v4.13** (undocumented internal-diagnostic bump — informational, not fixed). **This session:** `sw.js` VERSION **v271→v272** (precached file changed: Hebrew_Font_Maker.html). `FONT_MAKER_VERSION` **4.14→4.15** + one About-tab changelog entry. **`origin/main` remote-tracking ref was stale in the fresh clone** (showed the S40 close-out, `v238`, no merge-base) — a `git fetch origin main` corrected it: real `origin/main` = `e71b1fb` (v271), the exact base of this branch, so S62 is a **clean 2-commits-ahead** of main (no divergence, no rewrite). Deploy runs on merge — verify `success` after merge. No open PR at start → S62 opens a **new draft PR**. **Pass rotation:** ran the strictly-stalest **F** (cross-tool consistency, 4th run). Iters: 1 pass (F) + 1 fix = 2 (under budget — no P1/P2 remain; the rest of the backlog is P4/maintainer-call/Pass-C only).
- [x] 2026-07-10 | d818816 | Hebrew_Font_Maker.html | **DONE: Font Maker error-surface convergence (S62 Pass F) — the last 3 blocking `alert()`s → `askModal()`.** The custom-glyph editor's three validation guards (invalid code point, duplicate custom glyph, mark-with-no-outline convert) were the **only native `alert()`s left in the file** and the lone deviation from its own `askModal()` pattern (used at ~20 other error/confirm sites). Converged all three onto `askModal(title, msg, [{label:'OK'}])` (CLAUDE.md §5 — migrate `alert()`-driven validation opportunistically), so the whole tool surfaces errors in one styled non-blocking in-app dialog. The duplicate-glyph message routes `cp` through `esc()` (it now flows into `askBody.innerHTML`; matches the L5149 precedent). The two adjacent destructive-action `confirm()` guards left in place. `FONT_MAKER_VERSION` 4.14→4.15 + plain-language changelog. +6/−5. | Verified: headless 44 checks (11 × light/dark × 1280/800) — 0 pageerrors, **0 native dialogs fired** (a `page.on('dialog')` counter), each guard opens `#askOverlay` with the correct title, the duplicate-glyph guard echoes `U+E000` and did NOT add the glyph, `esc()` neutralizes injected markup. | done: 2026-07-10, S62
- [x] 2026-07-10 | (S62 Pass F — no code beyond d818816) | all tools (read-only survey) | **Pass F (cross-tool consistency, 4th run) — surveyed the three ledger-named candidate affordances (error surfaces, font-picker parity, empty states) across all tools via 3 read-only agents, then converged the cleanest.** **Chosen: error surfaces** — Font Maker's 3 glyph-editor `alert()`s → `askModal()` (its own dominant pattern; d818816). **Deferred with findings logged:** (a) **font-picker parity** — trope_tutor's `.font-upload-label` drifted from the shared upload-pill markup, BUT trope's label is deliberately a11y-hardened (S53 `--gold-text` #7f5a13, 6.2:1) while the shared pill uses `var(--gold)` #c9922a (~2.6:1 on light `--cream`) — a byte-identical convergence would **regress** contrast and exposes a latent suite-wide gold-on-light issue in ~5 pills → logged as a linked **Pass C** candidate (fix all pills' contrast + trope's structure together, not a half-convergence); (b) **empty states** — best pattern is the generator's branded `.empty-state`+`#emptyQuickStarts` / dictionary's in-card `.empty-btn` action buttons; laggards (flash_cards deck-start, FM no-traced-letters) need new cards (feature-ish) + cheap consistency debt (dead `.preset-empty` dashboard CSS, flash_cards `#wordsEmptyState` inline→class) → logged for a future Pass F on empty states. | Verified via the 3 cross-tool surveys + direct grounding of the chosen fix. | run: 2026-07-10, S62
- [x] 2026-07-10 | (S61 close-out) | branch/deploy note | S61 = **user-directed backlog refactor** (no discovery pass, like S43/S49/S59/S60), continuing on the designated branch `claude/session-13-5-auf4mh`, adding to the already-open **draft PR #81** (S58–S60; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v263→v264** (precached file changed: hebrew_blend_generator.html). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **No discovery pass ran → Pass F stays stalest** for S62. Iters: the generator chunked Class-Set build (1 focused refactor) + a **full-session regression sweep** (user-directed): S59 toast suite re-run 12/12, S60 nikkud suites 4/4 + 2/2, shared-block sha identity 3/3 blocks (app-toast JS/CSS + `.ivrit` engine, byte-identical across the trio after the S61 generator edits), 24/24 twelve-page load sweep — **the whole session's surface verified green together**.
- [x] 2026-07-10 | 5a9de31 | hebrew_blend_generator.html + CLAUDE.md | **DONE: generator Class-Set chunked build (the S13 Pass D candidate, re-measured S44 — the LAST parked large refactor).** Class-Set 40 + answer key built ~13,900 nodes in one synchronous **353 ms desktop / 1,305 ms @4×** longtask. The standard-blend path now streams versions above ~600 cells: version 0 synchronously (instant paint), the rest **one version per `setTimeout(0)` tick** (per-tick script ~1 ms measured; one-version-per-tick chosen over a 12 ms time-box because Chrome folds the appended sheets' style/layout into the tick's task — batching 14 versions made 90 ms tasks, 1/tick made **zero longtasks**, ~480 ms wall, page interactive throughout). The synchronous-render contract is preserved at every consumer via **`flushWorksheetBuild()`** — a `beforeprint` listener (both print buttons + Ctrl+P), `exportPDF`'s first statement, and `liveGenerate`'s rollback snapshot finish the build synchronously before reading `#worksheet`. A generation token + a **connectivity sentinel** (`_wsBuildAnchor`, checked by pump AND flush) make pending chunks self-cancel when any of the ~20 direct `ws.innerHTML` writers (Number Practice, Real Words drills, empty states — they bypass `renderWorksheet`) replace the worksheet, so stale sheets can never append onto or print under another mode's output (the adversarial design-review's key finding). Small/single-version worksheets keep the untouched synchronous path; chunked builds stagger only version 0's shuffle animation. Bundled: `liveGenerate` rollback now restores `classSetSheets` alongside `blends` (pre-existing bug — rollback + presentation re-render collapsed a class set to 1 version); dead `isShuffling` removed. +92/−4. | Verified: headless 26 checks × light/dark × 1280/800 — flush ≡ natural completion (byte-identical innerHTML), 80-sheet structure (alternating student/key, 40 cells each, labels + QR per version), `page.pdf()` + raw `beforeprint` + stubbed `exportPDF` mid-build all flush to 80 sheets, re-entrancy (mid-build re-generate → exactly 80, no leftovers), **sentinel** (foreign `ws.innerHTML` write mid-build → 0 stale sheets; beforeprint DROPS the orphaned queue), rollback restores all 20 sheets + D7 presentation re-render keeps 10 versions, sync path unchanged (CS1/CS5 synchronous, animate intact), perf **0 longtasks** (was one 353 ms), 0 pageerrors / 0 unexpected dialogs. | done: 2026-07-10, S61
- [x] 2026-07-10 | (S60 close-out) | branch/deploy note | S60 = **user-directed backlog refactor** (no discovery pass, like S43/S49/S59), continuing on the designated branch `claude/session-13-5-auf4mh`, adding to the already-open **draft PR #81** (S58–S59; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v262→v263** (precached file changed: torah_trainer.html). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **No discovery pass ran → Pass F stays stalest** for S61. Iters: the torah nikkud → CSS-var decoupling (1 focused cross-cutting refactor).
- [x] 2026-07-10 | 28edf54 | torah_trainer.html + CLAUDE.md | **DONE: torah full-reading re-render candidate (S16 Pass D) — nikkud coloring decoupled from `renderText`.** The reading's vowel colors were baked as per-syllable inline `style=` in the one-shot `innerHTML`, so **every** color/mode/scheme/dark change re-ran the whole tokenize+colorize+innerHTML rebuild (the app's biggest one-shot block on large parshiyot). `colorizeHebrew` now emits class-only `<span class="nik nik-<key>">`; a new **`applyNikkudColors()`** pushes the active palette into `--nikv-<key>`/`--nikh-<key>` body vars + a `body.nik-mode-<mode>` class (mirrors the existing trope CSS-var layer). 5 handlers (color-picker input, reset, scheme, mode, dark) now call `applyNikkudColors()` instead of `renderText()` → **var/class swap, zero reading re-render**; only the genuinely text-altering/structural toggles (showNikkud / showCantillation / layout / on-off / trope-on-off) still rebuild. `!important` on the 3 mode rules preserves the exact precedence inline styles had over hover/karaoke; `--nikh` = solid+`"50"` matches the former Highlight alpha byte-for-byte. Splitter + `data-twi` + karaoke untouched. torah_trainer.html +75/−33. | Verified: headless (light/dark × 1280/800) 35 checks/run — class-based spans (no inline style), correct `--nikv`/`--nikh` vars + computed colors across **all 3 modes × 2 schemes × 2 themes**, and the **no-rebuild proof** (`#ttReading.innerHTML` byte-identical after a color change, same `.tt-word` node persists, `data-twi` unchanged/monotonic); + 2 runs × 11 checks (on/off rebuild path + nikkud+trope composability incl. the Highlight collision fallback engage/disengage); 0 pageerrors. | done: 2026-07-10, S60
- [x] 2026-07-10 | (S59 close-out) | branch/deploy note | S59 = **user-directed backlog refactor** (no discovery pass, like S43/S49), continuing on the designated branch `claude/session-13-5-auf4mh`, adding to the already-open **draft PR #81** (S58; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v261→v262** (precached files changed: hebrew_blend_generator, classroom_dashboard, flash_cards). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **No discovery pass ran → Pass F stays stalest** for S60. Iters: the `showAppToast` shared-block extraction (1 focused cross-tool refactor).
- [x] 2026-07-10 | 972f56f | hebrew_blend_generator.html + classroom_dashboard.html + flash_cards.html + CLAUDE.md | **DONE: `showAppToast` shared-block extraction (the S39 Pass F candidate) + the `.ivrit` restore-alert fold.** Consolidated the byte-identical `showAppToast` (+ `#appToast` CSS) — which lived in 3 different places (generator page code; **inlined inside the flash_cards/dashboard `.ivrit` engine spans**, the S39 port) — into one `/* ═══ shared: app-toast ═══ */` JS block (placed just before the engine) + one `/* ═══ shared: app-toast CSS ═══ */` block (with its own self-contained reduced-motion neutralizer, erasing the three-way RM divergence + flash_cards' indentation mismatch), like ivritsuite-fonts / folder-tree. Folded the engine's blocking `alert('Restored successfully' …)` into a non-blocking `showAppToast(...)` (CLAUDE.md §5) — applied identically across the trio, which **re-unified the `.ivrit` engine block byte-identical across all three tool pages (fixes the S58 finding)**. `index.html` untouched (no `showAppToast`; keeps its actionable "reload open tool pages" `alert()`); torah `showHintToast` / trope `showToast` siblings unaffected. +116/−102. | Verified: sha byte-identity of the new shared JS block (0c889973), shared CSS block (0313dcc3), AND the re-unified `.ivrit` engine (d5decbfb) across the 3 files; **headless `.ivrit` save→restore round-trip on all 3 tools (12 runs, light/dark × 1280/800) — Merge now shows a TOAST, not a blocking dialog** (0 dialogs), restore text correct, reduced-motion `transition:0s`, import + disabled-FAB toasts intact, 0 pageerrors. | done: 2026-07-10, S59
- [x] 2026-07-10 | (S58 close-out) | branch/deploy note | S58 = fresh work on the **new** designated branch `claude/session-13-5-auf4mh`, which sat **exactly on `origin/main`** (`20d65ff`) at session start — **draft PR #78 (S55–S57) was merged**, and **2 `trope_tutor.html` UI tweaks landed outside the loop** (`0b811e2` collapse the empty status bar; `20d65ff` hide the trope glyph on Identify drill choices), bumping `sw.js` **v259→v261** (reconciled here, same as the S41/S50 outside-loop notes). No open PR at start → S58 opens a **new draft PR**. **No `sw.js` VERSION bump** this session — the only changed files (`README.md`, `THIRD_PARTY_LICENSES.md`, `CLAUDE.md`) are **not precached** (not in CORE_ASSETS), like S48/S45. No `FONT_MAKER_VERSION` bump (FM code untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **E** (freshness & site-health, 5th run) — site plumbing **CLEAN**, only P4 doc-drift, all fixed. Docs-only session. Iters: 1 pass (E) + 1 doc-freshness batch = ~1 (well under budget — no P1/P2/P3 remain).
- [x] 2026-07-10 | 83991f4 | README.md + THIRD_PARTY_LICENSES.md + CLAUDE.md | **Pass E (freshness & site-health) doc-drift — all P4, one commit.** **README:** Hebrew Font Maker **v4.5→v4.10** (trailed `FONT_MAKER_VERSION` by 5 releases; 4.6–4.10 were polish/bug-fixes → feature bullets unchanged); Flash Cards feature list gained **custom card count** + the **English/Latin card-face font picker** (the S49 `#engFontGrid`, previously unlisted). **THIRD_PARTY_LICENSES:** added the missing **Kaikki.org / Wiktionary** entry — the source of `data/hebrew_words.json`, the **only** external resource the site uses that was on-page/README-attributed yet absent from the central inventory — under **CC BY-SA 3.0** (matching README's framing). **CLAUDE.md:** bumped the `ivritsuite-fonts` (9-file) + uploader (7-carrier) **sha-verified dates 07-09→07-10** (re-sha'd byte-identical); **corrected the `.ivrit`-engine byte-identity claim** — re-sha showed flash_cards == dashboard but the generator differs **only** by the S39-ported `showAppToast` in its engine span (the documented triplication, tracked by the deferred extraction candidate), so "byte-identical across the three tool pages" had been stale since S39; FM **~12,300→~12,550 lines**; cross-referenced the trope tutor's S51–S55 widget keyboard a11y under convention 7. | Verified: 3 shared-block sha comparisons (fonts 9/9 + uploader 7/7 identical → dates bumped; `.ivrit` engine flash==dash, generator differs by exactly the 17-line `showAppToast` block → claim corrected, date not blindly bumped); FM version + `wc -l` re-grep; opentype.js "Used by: Font Maker" **re-confirmed accurate** (the mechanical audit's other-tool hits were `format('opentype')` CSS false-positives, not library loads). | fixed: 2026-07-10, S58
- [x] 2026-07-10 | (S58 Pass E — no code beyond 83991f4) | all root pages + docs | **Pass E (freshness & site-health, 5th run) — site plumbing CLEAN; only P4 doc-drift, all fixed in 83991f4.** Two read-only reconnaissance sweeps (mechanical site-health + doc-accuracy). **Mechanical CLEAN:** SW `CORE_ASSETS` **both directions** (all **22** entries exist on disk; every root `*.html` precached; only `og-card.png`/`zelle-qr.jpg`/the 18 `splash/*.png` omitted = documented runtime-cache intentional); sitemap (11 `<loc>` = index + 10 content pages, 404 correctly excluded, all resolve); all internal links + 18 `apple-touch-startup-image` splash hrefs + favicon/apple-touch-icon/manifest resolve; robots/CNAME(=ivritsuite.com)/manifest coherent; all 6 bundled-font + 3 FM-library (pyodide/harfbuzzjs/opentype.js) licenses documented. **Doc-drift** (README FM version, 2 flash-cards features, missing Kaikki license entry, CLAUDE.md sha-dates/engine-claim/line-count/conv-7) — found + fixed (83991f4). **Shared-block byte-identity re-sha** (the S58-note ask): fonts 9/9 + uploader 7/7 byte-identical; `.ivrit` engine identical flash==dash, generator differs only by the S39 `showAppToast` (claim corrected — not a drift bug, tracked by the deferred extraction). | Verified via the 2 audit sweeps + the sha/grep re-confirmations. | run: 2026-07-10, S58
- [x] 2026-07-10 | (S57 close-out) | branch/deploy note | S57 continued on the designated branch `claude/session-12-dvb8xp`, adding to the already-open **draft PR #78** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v258→v259** (precached file changed: hebrew_blend_generator). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **B** (console & error audit, 4th run — **CLEAN**, 24/24 page-runs with an interaction on every tool; first B over the S50–S56 surface). Iters: 1 pass (B) + 1 fix (the S27 contrast-to-measure batch — 3 suspects measured clean, 1 adjacent gold-link failure fixed) = 2 (stopped under budget — the remaining backlog is deferred refactors, maintainer calls, and informational residuals only).
- [x] 2026-07-10 | dd466e9 | hebrew_blend_generator.html | **P4 (S27 Pass C contrast tail — measured; 3 of 4 pairs already passing)** — alpha-composited WCAG measurement of the three logged suspects through the real UI (Real Words → My Word Lists with a seeded list, then the empty state): `.rw-section-header` **5.81/5.58** (light/dark), word-list count span **5.81/5.58**, `#rwListEmptyState` body **5.81/5.58** — all ≥4.5:1, **measured-clean, no change**. The one failure was the **gold link inside the empty state**: brand `--gold` on the light panel = **2.60:1** (the suite's recurring gold-on-light pattern). Now `#7f5a13` in light (**5.88:1** measured; the same readable-gold literal as the trope tutor's `--gold-text`, 42f1958) via a scoped rule (inline `style` removed so the rule wins), `var(--gold)` in dark (**7.96:1**, already passing), plus `text-decoration:underline` so the in-prose link doesn't rely on color alone (WCAG 1.4.1). | Verified by re-measurement through the same real-UI path, both themes, 0 pageerrors. | fixed: 2026-07-10, S57
- [x] 2026-07-10 | (S57 Pass B — no code) | all 12 root pages | **Pass B (console & error audit, 4th run — first over the S50–S56 surface: the trope tutor's entire loop-era code, torah print treatments + `beforeprint` handler, picker unnesting, uploader focusability, crossword RTL) — CLEAN: 24/24 page-runs, 0 pageerrors, 0 non-environmental console errors.** All 12 root pages over local HTTP, light-1280 + dark-800, load + one representative interaction per tool (index gear, generator letter tile + Generate, dashboard drawer, flash cards mode-pill + Start, dictionary search, FM letter tile, torah parsha select, trope tutor drill start + answer, resources/contact touch). Environmental noise verified by histogram: the trope tutor's high count (77–82/run) is **100% `Failed to load resource: net::ERR_FAILED`** — route-aborted external requests (fonts/gtag + the drill's PocketTorah MP3 retries per the documented degradation path); no regression from S48–S56. Prior runs S47/S37/S20 also CLEAN. | Verified via the 24-run audit + the noise-composition spot-check. | run: 2026-07-10, S57
- [x] 2026-07-10 | (S56 close-out) | branch/deploy note | S56 continued on the designated branch `claude/session-12-dvb8xp`, adding to the already-open **draft PR #78** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v257→v258** (precached files changed: hebrew_blend_generator, hebrew_dictionary, torah_trainer, trope_tutor). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **H** (teacher walkthrough) on the **generator** — first-ever H on the flagship tool; **CLEAN**. Iters: 1 pass (H) + 2 fixes (crossword clue RTL — generator at the 2/tool cap counting the pass target; shared-picker unnesting across 4 carriers) = 3 (stopped under budget — the remaining backlog is deferred refactors, maintainer calls, informational residuals, and capped-tool items).
- [x] 2026-07-10 | 845bb90 | hebrew_blend_generator.html + hebrew_dictionary.html + torah_trainer.html + trope_tutor.html | **P4 (S51 Pass C residual — `<a>` nested inside `<button class="font-opt">`)** — invalid interactive nesting (AT-unreliable) in the 4 torah-lineage picker copies; the font-source attribution is now a pointer-only `<span data-link>` that opens the same page (`window.open` + noopener + stopPropagation) — one accessible action per button (the font pick), mouse behavior unchanged, no phantom tab stop. The candidate said "7 carriers", but flash_cards/dashboard/resources never rendered attribution links — 4-file fix, single mechanical cross-tool commit (S54 uploader precedent). All 4 edits verified OUTSIDE the `═══`-marked shared blocks (byte-identity intact). | Verified headless 56/56 (4 pages × light/dark, 0 pageerrors): 0 nested anchors, span click opens the source URL with the active font unchanged, button click still picks, span not keyboard-focusable. | fixed: 2026-07-10, S56
- [x] 2026-07-10 | 9384202 | hebrew_blend_generator.html | **P4 (S31 Pass G B9 — crossword clues mis-ordered Hebrew text)** — an imported word list can carry a Hebrew translation as the clue, but `.cw-clue` inherited the worksheet's LTR direction, mis-ordering punctuation/numbers on screen and paper. Clue text now renders in a `dir="auto"` span at the single `clueList` chokepoint (serves the student sheet AND the answer key): first-strong-character ordering keeps English clues pixel-identical, Hebrew clues render RTL and gain `lang="he"` (chokepoint rule). | Verified headless 32/32 (light+dark × 1280/800, 0 pageerrors) through the REAL teacher path — Real Words mode → crossword drill → imported source with mixed Hebrew/English-translation lines → generate: 16 clues built, Hebrew clues `dir=auto`+`lang=he` with geometric RTL confirmed (first glyph right of last), English clues unchanged/no `lang`, clue numbers stay left, computed `direction:rtl` holds under print emulation. | fixed: 2026-07-10, S56
- [x] 2026-07-10 | (S56 Pass H — no code) | hebrew_blend_generator.html | **Pass H (teacher walkthrough / paper-cuts — first-ever on the generator, the flagship worksheet tool; 3rd H overall) — CLEAN, no paper-cuts logged, no seeds.** Role-played a brand-new teacher (empty localStorage, headless over HTTP, light-1280 + dark-800) preparing "Tuesday's aleph-bet worksheet" end-to-end. **Strong:** the empty state is instructive with **5 true one-click Quick-Start recipes inline** (clicking "Kamatz & Patach drill" produced a complete 40-cell worksheet + a guiding toast, empty state dismissed); defaults are sane (28 letters + 9 main vowels pre-selected, live "5,040 possible combinations" count); the manual path took 9 clicks to a real 20-cell sheet with name/date lines; the worksheet title is editable in the always-open LAYOUT panel and **live-updates the rendered sheet**; preset save validates inline (`#presetNameNote`, no alert) and persists; 🖨 Print + ⬇ Export PDF are visible post-generate; `.ivrit` backup + "📋 Copy shareable link" present; the only collapsed panels are genuinely advanced (Advanced/Font/Translit/Color/Backup); 0 pageerrors throughout. Consistent with the tool's deep S27–S32 polish history. **Struck-seeds rule honored:** nothing re-logged. | Verified via the 2-script walkthrough (arrival/recipes/manual/preset/print/backup probes + one-click/title follow-ups). | run: 2026-07-10, S56
- [x] 2026-07-10 | (S55 close-out) | branch/deploy note | S55 = fresh work on the **new** designated branch `claude/session-12-dvb8xp`, which sat **exactly on `origin/main`** (`b8758d3`, S54 close-out — **draft PR #77 was merged**). No open PR at start → S55 opens a **new draft PR**. `sw.js` VERSION **v256→v257** (precached files changed: trope_tutor, torah_trainer, Hebrew_Font_Maker, hebrew_blend_generator). `FONT_MAKER_VERSION` **4.9→4.10** + one About-tab changelog entry. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **I** (first-load & empty-state, 3rd run — CLEAN). **Maintainer direction this session: micro-feature track skipped and both flash_cards feature seeds permanently struck** (see the Feature seeds tombstone). Iters: 1 pass (I) + 4 fixes (trope polish batch, torah print tail, FM UFO xHeight, generator gematria blank — 4 different tools, all within the variety caps) = 5 (budget).
- [x] 2026-07-10 | 5523bc7 | hebrew_blend_generator.html | **P4 (S31 Pass G B7 — gematria write-on blank faint/stripped on paper)** — the 1px `var(--border)` background fill was stripped by the print dialog's default background-graphics:off and faint on B/W even when kept; in `@media print` it is now `border-top:1px solid #1a2744` (the same ink as the name/date `.ws-field-line`), screen rendering unchanged. | Verified headless 12/12 (light+dark × 1280/800, 0 pageerrors): 40 real blanks generated through the UI path; screen computed style unchanged; print emulation shows the navy border with bg removed. | fixed: 2026-07-10, S55
- [x] 2026-07-10 | 27d1b82 | Hebrew_Font_Maker.html | **P4 (S42 Pass G — UFO baked `xHeight` = `capHeight` = `letterTop`)** — Hebrew is caseless, so the duplicated x-height guide confused font editors (Glyphs/RoboFont); the optional `xHeight` key is now **omitted** from `buildUfoFontInfo` (plist simply doesn't write it), matching the exported TTF's unspecified OS/2 `sxHeight`; `capHeight` unchanged. Ships v4.10 + changelog entry. | Verified headless 20/20 (light+dark × 1280/800, 0 pageerrors, fake-Pyodide stub + traced-alef fixture): `buildUfoSpec().fontinfo` has no `xHeight`, `capHeight` == `letterTop` (600), zero null/undefined values (plist-safe), version + changelog present. | fixed: 2026-07-10, S55
- [x] 2026-07-10 | fb38cec | torah_trainer.html | **P4 (S53 Pass G tail, items 1–2 — divider rules vanish on paper; mid-karaoke print freezes `.active`)** — (1) the aliyah-divider `::before/::after` 1px background fills now print as `border-top:1px solid #555` (borders always print); (2) a `beforeprint` handler calls the canonical `pauseKaraoke()` + `clearKaraokeHighlight()` pair, with a print-CSS backstop neutralizing `.tt-word.active`'s background/box-shadow/outline/color — `text-decoration` deliberately untouched so the S53 trope clause-underline still prints. | Verified headless 16/16 (light+dark, print-emulation computed styles + real `page.pdf()` runs): screen divider unchanged, print divider = border with bg removed, `.active` neutralized, **trope print underline survives on an active trope word** (family color), `beforeprint` fires during `page.pdf()` and clears the highlight, 0 pageerrors. | fixed: 2026-07-10, S55
- [x] 2026-07-10 | 160c8fe | trope_tutor.html | **P4 batch (S51 Pass C polish — 8 of 9 items)** — melody two-tap armed state now exposed via `aria-pressed` (set on arm, cleared on disarm/answer); `#qHint` melody wording device-neutral ("Choose a number…", was touch-only "Tap…"; the in-button `.c-sub` matches); `#gearBtn` `aria-label="Settings"`; decorative header `✡︎` `aria-hidden` + the 🔥/🏆 drill streak line carries a worded `aria-label`; tablist gains APG arrow-key nav (Left/Right/Home/End focus + activate; both tabs stay tabbable); tradition `.radio-group` → `role="radiogroup"` with an accessible name; both `.tu-mgrid`s → `role="group"` so their `aria-label` is exposed; mastered cells gain a visually-hidden "mastered" text (new tiny `.vh` utility) + title suffix (was gold-border-only). Item 9 (nested `<a>` in the shared `.font-opt` picker) stays a Candidate — it's a 7-carrier shared-picker concern. | Verified headless 45/48 + a deterministic follow-up (light+dark × 1280/800, 0 pageerrors): all static aria checks, arrow-key tab nav both directions, mastered vh+title (seeded progress fixture), streak label wording, armed `aria-pressed` cycle confirmed synchronously (the 3 initial fails were the offline-audio race regenerating the question mid-check — the documented degradation path, not a defect). | fixed: 2026-07-10, S55
- [x] 2026-07-10 | (S55 Pass I — no code) | all 12 root pages | **Pass I (first-load & empty-state, 3rd run — first to include trope_tutor.html; 24 fresh-context page-runs + 10 depth probes over local HTTP) — CLEAN, nothing to fix.** **Broad sweep 24/24** (12 pages × light/dark, empty localStorage, 0 pageerrors): OS-aware dark honored everywhere (dark-early with no stored key under a dark OS; light stays light); FM `welcomeOverlay` auto-opens; dashboard `frModal` starter card + `frStarters` grid show; tour pulses fire on all 7 tool pages; no unexpected auto-open overlays; `lang="he"` present on every Hebrew-rendering page. **Depth (never-I'd surface):** trope tutor as a brand-new user — Learn renders (6 family chips → card incl. postpositive copy), drill starts, and the audio-blocked melody question **skips with a toast and regenerates into an answerable question** (feedback + Next) exactly as documented; progress key written with correct shape. Dashboard S50 schedule prefill: first rows correctly stay empty when there's nothing to infer. S54 uploader on a fresh profile: `.vh-file` rendered and focusable once its real container is open (collapsed panel correctly excludes it from focus). FM fresh: welcome → dismiss → letter select works, `#fontVersion` defaults "1.000" (v4.9 integrity fine on fresh). Torah fresh: dark bg correct; the "Failed to load … Sefaria API may be unreachable" status is the documented offline-env artifact (instructive, no crash). **800px dark: no horizontal scroll** (trope tutor, flash cards, dashboard). | Verified via the 4-script audit suite (34 runs total, 0 pageerrors). | run: 2026-07-10, S55
- [x] 2026-07-10 | (S54 close-out) | branch/deploy note | S54 continued on the designated branch `claude/session-11-uh5dj0`, adding to the already-open **draft PR #77** (open, not merged → no branch restart, no new PR). `sw.js` VERSION **v255→v256** (precached files changed: all 7 uploader carriers + dictionary). No `FONT_MAKER_VERSION` bump (FM's only change is the shared uploader markup — not an FM feature). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **D** (performance) on the **dictionary** (2nd pass; coldest tool). Iters: 1 pass (D) + 2 fixes (suite-wide uploader P3; dictionary collator P4) = 3 (stopped under budget — remaining backlog is P4 batches suited to their own sessions).
- [x] 2026-07-10 | fb38fda | hebrew_dictionary.html | **P4 (S54 Pass D — the `'he'` locale sort dominates every `onFilter`)** — every sort comparison called `localeCompare(…,'he')`, re-resolving the locale per call: 155 ms of the ~195 ms filter-toggle cost at 4× throttle and the bulk of the one >200 ms first-render block. Hoisted `const HEB_COLLATOR = new Intl.Collator('he')` beside `GEMATRIA` and swapped the five Hebrew comparators in `onFilter`'s sort (the English `transl` comparator untouched; the memoized one-time `buildRootIndex` left as-is per smallest-diff). Measured 155→79 ms @4× / 29→15 ms desktop — the last two >200 ms-at-4× moments now under threshold. | Verified on the real 13,081-word corpus: collator ordering **byte-identical** to the `localeCompare` ordering; alpha sort renders 100 cards; 0 pageerrors light+dark. | fixed: 2026-07-10, S54
- [x] 2026-07-10 | e292a34 | 7 files (generator, dashboard ×2 inputs, flash_cards ×2, dictionary, torah, trope tutor, index) | **P3 (S51 Pass C — suite-wide: "⬆ Upload your own font?" was not keyboard-operable)** — the file input carried `hidden` (`display:none` → unfocusable) with no `tabindex` on the wrapping label, so the uploader was mouse/touch-only in **every** carrier. All 9 inputs now use a visually-hidden-but-focusable `.vh-file` class (`position:absolute;width:1px;height:1px;opacity:0`) — **Enter/Space opens the picker natively** — plus a gold ring on the wrapping label via `label:has(.vh-file:focus-visible)`; markup stays parallel across carriers (S24 cross-tool convergence precedent; single commit). | Verified headless on all 7 pages × light/dark (**98 checks**): input reachable + focusable within each page's real containers (settings drawers, collapsed panels/adv-sections, the index gear modal), Enter fires the file chooser, label click still fires it, ring renders, inputs visually hidden but rendered, 0 pageerrors. | fixed: 2026-07-10, S54
- [x] 2026-07-10 | (S54 Pass D — no code beyond fb38fda) | hebrew_dictionary.html | **Pass D (performance, dictionary 2nd pass — first-ever D was on this tool) — effectively CLEAN; both S12 fixes HELD; 1 P4 found + fixed same-session.** Profiled over HTTP (13,081-word corpus), `PerformanceObserver('longtask')`, desktop + 4× CPU throttle, 0 pageerrors: **cold load** desktop interactive ~278 ms (1×54 ms block); @4× ~1.05 s with worst block 242–248 ms = the one-time first `onFilter` render behind the loading overlay (P4, halved by fb38fda). **Search typing** 0 longtasks at both speeds — the 150 ms debounce is load-bearing and intact. **Filter toggles** @4× 182–197 ms, decomposed: filter pass 2–20 ms (S12 precompute memo held), 100-card rebuild ~41 ms, `'he'` sort 155 ms (fixed). **Word Lists** (100-word list seeded): manager 4.9/20 ms, expand 2.3/10.8 ms, load-as-filter 40/99 ms — capped + denormalized store, no full-corpus work. **Modal** ≤5.4/17.8 ms. **Idle 4 s: 0 mutations, 0 longtasks** both speeds. Grep re-confirmed: the only full-`ALL_WORDS` passes are `onFilter`, the memoized root-index builders, and per-lookup shoresh rendering — nothing new in word-lists/`?s=`-restore/copy-share/a11y code since S12. | Verified via the instrumented profile (3 attributed cold-load runs). | run: 2026-07-10, S54
- [x] 2026-07-10 | (S53 close-out) | branch/deploy note | S53 continued on the designated branch `claude/session-11-uh5dj0`, adding to the already-open **draft PR #77** (open, not merged → no branch restart, no new PR). `sw.js` VERSION **v254→v255** (precached files changed: trope_tutor, torah_trainer). No `FONT_MAKER_VERSION` bump (FM untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **G** (print & export fidelity) on the **torah trainer** — first-ever print audit of the tool teachers explicitly print from. Iters: 1 pass (G) + 4 fixes (trope contrast P3 + touch targets P3; torah print P2-tier + P3-tier) = 5 (budget; trope-tutor and torah each at the 2/tool cap counting the pass target).
- [x] 2026-07-10 | 070e0e0 | torah_trainer.html | **P3 tier (S53 Pass G — page chrome prints; verses split mid-verse; fixed elements stamp every page)** — extended the print treatment: header/controls/`#ttStatus`/Read+Chant-all/footer (its SEO nav alone spilled an extra page)/`.tt-empty` and every `position:fixed` surface (drawer, backdrop, `#tipFloat`, toast, tour, `#fsExitBtn` — fixed elements re-stamp on EVERY printed page) now print-hidden; `.tt-verse`/`.tt-grid-row` get `break-inside:avoid` (generator precedent). Also **moved the whole `@media print` block to the END of the stylesheet** (generator S30 convention) — at its original top position, the equal-specificity `.tt-grid` column collapse from 60277fe lost by source order and never applied. | Verified headless end-to-end: fixture verses through the real render path; print-emulation computed styles (chrome hidden, breaks avoid, 1-column side-by-side, dark tokens light); real Letter PDF (Hebrew present, footer/controls text absent); 0 pageerrors. (Verification note: `.tt-word`'s 0.1s karaoke background transition needs a settle delay before reading print-computed styles — test-only artifact; print rendering resolves final values.) | fixed: 2026-07-10, S53
- [x] 2026-07-10 | 60277fe | torah_trainer.html | **P2 tier (S53 Pass G — three silently-wrong printed artifacts)** — (1) **dark mode printed navy sheets**: no print neutralizer existed (the generator's S30 pattern was absent); ported the token reset (`html.dark-early body, body.dark` re-stated light, non-`!important`, source-order wins) + white body. (2) **Trope coloring silently vanished on paper**: all six family treatments are background tints, stripped by the print dialog's default background-graphics:off with the legend print-hidden — no cue anything was missing; trope-classed words now print the existing clause-underline treatment (text-decoration always prints, B/W included). (3) **Side-by-side printed WRONG verse pairings**: the two columns are independent flows that drift out of sync (audit measured English 2:11–3:10 beside Hebrew 2:2–2:6 by page 3) and pagination froze the drift onto paper; print collapses to the existing ≤720px single-column sequential layout. (The column-collapse rule only became effective with 070e0e0's block move — see that entry.) | Verified with 070e0e0 (single end-to-end suite). | fixed: 2026-07-10, S53
- [x] 2026-07-10 | 42f1958 | trope_tutor.html | **P3 (S51 Pass C — gold text on light backgrounds fails contrast)** — added a scoped `--gold-text` token per the dictionary S41 precedent: **`#7f5a13`** in light (6.2:1 white / 4.7:1 warm-gray — measured darker than the in-file `.mark-hit` `#8a6216` precedent, which only reached 4.17:1 on the active chip's `--warm-gray`), `var(--gold)` in dark (7.2–8.7:1, already passing). Repointed the nine failing text selectors (`.tu-drill-streak`, `.big-pct`, `.tu-pb-banner`, `.tu-chip .visited`, `.tu-card-links a`, `.tu-rare-tag`, `.tu-intro a`, `.font-upload-label`, `footer a`); brand `--gold` untouched for borders/buttons/glyph tiles. | Verified headless with computed-style alpha-composited WCAG math, light+dark × 1280/800: all nine pairs ≥4.5:1 (large-text `.big-pct` ≥3:1 bar also cleared at 6.2:1), 0 pageerrors. | fixed: 2026-07-10, S53
- [x] 2026-07-10 | 8ea040f | trope_tutor.html | **P3 (S51 Pass C — small touch targets on primary student controls)** — `.tu-ex-play` (the per-example ▶, up to 20 per Learn page — the whole Learn interaction) 30×30→**38×38**; `.settings-close` 32×32→**40×40**; `.sreset` padded to ~31px tall. Row heights absorb the growth. | Verified headless: measured getBoundingClientRect sizes at both viewports, no horizontal scroll at 800px, 0 pageerrors light+dark. | fixed: 2026-07-10, S53
- [x] 2026-07-10 | (S53 Pass G — no code beyond the 2 torah commits) | torah_trainer.html | **Pass G (print & export fidelity — first-ever on the torah trainer; fixture verses through the real render path over HTTP, print-emulation + REAL Letter/A4 PDFs inspected via content-stream analysis) — the print treatment was 3 lines; 3 P2 + 3 P3 found, ALL 6 FIXED this session (2 commits); 4 P4s logged.** Headline: every generator S30 print pattern (chrome hide list, break-inside, dark-print reset, tint→ink substitution) was absent from the suite's most-printed tool. **CLEAN (verified):** `#ttTropeLegend` + jumpnav/audio-bar/resume-chip/verse-buttons correctly print-hidden as documented; RTL integrity on paper (real Unicode Hebrew + nikkud + te'amim in the PDF text layer, copy-pastable); no nikkud/trope clipping (print line-height identical to screen); interlinear + page-view structurally sound; Letter vs A4 differ only in break positions; 0 pageerrors across all 12 PDF runs. **Logged (P4):** aliyah-divider rule-lines vanish, karaoke-active state not print-neutralized, B/W nikkud ambiguity (informational), verse-aligned two-column print as a future refinement. | Verified via the 12-PDF audit + the two fix verifications. | run: 2026-07-10, S53
- [x] 2026-07-10 | (S52 close-out) | branch/deploy note | S52 continued on the designated branch `claude/session-11-uh5dj0`, adding to the already-open **draft PR #77** (open, not merged → no branch restart, no new PR). `sw.js` VERSION **v253→v254** (precached files changed: torah_trainer, classroom_dashboard, Hebrew_Font_Maker). `FONT_MAKER_VERSION` **4.8→4.9** + one About-tab changelog entry. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **A** (recurring-pattern sweep of everything since S41 — the never-swept outside-loop Trope Tutor + trope-coloring code, FM v4.3–v4.8, S50/S51 diffs). Iters: 1 pass (A) + 3 fixes (torah drawer P2, dashboard drawer P2, FM version-integrity P4) = 4 (stopped one under budget — remaining candidates deserve their own focused sessions).
- [x] 2026-07-10 | d343b1b | Hebrew_Font_Maker.html | **P4 (S52 Pass A — falsy-zero hit + adjacent rollback gaps: font-version integrity on export)** — (1) `parseFloat(v) || 1.0` at the doExport version tick AND the buildFontSpec head-table bake silently promoted a legal user-entered `0.x` pre-release font version to 1.x (the `#fontVersion` field is free-text); now `Number.isFinite`-guarded, so `0.500` ticks to `0.501` and garbage still falls back to 1.0. (2) The v4.3 bump-only-on-success rule had two leak paths that ratcheted the version per retry with no file shipped: shaping-gate-fail → **Cancel**, and WOFF2 conversion failure (incl. `finishDownload`'s swallowed-exception catch). `finishDownload` now resolves `true` only when a download actually fired; doExport + the Download-anyway button roll back on `false`, Cancel rolls back explicitly. Ships v4.9. | Verified headless 20/20 (both viewports, light+dark, 0 pageerrors; `window.loadPyodide` fake-engine stub per the house recipe): spec keeps 0.5 / garbage→1.0; 0.500→0.501 on success; gate-Cancel rolls back; Download-anyway keeps the bump; WOFF2 failure rolls back; WOFF2 success keeps. | fixed: 2026-07-10, S52
- [x] 2026-07-10 | b0d8cce | classroom_dashboard.html | **P2 port (S51 Pass C candidate — closed settings drawer stays in the keyboard tab order)** — third carrier of the copied drawer shell, same bug verified live (phantom stops inside the closed drawer), same 3-line CSS `visibility` fix as trope tutor 681e840; focus-restore-to-opener already existed. | Verified headless (both viewports, light+dark, 0 pageerrors): closed drawer `visibility:hidden`, 60 Tabs never enter it, open/trap/restore intact. (Verification note: descendants with their own `transition:all` chain the inherited visibility flip for ~150 ms after the modal's 0.3s delay — the tab-order fix is steady-state-complete ~450 ms after close; the transient is harmless.) | fixed: 2026-07-10, S52
- [x] 2026-07-10 | 7da3916 | torah_trainer.html | **P2 port (S51 Pass C candidate — closed settings drawer stays in the keyboard tab order)** — second carrier of the copied drawer shell, same bug verified live, same 3-line CSS `visibility` fix as trope tutor 681e840; focus-restore-to-opener already existed. | Verified headless (both viewports, light+dark, 0 pageerrors): closed drawer `visibility:hidden`, 60 Tabs never enter it, open/trap/restore intact. | fixed: 2026-07-10, S52
- [x] 2026-07-10 | (S52 Pass A — no code beyond d343b1b) | trope_tutor.html + torah_trainer.html + Hebrew_Font_Maker.html + S50/S51 diffs + scripts/build-trope-index.mjs | **Pass A (recurring-pattern sweep, everything since S41) — CLEANEST SWEEP SCOPE YET: 1 P4 hit (fixed), 7 of 8 classes CLEAN.** The never-swept trope_tutor.html shipped with the documented safety patterns genuinely implemented: clip-bound `s`/`e` reads all falsy-zero-safe (`== null` guards, direct assignments — the documented "0 is valid" fields verified); all four written localStorage keys correctly registered/exempt at **all five** index.html sites (export/import/erase + IVRIT_CFG gather/apply, progress via `tropeProgressMerge` max/union); `ivritSafeParse`/`ivritSafeAssign` on every untrusted parse; all index-derived Hebrew DOM-built (`createElement`/`textContent`); no shadowed globals; audio engine leak-free (token-guarded, rAF cancelled, per-call listeners cleaned on all three exits); JSON-LD FAQ/behavior claims all match visible content and actual code (`QUESTIONS_PER_SESSION`, mastery weighting, `CONFUSABLE_PAIRS`); resets confirm-gated; all controls reachable. torah trope-coloring: `TROPE_HEX6_RE` validated at the single read chokepoint. FM v4.3–v4.8: `ensureNikkudAnchors`/`wordAdvance`/autosave-restore all `!= null`-guarded. The 1 hit: FM `parseFloat(v) || 1.0` (fixed d343b1b, with the adjacent out-of-class rollback note). **Pattern health:** falsy-zero streak resets (1 hit); the other 7 active classes increment. Retired undo-wiring/slider-commit not re-checked (not an A2). | Verified via the scoped sweep (every grep hit read in context; TROPES blocks sha-confirmed byte-identical). | run: 2026-07-10, S52
- [x] 2026-07-10 | (S51 close-out) | branch/deploy note | S51 continued on the designated branch `claude/session-11-uh5dj0`, adding to the already-open **draft PR #77** (opened by S50; open, not merged → no branch restart, no new PR). `sw.js` VERSION **v252→v253** (precached files changed: Hebrew_Font_Maker, trope_tutor, index). `FONT_MAKER_VERSION` **4.7→4.8** + one About-tab changelog entry. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **C** (accessibility) on the **trope tutor** — first-ever audit of the tool (never loop-touched; also the variety pick). Iters: 1 pass (C) + 4 fixes (FM anchors, TT drawer P2, TT drill focus P3, index comment P4) = 5 (budget).
- [x] 2026-07-10 | dece726 | index.html | **P4 (S22 Pass A aside — document the deliberate `hebrewFontMaker_autosave` erase omission)** — added the recommended one-line comment block inside `eraseAllSettings`'s key list explaining why the FM autosave (localStorage fallback + IndexedDB) is deliberately NOT erased: a settings erase must never wipe a teacher's in-progress font project; FM projects live outside the AllTools payload and the `.ivrit` project file is their canonical save. Comment-only, no behavior change. Coldest tool (index, last touched S24). | Verified: page loads over HTTP with 0 pageerrors, `eraseAllSettings` still defined. | fixed: 2026-07-10, S51
- [x] 2026-07-10 | 0fea475 | trope_tutor.html | **P3 (S51 Pass C — drill drops keyboard/SR focus after every question)** — answering destroyed the focused `#qNextBtn` on re-render → `document.activeElement === BODY`, so a keyboard student re-tabbed from the page top (through the then-phantom drawer) after each of the 10 questions, and nothing announced the new question. `#qPrompt` + the results `<h2 id="resHeading">` now carry `tabindex="-1"` and receive focus at the end of `renderQuestion()`/`showResults()` — SRs announce the fresh prompt, keyboard users land right before the stem/choices; matches the existing `next.focus()` convention in `answer()`. | Verified headless over HTTP (both viewports, light+dark, 0 pageerrors): focus on `qPrompt` at start and after every Next, `qNextBtn` focused after answering (pre-existing behavior intact), full session to results with `resHeading` focused; melody two-tap flow exercised. | fixed: 2026-07-10, S51
- [x] 2026-07-10 | 681e840 | trope_tutor.html | **P2 (S51 Pass C — closed settings drawer stays in the tab order: ~13 invisible tab stops)** — the off-screen drawer (`transform:translateX(100%)` only) kept computed `visibility:visible` with no `aria-hidden`/`inert`, so every full-page keyboard traversal visibly lost focus for ~13 stops at x≈1608, and the `aria-modal` dialog was permanently exposed to SRs. CSS-only fix: `visibility:hidden` on `.settings-modal` with a `0.3s`-delayed `visibility` transition (slide-out animation still plays; `.open` restores visibility immediately). The existing focus-restore-to-opener on close means the delayed hide never strands focus; reduced-motion (`transition:none`) makes the hide instant, which is correct there. Same shell exists in torah_trainer + dashboard → logged as a P2-verify candidate. | Verified headless over HTTP (both viewports, light+dark, 0 pageerrors): closed = `visibility:hidden`, open = visible with focus trapped in, hidden again after slide-out, focus returns to `#gearBtn`, and **60 Tab presses never enter the closed drawer** (was ~13 stops per traversal). | fixed: 2026-07-10, S51
- [x] 2026-07-10 | d209ab0 | Hebrew_Font_Maker.html | **P3 (S42 Pass G, held from S50 for a dedicated iteration — partial `anchors` object TypeErrors the whole export)** — an imported/older project whose `.anchors` exists but lacks the `below/above/center` triad threw in `emitBaseAnchors` (`l.anchors.below.x` guarded only by mark-class length), aborting export to the generic "Export error" modal; `seedAnchorsIfNeeded` only full-seeded when `.anchors` was entirely absent and `ensureTropAnchors` backfills only trop keys. Added `ensureNikkudAnchors(l)` (idempotent; fills only absent keys with the **same bbox-derived values `seedAnchors` computes**, so a backfilled anchor exactly matches a fresh seed), wired into `seedAnchorsIfNeeded`'s else branch — protecting all ~10 call sites (anchor editor, spacing preview, migrate, export) — and made `emitBaseAnchors` call `seedAnchorsIfNeeded` unconditionally so the emit path repairs partial anchors too. Ships v4.8. | Verified headless 16/16 (both viewports, light+dark, 0 pageerrors) with the crafted crash fixture (`.anchors` = trop keys only): export builds without throwing, backfilled triad deep-equals a fresh seed, pre-existing keys preserved, complete anchors untouched (idempotence), fully-missing anchors still full-seed, FEA carries the pos-base rules. | fixed: 2026-07-10, S51
- [x] 2026-07-10 | (S51 Pass C — no code beyond the 2 fixes above) | trope_tutor.html | **Pass C (accessibility — first-ever audit of the trope tutor, over local HTTP so the full drill flow ran; Identify + Melody end-to-end 10/10, Hear in its offline-degradation path) — strong shape, 1 P2 + 3 P3 + a P4 batch found; P2 + 1 P3 fixed this session.** **CLEAN (verified):** zero pageerrors all runs; every visible interactive element keyboard-reachable AND operable (tabs, chips, ▶ buttons with per-example labels, choices, toggles via Space, tradition radios via arrows, sliders, FAQ, skip link → `#tuMain`); explicit gold `:focus-visible` rings + no `outline:none` anywhere; drawer-while-open trap/Escape/restore all correct; `#qFeedback` role=status announces correct/wrong as text (never color-only); `aria-pressed`/`aria-selected`/`aria-expanded` synced; tour dialog semantics + Escape/arrows/focus-restore; 68 `lang="he"` elements at the chokepoints; reduced-motion neutralizes every animation (one color-only transition noted); dark-mode contrast passes everywhere measured (worst 4.67:1); light-mode muted text passes (5.2–6.2:1); 800px = no horizontal scroll, choices ≥52px tall; audio-blocked Hear drill never dead-ends (substitute → regenerate → skip, toasted). **Fixed:** the closed-drawer tab-order P2 (681e840) + the drill focus-drop P3 (0fea475). **Logged:** gold-on-light contrast P3 (2.1–2.75:1, fix precedent `#8a6216` in-file), touch-target P3 (30×30 ▶), suite-wide uploader-not-keyboard-operable P3 (shared markup pattern, all 7 carriers), P4 polish batch (melody armed-state AT, hint wording, gearBtn label, emoji SR noise, tablist arrows, radiogroup, mgrid label, mastered-state AT, nested interactive), and the P2-verify candidate for torah_trainer/dashboard's copies of the drawer shell. **Not exercisable offline:** real audio seek/stop behavior, tour visuals (spot-checked statically), real touch gestures. | Verified via the 5-run audit (light+dark × 1280/800 + reduced-motion) + the two fix verifications. | run: 2026-07-10, S51
- [x] 2026-07-10 | (S50 close-out) | branch/deploy note | S50 = fresh work on the **new** designated branch `claude/session-11-uh5dj0`, which sat **exactly on `origin/main`** (`5e5bdb6`) at session start — **draft PR #74 was merged** (S46–S49 work live), and the **Trope Tutor + Torah-Trainer trope-coloring features landed outside the loop** (sw **v248→v251**, FM 4.5→4.6 — reconciled here, same as the S41 note for FM v3.9–v4.2). No open PR at start → S50 opens a **new draft PR**. `sw.js` VERSION **v251→v252** (precached files changed: generator, Font Maker, dashboard). `FONT_MAKER_VERSION` **4.6→4.7** + one combined About-tab changelog entry (word-spacing floor + gate-skip warning). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **F** (cross-tool consistency — toast/status feedback, all 7 tools incl. the new trope tutor). Iters: 1 pass (F) + 4 fixes = 5 (budget).
- [x] 2026-07-10 | 08c4b28 | classroom_dashboard.html | **P3 (S46 Pass H paper-cut — new schedule row's time input starts empty)** — `addScheduleRow()` now prefills the new row's `until`: it repeats the gap between the last two timed rows (the teacher's period length), or +45 min after a single timed row; stays empty when there's nothing to go on or the increment would pass midnight (gap sanity-capped at 12 h). Entry shape `{preset,until}` unchanged — saved schedules / `.ivrit` payloads unaffected; validation never flags the prefill (increment ≠ duplicate). | Verified headless 16/16 (light+dark × 1280/800, 0 pageerrors): first row stays empty; 09:00→09:45 (+45 default); 09:00/09:50→10:40 (gap repeat); 23:30→empty (midnight guard); rendered input matches model; entry-shape + no-duplicate-warning checks. | fixed: 2026-07-10, S50
- [x] 2026-07-10 | 2c584e4 | Hebrew_Font_Maker.html | **P3 (S42 Pass G — harfbuzz shaping-gate skip ships unvalidated marks silently)** — a hard HB-load failure made `harfbuzzGate` return `{skipped:true}` and the export proceeded with only the auto-clearing status toast. Mirrored the v4.5 `fell_back` idiom: `window._lastGateSkipped` resets per export beside `_lastFellBack`, is set **only when marks were enabled** but the gate skipped (a marks-off skip is benign — nothing to validate; the UFO path returns before the gate and stays clean via the reset), and `offerSaveToMyFonts` appends a persistent `.warn` line to the "Exported ✓" modal ("test it in a word processor, or export again once you're back online"). No retry button; no undo/dirty/project-state interaction. Ships v4.7 (combined entry with the word-spacing floor). | Verified headless 10/10 FM checks ×2 viewports: warning present when flagged, absent when not, 0 pageerrors light+dark. | fixed: 2026-07-10, S50
- [x] 2026-07-10 | dd5f84c | Hebrew_Font_Maker.html | **P3 (S42 Pass G — zero word-spacing → words run together in the installed font; falsy-zero-adjacent)** — the slider (`min="80"`) and `setWordSpacing` (`Math.max(80, …)`) both clamp to ≥80, but `wordAdvance()` floored at 0, so a stored 0 (ancient or hand-edited save — the only way to get one) emitted a zero-width `space` glyph. One-token fix: `Math.max(80, …)` so the export accessor agrees with the UI's own minimum. Pure read accessor — no `project` mutation, no undo/dirty. | Verified headless: `wordAdvance()`→80 and `buildFontSpec().glyphs.space.advance`→80 with a crafted `wordSpacing=0`, both viewports, 0 pageerrors. | fixed: 2026-07-10, S50
- [x] 2026-07-10 | 30e0251 | hebrew_blend_generator.html | **P4 (S50 Pass F convergence — generator preset-import success still `alert()`)** — S39 converged the dashboard + flash_cards import-success alerts on `showAppToast` but missed the generator's own textarea-import path (`importPresets`, `alert(\`Imported N preset(s)…\`)`). One-line swap to the generator's already-defined `showAppToast` — the lone remaining import-success `alert()` in the suite; completes the S39 convergence. | Verified headless 8/8 (both viewports): no `alert()` fired, toast shows "Imported 1 preset(s)…", `role="status"`, 0 pageerrors light+dark. | fixed: 2026-07-10, S50
- [x] 2026-07-10 | (S50 Pass F — no code beyond 30e0251) | all 7 tools | **Pass F (cross-tool consistency, 3rd run — toast/status feedback affordance, first audit to include the trope tutor) — strong convergence, 1 laggard fixed, 1 finding folded into the deferred extraction.** Cataloged every feedback mechanism: generator/dashboard/flash_cards share `showAppToast` (`#appToast`, 2600 ms); torah `showHintToast` (`#ttHintToast`, 8000 ms); trope tutor `showToast` (`#tuToast`, 6000 ms — built to the house pattern, explicitly "inline refusal — never a blocking alert()"); FM `status()` (`#statusMsg` bar, 4000 ms) + `askModal`; dictionary has **no visual toast** (inline `wlSetStatus` + SR-only `announce()` — its only alerts are the shared font-loader errors, so a toast would be net-new feature, not convergence — skipped). **A11y CLEAN:** every toast/status element in all 7 tools carries `role="status"`. Fresh findings: (1) the generator import-success alert (fixed, 30e0251); (2) the `.ivrit` engine's triplicate `'Restored successfully'` alert — belongs to the deferred shared-block extraction (candidate updated), NOT a per-tool fix (would break the engine's byte-identity). The known `showAppToast`-duplication divergence re-confirmed and left deferred per log-don't-force. | Verified via read-only 7-tool audit + the 30e0251 headless checks. | run: 2026-07-10, S50
- [x] 2026-07-09 | (S49 close-out) | branch/deploy note | S49 continued on the designated branch `claude/session-10-hqd501`, adding to the already-open **draft PR #74** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v247→v248** (precached file changed: flash_cards). No `FONT_MAKER_VERSION` bump. GitHub Pages deploy runs on merge — verify `success` after merge. **User-directed micro-feature session** (loop's **3rd micro-feature**) — **no discovery pass** run (precedent: S43/S25/26/28/29 backlog/feature sessions); **Pass F stays stalest** for next session. Iters: 2 (micro-feature).
- [x] 2026-07-09 | db0b112 | flash_cards.html | **MICRO-FEATURE (S34 Pass H seed — English/Latin display-font selector; loop's 3rd micro-feature, 2 iters)** — the card front for Numbers/Colors/Words + every romanization/translit line was hard-wired to `var(--serif)`; only Hebrew had a font picker. Added a second **"English / Latin Font"** grid in Advanced Settings (`#engFontGrid`), mirroring the Hebrew picker + the dashboard's dual-font pattern: a curated **`ENG_FONTS`** list (Sans/Serif/Kid-Friendly/Easy-Reading — all Google Fonts, already CSP-allowlisted → **no CSP edit**) + the shared My Fonts uploader (`onUploadEngFontPick`), all driving a new **`--eng-font`** CSS var. Cloned `setHebFont`/`initFontSelector`→`setEngFont`/`initEngFontSelector` (reusing the script-agnostic `loadHebFont`); repointed the **16 card-scoped `var(--serif)`** occurrences (inside `renderCurrentCard` + `.fc-translit`/`.lc-num`) to `var(--eng-font)`, leaving the 5 UI-chrome usages (headers/results/tour) on `--serif`; scoped `setHebFont`'s `.active` toggle to `#fontGrid` so the two grids don't clash. **Default `--eng-font: var(--serif)` → zero visual change until picked.** **Transient-free persistence:** `engFontId` is a `getSettings()` field → rides in the existing `hebrewFlashCards_settings` blob + `.ivrit` liveState — **no new localStorage key, no index.html edit.** 90-ins/18-del, single-file (within the fence). | Verified headless **59/59** (light+dark × 1280/800, 0 pageerrors): both grids render independently (11 Eng + 12 Heb, section headers); default `--eng-font`==serif; picking "Comic Neue" updates `--eng-font` + `.active` while **`--heb-font` is unchanged** (and vice-versa); results `.rtr`/chrome stay `--serif` while `.fc-translit` follows `--eng-font`; `engFontId` in getSettings, roundtrip stable, `.ivrit` gather carries it, **no new `eng*` localStorage key**; deck still starts+flips+navigates. | shipped: 2026-07-09, S49
- [x] 2026-07-09 | (S48 close-out) | branch/deploy note | S48 continued on the designated branch `claude/session-10-hqd501`, adding to the already-open **draft PR #74** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). **No `sw.js` VERSION bump** — the only changed files (`README.md` + `CLAUDE.md`) are **not precached** (not in CORE_ASSETS). **No `FONT_MAKER_VERSION` bump** (FM code untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **E** (freshness & site-health) — mechanically **CLEAN**, 3 P4 doc-drift fixed. Docs-only session (like S45); no sitemap change (no page content changed).
- [x] 2026-07-09 | e4c2d9c | README.md + CLAUDE.md | **Pass E (freshness & site-health) — mechanical all CLEAN; 3 P4 doc-drift fixed.** **Mechanical CLEAN** (verified read-only): sitemap (10 `<loc>`, index + 9 content pages, 404 correctly excluded, `lastmod` current); SW `CORE_ASSETS` **both directions** (all 21 entries exist on disk; every root `*.html` precached; only `og-card.png`/`zelle-qr.jpg` omitted = documented intentional); internal links + 18 `apple-touch-startup-image` splash hrefs + favicon/apple-touch-icon/manifest all resolve; THIRD_PARTY_LICENSES covers every CDN + bundled TTF (the S46 Cu2Qu fix reused existing `Cu2QuPen`/fontTools, the S47 fullscreen used the native API → **no new dependency**); robots/CNAME(=ivritsuite.com)/manifest coherent; and **every CLAUDE.md shared-UX count still holds** (tour "6 tools", tooltip carriers, reduced-motion "10 files" — the Present CSS is layout-only with zero transitions, so no RM obligation was added, `lang="he"` list, ivritsuite-fonts "8 files" — the flash_cards `#fsBtn` invalidated none). **3 doc-drift fixed:** (1) README FM version "(Currently v3.8.)" → **v4.5** (trailed `FONT_MAKER_VERSION` by 7 releases); (2) README Flash Cards feature list gained the **Present/fullscreen-for-projectors** affordance (S47 feature — restores parity with the Dashboard/Torah listings that already advertise fullscreen); (3) CLAUDE.md FM "~10,350 lines" → **~12,300** (actual 12,285; v4.0–v4.5 added ~1,900). | Verified by re-grep: README shows v4.5 == source `FONT_MAKER_VERSION`; flash_cards bullet contains "Present/fullscreen … projectors"; CLAUDE.md ~12,300 ≈ live `wc -l` 12,285; no other stale FM version in README. | fixed: 2026-07-09, S48
- [x] 2026-07-09 | (S47 close-out) | branch/deploy note | S47 continued on the designated branch `claude/session-10-hqd501`, adding to the already-open **draft PR #74** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v246→v247** (precached file changed: flash_cards). **No `FONT_MAKER_VERSION` bump** (Font Maker untouched). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **B** (console & error audit, all 11 pages) — **CLEAN**. **Micro-feature shipped** (2 iters): the flash_cards Present/fullscreen button (loop's 2nd micro-feature, from the S34 Pass H seed). Iters: 1 discovery pass (B) + 2 micro-feature = 3.
- [x] 2026-07-09 | 507c543 | flash_cards.html | **MICRO-FEATURE (S34 Pass H seed — Fullscreen / "Present" mode; loop's 2nd micro-feature, 2 iters)** — added a projector **Present** toggle (`#fsBtn`, ⛶) to the header's `.h-right` next to Tour + dark-mode, matching the `.h-btn` style. Click → `document.documentElement.requestFullscreen()` (webkit fallback); in fullscreen the **running** deck's card + Hebrew glyph scale up for projection (`body.fc-fullscreen #screenCard.active .fc-card/.fc-heb/.fc-translit`; setup/results untouched), and the fullscreen backdrop uses the `--bg` token so there's **no black bar** in either theme. **Feature-detected** (hidden where the Fullscreen API is absent). The `fullscreenchange` listener is the single source of truth (icon ⛶↔⤢, `aria-pressed`, title sync) so native **Escape** stays in sync; a rejected request fails silently (CLAUDE.md §5, no `alert`). **Purely transient** — nothing persisted: no `getSettings()`/localStorage change, no AllTools/`.ivrit` wiring, no new key. 48-line diff, one tool, no deps (well within the micro-feature fence). | Verified headless **82/82** (light+dark × 1280/800, 0 pageerrors): button present/in-`.h-right`/`.h-btn`/aria; feature-detect hides it when `requestFullscreen` removed; click calls `requestFullscreen` once; simulated `fullscreenchange` toggles `body.fc-fullscreen` + `aria-pressed` + icon both directions (Escape path); card+glyph enlarge only when `#screenCard.active`; `--bg` backdrop; deck still starts+flips+navigates. Plus **fresh-profile** clean + **.ivrit/settings round-trip** intact (53 keys, **no** fullscreen key leaked, `getSettings→applySettings` stable, `IVRIT_CFG.gather` returns `{presets,liveState}`). | shipped: 2026-07-09, S47
- [x] 2026-07-09 | (S47 Pass B — no code) | all 11 root pages | **Pass B (console & error audit, 3rd run — last S37/S20, both CLEAN) — CLEAN.** Loaded every root page headless (light-1280 + dark-800), route-aborting non-`file:`/`data:`/`blob:`, capturing `pageerror` + non-benign console errors/warnings on load **and one interaction per tool** (index gear, generator letter tile, dashboard drawer, **flash cards mode-pill + Start**, dictionary search, FM letter tile, torah). **20/22 page-runs CLEAN**; the only flags were **torah_trainer** (both themes): a `console.error` "Failed to load data files" at `init` + two `hebrew-transliteration` ESM-CDN import warnings — the **documented environmental artifact** (local `data/parshiyot` fetch fails over `file://`; the ESM CDN is route-aborted). **Confirmed environmental** via a local HTTP server: the "Failed to load data files" error **vanishes over HTTP** (page renders, `bodyHasContent:true`); the only residual over-HTTP error is `fetchSefariaText` hitting the **remote** Sefaria API (expected offline), exactly as S37/S20 found. **No regression** from S38–S46; nothing to fix. Notably flash_cards (this session's feature target) was CLEAN on load + interaction — good pre-flight. | Verified via the 22-run headless sweep + the HTTP-server confirmation of the torah env noise.
- [x] 2026-07-09 | (S46 close-out) | branch/deploy note | S46 = fresh work on the **new** designated branch `claude/session-10-hqd501`, which sits **exactly on `origin/main`** (`b16b2d1`, S45 close-out — PR #73 on the prior branch `claude/session-9-m73gkn` was **merged**). No open PR at session start → S46 opens a **new draft PR**. `sw.js` VERSION **v245→v246** (precached file changed: Hebrew_Font_Maker). `FONT_MAKER_VERSION` **4.4→4.5** + one About-tab changelog entry. GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **H** (teacher walkthrough / paper-cuts) on the **dashboard** (a projector-first teacher surface never H'd; last code S39) — variety pick. Iters: 1 fix (FM P2) + 1 discovery pass (H).
- [x] 2026-07-09 | aefe739 | Hebrew_Font_Maker.html | **P2 (S42 Pass G — silent Cu2Qu fallback ships faceted glyphs)** — in `hfm_build` the cubic→quadratic conversion (`Cu2QuPen`) was wrapped `except Exception: built=False`, and the `if not built:` branch re-emits the glyph as a **straight-line polygon** — but that branch fires for **two** indistinguishable cases: (a) legitimately no curves, and (b) curves present but conversion threw. Case (b) silently shipped a curvy letter **faceted** with no warning. Fix: added a `fell_back` accumulator that appends the glyph `name` **only** in the `except` (which is reachable only inside `if curves:` → exactly case (b)), returned it as `fellBack`; `doExport` maps those internal names → visible Hebrew characters via `spec.cmap` (`window._lastFellBack`, reset per export so the UFO path never inherits stale data), and the persistent **"Exported ✓"** success modal (`offerSaveToMyFonts`, the S43 QA surface) now shows a `.warn` line naming exactly which letters came out with straight edges, `lang="he"` + `esc()`-escaped. Font bytes unchanged (diagnostic-only). Shipped as v4.5. | Verified two ways: **(1) real fontTools e2e** (`fonttools` 4.63 installed offline) — a crafted spec with a valid-curve glyph, a malformed-curve glyph, and a no-curve glyph → `fellBack == ['bad']` exactly (valid + no-curve excluded), TTF still built; **(2) headless UI, 56/56** (light+dark × 1280/800, 0 pageerrors, stubbed): warning renders with mapped Hebrew chars in `<b lang="he">`, correct singular/plural, coexists with the LICENSE button, **no** `.warn` when `fellBack` empty, UFO path clean after a prior TTF export (reset holds), unknown glyph-name passthrough, hostile label `esc()`-escaped; plus `py_compile` on the extracted `PY_BUILDER`. | fixed: 2026-07-09, S46
- [x] 2026-07-09 | (S46 Pass H — no code) | classroom_dashboard.html | **Pass H (teacher walkthrough / paper-cuts) — first-ever run on the dashboard; strong shape, 1 P3 paper-cut logged.** Role-played a brand-new teacher (empty localStorage, headless) setting up a class: enabled Schedule Sync, added rows, inspected defaults/empty-states. **Strong:** the `#frStarters` first-run starter card shows (gated by `hebrewDashboard_setupSeen`), the Presets panel surfaces **✨ Quick-start layouts**, `settings.location` defaults to **New York** so Weather + Shabbat times work out-of-box with zero setup, the saved-schedule empty state reads "No saved schedules yet." + New-folder, and fullscreen + quick-controls exist (0 pageerrors throughout). **One paper-cut → P3 Candidate:** each new schedule row's `<input type="time">` starts **empty** (no prefill/auto-increment, no per-row Duplicate — only Move up/down/Remove), so building a multi-block day means typing every time from scratch. No trivial P1 → no fix forced this session (FM was the code tool per variety). | Verified via the headless walkthrough (2 probe scripts; 0 pageerrors).
- [x] 2026-07-09 | (S45 close-out) | branch/deploy note | S45 continued on the designated branch `claude/session-9-m73gkn`, adding to the already-open **draft PR #73** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). **No code change → no `sw.js`/`FONT_MAKER_VERSION` bump, no sitemap change** (ledger-only commit). Ran the strictly-stalest **Pass I** (first-load & empty-state); **CLEAN**, no fix forced (the remaining small candidates are all on the resting generator/Font Maker). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-09 | (S45 Pass I — no code) | all 11 root pages | **Pass I (first-load & empty-state, 2nd run — last S33) — CLEAN across all three legs.** **(1) Static absent-key audit:** every `localStorage` read is fallback-guarded — all 8 `JSON.parse(localStorage.getItem…)` sites carry `|| '{}'`/`'[]'`/`'null'` + try/catch; the settings loaders that omit a fallback string (`classroom_dashboard.html:4116`, `torah_trainer.html:1424`, `flash_cards.html:3679`) are safe because `ivritSafeParse(null)` → `JSON.parse("null")` → **null** (no throw) and each guards it (`if (saved) … else defaults`, all try/caught); the lone bare `hebrew_dictionary.html:1732 JSON.parse(str)` is the trusted same-origin corpus, not storage. **(2) Empirical empty-storage sweep** (fresh browser context per page = clean localStorage + IndexedDB; OS-aware dark via `colorScheme`): **22/22 loads CLEAN** (11 pages × light/dark, **0 pageerrors**, content rendered, dark default honored on a new profile, FM welcome modal correctly auto-opens on first visit). **(3) Tour-copy drift:** all 6 `TOUR_STEPS` engines' static-control targets resolve on a fresh profile — including the heavily-reorganized **FM v3.9–v4.4 tour (7/7)**; the only unresolved targets are the **3 documented content-dependent steps** (dictionary `#resultsGrid .word-card`, torah `.tt-verse-buttons` + verse cells) that need remote corpus/Sefaria data, exactly as S33 noted; the FM export/LICENSE copy was already reconciled in S43. New-user experience solid suite-wide across everything added since S33 (flash-cards custom count, FM v3.9→v4.4, generator print/a11y, dictionary+torah a11y). | Verified as described (static audit + 22/22 headless empty-storage sweep + 6-tool tour-target resolution).
- [x] 2026-07-09 | (S44 close-out) | branch/deploy note | S44 continued on the designated branch `claude/session-9-m73gkn`, adding to the already-open **draft PR #73** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v244→v245** (precached file changed: hebrew_blend_generator). No `FONT_MAKER_VERSION` bump (FM untouched — rested per variety after S41–S43). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **D** (performance) on the generator.
- [x] 2026-07-09 | 51a2e97 | hebrew_blend_generator.html | **P4 (S38 candidate — consolidate duplicate real-word CSS blocks)** — `.rw-card`/`.rw-heb`/`.rw-translit`/`.rw-translation`/`.rw-pos-tag`/`.rw-num`/`.rw-grid` were each declared **twice** in the same screen `<style>` scope (both after the `@media print landscape` block closes at L1217, so genuinely same-scope). The later block won for conflicting props (padding `10px→8px`, gap `4px→3px`, `.rw-heb` navy→text/1.3→1.2, translation `0.78→0.75rem`), so the earlier block was dead — **except** two props had no counterpart in the later block and **survived the cascade**: `.rw-translation { min-height:14px }` and `.rw-pos-tag { letter-spacing:0.04em }`. A naive "delete the earlier block" would have silently changed the card. Merged those two survivors into the winning block, then deleted the dead block → one source per selector, no drift risk. | Verified headless (synthetic `.rw-*` elements, before/after `getComputedStyle`): light-mode computed styles **byte-identical**; dark-mode `.rw-heb` still resolves to `var(--text)` (rgb 221,228,240); surviving min-height/letter-spacing intact; 0 pageerrors. | fixed: 2026-07-09, S44
- [x] 2026-07-09 | (S44 Pass D — no code) | hebrew_blend_generator.html | **Pass D (performance, generator 2nd pass — last D'd S13) — the known Class-Set 40 build RE-QUANTIFIED; idle CLEAN.** Faithful headless profile (Class-Set **40** versions × blendCount **40** + answer key, desktop + 4× CPU throttle, `PerformanceObserver('longtask')` + MutationObserver idle probe): the synchronous `generate()`→`renderWorksheet`→`appendStandardBlendSheets` build produces **13,840** worksheet DOM nodes in a single **353 ms desktop / 1,305 ms @4×-throttle** longtask — **worse than the S13 estimate** (210/368 ms), because the config is heavier and the tool grew (S27–S38 a11y/print/page-size work). **Idle recurring churn CLEAN** (0 mutations over 2.5 s — no interval/live-preview hotspot). Inspected `renderHebrewCell`: cells are built **detached** then attached (no per-cell reflow); the only per-cell DOM read is a cheap `getElementById` — **no cheap win available**. The genuine fix is the parked **chunked/deferred render** refactor, which changes the synchronous-render contract that print/`exportPDF`/`?ak=` depend on → **large, risky; log, don't force** (candidate stays parked with the updated numbers). One P4 cleanup **was** actionable and shipped this session (51a2e97 above). | Verified via the profile; the finding confirms the parked P3, no code from the pass itself.
- [x] 2026-07-09 | (S43 close-out) | branch/deploy note | S43 continued on the designated branch `claude/session-9-m73gkn`, adding to the already-open **draft PR #73** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v243→v244** (precached file changed: Hebrew_Font_Maker). `FONT_MAKER_VERSION` **4.3→4.4** + one About-tab changelog entry. GitHub Pages deploy runs on merge — verify `success` after merge. **User-directed backlog-fix session** (no discovery pass — Candidates was well-stocked; stalest pass **D** stays queued). Precedent: S25/26/28/29 were also backlog sessions.
- [x] 2026-07-09 | 2155ef1 | Hebrew_Font_Maker.html | **P2 (S42 Pass G — reliable LICENSE delivery for single-file exports)** — TTF/WOFF2 exports dropped `LICENSE.txt` as a **second** programmatic `downloadBlob` from a 400 ms `setTimeout` after the font; browsers routinely block/gate a second auto-initiated download, so the user could get the font with **no license file** (the zip/UFO embed it inside the archive and were safe). Replaced the fragile deferred download with a reliable one-click **⬇ LICENSE.txt** button on the existing "Exported ✓" modal (`offerSaveToMyFonts` gained a `licenseSeparate` arg) — a click inside the modal is never blocked, and the font stays one clean automatic download (no multi-download prompt, `.ttf` still double-clickable). The button names the file `<fontname>-LICENSE.txt`, which **also closes the logged P4** generic-`LICENSE.txt` collision for single-file exports. Zip/UFO unchanged; About-tab copy (3 spots) updated to match. Shipped as v4.4. | Verified headless (light+dark, 0 pageerrors, `downloadBlob` stubbed): a TTF export fires exactly **one** auto download (`<fam>.ttf`) and **no** deferred second (waited 700 ms); the modal shows the LICENSE button whose click downloads `<fam>-LICENSE.txt` with body === `licenseText()`; zip/UFO (`offerSaveToMyFonts(false)`) show no button; adversarial family `Foo/Bar` → `FooBar.ttf` + `FooBar-LICENSE.txt`. | fixed: 2026-07-09, S43
- [x] 2026-07-09 | (S42 close-out) | branch/deploy note | S42 continued on the designated branch `claude/session-9-m73gkn`, adding to the already-open **draft PR #73** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v242→v243** (precached file changed: Hebrew_Font_Maker). `FONT_MAKER_VERSION` **4.2→4.3** + one combined About-tab changelog entry (both export fixes are user-facing). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass rotation:** ran the strictly-stalest **G** (print & export fidelity) on Font Maker's font-file export path.
- [x] 2026-07-09 | c796158 | Hebrew_Font_Maker.html | **P3 (S42 Pass G #5 — bump the font version only on a successful export)** — `doExport` incremented `project.font.version` at the top of its `try`, **before** `ensurePyodide()` + the build, so a failed engine download (the common, repeatable failure) ratcheted the version on every retry and any build/FEA error left it inflated. Captured the previous version, moved the bump to run once the engine is ready (still **before** `buildFontSpec`, so the bumped value is baked into the font), and roll it back on the UFO-error, build-error, and exception paths. Shipped in the combined v4.3. | Verified headless (light+dark, 0 pageerrors, fake-engine stubs via `window.loadPyodide`): engine-load failure and build error both leave the version at 1.000; a successful export bumps exactly once to 1.001, baked before the download fires. | fixed: 2026-07-09, S42
- [x] 2026-07-09 | cd84087 | Hebrew_Font_Maker.html | **P1+P2 (S42 Pass G #1/#2 — spec-legal export identity strings)** — the exported PostScript name (nameID 6) and download filename derived from the family name by only stripping whitespace, so `/ ( ) < > [ ] { } %`, emoji, accents, or Hebrew letters produced an **out-of-spec nameID 6** (rejected/mangled by strict validators, PDF embedders, macOS Font Book) and a whitespace-only family name yielded an **empty filename** (a hidden ".ttf" dotfile) + empty PS name. Added one `fontSafeBase(fam)` helper (keep printable ASCII 33–126, drop PostScript- + filesystem-forbidden punctuation, cap so `<base>-Regular` ≤ 63, never empty → `MyHebrewFont`) and routed all three derivations through it — `psName` in `buildFontSpec`, the filename in `finishDownload`, and `spec.ufoName` — so every format (TTF/WOFF2/zip/UFO) gets a legal identity from one place. Unicode-capable name fields (family/full/copyright) keep the raw name. | Verified headless (light+dark × 1280/800, 0 pageerrors): for `Foo/Bar`, `"   "`, `Café (2)`, Hebrew, emoji, a 90-char name, `<script>`, and normal names, both `spec.names.psName` and `spec.ufoName` are non-empty, pure legal ASCII, ≤63, end `-Regular`; normal names unregressed. | fixed: 2026-07-09, S42
- [x] 2026-07-09 | (S42 Pass G audit) | Hebrew_Font_Maker.html | **Pass G (print & export fidelity) — first-ever audit of Font Maker's font-file export path** (TTF / WOFF2 / TTF+WOFF2 zip / UFO), mapping `exportFont`→`doExport`→`buildFontSpec`/`buildFEA`→fontTools `PY_BUILDER`→`finishDownload`. Fixed the top **P1** (psName) + a paired **P2** (empty filename) in cd84087 and a **P3** (version-bump-on-failure) in c796158; logged **7** more findings as Candidates (P2 license-second-download + silent Cu2Qu; P3 partial-anchor crash, zero word-spacing, shaping-skip; P4 LICENSE filename collision, UFO xHeight). Confirmed **SAFE** (recorded, not re-flagged): kern pairs referencing dropped glyphs are filtered (`:10499`); no falsy-zero `||` on export numerics (`effMetrics`/`letterAdvance` use `!= null`, Python `spec.get(k,default)` keeps a present 0); single `UPM=1000` (no mismatch); FEA compile failure surfaces an error modal (not silently shipped); LICENSE.txt is generated for every format (the fragile part is the *delivery*, logged P2). | Verified via the read-only export map + the two fixes above.
- [x] 2026-07-09 | (S41 close-out) | branch/deploy note | S41 = fresh work on the designated branch `claude/session-9-m73gkn`, which sits **cleanly on the latest `origin/main`** — the merged S40 history **plus** un-logged Font Maker feature work v3.9→v4.2 (SW v238→**v241**) that landed outside the loop. **No open PR** at session start → S41 opens a **new draft PR**. `sw.js` VERSION **v241→v242** (precached files changed: torah_trainer + hebrew_dictionary). **No `FONT_MAKER_VERSION` bump** — the FM work this session was a read-only sweep (no FM code change). GitHub Pages deploy runs on merge — verify `success` after merge. **Pass-rotation deviation (user-directed):** ran **Pass A** (recurring-pattern sweep of the new FM code) instead of the strictly-stalest G, because the FM v3.9–v4.2 additions had never been swept; G stays stalest for next session.
- [x] 2026-07-09 | 8215f24 | hebrew_dictionary.html | **P3 (S40 Pass C candidate — gold text contrast, WCAG 1.4.3)** — light-theme `--gold` `#c9922a` as small text failed AA: footer `a` on white ≈2.75:1 and `.shoresh-pat.filled .pat-match` on the 12%-gold-tinted card (`#f9f2e5`) ≈2.47:1. Added a **scoped** `--gold-text` token (`#87681d` in light; `var(--gold)` in dark, where `#e0a832` already ≈7:1) and pointed **only** those two text selectors at it — the suite-wide `--gold` token is untouched (buttons/borders/stars keep the brand color, per the candidate's blast-radius warning). | Verified headless (light+dark × 1280, 0 pageerrors): footer link 5.21:1 light / 7.16:1 dark; `.pat-match` `#87681d` on `#f9f2e5` = 4.68:1 light (offline-confirmed), unchanged in dark. | fixed: 2026-07-09, S41
- [x] 2026-07-09 | 609f433 | torah_trainer.html | **P3 (S40 Pass C candidate — misleading error copy)** — `fetchAndRender`'s `try` wrapped the whole render path (`fetchSefariaText` → `renderText` → post-render helpers), but the lone `catch` unconditionally appended "Sefaria API may be unreachable." to **every** error — misattributing render/code faults (e.g. the S40 `esc` shadowing bug, where the Hebrew was often already on screen) to a network outage. Fix: a `netDone` flag flips true once every Sefaria fetch resolves (just before `renderText()`); the "unreachable" hint is now appended **only** when the failure occurred during the network phase. Genuine fetch failures unchanged. | Verified headless (light+dark × 1280/800, 0 pageerrors): a network-phase throw keeps the hint; a `renderText` throw drops it. | fixed: 2026-07-09, S41
- [x] 2026-07-09 | (S41 Pass A sweep — no code) | Hebrew_Font_Maker.html (v3.9–v4.2 additions) | **Pass A recurring-pattern sweep of the never-swept FM feature work — CLEAN, no P1/P2/P3.** Swept all 6 in-scope classes over the auto-detect (Otsu/lighting/sheet-detect), full node-editor (delete-contour, remove-specks, snap, copy/paste points, flip/rotate/scale, marquee, pen tool, corner-fillet), waterfall, and Edit-points additions: **(1) slider-commit** — every new project-data `type="range"` begins a burst on `oninput` and commits on `onchange` (fillet `filletLiveInput`/`filletCommit`; size setters `setMarkScale`/`setDageshScale`/…; the mark-editor `meSetDotSize` correctly uses its own separate `meBeginEdit`/`meCommitEdit` stack); `adSep`/`adTh` mutate detection-only `adState` (applied solely at `adApply`), zoom/opacity/snap-guides are documented view-prefs. **(2) undo-wiring** — 22 `markDirty()` sites: every project mutation routes through `udDo`/`udBurstBegin` (auto-detect Apply uses `udDo(...withSource:true...)`; pen contour, node delete/paste/transform/specks/fillet all `udDo`); the rest are pure-UI/view (`_hiddenContours` visibility, opacity, snapGuides, guide-collapse) or the documented `savedAnchors` baseline. **(3) falsy-zero** CLEAN (size accessors use `!= null`; the two `epsilon || 0` sites floor to 1.5 either way). **(4) unescaped/unsafe-parse** CLEAN (new UI interpolates numbers/constants/`esc()`ed names; imports still go through the FM-local safe parser; no new bare `JSON.parse`). **(5) shadowed-global** CLEAN. **(6) localStorage-vs-AllTools** CLEAN (no new bare keys; FM is AllTools-exempt). **Retired undo-wiring + slider-commit stayed dead over the largest new-code surface since retirement.** | Verified via the sweep greps + targeted reads of every new mutation/slider site.
- [x] 2026-07-08 | (S40 close-out) | branch/deploy note | S40 continued on `claude/ivrit-improvement-loop-v2-3nam8d`, adding to the open **draft PR #69** (open, `mergeable_state:clean`, not merged → no restart, no new PR). The **P1** (d88fa99) was committed + pushed **first**. `sw.js` VERSION **v237→v238** (precached files changed: torah_trainer + hebrew_dictionary). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | d88fa99 | torah_trainer.html | **P1 (user-reported live bug) — "esc is not a function"** — `renderInterlinear` (L2070) + `renderSideBySide` (L2133) each did `const esc = cssEscape(numLabel)`, **shadowing the global `esc()` HTML-escaper** (L1858) with a string; the next lines call `esc(numLabel)` (data-vk + verse number) → TypeError on every render, **breaking the Interlinear + Side-by-side layouts entirely** (Page view, no shadow, worked). The load catch surfaced it with a misleading "Sefaria API may be unreachable" suffix (logged separately as a candidate). Fix = pure de-shadowing: renamed the local to `vkEsc` at both sites and pointed the 3 inline `onclick` args at it; `esc(numLabel)` now resolves to the escaper. `cssEscape` only escapes `'`, so for a `chapter:verse` label the output is unchanged — data-vk, onclick args, and the `[data-vk]` verse lookups stay consistent. | Verified headless (all 3 layouts render without throwing; `data-vk="44:31"` + `onclick="speakVerse('44:31')"` correct; `esc()` is a function again; 0 pageerrors, light+dark × 1280/800). | fixed: 2026-07-08, S40
- [x] 2026-07-08 | (S40 Pass C audit + shadowing sweep) | hebrew_dictionary.html + suite-wide | **Pass C (accessibility, dictionary 2nd pass) — strong shape.** Word-card `role=button`+Enter/Space, the word-detail modal (trap+Escape+focus-restore), `bindHdrA11y` collapse headers, `aria-expanded`/`aria-pressed` sync, and the `lang="he"` chokepoint are all correct; **both S12 leftovers are now closed** (`_scrollBehavior()` gates the smooth scrolls; `.shoresh-pat.filled` is keyboard-operable). 6 small findings — 5 fixed this session (dd21b3c + 85f37ab), the gold-on-light contrast logged as a candidate. **Shadowing sweep (new pattern, prompted by the P1):** grepped every tool for `const/let/var esc =` and other locals named like suite-globals (announce/hebDisplay/colorizeHebrew/getSettings/applySettings) — **CLEAN; the 2 torah sites were the only ones** (fixed above). | Verified as described (read-only audit + grep sweep).
- [x] 2026-07-08 | dd21b3c | hebrew_dictionary.html | **P3 (S40 Pass C — SR announcements & naming)** — made `#resultCount` a live region (`role="status" aria-live="polite" aria-atomic="true"`) so the result count + "no results" empty state are announced; gave `#searchInput` an `aria-label`; added a shared visually-hidden `#a11yStatus` live region + `announce()` helper, poked in the 3 copy handlers (`copyWord`, `copyDictShareLink`, `_wmCopyFlash`). | Verified headless (light+dark × 1280/800, 0 pageerrors): live region + aria-label present; `announce()` writes `#a11yStatus`; `copyWord` announces "Copied to clipboard". | fixed: 2026-07-08, S40
- [x] 2026-07-08 | 85f37ab | hebrew_dictionary.html | **P4 (S40 Pass C — focus-visible ring + reduced-motion carets)** — `.search-wrap input` + `#shoreshInput` had `outline:none` + a border-color-only `:focus`; added a `:focus-visible` gold outline ring (both keep the border change). Added `.shoresh-patterns-title::after` + `.emoji-cat-arrow` to the reduced-motion `transition:none` list. | Verified headless (light+dark × 1280/800, 0 pageerrors): `:focus-visible` rule present for both inputs; under reduced motion both carets' `transition` computes to 0s. | fixed: 2026-07-08, S40
- [x] 2026-07-08 | e6c0128 | hebrew_dictionary.html | **P3+P4 (S40 Pass C completeness — adversarial-verify follow-ups)** — the S40 adversarial workflow confirmed the 3 fixes above correct + regression-free, and surfaced 3 in-tool gaps, all closed here: **(P3)** `#shoreshInput` (the root-lookup input, the structural twin of `#searchInput`) had **no** programmatic name — added `aria-label="Enter a Hebrew root"` (WCAG 4.1.2/3.3.2, Level A; the audit + iter-2 missed the twin); **(P4)** `copyBulkText()` ("Copy All") gave SR users no feedback — now `announce()`s on success + fallback; **(P4)** `.pos-arrow` collapse caret was missing from the reduced-motion list — added. **Note:** this is dictionary touch #3 this session (nominal 2-per-tool cap) — a deliberate exception, as it *completes* the two already-shipped Pass C concerns (naming + RM) rather than opening a new one, and includes a Level-A P3. | Verified headless (light+dark, 0 pageerrors): shoresh aria-label present; copyBulkText announces "Copied to clipboard"; `.pos-arrow` transition 0s under reduced motion. | fixed: 2026-07-08, S40
- [x] 2026-07-08 | (S39 close-out) | branch/deploy note | S39 continued on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, adding to the already-open **draft PR #69** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v236→v237** (precached files changed: flash_cards + classroom_dashboard). GitHub Pages deploy runs on merge — verify `success` after merge. S39 continued on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, adding to the already-open **draft PR #69** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v236→v237** (precached files changed: flash_cards + classroom_dashboard). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 21d294d | flash_cards.html | **P3 (S39 Pass F — transliteration wayfinding hint)** — the global romanization-style `#translitStyleSelect` (Advanced → Transliteration) and the per-mode "Show transliteration" toggles (Numbers/Colors/Emoji/Words, Mode panel) are **orthogonal** (style vs. visibility) but split across panels with no signposting. Added a one-line static muted hint under the style select pointing to the per-mode visibility toggles. No JS, no state, no `getSettings()`/`.ivrit` interaction. | Verified headless (light+dark × 1280/800, 0 pageerrors): hint renders in the Transliteration sub-panel next to `#translitStyleSelect`; no behavior change. | fixed: 2026-07-08, S39
- [x] 2026-07-08 | c2b0274 | flash_cards.html + classroom_dashboard.html | **P4 (S39 Pass F convergence — non-blocking toast for import success)** — the two laggards had no reusable floating toast (unlike the generator's portable `showAppToast`) and used a **blocking** `alert('Imported successfully.')` for preset-backup import. Ported `showAppToast` (JS + `#appToast` CSS — self-contained: own element, `textContent`, `role="status"`, reduced-motion safe; flash_cards' universal RM reset already covers it, dashboard got `#appToast` added to its targeted RM list) into both and converted the two alerts. **Reframed from the S24 candidate:** every clipboard/copy path in the 3 laggards was **already** non-blocking (button-label swaps), so there was no copy-with-alert to fix; dictionary has no success alert (skipped — would be dead code); the shared `.ivrit` "Restored successfully" alert (+ its `ivritStatus` companion) stays (touching it = every embedding page). 2 tools, one concern. | Verified headless (light+dark × 1280/800, 0 pageerrors): `showAppToast` callable on both, creates `#appToast` role="status", `.show` toggles, light bg `--navy` / dark `#0a0f1c`, auto-hides; the two call sites no longer `alert`. | shipped: 2026-07-08, S39
- [x] 2026-07-08 | (S39 Pass F audit) | 6 tools + index | **Pass F (cross-tool consistency) — non-blocking toast / success-feedback affordance.** Inventoried toast helpers across all tools: generator `showAppToast` (L5751), torah `showHintToast` (L4535), Font Maker `status()` (L2036), the `.ivrit` engine `ivritStatus`; the 3 "laggards" (flash_cards, dashboard, dictionary) had **no floating toast**. Key reframe of the S24 candidate: **copies are already non-blocking** in all 3 (✓ Copied! label swaps), so the only in-scope blocking *success* alerts were the twin `alert('Imported successfully.')` (flash_cards L3999, dashboard L4898) — converged this session (c2b0274 above). Out of scope (verified): dictionary (no success alert), the shared-engine restore alert, all error/validation alerts + `confirm()` guards (CLAUDE.md §5). Logged a follow-up (extract `showAppToast` to a shared block) + corrected the C1 panels-collapsed candidate to a maintainer design call. | Verified as described (read-only audit + the one convergence fix).
- [x] 2026-07-08 | (S38 close-out) | branch/deploy note | S38 is fresh work on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, which sits **cleanly on `origin/main`@65f9b9d** — the merged S30–S37 history. A `git fetch origin main` **force-updated the stale local ref** (`3da0582→65f9b9d`, the exact staleness the S34 note diagnosed), confirming `HEAD==origin/main` (ahead 0 / behind 0), no divergence, and **no open PR** (the prior PR #68 lineage is merged/closed). So S38 stacks on the merged base and opens a **new draft PR**. `sw.js` VERSION **v235→v236** (precached file changed: hebrew_blend_generator; README/CLAUDE aren't precached). GitHub Pages deploy runs on merge — verify `success` after merge. S38 is fresh work on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, which sits **cleanly on `origin/main`@65f9b9d** — the merged S30–S37 history. A `git fetch origin main` **force-updated the stale local ref** (`3da0582→65f9b9d`, the exact staleness the S34 note diagnosed), confirming `HEAD==origin/main` (ahead 0 / behind 0), no divergence, and **no open PR** (the prior PR #68 lineage is merged/closed). So S38 stacks on the merged base and opens a **new draft PR**. `sw.js` VERSION **v235→v236** (precached file changed: hebrew_blend_generator; README/CLAUDE aren't precached). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | a65220f | hebrew_blend_generator.html | **P4 (S27 Pass C candidate — inverted bingo Cards spinner)** — the Cards number-spinner was inverted: top ▲ decreased (`adjustBingoCards(-1)`, "Fewer cards") and bottom ▼ increased. **Corrected the ledger's proposed fix**: swapping only the deltas would have left each aria-label contradicting its new action (the S27 labels describe the *current* action, not the intended one), so a delta swap = an a11y regression. Instead swapped each button's whole action (delta + title + aria-label), keeping glyphs/positions → conventional spinner: top ▲ = "More cards" = increase, bottom ▼ = "Fewer cards" = decrease, all mutually consistent. `adjustBingoCards` unchanged; the 2 buttons are its only callers. | Verified headless (light+dark × 1280/800, 0 pageerrors): ▲ raises 30→31 & clamps at 200; ▼ lowers 31→30 & clamps at 1; each button's onclick/title/aria-label agree. | fixed: 2026-07-08, S38
- [x] 2026-07-08 | (S38 Iter 2, no code) | hebrew_blend_generator.html | **P3 rw-card nikkud clip risk — MEASURED, NOT REPRODUCIBLE → candidate CLOSED.** The premise (tall marks — holam/shin-sin dots — clipped by `.rw-card { overflow:hidden }`) is structurally unreachable: `.rw-card` has `min-height:80px` but **no fixed/max height** (flex column auto-grows), and `.rw-grid` cells stretch, so there is **no vertical overflow** for `overflow:hidden` to clip. Faithful headless measurement (8 worst-case top-mark words incl. `לֹא` = tall lamed + holam, × light/dark × 1280/800): **overflowY=0 on every card**, `.rw-heb` line-box top sits **18.8px below** the card's clip edge (gapBelow 46.8px), **no top-clip, 0 pageerrors**. Kept `overflow:hidden` → no horizontal-bleed regression either. A speculative padding/line-height nudge to the html2canvas-rasterized export path was **not** warranted. (Aside logged as P4 below: duplicate `.rw-card`/`.rw-heb`/`.rw-grid` rule blocks.) | Verified via the faithful reading-drill-card measurement. | closed: 2026-07-08, S38
- [x] 2026-07-08 | d2718ca | README.md + CLAUDE.md | **P4 (Pass E doc reconciliation)** — README Font Maker "(Currently v3.6.)" → **v3.8** to match `FONT_MAKER_VERSION`; CLAUDE.md now cross-references the page-level toast helpers (generator `showAppToast` L5751, torah `showHintToast` L4535, generator `showPreviewNotice` L5783, engine `ivritStatus`) under the Font Maker Toasts primitive — closing the pending "CLAUDE toast-doc" candidate (whose own refs were stale: showAppToast L5644→5751, showHintToast L4514→4535, and it wrongly claimed CLAUDE.md "lists showPreviewNotice"). | Verified: referenced symbols re-grepped at the cited lines; doc-only, no behavior touched. | fixed: 2026-07-08, S38
- [x] 2026-07-08 | (S38 Pass E audit) | site-wide | **Pass E (freshness & site-health) — mechanical checks CLEAN.** sitemap `<lastmod>` vs git (all 10 pages = today's commit date), `sw.js` CORE_ASSETS both directions (21 entries @ v235, all resolve; only intentional omissions og-card/zelle-qr), all internal `href`/`src` + 18 splash hrefs resolve, THIRD_PARTY_LICENSES covers every bundled font + CDN (html2canvas/jspdf/opentype/pyodide/harfbuzz/transliteration + 22 Google Fonts + Ezra SIL/Culmus + GA4/PocketTorah/Sefaria), robots/CNAME/manifest consistent. Doc-drift: 2 P4 found + fixed (d2718ca above). Ledger's other flagged doc items (S36 retirements, S35 flash-cards feature, shared-block counts "8 files"/"11 pages"/tour "6 tools", reduced-motion "10 files", lang="he"/tooltip "Implemented on" lines) all verified **NOT drifted**. Harmless P4 note: CORE_ASSETS precaches duplicate root + `/icons/` icon copies (redundant, not a defect). | Verified as described (read-only audit).
- [x] 2026-07-08 | (S37 close-out) | branch/deploy note | S37 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v234→v235 (precached file changed: Hebrew_Font_Maker). **No `FONT_MAKER_VERSION` bump** — dead-code removal, not a user-facing change. GitHub Pages deploy runs on merge — verify `success` after merge. S37 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v234→v235 (precached file changed: Hebrew_Font_Maker). **No `FONT_MAKER_VERSION` bump** — dead-code removal, not a user-facing change. GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 60d4092 | Hebrew_Font_Maker.html | **P4 (S21 Pass C candidate — remove the dead `.panel-title` collapse handler)** — `initPanels()` bound a click-to-collapse handler to every `.panel-title`, but the workspace always runs `layout-tabbed` (added unconditionally at init, never removed) with `#catPanelGroup` nested in `#catHost`, so `#catHost .cat-panel .panel-title { display:none }` hides all 12 titles — never visible/clickable (category switching goes through the keyboard-operable `#catTabs`). Confirmed dead **empirically** (0 visible `.panel-title` at 1280 & 760 px). Removed the one-line function + its `init()` call; no a11y-wiring (titles never render). Fresh-tool pick (font-maker untouched since S26). | Verified headless (light+dark × 1280/800): `initPanels` gone (typeof undefined), init completes (56 letter tiles), titles stay hidden, category-tab switching intact (letter→fontdetails), 0 pageerrors, no reference errors. | fixed: 2026-07-08, S37
- [x] 2026-07-08 | (S37 Pass B audit, no code) | all 11 root pages | **Pass B (console & error audit) — CLEAN.** Loaded every root page headless (light-1280 + dark-800, load + one representative interaction each: index gear, generator letter tile, dashboard settings drawer, flash cards mode-pill + custom-count, dictionary search, Font Maker letter tile, dark-toggle everywhere). **0 pageerrors across all 22 page-runs.** The only console errors were 10× `URL scheme "file" is not supported` from the dictionary/torah `fetch('data/*.json')` — a `file://`-harness artifact (Chromium blocks `fetch()` of `file:` URLs); re-checked those corpus-loading pages over a local **HTTP** server → **0 pageerrors / 0 real console errors**, confirming the errors are purely environmental. No console-error regressions from the S21–S36 additions (dark-mode aria, FM keyboard, generator a11y/print, the flash-cards feature). Matches the S20 result. | Verified as described (file:// + HTTP re-check); ledger-only.
- [x] 2026-07-08 | (S36 close-out) | branch/deploy note | S36 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v233→v234 (precached file changed: flash_cards). GitHub Pages deploy runs on merge — verify `success` after merge. S36 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v233→v234 (precached file changed: flash_cards). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 952edcd | flash_cards.html | **P3 (S36 Pass A finding — clamp `cardCount` on restore; hardens the S35 feature)** — the S35 custom card-count clamped the custom *input* but the restore path (`applySettings`) + `setCardCount()` were unbounded, so a shared `.ivrit`/preset or hand-edited `hebrewFlashCards_settings` with e.g. `cardCount:500000` (shape-valid → `ivritSafeParse` passes it) would drive `generateDeck`'s `attempts<n*30` loop to ~15M iterations and **freeze the tab on Start**. Added a shared `_clampCardCount(n)` (NaN/garbage→10, else clamp to [1..200]) and routed `setCardCount`, `setCustomCardCount`, and the `applySettings` restore assignment through it — every value reaching `generateDeck` is now bounded + saved back (self-healing). | Verified headless (light+dark, fresh localStorage, 0 pageerrors): restore 500000→200, 0→1, -5→1, "abc"/missing→10, valid 12→12 / 75→75; chokepoint 999999→200; the freeze scenario (restore 999999 → generate) yields a bounded 200-card deck in ~10 ms; the S35 custom-input + preset behavior is unregressed. | fixed: 2026-07-08, S36
- [x] 2026-07-08 | (S36 Pass A sweep) | recurring-pattern sweep | **Pass A — re-swept all 8 ACTIVE classes over the S23–S35 additions** (flash_cards custom card-count, Font Maker keyboard crop/node-insert, generator a11y + print CSS, suite-wide dark-mode `aria-pressed`). **No P1/P2.** Falsy-zero CLEAN (new code uses `??`/clamps; only the 2 vetted `||` exceptions remain); unescaped-input/unsafe-parse CLEAN (count-grid `innerHTML` interpolates only constants + a clamped Number; page-size `@page` allowlists `a4|letter`; no bare `JSON.parse`/`Object.assign`); localStorage-vs-AllTools CLEAN (the feature added **no** key — `cardCount` rides `hebrewFlashCards_settings`); FM undo-wiring + slider-commit CLEAN (node-insert routes through `udDo`; keyboard crop reuses the deliberately-non-undoable `finalizeCrop`, like the pointer path); JSON-LD parity, destructive-bulk, workMode/step all CLEAN. **One actionable finding → fixed this session (952edcd above):** the unclamped-`cardCount`-on-restore robustness gap. | Verified via the sweep + my own falsy-zero/localStorage cross-greps; the finding drove the one fix.
- [x] 2026-07-08 | (S35 close-out) | branch/deploy note | S35 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v232→v233 (precached file changed: flash_cards; classroom_dashboard untouched — Pass D is read-only). Also reverted an S34 candidate the maintainer flagged as intentional (Numbers/Colors gender default — `d231c85`). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 54e26a7 | flash_cards.html | **MICRO-FEATURE (the loop's FIRST; Pass H seed) — custom card-count input** — closes the S34 Pass H friction "no custom card count" (the deck size was a fixed menu [5…100], so no 3-card demo or 12-card set). Added a "Custom" number input to the count grid (styled like a `.count-btn`, spans 2 cells, both themes) setting `cardCount` to any value in [1..200], clamped; preset↔custom selection is mutually exclusive via a shared `_syncCountUI()` that `setCardCount`/`setCustomCardCount`/`applySettings` all route through. Dual-audience (any-size letter/number/color decks). **No new localStorage key** — `cardCount` is already in `getSettings()`/`applySettings()`, so presets + `.ivrit` capture it; no AllTools wiring. +42/-2 lines (well under the ~150 cap). | Verified headless (light+dark, fresh/empty localStorage, 0 pageerrors): input min 1/max 200, preset 10 active on load; custom 12 activates the input + deactivates presets; clamps 9999→200, 0/-5→1, garbage→unchanged; preset click clears the custom field; `getSettings`/`applySettings` AND `.ivrit` liveState round-trips both restore a custom value; `generateDeck(3)`→3 cards. | shipped: 2026-07-08, S35
- [x] 2026-07-08 | (S35 Pass D audit, no code) | classroom_dashboard.html | **Pass D (performance) — 2nd pass, CLEAN.** Re-profiled the all-day projector tool (cold load + a ~4.2 s idle recurring-churn probe, desktop + 4× CPU throttle). **The S14 per-second schedule-preview rebuild fix HELD** — idle DOM churn is ~4 nodes over ~4 clock ticks (just the clock time text), 0 idle longtasks (the projector's real concern is well-controlled). Cold-load: one 321 ms longtask @4× throttle / 89 ms desktop — consistent with (slightly better than) the already-logged P4 cold-load candidate (boot block + 5-family Google Fonts); one-time, desktop-fine, no new action. 0 pageerrors. | Verified as described (headless PerformanceObserver longtask + MutationObserver churn probe); ledger-only, no code touched.
- [x] 2026-07-08 | (S34 close-out) | branch/deploy note | S34 runs on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, opened as **draft PR #68**. The branch sits **cleanly on `origin/main`@733c8bc** (the merged PR #67) — a clean **3-commit / 4-file / +29-13** diff, `mergeable_state: clean`. (An earlier *stale* local `origin/main` ref suggested a ~50-commit divergence; a `git fetch origin main` force-updated `3da0582→733c8bc` and disproved it — there is no divergence.) `sw.js` VERSION v231→v232 (precached file changed: hebrew_blend_generator; flash_cards untouched — Pass H is read-only). GitHub Pages deploy runs on merge — verify it concludes `success` after merge.
- [x] 2026-07-08 | (S34 Pass H audit, no code) | flash_cards.html | **Pass H (teacher walkthrough) — FIRST RUN.** Role-played a new teacher building a Tuesday aleph-bet deck end-to-end (headless, empty localStorage, light-1280 + dark-800). Defaults are sane and crash-free (2-letter, 26 letters / 9 vowels, 10 cards; **0 pageerrors**). Confirmed live: the three teacher-facing panels (Timer, Teacher Tools, Advanced) ship collapsed; **no fullscreen/present mode** (no `requestFullscreen` anywhere); **no print/export of the deck** (no `window.print`, delegated to the generator via a footer link); **no custom card count** (fixed `COUNT_OPTIONS` [5…100]); **no hands-free loop / shuffle** control; font grid Hebrew-only. (The Numbers/Colors gender-default difference was initially noted but is **intentional per the maintainer** — not a defect; not logged as a candidate.) Logged **2 P3 + 1 P4 Candidates** and **5 dual-audience Feature seeds** — the micro-feature track is now seeded for a future session (top seed: fullscreen present mode / custom card-count). | Verified as described (empirical headless walkthrough); ledger-only, no code touched.
- [x] 2026-07-08 | a22aa31 | hebrew_blend_generator.html | **P3 (S34 Pass G tail — dead duplicate `@page` reconciled)** — `@page{margin:0.5in}` (L1847) was dead/misleading: overridden by the later static `@page{margin:0.35in}` (equal specificity, later source wins) and by the injected `#pageSizeStyle` (`_applyPageSizeStyle()`, also 0.35in). Set it to 0.35in so all three `@page` declarations agree; no behavior change. | Verified headless: all three `@page` margins read 0.35in; `setPageSize('a4'|'letter')` still injects `size:A4|letter; margin:0.35in`; 0 pageerrors. | fixed: 2026-07-08, S34
- [x] 2026-07-08 | 4d1b73b | hebrew_blend_generator.html | **P3 (S34 Pass G tail — gold answer-ink darkened for B/W print)** — answer/answer-key content in `var(--gold)` (#c9922a, ~2.6:1 on white) printed as a faint ~50% gray on a mono laser (worst for gematria **answer keys**). Added a print-only override at the end of the main `@media print` block darkening `.gematria-value`, `.blend-answer`, `.rw-translation`, `.ws-wordbank-tn`, `.cw-clue-num` to `#6b4e12` (~7.7:1) — the dark brown-gold the reset already uses for `.answer-key-banner`. Placed after the dark-mode reset so it wins in both themes by source order; non-`!important` so intentional inline nikkud color-coding still overrides. Screen rendering unchanged (print-scoped). | Verified headless (light+dark, print vs screen media): the five selectors → rgb(107,78,18) under print in both themes and stay gold (rgb 201,146,42 / 224,168,50) on screen; contrast 7.71:1; 0 pageerrors. | fixed: 2026-07-08, S34
- [x] 2026-07-08 | (S33 close-out) | branch/deploy note | S33 was a **CLEAN discovery pass (Pass I) — no code change**, so this is a **ledger-only commit** (no `sw.js` bump: v231 stands; README/CLAUDE/ledger aren't precached). Continued on branch `claude/session-6-jli5do` / draft **PR #67**. No deploy impact.
- [x] 2026-07-08 | (S33 Pass I audit, no code) | all 8 root pages + 6 tour engines | **Pass I (first-load & empty-state) — FIRST RUN, CLEAN.** Static recon first: every `localStorage` read is fallback-guarded (`|| '{}'`/`|| '[]'`/`|| 'null'`) or try/caught with a null check, and `ivritSafeParse`=`JSON.parse` (and `JSON.parse(null)`→`null`, doesn't throw), so **no absent-key crash exists** for a brand-new / erased-settings user; no `getItem(...).method()` shape anywhere; new-user defaults are synthesized in code (flash cards letters/vowels, dashboard `DEFAULT_PRESET['Default']`, torah `DEFAULTS`) — no `undefined`/`NaN` leak. Then **empirical headless sweep** (localStorage cleared before every page script via `addInitScript`, all 8 root pages × light-1280 + dark-800, load + one first-use interaction each): **32/32 checks, 0 pageerrors**, and every empty-state / first-run renders — generator `.empty-state`→generate 40 cells, flash cards `#presetEmpty`, dashboard `#frModal` **auto-opens for a new user**, dictionary search box, torah `renderEmptyReading` card, Font Maker 27 selectable tiles, index 12 tool links, resources 336 cards. **Tour/onboarding drift audit** (drove all 6 `TOUR_STEPS` engines, called `reveal()`+`target()` per step): every static target resolves; the only unresolved steps (dictionary "Word cards" `#resultsGrid .word-card`; torah "Read, chant, or loop" `#ttReading .tt-verse-buttons` + "The text itself" `#ttReading .tt-heb`) are **content-dependent** — they need a loaded corpus / parsha that the offline harness can't fetch (Sefaria + `data/` file:// fetch blocked), and `_resolveStep` correctly skips them. **No stale selectors, no drift, nothing to fix.** New-user experience is solid suite-wide. | Verified as described; ledger-only.
- [x] 2026-07-08 | (S32 close-out) | branch/deploy note | S32 continued on branch `claude/session-6-jli5do`, adding to the already-open **draft PR #67** (not merged → no branch restart). `sw.js` VERSION v230→v231 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | 266e8d6 | hebrew_blend_generator.html | **P3 (S32 Pass G — PDF export follows the Landscape toggle for all types)** — `exportPDF` only rotated for tracing (bingo always landscape), so a blends/word-search/crossword PDF stayed portrait even with "Landscape Print" on — disagreeing with the on-screen 10-col widening and (post-cae2fbb) `window.print()`. Now `wantLandscape = isBingoPDF || document.body.classList.contains('landscape-mode')` — the toggle rotates the PDF for every worksheet type; bingo stays always-landscape. Downstream `pageW/pageH` read back from the pdf object, so image-fit + cut-lines auto-adjust; dropped the now-unused `isTracingPDF`; comment updated. | Verified headless (light+dark × 1280/800, stubbed jsPDF): blends export `'portrait'` toggle-off / `'landscape'` toggle-on; bingo `'landscape'` regardless; 0 pageerrors. | fixed: 2026-07-08, S32
- [x] 2026-07-08 | cae2fbb | hebrew_blend_generator.html | **P2 (S32 Pass G headline — "Landscape Print" now actually rotates `window.print()`)** — the toggle set `body.landscape-mode` (which only widened the on-screen blends grid to 10 cols) but was **disconnected from the paper orientation**: `window.print()` is bare, the S31 `@page` set `size:` with no orientation keyword (defaults portrait), and the only landscape print CSS keyed off the *print-dialog* orientation, not the toggle — so toggling Landscape + clicking 🖨 Print still gave a **portrait** page. Fixed: `_applyPageSizeStyle()` now emits `@page { size: <page> landscape }` when `body.landscape-mode` is set (CSS `@page size` takes an orientation keyword → the paper rotates), and `toggleLandscape()` re-applies the style. With the paper now landscape the existing `@media print and (orientation:landscape)` block fires → blends reflow to 10 cols on paper, matching screen. Added a tooltip to the toggle (rotates both Print and Export PDF). | Verified headless (light+dark × 1280/800): `@page` emits `size: letter landscape` / `size: A4 landscape` when toggled, drops the keyword off; switching page size preserves orientation; `applySettings` restores checkbox + landscape `@page`; blends render 10 cols landscape / normal portrait (40 cells both); tooltip present; 0 pageerrors. | fixed: 2026-07-08, S32
- [x] 2026-07-08 | (S31 close-out) | branch/deploy note | S31 continued on branch `claude/session-6-jli5do`, adding to the already-open **draft PR #67** (open, `mergeable_state:clean`, not merged → no branch restart). `sw.js` VERSION v229→v230 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | 299a558 | hebrew_blend_generator.html | **P2/P3 (S31 Pass G — block-break protection; closes B1–B4)** — S30 protected individual cells but not the block containers, so a whole crossword `<table>` could split mid-puzzle across a page (**B1 P2** — unreadable numbering/geometry) and word-search grids / clue lists / word banks could break awkwardly. Added `break-inside:avoid; page-break-inside:avoid` inside `@media print` for the item containers `.cw-clue`/`.ws-wordbank-item` (prevents mid-clue/mid-word splits) and, best-effort, the blocks `.cw-table`/`.cw-clues`/`.ws-grid`/`.ws-wordbank` (a block taller than the page still breaks — the browser overrides — but normally-sized puzzles stay whole). PDF export rasterizes per sheet, so it was unaffected. | Verified headless (print media, 1280): computed `break-inside === 'avoid'` on all six selectors; reading worksheet still renders its 40 cells; 0 pageerrors. | fixed: 2026-07-08, S31
- [x] 2026-07-08 | 1d0088d | hebrew_blend_generator.html | **P3 (S31 Pass G — Letter/A4 page-size control; closes the S30 A4-vs-Letter candidate)** — the browser-print path had no `@page size` (browser/OS locale chose the paper) and `exportPDF` hardcoded US Letter, so A4-locale teachers got Print-vs-PDF pagination mismatch and neither matched the Letter-tuned layout. Added a **"Page Size: Letter | A4"** segmented control (Layout panel, next to Landscape Print; reuses `.color-mode-btn`, already in `PILL_SEL` → aria for free), **default Letter** (no behavior change until chosen). `setPageSize()` (mirrors `setColorMode`) toggles the pill, injects a `<style id="pageSizeStyle">@page{size:Letter|A4;margin:0.35in}</style>` (CSS `@page` can't be class-scoped), and re-renders (browser-print rows-per-page depends on page height). `exportPDF` jsPDF `format` is now `pageSize==='a4'?'a4':'letter'` (downstream `pageW/pageH` read back from the pdf object → image-fit/cut-lines auto-adjust); the tracing/number-sheet rows-per-page height is size-aware (A4 11.69/8.27in). `pageSize` rides `getSettings()`/`applySettings()` → presets + `.ivrit` capture it with **no new localStorage key** and no AllTools wiring. The default `@page{size:letter}` is injected at load so even an A4-locale browser prints Letter by default until A4 is chosen. | Verified headless (light+dark × 1280/800, 36 checks): init injects `size:letter`; toggling flips `@page` size + pill `.active`/aria + `getSettings`; a stubbed jsPDF captures `format` `'a4'` then `'letter'`; `applySettings` restores pill+style; worksheet still renders; 0 pageerrors. | fixed: 2026-07-08, S31
- [x] 2026-07-08 | (S30 close-out) | branch/deploy note | S30 runs on branch `claude/session-6-jli5do` (per session instructions; carries all prior S-work on top of a stale/diverged local `main`). Ships as a **new draft PR** for this branch. `sw.js` VERSION v228→v229 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | fc10cc1 | hebrew_blend_generator.html | **P2/P3 (S30 Pass G — page-break protection)** — in the browser Print path, `.blend-cell`, `.ws-cell`, `.cw-cell`, and `.rw-card` had no break protection, so an item could split mid-cell/mid-card across a page boundary (unlike `.bingo-card`/`.caller-item`/`.match-item`/`.trace-row`, which already do). Added `break-inside:avoid; page-break-inside:avoid` for those four inside `@media print`, scoped to single items only — grids still flow across pages at cell boundaries. The PDF export rasterizes per sheet, so it was already unaffected. | Verified headless (print media, 1280): computed `break-inside === 'avoid'` on all four; the reading worksheet still renders its 40 cells; 0 pageerrors. | fixed: 2026-07-08, S30
- [x] 2026-07-08 | 3a658e0 | hebrew_blend_generator.html | **P2 (S30 Pass G top finding — dark-mode ink bleed on the printed worksheet)** — the browser Print path (`window.print()`) renders the live DOM through `@media print`, which reset only `body{background:white}` and never neutralized dark mode; a teacher printing with dark mode ON got dark slate worksheet backgrounds + light-on-dark Hebrew bleeding onto paper (`.cw-block`/word-search even force `print-color-adjust:exact`). `exportPDF()` was already immune (strips `.dark` on its html2canvas clone) — only `window.print()` bled. Added a dark-mode print reset inside `@media print`: reset the dark custom properties to light (fixes every `var(--*)`-based surface — bingo/caller cells, borders) + re-state the literal-color `body.dark` worksheet rules to light. All non-`!important`, after the dark rules → win by source order, while **intentional inline nikkud color-coding still wins over them (inline > stylesheet)**. `.cw-block` (black crossword cell) and the already-print-handled `.ws-cell.ws-word` deliberately left alone. | Verified headless (light+dark × 1280/800, print media, 58 checks): every worksheet surface (reading/bingo/tracing/matching/gematria/nametheletter + synthetic `.ws-cell`) → light bg + dark text under print+dark; `.cw-block` stays dark; inline nikkud span keeps `#0099bb`; screen dark mode unchanged (fix is print-scoped); light-print unregressed; 0 pageerrors. | fixed: 2026-07-08, S30
- [x] 2026-07-08 | aa18f3c | docs/IMPROVEMENT_LOG.md + docs/IMPROVEMENT_ARCHIVE.md | (S30 ledger v2 bootstrap) — added the mandated v2 sections in the session's first commit: `## Metrics` (Per-session log, Tool coverage, Pattern health, Retired patterns), `## Feature seeds` (empty), and extended the discovery-pass rotation to **A–J**. Archived the Done overflow (S24 and earlier, 89 lines) to the new `docs/IMPROVEMENT_ARCHIVE.md` so the working ledger stays cheap to read. | Verified: heading order + rotation A–J + archive head/tail by grep; log 156→~110 lines.
- [x] 2026-07-08 | (S29 close-out) | branch/deploy note | S29 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v227→v228 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S29, this session) | hebrew_blend_generator.html | **P3 (S27 Pass-C — `aria-pressed` on segmented pill button groups)** — the `.rw-drill-btn` / `.blend-type-btn` / `.color-mode-btn` / `.ivrit-mode-btn` / `.sc-cols-btn` single-select groups tracked the choice only via an `active` class, so AT couldn't announce which option was selected. Added `aria-pressed` mirroring `.active` via **one idempotent `_syncPillAria()` helper** (models `_syncTileAria`) rather than editing the ~16 `.active`-toggle sites: a **delegated document click listener** (`if (e.target.closest(PILL_SEL))`) covers every user toggle across all 5 classes, and `updateModeVisibility()` — which is also `applySettings()`'s final call — covers the programmatic re-syncs (preset / share-link / `.ivrit` restore + checkbox-driven mode changes); an init call stamps the static groups, and the 4 `.sc-cols-btn` template literals carry `aria-pressed` inline (they're rebuilt via `innerHTML`). The color-panel **Reset** action button is marked `data-noaria` and excluded from `PILL_SEL`. Single-select groups → `aria-pressed` (not `role=radiogroup`, which would need roving-tabindex/arrow-key work — left as a possible future upgrade). Purely additive; no behavior change. | Verified headless (light+dark × 1280): the contract `aria-pressed === String(.active)` holds at init, after clicks on all 5 classes, after the broad `.rw-drill-btn` clear (L9912) + `updateModeVisibility`, after `applySettings` restore (correct **without a click**), and for a `.sc-cols-btn` rebuild+click; Reset excluded + unlabeled; exactly one pressed per main group; 0 pageerrors. | fixed: 2026-07-08, S29
- [x] 2026-07-08 | (S28 close-out) | branch/deploy note | S28 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v226→v227 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S28, this session) | hebrew_blend_generator.html | **P2 (S27 Pass-C top candidate — accessible names for 22 unnamed config controls)** — the worksheet/drill config `<select>`/`<input>`/`<textarea>` controls relied on a visual-only sibling `<span>`/`<div>` (no `<label for>`, no wrapping `<label>`, no `aria-label`), so a screen reader announced only "combo box"/"edit". Added a disambiguating `aria-label` to each of the 22: `rwCount`, `wsWordCount`, `wsGridSize`, `cwWordCountSel`, `cwGridSize`, `matchCount`, `matchVersions`, `rwBingoGrid`, `rwBingoCards`, `rwBingoPerPage`, `cwImportTextarea`, `bingoGrid`, `bingoCards`, `bingoPerPage`, `npCount`, `npMin`, `npMax`, `timerDuration`, `blendCount`, `classSetCount`, `wsTitle`, `wsSubtitle`. Reused visible labels ("Number of Words" ×3, "Grid size" ×4, "Cards"/"Per page" ×2) are qualified by context (word search / crossword / bingo — hence `aria-label`, not `aria-labelledby` at the ambiguous span); `npMin`/`npMax` split "Number range" into Minimum/Maximum number; `cwImportTextarea` (no visible label) got an accurate invented name. Every name **contains the visible label text (WCAG 2.5.3 Label in Name)**. Purely additive attributes — no markup restructure, no behavior change. | Verified headless (light+dark × 1280): all 22 present with the exact expected `aria-label`, zero duplicate ids, 2.5.3 containment holds on the spot-checked set, worksheet still generates (no template-string corruption); 0 pageerrors. | fixed: 2026-07-08, S28
- [x] 2026-07-08 | (S27 close-out) | branch/deploy note | S27 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v225→v226 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S27, this session) | hebrew_blend_generator.html | **P3/P4 (S27 Pass C — accessibility 2nd pass on the generator, the discovery pass this session)** — audited the flagship tool afresh (esp. the ~S22 Word Lists / real-words drill UI). **No keyboard traps** — the only click-only non-`<button>` elements (7 `.rw-section-header` divs) are fully keyboard-wired by `_wireCollapseHdr`, and `outline:none` appears zero times. Fixed the two smallest, highest-confidence classes: (a) **icon-only button names** — the flip-card `🔊` audio buttons (real-words + self-check drills, 2 sinks) + the bingo Cards spinner `▲`/`▼` gained `title`/`aria-label` ("Play audio"; "Fewer cards"/"More cards", labelled by real action); (b) **reduced-motion** — the drill `cardFlip`, the **infinite** `.timer-display.urgent` blink (WCAG 2.2.2 + vestibular), and the `.rw-section-header::before` chevron rotate now sit in the `@media (prefers-reduced-motion: reduce)` block (CLAUDE.md §4). Logged the larger findings as fresh Candidates (unnamed `<select>`/`<input>` rows **P2**, segmented-control `aria-pressed` **P3**, inverted spinner arrows **P4**, contrast suspects **P4**). | Verified headless (light+dark): normal-mode animations intact (timer/shuffle 0.5s, chevron 0.2s), all → `none`/`0s` under reduced-motion; both `🔊` carry `aria-label="Play audio"`; spinner labels "Fewer cards"/"More cards"; 0 pageerrors.
- [x] 2026-07-08 | (S26 close-out) | branch/deploy note | S26 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v224→v225 (precached file changed: Hebrew_Font_Maker). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | (S26 iter 1) | Hebrew_Font_Maker.html | **P2 (S21 Pass C backlog — the last SVG-stage keyboard gap; both now closed)** — the combined-sheet crop marquee (cut a letter/mark out of a "one image, all letters" or combined mark-sheet upload) was pointer-only: `startCrop` bound via `pointerdown` on `#cropImg`, sized by pointermove, committed by pointerup→`finalizeCrop`. Added a dedicated crop-mode keydown handler (`workMode==='align' && isCropSelect(curLetter())`): arrows **move** a draft box (Shift = fine, 2 vs 10 stage px), `+`/`−` **resize** symmetrically (±12, clamped to the sheet rect + min size), **Enter/Space** crop, **Esc** cancel; the first key seeds a centred ~40% box. Draft lives in `cropDrag.kbd` (no `start` field) and renders through the existing `cropSelectSVG` `cropDrag.box` path — so the pointer move/up crop branches now require `cropDrag.start` to ignore it; `_setWorkMode` clears the draft on step change. An early `isCropSelect` guard keeps the align image-nudge handler from acting in crop mode. **A crop is non-undoable by design** — `l.source` is in `ITEM_SKIP` (excluded from undo snapshots, preserved live on restore), so `reselectArea()` is the re-crop affordance and the keyboard path matches the pointer path (no `udDo`; the initial plan's "add undo" step was dropped after discovering ITEM_SKIP). Hints (stage legend + letter/mark align panels) and the `?` cheat sheet ("Crop a letter from a sheet") document the keys. FONT_MAKER_VERSION 3.7→3.8 + changelog. | Verified headless (light+dark × 1280/800, real combined-sheet fixture → `isCropSelect` true): ArrowRight seeds the draft + renders the gold marquee `<rect>`; move +10 / Shift +2 / left-edge clamp to `f.ox`; `+`/`−` grow/shrink w by ±12; Esc clears (`cropDrag===null`); Enter → `finalizeCrop` sets `source.dataUrl`+`cropRect`, `isCropSelect` now false; `reselectArea()` reverts it (crop mode returns); the **pointer** path (`cropDrag` with `start` → `onStagePointerUp`) still finalizes — no regression from the guards; cheat sheet renders the crop group (Plus/Minus kbds); 0 pageerrors.
- [x] 2026-07-08 | (S25 close-out) | branch/deploy note | S25 ran on branch `claude/session-5-w827m0` (per session instructions; origin/main already had the merged S24 work). Ships as a **draft PR**, not a direct push to `main`. `sw.js` VERSION v223→v224 (precached files changed: Hebrew_Font_Maker, torah_trainer). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | (S25 iter 2) | torah_trainer.html | **P3 (S24 Pass F backlog — the last main content area without a friendly empty-state)** — `#ttReading` was blanked to `''` when no parsha/reading is loaded (both `renderText()`'s no-verses branch and `fetchAndRender()`'s `!r`/no-ref branch, which previously set only a status-bar line). Added a shared `renderEmptyReading()` card (mirrors the generator's `.empty-state`: decorative `aria-hidden` ✡︎ + "Pick a parsha to start" + a pointer at the dropdown), called from both sites; the `!r` branch also nulls `currentVerses` first. Static card, no animation → reduced-motion block unchanged. Dark mode recolors the heading `--navy`→`--gold-light`, like the generator. (Rarely seen — the page auto-defaults to the current week's parsha — but the initial pre-fetch and any no-ref state now show it.) | Verified headless (light+dark × 1280/800): card present + visible (offsetHeight>100) in both themes, h2 = "Pick a parsha to start", star `aria-hidden`+opacity 0.4, h2 color `--navy` light (rgb 26,39,68) / `--gold-light` dark (rgb 245,217,122), and the card returns when re-emptied; 0 pageerrors. (The unchanged non-empty render branch overwrites the card when verses load.)
- [x] 2026-07-08 | (S25 iter 1) | Hebrew_Font_Maker.html | **P2 (S21 Pass C backlog — keyboard node selection in Edit-points mode)** — points could only be *selected* by pointer (`.node-pt` pointerdown / background marquee), so the existing arrow-nudge / Delete / smooth-corner support in `nodes` mode was unreachable keyboard-only. Added `,`/`.` to cycle the single selection through every node in flat contour order (wrapping; picks first/last from an empty selection) and `i` to split the segment after the selected node at its on-curve midpoint (`_bezEval` t=0.5 → `insertNodeAt`, the keyboard twin of Alt-click). Both route through the existing `udDo`/`insertNodeAt` path, so undo + dirty + autosave are intact; no new project-mutation path. Registered in the `?` cheat sheet (Point-editing group) and the Edit-points hint line. FONT_MAKER_VERSION 3.6→3.7 + changelog. (Crop-box keyboard gap still open — separate P2.) | Verified headless (light+dark × 1280/800, square-polygon fixture → 4-node model): `.`/`,` cycle & wrap the selection (empty→0:0, →0:1, ,→0:0, ,→wrap 0:3), ArrowRight then nudges the keyboard-selected node by +5 (the whole point — previously impossible), `i` inserts a point (4→5, new node selected); 0 pageerrors.
- [x] 2026-07-08 | (S24 close-out) | branch/deploy note | S24 continued on branch `claude/session-4-1owrd5`, adding to open **draft PR #65**. `sw.js` VERSION v222→v223 (10 root HTML pages changed). Pages deploy runs on PR merge.
- [x] 2026-07-08 | aed9072 | 10 root pages + CLAUDE.md | **P3 (S24 Pass F — dark-mode toggle convergence, all 11 pages).** Every page already had dark mode + the shared `hebrewBlender_darkMode` key + a no-flash script; converged the toggle on the Font Maker model: **(a)** `aria-pressed` on all toggles (was Font-Maker-only), synced in `toggleDark` + on-load init; **(b)** `aria-label="Toggle dark mode"` on the icon-only toggles (torah/dashboard/generator; flash_cards already had it); **(c)** Light glyph standardized to `☀️` (was bare `☀` on index/resources/404/dictionary) + removed resources' stray trailing `;`; **(d)** *(user opt-in)* OS-aware first-visit default suite-wide — the 9 light-default pages now honor `prefers-color-scheme` when no explicit choice is saved (head IIFE **and** on-load resolver both use the same condition → no flash-to-light); explicit choice still wins. CLAUDE.md dark-mode baseline updated (OS-aware IIFE + aria-pressed). | Verified headless (all 11 pages × light/dark OS emulation): aria-pressed flips in lockstep with `body.dark` + persists; fresh+dark-OS → dark; saved `'0'`+dark-OS → light (explicit wins); saved `'1'`+light-OS → dark; aria-label on icon-only; glyph `☀️`; 0 pageerrors.
- [x] 2026-07-08 | (S24 Pass F audit, no code) | 6 tools | **Toast/aria-live consistency CLEAN** — every status/toast surface carries `role="status"` (implies aria-live): generator `#appToast`/`#previewNotice`, Font Maker `#statusMsg`, torah `#ttHintToast`/`#ttStatus`, dictionary `#loadingText`, shared `#ivritStatus`. Divergence is coverage only (3 tools lack a general non-blocking toast) — logged as candidates, not an a11y gap. `alert()`/`confirm()` counts match CLAUDE.md §5 (no drift). Empty-states: all tools have friendly/adequate ones except torah's bare `#ttReading` (logged P3).
- [x] 2026-07-08 | (this session) | README.md + CLAUDE.md | P3/P4 (S23 Pass E doc-drift) — the S21 Font Maker a11y work wasn't mirrored into the docs: README's Font Maker blurb said "(Currently v3.5.)" while `FONT_MAKER_VERSION` is 3.6 (fixed → v3.6, the only user-facing version string), and CLAUDE.md's `lang="he"` **Implemented on:** snapshot listed only 5 tools while Font Maker gained `lang="he"` on its glyph tiles in S21 (added `Hebrew_Font_Maker.html` (glyph tiles) — code backs it: 5 `lang="he"` in the file). Docs-only: **no `sw.js` bump, no sitemap regen** (README/CLAUDE aren't precached; no page content changed). | Verified by grep: README `Currently v3.5` count 0 / v3.6 present matching source; CLAUDE list includes Font Maker; `git diff --stat` = README + CLAUDE only.
- [x] 2026-07-08 | (S23 Pass E audit, no code) | site-wide | **Pass E mechanical checks CLEAN** — sitemap `<loc>`↔files + `<lastmod>` vs git (idempotent, current: 8 weekly pages+index 2026-07-07, privacy+contact 2026-07-06); `sw.js` `CORE_ASSETS` both directions (VERSION v222, all 21 paths exist, no precacheable root file missing; og-card/zelle-qr deliberately runtime-cached, data/ in DATA_CACHE); all internal links + `@font-face` + 18 splash hrefs + data fetches resolve; THIRD_PARTY_LICENSES covers all 6 bundled TTFs + all CDN hosts; robots/CNAME/manifest → ivritsuite.com with existing icons. Nothing broke since S19; S20/S21/S22 edits were page-content-only.
- [x] 2026-07-07 | (S22 close-out) | branch/deploy note | S22 continued on branch `claude/session-4-1owrd5`, adding to the already-open **draft PR #65** (not merged → no branch restart). `sw.js` VERSION bumped v221→v222 (precached files changed: hebrew_blend_generator, flash_cards). Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-07 | 46fec2f | hebrew_blend_generator.html | **P1 SECURITY (S22 Pass A — stored/reflected XSS in the Real Words drills)** — the `ea03110` "consume saved Word Lists" feature routed untrusted words (a `?s=` share link that **auto-runs `generate()`**, imported `ivritSuite_wordLists`, paste-in custom lists) into every drill's `innerHTML` with **no `esc()`**. Fixed at the render sink (CLAUDE.md rule 2): `colorizeHebrew()` now escapes its input (covers matching + gematria Hebrew), and every direct word-field interpolation is `esc()`-wrapped across all drills — reading/vocab/reverse/idpos, **identify** (incl. the compound `reveal ? w.pos : ''`), word-search bank, **crossword** clues (`e.clue`) + bank, **gematria** (`b.translit`/`b.translation`), **bingo** cards (`cell.*`) + caller. `esc()` is a no-op on clean Hebrew/translit so trusted blend/letter/number renders are unaffected. **The rigorous payload test found 4 sinks my first pass missed** (crossword clue, bingo cell, identify pos, gematria translit/translation) — end-to-end verification, not code-reading, is what closed them. | Verified headless (light+dark × 1280/800): injected `<img onerror>` via the share-link, imported-list, AND wordLists vectors through **all 10 drill types** — no img node, no execution, payload rendered as escaped text, legit Hebrew still renders + colorizes; colorizeHebrew escapes with color-coding on and off; 0 pageerrors. (Verify note: `rwCount` is a `<select>` of 10/20/30/40/50 — an off-list value silently yields an empty deck; the corpus-`all` source defers to an async fetch the offline harness blocks, so drive via `imported`/`lists`.)
- [x] 2026-07-07 | 22efcc1 | flash_cards.html | P3 hardening (S22 Pass A, from the sweep's PLAUSIBLE lead) — `esc()` on the color-/emoji-mode card `innerHTML` sinks (`card.translit`/`romanized`/`english`/`emoji`). **No reachable exploit found**: the untrusted word-list/share path (the "words" sub-mode) was **already escaped** (`esc(card.translit)` + `textContent` translation + `colorizeHebrew` heb), and these sinks currently render trusted data (fixed color names, the emoji lexicon, transliterator output). Kept as cheap defense-in-depth (no-op on clean data) consistent with the generator fix. | Verified headless (light+dark): payload in card.translit/romanized/english/emoji renders escaped in words + colors + emoji modes; 0 pageerrors.
- [x] 2026-07-07 | (S21 close-out) | branch/deploy note | This session ran on branch `claude/session-4-1owrd5` (per session instructions) and ships as a **draft PR**, not a direct push to `main`. `sw.js` VERSION bumped v220→v221 (precached files changed: Hebrew_Font_Maker, hebrew_dictionary, torah_trainer, classroom_dashboard). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-07 | 0487d47 | Hebrew_Font_Maker.html | **P3 ×3 (S21 Pass C — the last un-audited tool, now audited)** — grouped a11y fixes: (1) shared 2px gold `:focus-visible` ring on `.ltile`/`.wtab`/`.ctab`/`.tbtn`/`.mode-btn`/`.ctl-btn`/`.hbtn` (only `.hint-link`/`.lchip` had one before); (2) `lang="he"` on the Hebrew glyph-tile chokepoints (letter/punct/custom/precomp grids + placement-nav neighbor glyph); (3) `aria-pressed` on the on/off toggles (dark, Default/Custom, Per Symbol/Combined, English upper/lower, mark-editor mode), synced with `.active`. FONT_MAKER_VERSION 3.5→3.6 + one combined changelog entry. Attribute/CSS only — no `project` mutation (undo/dirty contract untouched). Pass C confirmed already-GOOD (not re-flagged): grid tiles (`a11yTiles`/`_a11yDecorateTile`), tablists (`_syncTablist`/`_tabKeydown`), ALL modals via `aOpenModal`/`aCloseModal` (focus-in / Tab-trap / Escape / restore — CLAUDE.md claim verified true), the universal-selector RM block, `#nextStepHint` aria-live, most icon labels. Logged 2 P2 SVG-stage keyboard gaps (node selection, crop-box) + 1 P4 dead handler as new Candidates. | Verified headless (light+dark × 1280/800): 0 pageerrors, 56 `lang="he"` nodes (Hebrew confirmed), 2px gold ring on all 7 selectors both themes, dark + English-case `aria-pressed` flip correctly, meMode static aria present.
- [x] 2026-07-07 | 491e106 | hebrew_dictionary.html | P4 (S21, closes the S12 RM-gaps candidate) — the RM block forced CSS `scroll-behavior:auto`, which does NOT override an explicit `behavior:'smooth'` in JS. Added `_scrollBehavior()` (reduce→'auto' else 'smooth') and routed the 3 smooth JS scrolls through it: `scrollToShoreshWord`, filter-chip reveal, and the mobile Filters button (the last not in the original ledger entry). Ledger correction: `_tourShow` passes **no** `behavior` — already non-smooth, a false positive. Also added the uncovered caret transitions (`.vg-arrow` inline, `.panel-title::after`, `.adv-section-title::after`) to the RM block via `transition:none !important`. | Verified headless (light+dark): `_scrollBehavior()` returns smooth/auto per media state, `scrollToShoreshWord` passes it through, caret transition-duration 0.2s normally / 0s under reduce; 0 pageerrors.
- [x] 2026-07-07 | 71ac52b | torah_trainer.html + classroom_dashboard.html | P4 (S21, closes the S20 tooltip-contrast candidate) — the low-contrast `.tip-icon` "?" badge (grey `--muted` glyph on the `--border` circle, 3.36:1 light) existed in only these two carriers (dictionary/generator already use white-on-`--muted`; flash_cards has no badge — the ledger's "5 byte-identical carriers" premise was inaccurate). Treated the "?" as text: light glyph → dedicated warm-grey literal `#4a4436` (5.28:1 on #c8bfa8); dark glyph pinned to `--muted` on `--warm-gray` (already 5.02:1). Tokens untouched; the two rules kept in sync. | Verified headless (both files × light+dark): light 5.28:1, dark 5.02:1; 0 pageerrors.
- [x] 2026-07-07 | 315fea9 | hebrew_dictionary.html | P4 (S20, closes the S12 leftover) — `.shoresh-pat.filled` jump-to-word tiles were click-only divs; each now gets tabindex/role=button/Enter+Space (action buttons, deliberately no aria-pressed — they're not toggles like the `bindLptA11y` filter tiles) + the page's standard 2px gold `:focus-visible` ring; empty tiles stay non-interactive. | Verified headless (light+dark × 1280/800, real `setDictMode('shoresh')` + `runShoreshLookup('כתב')`): 6 filled tiles wired + visible, Enter/Space scroll+flash the right word card exactly once, mouse path unregressed, real-Tab focus shows the 2px ring; 0 pageerrors. (Test-harness note: bare `enterShoresh()` leaves the view hidden — `setDictMode('shoresh')` is what flips `body.shoresh-mode`.)
- [x] 2026-07-07 | ddb6230 | torah_trainer.html | P3 (S20, closes the S18 grouped small-a11y candidate) — (a) `<main>` landmark around toolbar+reading plus a hidden-until-focus "Skip to the reading" link targeting a now-`tabindex="-1"` `#ttReading`; (b) all three renderers emit `lang="he"` on the Hebrew verse containers (`.tt-heb` interlinear/biblepage, `.tt-cell heb` side-by-side); (c) karaoke active word now carries `aria-current="true"` via `paintKaraokeIdx`/`clearKaraokeHighlight` (every paint site routes through them — verified by grep); **aria-live deliberately omitted** — per-word announcements every ~0.5 s would spam; (d) RM block now neutralizes the drawer slide, panel caret rotation, and skip-link transition. | Verified headless (light+dark × 1280/800 × RM/normal): one `<main>` containing controls+reading; first Tab = visible skip link, Enter focuses #ttReading, link re-hides on blur; every Hebrew word inside `lang="he"` in all 3 layouts; paint on/off/clear sync aria-current with `.active`; RM durations 0s vs intact normally; drawer opens/closes both modes; rover suite re-run green; 0 pageerrors.
- [x] 2026-07-07 | 0a5d491 | torah_trainer.html | P3 (S20, closes the S18 contrast candidate) — measured every small `--muted` suspect headless (alpha-composited over real backgrounds, both themes, 1280+800): legends/jump labels/FAQ answers/sublabels/row labels/aliyah dividers/status lines/font attributions ALL PASS at 5.2–6.2:1 (the ledger's ~4.0 estimate was wrong, as in the generator precedent). The real failures were both in `.radio-group` (4 groups: toolbar layout picker + 3 in settings): dark unselected labels (`--muted` on the `#2a3349` track) 4.13:1, and light **selected** label (`--gold` on the white pill) **2.75:1**. Dedicated literals, tokens untouched: `#99a2b8` dark unselected → 4.92:1 (flash-cards dark precedent), `#8a6216` light selected → 5.47:1 (dashboard readable-gold precedent; dark selected stays gold-light at 11:1). | Verified headless (light+dark × 1280/800): all measured spots ≥4.5:1, radio click still switches layout; 0 pageerrors.
- [x] 2026-07-07 | 9790276 | torah_trainer.html | **P2 (S20, closes the top-of-backlog candidate)** — roving-tabindex keyboard access for verse words (`.tt-word`, the primary interactive reading surface, previously mouse-only): ONE Tab stop into the reading (the current rover word carries tabindex=0/role=button; the other ~2,000 words stay plain text for SR document reading), ArrowLeft/ArrowRight move it (RTL: left = next), Home/End jump, Enter/Space fire the same delegated `onReadingClick` action, a Hebrew-word click re-anchors the rover (translit clicks don't — guarded), `renderText` re-inits after every innerHTML rebuild, `.tt-word:focus-visible` gold ring added. Hebrew words only, by design — every translit word maps to the same `data-twi` action, so functional parity is complete. | Verified headless (light+dark × 1280/800, 26 checks/run): exactly one rover in all 3 layouts; arrow/Home/End walk incl. clamp-no-wrap; Enter+Space activate the same verse action as a mouse click (chant/read spies); rover follows Hebrew clicks, ignores translit clicks (transliterator stubbed to force spans); re-render resets cleanly; real-Tab reaches the rover with a ≥2px ring (post-`transition:outline` settle); 0 pageerrors.
- [x] 2026-07-07 | (S20 audit, no code) | all 11 pages | **Pass B console/error audit CLEAN** — all 11 root pages over local http, light-1280 + dark-800, load + one interaction per tool (generate, dictionary search, dashboard drawer open/close, flash-cards start+Space-flip, torah settings open/Escape, Font Maker letter select via `#letterGrid .ltile`, resources filter, contact focus, index gear modal open/close): 22/22 page-runs with 0 pageerrors and zero non-environmental console errors/warnings. Only output: the documented environmental noise (torah's blocked-Sefaria `fetchSefariaText` console.error — caught gracefully; transliteration ESM fallback warnings by design).
- [x] 2026-07-06 | b778de4 | torah_trainer.html | P3 (S19, two small a11y fixes from the S18 grouped candidate) — (a) the nikkud color-picker `<input type=color>` rows had no label association (vowel name was a sibling span only); added `aria-label='Color for '+def.name`. (b) the karaoke follow-scroll used hardcoded `scrollIntoView({behavior:'smooth'})`; now honors `prefers-reduced-motion` (`'auto'` when reduce), mirroring the file's jump-nav scroll. | Verified headless (light+dark × 1280/800): all 13 color inputs labeled + picker still writes `nikudColorOverrides`; scroll behavior `'auto'` under reduced-motion / `'smooth'` without (scrollIntoView spy); 0 pageerrors.
- [x] 2026-07-06 | c7f5da1 | README.md + CLAUDE.md | P3/P4 (S19 Pass E doc drift) — README's Font Maker blurb said "Currently v2.0" (actual `FONT_MAKER_VERSION` 3.5); CLAUDE.md's Font Maker note said "~9,900 lines" (actual ~10,350). | Verified by grep; the rest of the freshness audit was CLEAN.
- [x] 2026-07-06 | 0362ed9 | torah_trainer.html | **P2 (S18 Pass C)** — the settings drawer was a bare `<div>` with no dialog semantics or focus management: opening left focus on the page, Tab escaped behind the drawer, closing didn't restore focus (Escape already closed it). Now mirrors the dashboard pattern — `role=dialog`/`aria-modal`/`aria-labelledby` (+ `aria-label` on ×), focus moves to the close button on open, `_settingsTrapKey` wraps Tab inside the modal (added/removed on open/close), focus returns to the gear on close. | Verified headless (light+dark × 1280/800): dialog attrs + existing title target, focus-in on real gear click, Shift+Tab→last / Tab→first wrap, 30-Tab walk never escapes, Escape closes + restores to gear, × mouse path unregressed; 0 pageerrors; no non-environmental console errors (Pass B fold-in — only the expected aborted-Sefaria-fetch noise).
- [x] 2026-07-06 | 205fa11 | torah_trainer.html | P3 (S18 Pass C) — the 8 settings-drawer `.panel-title` collapse headers were click-only `<div>`s; each now has tabindex/role=button/aria-expanded + Enter/Space (aria synced on toggle), and `syncPanelTitleAria` keeps aria in sync through expand/collapse-all. Mirrors the dictionary `bindHdrA11y` pattern. | Verified headless (light+dark × 1280/800): all 8 wired, Enter/Space toggle + aria, mouse path synced, expand/collapse-all sync all 8; 0 pageerrors.
- [x] 2026-07-06 | 6bd1b7b | generator + dashboard + flash_cards + torah_trainer | **P2 security (S17 Pass A, finding 2.1)** — the four vowel-color tools interpolate a resolved nikud color straight into a `style="color:…"`/background/underline span built via innerHTML. That color can come from `settings.nikudColorOverrides`, only guaranteed hex when set via the `<input type=color>` picker; on import (`.ivrit`/AllTools/share code) `ivritSafeAssign` blocks prototype pollution but not value strings, so a crafted override (`#000;"></span><img src=x onerror=…>`) breaks out of the attribute → stored XSS via the documented cross-teacher sharing surface. `getNikudColor` (all four) + torah's `colorFor` closure now accept an override only when it matches `/^#[0-9a-fA-F]{3,8}$/`, else fall back to the trusted default — the single chokepoint feeding every span. Legit picker output is always hex, so no real data changes. | Verified headless (4 tools × light+dark × 1280/800): a breakout payload is neutralized (falls back to a hex default, no `<img`/`onerror`/`">` in output) while `#ff0000` is still honored; torah no-override colorize byte-identical to the S16 baseline; 0 pageerrors.
- [x] 2026-07-06 | (this session) | index.html | P4 (S17 Pass A, finding 4.1) — `hebrewFontMaker_inputMode` added to `eraseAllSettings` (its sibling `hebrewBlender_inputMode` was already there; same UI-flag class — erase-only, export-exempt). | Verified headless: erase clears both inputMode flags + data keys, preserves the per-device `hebrewBlender_zoom`; 0 pageerrors.
- [x] 2026-07-06 | 6ce8b3c | torah_trainer.html | P4 (S16 Pass D) — `colorizeHebrew` called `getNikudColor` per colored syllable, and `getNikudColor` reads `document.body.classList.contains('dark')` every call — measured **5,572 DOM reads for a ~2,000-word reading**, re-run on every settings/nav change (`fetchAndRender`, 13 sites). The dark flag + active defaults map are invariant per word; resolved once per `colorizeHebrew` call via a local `colorFor()` closure that returns exactly what `getNikudColor(key)` would. `getNikudColor` left in place for the color-picker path. Reads dropped 5,572 → 2,000 (one per word). Absolute cost small (colorize 6–38 ms desktop / 28–38 ms throttled — not a >200 ms block); value is removing the per-syllable DOM read on every render. | Verified headless (light+dark × 1280/800): colorize output **byte-identical to a clean pristine baseline** in both themes (212,019 chars); highlight/underline modes + `nikudColorOverrides` still honored; color-picker `getNikudColor` path unchanged; 0 pageerrors. (Verify note: the profiler's `DOMTokenList.prototype.contains` spy injected stray SOH chars into big returned strings — a harness artifact; the real parity baseline is captured spy-free.) Also measured, logged not fixed: full-reading `renderText` innerHTML build (candidate above); `aliyah.json` 436 KB parse trivial (1.2–5.5 ms).
- [x] 2026-07-06 | 4a72aaa | flash_cards.html | P4 (S15 Pass D) — `generateCard()` rebuilt its emoji candidate pool (a `forEach` over all ~240–275 emojis, filtered by selected subs/gender) on every call, inside two hot loops: `generateDeck` (up to n*30 draws) and `sampleChoices` (listening-mode distractors, up to n*25 on every card advance). The pool depends only on the selection, not the draw. `buildDeckContext()` now precomputes it once per loop and threads it via `generateCard(ctx)`; ctx-absent callers build their own pool unchanged. Measured: per-deck emoji builds 20→1; the pool and resulting card set are identical. Absolute cost was small (emoji generateDeck(20) 4.7–14.2 ms — NOT a >200 ms block); value is removing per-advance redundant work on the drill's core loop, esp. low-end devices. | Verified headless (light+dark × 1280/800): ctx pool byte-identical to the inline build; pool built 1×/deck (was 1/draw); deck still 20 unique valid cards drawn only from the selected set; `sampleChoices` returns valid deduped choices incl. the answer and builds the pool ≤1×; standalone `generateCard()` unchanged; 0 pageerrors. Also measured (logged, not fixed): letters path `buildPool` ×3/card negligible (1.3–5.9 ms); `hebrew_words.json` parse 12–48 ms (2.6 MB — confirms S13, not a block); emoji parse 0.9–3.7 ms.
- [x] 2026-07-06 | 3bf4bbd | classroom_dashboard.html | P3 (S14 Pass D) — the per-second clock tick rebuilt the schedule preview's `innerHTML` every second with Schedule Sync on, though its content (schedule entries + active period index) changes only on an edit or period switch: measured **299 of every 300 writes redundant** (identical HTML). On a projector running for hours that's ~21,600 needless innerHTML writes + layout invalidations/day. `renderSchedulePreview` now derives a content key and skips the rebuild when unchanged (mirrors the existing `_lastScheduleKey` memo); the countdown line + Now/Next block, which legitimately count down each second, are untouched. Absolute cost was small (0.06 ms/tick desktop, 0.25 ms throttled — not a >200 ms block); the value is eliminating continuous DOM churn on a long-running display. | Verified headless (light+dark × 1280/800): 300 same-state ticks → 1 preview write (was 300); guarded HTML byte-identical to a full reference render (output parity); re-renders correctly on period switch (currentIdx change), schedule edit, and disable→hint; Now/Next still writes each tick; 0 pageerrors. Also measured this session: `applySettings(DEFAULT_PRESET)` 3.3 ms desktop / 20.9 ms throttled (survey overestimated — NOT a block, no action); cold-load one 452 ms longtask at 4× throttle only (desktop max 109 ms — logged as P4).
- [x] 2026-07-06 | a3507c0 | hebrew_blend_generator.html | P3 (S13 Pass D) — `renderWorksheet` re-transliterated the entire ~13,082-word `REAL_WORDS` corpus + custom list on every render whenever a non-default translit style was set, though the blend worksheet only renders `blends[]` (≤~100). `rebuildTranslitDisplay(scope)` now takes a scope; the render path passes `'blends'`, the rare data-change sites (style change, lib load, corpus load, custom import, applySettings) keep the full rebuild. Measured render-path `HT.apply` calls dropped 13,121 → 40 (~328×). | Verified headless (light+dark × 1280/800, stubbed HT to force non-default style offline): render made exactly `blends.length` apply calls (not 13k), blends `translitDisplay` byte-identical between scoped and full rebuild (output parity), real-words drill still shows corpus transliteration (corpus maintained by its own change sites), full rebuild still populates all 13,081; 0 pageerrors. Profiling floor: full rebuild 119 ms at 4× throttle with a cheap stub (real SBL lib heavier); cold-load corpus parse+flatMap measured clean at 17–21 ms desktop / 98 ms throttled (not a block); Class-Set 40 render logged as a separate candidate (210 ms desktop first-run / 368 ms throttled, ~8,000 cells).
- [x] 2026-07-06 | 373b455 | hebrew_dictionary.html | P3 (S12 Pass C) — card action icons met only 1.8–2.5:1 (the `--muted` glyph at resting opacity 0.4 on `.wc-copy/.wc-search/.wc-speak`, 0.55 on `.ec-speak`); resting opacity now 0.75 → measured 3.5:1 light / 3.45:1 dark (≥3:1 non-text minimum), hover-to-full/gold unchanged. The rest of the contrast candidate measured ALREADY PASSING (every `--muted` text suspect 5.0–6.2:1 in both themes — `.panel-subhead`, vowel sublabels, `.result-count`, `.wm-translit`, `.ec-translit`, `.ec-tag`, `.color-mode-btn`, `.search-clear`) — token untouched, no text changes. | Verified headless computed-contrast (alpha-composited over the real card bg) before/after in both themes; 0 pageerrors.
- [x] 2026-07-06 | b532e86 | hebrew_dictionary.html | P3 (S12 Pass C) — icon-only card controls got accessible names: `.wc-copy`/`.wc-speak`/`.wc-search` (aria-label matching each title) and the emoji-card `.ec-speak` template; same pattern as the word modal's `wm-mini` speakers. | Verified headless (light+dark): labels on every rendered instance in words + emoji modes; speak click still calls speechSynthesis exactly once; 0 pageerrors.
- [x] 2026-07-06 | 7168b74 | hebrew_dictionary.html | P3 (S12 Pass C) — focus visibility: removed the replacement-less `outline:none` from `.sort-sel`/`.dict-tool-btn` and added the file's standard 2px gold `:focus-visible` ring to them + `.word-card`/`.emoji-card`/`.lpt`/all collapse headers. | Verified headless via CDP-forced `:focus-visible` (light+dark × 1280/800): 2px solid gold ring on all 8 target shapes — NOTE `.lpt` has `transition:all 0.12s`, measure ≥400 ms after forcing or you read a mid-transition 1px outline; 0 pageerrors.
- [x] 2026-07-06 | 6d83604 | hebrew_dictionary.html | P3 (S12 Pass C) — all collapse/section headers keyboard-operable: 5 `.panel-title`, 6 `.adv-section-title`, 7 `.pos-sec-hdr`, vowel-group headers, `.emoji-cat-arrow` (aria-label + checkbox untouched), `.shoresh-patterns-title` — `bindHdrA11y` (tabindex/role=button/aria-expanded, Enter/Space, keydown guarded against the inner ✕ button) + `syncCollapseAria()` at every non-click writer (expand/collapse-all, mobile first-collapse, chip reveal, tour expand/restore). | Verified headless (light+dark × 1280/800): 52 checks — every shape toggles via Enter/Space with aria synced, ✕ guard, bulk/tour writers sync, emoji checkbox unaffected, shoresh via real `runShoreshLookup`; 0 pageerrors.
- [x] 2026-07-06 | 4157ecc | hebrew_dictionary.html | P2 (S12 Pass C) — the seven `.lpt` letter-filter grids (6 positional + any-letter, 224 tiles) were bare onclick divs, the core letter filter unreachable by keyboard; `bindLptA11y` (tabindex/role=button/aria-pressed + Enter/Space) on both builders, aria re-synced in `clearPos`/`clearAnyLetters`/`applyDictState` via `syncLptAria`. | Verified headless (light+dark × 1280/800): Enter/Space toggle class+aria+filter-Set and change the live result count, mouse path unregressed, bulk clears + applyDictState sync aria; serialize→apply→serialize round-trip byte-identical; 0 pageerrors.
- [x] 2026-07-06 | (S12 audit, no code) | hebrew_dictionary.html | Pass C confirmed CLEAN: word/emoji cards already role=button/tabindex/keydown; word modal has focus-in/trap/Escape/restore + backdrop close; bindTip tooltip engine (this file is its source) sets aria-expanded/describedby; tour saves/restores focus with Escape/arrows; all filter checkboxes native label+input; RM block covers pulse/tour-spot/load-bar/shoresh-flash; no `.slider` switches exist here (40018c0 N/A).
- [x] 2026-07-06 | 7855edc | classroom_dashboard.html | P4 (S11, closes the last backlog candidate) — tap-to-read day/weather text keyboard-operable: `_wireSpeakNode` (tabindex/role=button/Enter+Space→click) applied inside `renderDaysList`/`renderWeather` at the click-wiring spots (nodes rebuilt every render → re-wired fresh, gated by the same `tapToReadDow`/`tapToReadWeather` flags); `:focus-visible` gold outline CSS added for the four node types (page has no global focus default). | Verified headless (light+dark × 1280/800): nodes wired ON / bare OFF, Enter/Space speak the right text (speakDash spied), click path unregressed, attributes + operability survive re-render, 2px solid ring via CDP-forced :focus-visible in both themes; 0 pageerrors.
- [x] 2026-07-06 | 0d63af0 | index.html | P3 (S10 Pass A finding) — `eraseAllSettings` cleared only 2 of the 10 one-time UI flags; the other 8 added (generator tourSeen/welcomeSeen, dashboard tourSeen/setupSeen, torah tourSeen/hintSeen, Font Maker tourDone/mobileWarnDismissed) per the "erase means fresh start" guidance. Flags stay export-exempt; per-device prefs + Font Maker project data untouched. | Verified headless (light+dark × 1280/800, confirms stubbed): all 10 flags cleared, presets cleared, zoom/darkMode survive; 0 pageerrors.
- [x] 2026-07-06 | 408d382 | classroom_dashboard.html | P4 (Pass C leftover) — spoiler reveal keyboard-operable: delegated keydown on `#dashTextDisplay` (Enter/Space toggle, inert while editing, mirroring the delegated click) + `normalizeSpoilers()` re-applied after every display innerHTML write (spans are recreated each write) sets tabindex/role=button/aria-pressed; aria synced in both toggle paths; sanitizer still strips the attrs from persisted HTML. | Verified headless (light+dark × 1280/800): focus/Enter/Space toggle + aria, click path unregressed, attrs survive re-render, editing guard inert, persisted HTML clean; 0 pageerrors.
- [x] 2026-07-06 | c4a222d | classroom_dashboard.html | P4 (Pass C leftover) — first-run modal restores focus to the drawer's Quick-start opener on dismiss (skip/starter/Escape); the auto first-visit open (no opener) blurs the modal input instead of leaving focus on a hidden control. | Verified headless (light+dark × 1280/800): all three dismiss paths restore to `#starterLayoutsBtn`; auto path dismisses clean; 0 pageerrors.
- [x] 2026-07-06 | 0ae2477 | hebrew_blend_generator.html | P4 (Pass C leftover) — focus-loss nits: `toggleSidebar` hands focus to the visible twin button (was display:none-ing the activated one, dropping focus to body; guarded on activeElement so the tour's programmatic call steals nothing); `setDageshTav` re-focuses the equivalent rebuilt tile by grid + letter (tav ↔ dagesh-tav equivalent), no-op off-grid. Third nit (preset-row DnD mouse-only) closed NO-ACTION: the folder-tree "Move ▾" menu is the documented authoritative keyboard/touch path. | Verified headless (light+dark × 1280/800): hide/show handoff both ways, programmatic no-steal, alef/tav/rw-grid refocus, off-grid no-steal; 0 pageerrors.
- [x] 2026-07-06 | 1d1114e | hebrew_blend_generator.html | P3 (closes the contrast candidate) — dark `.rw-drill-btn` text was 4.52:1 on #252d3e (zero AA margin at 0.72rem); dedicated literal #99a2b8 → 5.39:1 (flash-cards `.sub` dark precedent), `--muted` untouched. Rest of the candidate measured ALREADY PASSING (the ledger's `--muted`+opacity guess was wrong — `.name` inherits `--text`): tile-name subtext 6.60/8.93 light, 7.37/5.26 dark; light drill buttons 6.15. | Verified headless computed-style (post-transition — `.letter-tile` has `transition: all 0.15s`, measure after ~400ms or you read mid-transition values): all six pairs ≥4.5 with margin; 0 pageerrors.
- [x] 2026-07-06 | 962002c | hebrew_blend_generator.html | P3 (Pass C) — removed the replacement-less inline `outline:none` from `#presetBackupBox`; keyboard focus ring restored. | Verified headless (manual input mode revealed): focused textarea shows the default outline; 0 pageerrors.
- [x] 2026-07-06 | 3dc347d | hebrew_blend_generator.html | P3 (Pass C) — 11 panel titles, 8 `.rw-section-header`s, and the vowel-group collapse headers (initVowels + custom-blend copy) were click-only divs; all now tabindex/role=button/aria-expanded with Enter/Space, tour expand/restore aria-synced. | Verified headless (light+dark): counts, Enter/Space toggles + aria on all three header shapes; 0 pageerrors.
- [x] 2026-07-06 | 3fec30b | hebrew_blend_generator.html | P2 (Pass C) — all six letter-tile grids (main, 3 lock, real-word, custom-blend ×2 builders) were click-only divs, the flash_cards defect unfixed here. Seven tile-creation sites now go through `bindTileA11y` (tabindex/role/aria-pressed/Enter+Space); bulk All/None/No-Sofit paths re-derive aria via `_syncTileAria` hooked into the `updateCombosCount`/`updateRwPoolInfo` chokepoints they already call. | Verified headless (light+dark × 1280/800): 31 main tiles wired, Enter/Space toggles state+aria+Set, bulk paths sync aria without clicks, rw/lock/cw grids wired, getSettings unregressed; 0 pageerrors.
- [x] 2026-07-06 | 662bc5c | classroom_dashboard.html | P3 — schedule-builder rows gained Move up/down buttons (aria-labeled, edge-disabled); focus follows the moved row so repeated Enter keeps walking it; drag reorder unchanged. | Verified headless (light+dark, schedule sync enabled): ABC→BAC→ABC via buttons, edge no-op, focus-follow, rows still draggable; 0 pageerrors.

- [x] 2026-07-06 | 341e4e8 | classroom_dashboard.html | P3 (Pass C) — student-picker overlay (`#pickOverlay`) now moves focus to its close button on open and restores the opener on close (Escape already worked). | Verified headless (light+dark): open → focus on ✕, Escape closes, focus back on the opener; 0 pageerrors.
- [x] 2026-07-06 | 26f2da3 | classroom_dashboard.html | P3 (Pass C) — light-mode gold text on cream failed AA (~2.6:1): the always-on holiday countdown line and dashboard-text links now use a dedicated darker gold literal (#8a6216); measured 5.47:1 light / 11.0:1 dark; `--gold` token untouched. | Verified headless computed-contrast both themes; 0 pageerrors.
- [x] 2026-07-06 | 295f6ff | classroom_dashboard.html + CLAUDE.md | P3 (Pass C, closes the documented caveat) — `wire()` tooltips now set `aria-expanded` (synced through show/hide incl. the one-open switch) and `aria-describedby="tipFloat"`, completing the canonical bindTip contract on all five tooltip carriers. CLAUDE.md caveat replaced with the completion note. | Verified headless (light+dark): wiring on all 3 icons, Enter/Space/Escape + aria sync, one-open aria handoff; 0 pageerrors.
- [x] 2026-07-06 | 78d3aff | classroom_dashboard.html | P3 (Pass C) — all collapse headers keyboard-operable: 13 drawer panel/sub-section titles, the 3 always-on column headers, and the video panel header (were click-only divs). tabindex/role=button/aria-expanded synced at every writer (click, Enter/Space, tour expand/restore, preset appliers). | Verified headless (light+dark × 1280/800): Enter/Space toggles + aria on all four header classes, applier-driven changes sync aria without a click; 0 pageerrors.
- [x] 2026-07-06 | a0e04fa | classroom_dashboard.html | P2 (Pass C) — settings-drawer focus management: focus moves to the close button on open, Tab wraps inside the modal (30-Tab walk never escapes behind the backdrop), focus restores to the gear on close; drawer carries role=dialog/aria-modal/aria-label. Escape already worked. | Verified headless (light+dark × 1280/800): real gear click, focus-in, Shift+Tab/Tab wrap, Escape + focus restore, ✕ mouse path unregressed; 0 pageerrors.

- [x] 2026-07-06 | 016295c | (deploy recovery, no code) | Session 6's pending deploy recovered: after two GitHub-side deploy-step failures (runs 28792937467/28793090167, build+artifact green each time), a re-trigger after the ~2h incident window succeeded — run 28802066980 concluded success and the live site serves sw v205. Playbook held: back off during the incident, re-trigger once clear.

- [x] 2026-07-06 | b7cfadd | hebrew_dictionary.html | P4 (closes the last Pass D remainder) — `onFilter` hot-loop precompute: immutable per-word search fields (`translitLower`/`translationLower`/`lenKey`/`vowelSet`/`hasVowel`) built once in the chunked flatten (derived from raw `translit`/`translation`/`word`, NEVER `translitDisplay` which mutates with translit style), `POS_KNOWN_SET` for O(1) membership, and the `checkedPos` 'other' test hoisted out of the per-word predicate. | Verified headless over local http: 14-state filter matrix (searches in Hebrew/translit/translation, length subsets, POS incl. 'other', era, positional letters, any-letter ± only, vowel exclusion, hideNoVowels) — counts AND first-12 word lists byte-identical before/after; steady-state onFilter at 4× CPU throttle 201 ms → 163 ms median (same-benchmark stash comparison); first-render task 250 → 203 ms; desktop profile unchanged; 0 pageerrors.
- [x] 2026-07-06 | 4236b7f | 6 uploader carriers + CLAUDE.md | P3 (Pass F) — the "My Fonts uploader" marked block also contained each page's pick handler (which legitimately differs on index + dashboard), making the byte-identity invariant unsatisfiable. End marker moved to cover only the truly-shared `ivUploadFontFromFile`; per-page handlers now sit below it with a comment. CLAUDE.md: uploader paragraph rewritten (per-page handlers incl. dashboard's `onUploadEngFontPick`), fonts-store carrier count corrected to 8, folder-tree section notes index.html's deliberate data-helpers-only usage (not dead code). | Verified: marked span sha-identical across all 6 files; all 6 pages load headless with the shared fn + handlers defined and the upload input present; 0 pageerrors.
- [x] 2026-07-06 | cfae257 | flash_cards.html + classroom_dashboard.html + CLAUDE.md | P3 (Pass F) — `.ivrit` engine re-synced to true byte-identity across the three tool pages: both files now carry the generator's block verbatim (they had dropped its 4 explanatory legacy-tolerance comments; the dashboard's copy also sat at a +2-space indent). CLAUDE.md's "byte-for-byte identical across all files" corrected: identical across the three tool pages, index.html a documented AllTools-adapted superset (extra `wlMergeIntoStorage`, legacy-blob payload, reload-hint alert) that must not be "fixed" back. | Verified: sha-identical across the 3 extractions; all 3 pages load with engine functions defined; `.ivrit` gather/apply round-trip on flash_cards; 0 pageerrors.
- [x] 2026-07-06 | (follow-up commit) | fonts/Reuben-LICENSE.txt + resources.html | P4 (Pass E follow-up; CC0 confirmed by the user) — Reuben now has file-level license parity with TzviScript: committed `fonts/Reuben-LICENSE.txt` (CC0 1.0 dedication, Baruch Sienna, made-with-IvritSuite line, matching the generated-notice wording) and the gallery entry gained `licenseFile` so downloads ship it verbatim. sw v204 (resources.html precached). | Verified headless (Playwright over local http, real `downloadFont`): Reuben.zip contains the committed license byte-for-byte; 0 pageerrors.
- [x] 2026-07-06 | d02fbde | CLAUDE.md | P3/P4 (Pass E) — AllTools table gained rows for `dictLastState` and the previously-undocumented `wordLists` (`ivritSuite_wordLists`); SW section now documents the version-independent `DATA_CACHE` behavior. | Verified: every `IVRIT_CFG.gather()` key is now documented (the 5 folder keys are covered by the folder-tree section under their localStorage names).
- [x] 2026-07-06 | 3afca1f | README.md | P3/P4 (Pass E) — guided-tour line updated to all six tools (was missing Flash Cards) and the `fonts/` description now lists the TzviScript files. | Verified by grep vs the 6 tour engines and `ls fonts/`.
- [x] 2026-07-06 | 0e9070d | THIRD_PARTY_LICENSES.md | P2 (Pass E) — removed the false "no font binaries are redistributed except Libre Baskerville" claim and itemized the four bundled TTFs with their coverage: Frank Ruhl Libre (OFL, `fonts/OFL.txt`, sw-precached), Reuben (Baruch Sienna, CC0 per the Resources gallery entry + generated download notice), TzviScript ×2 (author-made, OFL, own license files). FRL's table row notes the bundling. | Verified: every `fonts/*.ttf` filename now appears in the doc; the false claim greps to zero.
- [x] 2026-07-06 | f08e175 | flash_cards.html + CLAUDE.md | Feature (user decision, closes the 2026-07-05 Pass F Info item) — added the standard gtag snippet (byte-identical to the generator's) and the GA origins to the page CSP (script-src/img-src/connect-src, mirroring the generator); stale "no Google Analytics" CSP comment removed; CLAUDE.md allowlist note updated. | Verified headless (light+dark × 1280/800): snippet parity with the generator, all three CSP origin sets present, dataLayer + gtag() stub work with the remote script aborted, 0 pageerrors.
- [x] 2026-07-06 | (no code) | Hebrew_Font_Maker.html | Info item closed — the tooltip-less design (tour + shortcuts sheet + title attributes instead of a `data-tip` layer) is confirmed intentional by the user (2026-07-06). Divergence is by design; do not port the tooltip engine.
- [x] 2026-07-06 | 7bcd5f7 | Hebrew_Font_Maker.html | P4 — `markDirty()` added to the five view setters (`setTemplateOpacity`/`setTemplateShow`/`setImgOpacityAlign`/`setImgOpacityTrace`/`setGlyphOpacity`): their fields serialize into save/export/autosave (`JSON.stringify(project)`; `stripImagesForAutosave` keeps them), so an opacity-only change could be silently lost on crash/reload. Pure-UI pattern (dirty + autosave, no undo entry). FONT_MAKER_VERSION 3.5 + changelog. | Verified headless (light+dark): each setter updates its project field AND sets `_projectDirty`; no undo entries pushed; 0 pageerrors.
- [x] 2026-07-06 | 7ef380c | hebrew_dictionary.html | P4 (Pass D part (a)) — chunked the 13k-word corpus flatten into 2000-word setTimeout batches; loading UI held up and `_wordsReady`/`maybeBootstrap` deferred to their own task after the final batch (first render walks the complete array). | Verified headless over local http (desktop + 4× CPU throttle, longtask observer): no flatten task ≥200 ms even throttled (largest batch ~110 ms; was one 457 ms block); the corpus `JSON.parse` measured separately at ~74 ms throttled (not a problem); the only remaining ≥200 ms throttled task was positively correlated (function-wrap instrumentation) to the pre-existing first-render `onFilter` — rescoped as its own Candidate. Desktop unchanged; 13,081 words; identical search result counts; grid/loading states correct; 0 pageerrors.
- [x] 2026-07-06 | 5d33e76 | index.html + CLAUDE.md | P3 — backed up `hebrewDictionary_lastState` (the documented "known gap": erased but never exported/restored). Registered as `dictLastState` in exportAllSettings/importAllSettings + AllTools `IVRIT_CFG.gather/apply` (ivritSafeAssign merge; empty blob = never-set, skipped). Portability confirmed first: pure filter/view state, `q` stripped at write, `applyDictState` fully defensive. CLAUDE.md known-gap note updated. | Verified headless: export/erase/import round-trip, `.ivrit` gather/apply round-trip, empty-blob no-clobber, and a restored state actually re-applies in the live dictionary on load (sort/length/era/dageshTav all re-applied; first attempt used illegal test values — gem-sort requires gematria mode and len values are strings — corrected and re-run green); 0 pageerrors.
- [x] 2026-07-06 | 73cfb7d | index.html | P4 (Pass A, JSON-LD parity) — removed "Privacy Policy" from the "IvritSuite Tools" ItemList (a policy page listed as a tool) and corrected `numberOfItems` 8 → 7. | Verified headless: JSON-LD parses, numberOfItems matches entry count, positions contiguous 1..7, no privacy entry; 0 pageerrors.
- [x] 2026-07-06 | 94b1d14 | Hebrew_Font_Maker.html | P4 (Pass A, destructive-bulk) — `applyOutlineToAll` now confirm-gated like its sibling `resetTropSideColumns` (it clobbers every letter's outlineOnly/lineWeight; was undoable but unconfirmed). FONT_MAKER_VERSION 3.4 + changelog. | Verified headless (light+dark): cancel preserves per-letter values + pushes no undo entry; confirm applies + one entry; undo reverts; 0 pageerrors.
- [x] 2026-07-06 | de3424d | index.html | P3 (Pass A, localStorage-vs-AllTools) — registered `hebrewBlender_hebFont` / `_hebFontSize` (shared Generator+Dictionary display prefs) and `hebrewBlender_livePreview` in exportAllSettings/importAllSettings/eraseAllSettings and the AllTools `IVRIT_CFG.gather/apply`. Empty string = never-set, skipped on import (can't clobber a configured machine). | Verified headless: export/erase/import round-trip + `.ivrit` gather/apply round-trip + never-set no-clobber; generator + dictionary apply a restored font pref on load (`--heb-font` = Ezra SIL); 0 pageerrors.
- [x] 2026-07-06 | b1d2290 | resources.html + contact.html | P3 — added the `@media (prefers-reduced-motion: reduce)` blocks (last two animated pages without one): global neutralizer + `transform:none` for `.resource-card:hover` and its card-link arrow; `#fontToast`'s static centering transform untouched. | Verified headless (light+dark × 1280/800 × RM/normal): durations ~0 under RM, hover lift suppressed, toast transform intact, normal mode unchanged; 0 pageerrors.
- [x] 2026-07-06 | 40018c0 | flash_cards + generator + dashboard + torah_trainer | P3 — visible keyboard focus for the ~180 toggle switches (the hidden zero-size checkbox swallowed the browser focus ring): `input:focus-visible + .slider` outline rule added per file's selector shape (`.switch` / `.toggle` / bare). | Verified headless (4 files × light+dark × 1280/800): focused switch shows a ≥2px slider ring (after the `.slider` `transition:0.2s` settles in dashboard/torah — the ring fades in there by design), no ring when blurred, mouse toggle unregressed; 0 pageerrors.
- [x] 2026-07-06 | ba33911 | Hebrew_Font_Maker.html | P3 (falsy-zero, completes the sweep's known remainder) — the 4 `l.advance || default` center-X midpoint fallbacks (`seedAnchors`, `ensureTropAnchors`, `qaAnchors`, `anchorMidX`) now use the `!= null` pattern from `effLetterAdvance`, so a stored `advance === 0` is honored. FONT_MAKER_VERSION 3.3 + changelog. | Verified headless (light+dark × 1280/800): `anchorMidX`/`seedAnchors` return 0-midpoint for advance=0; `advance: null` still gets the default; `qaAnchors` runs clean; 0 pageerrors.
- [x] 2026-07-06 | 2b8c989 | flash_cards.html + index.html | P3 — guided-tour port (last of the 6 tools without one): generator engine copied ("keep the engines in sync") with flash-cards steps (Mode, Letters, Vowels, Teacher Tools, Advanced, Start), header "❓ Tour" pill with first-visit pulse gated by `hebrewFlashCards_tourSeen` (added to index.html `eraseAllSettings`; export-exempt per flag guidance); `_tourOpenSidebar` dropped (no sidebar); `_tourExpandPanel`/`tourEnd` keep panel `aria-expanded` synced via `_syncPanelAria`. | Verified headless (light+dark × 1280/800): button/arrow/Escape navigation, step skip on card screen (tour ends gracefully; card Space-flip unaffected after teardown), pre-tour collapse state + focus restored, `getSettings()` byte-identical before vs after a full run, pulse gating both ways, reduced-motion steady-ring + no spot transition, erase clears the flag; screenshots of steps 1/4; 0 pageerrors.
- [x] 2026-07-06 | 125d25f | flash_cards.html | P3 (Pass C finding) — `.sub` help text now has dedicated colors (#5f6a80 light / #99a2b8 dark) instead of the shared `--muted` token; measured 5.44:1 / 6.20:1 (was 3.09 / 4.24). Token untouched for its ~40 decorative uses. | Verified headless: worst visible `.sub` contrast ≥4.5 in both themes at both widths (6 elements sampled after expandAllMenus), `.label` regression check unchanged; 0 pageerrors.
- [x] 2026-07-06 | 1202369 | flash_cards.html | P3 (Pass C finding) — the 8 `.panel-title` collapse headers are keyboard-operable: tabindex/role=button/`aria-expanded` (initialized from the mixed markup state), Enter/Space toggles, and aria stays synced through all three collapse writers (`initPanelCollapse` toggle, `expandAllMenus`, `collapseAllMenus`) via new `_syncPanelAria`. | Verified headless (light+dark × 1280/800): Enter/Space toggle + aria sync, panel-body display flips, mouse path synced, expand/collapse-all sync all 8; 0 pageerrors.
- [x] 2026-07-06 | 7fbce56 | flash_cards.html | P3 (Pass C finding) — removed the dead settings drawer (never openable — no code added `.open`; its ✕ and dark toggle were tab-reachable while aria-hidden/off-screen): markup, CSS block, `initDrawer` + init call, and the `darkToggleDrawer` sync refs in `toggleDark`/`initDarkMode`. Its only control duplicated the header `#darkToggle`. | Verified headless (light+dark × 1280/800): zero `drawer` refs remain (grep = 0), no drawer DOM nodes, header dark toggle still flips/persists/updates icon, tab walk lands on no hidden controls (the flagged `.switch` inputs are the standard visually-hidden-checkbox pattern inside visible labels — logged separately as a focus-visibility candidate); 0 pageerrors.
- [x] 2026-07-06 | (close-out commit) | CLAUDE.md | P3 doc drift — accessible-tooltip section: flash_cards no longer "the laggard" (adopted the pattern this session), removed the unverified dashboard `aria-expanded` claim (replaced with a verify-note; `wire()` doesn't set it); reduced-motion section: list corrected to 8 files incl. flash_cards + index, privacy.html noted as a no-op (zero animations). | Verified by grep against the live files this session.
- [x] 2026-07-06 | cdbfa72 | Hebrew_Font_Maker.html | P3 (undo-wiring, completes the sweep's known remainder) — `toggleGuides` now wraps in `udDo([{t:'guides'}])` like its 8 sibling guide mutators; `savePlacement` wraps its `placementSaved`/`placementInherited`/`savedAnchors` writes in `udDo([itemScope()])` (savedAnchors stays snapshot-exempt per `ITEM_SKIP`, so an explicit `markDirty()` is kept for that baseline write; module-level `lastSavedAnchors` carry-forward is deliberately outside the entry). FONT_MAKER_VERSION 3.2 + changelog. | Verified headless (Playwright, light+dark × 1280/800): toggle → 1 labeled undo entry, undo restores `guides.show`; confirm → 1 entry, undo reverts flags while savedAnchors survives (ITEM_SKIP design); identical re-confirm pushes no junk entry (no-op slice dropped); 0 pageerrors.
- [x] 2026-07-06 | d858a5f | sw.js | P3 — precache `404.html` (was the only root HTML absent from `CORE_ASSETS`, so the custom 404 was never available offline). The navigate-fallback-to-index behavior is deliberate and unchanged. | Verified: `node --check` on sw.js; CORE_ASSETS parsed and every entry exists on disk incl. /404.html; 404.html loads headless with 0 pageerrors (light+dark).
- [x] 2026-07-06 | a6def5a | index.html | P3 — added the `@media (prefers-reduced-motion: reduce)` block (global neutralizer in flash_cards' shape + explicit `transform:none` for the `.page-card` hover lift). | Verified headless (Playwright `emulateMedia`, light+dark × 1280/800): reduced → transition durations ~0 and hover transform `none`; normal → durations and the −2px lift intact; 0 pageerrors.
- [x] 2026-07-06 | 331c654 | flash_cards.html | P2 (Pass C finding) — core setup controls were click-only `<div>`s; added `bindTileA11y` (tabindex=0, role=button, `aria-pressed` synced to `.selected`, Enter/Space activates) to the 31 main letter tiles, the per-position lock-grid tiles, and the 13 vowel tiles; vowel-group collapse headers got tabindex/role/`aria-expanded` + Enter/Space. | Verified headless (light+dark × 1280/800): Enter/Space toggles state + aria-pressed + `getSettings()`; real Tab reaches tiles; group header collapse/expand via keyboard + mouse; lock-grid tiles exercised with lock enabled; mouse clicks unregressed; 0 pageerrors.
- [x] 2026-07-06 | c5ae6c7 | flash_cards.html | P2 — replaced the hover/click-to-pin `tooltipIIFE` with the shared accessible bindTip engine (from hebrew_dictionary.html) adapted to `.has-tip` triggers + `#tipFloat`'s `.show`/opacity model: tabindex/role/`aria-expanded`/`aria-describedby`, Enter/Space toggle, Escape/blur/outside-click/scroll close, mouse-only pointerenter, viewport-clamped, no auto-hide timer. The one native-`<button>` trigger (emoji-gender) is hover/focus-only so its own click action stays single-bound. | Verified headless (light+dark × 1280/800): focus/Enter/Space/Escape/blur behavior + aria-expanded sync, hover show/hide, viewport clamp, emoji-gender click still toggles `.active` exactly once; 0 pageerrors.
- [x] 2026-07-05 | 61cb083 | resources.html + fonts/ | Feature (user-requested) — added the **TzviScript** font (Block, nikkud, OFL; by Harrison Bleiberg, made with the Font Maker) to the downloadable fonts gallery: committed `fonts/TzviScript.ttf` + `fonts/TzviScript-LICENSE.txt` and appended a `FONTS` entry. Also taught `downloadFont` to ship a per-font committed `licenseFile` verbatim when present (fallback to the generated notice), so this OFL font bundles the full OFL text rather than the CC0-only stub. sw v192; no CSP/CORE_ASSETS change (self-hosted, runtime-cached); THIRD_PARTY_LICENSES left as-is (user-made Font-Maker fonts aren't covered there, per the Reuben precedent). | Verified headless (Playwright over a local http server so fetch works): card shows Block/Nikkud✓/Trop✗/OFL + TzviScript preview family; Block/Nikkud/OFL filters include it and CC0 hides it; real downloadFont ships the committed OFL LICENSE.txt byte-for-byte; 0 pageerrors (desktop+800) + screenshot. Note: the font's own glyph coverage is partial (preview shows tofu for some sample letters) — flagged to the user. Follow-up: the OFL header's Reserved Font Name, originally the Maker default "Block Regular", was corrected to "TzviScript" at the user's request (sw v193). Follow-up: linked the "Harrison Bleiberg" author credit to https://harrisonbleiberg.wpcomstaging.com/ via the entry's `authorUrl` (sw v194).
- [x] 2026-07-05 | f2b0e71 | Hebrew_Font_Maker.html | UX (user-reported follow-up to v2.9) — the placement size sliders were reverted from the side-by-side row back to a vertical **stack** (one per line), and the ✓ Confirm placement + Reset letter/Reset all buttons were moved into their own bordered **panel to the right** of the sliders (#stageBelow = two-column flex: sliders+hint left, actions panel right; wraps on narrow screens). FONT_MAKER_VERSION 3.0, sw v191. | Verified headless (Playwright): 3 stacked `.size-slider-line`s in `.size-slider-col`, `.anchor-confirm-panel` to the right with Confirm+Reset, old `.size-slider-row`/`.anchor-confirm-foot`/divider gone, under-strip = Apply only, Fix-A undo intact, 0 pageerrors (light+dark, desktop+800) + screenshots.
- [x] 2026-07-05 | 67c66e0 | Hebrew_Font_Maker.html | UX (user-reported) — the Place Nikkud panel mixed control scopes with one dense hint; regrouped under scope-explicit headers ("Qamats — every letter" for Height/Edit shape; "Below position — Alef only" for X/Y; trop groups Height/Horizontal/spread under "— every letter") with one short hint per section. FONT_MAKER_VERSION 2.9. | Verified headless: header order in below+trop, dense hint gone, Height (tropTune) & X/Y (per-letter) undo intact, 0 pageerrors + screenshots.
- [x] 2026-07-05 | c598821 | Hebrew_Font_Maker.html | UX (user-reported) — size sliders below the canvas stacked side-by-side on one row with vertical dividers (shortened 74px bars, compact labels). | Verified headless: 3 cells one row, dividers on cells 2+, slider undo burst intact, 0 pageerrors + screenshot.
- [x] 2026-07-05 | 1474723 | Hebrew_Font_Maker.html | UX (user-reported follow-ups to the v2.8 layout) — placement canvas fills the center width again (dropped the 560px cap + the .mode-anchors hook), and a letter's controls are grouped below it: size sliders → divider → ✓ Confirm placement + Reset letter/Reset all (Confirm pulled from the right column, Reset from the under-strip; under-strip = Apply only). sw v189. | Verified headless (Playwright): #stageBelow order sliders→hr→Confirm→Reset, under-strip Apply-only & no Confirm/Reset, right column no Confirm, canvas max-width 100%, Fix-A undo intact, 0 pageerrors (light+dark, desktop+800) + screenshot.
- [x] 2026-07-05 | 9544fde | Hebrew_Font_Maker.html | UX (user-reported) — compact the Place Nikkud/Trop layout: size sliders moved directly below the canvas (#stageBelow), ✓ Confirm placement + pill moved to the bottom of the right column, under-strip slimmed to Apply + Reset, placement canvas capped to 560px via `.ws-grid.mode-anchors` (anchors-only, Trace/Align unchanged). FONT_MAKER_VERSION 2.8. | Verified headless (Playwright): DOM placement of sliders/Confirm/under-strip, 560px cap in anchors with no leak into trace, #stageBelow clears off-mode, relocated size slider keeps its readout + Ctrl+Z undo, 0 pageerrors (light+dark, desktop+800px) + screenshot.
- [x] 2026-07-05 | 7db38d0 | Hebrew_Font_Maker.html | Bug (user-reported) — Place Nikkud "above" class: the picker now drives the on-canvas preview (`activeVowelCp` returned a hard-coded holam, so selecting Rafe / Shin dot / Sin dot did nothing); and Shin-only dots are hidden from the above-mark grid on non-Shin letters, clamping a stale pick to holam. | Verified headless (Playwright, real setters): Shin shows all 4 marks, Tsadi shows Holam+Rafe only, Rafe selection sets activeVowelCp=05BF, 0 pageerrors.
- [x] 2026-07-05 | faefe8b | index.html + CLAUDE.md | P2 — register `hebrewFlashCards_settings` in AllTools (export/import/erase + `.ivrit` gather/apply); the only tool settings blob missing from backup, so restore on a new device silently dropped all flash-cards live config. | Verified headless (Playwright): round-trips through both the export/import textarea path and the `.ivrit` gather/apply path, and cleared by erase; 0 pageerrors (light+dark, desktop+800px).
- [x] 2026-07-05 | eaa8be7 | Hebrew_Font_Maker.html | P2 — make the 5 mark/trop size sliders undoable (undo-wiring): route `setMarkScale`/`setDageshScale`/`setCholamScale`/`setHolamScale`/`setTropScale` through `udBurstBegin([{t:'tropTune'}])` + `onchange="udBurstCommit()"`, and extend the `tropTune` undo snapshot/restore to actually capture the 5 `*Scale` fields (it previously snapshotted only tropSideOffset/halign/markNudge, so the undo entry wouldn't have reverted a size). FONT_MAKER_VERSION 2.6→2.7 + changelog. | Verified headless (Playwright): each setter's undo reverts the scale, one entry per drag (burst coalescing), readouts update, 0 pageerrors (light+dark, desktop+800px).
- [x] 2026-07-23 | (S140 close-out) | branch/deploy note | S140 ran **Pass H** (teacher walkthrough / paper-cuts, stalest since S130) on **trope_tutor.html** (2nd H on it [S87/S140]) — role-played a teacher learning + drilling the trope names end-to-end. **Mature / well-onboarded — 0 clean same-session fixes** (like the S130 generator H); logged 1 feature seed + 1 P4. Walked: Learn (intro explains te'amim + links Torah Trainer; 6 family chips, sensible "Sof Pasuk = home base" start; rich cards — glyph, Ashk/Seph names, motif staff, 4 spread-picked examples w/ ▶, mastery ✓) → settings (tradition ashk/seph radios default ashk, all 3 drill types on, rate 1.0×, font size — all discoverable) → Drill ("10 questions per session", types summary names the ⚙ path, 26-cell mastery grid; Q flow clear — prompt/stem/4 choices/feedback names the correct answer + Replay + Next, no auto-advance, no dead-end). 0 pageerrors. **Findings:** (1) **feature seed** — the "See it in the parsha →" link is a constant `torah_trainer.html` (L1787) ignoring the card's known `ex.p`/`ex.ref`, so a teacher re-navigates; a `?ref=` deep-link would fix it but needs torah_trainer param support + is Sefaria-unverifiable headless → logged, deferred. (2) **P4** — the outside-loop motif staff has no visible caption/legend/intro-mention (aria-labeled only) → a bilingual copy add needs maintainer Hebrew → logged. **Iters 1/5** (0 open P1/P2/P3, all P4-gated; both H findings gated — deep-link Sefaria-unverifiable, staff-copy needs Hebrew). **Drift:** none since S137 — `origin/main` @ 67b3d85, PR #126 open/unmerged (branch 4 ahead); `sw.js` v389, FM 5.5. No precached file changed → no SW bump / no sitemap. Branch/PR: continues on `claude/improve-loop-8ges97` / draft **PR #126** (now S137–S140). Deploy `success` verified after merge.
- [x] 2026-07-23 | (S141 close-out) | branch/deploy note | S141 ran **Pass B** (console & error audit, stalest since S131) — **12th run; effectively CLEAN, 1 clean non-gated fix taken.** Chosen timing was ideal: B last ran S131, predating a large **outside-loop Torah Trainer drift** (see Drift) that no pass had touched. **Method:** (1) 24 load runs = 12 production pages × light/dark over local HTTP, route-aborting externals, capturing pageerror/console/failed-local/`[i18n] miss`; (2) a **synthetic-reading interaction audit** of Torah Trainer — stubbed `fetchSefariaText` to inject a 3-verse reading with divine names (Elohim + Tetragrammaton) + cantillation, then exercised all **4 new outside-loop features** end-to-end: `detectShem` (→ true, shem-note + geniza-row show), Student Handout (print stubbed; body-class cleanup verified), per-verse copy, bulk select-and-copy (selectAll → textarea + count, toggleBulkVerse, close-bar-exits-bulk-mode), and the 3 copy-format toggles (ref/translation/transliteration through their real change handlers); (3) Trope Tutor `?debug=motifs` — 5 staves render, 7× family-cycle with stable DOM (171 nodes), staff audition + startDrill; (4) basic interaction on the other 5 tools. **Result: 0 pageerror / 0 non-noise console / 0 failed-local / 0 `[i18n] miss`** across every run — the only console output is torah_trainer's documented offline Sefaria/ESM artifacts (external, route-aborted). **1 fix (c100ed4):** the S137 residual — generator answer-key "…Teacher Copy" subtitles printed faint gold on B/W (inline `color:var(--gold)` at 11 byte-identical sites escaped the print-darkening rule). Added a `ws-ak-subtitle` hook (one `replace_all`, inline gold kept → screen byte-identical both themes) + a scoped print-only `!important` darken; verified screen L=150/171 unchanged, print #6b4e12 (L=80) every config. Variety-appropriate (generator's only prior recent touch was S137; ≤2/tool respected). **Iters:** 1 pass (B) + 1 fix = **2/5** (stopped — rest of backlog gated: trope motif-staff caption + FM tooltip i18n both need maintainer Hebrew, dashboard `:focus-visible` parked, perf items parked, dictionary theme = fenced `data/` project). **Drift (large, all outside-loop user-directed):** since S140, **PR #126 MERGED** (the trope→torah "See it in the parsha" deep-link, was v389→v390), then 4 direct-to-main Torah Trainer feature batches landed — Student Handout + שם ה׳ divine-name warning (v390→v391), per-verse copy + bulk-copy mode (v391→v392), copy-bar transliteration toggle (v392→v393), bulk-select visible checkbox (v393→v394); FM still **5.5**. All of this new surface audited clean here. **Branch:** PR #126 merged → per protocol a **fresh branch `claude/improve-loop-decisions-cdxt2f` off latest `origin/main`** (`8f2e273`) + a **new draft PR** at close-out (not a continuation). **SW: v394→v395** (generator precached file changed in the fix). No `FONT_MAKER_VERSION` bump (no FM change). `check-i18n` clean (no new UI strings — a CSS class + print rule only); `update-sitemap` run (generator content changed). Deploy `success` verified after merge. **Stalest next: Pass E** (freshness & site-health, last S132 2026-07-22).
- [x] 2026-07-24 | (S142 close-out) | branch/deploy note | S142 ran **Pass L** (SEO & discoverability audit — **first-ever run**, registered 2026-07-23 as never-run → stalest). **Static-source audit of all 11 indexable pages** (offline node extraction of `<title>`/description/canonical/OG/Twitter/JSON-LD/`<h1>`/`<img>` + headless crawl-graph). **Mostly clean; 2 clean fixes taken** (h1 dedup non-gated; SEO copy trim gate-2-approved). **Findings:** titles/descriptions all unique (0 dupes); canonicals all correct incl. homepage bare-root; 404/i18n-test correctly noindex & sitemap-excluded; OG/Twitter present (og:image resolves to real files — og-card.png?v=3 + og-fontmaker.png); **JSON-LD all parse + claims match UI (CLEAN — L now owns this re-check, moved from A2)**; sitemap = exactly the 11 indexable URLs, sitemap↔canonical agree; robots.txt intent OK; crawl-graph = all 10 non-home pages reachable from index via static `<a href>`; all real content `<img>` have alt (2 "noalt" hits were false positives inside JS comments). **Iters 3/5** (Pass L + h1 dedup + SEO copy trim; stopped — remaining backlog P4-parked/gated). **Gates:** gate-1 (micro-feature) — **no open feature seeds**, none built; gate-2 (SEO copy) — asked, maintainer chose "Apply all 4 as drafted." **Drift:** none un-ledgered — dictionary theme project + POS-aware panel (PR #129) already in the ledger; `sw.js` v399→v400 (POS-aware) already landed; skill-update commit e164bd5 (pass L registration) already reflected in the rotation table. **`sw.js` v400→v401** (4 precached pages changed: privacy, contact, torah_trainer, trope_tutor). **FONT_MAKER_VERSION** unchanged (no FM change). **Scripts:** `check-i18n.js` clean (no NEW violations; 14 baselined); `update-sitemap.mjs` run as last step. **Branch:** previous PR #129 merged → S142 on the assigned fresh branch `claude/improve-loop-68agmk` off latest `origin/main` (+ the pre-existing pass-L skill-update commit) → **new draft PR** at close-out. Deploy `success` verified after merge.
- [x] 2026-07-24 | (S143 close-out) | branch/deploy note | S143 ran **Pass E** (freshness & site-health, stalest since S132) via a **7-dimension fan-out workflow** — each dimension adversarially verified + a completeness critic (10 agents, 0 errors, ~904K subagent tokens). **6/7 dimensions CLEAN with reproducible evidence; 1 doc-only fix taken.** (1) **sitemap↔git:** all 11 `<lastmod>` = file's last-commit date, `<loc>` set = exactly the 11 indexable pages, 404/i18n-test excluded. (2) **SW precache forward:** all 25 CORE_ASSETS resolve on disk, VERSION v401 valid. (3) **SW precache reverse:** every app-shell file listed; documented omissions correct (og-card/og-fontmaker/zelle-qr/splash PNGs/`/data/` DATA_CACHE/download-gallery fonts); no new shell asset since S132. (4) **internal-links:** 402 static local refs + 7 `fetch('data/…')` paths + the local `@font-face` all resolve (the 7 unfiltered hits = JS-template false positives). (5) **licenses:** all 7 bundled fonts documented w/ sibling license files; all CDN libs + remote services acknowledged. (6) **robots/CNAME/manifest:** all present + parse; manifest icons resolve; `.nojekyll` present. (7) **docs-accuracy → 2 findings, both fixed (c2169fb).** **Critic surfaced 3 process notes, all verified CLEAN (no code fix):** (a) **sw.js-VERSION-vs-precached-file staleness** — no precached file committed after the v401 bump (the most-missed real E failure mode) → **now formally owned by the sw-precache dimension going forward**; (b) **`?v=` cache-key reconciliation** — `manifest.webmanifest?v=3` resolves to the bare precached entry via sw.js `caches.match(req,{ignoreSearch:true})` (the `/data/` branch matches WITHOUT ignoreSearch by design); (c) the 2nd docs finding (i18n-test locator) was reconciled + fixed here, not dropped. **Iters 2/5** (Pass E + doc fix; stopped — rest of backlog P4-parked/gated, no clean non-gated variety-appropriate win). **Gates:** none (doc-count fixes aren't gate-2; no open feature seeds → no gate-1). **Drift:** `origin/main` advanced `eb5c5d6`→`e164bd5` out-of-band = the pass-L skill-update commit landing on main (non-precached skill files only, no code/version change); my branch a clean 3-ahead; PR #131 diff = the S142 SEO work. `sw.js` v401, FM 5.5 unchanged. **No SW bump** (CLAUDE.md not precached); **no sitemap change** (no page content changed); `check-i18n` clean. **Branch:** PR #131 open/unmerged → S143 **continues** on `claude/improve-loop-68agmk` / draft **PR #131** (now S142 + S143), no restart. Deploy `success` verified after merge. **Stalest next: Pass F** (cross-tool consistency, last S133 2026-07-22).
- [x] 2026-07-24 | (S144 close-out) | branch/deploy note | S144 ran **Pass F** (cross-tool consistency, stalest since S133) — affordance: the **shared-block byte-identity re-audit** (F's founding check, blocks last sha-verified 2026-07-10, since overrun by the largest outside-loop drift window: FM v5.0→5.5, 4 Torah batches, dictionary theme, trope motifs). Executed as an **enumerate → per-block sha audit → adversarial verify → completeness critic workflow** (14 agents, 0 errors, ~1.19M subagent tokens). **Enumeration:** 11 blocks (incl. undocumented single-carrier markers + expected-divergent lineages). **Byte-identity re-trued:** app-toast JS+CSS (3+3), folder-tree JS (4 incl. index), ivritsuite-fonts (**9/9**), uploader (**7/7**), .ivrit engine (3-way + index documented-deliberate variant), TROPES taxonomy (trope_tutor ↔ build-trope-index.mjs). **2 real drifts found & fixed:** folder-tree CSS (c93d529) + bindTip comment (c2647a1). **Critic corrected 3 audit rows** (ivrit-engine wrongly lumped index into the identical set; guided-tour-engines is a documented per-file lineage [7 carriers, 2 unmarked — dashboard/dictionary], not an identity block; bindTip pair was NOT identical) — all reconciled here, none dropped. **New P4 logged:** unmarked `.ivrit-*` CSS block (marker-or-exempt decision). **Doc-truth:** sha dates refreshed + resources.html reclassified (18029d5). **Iters 4/5** (pass + 3 fixes; stopped — rest of backlog P4-parked/gated). **Gates:** none tripped (mechanical re-unification + internal docs; no open feature seeds). **Drift:** none — `origin/main` still @ e164bd5; PR #131 open → continued on `claude/improve-loop-68agmk` (now S142+S143+S144). **`sw.js` v401→v402** (3 precached pages changed: generator, dashboard, trope_tutor). FM 5.5 unchanged (no FM edit). `check-i18n` clean; `update-sitemap` run last. Deploy `success` verified after merge. **Stalest next: Pass K** (i18n audit, last S134 2026-07-22).
- [x] 2026-07-24 | (S145 close-out) | branch/deploy note | S145 ran **Pass K** (i18n/localization audit, stalest since S134 — 8th run). **check-i18n fully CLEAN:** Check A no new violations (14 baselined printed-output literals), Check B no CSV debt + no untranslated-attribute backlog. Probed the Check-A template-literal blind spot over surface changed since S134 (S135 dashboard a11y, design-call batch, dictionary theme project, Torah feature batches, frozen-footer generator, #132): **1 cluster found + fixed** (dashboard Jewish-calendar projected widgets, 3a3fd85); rest CLEAN (dictionary theme labels all `data-i18n`; FM residual tooltips cleared S141; no template-literal `.title`/aria/placeholder or toast/alert English in the 4 hottest files). **Iters 3/5** (1 pass + 2 fixes) — also resolved the S144 P4 `.ivrit-*` CSS candidate via option (b) doc note (4d086db, user-directed "DO b"); rest of backlog P4/gated. **Drift vs S144 close-out:** prior loop PR #131 (S142–S144) MERGED; outside-loop **#132** (dashboard solemn-day holiday banner 🕯️, maintainer) merged → `sw.js` v402→v403, FM 5.5 unchanged. **Branch/PR:** #131 merged → fresh branch `claude/improve-loop-ajbvkw` off `origin/main` @2ab0197; new draft PR at close-out. **SW v403→v404** (classroom_dashboard.html precached, edited). **FONT_MAKER unchanged** (no FM work). Scripts: check-i18n (clean), update-sitemap. **Gates:** none deferred (gate-1: no open feature seeds → no micro-feature; gate-2: not triggered — the i18n wiring completes maintainer-staged keys with English wording byte-identical, not a copy/default-behavior change). Commits gpgsig-SSH signed + committer noreply@anthropic.com (local %G?=N is the known SSH-verify artifact — matches prior on-`main` loop commits).
- [x] 2026-07-24 | (S146 close-out) | branch/deploy note | S146 ran **Pass C** (accessibility, one tool — stalest since S135) on **torah_trainer.html** (dedicated C last S18 + a 2nd pass; first C over the S141 outside-loop feature growth). **a11y-strong; 1 systemic fix.** Empirical headless AX audit (accessible-name enumeration + real-keyboard Tab + contrast calc + synthetic-reading injection to render verse controls without Sefaria): the NEW S141 surface is fully accessible — Student-handout + Copy disclosures carry `aria-expanded`/`aria-controls` (toggled correctly) + `role=group`/`aria-labelledby`; verse pills are real `<button>`s with localized contextual `aria-label`s; bulk-select checkboxes localized-labeled; focus visible (2px keyboard outline / UA `outline:auto` ring); contrast AA both themes (5.6–14:1); reduced-motion present; 0 pageerrors both themes. **The one gap: the settings drawer had 6 nameless controls** (S135 dashboard pattern) → fixed c1cd264. Logged P4: `#ttCustomVEnd` placeholder-as-name. **Drift vs S145:** none — `origin/main` @2ab0197 (v403), PR #133 (S145) open+unmerged → S146 continues on `claude/improve-loop-ajbvkw`/PR #133 (no restart, no new PR). **SW v404→v405** (torah_trainer.html precached, edited). **FONT_MAKER unchanged** (no FM work). Scripts: check-i18n (clean), update-sitemap. **Gates:** none (a11y label-association is not a copy/behavior change; no open feature seeds → no micro-feature).

- [x] 2026-07-25 | (S158 close-out) | branch/deploy note | S158 ran **Pass A** (recurring-pattern sweep, stalest since S147) over the never-A-swept surface `0dc1e50..HEAD` (S148–S157). **Drift check: CLEAN** — `origin/main` still `b964295`, `sw.js` v414, FM 5.5, nothing outside the loop. **Branch/PR:** PR #135 was still **open and unmerged**, so per protocol S158 continued on the SAME branch `claude/improve-loop-z3sk75` and the same PR (no restart, no second PR). **`sw.js` v414→v415** (precached: `torah_trainer.html`, `flash_cards.html`, `hebrew_blend_generator.html`, `hebrew_dictionary.html`, `locales/ui-strings.csv` → rebuilt `en.json`/`he.json`). **`FONT_MAKER_VERSION` NOT bumped** — Font Maker untouched. **Scripts:** `build-locales.js` (3979→3981 keys; the lone empty-`he` cell `worksheet.layout.title_default` is unchanged pre-existing debt), `check-i18n.js` **clean**, `update-sitemap.mjs` last. **Gate 2 asked + both approved:** (a) flash_cards min/max wording → **"Minimum"/"Maximum"**, shipped as `shared.range.*`; (b) vowel-heading contrast → **"darken only the heading text"**, shipped across all 3 carriers. **Correction recorded against my own gate wording:** I told the maintainer dark mode was "already near-passing" — measuring against each group's *composited* tint instead of the flat panel showed dark red/blue/purple at **3.36–3.60:1**, so dark got the same treatment rather than being left failing on a claim that proved wrong. **Nothing deferred this session** — the backlog's blocked items were asked and cleared, except the S157 orphaned-keys decision, which stands and was deliberately NOT re-asked. Deploy `success` verified after merge.
- [x] 2026-07-25 | 03f7753 | flash_cards.html + hebrew_dictionary.html | **Vowel-heading contrast fixed in the two sibling carriers** (S158, completing the gate-2-approved decision). Both build the same headings from the same hues; CLAUDE.md requires vowel colour-coding changes to land consistently, so stopping at the generator would have left two tools with the identical defect. Both gain `headerText`/`darkHeaderText` + a mirrored `vowelHeaderColor()`; swatches/chips/borders keep the exact taught hue; the dictionary also loses the same `opacity:0.75`. **Value correction:** flash_cards' dark panels sit on a *lighter* surface, so 3 dark hues that cleared on the generator came in at 4.30–4.40 — red/blue/purple were nudged further (`#e87c7c`/`#779ce7`/`#b988dc`) and applied to **all three** files, so one value per hue clears on the lightest carrier and the tools don't drift. **Verified** with a probe that composites each group's own translucent tint (the generic sweep walks to the first opaque bg and reads the wrong backdrop for these nested boxes): 7/7 headings pass light+dark on generator and flash_cards; dictionary whole-page 4 dark failures → 0. Functional: worksheet generates, drill reaches card screen, dictionary sort re-renders. flash_cards' remaining 9 items are its `--muted` token (3.09:1) — logged separately, not this concern.
- [x] 2026-07-25 | 3672046 | hebrew_blend_generator.html | **Vowel-group headings made readable without losing their taught colour** (S158, gate-2 approved "darken only the heading text"). Each heading is painted in its group's hue and *names* that hue, so the colour is the lesson. Added `headerText` (light) + `darkHeaderText` (dark) per group — same hue nudged until it clears 4.5:1 — used ONLY for heading text; borders/tints/chips verified byte-identical before and after a dark round-trip. **Two measurement corrections:** the true ratios were worse than first quoted (compositing each group's own tint gives aqua **1.99**, yellow **1.91**, not 2.35/2.19), and the heading's `opacity:0.75` was dimming it further, so that was dropped. Both schemes get the fields per the CLAUDE.md both-schemes rule (TaL AM hides headings today, so its values are inert). **Verified:** all 7 pass light (4.61–5.14) and dark (4.62–5.58); page-wide light failures 8 → 0; dark toggle + Default/TaL AM switching still re-render (TaL AM 0 headings, default 7, 13 chips); worksheet still generates.
- [x] 2026-07-25 | 74c2fcd | flash_cards.html + locales/ui-strings.csv | **The suite's last 4 nameless controls named** (S158 — closing the S157 gate-2 candidate; maintainer chose "Minimum"/"Maximum"). Both pairs ("Range: [1] – [100]", "Word length: [1] – [20]") share ONE visible label with a bare "–", so naming from it alone would have given two controls the same name. Each input's `aria-labelledby` now composes its pair's label **plus** a min-or-max word → "Range: Minimum" / "Word length: Maximum". The two words live in **one visually-hidden span each** (aria-labelledby resolves by id, so all 4 inputs share 2 spans, not 4). Keys are deliberately `shared.range.*` not `flashcards.*` so the next min–max pair in the suite reuses them. Adds a `.vh` class beside the existing `.vh-file`, reusing the page's own `#a11yStatus` clip pattern. **Verified** in the real AX tree, EN + HE ("טווח: מינימום", "אורך המילה: מקסימום"); hidden spans measure 1×1; no dangling refs; drill still starts. **Suite now at 0 nameless interactive controls.**
- [x] 2026-07-25 | 790ca80 | torah_trainer.html | **19 colour-picker inputs announced a raw i18n key** (S158 Pass A — the sweep's one real finding, and a **2nd instance of the S152-registered pre-ready-i18n pattern**). `initColorPickers()`/`initTropeColorPickers()` set each input's `aria-label` via `I18n.t('torah.settings.color_for_aria', {name})` while building at init — **before `I18n.ready` resolves** — and `applyI18n()` rebuilt six other things but not these two lists, so the raw key stuck permanently. Hebrew-only (English was unaffected, which is why earlier passes missed it); the key itself always existed and was translated. **Invisible to both gates:** the runtime's `dictLoaded` warn-gate suppresses the pre-ready console warning, and `check-i18n` is static-only. Fixed by re-calling both builders in `applyI18n` alongside its existing siblings — both are pure re-renders. **Verified:** 24-run raw-key sweep 19 → 0; reads "Color for Kamatz" / "צבע עבור Kamatz" across a live switch both directions for all 19; **read-only rule checked explicitly** — a custom colour override set before the switch survives it unchanged, the input still shows it, and the rebuilt listeners still fire.
- [x] 2026-07-25 | (S157 close-out) | branch/deploy note | S157 ran **Pass C** (accessibility, stalest since S146) on **`hebrew_blend_generator.html`** — the stalest dedicated-C tool by a wide margin (**S27, 2026-07-08**, the first C ever run and a 1-iteration one), and the tool with the most un-C'd growth since. **Drift check: CLEAN** — `origin/main` still at `b964295` (the S152–S156 direct-push the ledger already records), `sw.js` **v413**, `FONT_MAKER_VERSION` **5.5**; nothing landed outside the loop. **Branch/PR:** the previous loop branch's work was fast-forwarded into `main` at S156, so S157 cut fresh from latest `origin/main` onto `claude/improve-loop-z3sk75` → **new draft PR**. **`sw.js` v413→v414** (precached files changed: `hebrew_blend_generator.html`, `flash_cards.html`, `hebrew_dictionary.html`, `locales/ui-strings.csv` → rebuilt `locales/en.json` + `locales/he.json`). **`FONT_MAKER_VERSION` NOT bumped** — correct: Font Maker was untouched (it audited 0 nameless, from its own S125 C pass). **Scripts run:** `build-locales.js` (3978→3979 keys; the single pre-existing empty-`he` cell `worksheet.layout.title_default` is unchanged debt, not new), `check-i18n.js` **clean** (Check A clean + 14 baselined, Check B clean), `update-sitemap.mjs` as the last step. **Decisions deferred to maintainer:** (a) wiring the 75 orphaned `worksheet.glyphname.*`/`vowels.*` keys — **asked at gate 2, answered "Log it, don't build now"**, logged as a Candidate; (b) the 4 flash_cards min/max spinbutton names — needs 2 new strings, no reusable key exists, so logged rather than invented; (c) the vowel-group-header contrast, a pedagogical-colour design call, logged. **Gate 2 asked + approved:** the zoom slider's new key `worksheet.zoom.slider_aria` (EN "Zoom" / HE "זום", matching the dashboard's wording for the same control). Deploy `success` is verified **after** the maintainer merges the PR.
- [x] 2026-07-25 | fdce7c9 | hebrew_dictionary.html | **The Sort dropdown's label properly attached** (S157 Pass C, 3rd tool). `#sortSel` was the dictionary's only nameless control; its `<label data-i18n="dictionary.toolbar.sort_label">Sort:</label>` sat immediately before it in the markup with no `for=`. Fixed with a native `for="sortSel"` rather than ARIA — the right tool when the label already exists, 0 new copy — which also makes the label clickable (wider touch target). **Verified:** 1→0 nameless in the real AX tree across light/dark × 1280/800px (0 pageerrors); clicking the label focuses the select; changing the sort still re-renders results; name follows the UI language ("Sort:" / "מיון:").
- [x] 2026-07-25 | c91a121 | flash_cards.html | **31 nameless controls → 4** (S157 Pass C, 2nd tool). Same shape as everywhere: `<label class="switch">` wraps only the checkbox + slider span, so the sibling `<span class="label">` was never associated. `nameSwitchRows()` (sibling of the dashboard's `nameToggleSwitches()` and the generator's `nameRowLabeledControls()`) points each switch at its own visible label → name follows the UI language; the 13 nikkud colour pickers self-name in `initColorPickers()` via `aria-label="${esc(def.name)}"`. Three controls had no in-row label and got explicit `aria-labelledby`: `#translitStyleSelect`→its "Transliteration" subheading, `#ttsRateSlider`→its section heading (**it had been picking up "Slow"**, the range's low-end caption — the same trap the generator hit), `#activeProfileSelect`→"Profiles & Results" (imprecise but real; a dedicated name needs new copy, logged). **Verified:** 31→4 across light/dark × 1280/800px with every collapsed panel **and `.hide` block force-opened** (0 pageerrors); names correct + Hebrew-switching; **0 dangling `aria-labelledby` refs**; a drill still starts and reaches the card screen. Residual 4 = the min/max spinbuttons (gate-2 candidate).
- [x] 2026-07-25 | 69cbe74 | hebrew_blend_generator.html | **First-run "Take a quick tour →" link was below WCAG AA** (S157 Pass C contrast sweep). `--gold` (#c9922a) on white at 13px = **2.75:1** (need 4.5) — and hovering made it *worse*: `--gold-light` (#f0d080) on white = **1.5:1**, so the link nearly vanished exactly when reached for. The file already carries `--gold-text` (#7f5a13 light, falling back to `--gold` on dark) for precisely this case and every other gold link on the page already used it; this was the lone laggard. Hover now darkens toward `--text` (it was the **only** `--gold-light` hover in the file, so no convention broken). **Measured after:** light 6.22 rest / 14.81 hover, dark 7.16 / 11.98 — all four states pass. 0 pageerrors across the matrix.
- [x] 2026-07-25 | 1cd1362 | hebrew_blend_generator.html + locales/ui-strings.csv | **90 nameless interactive controls → 0** (S157 Pass C, the session's headline finding — the whole generator sidebar). Same systemic class the loop fixed on the dashboard (S135, 39 switches + 2 sliders + 13 pickers) and torah (S146, 6 controls): the toggle-switch `<label class="toggle">` wraps only input+slider, and the sliders / number fields / selects have no `<label for>`, so ~70 checkboxes, 13 colour pickers, 4 sliders, 3 number inputs and 3 selects were nameless. `nameRowLabeledControls()` = the dashboard helper **widened to every adjacent-text-labeled control type**; it points `aria-labelledby` at each control's own visible `data-i18n` label, so **names follow the UI language for free** (verified in HE). Colour pickers self-name in `initColorPickers()`. **Three labels had to be wired first** — "Faded models per line", "Solid example first", "Bingo Mode" were bare text nodes with **no `data-i18n` despite all three keys already existing and being translated**, so they rendered English in Hebrew mode AND would have forced an English-only accessible name; wiring them fixed both (confirmed live: now דגמים דהויים בכל שורה / דוגמה מלאה תחילה / מצב בינגו). Two controls had no text to borrow: `#ttsRateSlider` was picking up "Slow" (range caption) → repointed at its section heading; `#zoomSlider` had nothing → one new **gate-2-approved** key `worksheet.zoom.slider_aria`. **Verified:** 90→0 in the real Chromium AX tree (CDP `Accessibility.getFullAXTree`, not a hand-rolled shim) across light/dark × 1280/800px, 0 pageerrors in all four; **every name checked for correctness, not just non-emptiness** — none swallowed a tooltip, none >48 chars, 17 shared names are the same option repeated per drill mode (only one mode visible at a time); colour pickers re-name after a rebuild; Generate still builds its sheet.

- [x] 2026-07-25 | (S159 close-out) | branch/deploy note | S159 ran **Pass G** (print & export fidelity, stalest since S148) on **`flash_cards.html`** — the tool that had **never** had a dedicated G and was the only one in the suite with **no `@media print` block at all**. **Drift: CLEAN** (`origin/main` still `b964295`, sw v415, FM 5.5). **Branch/PR:** PR #135 still open+unmerged → continued on the SAME branch/PR (3rd session on it). **`sw.js` v415→v416** (precached: `flash_cards.html`, `hebrew_blend_generator.html`). **FM version NOT bumped** (untouched). **Scripts:** `check-i18n.js` clean (no CSV keys added this session, so `build-locales` not needed), `update-sitemap.mjs` last. **Stopped at 4 of 5 iterations** — flash_cards hit the 2-per-tool cap and the only remaining candidate reachable was the P4 duplicate id; a rushed 5th risked verification debt on a session that had already shipped a data-loss fix. **No gate questions this session** (no user-facing copy changed; the print block is CSS, the two backup fixes are payload-only). **Nothing deferred.** New pattern registered: **ivrit-gather-gap**. New feature seed logged (print a whole deck as a cut-out sheet).
- [x] 2026-07-25 | 3a06669 | hebrew_blend_generator.html | **Worksheet `.ivrit` lost the chosen Hebrew font + live-preview toggle** (S159, the `ivrit-gather-gap` sweep's 2nd hit, P3). `getSettings()` carried `hebFontSize` but not `hebFont`, so a restored worksheet used the right size in the **default face**; `livePreview` was missing the same way. Both are registered in AllTools, so only the per-tool file was short. Fixed by adding both beside `hebFontSize` in `getSettings()`/`applySettings()` — the CLAUDE.md mechanism that keeps presets *and* `.ivrit` complete in one move — with presence guards (`if (s.hebFont)`, `if (s.livePreview !== undefined)`, never `||`) so **legacy presets cannot clobber the current font**. Dashboard checked for the same gap: **clean**. **Verified** by round-trip: chose Noto Serif Hebrew + live-preview off → exported → reset to defaults → restored; font returns in localStorage **and** on the `--heb-font` CSS var, toggle+checkbox return off; a legacy preset with neither field leaves both untouched.
- [x] 2026-07-25 | d7a6f23 | flash_cards.html | **Flash Cards `.ivrit` dropped every student profile — P1 data loss** (S159 Pass G, export half). `IVRIT_CFG.gather()` wrote presets, both folder trees and liveState but **not the profiles**, while `apply()` was already fully wired to restore them (`data.flashCardProfiles` → `mergeProfilesBlob`, with a comment saying so). A teacher backing up via the tool's own Save-to-.ivrit and restoring on another machine got the profile **folder tree with nothing in it** — every student's drill history gone, and the restore *looked* successful because the folders came back. The AllTools bundle always carried `flashCardProfiles` correctly; only the per-tool file was incomplete. One-line fix (`flashCardProfiles: readProfiles()`, whose `{activeProfile, profiles}` shape is exactly what `mergeProfilesBlob` expects). **Verified** by real round-trip through the engine's own apply path: 2 profiles (incl. Hebrew name דָּוִד) + 3 result records → export → wipe → restore returns both names, all results and `activeProfile`; **replace mode** checked separately (a stale profile is cleared, not merged).
- [x] 2026-07-25 | 7524f14 | flash_cards.html | **Flash Cards printed the entire app instead of the card** (S159 Pass G, print half). It was the only tool with **no `@media print` block**. Light mode printed **44 pieces of chrome** — every settings panel and button — plus a full-width navy header bar as a solid ink stripe over 2708px of teacher-side configuration; **dark mode printed the whole 1280×2709 page as one dark fill (luminance 0.09)**. Added the block in the shape the other tools use: remap dark tokens to light values, hide chrome, and give background-dependent elements a text+border channel (print dialogs default background-graphics OFF). Two things the token remap couldn't reach needed their own selectors — the `<html>` element carries its own dark background, and the dark theme paints both card faces with **literal hexes**. **Scope deliberately the print baseline, not a feature** (deck-sheet printing logged as a seed). **Verified:** all 4 combinations (light/dark × setup/mid-drill) go 3→0 ink blocks and 44→0 chrome; real PDFs at Letter and A4 in both themes (1 page each, no pagination difference); the rendered page inspected — white paper, bordered card, navy Hebrew with crisp nikkud, and the answer face (the navy slab) now white with a border and dark-ink transliteration; **screen rendering untouched** (dark still dark, chrome present, both faces still their dark-theme colours).

- [x] 2026-07-25 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** → fast-forwarded `origin/main` from `b964295` to the **S157–S160** loop work (`6718ac5`). **Strict linear FF** — `git merge-base --is-ancestor origin/main HEAD` verified before pushing, all **18 commit hashes preserved**, no rebase/squash. **4th ledgered direct-to-main authorization** (after S78 and the S145–S151 / S152–S156 pushes). PR #135 auto-closed as **merged** when its commits landed on `main`; the session was auto-unsubscribed and the hourly check-in cancelled. **Deploy VERIFIED:** the `pages build and deployment` run for `6718ac5` concluded **success** (2026-07-25T15:19:35Z) — `sw.js` v417 is live, so the four sessions' fixes now reach users. **Caught at the pre-push check (worth repeating):** `git diff --stat` showed **7,961 changed lines in `locales/ui-strings.csv`** for what should have been 3 added keys — the Python read/write edits used across S157–S158 had silently rewritten all **3,979 CRLF line endings to LF**. Restored (`6718ac5`), bringing the diff against main to exactly **3 insertions / 0 deletions**, with `build-locales` output verified byte-identical before and after. **Lesson: when editing `locales/ui-strings.csv` (or any CRLF file) via a script, preserve the line endings — read/write in binary, or re-check `git diff --numstat` before committing.** A `--stat` line wildly out of proportion to the intended change is the tell.
- [x] 2026-07-25 | (S160 close-out) | branch/deploy note | S160 ran **Pass D** (performance, stalest since S149) on **`torah_trainer.html`** — the stalest D tool by a wide margin (**S66**) and the one with the most un-profiled growth (S60 CSS-var colour layers, S141 handout + per-verse/bulk copy, S154/S158 changes). **Measure-only, 0 perf code.** **Drift: CLEAN** (`origin/main` `b964295`, sw v416, FM 5.5). **Branch/PR:** PR #135 still open+unmerged → 4th session on the same branch/PR. **`sw.js` v416→v417** (precached: `flash_cards.html`, `hebrew_blend_generator.html`). **FM version NOT bumped** (untouched). **Scripts:** `check-i18n.js` clean (no CSV keys added), `update-sitemap.mjs` last. **Stopped at 3 of 5 iterations** — the pass warranted no code, and the backlog went **empty**: after the two fixes the only remaining item is the maintainer-deferred orphaned-keys decision, plus one size-M feature seed that deserves a fresh session's full budget rather than the tail of a long one. **Gate 2 asked + approved:** the flash_cards `--muted` token darkening. **Nothing deferred.**
- [x] 2026-07-25 | dbb95cd | hebrew_blend_generator.html | **Two checkboxes shared one element id** (S160 — closing the S157 P4, the last actionable backlog item). Both Dagesh Tav / Sav toggles (main Letters panel + Real Words letter panel) carried `id="dageshTavToggle"` — invalid HTML, and `getElementById` can only reach the first. **Not a live bug:** both read sites already used `document.querySelectorAll('#dageshTavToggle')`, so the duplicate was *accidentally providing* the sync between them; the trap was for any future code reaching for the id normally. Real-words copy → `rwDageshTavToggle` (matching the file's `rw` prefix), both gain a shared `js-dagesh-tav` class, and the two sync sites select by class — so the syncing is now deliberate rather than a side effect of malformed markup. **Verified:** duplicate ids 1 → 0; both still move together through `setDageshTav(true)`, `(false)` and the `applySettings` restore; tav/sav swap still reaches the letter grid; worksheet still generates; the S159 `.ivrit` font round-trip still passes.
- [x] 2026-07-25 | c3d3cbe | flash_cards.html | **The suite's faintest text made readable** (S160 — closing the S159 candidate; **gate-2 approved "darken the token"**). `--muted` was `#8893a8` = **3.09:1** on white, carrying **nine** kinds of text (letter-tile names, vowel names, empty states, section headings, translit help, FAQ, body copy); the generator's equivalent is 6.15:1, so flash_cards was the outlier, not the house style. Light → `#68758e` (4.62:1), dark `#7c8499` → `#838b9f` (4.24 → 4.63:1), same hue in both. That cleared 8 of 9. **The 9th was a different defect:** the `[DEFAULT]` marker sits inside the *active* font button, which is gold — muted grey on gold was **1.29:1**, the page's worst reading. The stylesheet already had the correct rule (`.font-opt.active small { color: var(--navy-deep) }`) but the marker was built with an inline `color:var(--muted)` that overrode it; **dropping the inline colour lets it inherit**, which is what the rule was written for. **Verified:** whole-page sweep **9 → 0 in light and 9 → 0 in dark**; the rendered font grid was *looked at*, not just measured, to confirm it reads correctly.

<!-- Archived from the working ledger at the S167 close-out: Done had grown to 7 sessions (S161–S167);
     S161 and S162 moved here, oldest-first policy per the loop protocol. -->
- [x] 2026-07-26 | (S162 close-out) | branch/deploy note | S162 ran **Pass B** (console & error audit, stalest since S152) and then spent the rest of the budget on the **gate-1-approved micro-feature**. **Drift: CLEAN** — `origin/main` unmoved at `20efadb`, `sw.js` v418, FM 5.5; nothing landed outside the loop. **Branch/PR:** PR #136 still **open and unmerged**, so per protocol S162 continued on the SAME branch `claude/improve-loop-3d733x` and the same PR (2nd session on it; no restart, no second PR). **`sw.js` v418→v419** (precached file changed: `flash_cards.html`). **`FONT_MAKER_VERSION` NOT bumped** — Font Maker untouched. **Scripts:** `build-locales.js` (4 new keys), `check-i18n.js` clean, `update-sitemap.mjs` last. **Iterations: 3 of 5** (Pass B = 1, micro-feature = 2). **Gate 1 ASKED and ANSWERED:** "build fronts + duplex backs" — the more ambitious of the two build options; the size risk was flagged before building and the diff landed at **89 lines**, inside the ceiling, so no split was needed. **No gate-2 decision arose** (new strings are new UI, authored fresh in both languages, not a rewrite of existing copy). **Verification debt: none.**

- [x] 2026-07-26 | 10bcbfa | flash_cards.html + locales/ui-strings.csv | **Cut-out card sheet shipped** — see the Feature-seeds entry above for the design. **Verification (micro-feature standard):** duplex mapping checked **against the deck data itself** for n=9/20/7/5 — every answer sits behind its own card, including padded final rows; fronts-only gives 3 pages / 0 answer pages for 20 cards; **real PDFs generated on Letter and A4 × light and dark** with 0 app chrome on paper and white paper in dark mode; the sheet never renders on screen and `afterprint` clears it (4 theme/viewport combos); fresh-profile check (empty localStorage AND IndexedDB); Hebrew UI; `.ivrit` round-trip incl. the stored-`false` case; S159's single-card print path re-verified unchanged. **Two bugs were caught by this verification and fixed before commit:** `saveSettingsToStorage()` didn't exist (the tool's function is `saveSettings()`), and an early duplex assertion only proved the mirrored positions were *populated*, not that each answer was the *right* one — replaced with a deck-data comparison, which is what actually proves the feature.

- [x] 2026-07-26 | (S162 Pass B) | all 12 pages | **Console & error audit — 14th run, fully CLEAN.** 48 load runs (12 pages × EN/HE × light/dark) + 16 interaction runs (one real user flow per tool, in both languages) over the never-B-swept S153–S161 surface: **0 pageerror, 0 real console output, 0 `[i18n]` miss, 0 failed local requests, 0 raw i18n keys in the DOM.** Flows driven: generator generate-worksheet + translit restyle, flash-cards drill, dashboard drawer + pick + groups, dictionary search + word modal, torah reading + trope colours, trope-tutor tabs, Font-Maker letter select, index AllTools modal. **Two apparent findings were my own measurement errors, not defects** — the worksheet's cells are `.blend-hebrew`, not `.heb` (it renders 40 blends correctly), and the dictionary modal's `offsetParent` is null because it is `position:fixed` (it is open at 1280×900). Both re-checked before being dismissed; neither reached the backlog.

- [x] 2026-07-26 | (S161 close-out) | branch/deploy note | S161 ran the rotation's **Pass I** (first-load & empty-state, stalest since S150) **and then Pass H** (teacher walkthrough) as a second pass — the deviation the S160 pointer explicitly recommended, because the backlog was empty and H is the loop's only intake. **Drift check: CLEAN** — `origin/main` at `20efadb` (the S157–S160 direct-push + its ledger note, both already recorded), `sw.js` **v417**, `FONT_MAKER_VERSION` **5.5**; nothing landed outside the loop. **Branch/PR:** the previous loop PR (#135) **merged**, so per protocol S161 cut a **fresh branch off latest `origin/main`** (`claude/improve-loop-3d733x`) and opens a **new draft PR** at close-out. **`sw.js` v417→v418** (precached file changed: `classroom_dashboard.html`). **`FONT_MAKER_VERSION` NOT bumped** — the Font Maker was not touched this session. **Scripts run:** `check-i18n.js` (clean — Check A no new violations, 14 baselined; Check B clean), `update-sitemap.mjs` (last step). `build-locales.js` **not** run — no CSV keys were added (the i18n fix wired keys that already existed). **Iterations: 4 of 5 used** (Pass I, 2 fixes, Pass H); the 5th went to a CLAUDE.md accuracy correction found by H. **Decisions deferred to maintainer (unattended run — no answer could arrive):** **gate 1** — the size-M `flash_cards` "print a deck as a cut-out card sheet" seed was **not** built; conservative default taken (skip the micro-feature), and it remains the top seed for a fresh session, as the S160 pointer intended. **No gate-2 decision arose:** the picker i18n fix wired existing CSV strings whose English values are byte-identical, so no user-facing copy changed. **Verification debt: none.**

- [x] 2026-07-26 | (S161) | CLAUDE.md | **A stale line in the constitution would have told a future session to break a working feature** (found by Pass H while print-checking the torah handout). The Trope Color Coding section ended "Legend is print-hidden with the other reading chrome" — but **S90 deliberately made the legend print**, because the reading carries its clause colors onto paper as underlines and the color→family key belongs there too; the `@media print` block flattens the card, forces swatches with `print-color-adjust`, and drops the tutor link. A session trusting the old line would have "restored" the legend to hidden and silently removed the key from every printed handout. Replaced with the accurate description plus an explicit "do not restore it to print-hidden". **Verified:** print-media emulation with trope coloring on shows the legend in **both** light and dark (dark still prints on white — no ink bleed); the `[hidden]` gate still means it never prints when coloring is off or no reading is loaded. Docs-only — no precached file, no `sw.js` implication.

- [x] 2026-07-26 | b99415a | classroom_dashboard.html | **The student picker showed English on the projector in a Hebrew classroom** (S161 Pass I; P3). The picker overlay is the most student-facing surface on the dashboard — it goes up fullscreen in front of the class. In Hebrew it correctly rendered the names and the `קְבוּצָה 1` group headings, then labelled its buttons **"Pick Again" / "Reshuffle" / "Close"** in English and showed the empty-roster message in English. **Nothing needed translating:** all five strings already existed in `ui-strings.csv` **with Hebrew**, and the `context` column names these exact call sites ("pick overlay close button", "pick result button", "pick overlay empty state"). Four keys — `picker.close`, `picker.pick_again`, `picker.empty_pick`, `groups.reshuffle` — were **referenced nowhere in the code at all**. The sibling `makeGroups()` path was already wired correctly, which is what made the split visible. A **2nd instance of Check A's documented blind spot** (English passed as a plain function argument — `addPickBtn(...)`, `renderPickMessage(...)`). Group headings deliberately left on the `headerLang` content toggle per CLAUDE.md's translate-vs-content boundary. **Verified:** English output **byte-identical** across all three overlay states (so English users see zero change); Hebrew now fully localized; dark + 800px; 0 pageerrors, 0 `[i18n]` miss; `check-i18n` clean.

- [x] 2026-07-26 | ec41bb0 | classroom_dashboard.html | **Two `aria-modal="true"` dialogs let keyboard focus walk out behind them** (S161 Pass I; P2 — accessibility blocker). The **first-run setup card** and the **student-picker overlay** both declare `role="dialog" aria-modal="true"`, which tells assistive tech the rest of the page is inert — but neither wrapped Tab. The picker is the serious one: it is a **full-viewport opaque overlay**, so after two tabs focus landed on the day/weather/timer controls *underneath* with no visible focus ring anywhere on screen. Both already had Escape and focus save/restore; only the Tab wrap was missing. Fixed by extracting the settings drawer's existing `_settingsTrapKey` into a shared **`_trapTabWithin(container, e)`** that all three dialogs now use — one definition instead of three. **Why every prior Pass C missed it:** the first-run card only exists for a brand-new user, and C always runs with populated localStorage, so it never saw the card open — exactly the blind spot Pass I exists to cover. **Verified:** light+dark × 1280/800px, Tab **and** Shift+Tab, **12/12 cases 0 escapes**; settings drawer re-verified for regression after the refactor; Escape/skip/reopen paths and the `setupSeen` write unchanged; reopen ×3 confirms no double-bind; 0 pageerrors.

<!-- Archived at the S168 close-out: Done had grown to 6 sessions (S163-S168); S163 moved here. -->
- [x] 2026-07-26 | (S163 close-out) | branch/deploy note | S163 ran **Pass L** (SEO & discoverability, stalest since S153) as its first act and then, on the maintainer's gate-1 answer ("run a 2nd Pass H to refill seeds"), a **second pass — H on `Hebrew_Font_Maker.html`**. **Tool-selection correction:** the gate question named the dashboard (S107) as stalest-H; that was **wrong** — the Font Maker's only H was **S46**, 117 sessions ago and by far the stalest, so H ran there instead (the maintainer's choice was *which pass*, and stalest-first is the loop's own rule). **Drift check: NOT clean** — `origin/main` moved `771fe00` → **`4eabe2d`**: the S161–S162 ledger note (`7282652`) plus one **outside-loop feature commit**, `4eabe2d` "Flash Cards' duplex option only appears once you choose to print" (moves the `sheetDuplex` switch out of the sticky CTA into a drop-down opened by Print card sheet; static-markup move, ids kept, so `getSettings()`/`applySettings()` untouched), which carried `sw.js` **v419→v420**. That surface is a direct follow-on to S162's card sheet and was **covered by this session's naming sweep** (flash_cards was one of the six swept files). **Branch/PR:** the previous branch's PR **#136 merged**, so per protocol S163 cut a **fresh branch** `claude/improve-loop-d54tql` off latest `origin/main` and opens a **new draft PR**; no open PR existed at session start. **`sw.js` v420→v421** (precached files changed: `index.html`, `hebrew_blend_generator.html`, `flash_cards.html`, `hebrew_dictionary.html`, `classroom_dashboard.html`, `Hebrew_Font_Maker.html`). **`FONT_MAKER_VERSION` NOT bumped** (stays 5.5) — both FM changes are accessibility **fixes**, not features, per the CLAUDE.md "pure fixes don't bump" rule. **Scripts run:** `check-i18n.js` (clean, 14 baselined, Check B clean), `update-sitemap.mjs` (last step); `build-locales.js` **not run — deliberately zero new CSV keys this session**. **Decisions deferred to maintainer:** none outstanding; all three gate questions were answered in-session (see the per-session metrics line).

- [x] 2026-07-26 | 07bebf7 | flash_cards.html | **Deck-size buttons exposed only a colour, not a state** (S163 — the `class-only-selected-state` sweep's last hit outside the Font Maker, and the suite's only one). The ten count buttons (5…100) marked the chosen size with `.active` alone, so a screen reader read all ten identically. `aria-pressed` now rides with `.active` in **both** setters — the initial grid render and `_syncCountUI()`, which is **also the preset/`.ivrit` restore path**. **Verified:** light+dark × 1280/800, exactly one pressed after `setCardCount()` with class↔aria agreement on all ten; a full `getSettings()` → change → `applySettings()` round-trip moves the pressed state back to the restored size (25→50→25); 0 pageerror.

- [x] 2026-07-26 | 03340be | Hebrew_Font_Maker.html | **19 mutually-exclusive selector buttons announced no selected state** (S163 Pass H, P3 — the new `class-only-selected-state` pattern's headline hit). The three input modes, mark editor/upload, circle/square dots, square/rounded bar ends, the combined-form + mark tiles, and the custom-glyph type/shape pairs all signalled "current choice" with a CSS `.active` class only. **This was an inconsistency inside one file, not a missing house convention** — FM already used `aria-pressed` in 30 places and `meSyncMode()` is its own correct pattern. **FIXED:** 15 template-literal buttons mirror the *exact* condition that already drives `.active` (so the two cannot disagree); the 4 static custom-glyph toggles get an initial `aria-pressed` matching their initial `.active`, and `cgSetType()`/`cgSetShape()` now set it beside the class, following `meSyncMode()` line for line. **Deliberately untouched:** the `.me-piece-row` list row (a selectable row — different ARIA shape) and `#meModeEditor`'s static `aria-pressed="true"` (closed-modal markup whose open-time `meSyncMode()` sets both correctly → latent, not live). **Verified:** 13 rendered selector buttons agree class↔aria in light+dark × 1280/800; exactly 1 pressed after `setInputMode('combined')`; `cgSetType`/`cgSetShape` driven through mark/base and dup/trace with both in lockstep; 0 pageerror.

- [x] 2026-07-26 | 4451a41 | 6 tools (index, generator, flash_cards, dictionary, dashboard, font-maker) + 0 CSV keys | **17 text fields lost their accessible name the moment a teacher typed in them** (S163 — closing the S161 P4, which had recorded **2** of them; the real class was **17 across 6 tools**). Under ACCNAME `placeholder` is the last-resort name source, so it works until the field has a value and is then gone. Both dashboard location boxes + its timer/video/preset-name/backup fields, the generator's preset name + backup box, flash cards' preset name / share-code in / share-code out / backup, the dictionary's two gematria range boxes, the home page's AllTools backup box, and the **two Font Maker kerning-glyph boxes whose placeholder was a bare en dash** (naming nothing even when empty). **FIXED (gate-2 answered: "reuse existing strings everywhere"):** each gets an `aria-label` + `data-i18n-aria-label` built from a key that **already exists and is already translated** — **zero new copy, zero new Hebrew, no CSV change** — with the static fallback taken **verbatim from the CSV `en` column** so pre-JS and post-JS names are byte-identical. FM's pair hoists `sideUI`'s existing label into one `sideLabel` used by **both** the visible caption and the `aria-label`, so they cannot drift and the name inherits the caption's freshness semantics. **Verified:** sweep 17→0 across all 11 pages; all 15 static controls read back a correct name from Chromium's **real AX tree with a value typed in**, EN + HE (30 checks); FM kerning re-rendered through `renderKerningSection()` with name === visible caption in both languages/themes; light+dark × 1280/800 on all 6 files, 0 pageerror, 0 raw i18n keys in the DOM.

- [x] 2026-07-26 | (S163 Pass L) | 13 root pages | **SEO & discoverability audit — 3rd run, structurally CLEAN again; 0 defects, 4 cosmetic length findings all maintainer-accepted as-is.** Static-source audit (never the rendered DOM). **Clean on:** canonicals 11/11 with the bare-root homepage; `404.html` (`noindex, follow`) + `i18n-test.html` (`noindex`) correctly excluded and absent from the sitemap; **sitemap ↔ canonical exact 11/11**; robots.txt carries the Sitemap directive and **zero `Disallow` lines** (correct intent — a robots-blocked page never gets to read its own `noindex`); **35 JSON-LD blocks all parse**; every block **byte-identical to its S153-verified state on all 13 pages** (so the only live risk was the UI moving under a static claim — spot-verified the claims most exposed to post-S153 change: dictionary "7-step walkthrough" = exactly 7 `TOUR_STEPS`; torah's 6 named trope families = `TROPE_COLOR_DEFS` exactly; its 5 named transliteration schemes all present; flash-cards 1/2/3-letters control + Start + ✓Yes/✗No marking; generator Letter+A4 + Generate/Print; dashboard Keep-screen-awake + pencil-edit); index `ItemList` + `SiteNavigationElement` 8/8 resolve to real files; **og:image declared 1200×630 = actual 1200×630**; 1 `<h1>` per page; 0 `<img>` without `alt`; crawl-graph all 11 reachable from the homepage at depth ≤1 via plain static `<a href>`. **Findings (gate-2, maintainer chose "leave all four as-is"):** FM description 164 chars (4 over), trope_tutor title 61 (1 over), privacy title 27 + description 92 (short but accurate for a legal page) — truncation is pixel-width-based, so all four are marginal. **Recorded as measured-and-accepted; do not re-log.**

- [x] 2026-07-26 | (S163 Pass H) | Hebrew_Font_Maker.html | **Teacher walkthrough — 2nd-ever H on the Font Maker and the first since S46 (117 sessions, the stalest-H tool in the loop's history by a wide margin). Largely CLEAN; 1 P3 fixed, 1 feature seed, 2 P4s logged.** Scenario: a teacher turning their handwriting into a font to use in worksheets. **Sound end-to-end:** welcome modal → `nextStepHint` guidance that advances with progress ("Start by uploading and tracing your first letter." → "Next up: Bet — 26 to go."); letter tiles carry status in their accessible name ("Alef — not started" / "traced"); the **export guard is a model of no-dead-end design** — exporting with 1 of 27 traced opens "Some letters aren't traced", names all 26 blanks as **clickable chips that jump you there**, and offers Cancel / Export anyway; **autosave + re-entry work correctly** (3s debounce → gzip → IndexedDB; on return, "Continue where you left off?" with Continue / Start fresh, and Continue restored all work + the font name); the Load menu has a **Recent section with a real empty state** ("No recent projects yet — save one first."); the printable template-sheet path exists for teachers with nothing to scan; and My Fonts states the cross-tool payoff ("Pick them in any IvritSuite tool's font selector"). **Method note:** an early probe reported "autosave did NOT restore" — that was the harness stripping `.overlay.open` on load, which **destroys the restore prompt**. Confirmed against the real code (`maybeRestoreAutosave`) before reporting.

- [x] 2026-07-26 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** → fast-forwarded `origin/main` from `20efadb` to the **S161–S162** loop work (`771fe00`). **Strict linear FF** — `git merge-base --is-ancestor origin/main HEAD` verified before pushing, all **7 commit hashes preserved**, no rebase/squash, clean working tree. **5th ledgered direct-to-main authorization** (after S78, the S145–S151 push, the S152–S156 push and the S157–S160 push). Draft **PR #136 auto-closed as merged** by the push (`merged_at` 2026-07-26T03:49:23Z) and the session auto-unsubscribed from its activity; the hourly self-check-in trigger was deleted as obsolete. `sw.js` **v417→v419** across the two sessions (precached: `classroom_dashboard.html`, `flash_cards.html`). Pages deploy for `771fe00` verified after the push.

<!-- Archived at the S169 close-out: Done had grown to 6 sessions (S164-S169); S164 moved here. -->
- [x] 2026-07-27 | (S164 close-out) | branch/deploy note | S164 ran **Pass E** (freshness & site health, stalest since S154) and spent the rest of the budget on the backlog after the maintainer **declined the feature seed** at gate 1. **Drift check: CLEAN** — `origin/main` unmoved at `4eabe2d`, `sw.js` v421, `FONT_MAKER_VERSION` 5.5, worktree clean; nothing landed outside the loop since S163. **Branch/PR:** PR **#138 still open and unmerged**, so per protocol S164 continued on the SAME branch `claude/improve-loop-d54tql` and the same PR (2nd session on it; no restart, no second PR). **`sw.js` v421→v422** (precached file changed: `Hebrew_Font_Maker.html` only). **`FONT_MAKER_VERSION` NOT bumped** (stays 5.5) — all three FM changes are accessibility/i18n **fixes**, not features. **Scripts run:** `check-i18n.js` (clean), **`build-locales.js` (2 new keys → 3989)**, `update-sitemap.mjs` (last step). **Variety governor:** font-maker hit its **2-iteration cap** (mark-editor i18n + 13 sliders), which is why the 7 remaining nameless sliders were logged rather than fixed. **Gates: 3 asked, 3 answered, none deferred** — feature seed → *skip this session*; the two bar-toggle labels → *add new keys, keep "ends"*; the Hebrew wording → *קצוות מרובעים / קצוות מעוגלים approved*.

- [x] 2026-07-27 | 18067f0 | Hebrew_Font_Maker.html | **13 sliders had no accessible name at all** (S164 — the S157-registered `nameless-adjacent-text-labeled control` pattern, which turns out **not** to have been clean in FM). The mark, precomposed-form and anchor panels build sliders by hand instead of through `vSlider()`, so they missed S125's naming pass: each sits beside visible localized text (Dot size, Ink threshold, Detail, Corners, Line weight, Dagesh size, Holam size, Nikkud size, …) that was never associated with it. **FIXED, zero new copy:** each takes `aria-label` built from the `I18n.t()` call that already renders its visible label **on the same line**, wrapped in `esc()` to match the file's idiom — so name and visible text cannot drift and the name follows the UI language for free. **Scope deliberately limited** to the unambiguous set (label = last `I18n.t()` on the slider's own line); **7 sliders whose label lives elsewhere — one still hardcoded English — are logged as a candidate, not guessed at.** **Verified:** all 13 tags carry the attribute and all 19 distinct keys resolve to real EN **and** HE strings (no fallbacks, no raw keys); the mark-editor slider read back "Dot size"/"גודל נקודה" from Chromium's **real AX tree** in both languages; previously-rendering sliders still 0 nameless / 0 raw keys across light+dark; check-i18n clean.

- [x] 2026-07-27 | e5fcfee | Hebrew_Font_Maker.html + 2 new CSV keys | **The mark editor spoke English in a Hebrew UI** (S164 — closing the S163 Pass H candidate). Five labels in `meRenderControls()` were literal English inside a JS template (the "Dot size" slider label + the four shape toggles), while the panel **right beside it** renders the identical controls through `I18n.t()`. **`check-i18n` cannot catch this** — Check A blanks script regions, the documented template-literal blind spot. **FIXED:** three labels reuse already-translated keys; the two bar toggles read "Square ends"/"Rounded ends" where the existing keys say "Square"/"Rounded", so rather than silently shorten visible copy they got **their own keys with maintainer-approved Hebrew** (`markctl_bar_square_ends` = ▭ קצוות מרובעים, `markctl_bar_rounded_ends` = ▢ קצוות מעוגלים — adjective agreeing with קצוות, glyph leading as in every sibling key), placed with the `markctl_*` group, CRLF preserved. **Verified:** both piece types rendered through the real `meRenderControls()` in EN+HE × light/dark × 1280/800 — all five correct, no raw key, 0 pageerror — **and an EN→HE switch re-renders them**, so this is not another `pre-ready-i18n` instance.

- [x] 2026-07-27 | a34aee3 | THIRD_PARTY_LICENSES.md | **The one loaded third-party asset with no licence entry** (S164 Pass E, the pass's single finding). Of eleven externally-loaded origins, ten are documented (jsDelivr, cdnjs, Google Fonts, Analytics, Hebcal, Web3Forms, hCaptcha — by service name rather than hostname); **`files.readme.io` had no mention anywhere.** It serves **Sefaria's wordmark**, shown on `index.html` beside the Torah Trainer card and in `torah_trainer.html`'s "Hebrew text via …" footer — two theme-swapped variants, **hot-linked not redistributed**, allowlisted in the `img-src` of exactly those two pages' CSP. The Sefaria section documented text/translation licensing carefully, so the gap was narrow but real **and of a kind that matters: the logo is a trademark, and the CC/public-domain terms covering Sefaria's *content* grant no rights in the mark.** Added what is displayed and where, that the host is one neither project controls, and what a forker should check — **without asserting a permission this repo cannot verify.** Docs only, not precached. **Verified:** both URLs and both carrier pages match byte-for-byte; `files.readme.io` appears in exactly two CSPs and nowhere else.

- [x] 2026-07-27 | (S164 Pass E) | whole repo | **Freshness & site health — 15th run; 6/6 dimensions clean, 1 finding (fixed above).** **SW precache both directions:** 25/25 listed assets exist, 17/17 shipped pages+locales+scripts listed, `i18n-test.html` correctly absent. **Internal links: 405 static refs, 0 unresolved.** **Sitemap lastmod ↔ git: 11/11 agree.** **Root files:** CNAME (`ivritsuite.com`), `.nojekyll`, robots, manifest (parses, 3 icons all resolve, `start_url=/`), sitemap, LICENSE, licences, README, favicon + 4 icons — all present. **Doc accuracy:** CLAUDE.md's 12-production-pages / 13-root-files / 11-indexable counts all still true. **Shared-block byte identity re-verified:** `ivritsuite-fonts` 9 carriers, `app-toast` 4, `My Fonts uploader` 7 — one distinct hash each. **Method note:** the licences check needs a *hand* judgement, not a string test — a loose substring match cleared `files.readme.io` (on the word "files") while an exact-hostname match over-reported five origins that are documented by service name. Both were wrong; each origin was confirmed by grepping for its service.

- [x] 2026-07-27 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** → fast-forwarded `origin/main` from `4eabe2d` to the **S163–S165** loop work (`b812a8e`). **Strict linear FF** — `git merge-base --is-ancestor origin/main HEAD` verified **before** pushing, worktree clean, `check-i18n` re-run clean, all **15 commit hashes preserved** and re-verified as ancestors of `main` after the push; no rebase, no squash. **6th ledgered direct-to-main authorization** (after S78, the S145–S151, S152–S156, S157–S160 and S161–S162 pushes). **PR #138 auto-closed as merged** by the fast-forward and the session was unsubscribed from its activity; the pending self-check-in trigger was deleted as obsolete. Ships to the live site: `sw.js` **v420→v423** (three sessions, one bump each), 50 accessible-name/state fixes across 6 tools, the Dictionary failed-load crash, the Torah Trainer failure-path raw keys, the Font Maker mark-editor localization (2 new CSV keys), the Sefaria-wordmark licence entry, and the CLAUDE.md `serviceWorkers: 'block'` test caveat. **FONT_MAKER_VERSION stays 5.5** — every FM change in this batch is a fix, not a feature.

- [x] 2026-07-27 | (S165 close-out) | branch/deploy note | S165 ran **Pass F** (cross-tool consistency, stalest since S155) on the affordance **"what does a tool show when its data fetch fails"** — the school-network case. **Drift: CLEAN** — `origin/main` unmoved at `4eabe2d`, `sw.js` v422, FM 5.5, worktree clean. **Branch/PR:** PR **#138 still open + unmerged** (`mergeable_state: clean`), so S165 continued on the SAME branch `claude/improve-loop-d54tql` and the same PR — **3rd session on it**. **`sw.js` v422→v423** (precached changed: `hebrew_dictionary.html`, `torah_trainer.html`). **FM version untouched** (FM not modified). **Scripts:** `check-i18n.js` clean; `build-locales.js` not run (**zero new CSV keys** — both fixes reuse existing ones); `update-sitemap.mjs` last. **Gate: 1 asked, 1 answered** — with 2 iterations left the maintainer chose *fix the Flash Cards message* over building the queued feature; **that fix turned out to be unnecessary** (see below), so **the feature seed remains queued and un-built for a third session**. **Iterations 4 of 5** — the 4th resolved to "no change needed", which is a real outcome, not a skipped one.

- [x] 2026-07-27 | 2eb5775 | CLAUDE.md | **Offline/failure tests must open the context with `serviceWorkers: 'block'`** (S165 — the session's most reusable finding). `sw.js` calls `skipWaiting()` + `clients.claim()`, so the worker activates on the **first** load and then serves `/data/` from the version-independent `DATA_CACHE` — and **service-worker requests bypass Playwright's `page.route`**. A route that aborts `/data/` therefore stops blocking the moment the worker is live: the fetch succeeds, no failure path runs, and the test reports the happy path while looking like it tested failure. Added to the Playwright recipe with the one-line fix.

- [x] 2026-07-27 | (S165 — NO CHANGE NEEDED) | flash_cards.html | **"Flash Cards is silent when its data fails" was a probe artifact, not a defect.** Pass F's first run reported it, and the maintainer picked it at gate 1 over the queued feature. Before writing any code I instrumented every write to `#wordsCount` and found `ALL_WORDS_DATA` **populated** — the block had never held (the service worker was serving the corpus), and the "0 words in pool" text was simply correct for the default `wordsSource === 'mylists'` with no saved lists. Re-tested with `serviceWorkers: 'block'` and `wordsSource = 'alldict'`: Flash Cards shows **"Failed to load dictionary words." / "טעינת מילות המילון נכשלה."** — a proper localized message. **No code changed.** The value of the iteration was catching it before shipping a fix for a non-bug, and the harness rule it produced (2eb5775).

- [x] 2026-07-27 | 9ba18e5 | torah_trainer.html | **The reading area showed developer key names when data was blocked** (S165 Pass F; P2). `torah.reading.empty_title`, `torah.reading.empty_body` and `torah.status.pick_parsha` rendered literally, **in both languages** — the **third instance** of the `pre-ready-i18n / never-re-rendered` pattern (registered S152, hit again S158). Failure-path only, which is why earlier passes missed it: when the data loads, the empty state is written *after* `I18n.ready`. `applyI18n` did re-render the reading — but only when one was actually loaded, and the empty state was exactly the branch it skipped. **FIXED:** the card is repaired **in place** rather than via `renderEmptyReading()`, because that fires `_tourOnReadingChanged()` → `_tourShow()`, which advances a running tour, and `applyI18n` must never disturb tour/audio state. The status line needed more: healing the stuck key leaves the line in the **old language** after a later switch, so `setStatus(msg, isError, key)` now optionally records its key and `applyI18n` re-resolves from it; interpolated messages (the "Failed to load {label}" family) record no key and are untouched — verified by the blocked=false rows where the empty card shows but the status is a real message. **Verified:** blocked and unblocked × EN/HE × light/dark — 0 raw keys, 0 pageerror; EN→HE→EN round trip moves both card and status and returns them intact; pre-fix replay under the corrected harness reproduces all three keys.

- [x] 2026-07-27 | 02f4ed0 | hebrew_dictionary.html | **The Dictionary hung on "Loading dictionary…" forever when the network blocked its corpus** (S165 Pass F; **P2, and the only tool of seven that crashed**). The corpus fetch is kicked off at **parse time** while `js/i18n.js` is **deferred**, so a fast failure lands in `.catch()` before `I18n` exists — and a bare `I18n.t()` on an undeclared global **throws a ReferenceError**, killing the handler before it could set the message or hide the progress bar. The teacher got a permanent spinner and an uncaught error. **FIXED:** rather than only guarding the call, the handler re-points the element's `data-i18n` at the failure key and paints the English fallback immediately — correct under **both** orderings (I18n already up → right language at once; I18n later → `applyStaticI18n()` fills it, and a later language switch re-translates it, which a plain guard would not). Fallback is the CSV's own `en` value, so **no new copy**. **Verified:** data blocked × EN/HE × light/dark, **plus the same matrix with `js/i18n.js` deliberately delayed 1.2 s past the failure** to force the pre-ready ordering — all 8 show the correct localized message, bar hidden, 0 pageerror; happy path unaffected (loader hides, 100 result cards); pre-fix replay reproduces the ReferenceError.

- [x] 2026-07-27 | (S165 Pass F) | all 7 tools | **Cross-tool consistency — 14th run. Affordance: behaviour when the tool's data fetch fails.** Chosen because it is the teachers' real environment (filtered school wifi, captive portals, offline) and had never been swept. Contract compared: no `pageerror`, a visible message, localized, not a dead end. **Result: 2 of 7 broken, both fixed** (dictionary crash, torah raw keys) — **5 fine.** **The exemplar to converge on is `trope_tutor.html`**: *"Example clips unavailable (couldn't load the trope index) — Learn mode still shows every mark."* — it names what failed, what still works, and is fully localized. The generator and Font Maker degrade gracefully with intact empty states; Flash Cards and the dashboard both carry real localized failure messages (`flashcards.words.load_failed`, `dashboard.weather.unavailable`, `dashboard.days.shabbat_unavailable`). **Method caveat that shaped the whole pass:** the first run's verdicts for Flash Cards and the dashboard were **worthless** — lazy fetches were never triggered, and the service worker was defeating the route block entirely (see 2eb5775).

- [x] 2026-07-29 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** at the end of the S166 working day, so `claude/backlog-clearance-dxq2kb` was fast-forwarded onto `main` (`a15674a` → `7fe09aa`, **15 commits**, clean FF, no drift) and **draft PR #140 closes as merged rather than being reviewed**. This is the S78 / 2026-07-28 precedent: the loop's default is a `claude/*` branch + draft PR, and direct-to-main requires exactly this kind of in-session authorization. **What ships:** the S166 loop session (7 nameless FM sliders, stacked first-run dialogs, the S156 transliteration residual), the queued FM→Generator handshake feature, three maintainer-directed backlog follow-ons (auto-detect + 9 scattered FM sites localized, torah parallel side-by-side printing, the 66 glyphname keys reversing the S157 deferral), and the adversarial audit that found 5 defects in that same work. `sw.js` **v425→v431**; `FONT_MAKER_VERSION` **5.5→5.6** (the handshake feature; the i18n/a11y commits correctly did not bump). **Deploy verified after this push, not before** — Pages runs on merge to `main`. **Two items still on the maintainer:** the v5.6 Hebrew changelog prose is loop-drafted and wants review, and two now-dead duplicate keys (`fontmaker.adpick.box_title`, `fontmaker.ad.pick_group_aria`) await a delete call.

- [x] ✅ **⚑ Out-of-band (2026-07-29, maintainer-requested audit of the full session — the PR #139 precedent; fixes on the same branch `claude/backlog-clearance-dxq2kb` → PR #140; `sw.js` v430→v431; session counter untouched):** an independent adversarial pass over all 13 commits, and it earned its cost again. **5 findings, all fixed (one commit):** (1) **the biggest — in a Hebrew UI the real-words grid (31 tiles) and lock grids (26) still showed ENGLISH names**: 2241bf8 wired their builders correctly, but they build before `I18n.ready` and the applyI18n hook never rebuilt them — the harness measured only `#letterGrid`, so it certified "all six surfaces" while three stayed English. Both now rebuild on ready/switch, with rw selection re-applied from `rwLetterFilter` (verified by real clicks across a live switch). (2) middle-click on the My Fonts handshake link opened the new tab **without** setting the font key (`click` never fires for middle button) — `auxclick` added; right-click→open-in-new-tab fires neither event and is accepted (font stays one click away in the generator picker); the 781f3df claim is corrected by this entry. (3) the auto-detect summary **lost its three bold counts** in the i18n wiring — restored via `_adTB` (params bolded post-`esc`, text still escaped). (4) EN sofit tiles were **inconsistent by build time** ("Chaf sf" pre-ready vs "Chaf sofit" after any rebuild) — abbr tiles now stay "sf" in English. (5) the deferred restore prompt was **lost on the mobile-warn→welcomeSeen path** (no re-offer when the welcome is skipped) — re-offered from `dismissMobileWarn`. **Verified clean:** shared-block byte identity (ivritsuite-fonts one hash across all 9 carriers, uploader 7, app-toast 4; the new link sits outside the block), XSS/localStorage/parse sweep over the full diff (all 9 new innerHTML sinks escaped or trusted, 1 setItem to a registered key, 0 new JSON.parse/Object.assign), CSV integrity (CRLF intact, 0 field mismatches, both new keys in both built locales), the About tab renders v5.6 with v5.5 in history, and the **untested translit-only side-by-side print case** (2 columns, 0 drift — added to the regression matrix). **All 8 harnesses re-run at HEAD: green** (glyph incl. live-switch, translit 12/12 + units, handshake 8/8, ad 8/8, scattered 9/9, modal 12/12 no double-dialog). **Accepted, noted:** the no-letters-found message is now fully warn-red (was first-sentence-only; one key can't split a translated sentence); a language switch resets vowel-group collapse state and drops letter-grid focus; My Fonts row titles don't re-translate while the modal is open (pre-existing for its other buttons). **Ledger corrections:** open `- [ ]` census is 0, not the 1 previously reported; 781f3df's middle-click claim was wrong until this audit. **1 new pre-existing candidate logged** (dagesh-toggle rw selection desync, reproduced live). Audit's recurring lesson, now five-for-five this session: every false clean came from a probe that never actually exercised the surface it vouched for.

- [x] ✅ **⚑ Out-of-band (2026-07-29, user-directed — SAME branch `claude/backlog-clearance-dxq2kb` → draft PR #140; `sw.js` v429→v430; session counter untouched):** the maintainer asked about the standing S157 glyphname deferral and then **reversed it**, so letter/vowel names now follow the UI language (2241bf8). **This empties the actionable backlog**: the only remaining candidate is torah item (3), nikkud colours on B/W print, which has no fix. **No rotation pass ran; C stays stalest.** 0 new CSV keys (all 66 already existed and were translated), so no `build-locales` run; `check-i18n` clean; `update-sitemap.mjs` run last. **Method notes:** measuring key→name coverage BEFORE writing code turned a vague "multi-surface" item into a bounded one and revealed the sub-label mapping gap up front; and matching a literal **non-breaking space** through a shell heredoc failed repeatedly until the patterns were switched to whitespace-agnostic regexes — prefer `\s` over a pasted U+00A0.

- [x] ✅ **⚑ Out-of-band (2026-07-29, user-directed — SAME branch `claude/backlog-clearance-dxq2kb` → draft PR #140; `sw.js` v428→v429; session counter untouched):** the maintainer picked the torah print residual off the remaining list, so **side-by-side printing regained its parallel columns** (b158af4) — the one open item in that entry that had a fix. **No rotation pass ran; the pointer is unchanged** (C stalest). No new i18n keys, no CSV change, no `FONT_MAKER_VERSION` change (different tool). `check-i18n` clean; `update-sitemap.mjs` run last. **A scripted multi-part edit broke the file** — a `for` loop converted to `forEach` kept its old `}` closing brace, and the resulting `SyntaxError` was only discovered when the test timed out waiting for a function that no longer existed; **syntax-check immediately after a scripted edit, not after the test fails.** Two probes also had to be corrected mid-run for testing nothing: a layout case passed `'bible'` when the real value is `'biblepage'` (so it silently re-tested side-by-side and reported identical numbers), continuing this session's pattern of vacuous assertions caught only by reading the numbers rather than the PASS line.

- [x] ✅ **⚑ Out-of-band (2026-07-29, user-directed backlog work — SAME branch `claude/backlog-clearance-dxq2kb` → draft PR #140; `sw.js` v427→v428; session counter untouched):** the maintainer asked to keep working the backlog, so the **top Pass-K candidate shipped** (adb257d + d7ca797) and the orphaned-key census was **closed won't-fix** on my own recommendation. **No rotation pass ran — the pointer is unchanged** (S166 ran K; **C is stalest**). `FONT_MAKER_VERSION` stays **5.6** — this is pure i18n, unlike the handshake feature earlier today which correctly bumped it. **0 CSV keys added** (all wiring reused approved copy), so no `build-locales` run was needed; `check-i18n` clean; `update-sitemap.mjs` run last. **Gate 2 touched once and flagged, not hidden:** the picker heading moved onto its family key, changing one shipped Hebrew word (הקצו vs שייכו, both maintainer-authored). **The candidate list is now down to 2 open items, both deliberate holds** — the S157-deferred glyphname decision and the informational torah print residual — and the **feature-seed queue is empty**, so the next session's gate 1 has nothing to offer unless Pass H is run out of rotation or a feature is named directly.

- [x] ✅ **⚑ Out-of-band (2026-07-29, user-directed micro-feature — SAME branch `claude/backlog-clearance-dxq2kb` → the same draft PR #140; `sw.js` v426→v427; session counter untouched):** after the S166 close-out the maintainer greenlit the **queued feature seed at gate 1**, so the Font Maker → Worksheet Generator handshake shipped (781f3df; full entry under Feature seeds). **No rotation pass ran — the pass-rotation pointer is unchanged** (S166 already ran K). Gate 1: asked and approved. Gate 2: not triggered for the link label (it reuses already-approved EN+HE verbatim), but the **v5.6 changelog prose is genuinely new copy — drafted in EN+HE and flagged for maintainer review**, not silently shipped. `FONT_MAKER_VERSION` **5.5→5.6** (a feature, so the bump rule applies — unlike the two S166 Font Maker changes, which were a11y/i18n and correctly did not bump). **1 new CSV key** (`fontmaker.modals.myfonts_use_in_generator`) + the changelog key → `build-locales.js` run; `check-i18n` clean; `update-sitemap.mjs` run last. **CSV editing note worth keeping: `locales/ui-strings.csv` has CRLF line endings** — a text-mode Python read/write silently rewrote all 4,056 lines to LF and produced a whole-file diff twice before it was caught; edit it in binary and rejoin on `\r\n`.

- [x] ✅ 2026-07-29 | (S166 close-out) | branch/deploy note | **Backlog-clearance session, maintainer-directed.** Branch `claude/backlog-clearance-dxq2kb` off `origin/main` @ `a15674a`; **no open PR existed** (the S163–S165 and Flash Cards work went direct-to-main by explicit authorization), so a fresh draft PR was opened at close-out. **Drift check:** `origin/main` = `a15674a`, `sw.js` v425, `FONT_MAKER_VERSION` 5.5 — all matching the last close-out, no undocumented outside-loop drift. **SW v425→v426** (precached files changed: `Hebrew_Font_Maker.html`, `hebrew_blend_generator.html`). **FONT_MAKER_VERSION deliberately NOT bumped** — the two Font Maker changes are an accessibility fix and a dialog-ordering fix, not a feature, per the S125/S141 precedent that a11y/i18n work does not bump. Ran `node scripts/check-i18n.js` (clean; 14 baselined, Check B clean) and `node scripts/update-sitemap.mjs` as the last step. `build-locales.js` NOT run — **0 CSV keys added all session** (every fix reused existing translated keys). **Gates: 2 asked, 2 answered** — (1) session shape: maintainer chose *max code clearance* over building the queued feature seed; (2) the latent `aria-pressed` P4: maintainer chose to *fold it into the slider commit*. **Decisions deferred to maintainer: none.** Deploy `success` is verified after merge, not in-session.
- [x] ✅ DONE S166, fd71875 | hebrew_blend_generator.html | **Transliteration restyle now reaches matching, word search and crossword** — the S156 residual, closed. The three drills cache *derived* structures, so 9e95399's word-copy refresh never reached them. Each is refreshed **in place, never regenerated** (the cache exists so the printed sheet keeps matching its QR answer key — regenerating would repeat the reverted S155 mistake): matching keeps a `src` reference and re-derives its two baked target strings; word search holds the word objects themselves so the existing refresh reaches the word bank, leaving grid/placement/fillers untouched; crossword re-derives each baked clue and pushes it into the two clue lists (which hold copies keyed by `heb`), leaving layout and numbering untouched. The crossword clue rule was extracted to a shared **`cwClueFor()`** so the builder and the restyle path cannot drift. **Verified with a negative control** — pre-fix 0 of 12 runs restyled anything; post-fix **3 consecutive full matrices** (drill × light/dark × 1280/800) all restyle with the layout fingerprint identical and 2→2 sheets (answer key survives), 0 pageerrors — plus unit checks that the S156 `items`/`words` path still refreshes, blend-derived items are still skipped, crossword `both` mode still joins, and a legacy cached item without `src` does not throw. **Harness note:** esm.sh is route-aborted headless and the real `HT.apply` returns its input unchanged, so a restyle is invisible unless `window.HebrewTransliterator.apply` is stubbed (S99 precedent).
- [x] ✅ DONE S166, 6975026 | Hebrew_Font_Maker.html | **Two first-run dialogs no longer open at once** — the S163 P4, closed, and slightly wider than logged (the restore prompt could also cover the guided tour). See the Candidates entry for the full method.
- [x] ✅ DONE S166, 3219909 | Hebrew_Font_Maker.html | **The last 7 nameless sliders named; the `nameless-adjacent-text-labeled control` sweep is now 0 in every tool.** Each `aria-label` reuses the exact `I18n.t()` call that renders its visible label (name == visible text, WCAG 2.5.3, follows a language switch) — the S125/S164 pattern, 0 new keys. The precomposed-form **Width** slider additionally had its *visible* label hardcoded in English; it now resolves through the existing translated `fontmaker.panels.width`. The latent `#meModeEditor` `aria-pressed` markup fix was folded in at maintainer direction. **Verified in the real Chromium AX tree** over local HTTP, EN/HE × light/dark × 1280/800: 17 sliders, **0 nameless, 0 raw i18n keys, 0 pageerrors**; Hebrew names resolve (סף דיו, פירוט, פינות, עובי קו, רוחב, …). 5 of 7 confirmed through their real panel render; `#tropSpread`/`#vowelPairDx` sit behind narrow panel state and were confirmed by mounting their emitted markup. **Three harness lessons worth keeping:** (1) these builders take **arguments** (`renderTraceLeft(L, l)`, `renderWideFormControls(pc)`) — calling them bare throws and yields a *false clean*; (2) `project.letters` is an **Array** of objects carrying `.codepoint`, not a codepoint-keyed map; (3) **top-level `let` globals (`curKind`/`curCp`) cannot be assigned from `page.evaluate`** — the assignment creates a `window` property that the lexical binding shadows, so use the page's real setters (`_selectItem`/`_setWorkMode`). An early version of this harness reported a confident **"PASS — 0 nameless"** while all three builders had silently thrown and only 3 unrelated sliders rendered.

- [x] 2026-07-29 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** → fast-forwarded `origin/main` from `79b64cb` to the two Flash Cards features + their pre-merge audit (`4acf5d4`). **Strict linear FF** — `git merge-base --is-ancestor origin/main HEAD` verified **before** pushing, worktree clean, `check-i18n` + `build-locales` re-run clean, all **5 commit hashes preserved** and re-verified as ancestors of `main` after the push; no rebase, no squash. **7th ledgered direct-to-main authorization** (after S78, the S145–S151, S152–S156, S157–S160, S161–S162 and S163–S165 pushes). **PR #139 auto-closed as merged** by the fast-forward and the session was auto-unsubscribed from its activity. **Pre-flight caught a stale local `main`** (b964295, 44 commits behind) — diagnosed as a clean ancestor of both `origin/main` and the branch, so the FF absorbed it in one step; worth checking, because a divergent local `main` here would have turned a fast-forward into a merge commit. Ships to the live site: `sw.js` **v423→v425** (two bumps, one per feature), the printable one-page student progress report, the 12-level Learner Ladder, 66 new EN/HE keys, and the audit's two P2 fixes + five hardenings — including the **pre-existing** My-Fonts coercion bug that had been silently resetting uploaded fonts on every preset/`.ivrit` round trip. **FONT_MAKER_VERSION untouched** (the Font Maker was not modified).

- [x] ✅ 2026-07-28 (pre-merge adversarial audit of the Learner Ladder — maintainer-requested, fixes committed on the same branch) | flash_cards.html + 3 CSV rewords | **The audit's headline: level 8 "Add Shva" was a functional no-op, green tests notwithstanding.** `generateCard` never allows shva as a syllable **nucleus** (Rule N1 — a `'sh'` pick is rerolled then hard-rejected); shva renders ONLY as the automatic *connector* on a non-vowelled position before the last vowel (בְּרָ). Level 8 was mode 1 (single position, always vowelled) and the blend levels pinned every position vowelled — so **no level in the shipped ladder ever displayed a shva**, level 8 drilled the identical distribution as level 7, and "All thirteen" in level 9 really showed twelve. **Why 184 green checks missed it: the deck-fidelity test asserted only "no marks outside the allowed set" (subset), never "the promised mark occurs" (occurrence) — a subset assertion is NOT an occurrence assertion; pair them.** FIXED per maintainer decisions (both gated): (1) **shva is now the bridge level** — mode 2 with `connector:true` (`_ladderOverrides` pins `vowelPositions:[mode]`, the app's own CV̊-CV pattern), moved after `allvowels` to sit directly before the two-letter blends it bridges into; every card now deterministically carries the connector shva on letter 1 (ids are stable storage keys, so the reorder renumbers labels only); the inert `'sh'` key is gone from every vowel set (`V_ALL` = 12 real keys). (2) **Auto-save banks new-best only** — the first attempt at a level always banks (a first fail is exactly the weakness signal) and retries bank only on an improved `bestPct`, so Try-Again grinding can no longer flood the 50-entry history cap and silently evict the teacher's own saved drills (the audit's second should-fix). Plus five hardenings from the same audit: the Save button re-asserts its "✓ Saved" state when `applyI18n` re-runs `showResults` on a language switch (the reset invited a duplicate manual save with a fresh `savedAt` that import de-dupe would never collapse); **`applySettings` font resolution now accepts My Fonts ids** (`allFonts()`/`allEngFonts()`, not the built-in lists alone) — previously ANY settings round trip (preset load, `.ivrit` restore, ladder exit) silently coerced an uploaded font back to the default and the ladder exit *committed* it, a pre-existing bug the ladder made routine; a loaded share-code inline word pool is stashed/restored across enter/exit (the round trip nulled it); `_ladderExit` catches a throwing restore and **skips** `saveSettings` on failure (disk already holds the correct pre-ladder blob — persisting a half-restored state was the only way to lose it); the level map's ▶ mirrors in RTL via the existing `dir-arrow` convention (✓/🔒 deliberately don't). Three level descriptions reworded (EN+HE, maintainer-approved): `desc_shva` (the connector pattern), `desc_allvowels` (drops the false "thirteen"), `desc_blend2a` (names its four vowels — the old "first four" contradicted its own key list, which swaps tzere for chirik by design). **Verified: suite upgraded to 214 checks incl. the missing class — occurrence assertions** (every shva-level card carries the connector on letter 1 across ~250 draws; all three hatafs appear in `allvowels`; non-connector levels stay shva-free), new-best-only banking counts, language-switch button state, a My-Fonts byte-identical round trip, word-pool restore, and a forced-throw exit that must strand nothing; report suite still 104/104; negative control still discriminates. Audit also re-confirmed all structural invariants independently (single `SETTINGS_KEY` write site, no chokepoint recursion, no XSS — a missing i18n key *drops* an interpolation rather than injecting it, letter groups exactly partition the 31-letter display set). Accepted/backlog (deliberately not this PR): `names3` fills card 10 with one repeat (9-letter pool, documented `generateDeck` stuck-pass); focus lands on `<body>` at drill start (identical to the existing Start-button path); duplicate word-list *names* collapse across any settings round trip (pre-existing `wlNameIndex` semantics); the manual-save variant of the language-switch button reset for non-ladder drills (pre-existing); `mergeProfilesBlob` throws re-importing a profile missing `results` (pre-existing, adjacent); `.ladder-card-pitch` muted-on-paper ≈4.4:1 light (house token pairing). `sw.js` stays v425 — that bump is on this unmerged branch and already covers these edits.

- [x] ✅ SHIPPED 2026-07-28 (user-directed), 7bff4f2 | flash_cards.html | **Learner Ladder — a guided decoding path.** Every drill in this tool is teacher-configured, so a learner opening the page alone had no answer to *"where do I start?"* and no sense of progress between sessions. A **🪜 Learning Path** card at the top of the setup screen opens a level map of **12 ordered levels** — letter names (3), then one vowel family at a time (6), then two- and three-letter blends (3) — each one tap: configure → drill → score → record → unlock the next at **80%** (deliberately the same threshold as `renderProfileHistory`'s ✓, so "passed" and "✓" never disagree). **No second deck generator and no pedagogy engine** — a level is just a locked-down configuration of the existing machinery, held in one `FC_LADDER` data array so content and mechanics stay separable. **The core of the change is settings safety.** `saveSettings()` has **~70 call sites**, so a level that reconfigures the drill would otherwise persist itself over the teacher's setup; following torah_trainer's `_handoutEnter`/`_handoutExit`, a level snapshots `getSettings()`, applies overrides, and restores on the way out with `saveSettings()` a **no-op for the duration**. **`showScreen()` turned out to be a 3-line chokepoint**, so ONE guard there covers every route home (header button, End Practice Early, Return to Options) instead of a restore sprinkled at each call site; `_ladderEnter` also unwinds if `startPractice` bails, which it can on an empty deck. Levels **pin** the deck-shaping fields and **inherit** presentation (font, colours, audio, translit style), so a snapshot carrying Other mode, a letter lock or listening can't quietly produce the wrong deck. **Blend levels pin `vowelPositions` to every position:** the app default (`{mode}`) puts a **shva connector** on the earlier letters (בְּרָ), and a first blend drill wants plain CV-CV (בָּרָ). **Progress lives inside the existing profile object** as `ladder`, so **no new localStorage key, no AllTools wiring, no migration** — `.ivrit` backup already exports profiles wholesale; **unlock state is derived from `bestPct`, never stored**, so no pointer can de-sync across a merge. **Two latent field-dropping merge sites found and fixed:** `renameProfile` and `mergeProfilesBlob` both rebuild a profile from `results` alone and silently dropped the new field — both now merge it (higher `bestPct` wins, **earliest** `passedAt` kept). **Two re-entrancy hazards handled:** Redo / Practice-Mistakes replay a deck that is no longer the level's own draw, so they **detach the scoring claim while keeping the settings binding** (restoring mid-deck would swap the configuration out from under the open cards); and the record + auto-save are guarded because **`applyI18n` re-runs `showResults` on a language switch**, which must not bank the same drill twice. `saveCurrentResult`'s write step is extracted as **`saveResultToProfile`** so the ladder's auto-save shares it rather than forking storage — ladder drills then feed weakness tracking and the progress report for free. **43** new `flashcards.ladder.*` keys (EN+HE, **gate-2 approved**; one planned key dropped in favour of reusing `results.save_button_done`). **Gate 1 (the 12-level progression + letter/vowel groupings) was also maintainer-approved** — pedagogy is the maintainer's call, not the implementer's. Verified headless (`serviceWorkers:'block'`, external origins aborted): **184 checks, 0 pageerror** — settings **byte-identical** across a full multi-level session, pass/fail/replay-lower paths, both merge sites, locked-row + focus behaviour, no-profile flow, `startPractice` bail-out, XSS probe, EN+HE with RTL **measured**, light/dark × 1280/800. **Deck fidelity is asserted on the rendered nikkud** (~150 draws per level) after the first attempt passed **vacuously** — cards are only `{heb, translit}`, so the intended `vowelKeys`/`letterHebs` reads were `undefined`; the rewrite classifies U+05BC by context (dagesh in בּ vs. **shuruk** in וּ) and is backed by a **negative control** proving it detects injected violations. The progress-report suite still passes **104/104** after the refactor. `sw.js` v424→v425.

- [x] ✅ SHIPPED 2026-07-28 (user-directed), 68dc30f | flash_cards.html | **Printable one-page student progress report.** Teacher Tools' profile detail gains a **📄 Progress Report** button that prints a parent-conference-ready sheet built entirely from `hebrewFlashCards_profiles` — **no new collection, schema change, network call, persistent state, AllTools key or CSP change** (so no preset/`.ivrit` wiring gate). **Architecture copies this page's OWN print precedent, the S162 card sheet (`printCardSheet`), not torah_trainer's handout:** a static print-only host `#fcReportSheet` beside `#fcSheet`, a `body.fc-report-print` class, and an **`afterprint`** teardown — `_reportEnter`/`_reportExit` are top-level + idempotent so headless tests can drive them without blocking on the dialog, and `afterprint` rather than `try/finally` avoids a blank page where `print()` returns early (mobile Safari). Sections: header (name via `esc()`, generated date, period) · summary (drills / cards / overall accuracy / best) · focus areas · strengths · recent-drills `<table>` · teacher comments + signature + storage-locality line. Focus and Strengths share **one row cascade** (`combos` → `letters`+`vowels` → `other`) so a report never mixes granularities; reuses `analyzeWeaknesses`, `weakRows`, `describeResultMode`, `esc`, `announce` and `renderProfileHistory`'s ✓/–/✗ thresholds so screen and paper agree. **The streak / personal best is deliberately omitted** — `PB_STREAK_KEY` is device-global, not per-profile, so printing it beside a student's name would be wrong data on a document handed to a parent. **Two print bugs found while verifying and fixed:** `body{min-height:100vh}` pinned the body to a full page box, and `main.wrap`'s `padding:…100px` survived its hidden children (114px of dead space) — together they pushed a fitting report onto a blank second sheet; the fix hides the **wrapper**, not just `.screen`. Drills capped at **8** (not 10) so a maximal report still fits one **Letter** page. 23 new `flashcards.report.*` keys (EN+HE, **gate-2 approved by the maintainer**), reusing `profile.no_selected_alert` / `weaknesses.missed_ratio` / `weaknesses.mastered` rather than minting near-duplicates. Letter/vowel names and dates stay English by existing convention (transliterated `LETTERS[].name`; `toLocaleDateString` matching `renderProfileHistory`). Verified headless (`serviceWorkers:'block'`, external origins aborted): **104/104 checks, 0 pageerror** — hand-reconciled summary numbers, mark thresholds, print emulation + `page.pdf()`, dark-mode print parity, EN+HE with RTL mirroring **measured**, both guards, XSS probe (`<b>Test&"'</b>` inert), edges (50-result cap note, single-date period, other-mode-only fallback, zero-miss "mastered"), light/dark × 1280/800, real click path, and coexistence with the card sheet's `afterprint`. **1 page on Letter AND A4, in both languages.** `sw.js` v423→v424.

- [x] 2026-07-29 | (S167 close-out) | branch/deploy note | "/improveloop" → S167 on branch **`claude/improve-loop-u4k2pf`**. **Drift check: the branch was byte-identical to `origin/main` (0 ahead / 0 behind) with no open PRs** — S166 plus five out-of-band landings had been pushed straight to `main` under the maintainer's authorization (`a5db015`), so the designated branch was already a clean cut off latest `main`; continued on it and opened a **new draft PR** at close-out. Versions at start: **SW v431, FM 5.6**. Ran **Pass C (accessibility) on `flash_cards.html`** — chosen at the maintainer's answer over the by-the-book stalest tool (the dictionary, which has *never* had a dedicated C in 167 sessions) because flash cards' last dedicated C was **S82 / 2026-07-14** *and* ~2,000 lines of Learner Ladder + progress report + card sheet had landed out of band and never been audited. **Iters: 1 pass (C) + 4 fixes = 5 (full budget).** `sw.js` **v431→v432** (precached: generator, flash_cards, dashboard, dictionary, torah_trainer, Hebrew_Font_Maker, locales/en.json, locales/he.json). **No `FONT_MAKER_VERSION` bump** — the FM change is localization wiring of pre-existing keys, not a feature, matching the 07-29 glyphname/auto-detect precedent. Scripts run: `check-i18n` (clean, 14 baselined), `build-locales` (9 new keys → 4066), `update-sitemap.mjs` (last step). **Gates: all three answered up front** — gate 1 → *skip the micro-feature* (seed queue empty); gate 2 → *pre-approved reuse + short literal labels*, which is what the two naming commits used (36 of 50 sites reused an existing translated string; 9 new keys). **No decisions deferred to the maintainer.** Deploy runs on merge to `main`, so Pages `success` is verified after merge, not in-session.

- [x] 2026-07-29 | (S167 Pass C — accessibility, one tool) | flash_cards.html | **Pass C — `flash_cards.html`, its 3rd dedicated C (S8 → S82 → S167) and the first over the huge out-of-band growth. Structurally very strong; the real finding was a naming *quality* class the previous sweeps' detector could not see.** **Clean, with counts:** **1,940 focusable inspections** across 4 surfaces × light/dark × 1280/800 → **0 nameless, 0 placeholder-only, 0 title-only, 0 missing focus ring** (every control's focus style measured by actually focusing it and diffing computed style, with a styled-sibling proxy check for the visually-hidden toggle inputs). Reduced motion: **0 still animating** under `prefers-reduced-motion` in either theme. Modal contract on the ladder and weakness views: `aria-labelledby` resolves, initial focus lands inside, **30 Tab presses never escape the dialog**, Escape closes, focus returns to the opener. Contrast: 258 setup + 47 ladder-row text nodes measured alpha-composited, including the **disabled** ladder rows — clean. **Findings:** (1) **symbol-only accessible name** — new pattern, registered and swept suite-wide (50 sites, 6 tools; fixed in ddc9180 + 2c4b78f); (2) `.weak-tab` class-only-selected-state; (3) 3 marginal `--muted` contrast sites; (4) sub-24px targets on the ladder × and the shared folder-tree buttons; (5) `#dashEditor` nameless. **Two harness self-corrections worth keeping:** the deep sweep's `div[role="tab"]` "nameless" hit in the Font Maker was **my detector's gap, not a defect** — `role=tab` supports name-from-content, and the S166 "0 nameless" claim stands; and the Font-Maker read-only assertion first passed **vacuously** against `window._dirty`/`window._udStack`, which do not exist — re-run against the real `_projectDirty`/`undoStack`/`redoStack` before it was allowed to count.

- [x] 2026-07-29 | be37855 | Hebrew_Font_Maker.html | **The Font Maker's primary navigation spoke English in a Hebrew UI** (S167 Pass C, iteration 5). The always-visible category tabs — Font Details / Letters / Nikkud / Trop / Punctuation / Spacing / Advanced Settings, plus five optional glyph-set tabs — were built from a hardcoded English `NAMES` map, while a **complete, fully translated twelve-key `fontmaker.tabs.cat_*` family sat in the CSV keyed to those very category ids with `grep -c` → 0 references**. The same shape as the auto-detect modal cleared out of band on 07-29. **FIXED:** `catName(c)` resolves through `I18n.t('fontmaker.tabs.cat_' + c)` via `esc()` (house convention for I18n-into-`innerHTML`), `NAMES` demoted to the pre-ready/no-I18n fallback, and `applyI18n` now re-runs `renderCatTabs()` — safe because it re-derives the tab set from `project.font` and the active tab from `catTab`, mutating neither. Verified light+dark × 1400/800 across en→he→en: all 7 default **and** all 5 optional tabs render Hebrew, no raw keys, active tab survives, and `_projectDirty`/`undoStack`/`redoStack` unchanged; **negative control fails 8 checks**. 0 new keys, 0 new copy.

- [x] 2026-07-29 | 2c4b78f | classroom_dashboard.html + flash_cards.html (+ locales) | **Twelve icon buttons that had no label at all** — the second half of the symbol-only-name sweep, the sites with no `title` to mirror. Dashboard: the **7 emoji-sequence buttons** in the days widget, which announced their emoji ("egg, hatching chick, chicken") rather than the set — now Chicks / City / Time / Arrows / Plants / Boy / Girl — plus the TTS-rate reset, the lone one of the suite's thirteen resets with no title (reuses `dashboard.common.restore_default`, so it now matches its twelve siblings). Flash cards: the **saved-result delete**, a destructive control announced as "✕" (now "Delete {date}" through the existing `flashcards.actions.delete_aria`), the weakness card-count cancel (reuses `flashcards.weaknesses.cancel`), the weakness-view close, and the TTS-rate reset. **The weakness view mattered most: it puts initial focus on its close button**, so opening it previously announced "multiplication sign, button" as the dialog's first utterance — measured before and after. 9 new keys (EN+HE), 3 reuses. Verified light+dark × 1280/800 × en/he with a seeded profile so the saved-result row renders and all overlays driven open: symbol-only names now **0** on both tools, focus lands on "Close"/"סגור", 0 pageerror.

- [x] 2026-07-29 | ddc9180 | 6 tools | **NEW PATTERN `symbol-only accessible name` — registered and swept the same session; 38 controls fixed as pure wiring.** An icon-only button's glyph **is** its accessible name (content outranks `title` in the ACCNAME chain), so a screen reader announced "anticlockwise open circle arrow, button" while the correct, already-translated label sat unreachable in `title=`. Every site in this commit already carried the right string, so: **0 new copy, 0 new CSV keys.** generator 4 (trace spacing/size resets, TTS-rate reset, mobile share) · **dashboard 19** (12 "Restore default" resets, new/rename/delete class, schedule-row delete, the preset/schedule row buttons built by `mk()`, and the editor toolbar's link button) · **font maker 13** (delete guide/glyph/sheet/piece/kern/kern-class, convert-to-main, raise/lower/reset for vowels and marks) · dictionary 3 · torah 2 · flash cards 1 (the card flip button). For the two JS builders the mirror is **conditional on the label carrying no letters**, so a worded button keeps its visible text as its name and WCAG 2.5.3 stays satisfied. Verified on all 6 pages, light+dark, **EN and HE** (`data-i18n-aria-label` re-translates on switch): 0 symbol-only left on five of them, 0 pageerror, no raw keys.

- [x] 2026-07-30 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** immediately after the S172 close-out, so `claude/improve-loop-j3sj8w` was **fast-forwarded** onto `main` — verified a strict fast-forward first (`git merge-base --is-ancestor origin/main HEAD`, 18 commits ahead / 0 behind, clean tree), so no merge commit. This lands **S170 + S171 + S172 together**, plus the two out-of-band items on the same branch (the switcher `aria-label` reversal e66add4 and its ledger note). **PR #142 was auto-closed as merged by the landing** and this session was unsubscribed from it; the pending hourly self check-in was deleted rather than left to fire against a merged PR. Versions landing: `sw.js` **v438** (from v434 across the three sessions), `FONT_MAKER_VERSION` unchanged at **5.6**. Per the loop's branch protocol this supersedes the `claude/*`-branch rule **only on explicit in-session authorization** (precedents: S78, S166, and the S167–S169 landing) — the default for future sessions remains a `claude/*` branch feeding a draft PR. **Deploy verified in-session: `pages build and deployment` run 30504728002 (run #1075) for `d74b498` concluded `success` in 30 s** — the second time in the loop's history that deploy success was confirmable without waiting on a maintainer merge.

- [x] 2026-07-30 | (S172 close-out) | branch/deploy note | "/improveloop" → S172 **continues on branch `claude/improve-loop-j3sj8w`, adding to the still-open draft PR #142** (open / draft / mergeable-clean, 0 check runs — no CI on this repo, 0 review threads), which now carries S170 + S171 + S172. **Drift check: none** — `origin/main` still at `a011825`, HEAD 12 ahead / 0 behind at session start, `sw.js` v437 and FM 5.6 exactly as S171's out-of-band switcher work left them. Ran **Pass B (console & error audit)**. **Iters: 1 pass + 4 fixes = 5 (full budget).** `sw.js` **v437→v438** (precached: flash_cards, hebrew_blend_generator, Hebrew_Font_Maker, locales/en.json, locales/he.json). **No `FONT_MAKER_VERSION` bump** — two Font Maker iterations, both localization wiring, which CLAUDE.md classes as fixes not features (the S169 precedent). Scripts: `check-i18n` clean after every commit; **`build-locales` run** (net **−4** CSV rows: 17 duplicates added then removed, 3 rows edited to restore markup, 0 net new keys); `update-sitemap.mjs` last. **All three gates asked AND answered** (third attended session running): gate 2a → *inline note + `aria-disabled`* for the generator's Generate alert; gate 2b → *do the Font Maker modal, author EN + Hebrew*; gate 1 → *no second pass, spend it on fixes* (**seed queue empty for a seventh session; Pass H still not run**). **Nothing deferred.** **I made and corrected a real mistake this session — recorded rather than smoothed over:** iteration 4 authored 17 new `fontmaker.cg.*` keys after concluding "no keys exist", when a complete translated `fontmaker.panels.custom_*` family already covered all 17. Corrected in 50044d6 by repointing at the existing family and deleting the duplicates. **Root cause: I searched for the panel's English sentences instead of for its key namespace, and the CSV's context column says "field label"/"toggle button", not the English text — so the grep could not have found them.** Verification never caught it because both key sets produce correct Hebrew; only the namespace census did.

- [x] 2026-07-30 | (S172 Pass B — console & error audit) | 12 pages × 2 languages | **Pass B — 15th run, first over the never-B-swept S163–S171 surface plus the out-of-band Learner Ladder / progress report / card sheet. FULLY CLEAN, and the interesting part is what it took to trust that.** 24 cells (12 pages × EN/HE), on load **and** after one real interaction per tool: **0 pageerror, 0 console errors, 0 console warnings, 0 failed local requests, 0 unhandled rejections, 0 `[i18n] missing key`** — the last channel being S171's double-localization detector folded in per that session's pointer, exactly as recommended. `serviceWorkers: 'block'` for determinism (a live worker serves `/data/` from DATA_CACHE and bypasses `page.route`, the S165 lesson). **The round-1 result was worthless and I nearly shipped it:** every interaction was driven by a guessed selector, so "no errors" could equally have meant "nothing happened". Round 2 made every step return a **receipt** proving the page changed — and **three steps had proved nothing**: `index.html` clicked a button that opened no modal, the Font Maker matched **0** letter tiles (`.ltile`, not `.tile`), and torah's button lookup was English-text-only so it never clicked in Hebrew. Fixed and re-run, the receipts now read: generator 1 sheet built; flash cards a 3-card drill to results **plus** save → weaknesses (4 tabs) → progress report (52 nodes) → card sheet → ladder (12 rows); dashboard drawer open with 13 panels; dictionary 9 results; FM 27 tiles with selection moving 05D0→05D3; index modal open with focus inside; torah clicked in both languages. **Confirms S165's failure-path fix holds**: with Sefaria blocked, torah shows a localized message in each language ("Could not determine the current parsha (Sefaria API unreachable)." / "לא ניתן לקבוע את הפרשה הנוכחית (ה-API של Sefaria לא נגיש)."). **Standing lesson, now three sessions old and generalised: an interaction without a receipt is not evidence.**

- [x] 2026-07-30 | dc51fe6 | flash_cards.html | **Flash Cards letter and vowel names now follow the UI language** — the `flashcards.glyphname.*` family, **45 keys (32 letters + 13 vowels), authored, fully translated, referenced nowhere**. Third and largest instance of `authored-but-unreferenced` after `fontmaker.ad.*` and `dashboard.glyphname.*`, and **cap-deferred twice (S170, S171)** before this. Wired at five sites: letter picker tiles, vowel picker tiles, the colour-picker row's visible label **and** its `aria-label` (kept identical so the announced name always matches the seen one), and the vowel tooltip. **Cleaner than the dashboard's copy: every name here slugifies straight onto its key** ("Hataf Patach"→`hataf_patach`, "Chaf Sofit"→`chaf_sofit`), so one generic helper covers both families with no per-entry map. Also wired the **"Teacher Tools" panel title**, whose key existed and was translated while the markup never referenced it. `applyI18n` now rebuilds the letter grid and colour pickers alongside the vowel grid; both rebuilds are state-derived, so this cannot repeat the S167 shape — **asserted, not assumed: three selected letters and a custom vowel colour both survive the switch.** 0 new copy. 48/48 assertions across light/dark × 1280/800; negative control 7/12 with every name English.

- [x] 2026-07-30 | 2247fde | hebrew_blend_generator.html | **Generate reports "no vowels selected" inline instead of in a dialog** (gate-2 approved as note + `aria-disabled`). Same `blocking-alert-for-a-routine-path` class as S171's dashboard geolocation fix, and the pattern's second hit closed. **Worth remembering what was already there: the worksheet area has an inline empty state carrying this exact sentence, but the `alert()` fired and returned before rendering could reach it** — the tool already knew how to say this politely and never got the chance. **Three copies of Generate exist** (sidebar, frozen desktop footer, mobile FAB) and a teacher may have pressed any of them, so each gets its own `.gen-note` sibling — a single note beside the sidebar button would still have been a dead click for someone who pressed the footer at the bottom of a long page, which is the exact failure being removed. One helper drives all three by class and sets `aria-disabled` (not `disabled`, so the control stays focusable and a screen-reader user can reach it and hear why). **The note retires the instant its cause is fixed** — `updateCombosCount` already runs on every vowel change — so selecting a vowel clears it without pressing Generate again. Stores the KEY, so `applyI18n` re-renders it. 0 new keys. 26/26 assertions; no native dialog fires, asserted on Playwright's `dialog` event.

- [x] 2026-07-30 | d39920d + **50044d6 (self-correction)** | Hebrew_Font_Maker.html | **The custom-glyph panel speaks Hebrew — and the fix's own history is the lesson.** Every string in the add-a-glyph panel was hardcoded English inside a template literal (Check A's documented blind spot: the gate reads plain-literal assignments and static markup, and an interpolated string is invisible to both). **d39920d authored 17 new `fontmaker.cg.*` keys on the finding that none existed. That finding was wrong** — a complete, fully-translated `fontmaker.panels.custom_*` family already covered all 17. **50044d6 repointed the helper at the existing family and deleted the duplicates**, which is strictly better: it uses the maintainer's own terminology (**"גליף"**, which the rest of the file already uses, rather than my "תו") and leaves no duplicate copy to drift. Wired through `_cgT`, a sibling of the file's existing `_adT`, same guard: resolve at render time, fall back to the English literal, never a raw key. The duplicate-source dropdown now runs its letter names through `gName()`, the localizer this file already had. **Deliberately NOT added to `applyI18n`**: `renderCustomGlyphsPanel` resets `cgType`/`cgShape`, so a forced rebuild on a language switch would silently discard a half-filled form — both halves asserted (reopening after a switch shows Hebrew; a typed code point survives a switch). Two hint rows had their `<b>` stripped in the CSV while the live English shows it, so the markup went back into those existing rows in both languages rather than keeping a duplicate family to carry it. 28/28 assertions × EN/HE × light/dark.

- [x] 2026-07-30 | 6d52e47 | Hebrew_Font_Maker.html | **The mark and punctuation panels speak Hebrew** — both build themselves in template literals, so the two "Make my font Nikkud/Trop-compatible" checkboxes, the Mark-shapes and Upload-mode groups with their Default/Custom and Per Symbol/Combined toggles, the bold Sheva Na checkbox, and "Include digits & punctuation in my font" were all hardcoded English; plus the tour's Next button, static markup that only needed `data-i18n`. **0 new copy — every key was already authored and translated.** The `{label}` param now receives a localized short category name, reusing the already-translated QA-tab pair (Nikkud/ניקוד, Trop/טעמים) rather than adding a key for a word the CSV already had. Wired through `_pT`, a third sibling of `_adT`/`_cgT`. `mark_shevana`'s row had its `<b>Sheva Na</b>` stripped while the live English shows it, so as with the two custom-glyph hints the markup went back into the existing row. 40/40 assertions × EN/HE × light/dark. **One pre-existing issue surfaced and logged, not fixed:** enabling the trop panel emits **28 `[i18n] missing key` warnings for `fontmaker.glyphname.<trope>`** — keys REFERENCED but never authored, the inverse shape. Negative control confirms identical 28 before this change. **Pass B missed it because B never enabled that panel** — a coverage note for the next B.

- [x] 2026-07-30 | (S172 — key-integrity census, validated) | Hebrew_Font_Maker.html + locales | **157 of 445 `fontmaker.panels.*` keys are authored, fully translated, and referenced nowhere — by far the largest instance of `authored-but-unreferenced`, and the session's headline finding.** Surfaced only because iteration 4's duplicate-key mistake forced a namespace-level census instead of a text search. **The number is trustworthy and that was checked**: the file builds **no** `fontmaker.panels.*` key dynamically (`grep "'fontmaker.panels.' +"` → 0), so a static reference scan cannot produce false "unreferenced" hits; and all 157 have non-empty Hebrew, so this is finished translation work sitting unused, not debt. **Two counting traps hit and fixed en route** — the first census returned "0 keys in CSV" because a Python **text-mode** read had already collapsed CRLF (the same trap S171 recorded on the write side, now confirmed to bite on reads too), and the first grep-based pass reported ~150 without checking for dynamic keys. This session wired 28 of the 157 (custom-glyph panel 17, mark/punct panel 11); **129 remain**, logged as the top Font Maker candidate.

- [x] 2026-07-30 | e66add4 | js/i18n.js + locales (all 12 pages) | **The language switcher now names itself in the language you are in** (user-directed, out-of-band; **a same-day reversal of S171's gate-2 won't-fix**). The `<select>` had a hardcoded `aria-label="Language"` — its only accessible name, since the control shows just a flag and a code. **Why the reversal is right: that attribute is read exclusively by screen-reader / voice-control users, so the "a stranded user needs a recognisable English word" argument was defending a string its intended beneficiaries never encounter.** Localizing also dodges the wrinkle the bilingual option had — with the UI in Hebrew the document is already `lang="he"`, so the Hebrew name gets a Hebrew voice with no `aria-labelledby` split. **Implementation, three cooperating pieces because the shared runtime must not depend on 12 separate `applyI18n` call sites:** (a) a guarded `switcherAria()` — `t()` returns the KEY pre-ready and writing that into `aria-label` would name the control `shared.lang.switcher_aria`, so it falls back to the English literal instead (and `t()` already suppresses its warning pre-ready, so this adds **no** new missing-key noise — the thing 05a1f29 just cleared); (b) `updateSwitcherState()` re-applies the name on every live switch, alongside the value it already synced; (c) a `ready.then()` refresh for the first load, because `mountSwitchers` runs at DOMContentLoaded and can beat the dict fetch — added as a separate statement, NOT a reassignment of `ready`, so `I18n.ready` stays the same promise. The declarative `data-i18n-aria-label` sibling is set too, so `applyStaticI18n` covers it as well. 1 new key (`shared.lang.switcher_aria`). **Verified on all 12 pages × both start languages = 24 cells, all passing:** the name is asserted from Playwright's **accessibility snapshot** (what a screen reader actually consumes), not just the attribute — correct on initial load, follows a live switch in both directions, restores on switching back, never once a raw key, 0 missing-key warnings, 0 pageerror. **The race was proved, not assumed:** delaying `/locales/he.json` by 2.5 s so mount wins shows the English fallback while pending and self-corrects to שפה once the dict lands.

- [x] 2026-07-30 | (S173 close-out) | branch/deploy note | "/improveloop" → S173 ran on the **designated fresh branch `claude/improve-loop-flfnp9`** cut from latest `origin/main` (88eb53a), because S172's PR #142 branch was fast-forwarded onto `main` by the maintainer (previous entry) — merged-equivalent, so no branch reuse. **Draft PR #143 opened at close-out** (open/draft/mergeable-clean, 0 check runs — no CI on this repo). **Drift check: none** — `origin/main` unmoved at 88eb53a, sw.js v438 and FM 5.6 matched the S172 close-out. **SW v438→v439** (precached files changed: `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `locales/en.json`, `locales/he.json`, `sw.js`). **FM version NOT bumped** — both FM iterations are pure localization wiring (no feature, no behavior change), same call as S172's identical-class work. Scripts run: `build-locales` (×2, after each CSV markup restore), `check-i18n` (clean both gates), `update-sitemap.mjs` (final step). **Session run unattended (remote autonomous run — AskUserQuestion would have blocked); decisions deferred to maintainer per the gate fallback:** (a) gate 1 — the new "prefill Save-as-Word-List name from theme" seed was NOT built, queue holds it for the next attended session; (b) gate 2 — the three FM copy calls logged as the new P4 candidate (kern plural fork, metric_hint's missing sentence); nothing user-facing was reworded beyond wiring existing translations.
- [x] 2026-07-30 | (S173 Pass H — teacher walkthrough, dictionary) | hebrew_dictionary.html | **Pass H — 105 sessions since the dictionary's only prior H (S68), and the pipeline built since then held up end-to-end.** Scenario "prepare the Food unit" driven headless with receipts: theme browse (counts render) → narrow lengths → **⭑ Save as Word List** (modal + status + BOTH handoff links revealed) → manager (view/rename/delete/load-as-filter/both handoffs) → **generator `?wl=` = zero-click worksheet** (Real Words on, list pre-checked, food words on sheet) → **flash cards `?wl=` = one-click drill** (card showed רִבָּה) → print (S127's beforeprint materializes all 56 rows; chrome hidden). **3 friction candidates logged, 2 of them fixed in-session** (pre-corpus "No words found" a72ee83; invisible theme filter 02aa05d); **open: the theme-save bridge strands the page in bulk mode** (56 checked+tinted cards, screenshot in the candidate). **1 seed logged — the queue's first since S167** (prefill the list name from the active theme; typing it is the only typing in the whole flow). Also closed a stale duplicate flash_cards candidate left unticked at S172. Harness lesson re-learned on the FIRST probe: my modal detector searched `.overlay.open` while the dictionary uses `#wordModal.wm-overlay.open` — the save modal "didn't open" until the receipt selectors were fixed; and receipt 5 raced the **1000ms session-save debounce**. 0 pageerror across every step.
- [x] 2026-07-30 | b2897b3 | Hebrew_Font_Maker.html + locales | **The mark-panel hints and custom-upload flow speak Hebrew — the whole `mark_*` family (8 keys) wired in `renderMarkPanel`**, incl. the trop placement hint and Sheva Na explainer whose CSV rows had their `<b>`/`&nbsp;` markup stripped while the live English carried it (restored EN+HE in the existing rows — 4th and 5th instances of the S172-documented class). Params via `labelL` matching the `mark_compatible` precedent; the zero-marks `coming_soon` branch receipt obtained by truncating `NIKKUD_MARKS` in-probe. Census 99→91. Verified EN+HE through every branch, 0 missing-key, 0 pageerror.
- [x] 2026-07-30 | 02aa05d | hebrew_dictionary.html | **Active theme filters now show as chips in the Active-filters bar** (S173 Pass H). Full detail on the candidate line above (marked DONE); the compounding factor was session restore — a returning teacher had NO visible reason for a filtered list. Chips reuse the translated theme names (0 new copy); per-chip ✕; Reset-all already covered.
- [x] 2026-07-30 | a72ee83 | hebrew_dictionary.html | **The dictionary no longer claims "No words found" while the corpus is downloading** (S173 Pass H). `_wordsReady`/`EMOJI_DATA` guards; loading text reused; ~100ms flash on localhost but the whole multi-MB fetch on school wifi. Detail on the candidate line above (marked DONE).
- [x] 2026-07-30 | 29883a8 | Hebrew_Font_Maker.html + locales | **21 more authored-but-unreferenced `fontmaker.panels.*` keys wired** (slice 3 after S172's two): 7 metric drag-handle titles (SVG `<title>` tooltips in `metricHandlesSVG`), 5 form-tab strings (`renderFormPanel`), 4 combined-sheet nav strings, 4 kern pair badges/titles, and `kern_autotightness_hint` with its stripped `<b>` markup restored EN+HE. **Census method fixed first: suffix-aware expansion of `_pT`/`_cgT` prefixed lookups** (naive scan said 146; true count was 120 → now 99 after this commit). Deferred as copy calls: `kern_export_summary`/`skip` (plural fork) + `metric_hint` (stale sentence) — logged as the gate-2 P4. Verified EN+HE with builder-output receipts, dark+800px, 0 missing-key, 0 pageerror; check-i18n clean.
- [x] 2026-07-30 | 5c3898f | docs/IMPROVEMENT_LOG.md | **S173 Pass H findings logged** (3 candidates + 1 seed + 1 stale-duplicate cleanup) — the pass's own commit, before any fix.
- [x] 2026-07-30 | (S174 close-out) | branch/deploy note | "rerun the loop" → S174 **continues on branch `claude/improve-loop-flfnp9`, adding to the still-open draft PR #143** (open/draft/mergeable-clean, 0 check runs — no CI on this repo), per the protocol's previous-PR-open rule. **Drift check: none** — `origin/main` unmoved at 88eb53a; sw.js v439 + FM 5.6 matched. **SW v439→v440** (precached changed: `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `classroom_dashboard.html`, `locales/en.json`, `locales/he.json`, `sw.js`). **FM version NOT bumped** — localization wiring + one aria-label, no feature. Scripts: `build-locales` ×2 (markup restores), `check-i18n` clean after every iteration, `update-sitemap.mjs` last. **Gates: ASKED this time (4 questions batched post-pass) but the maintainer did not answer → all four took the conservative default and remain open to re-ask:** (1) gate 1 — the prefill-list-name seed NOT built; (2) kern export summary/skip plural fork — unchanged; (3) margins-hint HE sentence — unchanged; (4) 404.html canonical removal — unchanged. **Environment notes for future sessions:** the container's git clone is SHALLOW (date-anchored diffs/`-S` silently lie — `git fetch --shallow-since=<date>` first), and an 11-agent Workflow fan-out died to an environment fault stripping subagent tool parameters (~848k tokens, 0 output) — the inline diff-driven check replaced it and is cheaper anyway.
- [x] 2026-07-30 | (S174 Pass L — SEO & discoverability, 4th run) | 13 root pages | **Structurally CLEAN on every dimension for the 4th consecutive run** — full detail in the rotation row (canonicals/sitemap/robots/OG/og:image/crawl-graph/h1/alt/35-parsing-JSON-LD; JSON-LD byte-unchanged since S163's verification, proven post-deepening; flash_cards' claims hand-re-verified against its 788-line-changed UI — all hold). One P4 deferred (404 canonical+noindex mixed signal). Checker lesson recorded: strip `<script>` regions before h1/img regexes (3 false hits otherwise).
- [x] 2026-07-30 | 9816bb4 | classroom_dashboard.html | **The dashboard message editor finally has an accessible name** — `#dashEditor` (contenteditable) named via the owning panel's existing translated title, no role change. Closes the S171-logged P4. Verified both languages/themes/widths + editor→display sync receipt.
- [x] 2026-07-30 | 273d6d7 | Hebrew_Font_Maker.html | **Anchor X/Y hints and trop column controls speak Hebrew** (authored-but-unreferenced, 9th instance): xy_what_* ×4 + moves_xy_hint frame, halign ×4, and the drag-handle hint's {class} param routed through the existing `anchorLabelT()` helper (it existed for exactly this; three sites bypassed it — the other two compose English frames with NO keys, logged as gate-2 copy work, not half-wired). preview_2nd_mark/_none confirmed authored-duplicates. Verified via renderAnchorLeft receipts in nikkud + trop modes, EN+HE.
- [x] 2026-07-30 | 9b37441 | Hebrew_Font_Maker.html + locales | **Letter-nav, centering, load menu and align hints speak Hebrew** (authored-but-unreferenced, 8th instance): 19 keys — Prev/Next (bare + {char} + titles), Center ↔/↕ + titles, both Next: Nikkud sites + Next: Trace, Load-project menu ×3, and the four align-step hints whose CSV rows got their stripped `<b>` markup restored EN+HE (6th–9th restore instances). guide_h/v/rename_title identified as authored-duplicates of the wired guide_orient_*/guide_rename. Verified via builder receipts (renderAlignUnder ×3 input modes, renderTraceUnder, renderAnchorLeft, toggleLoadMenu), EN+HE.
- [x] 2026-07-30 | 0c7514d | hebrew_dictionary.html | **The theme-save bridge no longer strands the page in bulk mode** (S173 Pass H's open finding): `_themeBridgeBulk` armed only when the bridge itself flips bulk on; `closeWordModal` restores. 4 receipts × light/1280 + dark/800 (save, cancel, pre-enabled preserved, word-card neutral).
- [x] 2026-07-30 | e173d3c | docs/IMPROVEMENT_LOG.md | **S174 Pass L findings logged** (rotation row + the 404 P4) — the pass's own commit.
- [x] 2026-07-30 | (S175 close-out) | branch/deploy note | "/improveloop" → S175 **continues on branch `claude/improve-loop-flfnp9`, adding to the still-open draft PR #143** (now S173+S174+S175). **Drift check: none** — `origin/main` unmoved at 88eb53a; sw.js v440 + FM 5.6 matched. **SW v440→v441** (precached changed: `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `404.html`, `locales/en.json`, `locales/he.json`, `sw.js`). **FM version NOT bumped** — localization copy/wiring only. Scripts: `git fetch --unshallow` (pass E prerequisite), `update-sitemap` (in-pass correction + final idempotent run), `build-locales` ×3, `check-i18n` clean after every iteration. **ALL FOUR standing gate questions were asked AND ANSWERED (all approved with the recommended options) — the first fully-answered batch since S170:** seed built (0729ad2), kern plural forked + margins hint authored+wired (4c8f3c1), 404 canonical removed (1c567d4). **No decisions left deferred.**
- [x] 2026-07-30 | (S175 Pass E — freshness & site health, 16th run) | repo-wide | **6/6 dimensions clean + one real correction** — full detail in the rotation row. The correction: sitemap lastmod for resources/privacy/contact had been inflated by the shallow-clone artifact; fixed under full history (7585ea0). 361/361 internal refs resolve; precache both directions; licenses complete; shared blocks byte-identical (app-toast ×4, ivritsuite-fonts ×9); CLAUDE.md counts verified; robots/CNAME/manifest fine. Three checker false-positive classes documented and killed (script regions, attr-suffix `src=`, `═══` marker pads).
- [x] 2026-07-30 | 1c567d4 | 404.html | **404 drops its canonical** (S174 Pass L nit; gate-2 approved) — noindex,follow stays, page stays sitemap-absent. Verified both themes/widths.
- [x] 2026-07-30 | 4c8f3c1 | Hebrew_Font_Maker.html + locales | **Kern export summary + margins hint speak Hebrew** (the S173 copy-blocked trio, gate-2 approved): kern_export_summary forked `.one`/`.other` + wired with the skip note (no more hand-pluralization or mixed-language line); metric_hint markup-restored + the missing Fit-margins sentence authored EN+HE and wired. Singular/plural/live-hint receipts EN+HE; 4,070 CSV keys.
- [x] 2026-07-30 | 0729ad2 | hebrew_dictionary.html | **MICRO-FEATURE SHIPPED (gate-1 approved): Save-as-Word-List prefills the name from the active theme(s)** — localized, " + "-joined, editable, one-shot, bridge-only; dup guard intact; manual path untouched; no new persistent state. Fresh-profile verified EN+HE ×4 receipt classes.
- [x] 2026-07-30 | 7585ea0 | sitemap.xml + docs | **S175 Pass E commit — sitemap dates de-inflated** (resources→07-19, privacy/contact→07-24) after unshallowing; E rotation row updated.

- _(archived from the working ledger at the S184 close-out — Done keeps the last 5 sessions; these are the S179 block and the 2026-07-31 out-of-band Learning Path batch.)_

- [x] 2026-08-01 | (S179 close-out) | branch/deploy note | "/improveloop" (ultracode session) → S179 ran on the **designated fresh branch `claude/ultracode-effort-cicysb`**, cut from latest `origin/main` (1dcd2d7) per the S178 pointer (PR #144 merged/closed). **Draft PR opened at close-out.** **Drift check: FOUND — four user-directed Learning Path features landed direct-to-main after S178** (progress-report standing line 62422cd, `?path=1` deep link 24d902f, landscape certificate 01eff99, certificate ceremony c694580), moving **sw.js v444→v447**; FM stayed 5.6. All four were already ledgered by the out-of-band entries above, and Pass A was pointed at them deliberately — this sweep is the first of any kind to cover that surface. **Clone arrived SHALLOW again (50 commits, 4th session running) — unshallowed to 1,823 before anything else.** **SW v447→v448** (precached changed: `hebrew_dictionary.html`, `index.html`, `classroom_dashboard.html`, `flash_cards.html`). **FM version NOT bumped** — the Font Maker was not touched. Scripts: `check-i18n` clean (no CSV keys added, so no `build-locales` run needed); `update-sitemap.mjs` last. **Both gate questions asked AND answered — 5th consecutive zero-deferral close-out:** the fix slate = the 4 data-integrity P2s (chosen over two Hebrew-UI-wiring alternatives), and gate 1 = skip the micro-feature + **run Pass H next session** to refill the starved seed intake (an explicit override of stalest-first, which would have picked G). **Session note: the Pass-A workflow died when the session paused, after all 5 sweepers and 23 of 31 verifiers had finished — results were recovered from `journal.jsonl` exactly as the S178 method note prescribes, so nothing was re-run.** The 8 unverified findings are all `blocking-alert` sites, logged as candidates with their static census.
- [x] 2026-08-01 | 407a94a | flash_cards.html | **A Flash Cards backup now carries your folders, not just your presets** (S179 Pass A, ivrit-gather-gap). `flashBackupSave()` — the scope-aware path that shadows the shared engine's `ivritSaveFile()` — hand-built its payload and never wrote either folder tree, though `gather()` includes both and `apply()` restores both. Measured pre-fix on the real button: presets→`["presets","liveState"]`, profiles→`["flashCardProfiles"]`, both→those three; restoring returned everything FLAT ("Fall Semester"/"Period 3" gone). Fixed by emitting the matching tree per scope via `ftRead`. **Verified** 3 scopes × light/1280 + dark/800 (correct keys in all 6, 0 pageerror) + capture→wipe→real-`apply()` restore rebuilding the nesting intact.
- [x] 2026-08-01 | 391fc69 | classroom_dashboard.html | **A damaged schedule no longer blanks the Schedule Builder** (S179 Pass A, untrusted-shape-on-read #2). Neither feeder checked the shape (`loadSettingsFromStorage`'s `ivritSafeAssign`, `loadSavedSchedule`'s deep clone), so a null row threw `reading 'preset'` and a non-array threw `filter is not a function` — builder at 0 rows, validation + the real load entry point + even `addScheduleRow` throwing, with every schedule still in storage. Fixed with a `normalizeSchedule` beside the file's existing `normalizePanelLayout`/`migrateRostersInSettings` normalizers, called from both feeders. **Verified** 6 shapes × 2 matrix cells: **6 failing cells → 12/12 clean**, well-formed rows around a bad one survive and stay editable, Dashboard `.ivrit` round-trip unchanged (incl. `applySettings`' deliberate live-schedule preservation).
- [x] 2026-08-01 | baf68bc | index.html | **Importing a backup no longer wipes a student's Learning Path progress** (S179 Pass A). `mergeFlashCardProfiles` took the whole profile only for a NEW name; for an existing name it merged `results` and returned, never reading `src.ladder` — so an AllTools restore silently discarded level progress and the completion certificate. Added `mergeFcLadder` (bestPct = better, passedAt = first-earn, `ivritSafeAssign` on the import path) mirroring flash_cards' `mergeLadder`; both index import paths route through the one function. **Verified** with a student present on both sides: pre-fix level 2 stuck at the local 82%/later date and **level 3 lost entirely**; post-fix 95% at the earlier first-earn, level 3 imported, level 1 untouched, results still merged, a new student still imported whole — and the two files' merges now produce **byte-identical output**, both themes, 0 pageerror.
- [x] 2026-08-01 | 47818ef | hebrew_dictionary.html | **A damaged word list no longer makes the Word Lists button do nothing** (S179 Pass A, untrusted-shape-on-read #1 — the session's top candidate). `wlReadStore()` checked only that `lists` was an object, so one bad list broke all three consumers reading through it (`wlRenderManagerInto`, `wlOpenSaveModal`, `wlOpenAddToListModal` share `(store.lists[b].updated || 0)`; the manager adds `list.words.length`). Measured pre-fix, both themes, store bytes intact every time: a null list and a `words`-less list each threw so **the manager never opened**; `words:"…"` counted the STRING's length then threw on View words; `words:[null]` threw on `.word`. Fixed by normalizing per-list shape at the read chokepoint — the same guard both sibling consumers already apply — non-destructively (reads never rewrite storage). **Verified 8 failing cells → 16/16 clean**, full lifecycle unchanged (add/merge/rename/reorder/remove/delete, persisted shape still `{name,created,updated,words}`), both other consumers open against a malformed store, AllTools `.ivrit` round-trip restores both lists with correct counts.
- [x] 2026-08-01 | 2a68628 | docs/IMPROVEMENT_LOG.md | **S179 Pass A logged** — the ~17 ACTIVE classes + a first-ever full A2, over `30d653f..HEAD` (74 commits) including the four out-of-band Learning Path features. Run as a 5-way parallel workflow plus a per-finding adversarial verify pass: **31 findings, 23 verified, 0 REFUTED, 5 corrected to PARTIAL**. 6 classes clean with receipts, 11 hit; all 6 retired patterns re-confirmed dead; both of the ledger's "not yet swept" backlogs (parse-per-call, untrusted-shape-on-read) cleared by doing them.

- [x] 2026-07-31 | (direct-to-main) | branch/deploy note | **Maintainer explicitly authorized "Push to main"** at the end of the Learning Path feature session, so `claude/ladder-progress-report-line-mbhou1` was **fast-forwarded** onto `main` (strict fast-forward verified first — `git merge-base --is-ancestor origin/main HEAD`, clean tree, 7 ahead / 0 behind, no merge commit). `f8a433b..dd6631c`. Lands the **four user-directed features together** (report Learning-Path line, `?path=1` deep link, completion certificate, certificate ceremony upgrade; **SW v444→v447**; FM unchanged 5.6; session counter untouched — all four were out-of-band, not loop iterations). **PR #145 auto-closed as merged**; the session was auto-unsubscribed and had no pending check-in trigger to clean up (the hourly re-arm had been declined earlier). **Deploy verified in-session: `pages build and deployment` run 30641294705 for `dd6631c` concluded `success`** (15:05:03Z). Next loop session cuts a FRESH `claude/*` branch off latest `origin/main` per the branch protocol — never reuse #145.
- [x] 2026-07-31 | c694580 | flash_cards.html + locales/ui-strings.csv | **Certificate "full ceremony" upgrade** (maintainer-directed pizazz pass; direction picked from three previewed mockups, 2 new strings approved at the gate): gold brand hexagram as inline SVG (splash geometry — two stroked triangles, R=41/viewBox 100), navy+gold two-tone frame + hollow-diamond corner ornaments (border-only, so they print with background graphics OFF — the whole design stays fills-free), italic serif lead-in (`flashcards.cert.lead_in` — EN "This is to certify that" / HE "תעודה זו מוענקת ל־", which reads with the name + approved body as one classic certificate sentence), gold rule under the name, ✦-flanked date (the answer-key ornament idiom), and a gold double-ring seal (big `{total}` numeral over `flashcards.cert.seal_levels` "Levels"/"שלבים") centered between the signature lines. Color discipline per house rules: small gold TEXT uses readable `#7f5a13`, large geometry uses brand `#c9922a`; the seal numeral stays ink. All 44 harness checks green (landscape 792×612 ONE page, report-portrait isolation, teardown incl. printToPDF's real afterprint, XSS-name inert, 11/12 no-op, EN+HE fully-RTL × dark × 800px); EN+HE print renderings eyeballed. SW v446→v447.
- [x] 2026-07-31 | 01eff99 | flash_cards.html + locales/ui-strings.csv | **The Learning Path ends in an artifact — a printable landscape completion certificate** (maintainer plan prompt, feature B): a third print trio parallel to the report (`_certEnter`/`_certExit`/`_certActive`, `#fcCertSheet`, `body.fc-cert-print`, its own guarded afterprint listener — the three teardowns verified not to cross-fire), landscape via a TEMPORARY injected `<style id=fcCertPage>` `@page` removed in `_certExit` (premise correction: the generator's `pageSizeStyle` precedent is persistent-not-temporary; inject/remove is what actually isolates orientation — report + card sheet verified still portrait after a cert cycle). Double-rule ink frame (B/W-safe), name `dir="auto"` + esc() (XSS fixture renders inert), completion date = **max(passedAt)** (first-earn semantics ⇒ the day the final level was first passed; fixture proved max≠last-level), signature/date labels REUSED from the report keys. Entry points: final-level strip button + level-map footer (`close()` before print — the inline-styled overlay is invisible to print CSS), both + `openCertificate` gated on the DERIVED `ladderNextIndex === -1` (11/12 fixture: no buttons anywhere, direct call a no-op). 5 CSV keys (gate-2 approved as drafted: formal genderless award-citation HE, incl. a 5th `cert.suite` key so the HE certificate has no stray English). Verified headless: landscape 792×612 single page via `preferCSSPageSize`, EN+HE (fully RTL) × light/dark × 1280/800px, printToPDF's REAL afterprint exercised the production teardown path, 0 pageerror. SW v445→v446.
- [x] 2026-07-31 | 24d902f | flash_cards.html | **`?path=1` deep-links straight into the Learning Path level map** (maintainer plan prompt, feature A — the ladder's distribution mechanism; zero new strings): presence-based param handler at the DOMContentLoaded tail, registered AFTER the `applyI18n` registration so the `I18n.t()`-built map opens post-translation (boot-race verified: no raw keys in the modal, EN+HE); reuses `openLadderMap()` wholesale — the ladder-voiced profile prompt covers first-timers (create → map; cancel → clean setup); the URL is never stripped (bookmark + refresh re-entry is the point; matches `?wl`); coexists with `?wl` with no precedence logic (wl configures the setup view underneath, map on top — both intents verified honored). 0 pageerror across 6 scenarios.
- [x] 2026-07-31 | 62422cd | flash_cards.html + locales/ui-strings.csv | **The progress report states the student's Learning Path standing** (maintainer plan prompt — deliberately one line): after the summary stat grid, before the history-cap caveat, "Learning Path: Level {n} of {total} — {title}" via the existing `readLadderLevels`/`ladderNextIndex`/`ladderTitle` (n = first UNPASSED level, not the passed count), or a declarative complete line (deliberately NOT `finished_path` — that's the results strip's celebration register); omitted entirely when the profile has never touched the path (Strengths' omit-over-empty rule — no "Level 0 of 12" deficit framing on a parent-facing handout). 2 CSV keys (HE approved at gate-2 as drafted), `.fcr-ladder` prints upright 9pt ink vs the italic notes, whole string esc()'d + `.bidi-isolate`. **Measured call: the one-page Letter budget had only ~4px of slack and the line costs ~23px — paid for by sheet line-height 1.45→1.4 + `.fcr-row` padding 1.5pt→1pt (both imperceptible), leaving ~12px headroom, more than the budget had before the line.** Verified headless: 4 ladder states, EN+HE (RTL numeral placement confirmed on the print rendering), light+dark, maximal report (50 results / 6 focus / 6 strengths / 8 drills / cap note / line) = ONE Letter page, 0 pageerror. SW v444→v445.

## Archived from the working ledger at the S185 close-out (2026-08-06) — S180 session block

- [x] 2026-08-02 | (S180 close-out) | branch/deploy note | "/improveloop" (ultracode session) → S180 **continued on `claude/ultracode-effort-cicysb`, adding to the still-open draft PR #146** (open/draft/mergeable-clean, 0 check runs — no CI on this repo), per the previous-PR-open rule. **Drift check: none** — `origin/main` unmoved at 1dcd2d7; sw.js v448 + FM 5.6 matched; clone still full-history from S179's unshallow. **SW v448→v449** (precached changed: `torah_trainer.html`, `locales/en.json`, `locales/he.json`). **FM version NOT bumped** — the Font Maker was not touched. Scripts: `build-locales` ×1 (one new key), `check-i18n` clean, `update-sitemap.mjs` last. **Both gates asked AND answered — 6th consecutive zero-deferral session:** the session plan (2 P2 fixes + the share-link micro-feature) and the fullscreen behaviour call (honour the teacher's slider, over a projector-floor alternative). **⚠ PROTOCOL DIVERGENCE, flagged deliberately:** the maintainer's chosen plan put **3 iterations on `torah_trainer.html`** (2 fixes + the micro-feature's per-tool charge) against the variety governor's **2-per-tool cap**. Taken as an explicit maintainer decision, and defensible on the loop's own logic — the pass itself was on torah_trainer (passes are cap-exempt precisely so a pass's own tool can absorb its fixes) and this was the stalest tool in the loop's history at 103 sessions. Noted so the skill's cap can be reconciled with the gate-answer path. **Session note: the Pass-H workflow was STOPPED deliberately** once its walkthrough phase was complete and 3 verify verdicts were in — the verify fan-out was re-checking P3/P4 friction that was already logged and was blocking edits to the very file being fixed. One walkthrough agent (the printed-handout scenario) died mid-run; its surface was covered anyway by the top finding's own print-time receipts.
- [x] 2026-08-02 | ae58853 | torah_trainer.html + locales | **MICRO-FEATURE: "🔗 Copy link" — the Torah Trainer can finally hand a student the reading you just set up** (S180 Pass H's top seed, gate-1 approved on its first offer). The page had CONSUMED `?parsha=&v=` since the Trope Tutor handshake shipped but could not produce one, so a tutor with the aliyah configured had nothing to send: no link, no code, no `.ivrit` engine here. Adds `practiceLinkURL()` + `copyPracticeLink()` and one small consumer extension (an optional, regex-validated `?scope=`; no-scope links keep opening the full parsha). The button **hides** when the reading has no `?parsha=` form (custom range, holiday) instead of sitting there dead — the exact paper-cut this pass kept finding. **Zero new wording** (label reuses the dictionary's approved EN+HE under `torah.share.button`; confirmation reuses `shared.toast.copied`), no new persistent state, ~50 lines. **Verified fresh-profile** (empty localStorage AND IndexedDB) × both themes × both languages: real button → clipboard matches exactly; a brand-new profile opening the link lands on Ki Tavo / aliyah 3 / 26:16 with both selects synced; back-compat link → full parsha; a garbage scope (`"><img src=x>`) rejected without reaching the `<select>`; button genuinely absent on custom/holiday. 0 pageerror. **The fresh-profile arm earned its keep:** it caught `.tt-ctrl`'s `inline-flex` overriding the `hidden` attribute — the third time this file has hit that trap — fixed with the same `#id[hidden]{display:none}` its neighbours use.
- [x] 2026-08-02 | b7b7b7a | torah_trainer.html | **Projecting a reading now keeps the font size the teacher chose** (S180 Pass H). `body.fullscreen` hardcoded `font-size` on the reading elements, out-specifying the `--tt-heb-size`/`--tt-translit-size`/`--tt-english-size` vars the drawer sliders set — so the button the page sells as SmartBoard projection made 2.6rem Hebrew **23% smaller** (41.6→32px), in both directions, with the slider dead and its readout still reporting the value it no longer honoured, and `#gearBtn` a 0×0 box so there was no way to fix it without leaving fullscreen. **Verification corrected the walkthrough twice:** "Page view honours the slider" was FALSE (the reporter measured the wrapper `<p>`, not the `.tt-heb` spans — all three layouts shrink), and the real inconsistency is on the **English** row (interlinear 32→18.4px; side-by-side and page view untouched). Fix drops the `font-size` declarations, keeps the line-heights. **Verified by clicking the real `#fsBtn` into native fullscreen: 6 cells → 6 clean**, Hebrew 41.6→41.6 and English 32→32 in all three layouts, both themes, 0 pageerror.
- [x] 2026-08-02 | 1584f00 | torah_trainer.html | **Hiding nikkud no longer glues words together or desyncs the chant highlight** (S180 Pass H, the session's top find). `NIKKUD_RE` was the plain range U+05B0–U+05C7, which contains four PUNCTUATION codepoints — maqaf U+05BE, paseq U+05C0, sof pasuk U+05C3, nun hafukha U+05C6 — while its own comment said "nikkud only" and the sibling `TEAMIM_RE` already kept all four. Measured on every surface a teacher touches: `"וַֽיְהִי־אֽוֹר׃"` → `"ויהיאור"` (two words fused, verse-ending ׃ gone) on screen, in `verseCopyText()`'s clipboard string, and — snapshotted **inside `window.print()`** under `body.tt-handout-print` — on the **printed student handout**. **The consequence the walkthrough missed:** `tokenizeHebrew` splits on the maqaf because PocketTorah counts each side as a separate sung word and `data-twi` indexes the timings array directly, so hiding nikkud dropped the sung-word count **27→24** and every word after the first maqaf highlighted against the wrong audio timing. **Verified: 27→27 restored, per-verse 7/14/6, maqaf + sof pasuk survive on all four surfaces, trope tinting now identical (24 words) with nikkud on AND off** where the merge used to force the realign fallback; 8-cell matrix (light/1280 + dark/800 × nikkud × trope) clean, 0 pageerror.
- [x] 2026-08-02 | 4036048 | docs/IMPROVEMENT_LOG.md | **S180 Pass H logged** — `torah_trainer.html`'s 2nd-ever teacher walkthrough and its first since **S77, 103 sessions ago**, run as 4 parallel lesson-prep scenarios + adversarial verification. ~28 findings and **12 seeds — the queue had been empty for six sessions and is now full**. 3 of 4 scenarios reported; 2 independently found the fullscreen size bug.


- [x] 2026-08-02 | 9067d34 | docs/IMPROVEMENT_LOG.md | **S181 Pass G logged** — `torah_trainer.html`'s **first-ever dedicated G**, 4 surfaces over real PDFs at Letter AND A4, ~24 findings / 7 P2s + a large clean-with-receipts list | 4-agent workflow + adversarial verify; page counts read from PDF bytes, validated against a squeezed-page-box control (2→6)
- [x] 2026-08-02 | ce1eab2 | torah_trainer.html | **A4 paper silently discarded every Hebrew font-size choice** — the handout's 3 presets AND the teacher's slider, unconditionally, with the control still reading XL | 16/16 print-media cells agree Letter↔A4 across 2 layouts × light/dark × 4 sizes + a Page-view control; pagination now tracks Letter (handout large 3/3, xl 5/4 — was 3/2, 5/2); 0 pageerror
- [x] 2026-08-02 | a4c581d | torah_trainer.html | **Printing from dark mode washed out Page-view verse numbers (2.75:1) and printed the trope key as a near-black bar (1.30:1)** — the print dark-token reset was missing `--gold-text` and `--navy-deep` | contrast computed under print media in both themes: 2.75→**6.22:1** and 1.30→**14.81:1** on white; dark now prints identically to light; 0 pageerror
- [x] 2026-08-02 | (S181 post-close, **maintainer-authorized direct-to-main landing**) | branch/deploy note | The maintainer said **"Push to main"** in-session, which per the branch/PR protocol is the explicit authorization that supersedes the draft-PR rule (2nd ledgered precedent after S78). `claude/ultracode-effort-cicysb` was a **clean fast-forward** (`origin/main` 1dcd2d7 an ancestor of 15518c3, 0 behind, 15 commits ahead), pushed as `git push origin claude/ultracode-effort-cicysb:main` — so **all three sessions S179+S180+S181 landed together** and GitHub auto-marked **PR #146 merged** (this session was auto-unsubscribed from its activity; the hourly self check-in was deleted). `main` now at 15518c3, SW v450. Pages "pages build and deployment" run for 15518c3 verified after the push.

- [x] 2026-08-02 | (S182 close-out) | branch/deploy note | "Let's clear some backlog from the improve loop" → S182 ran on the **designated fresh branch `claude/improve-loop-backlog-qsosjk`**, cut from latest `origin/main` (4f1038b) per the S181 pointer (**PR #146 confirmed merged** 2026-08-02T04:13Z by the maintainer, so the previous-PR-open rule did not apply). **Draft PR opened at close-out.** **Drift check CLEAN** — the only commit past S181's last code commit was 4f1038b, ledger-only; `sw.js` v450 and `FONT_MAKER_VERSION` 5.6 both matched the S181 close-out note, nothing landed outside the loop. **All four decision gates were asked in ONE batch at session start** (the maintainer was present) and all four took the recommended option: (1) **the 2-per-tool variety cap was explicitly OVERRIDDEN** — the standing protocol question the ledger raised at S180 and S181 is now answered, **a gate answer outranks the cap**; (2) **Pass D skipped by direction** (all 5 iterations spent on fixes); (3) gate-2 on the transliteration column → **collapse AND explain on screen**; (4) gate-1 micro-feature → **skipped**. `sw.js` **v450→v451** (torah_trainer.html + locales/en.json + locales/he.json are all precached). **FONT_MAKER_VERSION untouched** — the Font Maker was not edited. `build-locales.js` run (2 new keys, both translated); `check-i18n.js` clean on both gates; `update-sitemap.mjs` run last. Deploy runs on merge to `main`; Pages `success` is verified after the merge.

- [x] 2026-08-02 | ede3b22 | torah_trainer.html | **Row separators print as one line instead of three stubs at different heights** (S181 Pass G P3). The parallel print layout makes the column wrappers `display:contents`, so every CELL becomes a direct grid item — which quietly turns the screen's `.tt-grid{align-items:start}` into a paper bug: each cell shrinks to its own content height while `.tt-print-parallel .tt-grid-row` draws its `border-bottom` per cell. Measured on a 14-verse reading, cell bottoms within one row spread by up to **158.39px (avg 123.42px)** across three columns and **54.00px** across two, identical Letter/A4. `align-items:stretch` → **0.00px on all four combinations** (reproduces S181's agent measurement, which reported 182.3/125.2). Free: the row height was already set by its tallest cell, so pagination is identical at 10/18/26/34 verses on both papers, and the S53 one-grid-row-per-verse guarantee still holds (asserted). **Second instance of print-media-leak** — a screen-correct declaration that is wrong once print restructures the box tree.

- [x] 2026-08-02 | af90983 | torah_trainer.html + locales | **An unreachable transliterator no longer prints a labelled EMPTY column** (S181 Pass G P2, **gate-2 approved: collapse AND explain**). Offline / CDN outage / school network blocking esm.sh → `applyTranslit()` returns `''` for every verse, and the layouts emitted the column regardless: side-by-side reserved a labelled empty band at **31.4% of the sheet**, interlinear stamped a "Translit" label over an empty row on **every** verse, and the toggle still read ON. Gated **once in `renderText()`** where all three layouts read it (better than the drafted per-function fix); `.every()` short-circuits on the first verse that transliterates, so a live library costs one extra call. The maintainer chose collapse-**and**-explain over silent collapse, so a muted `.tt-notice-chip` (neutral sibling of the שם ה׳ chip — it reports a limitation, not holiness) appears beside the reference header with an accessible tooltip naming the likely cause; screen-only, print-hidden. 2 new CSV keys, both translated. Verified over sidebyside/interlinear/biblepage × EN/HE, plus a **stubbed LIVE library** proving the happy path untouched, plus the printed grid dropping 3→2 columns dead and staying 3 live.

- [x] 2026-08-02 | (attempted, **REVERTED**) | torah_trainer.html | **The לגניזה widow fix does NOT work — the drafted one-liner is refuted by measurement** (S181 Pass G P2). `break-before:avoid-page; page-break-before:avoid` was added to the existing `body.tt-handout-geniza #ttGenizaMark` rule and **measured to change nothing**: with the rule shipped vs forcibly disabled, the orphan count was **1/13 in BOTH arms**. Cause: the marker's predecessor in the box tree is `#ttReading` — one block already fragmented across pages — not the last verse, so there is nothing `avoid` can legitimately pull back. Reverted rather than shipping dead CSS. **Two things the session DID establish:** (a) a **natural repro**, which S181 did not have in this form — interlinear, handout `large`, Letter, **15 verses** prints `3p [478,617,6]`, that final sheet carrying only the six letters of לגניזה; (b) S181's reported receipt (Letter n=13 → 2 vs 3 pages) **did not reproduce on the pre-session baseline either** (0/14 on Letter and A4 with synthetic verses), so the trigger is sensitive to real verse lengths. See the re-logged candidate — it needs a DOM grouping change, not a CSS declaration, and is **re-sized P2→P3/M**.

- [x] 2026-08-02 | 864c475 | torah_trainer.html | **The printed clause underline no longer butts into the te'amim below it** (S181 Pass G P2). Print swaps the trope background tint for a 4px clause rule; at the screen leading of 1.7 that rule has **0px of separation** from the next line's cantillation — it ends on the row immediately before the marks begin, Letter and A4 alike. `line-height:1.95` for trope-on Hebrew → **6–7px**. Scoped to `body.trope-on` because it is the band, not the text, that needs the room: the leading costs one sheet in **2 of 16** long interlinear configurations and no uncoloured print should pay it. **Deliberately not fixed by shrinking `text-underline-offset`**, which would have been paper-free — the nikkud highlight fallback already owns 0.4em+2px and the two-tier decoration hierarchy has to survive both layers being on. **Corrects S181 on two counts:** the band **clears** by 0–1px rather than cutting 2.5px *into* the marks (two independent methods agreed), and the scope is not interlinear-only — side-by-side carries the same 1.7 leading and the same rule, so `.tt-cell.heb` needed it too.

- [x] 2026-08-02 | 6896ba5 | torah_trainer.html | **A short reading no longer spills onto an extra sheet of paper** (S181 Pass G P2). The boxes below the last line paginate like content, so a reading ending near a boundary pushes empty box onto a sheet of its own. Measured under print emulation at the true printable width: **44px** of trailing dead space in Page view on Letter, **28px** interlinear and side-by-side, 6px less on A4 (the mobile block's narrower `.tt-page` padding reaching print). Contributors: `.tt-page`'s bottom padding, `.tt-reading`'s, and the last block's own margin/padding — none of which does any work on paper, since the print dialog's page margin is what holds text off the sheet edge. Zeroing all four → **0.00px trailing in all six layout × paper combinations**; across **70 real PDFs, 4 dropped a whole sheet and none gained one**. **Corrects S181's characterisation:** the wasted sheet is not reliably BLANK — all 4 recovered cases carried the reading's tail (152–201 text runs against ~2000 on the sheet before), and an isolated control shows the trailing box pulls the last line over with it. Rate matches (4/70 here, 4/56 there); it was the "fully blank" wording that was too strong.

_(Archived at the S188 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-02 | (S182+S183 post-close, **maintainer-authorized direct-to-main landing**) | branch/deploy note | The maintainer said **"Push to main"** after the S183 close-out, which supersedes the loop's draft-PR rule (**third ledgered precedent**, after S78 and the 2026-07-31 / 2026-08-02 landings). Verified a **strict fast-forward first** — `git merge-base --is-ancestor origin/main HEAD` YES, 13 ahead / 0 behind, clean tree — then `git push origin HEAD:main`: `4f1038b..86cd042`, **no merge commit**. Lands **S182 + S183 together** (9 fixes + 1 attempted-reverted; `sw.js` **v450→v452**; FM unchanged 5.6). **PR #147 auto-closed as merged** (17:38:55Z) and the session was auto-unsubscribed from its activity. **Deploy verified in-session: `pages build and deployment` run 30759340532 for `86cd042` concluded `success`** (17:38:55Z) — the site is live. The pending hourly PR check-in was retired rather than left to fire against a merged PR. **Next loop session cuts a FRESH `claude/*` branch off latest `origin/main` (86cd042) — never reuse #147.**

- [x] 2026-08-02 | (S183 close-out) | branch/deploy note | "more backlog work please" → S183 **continued on `claude/improve-loop-backlog-qsosjk` and its still-open draft PR #147** (open/draft, 0 check runs — no CI on this repo), per the previous-PR-open rule. **Drift check CLEAN** — `origin/main` still at 4f1038b, `sw.js` v451 and `FONT_MAKER_VERSION` 5.6 exactly as S182 left them; nothing landed outside the loop. **Pass skipped a SECOND consecutive time by explicit direction** (backlog clearance) — **D is now 14 sessions stale and should be run next session regardless of framing**. **Variety restored after S182's 5/5 single-tool session:** font-maker 1, index 1, generator 1, flash-cards 1, dashboard 1, dictionary 2, torah-trainer 1 — **no cap override needed or used**. One gate-2 question asked and answered (the chant-failure message). Gate 1 not re-offered: the maintainer answered "skip" in the same continuous conversation one session earlier and the queue is unchanged. `sw.js` **v451→v452** (torah_trainer, hebrew_dictionary, flash_cards, hebrew_blend_generator, classroom_dashboard, Hebrew_Font_Maker, index + both locale JSONs — 8 precached files). **FONT_MAKER_VERSION untouched at 5.6:** the Font Maker change is a pure a11y fix, not a feature, and the versioning rule bumps on features. `build-locales.js` run (4 new keys, all translated); `check-i18n.js` clean on both gates; `update-sitemap.mjs` run last.
- [x] 2026-08-02 | a6bf328 | torah_trainer.html + locales | **Chanting that cannot load its audio now says so instead of going quiet** (S180 Pass H P2). `loadKaraoke()` wired play/pause/loadedmetadata/timeupdate/ended but **no error handler**, so a blocked CDN gave 15 s of silence with the Chant-all button still reading "⏹ Stop chant" and `#ttStatus` empty — the tool's core feature failing with no message. Now reported on the status line (error-flagged), as a toast, and in the drawer diagnostic; chant-all state cleared so the Stop control stops lying; loop + highlight dropped; the queue abandoned rather than advanced (every aliyah is on the same unreachable host). **Gate-2 approved copy**, 1 new key, translated — the cause is named because the common real one is a school network, which is exactly why the existing `chant_unavailable` was the wrong string to reuse. Verified against a real `MediaError code 4` in EN+HE, with a negative control proving silence on a reachable, decodable file.
- [x] 2026-08-02 | 7d03f6f | hebrew_dictionary.html + locales | **Bulk-copy mode is usable by screen reader** (S178 Pass C P2, all three parts). (a) 100 nameless checkboxes per page → "Select {word}", 100 distinct AX names; (b) selection was a CSS class only, so cards announced the same "view details" label selected or not — a `setCardBulkState()` helper now moves `.bulk-selected` **and** `aria-pressed` together and **every** mutator routes through it (card click, checkbox change, selectAllOnPage, selectAllMatching, clearBulkSelection, and both card builders), with the attribute **removed** outside bulk mode because the card is then a plain button, not a toggle; (c) the readonly output textarea named. Copy gate-approved at S178 incl. Hebrew; 2 new keys. Verified via Chromium's AX tree in EN+HE: 0 nameless, 100/100 exposing `aria-pressed`, select-page → 100 and clear → 0.
- [x] 2026-08-02 | 4789548 | hebrew_dictionary.html + locales | **Keyboard users can skip past the 100-card results grid** (S178 Pass C P2). **Measured: 380 Tab presses** from the search box to "Show more" — S178 estimated ~300, the real number is higher. A WCAG 2.4.1 bypass link now sits immediately before `#resultsGrid` (the placement the pattern requires — in front of the repeated block, not up in the header) and lands on a `#resultsEnd` anchor just before the button, **saving 299 stops**. Visually hidden until focus, then it takes normal flow: this link is mid-page and has no positioned ancestor to anchor against, unlike the suite's header skip links. Copy gate-approved at S178 incl. Hebrew; 1 new key.
- [x] 2026-08-02 | 21e6f54 | hebrew_blend_generator.html + flash_cards.html + classroom_dashboard.html | **Four validation dead-ends now explain themselves in the panel, not in a modal** (S179 Pass A P2, 4 sites, 0 new copy). generator `generate()` + `generateNameTheLetter()` → `_setGenerateNote`; flash cards `startPractice()` (which dumped its whole issue list through `alert(issues.join('\n'))`) + the empty-deck case → a new `_setStartNote`; dashboard `savePreset()` → a `_setPresetNameNote` the migration had never reached. Each stores the **KEY** so `applyI18n` can re-render a live note, each is wired into its page's `applyI18n`, each sets `aria-disabled` (not `disabled`) so the control stays focusable, and each note **retires when the input is fixed** rather than requiring another button press. Verified over all four paths × EN/HE (no dialog, translated note, both flash-cards issues listed, card screen not entered) plus happy-path regression on all three tools. Census: generator 16→14, flash cards 19→17, dashboard 11→10.
- [x] 2026-08-02 | 03f64a4 | Hebrew_Font_Maker.html + index.html | **A screen reader names the My Fonts row buttons instead of reading "trash can"** (S179 Pass A P2, symbol-only accessible name). Name-from-content beats `title`, so ⬇/🗑 were announced as bare glyphs once per saved font — the **destructive** one included. `aria-label` mirrored from the already-translated `title` (the ddc9180 precedent), 0 new copy. Verified through Chromium's AX tree with 2 fonts seeded into the shared IndexedDB store, both carriers × EN/HE: **4 symbol-only names → 0**, with the pre-fix code re-run as a negative control. **Still open (gate 2):** the buttons name the ACTION but not the ROW, so ten fonts give ten identical "Remove from My Fonts" — needs a `{name}`-parameterised key.

_(Archived at the S189 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-06 | (S184 close-out) | branch/deploy note | "Let's work on clearing the backlog in the improveloop" → S184 ran on the designated fresh branch `claude/ultracode-effort-bssphe`, cut from `origin/main` at `dbad5a6` (PR #147 gone — S182+S183 had landed direct-to-main, so no open PR to continue). **Drift check CLEAN**: HEAD was byte-identical to `origin/main`, `sw.js` v452, FM 5.6 — exactly where S183's close-out left it, nothing landed outside the loop. **Ran Pass D despite a third consecutive backlog framing**, per the S183 pointer's explicit ⚑ instruction. Draft **PR #148** was opened after the first two fixes; the maintainer then said **"Push to main"** mid-session (**fourth ledgered direct-to-main authorization**, after S78/S181/S183), so the branch was fast-forwarded onto `main` — which auto-closed #148 as merged. **Two `sw.js` bumps this session, deliberately**: v452→v453 accompanied the mid-session landing (3 precached files: generator, flash cards, dictionary) and v453→v454 the close-out landing (torah_trainer + this ledger). The once-per-session rule assumes one deploy; a maintainer-directed mid-session landing is a second deploy and needs its own cache name, or the live site serves stale HTML. **FONT_MAKER_VERSION unchanged (5.6)** — no Font Maker change this session. Scripts run: `check-i18n.js` (clean, 0 new violations — no new user-facing strings anywhere in the session), `update-sitemap.mjs` twice (last as the final step). **No gate questions asked**: all four fixes were mechanical with zero new copy, and no micro-feature was offered because the budget went to the pass + the P2 shelf. **Nothing deferred to the maintainer.**

- [x] 2026-08-06 | 7a5cfca | torah_trainer.html | **"Include translation" in the copy bar now actually copies the translation** (S180 Pass H P2, part (a)). English was fetched only when the DISPLAY toggle asked for it — `fetchSefariaText(ref, settings.showTranslation, …)` — so a teacher with the copy option ticked and the English row off screen got `translation:''` on every verse and a Hebrew-only clipboard, silently. `fetchAndRender` now asks when EITHER toggle wants it, and ticking `#ttCopyIncTrans` on a Hebrew-only reading re-fetches once (same shape as the translit toggle directly above, which already kicked its lazy load). Display unaffected — `renderText` still gates the English row on `showTranslation` alone. Part (b) — the empty English row on a fresh profile — is re-scoped and still open as a gate-2 candidate. | **Method note that nearly cost a false CLEAN:** the harness was answering every Sefaria request with an English version whether or not one was requested, which made the bug look already-fixed (checks A and B PASSED on unfixed code). Keying the fixture off `version=english`, as the real API is, flipped both to FAIL pre-fix. 36/36 post-fix across light/dark × 1280/800, plus a both-themes regression check that display-translation still renders 20 English rows and the Hebrew-only path still renders 0 with an empty status.
- [x] 2026-08-06 | 6ac733a | torah_trainer.html | **Hiding vowels or te'amim now updates the copy preview, not just the screen** (S180 Pass H P2). `verseCopyText()` strips both to match the display toggles, but the `#optShowCantillation`/`#optShowNikkud` handlers called only `renderText()`, never `updateCopyTextarea()` — so with verses selected, "Copy All" put marks on the clipboard the screen no longer showed (a teacher building a nikkud-free worksheet pasted fully pointed text into it). | 16/16 assertions across light/dark × 1280/800, reproduced pre-fix. **Method note:** the probe first asserted on sof pasuk and paseq too and FAILED a correct implementation — `TEAMIM_RE` is U+0591–U+05AF, cantillation only; sof pasuk (U+05C3) is punctuation and is deliberately kept (the S180 `NIKKUD_RE` lesson, in mirror image).
- [x] 2026-08-06 | 32be832 | hebrew_dictionary.html | **Dictionary mode buttons say which one is on — and stop blanking themselves** (S178 Pass C, class-only-selected-state, all 3 groups). Words/Emojis/Shoresh, emoji gender All/♂/♀, and colour-coding Letter/Highlight/Underline marked the live choice with `.active` only; all nine buttons now carry `aria-pressed`, seeded in markup and synced through one `setSegActive()` helper so class and ARIA cannot drift. **Grounding found a second, visible bug:** the mode switcher reuses the `.emoji-gender-btn` class and two sync sites swept it unscoped — measured against the pre-fix file, picking an emoji gender left the mode switcher with NO highlighted button at all (`words:false emojis:false shoresh:false`), and the same sweep runs on load when a gender was saved without emoji mode. Both scoped to `[data-gender]`, matching the third site that already was. | 44/44 assertions across light/dark × 1280/800 covering load, each switch, and the saved-gender restore path.
- [x] 2026-08-06 | 1f5b9c2 | flash_cards.html + hebrew_blend_generator.html | **Saving a setting no longer re-reads your whole word-list library once per list** (S179 Pass A, parse-per-call on a growing store — the class's first fix). `getSettings()` resolved every selected list id through `wlGetListName()`, each call re-parsing the entire `ivritSuite_wordLists` blob via the reviver-based `ivritSafeParse`; `saveSettings()` calls `getSettings()` on every control change with no debounce, so touching any control cost one full parse per selected list. Both files gain `wlNamesForIds(ids)` — one store read for the whole set. **A regression was caught before commit:** the first version always read the store, which would have ADDED a parse for the majority of teachers who select no lists; the committed version returns early on an empty set. | 40/40 assertions across both tools × light/dark × 1280/800: output byte-identical to the replaced per-id expression, 6 ids cost 1 read where the old path cost 6, empty costs 0.
- [x] 2026-08-06 | (S184 Pass D — performance, one tool) | torah_trainer.html | **Pass D — `torah_trainer.html`, its first dedicated performance pass since S66 (118 sessions), and the tool that has since absorbed the copy bar, bulk verse select, jump nav, three layouts, chant-all, verse loop, trope colour coding, the print pipeline and the share link — none of it ever profiled.** Run over a purpose-built harness (local HTTP, service workers blocked, Sefaria fulfilled with 146 verses of real marked Hebrew harvested from `data/trope/trope_index.json`, PocketTorah MP3 fulfilled with a decodable PCM WAV since headless Chromium has no MP3 decoder, the transliteration ESM served at its real esm.sh URL so the page's own loader path runs unchanged; baseline 3,229 `.tt-word` / 4,269 `.nik` / 21,320 px). **PARTIAL PASS — 2 of 5 surfaces.** The 5-surface workflow was killed when the session was suspended; boot/render and toggle-churn reported, **copy+bulk-select scaling, karaoke runtime, and session hygiene/leaks never ran**, and neither did the adversarial-verify phase — so both headline findings were re-measured by hand in two separate process runs before logging, **which corrected one**: `toggleDark` was reported as a steady 212–232 ms but re-measured at medians of 352 ms and 161 ms (samples 148–444), while the `content-visibility` remedy held at 90 ms in both runs — a ~2–4× win, not the ~7× a single baseline implied. **Headline finds:** every translit-enabled cold load renders the whole parsha twice and paints a false "Transliteration unavailable" chip on the discarded pass (exactly 2 reading writes both runs, 134/143 ms apart); and colour/font drags run at ~10–24 fps on a full parsha because the reading is one enormous restyle scope. **Clean receipts:** S60/S66's no-reading-rebuild invariant STILL HOLDS and now covers everything added since — 0 `#ttReading` mutations for `toggleDark`, all three colour modes, `setVowelScheme` both ways, and both colour pickers, re-confirmed independently; `renderText` is cleanly linear (5.3/13.4/35.2/75.2 ms at 20/60/146/300 verses, ~0.24 ms/verse flat) with **zero** forced layout in its tail (CDP LayoutCount 0, RecalcStyleCount 0 across a 499 KB innerHTML write); `saveSettings` debouncing verified by patching `Storage.prototype.setItem` — 40 rapid picker inputs → exactly 1 write; init's UI builders all sub-millisecond; 2,064 `I18n.t()` calls per render cost a combined 1.1 ms; Bible Page is the cheapest layout (24.9 vs 32.3 vs 34.5 ms). 0 pageerrors in every probe. | discovery only, no code; 4 new candidates + 1 informational logged

_(Archived at the S190 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-06 | (S185 post-close, **maintainer-authorized direct-to-main landing**) | branch/deploy note | The maintainer said **"push to main"** after the S185 close-out, which per the branch/PR protocol is the explicit authorization that supersedes the draft-PR rule (**fourth ledgered precedent**, after S78, the 2026-07-31 / 2026-08-02 landings, and S184). Verified a **strict fast-forward first** — `git merge-base --is-ancestor origin/main HEAD` passed and `rev-list --left-right --count` read `0 6`, so `main` moved `724a1b3 → b199f99` with no merge commit and nothing of the maintainer's overwritten. All six S185 commits landed: the four fixes, the close-out (ledger + `sw.js` v454→v455), and the sitemap refresh. **PR #149 auto-closed as merged** on the push; per the subscription rules it is the final outcome and is not to be reopened. **Pages deploy verified after the landing** (the deploy only runs on `main`).

- [x] 2026-08-06 | (S185 close-out) | branch/deploy note | "Let's work on clearing the improveloop backlog" → S185 ran on the **designated fresh branch `claude/improveloop-backlog-clearing-3385j3`**, cut from latest `origin/main` (`724a1b3`); PR #148 had auto-closed merged when S184 was fast-forwarded to `main`, so the previous-PR-open rule did not apply. **Draft PR opened at close-out. Drift check CLEAN** — HEAD byte-identical to `origin/main`, `sw.js` v454 and `FONT_MAKER_VERSION` 5.6 exactly as S184 left them; the newest commit on `main` was S184's own sitemap refresh, so nothing landed out of band. **SW v454→v455** in the final commit (three precached files changed: `torah_trainer.html`, `flash_cards.html`, `Hebrew_Font_Maker.html`). **FONT_MAKER_VERSION NOT bumped** — the FM change is a pure accessibility fix (one `aria-label` reusing an existing translated string), not a feature; the rule bumps on release features only. Scripts run: `check-i18n` **clean** (Check A clean + 14 baselined, Check B clean — no CSV keys were added, so `build-locales` was not needed), `update-sitemap.mjs` as the last step. **Iterations 5/5: 1 pass + 4 fixes.** Variety: torah-trainer ×2 (at cap, the pass is cap-exempt), flash-cards ×1, font-maker ×1 — no cap hit, no override needed. **Decisions asked and answered:** gate 1 → skip the micro-feature, spend all 4 iterations on backlog (all 11 open seeds are torah_trainer, which was already at its cap); gate 2 → the surviving TTS third of the P2 gets its English fixed with the placement left alone. **No decisions deferred.** **Protocol divergence to flag for the skill:** the Pass-D fan-out was attempted as a Workflow first and every subagent hit the ledgered S174 environment fault (tool parameters stripped before execution — Bash/Read/Grep/Glob all failed identically), so 3 agents burned ~237k tokens for 0 findings; the pass was then run **inline**, which is the remedy the S174 entry already recorded. **Treat subagent fan-out as unavailable in this container until a session proves otherwise.** Deploy lands on merge.

- [x] 2026-08-06 | 75593d1 | Hebrew_Font_Maker.html | **Both kerning-side dropdowns now announce which side they set** (S179 Pass A, nameless-adjacent-text). The sibling `<input>` took its name from `sideLabel`; the class `<select>` below it got nothing, and its visible caption is a plain `<div class="metric-sub">` with no association mechanism — so a screen-reader user editing a kern pair met two unnamed comboboxes. Reuses the same already-translated variable, 0 new copy. | AX-tree verified EN+HE × light/dark × 1280/800, after forcing the `display:none` ancestor chain visible (the probe returned nothing until then — the S183 note again) and asserting a real 166×24 box first
- [x] 2026-08-06 | 94d1222 | flash_cards.html | **A Flash Cards backup no longer wipes the student's personal-best streak** (S179 Pass A, ivrit-gather-gap — the class's 3rd fix). `hebrewFlashCards_pbStreak` rode along in index.html's AllTools bundle but appeared in neither of the tool's own two save paths nor in `apply()`, so a save-and-restore from the Flash Cards page handed the student back a 🏆 of 0. Now carried by `gather()` and by `flashBackupSave()`'s profiles/both scopes, and restored with the **max** rule AllTools already uses, so the two import paths agree and a restore can never lower a better streak. | 6 round-trip assertions × light/dark: gather 17, merge 3→17, stored 42 survives incoming 9, replace takes 9, absent key leaves 25, presets-only omits it; 0 pageerror
- [x] 2026-08-06 | f5a0d72 | torah_trainer.html | **In a Hebrew UI the English column no longer throws its full stops to the wrong end** (S181 Pass G). Side-by-side was the only layout whose Latin-script columns were never pinned LTR, so they inherited the page's rtl base and every sentence-final period landed at the start — on screen and on paper. **The candidate's drafted one-liner was corrected before shipping:** pinning the COLUMN would also have flipped the `<h3>` headers, which are translated chrome (אנגלית / תעתיק), not Latin content; pinning `.tt-grid-row` fixes the cells and the verse number while the headers keep following the UI language. | EN+HE × light/dark × 1280/800 on 146 verses, **screen AND print media**: English rows ltr, Hebrew rows rtl, headers per UI language
- [x] 2026-08-06 | 30fe852 | torah_trainer.html | **The "no Hebrew voice" warning now speaks the teacher's language** (S180 Pass H P4 + the surviving third of the S180 P2; gate-2 answered "fix the English, keep the placement"). The literal was hardcoded English while `torah.audio.tts_no_voice` sat authored, translated and unreferenced. **The naive fix would have shipped a raw key and a listener leak:** the function runs before `I18n.ready` and `applyI18n` never re-ran it (pre-ready-i18n class, 4th instance), and its `check()` was a fresh closure registered on `speechSynthesis` — which survives every re-render — so re-running the old shape from `applyI18n` would have stacked a `voiceschanged` listener per language switch. A named `syncTTSVoiceStatus()` solves both. | EN+HE × light/dark × 1280/800: real Hebrew on screen, 5× applyI18n adds **0** listeners, 0 pageerror
- [x] 2026-08-06 | (S185 Pass D — performance, one tool) | torah_trainer.html | **Pass D FINISHED — the three surfaces S184's suspended session never profiled, and all three are CLEAN.** Rebuilt S184's harness from its ledger description (local HTTP, service workers blocked, Sefaria fulfilled with 146 verses of REAL marked Hebrew assembled from `data/trope/trope_index.json`, PocketTorah MP3 fulfilled with a decodable 20 s PCM WAV, transliteration ESM served at its real esm.sh URL; baseline 146 verses / 3,205 `.tt-word` / 14,433 px — deliberately the same scale as S184's 3,229). **(1) Copy bar + bulk select at scale — the S184 suspicion is REFUTED.** `syncBulkClasses` IS the whole-document sweep it looks like (146 elements + an inner `querySelector` each), but it costs a FLAT **0.4 ms** whether 1 or 146 verses are selected; `updateCopyTextarea` is linear in N-selected at 0.2 ms → **1.1 ms** at 146 (2.4 ms med with all three format toggles on). The Nth checkbox click totals **0.5–1.5 ms** at every N — an order of magnitude under the 16 ms perceptibility bar. `selectAllVerses` 11.5 ms at 146; the zero-selected case 0 ms; 20 real clicks cost 24 layouts / 6 ms total. The S60/S66 no-rebuild invariant HOLDS across bulk mode and the copy bar (0 childList rebuilds; the 145 records on bulk-off are the un-tint attribute writes). **(2) Karaoke runtime — CLEAN and, the useful part, LENGTH-INDEPENDENT.** Over a real 15 s chant with the genuine Bereshit-1 timing curve (469 words) compressed onto the fixture: `onAudioTimeUpdate` self-time **med 0.6 ms / p95 0.9 / max 1.0**, 37 ms of main thread over 15 s = **0.25% of wall clock** at a highlight rate ~3.6× faster than the real 469-changes-per-445 s, so the real cost is nearer **0.07%**. `getBoundingClientRect()` in the tick handler DOES force exactly **1 layout per highlight change** (1.21/change measured; idle produces 0) — but at **~0.24 ms** each, and a genuinely short reading (12 verses / 255 words / 1,184 px, run as its own fixture) measures the SAME 0.6 ms tick and the same 0.014 s of layout, so the cost does not grow with the parsha. `matchMedia` is per-scroll (4 in 15 s), NOT per tick as inspection suggested; `buildKaraokeWordSpans` runs 0 times during playback; `paintKaraokeIdx` med 0.0 ms. **(3) Session hygiene — CLEAN, and the retired `listener/interval accumulation` pattern STAYS DEAD on the suite's most-changed tool.** Across 12 parsha loads with forced GC before each reading, DOM nodes **15,983 → 15,983**, jsEventListeners **819 → 819**, live elements **5,619 → 5,619** — zero growth, reproduced identically in a second process. `setupVerseObserver` DOES disconnect its predecessor: **9 constructed / 9 disconnected** over 9 further loads, 146 `.observe()` calls each. Zero `setInterval` anywhere on the page; 0 MutationObserver/ResizeObserver; 8 chant start/stop cycles and 12 layout switches add no net listeners on any surviving target. | discovery only, no code; 0 new defects, 1 informational candidate (`textCache`), 3 clean receipts
- [x] 2026-08-07 | (S186 post-close, **maintainer-authorized direct-to-main landing**) | branch/deploy note | The maintainer said **"Push to main"** after the S186 close-out — the explicit authorization that supersedes the draft-PR rule (**fifth ledgered precedent**, after S78 and the S181/S183/S184/S185-era landings). Verified a **strict fast-forward first** — `git merge-base --is-ancestor origin/main HEAD` passed, 5 ahead / 0 behind, clean tree — then this record was committed on the branch and `git push origin HEAD:main` landed the 6 commits in ONE push (one deploy), auto-closing draft PR #152 as merged. **No extra sw.js bump**: this is the session's single deploy and v459 (bumped at close-out) is its cache name. Pages-deploy `success` verified right after the push (or re-triggered per the CLAUDE.md deploy playbook if it failed — see the session transcript's final report for the run result).

- [x] 2026-08-07 | (S186 close-out) | branch/deploy note | "Let's work on the improveloop backlog" → S186 ran on the designated branch `claude/improveloop-backlog-2l8drf` (existing on origin and byte-identical to `origin/main` at 523cef4 — its earlier work had already been landed on main, so this was effectively a fresh cut; **no open PR existed anywhere**, draft PR opened at close-out). **Drift check: SUBSTANTIAL out-of-band landings since S185's close** — the Font Maker **onboarding wizard** (7f87891) + **Draw mode** (ae3c6af; FM 5.6→**5.7**), and the **dashboard weekly Schedule Sync grid** (79beddc..523cef4, 10 commits, ~1,400 lines), sw.js v455→**v458**, CLAUDE.md updated for both, and the ledger already carrying their two seed-intake blocks. **Pass I was the staleness pick AND landed exactly on that new surface — fully clean** (rotation row has the receipts). A **fifth consecutive backlog framing, and the pass ran anyway** (the S184/S185 precedent held). **SW v458→v459** (precached files edited: flash_cards, classroom_dashboard, Hebrew_Font_Maker, index, torah_trainer, locales/en+he). **FONT_MAKER_VERSION stays 5.7** — S186's FM edits are a11y-attribute fixes, not a feature. Scripts run: `build-locales.js` (2 new shared.fonts.* keys, both translated), `check-i18n.js` (clean — Check A clean + 14 baselined, Check B clean), `update-sitemap.mjs` (last step). **Gates: 2 asked at the post-pass batch, 2 answered** — gate 1 micro-feature **skipped** (recommended default; 25 seeds stay queued), gate 2 My Fonts row-naming copy **approved as drafted and shipped this session** (cd36f76). **Carry-overs for S187:** the gate-APPROVED FM 'no ink detected' P4 was **cap-blocked** (FM spent its 2 iterations on c566f50 + cd36f76) — build it next session without re-asking; the P4 pwa-dismissed erase gap was priority-displaced by the gate-2 approval (two-line fix, still open).
- [x] 2026-08-07 | 62a9981 | torah_trainer.html | **The transliteration status line now heals from a pre-ready write and follows language switches** (S179 Pass A, pre-ready-i18n — the class's 5th fix). `wireTranslit()`'s loaded/failed listeners could fire while the locale JSON was still in flight, freezing `#optTranslitStatus` at a literal key that no later language switch fixed. Both writers now record `dataset.i18nKey` and the existing `applyI18n` status healer re-resolves it — the exact idiom `#ttStatus` already uses, zero new listeners (the S185 leak trap does not arise: this is a read-only heal, no re-registration). | Verified by reproducing the exact frozen state (locale fetch delayed 2.5 s, `translitlibfailed` dispatched inside the pre-ready window → raw key on screen), then asserting the heal on locale arrival, EN→HE live-switch follow on both arms, `applyI18n` ×5 idempotent, 0 pageerror, light/1280 + dark/800.
- [x] 2026-08-07 | cd36f76 | Hebrew_Font_Maker.html + index.html + locales | **Every My Fonts row's Remove/Download button now says which font it acts on** (S179 Pass A residual, gate-2 approved this session). S183's 03f64a4 gave the ⬇/🗑 buttons real action names, but ten saved fonts still announced ten identical "Remove from My Fonts". The aria-labels are now {name}-parameterised via two new `shared.fonts.*` keys (EN "Remove {name} from My Fonts"/"Download {name}", HE approved as drafted); the visible tooltips stay the generic action; index's `mk()` helper gained an `aria` arg so the Use button is untouched. | Verified with 2 fonts seeded into the shared IndexedDB store on BOTH carriers via Chromium's AX tree: per-row distinct names in EN and HE, titles unchanged, 0 pageerror (FM leg also covered dark/800).
- [x] 2026-08-07 | c566f50 | flash_cards.html + Hebrew_Font_Maker.html | **Three dialog inputs get real accessible names instead of placeholder-or-nothing** (S179 Pass A, placeholder-as-only-accessible-name — all 3 sites closed). flash_cards' new-profile name input (focused on modal open — the first stop for a `?path=1` arrival), its card-count Custom spinbutton (announced as "#"), and FM's pre-filled save-as input (whose placeholder never renders) now carry `aria-label` reusing each dialog's existing translated string — 0 new copy. | AX-verified via CDP `getPartialAXTree` in EN+HE × light/dark: "Student name…"/"שם התלמיד…", "Custom:"/"מותאם:" (role spinbutton kept), "Font name"/"שם הפונט" with the pre-fill intact; 0 pageerror.
- [x] 2026-08-07 | f3800c8 | flash_cards.html + classroom_dashboard.html | **Backup-scope and emoji-set pickers announce which option is on** (S179 Pass A, class-only-selected-state — the 2 open arms closed; the class is back to 0 known open groups). flash_cards' Presets/Profiles/Both pills (sitting beside an Auto/Manual pair that already had `aria-pressed`) and the dashboard's 8 `#emojiSetBtns` now seed `aria-pressed` in markup and sync it at every `classList.toggle('active', …)` site — class and ARIA cannot drift. | Verified light/1280 + dark/800: initial state, programmatic + click transitions, drawer close/reopen re-sync (dashboard), exactly one pressed=true per group at every step; AX tree exposes pressed on the collapsed-panel buttons once the panel is expanded (the S183 display:none-drops-AX-nodes trap, respected in the probe, not the product).
- [x] 2026-08-07 | (S186 Pass I — first-load & empty-state) | all 12 pages + the two out-of-band feature surfaces | **15th run, FULLY CLEAN — and the first first-run audit of the FM wizard/Draw mode and the dashboard weekly grid.** Full receipts in the rotation row: 24 fresh-context load runs clean (2 documented Sefaria-abort flags only), 333 buttons clicked across 8 surfaces with 0 throws, weekly-editor empty states instructive with the open-without-editing-creates-no-key promise verified, wizard gate/validation/Draw-landing verified, 0 new candidates.
- [x] 2026-08-08 | (S187 close-out) | branch/deploy note | `/improveloop` → S187 ran on the designated branch **`claude/improve-loop-pudobq`**, which existed on origin byte-identical to `origin/main` at `2fec8ce` (its earlier work had already landed), so this was effectively a fresh cut; **no open PR existed anywhere** (`list_pull_requests state=open` → `[]`), draft PR opened at close-out. **Drift check: SUBSTANTIAL out-of-band landings** — 22 commits since S186's landing (`61db317..HEAD`, +1,194/−96), overwhelmingly the **Font Maker Draw-mode brush suite** (Hebrew_Font_Maker.html +1,083: pen styles, partial eraser, line/move/stamp tools, stylus tilt, live tile previews, actual-size preview, keyboard shortcuts) plus dashboard preset-list scrollbar fixes and 54 new i18n keys. **`sw.js` v459→v466 and `FONT_MAKER_VERSION` 5.7→5.9 landed outside the loop** — never trusted the previous close-out's note, checked the files. That delta is exactly what made **Pass B** the right staleness pick, and the pass was aimed at it. **This session: SW v466→v467** (Hebrew_Font_Maker.html, index.html, locales/en.json + he.json all precached). **FONT_MAKER_VERSION stays 5.9** — a geometry fix and an i18n wiring fix, not a feature (same call as S186). Scripts run: `build-locales.js` (1 new key, 4,243 total), `check-i18n.js` (**both gates clean** — Check A clean + the 14 permanent baselined findings, Check B clean with no CSV debt beyond the pre-existing `worksheet.layout.title_default`), `update-sitemap.mjs` as the last step. **Decisions asked and answered at the one gate batch (3/3):** torah empty-translation row → *fetch it, no new copy*; the FM trace-readout split → *show the text in both places* (folded into the same concern, see below); next micro-feature seed → *type-a-range verse picker*, banked. **Nothing deferred to the maintainer.** Deploy lands with the PR merge.

- [x] 2026-08-08 | (S187 Pass B — console & error audit) | all 12 pages + the FM Draw-mode suite + the dashboard weekly grid | **16th run, and the first to see any of the ~1,194-line out-of-band delta. 1 real finding in the whole sweep (fixed, 74e0530); every other channel clean — and Pass B's own recorded blind spot is now CLOSED.** **Load sweep:** 12 pages × EN/HE = 24 runs, local HTTP, `serviceWorkers:'block'`. All 24 flagged, and all 24 flags were the two documented environmental classes — Playwright's own "Service Worker registration blocked" warning (24/24) and torah_trainer's blocked Sefaria fetch + blocked esm.sh/jsDelivr translit CDN (both languages). **0 real load findings**; receipts confirm every page localized and flipped `dir` (EN ltr / HE rtl, 11–416 `data-i18n` nodes each). **Interaction phase, every step receipted:** (a) **FM Draw mode** driven through the REAL wizard (name → trope ON → Drawing mode): all 5 pen styles commit strokes carrying the right `st` tag (`round`/`c`/`b`/`d`/`h`), all four style-specific sliders drag and are accessibly named in both languages (Brush size / Nib angle / Taper length / Dot spacing / Outline thickness → in HE גודל מכחול / עובי קו המתאר …), smoothing select, line tool, all 3 stamps (crown 6→12, dot, serif), move tool drag (stroke geometry moved) + lasso (4 selected, Delete enabled) + delete (14→10) + Ctrl+Z restore (→14), partial eraser (14→16 — it splits) and whole-stroke eraser (16→12), all five tool shortcuts D/L/V/S/E mapping correctly, `[`/`]` resizing the brush, and the v5.9 Trace hand-off turning 12 strokes into 10 contours. (b) **The FM trop/nikkud mark panels — the blind spot the S172 row recorded** ("B does not enable the FM trop mark panel, which is where 28 `referenced-but-unauthored` key warnings live"): they only build their glyph grid in **Custom** mark-shape mode, which is why 15 B runs never reached them. Driven now: 17 nikkud + 31 trop tiles, 12 tile→mark-edit-modal opens, both upload modes, back to Default — **0 channel hits and 0 `[i18n] missing key`**, confirming S176's 28 authored trope keys closed that worry for good. (c) **The dashboard weekly Schedule Sync grid** (the other out-of-band surface): drawer + 13 panels toggled, preset saved, editor opened with a 6-chip palette, 2 periods added (10 cells), weekend on (14 cells), 6 cells painted, schedule saved to storage, both modals closed — clean in EN/light/1280 **and** HE/dark/800. (d) generator: 40-cell / 391-char worksheet + dark toggle. (e) flash cards: a real 3-card drill to results, all 3 scored. (f) dictionary: 13,081-word corpus, 9 result cards, word modal + Escape. (g) trope tutor: 5 learn cards, tablist walked, glyph clicked. (h) index/resources/contact: 21 buttons, **plus the real AllTools modal + a real export (956 chars, 29 top-level keys)** — closing S172's other recorded weakness ("index.html clicked a button that opened no modal"). **Method notes below; three arms initially proved NOTHING and the receipts are the only reason that was visible.**

- [x] 2026-08-08 | (S187 — pattern sweeps on the stalest tool, no code) | trope_tutor.html | **Both patterns the S186 pointer named trope-tutor as the unswept carrier of come back CLEAN — and one of them exposes a flaw in its own detection rule.** (1) **`RTL-inheritance-on-a-Latin-script-container`: 29 Latin-carrying leaves ALL compute `direction:rtl` under the Hebrew UI (screen AND print) — and NOT ONE is visually reordered.** Measured properly: per-character `Range.getBoundingClientRect()` x-positions in EN vs HE, visual sequence compared for all 7 punctuated Latin strings (`"Sof Pasuk (Siluk)"`, `"Bereshit 1:1"`, `"Va'eira 6:2"`, …) — **0 differences**, because each is a single uninterrupted LTR run and the bidi algorithm keeps such a run in logical order regardless of base direction (bracket pairs included, per BD16). **The pattern's stated detection — "compare computed `direction` against the script the element carries" — over-reports by 29 on this page**; the visual-order comparison must be a mandatory second stage, and the real torah_trainer hit (f5a0d72) moved only because its damage was *sentence-final* punctuation at the END of a run. Detection definition amended in Pattern health. (2) **`silent-external-media-failure` on the `#tuAudio` clip engine: CLEAN, and measured, not just read.** Per S183's method note the MP3 was **fulfilled with a 502** rather than aborted (an abort leaves the element stalled, not errored — and models a school proxy badly): result `MediaError.code 4`, `_badFiles.size 2`, and a visible localized `role="status"` message "Audio unavailable right now — check your connection.", 0 pageerror. The engine also carries a per-load `error` listener AND a 20 s black-hole timeout for exactly the no-error-event case the pattern names.

- [x] 2026-08-08 | 8802086 | Hebrew_Font_Maker.html | **The trace readout stops contradicting itself about the empty state** (S187 gate-2 answer: "show the text in both places"). Completion of the same concern as e0847af rather than a sixth iteration — the maintainer's answer landed on the very item being worked. All three `#traceInfo` builders emitted `''` for an untraced letter while `updateTraceInfo` wrote the message, so the readout said nothing on entry and "no ink detected" after a threshold drag, **and blanked itself on every language switch**. All three now use the same `_pT('trace_info_no_ink', …)`. Verified: readout localized at load in both languages and **following a live EN↔HE switch in both directions** (was `""` before), against a traced positive control of "1 contour(s) · adv 505" / "1 קווי מתאר · קידום 505". 0 `[i18n] missing key`, 0 pageerror.

- [x] 2026-08-08 | e0847af | Hebrew_Font_Maker.html + locales | **The trace readout's "no ink" state speaks the teacher's language** (S177 Pass K, gate-approved at S178, cap-blocked in S186 — the pointer's carry-over (1), built without re-asking). One new `fontmaker.panels.trace_info_no_ink` (EN "no ink detected", HE "לא זוהה דיו"); the existing `fontmaker.status.no_ink` toast keeps the corrective "adjust threshold / invert" wording, which is too long for this compact span. **Grounding corrected the candidate's implied reachability:** the branch is not reached by entering Trace but by `doRetrace()` — dragging the threshold until no ink is detected, exactly the situation the sibling toast is about. Verified through that real path in EN/light/1280 and HE/dark/800.

- [x] 2026-08-08 | eae8fc7 | index.html + pwa.js | **"Erase All Settings" now clears the PWA install-banner dismissal** (S179 Pass A, localStorage-vs-AllTools — the sweep's LAST key-registry gap, priority-displaced twice; the pointer's carry-over (2)). `pwa.js` writes `hebrewBlender_pwaDismissed` when the teacher closes the Add-to-Home-Screen banner and nothing ever cleared it, so an erase left the prompt suppressed forever on that device. Registered in `eraseAllSettings` **only**, per CLAUDE.md's flag guidance (a one-time UI dismissal stays export-exempt). Verified in EN/light/1280 + HE/dark/800 by clicking through both confirm gates: flag null, seeded preset gone, `hebrewFontMaker_autosave` untouched (the documented never-erase key), and **still null after a reload** so pwa.js can re-offer. **That last assertion first reported the flag back at `'1'` and nearly became a "pwa.js re-sets it on load" finding — the cause was my own `addInitScript` re-seeding on navigation.** pwa.js only ever writes the key from the × button and the `appinstalled` event.

- [x] 2026-08-08 | 74e0530 | Hebrew_Font_Maker.html | **The side-bearing band no longer vanishes on a left-overhanging letter** (S187 Pass B — the pass's only finding, and a NEW registered pattern). `letterRSB()` clamps its derived value with `Math.max(0, …)`; **`letterLSB()` does not**, so ink starting left of the origin — trivial in Draw mode, where the canvas is far wider than the glyph box — fed a negative width into the left margin rect. Per the SVG spec a negative width is an error and the element is **not rendered**, so the band silently disappeared *and* Chromium logged `<rect> attribute width: A negative value is not valid` on **every** stage render (draw, trace, align and anchors all draw it). Isolated with an `innerHTML` setter wrapper that captured 4 real assignments with JS stacks — the console shows only 1 because Chromium collapses the repeats, so the raw console count understates this class. Clamped **at the render site, not in the accessor**, so the metrics readout keeps reporting the true ink-left; `wideFormGuidesSVG` carries the identical `marginZone(0, …)` shape and got the same guard. Verified in EN/light/1280 and HE/dark/800: with the repro condition still present (ink-left −266, derived LSB −311) all four work modes render **0** negative rects and **0** SVG errors, and a **negative control** that slides the ink inside the advance still draws both bands (34.9 and 12.8 units).

_(Archived at the S193 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-08 | (S188 close-out) | branch/deploy note | `/improveloop` → S188 **continued on the S187 branch `claude/improve-loop-pudobq` → the SAME draft PR #160** (open, unmerged, `mergeable_state: clean` — the protocol's no-restart case; PR body rewritten to cover both sessions). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `2fec8ce`, exactly where S187's close-out left it; `sw.js` v467 and `FONT_MAKER_VERSION` 5.9 both as recorded (verified, not taken on trust). **SW v467→v468** (precached files changed: `torah_trainer.html`, `locales/en.json`, `locales/he.json`). **FONT_MAKER_VERSION stays 5.9** — the Font Maker was not touched at all this session. Ran `build-locales.js` (+10 keys → 4,253; only pre-existing debt `worksheet.layout.title_default` remains), `check-i18n.js` **clean on both gates**, and `update-sitemap.mjs` LAST. **The clone was `git fetch --unshallow`ed at session start (55 → 1,913 commits, back to 2026-04-16) BEFORE any date-based receipt** — S175's standing rule, which is exactly what S187 skipped. **Decisions deferred to maintainer: none** (1 gate asked, 1 answered). Deploy `success` is verified after the maintainer merges.

- [x] 2026-08-08 | (S188 Pass E — freshness & site health) | repo-wide (13 root pages + sw.js/pwa.js/js/i18n.js/manifest/robots/CNAME/sitemap/licenses/splash) | **17th run: 7 dimensions, 6 clean, 1 hit — and the hit was the loop's own regression from last session.** Full receipts in the Candidates block above. The finding: four `<lastmod>` dates inflated to 2026-08-06 (trope_tutor, true 2026-07-29; resources 2026-07-19; privacy and contact 2026-07-24) — S175's shallow-clone artifact, reintroduced because its remedy was a documented rule and nothing more. **Four checker faults were found and killed before any result was trusted**, on top of S175's three inherited ones: (a) `content=` is meta PROSE and CSP text, not a path — the first run reported **213 "broken refs" of which 210 were `content=` attributes** and 3 were `new URL(req.url)` matching a `url()` regex; the corrected scan resolves 412 refs with **0** broken; (b) `url(...)` must be scanned inside `<style>` regions only; (c) `apple-touch-startup-image` carries `media=` BEFORE `href=`, so an href-adjacent regex found **0 tags and then "passed" the cross-page identity check vacuously** — the whole-tag match finds 18; (d) `app-toast` and `folder-tree` each head a CSS block and a JS block with ONE shared open-marker string, and the end marker itself contains the open marker, so a naive `indexOf` walk mislabels blocks and reports phantom "OPEN with no close" — occurrence-walking labelled by which close marker is reached gives the true six-block picture. **Dimension 4's real lesson: a LOADED host is a dependency, a LINKED host is content.** Run 1 conflated them and reported "82 external deps" — 71 of which were `<a href>` entries in the resources directory. Restricted to genuine loads (script/link/iframe/media/@font-face/fetch/Worker/base-const) the number is **11**, all covered.

- [x] 2026-08-08 | 6358898 | torah_trainer.html | **"Show translation" no longer hands you a labelled EMPTY English row with no explanation.** The residual half (b) of the S180 Pass-H P2 whose half (a) shipped in S184 (7a5cfca); gate-2 answered at the S187 batch as "fetch it, no new copy" — and it adds **0 strings**. **Grounding corrected the candidate's own diagnosis:** it is not a missing default. `populateVersionDropdown` DOES default `translationVersion` to JPS 1917 — asynchronously — and the first `fetchAndRender()` can win the race. With the value still `''`, `fetchSefariaText` deliberately sends no `version` param (rather than republish Sefaria's possibly-copyrighted default English), so no English returns; the dropdown then fills in a version name, but **only a user `change` event re-fetches**, so the reading stayed English-less permanently. And the recovery block that exists for precisely this outcome was itself gated on `settings.translationVersion` being non-empty — it could never fire on the one path that produces the empty value. Dropping that clause routes both cases through one recovery; the "switched version" note is suppressed when there was no version to switch away from (the footer already names version + license), and the Version box is re-synced to the text that actually rendered. **Verified with Sefaria FULFILLED by fixtures** (not aborted — `_engCount` had to be controlled) and the versions list served slowly so the race is deterministic instead of hoped for, run against **a pre-fix copy of the page and the fixed one**, EN/light/1280 + HE/dark/800: pre-fix **3 English rows rendered, 0 with text**, empty status line, Version box reading "JPS 1917" (the candidate reproduced exactly); fixed **3 of 3 filled**, JPS adopted, dropdown agreeing, status correctly silent. Negative control: an uncovered *selected* version still falls back AND still reports the switch, in both languages. 0 pageerrors.

- [x] 2026-08-08 | 32ba7ee | torah_trainer.html + locales | **MICRO-FEATURE — type the verse numbers instead of hunting them down the page.** The S180 Pass-H seed, gate-1 chosen at the S187 batch and funded early per that pointer. The walkthrough's teacher knew all four references before opening the tool and still had to find them by eye down a **13,718 px** page, one click each, with no shift-click for the two that were consecutive. The Copy-verses bar now takes them as text — `2:16-2:20, 3:1, 3:6` — accepting single refs, ranges, and same-chapter shorthand (`3:1-3`), comma/semicolon/space separated, Enter to submit, scrolling to the first newly-selected verse. **Ranges resolve by INDEX into `currentVerses`** — the loaded reading's own order — so a range crossing a chapter boundary needs no chapter arithmetic, and a ref outside the reading is NAMED in the note rather than silently dropped. **Additive by design** (hence "Add"): typing never wipes hand-clicked verses. **Nothing is persisted** — asserted on a fresh profile, not assumed — so there is no preset/`.ivrit`/AllTools wiring to do. Gate-2 approved copy ("Type verse numbers" + [Add]); the result note is phrased to stay grammatical for one unmatched ref or several (a deliberate, noted refinement of the approved preview's "4:2 isn't in this reading", which mis-agrees on a plural); new keys plural-split per the CSV convention; the interpolated note carries its params so it follows a live language switch through the page's existing `dataset.i18nKey` healer (the S186 frozen-status-line class), params read back via `ivritSafeParse`. **Verified: 30 checks × EN/light/1280 + HE/dark/800, all passing, 0 pageerrors, 0 missing i18n keys** — the seed's own four-ref scenario, cross-chapter and reversed ranges, shorthand, junk input, out-of-reading refs, re-adds, additivity. **Negative controls:** Clear, Select all and leaving bulk mode all still behave, with a "a note WAS showing first" assertion so the Clear check can't pass vacuously. **Strict pass** on a fresh profile (empty localStorage AND IndexedDB): accessible name resolves via `<label for>` (not the placeholder), the note is a `role=status`/`aria-live=polite` region referenced by `aria-describedby`, scroll receipt **5,246 px page / scrollY 0 → 4,346 with the verse in view**, and **0 new storage keys**. 84 lines in the page — inside the ~150-line fence.

- [x] 2026-08-08 | bd122b3 | scripts/update-sitemap.mjs + sitemap.xml + CLAUDE.md | **`update-sitemap` refuses to run on a shallow clone** (silently-wrong-published-artifact class; Pass E's one finding). `git log -1 --format=%cs -- <file>` returns the shallow **boundary** commit for any file not touched inside the window, so the date looks valid and the script reports success. S175 found this, fixed the dates, and made "deepen first" a **standing rule** — then S187's close-out reintroduced it, which is the argument for a mechanical guard over another line of documentation. Now: detect `git rev-parse --is-shallow-repository`, refuse, exit 1, write nothing, and name `git fetch --unshallow` in the message; CLAUDE.md's Deploy section documents it. Dates corrected in the same commit. **Verified three ways: (1) positive control** — a real `git clone --depth 1` exits 1 with `sitemap.xml` byte-unchanged; **(2) counterfactual** — the PRE-fix script in that same shallow clone collapses **all 11 `<lastmod>` values to the single boundary date**, so the guard prevents real corruption rather than guarding a non-problem; **(3) negative control** — on the deep clone it still runs, all 11 entries independently match `git log -1`, and a second run is a no-op. No browser matrix: a build script plus a data file, no UI surface (stated rather than faked).

_(Archived from the working ledger at the S194 close-out — Done keeps the last 5 sessions.)_

- [x] 2026-08-08 | (S189 close-out) | branch/deploy note | `/improveloop` → S189 **continued on the S187/S188 branch `claude/improve-loop-pudobq` → the SAME draft PR #160** (open, unmerged, `draft: true`, `mergeable_state: clean` — the protocol's no-restart case; the PR now carries S187 + S188 + S189). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `2fec8ce` and fully contained in HEAD (branch 9 ahead / 0 behind at session start), `sw.js` v468 and `FONT_MAKER_VERSION` 5.9 exactly where S188's close-out left them (main's own copy still reads v466, i.e. the two loop bumps are unmerged, as expected). `sw.js` **v468→v469** (precached files changed: `hebrew_blend_generator.html`, `flash_cards.html`, `hebrew_dictionary.html`, `classroom_dashboard.html`, `locales/en.json`, `locales/he.json`). **FONT_MAKER_VERSION unchanged (5.9)** — the Font Maker was measured by the pass and came back clean on every dimension; not one line of it changed. Scripts: `build-locales.js` (3 new keys → 4,256; the only untranslated key remains the pre-existing `worksheet.layout.title_default`), `check-i18n.js` clean on both gates after every commit, `update-sitemap.mjs` last (deep clone confirmed first — the S188 guard would have refused otherwise). **Gates: 2 asked, 2 answered, 0 deferred.** Gate 1 → the maintainer chose to skip the micro-feature and spend iterations 2–5 converging all four Pass-F dimensions instead. Gate 2 → the generator's two new strings approved as drafted ("Loading Hebrew words…" / "Couldn’t load the Hebrew words."). The flash-cards loading string was NOT gated: its EN and HE are both copied verbatim from the shipped `worksheet.empty.loading_emoji` row, so no wording decision existed. **Nothing deferred**, and one new gate-2 candidate logged for next session (the dictionary's `file://` failure copy). Deploy `success` is verified after the maintainer merges.

- [x] 2026-08-08 | (S189 Pass F — cross-tool consistency) | all 7 tools' primary slow operation | **16th run; affordance: busy / loading feedback. 5 of 7 tools clean, 2 laggards, 4 findings — every one fixed this session — and 2 further candidate findings refuted by grounding before they reached the backlog.** Full receipts in the Candidates block above. The session's through-line became a **new recurring pattern** (`two-state-ready-flag-for-a-three-state-load`, registered in Pattern health): three separate tools decided what to render from a single truthiness check on a lazily-fetched corpus, so the "not ready" arm was also what a *failed* load rendered — permanently. | discovery + 4 fixes; 1 new pattern registered, 1 new gate-2 candidate logged

- [x] 2026-08-08 | eb385b8 | classroom_dashboard.html | **"Loading weather…" is finally said out loud.** Across all seven tools this was the ONLY busy/error line no assistive tech was ever told about: the weather block's three states (loading / unavailable / location not found) are painted into a plain `div` with no `role` and no `aria-live`, while every sibling tool announces. Routed through **`#a11yStatus`** — the polite live region this page already carries for panel-move announcements — rather than by marking `#weatherBlock` live, because that block also holds the temperature, which re-renders on every 30-minute refresh and on a dark-mode toggle, so a live region there would re-announce the forecast indefinitely. The message is now assembled once and escaped at the `innerHTML` boundary instead of partway through the interpolation, so the live region can take the raw text. No new copy — all three strings already exist and are translated. | Verified headless over local HTTP, EN/light/1280 + HE/dark/800, four scenarios each (slow success, forecast 502, geocode miss, and a location containing `A<b>"&x`): the status reaches the live region in all three states, a successful forecast is NOT announced, and the escaping is provably intact — the block renders `&lt;b&gt;`/`&quot;`/`&amp;` while the live region holds the raw string. 0 pageerrors.

- [x] 2026-08-08 | 5bc9524 | hebrew_dictionary.html | **The count chip stops saying "Loading…" after a load has failed — and the root cause was deeper than the symptom.** The visible failure line was already handled well (`#loadingText`, `role="status"`, correct even for a failure landing before the deferred `js/i18n.js` runs). The two count readouts were not: they start at "Loading…" and every later render re-derived that text from `!_wordsReady`. **A ready flag has two states; a fetch has three.** So after a definitive failure the count chip — this page's `aria-live="polite"` channel — announced "Loading…" indefinitely, and every re-render and language switch refreshed it. Fixed at the source with `_wordsFailed`, **and the identical shape two lines away in the same function** (`EMOJI_DATA` → `_dictEmojiFailed`) rather than leaving the twin bug in place; blank is honest here, since the failure itself is reported in `#loadingMsg` and in the emoji category tree, both localized. The stale `data-i18n` is dropped from `#resultCount` so `applyStaticI18n()` cannot refill it. | Verified headless, EN/light/1280 + HE/dark/800, 9 checks per cell. **The first attempt patched only the catch and PASSED IN ENGLISH while failing in Hebrew** — under HE the locale fetch resolves later, so `applyI18n` re-rendered after the catch and restored "Loading…"; that language-dependent flake is what exposed the real cause. Negative controls: a successful load still fills both readouts (13,081 words / 1,581 emojis), a language switch keeps a real count, the loading block still hides. 0 pageerrors, 0 missing i18n keys.

- [x] 2026-08-08 | 16db548 | flash_cards.html + locales | **The emoji list now says whether it is loading or gave up.** The emoji sub-mode's category tree sat completely blank for the whole ~457 KB download and said nothing at all — while the **words** sub-mode immediately beside it, in the same options group, has shown "Loading dictionary…" via `updateWordsCount` all along. On a failed load it did print a message, but a **hardcoded English literal**, so a Hebrew-UI teacher got English. That literal's key already existed: **`flashcards.words.emoji_load_failed`, authored AND translated, referenced nowhere** — a hit on the ACTIVE `authored-but-unreferenced i18n key` pattern, so wiring it cost no new copy; the loading line's EN and HE are both verbatim from the shipped `worksheet.empty.loading_emoji` row, so no wording decision existed there either. Both states render from `renderEmojiCatTree()` — the chokepoint — rather than from the fetch's `catch`, so `applyI18n`'s existing `syncOtherSubUI()` call re-resolves them on a language switch; writing them at the fetch would have installed the `pre-ready-i18n / never-re-rendered` shape this file has been bitten by before. | Verified headless, EN/light/1280 + HE/dark/800: corpus stalled 3 s (loading line shown, follows a live EN↔HE switch), 502 (CSV error line, still correct 4 s later, right language both ways, no raw key), plus negative controls that the loaded tree renders unchanged (exactly 2 visible categories, one fetch only) and the numbers/colors/words sub-modes still open their own panels. 0 pageerrors, 0 missing i18n keys.

- [x] 2026-08-08 | 2387d06 | hebrew_blend_generator.html + locales | **"0 matching words" no longer means "still downloading" — the pass's P2, and the only finding that was worse than silence.** The word corpus is **2.9 MB** and lazily loaded, but the Real Words pool caption is written before the fetch starts and not again until it lands, so for the entire download it read "0 matching words" — which a teacher reads as a statement about their letter and vowel selection, not about a download. On a failed load (school proxy, no connection) that false zero stayed on screen **permanently**, with no error anywhere on the page. The caption now distinguishes in-flight, failed and loaded. **The refresh is triggered inside `ensureRealWords()` rather than at one call site**, because every trigger path (`setRwSource` → `_syncRwSourceUI`, `applySettings`, the `?s=` share restore) writes the caption before the fetch exists — the first attempt's guard was correct and still never fired. `_realWordsFailed` distinguishes "attempted and failed" from "not attempted yet", so a fresh page never claims a failure, and it resets on retry. Also converges the caption onto the dictionary `#resultCount` canonical for this affordance: `role="status"`. Gate-2 copy approved as drafted. | Verified headless, EN/light/1280 + HE/dark/800, 15 checks per cell: corpus stalled 3 s (loading line shown; follows a live EN↔HE switch), a 502 observed for **6 s** so "stuck" cannot be a short-sample artifact, and a retry after failure returning the real count. Negative controls: with the corpus loaded and every length filter off, a **genuine** "0 matching words" still appears (so the fix did not blanket-suppress the readout), and the curated My-Word-Lists / imported sources are untouched. 0 pageerrors, 0 missing i18n keys.

- [x] 2026-08-11 | (S192 close-out) | branch/deploy note | `/improveloop` → S192 continued on the designated branch **`claude/improve-loop-agk18i`** and its **still-open, unmerged draft PR #162** (the branch/PR protocol's continue-don't-restart case — S191's 6 commits were still sitting unmerged on it, so no fresh cut and no second PR). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `07e4631`, exactly where S191 found it; `sw.js` v472 and `FONT_MAKER_VERSION` 5.9 both unchanged from S191's close-out, verified by reading the files rather than trusting the note. The clone arrived **non-shallow** this time, so no `--unshallow` was needed (contrast S191). **This session: SW v472→v473** (Hebrew_Font_Maker.html, classroom_dashboard.html, flash_cards.html, index.html, contact.html, privacy.html, 404.html — 7 precached files changed; no locale JSON changed because **zero CSV keys were added**). **FONT_MAKER_VERSION stays 5.9** — the FM change is an i18n wiring fix, not a feature (same call as S186/S187). Scripts run: `check-i18n.js` (**both gates clean** — Check A clean + the 14 permanent baselined findings, Check B clean, no CSV debt), **`build-locales.js` deliberately NOT run** (no CSV change — every fix wired a key that already existed), `update-sitemap.mjs` as the last step. **Gates: NONE ASKED.** Every one of the four fixes wired an already-authored, already-translated key, leaving English output byte-identical — the ledger's own "pure wiring: 0 new copy, 0 new Hebrew, no gate 2" precedent (S166). **Nothing deferred to the maintainer.** **Protocol divergence to flag:** the ledger pointer's "**Queued and needing no gate — take these first**" list was NOT taken this session; see the pointer below for the reasoning (the top queued item is a 4th torah_trainer iteration in two sessions, which the variety governor's hard per-tool cap and its "prefer tools untouched in the last 2 sessions" tie-breaker both argue against). Deploy lands with the PR merge.
- [x] 2026-08-11 | 3dc4f41 | flash_cards.html | **The vowel-position pills speak Hebrew.** `updateVowelPositionUI` built the 1st/2nd/3rd pills from a plain English array (`const labels = ['1st','2nd','3rd']`), so `flashcards.vowels.pos_1st/_2nd/_3rd` — authored AND translated — had never once been shown. Each button now carries its own `data-i18n`, which is what makes a language switch land. **`updateVowelPositionUI` was deliberately NOT added to `applyI18n`**: it mutates `vowelPositions` (`vowelPositions.add(mode)`), and CLAUDE.md requires `applyI18n` to be read-only — the per-button binding gets the same result through `applyStaticI18n` with no rule broken. Text write falls back to the English literal when `t()` returns the key (pre-ready). | verified headless (local HTTP, `serviceWorkers:'block'`) EN+HE × light/1280 + dark/800: pills translated in HE (ראשונה/שנייה/שלישית), byte-identical in EN, live switch re-translates, **clicking still toggles `vowelPositions`** (they are functional controls, not labels), no raw key on screen, 0 failing cells, 0 pageerrors
- [x] 2026-08-11 | 3d342f5 | index.html + contact.html + privacy.html + 404.html | **The footer Privacy and Contact links speak Hebrew.** `shared.footer.privacy_link` is authored and translated and was referenced by **ZERO pages** — all four chrome pages hardcoded the English, so a Hebrew-UI teacher saw an English footer link on every chrome page while the translation sat unused. index's Contact link wired to `contact.page_title` (byte-identical English, names the same destination). **Deliberately NOT wired: the attribution credits in the same footer** (Kaikki word data, the CC BY-NC-SA note and link, GitHub, Created by) — CLAUDE.md lists footer brand/attribution credits as never-translate, so those five authored keys stay unused **on purpose**; a future session must not "finish the job". The static source a crawler indexes is unchanged (`data-i18n` acts only at runtime) and privacy.html's JSON-LD breadcrumb name is untouched. | verified headless 4 pages × EN+HE × light/1280 + dark/800 = **16 cells**: links translated in HE, byte-identical in EN, hrefs unchanged, **credits asserted still-English in both languages**, 0 failing cells, 0 pageerrors
- [x] 2026-08-11 | 345859b | classroom_dashboard.html | **The group-size label stops reverting to English.** `updateGroupNLabel()` wrote a hardcoded English ternary onto `#groupNLabel`, which carries a correct `data-i18n` binding — so in the Hebrew UI `applyStaticI18n` translated the label and this line immediately reverted it. Both counterpart keys (`dashboard.picker.group_count_label` / `group_size_label`) were already authored and translated; **the count label's Hebrew had never been shown at all**. Retargeting the binding (not just the text) is what makes a language switch land here, because `applyI18n` re-runs `applyStaticI18n` but never calls this function. Text write guarded against the pre-ready case (`t()` returns the key before locales load, and this block runs at init — its neighbour `updatePickerRosterCount` is documented in `applyI18n` as exactly that), so a raw dot-key can never reach the screen. | verified headless EN+HE × light/1280 + dark/800: both modes correct in both languages, live switch keeps the active mode's label, no raw key on screen, 12/12 assertions, 0 pageerrors
- [x] 2026-08-11 | 3491258 | Hebrew_Font_Maker.html | **The kern class dialog title stops reverting to English.** Same shape as the dashboard fix: `openKernClass()` wrote a hardcoded English ternary onto `#kernClassTitle`, which carries a correct `data-i18n` binding, and both `fontmaker.modals.kernclass_title_edit` / `_new` were already authored and translated — the *edit* key had simply never been called. Binding retargeted alongside the text, matching the existing suite idiom at `flash_cards.html` `timerShowLabel` (found by grepping for precedent rather than inventing a third treatment). **No `FONT_MAKER_VERSION` bump** — a wiring fix, not a feature. | verified headless EN+HE × light/1280 + dark/800: both modal states correct in both languages, **live language switch with the modal open follows the retargeted binding**, 12/12 assertions, 0 pageerrors
- [x] 2026-08-11 | (S192 Pass K — i18n / localization audit, 12th run) | all 12 pages | **Both gates clean again** (Check A clean + 14 baselined; Check B clean, no CSV debt) — the 5th consecutive clean-gate K, which is why the run's whole value was in the blind spot and the ACTIVE-pattern sweep. **Blind-spot probe over the K-unswept delta `c88da45..HEAD` (126 commits, ~4,390 added HTML lines, never K-swept): 0 real finds** — and getting to that zero took two probe rewrites and four dismissals that each had to be *grounded*, not assumed: (a) 10 raw hits were static attributes carrying their `data-i18n-*` sibling (the correct pre-JS fallback); (b) the `_pT(key,'English')` sites are the documented prefix-helper fallback — **all four keys verified present in the CSV** rather than taken on faith; (c) the Font-Maker changelog `<li>` block is `HELP_CONTENT.about`, the by-design English fallback, and **`fontmaker.changelog.v5_8`/`v5_9` were verified present AND translated**, so the maintainer's out-of-band releases wired their i18n correctly; (d) the out-of-band dashboard `fmtClock()`'s hardcoded `'en-US'` matches the deliberate projected-content convention — **all 6 `toLocaleTimeString`/`toLocaleDateString` calls on the page hardcode it**, including the pre-existing big clock, and CLAUDE.md routes projected content through `headerLang`, not `I18n.lang`. **The static orphaned-key census was abandoned as untrustworthy for the THIRD time** (S166 got 721/2/424; this session got **1082, then 677** after generalizing wildcard expansion, still dominated by dynamically-keyed families like `*.glyphname.*` and `*.tour.*`). **So the pass replaced it with a RUNTIME detector, and that is the reusable result:** load each page with the Hebrew UI, collect every English string still on screen (text nodes + `title`/`aria-label`/`placeholder`), and **reverse-look-up each one against `en.json`, keeping only matches whose `he.json` value differs**. That turns "is this English intentional?" into "a Hebrew translation for this exact string exists in the repo and is not being used". **35 hits across 12 pages, 0 pageerrors**, triaged to **8 real** — the rest correctly dismissed as coincidental cross-concept string matches (the generator's `Letter` is the *paper size*, not the colour mode; the dashboard's `Default` is a *user preset name*; `Alef` ×6 is a *font family name* in the picker, not the glyph) or as CLAUDE.md scope-boundary content (the trope tutor's Mercha/Tipcha/Munach/Etnachta are the **taught pronunciation names**, explicitly never-translate). **4 fixed this session; 4 logged as Candidates.** **One NEW pattern registered and swept — `wired-then-clobbered label`** (see Pattern health): 4 raw candidates, **2 real** (FM + dashboard, both fixed), 2 correctly dismissed as printed worksheet output whose binding my ID-proximity heuristic had mismatched to an unrelated aria key.

- [x] 2026-08-11 | (S191+S192+S193 post-close, **maintainer-authorized direct-to-main landing**) | branch/deploy note | The maintainer said **"push to main"** after the S193 close-out, which per the branch/PR protocol is the explicit in-session authorization that supersedes the draft-PR rule (**4th ledgered precedent**, after S78, S181, and S182+S183). `main` was a strict **ancestor** of the branch head (`git merge-base --is-ancestor origin/main HEAD` true), so this landed as a **clean fast-forward** — `07e4631 → 6efc532`, no merge commit, no conflict possible, and the branch content is byte-identical to what the draft PR had been carrying. **18 commits across three sessions land at once: S191 (Pass C accessibility, 4 fixes), S192 (Pass K i18n audit, 4 fixes), S193 (Pass A pattern sweep, 4 fixes)**, plus their three close-outs and sitemap refreshes. `sw.js` lands at **v474** (three loop bumps, v471→v472→v473→v474, unmerged until now); `FONT_MAKER_VERSION` stays 5.9 across all three. **PR #162 closes as merged by the fast-forward** rather than by a merge button, so no separate merge commit exists to look for in the history. **Batched deliberately into ONE push** including this ledger note, per CLAUDE.md's warning that rapid successive pushes cancel in-flight Pages deploys — so exactly one "pages build and deployment" run should exist for this landing, and **its conclusion is the deploy receipt for all three sessions**. The self check-in armed at the S193 close-out is retired at this point: its stop condition is "PR merged or closed".
- [x] 2026-08-11 | (S193 close-out) | branch/deploy note | `/improveloop` → S193 **continued on the designated branch `claude/improve-loop-agk18i` → the SAME draft PR #162** (open, unmerged, `draft: true`, `mergeable_state: clean` — the protocol's no-restart case; the PR now carries S191 + S192 + S193). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `07e4631`, exactly where S192's close-out left it; `sw.js` v473 and `FONT_MAKER_VERSION` 5.9 both verified by reading the files rather than trusting the previous close-out. The clone arrived **non-shallow** (`git rev-parse --is-shallow-repository` = false), so no `--unshallow` was needed before any date-based receipt — checked first, per the S175/S188 standing rule. **SW v473→v474** (precached files changed: `trope_tutor.html`, `torah_trainer.html`, `hebrew_dictionary.html`, `classroom_dashboard.html`, `hebrew_blend_generator.html`; **no locale files** — see below). **FONT_MAKER_VERSION stays 5.9** — the Font Maker was not touched at all this session; the only FM finding (3 UA-default `.hint` links) is logged as a candidate because it renders only in a mode the sweep did not enable. Scripts: **`check-i18n.js` clean on both gates** (Check A clean + 14 baselined, Check B clean) after every commit; **`build-locales.js` deliberately NOT run — no CSV keys were added or changed.** All four fixes are mechanical or pure wiring against keys that already existed, and English output is byte-identical, which is also why **0 gates were asked**: nothing this session was a copy or SEO decision. `update-sitemap.mjs` ran LAST. **Regression sweep across all 5 touched pages × EN/HE × light/dark: 20 loads, 0 pageerrors, 0 raw i18n keys on screen.** **Decisions deferred to maintainer: none.** **Protocol divergence flagged for the skill: none this session** — and specifically, S192's flagged divergence is now closed: the two queued items it deferred on variety-governor grounds (the torah_trainer verse-count announcement and the `.btn-xs` floor) were both taken here, which is what the governor's "priority still wins across sessions" clause is for. Deploy `success` is verified after the maintainer merges.
- [x] 2026-08-11 | 97a16b7 | classroom_dashboard.html + torah_trainer.html + trope_tutor.html + hebrew_blend_generator.html | **Small buttons reach the suite's 30px touch-target floor in four more tools.** S102 ratified the floor and applied it to `flash_cards.html` **only**; the four other `.btn-xs` carriers never got it. **Measured before touching anything: 44 rendered targets, 44 under 30px, 14 under WCAG 2.5.8's 24px, heights 18–29px** (the generator's 20–27px the smallest in the suite) — these are All / None / No Sofit / Clear / Select all / Duplicate, the controls a teacher taps most, on classroom tablets. Each file gets flash_cards' own declarations rather than a new invention (`min-height`/`min-width:30px` + `inline-flex` centering, which is what turns the min-height into a taller BOX instead of taller text), formatted to local style; nothing else changed. **Verified light/1280 + dark/800 on all four plus flash_cards as control: 44/44 now ≥30px, 0 under 24px.** Two layout guards, because a min-height reflows tight rows: horizontal page overflow 0 everywhere (unchanged from baseline), and **no `.btn-xs` overflows a clipping ancestor in any of the 8 cells**. 0 pageerrors. Note the "7 of 8 files" in the S192 pointer was approximate — only **5** files carry the class.
- [x] 2026-08-11 | 05a0112 | hebrew_dictionary.html | **The vowel filter's nine sound labels speak Hebrew** (`authored-but-unreferenced i18n key family`; the biggest single-tool haul of the S192 runtime detector, re-confirmed by this session's Pass A re-sweep). "AH sound" / "short EH" / "UH / silent" rendered straight from the plain `sub:` field of `VOWEL_DEFS`, so a Hebrew-UI teacher read English on **13 chips** while `worksheet.vowels.sub.*` sat fully translated and referenced by nobody but the generator. Fixed with the generator's own `vowelSub()` + `VOWEL_SUB_KEY` **copied verbatim** (so a new sound label is added in one shape), placed beside `glyphVowelName()` whose missing-key fallback it mirrors. `relabelVowelFilter()` now relabels the second line too — **its comment used to read "the group headers/sub descriptors stay as-is", i.e. the comment WAS the gap** — and still relabels in place rather than rebuilding. **No new keys, no CSV change, EN byte-identical → pure wiring, no gate, `build-locales` not needed.** Verified EN+HE × light/1280 + dark/800: 13 chips carry 9 distinct labels all matching the locale file, a live switch relabels both directions, **all 13 checkbox states survive it**, no raw dot-keys, 0 pageerrors.
- [x] 2026-08-11 | 5ec698f | torah_trainer.html | **Selecting verses is announced to screen readers** (the S191 Pass-C finding S192 deferred on variety-governor grounds; top of the queue since). Select all / Clear / a verse click / the range picker all changed `#ttCopyCount` and `#ttBulkDockCount` with **zero live-region coverage** — no confirmation that anything was selected, which is the one piece of feedback bulk selection exists to give. `#ttCopyCount` becomes the region (`role=status` + `aria-live=polite`, matching the page's two existing ones); the dock keeps mirroring the number **silently**, so a change speaks once, not twice. **The write is guarded to fire only on a real change** — `updateCopyTextarea()` is also called by five display toggles and by `applyI18n`, so an unguarded `textContent` assignment would announce "3 selected" for flipping a checkbox that never touched the selection, and a region that speaks when nothing happened is one users learn to tune out. Verified EN+HE × light/1280 + dark/800 **with a MutationObserver on the region** (what an AT actually listens to) rather than by reading text: select-one / select-all / clear / re-select each fire exactly 1; a no-op re-write and a real "include reference" toggle each fire 0; dock resolves to no `aria-live`, own or inherited. 0 pageerrors.
- [x] 2026-08-11 | cd52e15 | trope_tutor.html | **The About panel's two links stop vanishing in dark mode** (`theme-flipping-token-on-a-fixed-colour-plate` — Pass A's one hit, and **the mirror image of the S191 instance**: there the token inverted and the plate did not, here the plate inverts and the colour does not). They are the only anchors inside the settings drawer, so `footer a { color:var(--gold-text) }` never reached them and they fell back to the **UA default `#0000EE`** — 8.9:1 on the light card, **2.0:1 on the dark one**, in the exact panel a teacher opens for the reset buttons and the audio credits. One CSS rule using the token authored for the job (`--gold-text` = `#7f5a13` light / `var(--gold)` dark, already the page's link colour in `.tu-intro a` / `.tu-card-links a` / `footer a`). **UA underline deliberately kept** — the links sit mid-sentence, so colour must not be the only cue (WCAG 1.4.1). Verified EN+HE × light/1280 + dark/800: **dark 1.99:1 → 8.73:1**, light 8.88:1 → 5.88:1 (both AA), underline present in all four cells, hrefs unchanged, 0 pageerrors. CSS not inline style, because the paragraph is `data-i18n-html` and is rebuilt from CSV on every language switch — re-checked after a live switch.
- [x] 2026-08-11 | (S193 Pass A — recurring-pattern sweep, first since S179) | 7 pattern classes | **1 hit, 6 clean, and 4 candidate findings killed by grounding before any was logged.** **HIT:** `theme-flipping-token-on-a-fixed-colour-plate` — trope_tutor's 2 About-panel links (fixed cd52e15). Generalized into a **cheap reusable detector for the whole class of "unstyled anchor on an inverting plate": computed colour ∈ {`rgb(0,0,238)`, `rgb(85,26,139)`, `rgb(0,0,255)`} = the UA defaults, swept over all 12 pages in dark mode** → 7 anchors, **2 visible (both fixed), 5 not rendered** in the default state. **REFUTED before logging:** (1) the generator's 3 buttons at 1.84:1 in dark are `disabled` — `body.dark .btn:disabled` deliberately sets `#3d4f6a` with `opacity:1`, and disabled controls are exempt from 1.4.3; (2) the dictionary's "Skip past results" is a skip link whose `:focus` rule sets `background:var(--navy);color:#fff` — the detector saw the *unfocused* state; (3) user-font `font.stack` interpolated raw into a `style=` attribute in 6 files is sanitized at construction (`.replace(/['"\\<>]/g,'')`), so it cannot break the attribute; (4) the dashboard's `presetColors` and the dictionary's `wlReadStore` bare `Object.assign` both take a **fresh `{}`** target and an already-`ivritSafeParse`d / freshly-rebuilt source (`normalizePresetColors` filters by `hasOwnProperty` + strict `#rrggbb`). **CLEAN:** the three consequence-critical classes re-swept over `0dc1e50..HEAD` (**6,019 insertions** — not S158's zero-delta: 50 new `innerHTML`, 8 `JSON.parse`, 9 `Object.assign`, 19 `localStorage` calls, each attributed to its file and triaged) — `unescaped-input/unsafe-parse` **clean, streak 12**; `localStorage-vs-AllTools` **clean, streak 12** (full key census: the only 5 keys absent from `eraseAllSettings` are the documented per-device prefs `zoom`/`hideZoomBar`/`darkMode`/`lang` plus FM **project** data, which is deliberately outside the settings model); `destructive-bulk` **clean, streak 12** (`removeWeekPeriod` is `confirm`-guarded exactly when the column holds assignments). `invalid-SVG-geometry` **clean — both S187-named carriers emit ZERO `<rect>`**, so the shape's surface is Font-Maker-only in practice (23 of 28 suite-wide). `two-state-ready-flag` **clean — all 3 S189 hits are fixed** and now carry the three-state idiom (`_emojiFailed`, `_wordsFailed`, `_realWordsPromise || _realWordsFailed`). `authored-but-unreferenced` re-swept with S192's runtime detector: **27 hits, 0 NEW** — every family either already logged or on the documented benign axis, and **the count dropped from 35 by exactly what S192 fixed**, which is the method's first stability receipt.

_(Archived at the S199 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-11 | (S194 close-out) | branch/deploy note | `/improveloop` → S194 ran on the designated branch **`claude/improve-loop-mz3ifp`**, cut fresh off `origin/main` — the protocol's *merged* case: the S191–S193 work was landed direct-to-main by maintainer authorization (`519f31e`), so there was no open PR to continue and a **new draft PR** opens at this close-out. **Drift check: NOTHING landed outside the loop** — `origin/main` sits exactly at `519f31e`, the S191–S193 landing commit itself, with HEAD identical to it at session start; `sw.js` **v474** and `FONT_MAKER_VERSION` **5.9** both verified **by reading the files**, not by trusting the previous close-out. **The clone arrived SHALLOW** (`git rev-parse --is-shallow-repository` = true, 50 commits) — `git fetch --unshallow` was run **before** any date-based receipt, per the S191 method note; without it `update-sitemap.mjs` would have refused (correctly) and the drift check would have read a stale `origin/main`. `sw.js` **v474→v475** in the final commit; precached files changed: `Hebrew_Font_Maker.html`, `classroom_dashboard.html`, `torah_trainer.html`. **No `FONT_MAKER_VERSION` bump** — both Font-Maker changes are defect fixes (a false-positive validator, a missing link colour), not features, and CLAUDE.md reserves the bump + changelog entry for releases. Scripts run: `check-i18n.js` (clean — Check A clean + the same 14 baselined, Check B clean, no new violations), `update-sitemap.mjs` (last step, on the now-unshallowed clone). **`build-locales.js` deliberately NOT run — zero CSV keys were added this session** (no new or changed user-facing strings in any of the four fixes). **Decisions deferred to maintainer:** one gate-2 item, below. Deploy `success` is verified **after merge**, not in-session.

- [x] 2026-08-11 | 0164489 | torah_trainer.html | **The five slider reset buttons reach the 30px touch-target floor.** The `↺` buttons beside Hebrew size, transliteration size, English size, karaoke rate and playback rate measured **24×21px** — under the suite's ratified floor (S102) and under WCAG 2.5.8's 24px on the short axis — and they sit **directly beside a slider thumb**, so a mis-tap on a classroom tablet drags the size instead of resetting it. Given the same declarations `.btn-xs` already carries **eleven lines above in this same file**, rather than a second treatment. **The queued candidate's "59" is corrected to 31 by re-measurement** (drawer + all 9 panels open), and most of the 31 are not real targets: **11 are 0×0 visually-hidden checkboxes** whose styled label is the actual hit area, and several are **4px range-slider tracks** whose thumb and full width are draggable. | verified headless light+dark × 1280/800, pageerror==0: 24×21→30×30 on all five, every slider row still one line (**height hugs its tallest child, 30/30 — no wrap**), no horizontal overflow, `↺` glyph intact. **The first harness run reported a row-layout failure that was its own too-tight top-alignment tolerance** — established as a harness artifact by re-running the same check against the pre-change file (21/21 pass, 30/30 fail) before changing anything.

- [x] 2026-08-11 | 01ebdb5 | Hebrew_Font_Maker.html | **The Advanced-Settings hint links stop vanishing in dark mode** (`theme-flipping-token-on-a-fixed-colour-plate` / the UA-default-anchor detector S193 registered — its **first hit on a new carrier**, and the queued P4). The template **PDF** and **PNG** download links and the **SVG Upload Guide →** link are `<a>` inside a `.hint`; `.hint { color: var(--muted) }` does **not** cascade into an anchor — the UA's own `a` rule wins — so they render `#0000EE`, measuring **1.81:1** on the dark Advanced panel at 11.2px. These are the only route to the printable letter sheets and the SVG instructions. One rule (`.hint a { color: var(--gold-text) }`) using the token `footer a` on this page already uses; underline deliberately kept, since unlike the footer links these sit **inside running sentences** where it is the only remaining affordance. **The queued candidate's gating claim was WRONG and the correction makes the finding bigger:** it said they render "only once SVG-upload/English mode is enabled" — they are the explanatory text *under* each checkbox and show for anyone who opens the **Advanced tab**, checkbox untouched (the reason an earlier probe read `rendered:false` was that the whole `.panel.cat-panel` is `display:none` until `setCatTab('advanced')`). Blast radius bounded by count: **exactly 3 `.hint` anchors exist in the document.** | verified headless light+dark × 1280/800, pageerror==0, measured with the Advanced tab active so they actually render: **dark 1.81→7.96:1**, light 8.88→5.88:1, underline retained.

- [x] 2026-08-11 | 1010c0b | classroom_dashboard.html | **Brand gold stops failing contrast as small text on light plates** (the top queued no-gate candidate). **14 text nodes** rendered raw `--gold` (`#c9922a`) on light surfaces at **2.6–2.75:1** against the 4.5:1 that 12.8–13.6px text needs: the **5** "Related Hebrew Tools" footer cross-links on cream (2.6:1), the **8** checked segmented-control labels on white (2.75:1) — date format, clock format, weather unit, day-name language, picker mode, i.e. the controls a teacher reads to confirm *what is currently selected* — and the first-run "Take a tour →" link. Swapped to **`--gold-text`** (`#7f5a13`), the token this file already defines for exactly this case and already uses on the font-picker preview; the generator and flash cards have used it for their footer links all along, so this **converges the dashboard onto the existing treatment rather than inventing one**. Dark is untouched **by construction** (`--gold-text` resolves to `var(--gold)` there, and the segmented control keeps its own `body.dark`/`--gold-light` rule). **The queued candidate said 10; the true count is 14** — found only after forcing the settings drawer, all **13** panels and all **9** `<details>` open, because the footer block lives inside the drawer and a closed-drawer sweep silently skips it (the S191 Pass-C progressive-disclosure lesson, re-earned). | verified headless light+dark × 1280/800, pageerror==0: footer 2.6→**5.88:1**, segmented labels + tour link 2.75→**6.22:1**, dark unchanged at 8.73:1 / 11:1.

- [x] 2026-08-11 | f8a604f | Hebrew_Font_Maker.html | **QA Check stops warning about the app's own built-in comma** — Pass H's headline find, and a **P3 that fires in 100% of projects ever created**. The outline scan reported "⚠ 2 glyphs with self-intersecting or overlapping outlines: **Comma, Semicolon**" on a project where **nothing had been drawn at all** — both are the app's own built-in punctuation (`srcKind:'single'`, `traced:false`, `hasDraw:false`). **The overlap is real but deliberate and harmless:** the comma's tail is tucked into its round head so the join looks seamless, and its two contours are **both outer** (`winding:['cw','cw']`; Semicolon `['cw','cw','cw']`), which the **nonzero fill rule unions** — no inversion is possible. The remedy the message offered ("edit the nodes or re-trace to fix") was not even available for a glyph nobody drew. The overlap loop now skips a pair **only when both contours are outer**; **outer/hole and hole/hole overlaps — the cases that actually cancel a region — still fire** (hole/hole was added deliberately: two overlapping counters double-subtract and invert, so "windings differ" alone would have been a narrower rule than correctness allows). A permanent unclearable warning is how a teacher learns to **ignore the panel that also carries the real findings about her own letters**. | verified headless light+dark × 1280/800, pageerror==0, 44 checks: untouched project raises **0** outline warnings and the QA panel keeps its placement verdict; **positive controls** confirm outer+hole and hole+hole still flag, self-intersection still detected (its call signature changed), normal counters + disjoint contours still clear, and a contour with **no** `winding` field still defaults to outer.

- [x] 2026-08-11 | (S194 Pass H — teacher walkthrough / paper-cuts) | Hebrew_Font_Maker.html | **The stalest-H tool by a wide margin — its last and only H was S97 (2026-07-18, 97 sessions), chosen by applying S180's own test rather than repeating its answer** — and the pick landed on genuinely un-walked surface: everything from v5.2 to v5.9 (the onboarding wizard, Draw mode, the chisel nib, the partial eraser) shipped *after* that single H. Scenario role-played end to end: *"I want my own handwriting as a font so my worksheets look like the board"* — fresh profile → wizard → Draw source → draw Alef → Save letter & next → two more letters → QA → export. **The flow is strong and that is the honest headline:** the wizard's 3 steps are clear and validate inline, "Save letter & next →" auto-traces and advances (`"Alef saved and traced."`), the next-step hint reads **"Next up: Dalet — 24 to go."**, and the export guard refuses politely with all 24 untraced letters as **clickable jump chips**. `pageerror == 0` throughout, no horizontal overflow at 800px, dark mode clean, and `offerSaveToMyFonts` fires after every successful export so the font reaches the worksheet tools. **1 real finding (fixed this session, f8a604f) and 0 new feature seeds — the tool is mature and well-onboarded**, the same verdict S130/S140 reached for the generator and trope tutor. **3 would-be findings killed by grounding before any was logged:** (1) "drawing does nothing after the wizard" — the harness read `L.draw.length` where `.draw` is `{v,strokes}`, an **object**, and separately read `window.workMode` when top-level `let`s are not window properties (the S191 note, re-earned); a bare-identifier probe showed strokes registering correctly. (2) "no My Fonts handoff" — `offerSaveToMyFonts` is called from **4 sites**, after every successful export; the probe had simply never reached a completed export. (3) "the hint links only appear in SVG/English mode" — inherited from the queued candidate and **false**, see 01ebdb5.

_(Archived at the S200 close-out — Done had reached 6 sessions; hygiene rule is 5.)_

- [x] 2026-08-11 | (S195 close-out) | branch/deploy note | `/improveloop` → S195 **continued on `claude/improve-loop-mz3ifp` and draft PR #163** (still open + unmerged at session start, so per the branch protocol no new branch and no new PR — S194's 5 commits plus this session's 5). **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `519f31e` (main's `sw.js` still **v474**; the branch's **v475** is S194's own bump), `FONT_MAKER_VERSION` unmoved at **5.9**. Clone arrived **unshallow already** (S191's trap re-checked, not assumed: `git rev-parse --is-shallow-repository` = false, 1948 commits). **SW v475→v476**, precached files changed: `trope_tutor.html`, `torah_trainer.html`. **No `FONT_MAKER_VERSION` bump — the Font Maker was not touched this session.** `check-i18n.js` **clean** (Check A clean + the same 14 baselined; Check B clean) — and the drawer-duplicate deletion *removed* 6 hardcoded English literals rather than adding any. `build-locales.js` **not run — zero CSV keys added** (every change is CSS or a deletion). `update-sitemap.mjs` run **last**. **Decisions deferred to maintainer: none** — both gates were asked and answered this session, including the gate-2 item S194 had deferred. Deploy `success` is verified after merge, not in-session. **⚑ LANDED DIRECT TO `main` BY EXPLICIT IN-SESSION MAINTAINER AUTHORIZATION** ("Push to main", 2026-08-11, after the close-out summary was delivered) — the branch-protocol carve-out, same as S78 and S191–S193. `claude/improve-loop-mz3ifp` fast-forwarded `main` from `519f31e` with all **10** commits (S194's 5 + S195's 5); no drift, no merge commit, PR #163 closes as merged by the push. Pages deploy verified **after** the push, which is the one close-out step a branch-and-PR session cannot do in-session.
- [x] 2026-08-11 | 0089150 | torah_trainer.html | **The reading and settings dropdowns reach the 30px touch floor.** Closes the last actionable slice of the touch-target census S194 re-scoped 59→31: `.btn-xs` closed suite-wide in S193, `.sreset` in S194, and these four are what genuinely remained — `#parshaSelect` + `#scopeSelect` (control bar) and `#optTranslitStyle` + `#optLoopPause` (drawer), all **29px** against the ratified 30px floor (S102). `min-height:30px` on the whole `.tt-ctrl`/`.form-row` selector group, not on `select` alone, so a dropdown never ends up a different height than the input beside it. **The 5th sub-floor control was deliberately left: the shared `.i18n-select` (26.2px) is styled in `js/i18n.js` and renders on all 12 pages — a different blast radius, logged as its own candidate.** Verified light+dark × 1280/800: 4×30px, every row holding one still hugs its tallest child, no horizontal overflow, parsha dropdown still 71 options, `pageerror == 0`. **Method note: the first row assertion failed a CORRECT fix** — it checked every `.tt-ctrl`/`.form-row`, and the "Translit / Translation" toggle row legitimately wraps to 48px over 22px children **on the unmodified file too** (baseline-verified by stash). Tallest-child 22px already proved no in-scope control was in it.
- [x] 2026-08-11 | 200bbf8 (**gate-2 ASKED AND ANSWERED — maintainer chose delete-the-duplicate over wire-the-keys**) | torah_trainer.html | **The settings drawer's untranslated duplicate tool list is gone.** Closes the P3 S194 re-scoped from "2 strings, pure wiring, no gate" to **6 strings whose canonical `shared.nav.*` keys carry different English on 5 of them**. **The scope correction that made the decision easy: the footer twin is a strict SUPERSET** — 7 wired links covering all 5 of the drawer block's destinations *plus* the Trope Tutor and the Font Maker — so deleting loses zero destinations and zero translation coverage. Nothing targeted the block (no id, no class, no tour step). Verified across **8 cells** (1280/800 × light/dark × EN/HE) with the drawer open and every panel + `<details>` expanded: duplicate gone, exactly one Related-Hebrew-Tools nav remains, footer's 7 links all render, all 5 deleted destinations still reachable from the page, About panel keeps its content, `pageerror == 0`.
- [x] 2026-08-11 | b931b64 | trope_tutor.html | **A trope card is no longer split across two sheets** (S195 Pass G). `.tu-card` carried `break-inside:auto`, so **2 of the 5 cards straddled the page boundary at BOTH Letter and A4** — a card is one reference unit (mark + both tradition names + melody staff + four chanted examples), so half a card per sheet is unusable, not merely smaller. **Method note, and the reason this fix needed a different instrument: `break-inside` moves the printer's break WITHOUT moving any DOM geometry**, so the rect-based straddle model that FOUND the defect reports identical numbers before and after and cannot verify the fix at all. Verified instead from the **real PDF's per-page text** (pypdf, with the container's broken `cryptography` Rust bindings stubbed out), counting card heads (the `◌` carrier glyph) against trailing "See it in the parsha" links per page. **Control run proves the detector discriminates:** pre-fix = `[(4,2),(1,3),(0,0)]` unbalanced; post-fix = `[(2,2),(2,2),(1,1)]` Letter, `[(2,2),(3,3)]` A4, all 5 cards present, both themes.
- [x] 2026-08-11 | a26aa1f | trope_tutor.html | **The printed trope chart is legible from both themes** — S195 Pass G's headline, and the tool's first print fix ever. The whole `@media print` block was **one line hiding five selectors**; it had never established print colours. **Printing from dark mode produced a sheet that looks blank: 122 of 123 text nodes landed at 1.28:1 on white paper** (`--text` #dde4f0) — every trope name, every mark glyph, every example word; the sole survivor was the one button carrying its own navy. **And light mode lost the chart's own name in both themes**: `header h1` is `#fff` and the subtitle is `--gold-light`, both chosen for the navy header plate the print dialog drops → white-on-white, 1:1. Fixed by remapping the tokens under print media so both themes converge on the light palette (**the flash_cards S159 treatment**), plus selector overrides for the three literals a remap cannot reach — header title/subtitle and **`.mark-hit`, the cluster showing WHICH mark the example demonstrates**. Verified light+dark × 1280/800: every printed text node now ≥ 4.5:1 (n=122; the only sub-4.5 item left is the brand `.star`, a mark not text, keeping `--gold` per the token's documented note), title 14.81:1, mark-hit worst 5.47:1, **screen rendering unchanged in both themes**, `pageerror == 0`, real PDFs at Letter and A4.
- [x] 2026-08-11 | (S195 Pass G — print & export fidelity, one tool) | trope_tutor.html | **Pass G — the Trope Tutor's FIRST-EVER print audit, and the pass found the worst printed artifact the loop has seen.** **Target chosen by re-deriving the tally, not inheriting it** — the S194 pointer named "flash_cards or the generator" from the S169 run-count tally, but that tally counts G *fixes*, not dedicated runs; the per-run history it told me to check shows **trope_tutor at ZERO dedicated G runs** (its "trope 3" were fixes landed during *other* tools' G passes, e.g. S53). Surface census first: trope_tutor has a `@media print` block but **no `window.print()`** — a Ctrl+P surface — and the Learn tab renders exactly the artifact a teacher pins up: a trope reference chart. **4 findings, 2 fixed this session, and 3 would-be findings killed by grounding before any was logged**: the settings drawer and `<audio>` both looked "visible in print" to a `getClientRects()` probe (the closed drawer sits at `translateX(100%)` and still has rects — it does NOT print, confirmed on the real PDF), and the `.skip-link` measured 1:1 white-on-white but sits at `top:-48px`, off-sheet. **Remaining, logged:** interactive chrome prints (the "Next family →" button and the 6 family chips, one carrying a meaningless "✓"). **Method note: a style probe and a PDF disagree, and the PDF wins** — three of the four style-probe alarms were artifacts of measuring `getComputedStyle`/rects instead of the artifact.

- [x] 2026-08-13 | (S205 close-out) | branch/deploy note | `/improveloop` follow-up (**"More fixes"**) → S205 **continued on `claude/improveloop-all-fixes-jdfkm4` and draft PR #167** (open + unmerged at session start, so no branch restart — protocol followed; S204's 7 commits plus this session's 5 accumulate on the same PR). **Drift check:** origin/main unmoved at 57a4541; sw.js v486 / FM 5.12 matched — no outside-loop landings. **Pass run: — (skipped again on the same explicit user direction; K/H/G stay stalest, H-on-flash_cards still the standing recommendation — now three sessions running).** **Gates: none asked** (gate 1 pre-answered by the directive; no gate-2 surface). **SW v486→v487** (precached files changed: classroom_dashboard.html, torah_trainer.html, trope_tutor.html, Hebrew_Font_Maker.html, hebrew_dictionary.html). **FM: no FONT_MAKER_VERSION bump** (the canvas wiring is a fix, not a feature). Scripts: check-i18n clean, update-sitemap (full clone — unshallowed S204). Decisions deferred to maintainer: none.
- [x] 2026-08-13 | fe9246e | hebrew_dictionary.html | **The word-card copy/search/speak trio reaches the 24px WCAG floor** (S178 haul, the largest remaining count: 21×21 ×~300 cards; offsets re-pitched 7/33/59). **Deliberately 24, not the 30 house bar** — baseline measurement showed long Hebrew words ALREADY pass beneath the floating trio by design (117 text intersections at 800px pre-fix), so 30px boxes would deepen that zone; the trio joins the tolerated 24-29 band. | verified: 40 real cards × both cells — 24.0×24.0 ×3, 0 button overlaps, text intersections FLAT vs baseline (5→5, 117→116), copy click still sets .copied, card screenshot clean, 0 overflow/pageerrors
- [x] 2026-08-13 | c135e55 | torah_trainer.html | **Maqaf-joined proclitics inherit their partner's trope clause colour** (S180 Pass H P3 — the phrase tint had holes mid-phrase: 9 of 63 sample words, every one the proclitic half of a pair). famOf precompute + ONE backward inherit-only pass inside tokenizeHebrew; chains (אל־כל־דברי) resolve in the same walk; a token with its own family keeps it and a desync tail contributes nothing (no colour, never wrong colour); splitter + data-twi untouched. | verified: route-fulfilled Sefaria fixture (Deut 6:5 + a synthetic verse with a zakef-katon chain and a revia pair), 3 cells incl. marks-hidden: all 16 words coloured, the chain takes katon and על takes revia — the RIGHT family — data-twi sequential 0-15, 0 pageerrors
- [x] 2026-08-13 | 39282c9 | Hebrew_Font_Maker.html | **The printable template + specimen PDF speak Hebrew — all 30 fontmaker.canvas.* keys wired** (the LAST authored-but-unreferenced family; the S179 five-family haul is now fully closed). TEMPLATE_SPECS gains titleKey (title stays English on purpose — filename stem + auto-detect label); tpl_page_of/tpl_instruction with dir=auto on the heading divs; specimen cover (my_font/specimen_subtitle/special_forms), chart title+continued, 10 chart_<key> column headers, 3 anchor readouts, 5 section titles. Residuals: 'template saved.' toast has no key (NEW candidate), jsPDF full render stays CDN-blocked headless (ledgered S169). | verified: EN+HE cells — all 30 keys resolve, REAL buildTemplatePageHTML output reads 'Hebrew Letters · page 1 of 2' / 'אותיות עבריות · עמוד 1 מתוך 2' + localized instruction + 20 cells, chart heads/anchor lines correct both languages, 0 pageerrors; check-i18n clean
- [x] 2026-08-13 | 450bfc4 | torah_trainer.html + trope_tutor.html | **The TTS/speed sliders' 4px operable strip reaches 24px** (the S204-logged candidate; `.slider-row input[type=range]` carried an explicit height:4px — the 3ae6891 defect shape). The pre-fix suite-wide grep for the byte-identical rule (S201 lesson) found the copied twin in trope_tutor.html — both carriers fixed in one commit. | verified: both carriers × light/1280 + dark/800 with the settings drawer opened: every visible slider 24px, 0 pageerrors
- [x] 2026-08-13 | 1a4ba95 | classroom_dashboard.html | **transition:all backlog — the LAST arm closed, class closed suite-wide.** 7 sites (3 bare-duration shorthands included per the S204 method note): .hbtn/.btn/.vowel-scheme-btn/.font-opt/.radio-group label/.slider track → explicit colour/opacity lists; the toggle-switch knob (the arm's one real motion) transform-scoped + added to the reduced-motion block. Closure grep: 0 literal transition:all AND 0 bare-duration shorthands across all five tool files. | verified: EN/light/1280 + HE/dark/800 + reduced-motion cell — computed lists exact ×7, knob 0.2s→0s under reduce, toggles flip, 0 pageerrors
- [x] 2026-08-14 | (S206 close-out) | branch/deploy note | `/improveloop` (bare — no skip directive this time) → S206 **continued on `claude/improveloop-all-fixes-jdfkm4` and draft PR #167** (open + unmerged at session start: S204's 6 + S205's 6 + this session's 4 commits accumulate on the same PR). **Drift check:** origin/main unmoved at 57a4541; sw.js v487 / FM 5.12 matched the S205 close-out — no outside-loop landings. **Pass run: H (flash_cards)** — the three-session standing recommendation finally run (full result in the rotation row + the Pass H entry below). **Gates: 2 asked, 2 answered, 0 deferred** — gate 1: the maintainer chose the pass's own ?s= share-link seed on its FIRST offer (built this session, 4e3a69f); the card-sheet question: the **opt-in "print the practiced deck" row approved — queued as a PRE-APPROVED seed** (the natural S207 micro-feature). **SW v487→v488** (precached files changed: flash_cards.html, hebrew_dictionary.html, locales/en.json + locales/he.json). **FM: untouched — no FONT_MAKER_VERSION question.** Scripts: check-i18n clean (after the feature and at close-out), build-locales ×2 (1 new key; 1 context-cell edit), update-sitemap last (clone already unshallowed S204). Decisions deferred to maintainer: none.
- [x] 2026-08-14 | (S206 Pass H — teacher walkthrough, flash_cards: its first dedicated H since the S34-era founding runs) | flash_cards.html | **The cleanest H target the loop has walked** (full detail in the rotation row). A Bet/Vet lesson prep end-to-end across 6 contexts incl. a 390px student phone and HE/dark/800: 0 pageerrors, strong defaults (sticky Start bar, 7 clicks to a targeted deck), inline validation at every stumble the walkthrough hit. **4 would-be findings killed by grounding** (the Save "dead click" was probe blindness to chooseProfileModal; the ?path=1 "regression" was the profile-first design working; the report ladder line "missing" is the documented deficit-omit rule; share-code whitespace fragility is already stripped by the decoder). Intake: 2 P3s + 1 P4 — ALL resolved or gate-answered this same session — plus the long-missing flash-cards seed, shipped same-session. Method: fixed-position overlays have offsetParent === null, so an offsetParent-based visibility filter silently drops every open modal.
- [x] 2026-08-14 | 4f71a08 | hebrew_dictionary.html | **The ✕ clear buttons reach the 24px floor** — the haul's dismiss-button slice: `.pos-clear` 14×12 ×7 (nested inside the clickable section headers, where the bigger box also cuts clear-vs-toggle misfires) gains min 24×24 + inline-flex centring; the search-clear circle 20→24. Two CSS rules, glyphs unchanged. | verified: EN/light/1280 + HE/dark/800 — all 7 at 24×24 with the ✕ dead-centred (Δ0.0px), section headers 25→34px (≤36 guard), the search ✕ centred inside the ~34px input and shows/clears/hides on interaction, ✕ clicks still don't toggle the section, 0 page overflow, 0 pageerrors
- [x] 2026-08-14 | 6a65ed5 | flash_cards.html | **The Practice-Weak-Cards dead end states its reason inline — and the queued P4 was re-scoped on grounding:** the no-mistakes alert() was UNREACHABLE (renderProfileHistory hard-disables the button for a no-mistakes profile), so the real paper-cut was the reasonless disabled button — the exact dead-click shape the shared inline-validation rule names. Shipped the house Start-button pattern (documented in-file at _setStartNote): disabled→aria-disabled (focusable, clickable) + a #weakNote inline note (role=status aria-live=polite, key-based so a live language switch re-renders it, retired on fresh attempts and on result saves). 0 new strings — the existing key re-used; CSV context cell updated. | verified: both matrix cells — no dialog fires, note shows EN+HE, I18n.setLang('he') re-renders it live, fresh attempt retires it, the with-mistakes path still opens the count modal, 0 pageerrors
- [x] 2026-08-14 | 4e3a69f (**MICRO-FEATURE — gate-1 chosen on its FIRST offer, the same session its Pass H logged it; 2 iterations; 57 lines, inside the ~150 fence**) | flash_cards.html + locales/ui-strings.csv | **"📎 Copy Link" — the ?s= share-link twin of the Teacher Share Code.** Emit: one button beside Copy Code copies `location + '?s=' + <the same shareCodeEncode payload>` in the URL-safe base64 alphabet shareCodeDecode natively accepts (its `-`/`_` tolerance existed for exactly this). Load: a ?s= init hook (I18n.ready-gated, registered after ?path) runs the loadShareCode path — applySettings + saveSettings + startPractice — **silent on garbage** (a broken link lands on normal setup); the param is never stripped (parents bookmark it; refresh re-enters the drill). **Kills the S206 student-side discoverability P3**: the teacher shares a LINK a parent just taps; the buried paste box stays for copy-restricted channels. Words-mode links carry the wordsInline embed. 1 new key (EN+HE); 0 new storage; the button is a stateless action — no getSettings/.ivrit wiring exists to add. | verified: 13 checks — teacher copy + feedback swap + 1.5s restore (EN/light/1280); fresh-profile student lands mid-drill from the real copied URL at 390px AND at HE/dark/800 (RTL); URL-safe alphabet asserted; garbage ?s= silent with 0 pageerrors; a words-mode link drills its embedded pool on a store-empty machine and leaves the store untouched
- [x] 2026-08-14 | (S207 close-out) | branch/deploy note | **"Run the loop normally"** → S207 **continued on `claude/improveloop-all-fixes-jdfkm4` and draft PR #167** (open + unmerged at session start — origin/main does not contain the branch head; branch in sync at fcb24c9), so no new branch and no new PR: **the PR now carries S204–S207**. **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `57a4541`, `FONT_MAKER_VERSION` unmoved at **5.12**. **SW v488→v489** in the final commit; precached files changed: `hebrew_blend_generator.html`, `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `flash_cards.html`, `torah_trainer.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** the FM changes are i18n wiring/retranslation fixes, not a feature release. `check-i18n.js` clean after every change (Check A clean + the same 14 baselined; Check B clean); `build-locales.js` ran twice (1 new key 1684df2, 2 new keys 2ba9646 → 4,276 keys); `update-sitemap.mjs` run **last** on a verified-unshallow clone. **Gates: 0 asked** — the micro-feature was pre-approved at the S206 batch (re-asking would re-litigate a made decision); the practiced-row EN copy shipped as gate-approved with HE drafted + REVIEW-marked; `dictionary.audio.beta_badge` copies the ratified sibling translation (בטא) rather than authoring wording. **Maintainer review queue: the 2 REVIEW-marked HE drafts** (`flashcards.sheet.practiced` / `_tip`). Deploy `success` is verified after merge, not in-session.
- [x] 2026-08-14 | (S207 Pass K — i18n audit, 13th run) | 5 fixed in-pass (1684df2) + 2 logged + 2 already-queued confirmed | **Both gates clean for the 6th consecutive K** (Check A clean + 14 baselined; Check B clean), the 85-commit `5415500..HEAD` diff probe clean (every added sink routes through I18n.t/`_pT`; the one new `_pT` key verified present), so the value was again past the gates: the **runtime reverse-lookup detector** (12 pages × HE, 0 pageerrors) returned **73 raw hits → 4 real fix classes + 2 logs after triage**. The headline class is architectural, two carriers: **panels built before `I18n.ready` that applyI18n never rebuilds** — the generator's vowel-lock tile names (`initLockVowelGrids` missing from the 2026-07-29 audit's own rebuild list; proved English at the reveal AND after a live EN→HE switch while the letter tile beside read אלף) and the FM custom-glyph add-form (`renderGrids`' once-guard re-renders only the GRID after first build, so the form's `_cgT` strings — authored, translated, and "wired" per the S177 census — kept boot English forever). Plus the kern-class modal's Cancel (S192 fixed the same modal's TITLE; the Cancel was invisible to a strict-visible detector because the modal starts closed), the dictionary BETA badge (three sibling tools wire theirs), and the queued **form-tab-intros family wired** (3 keys + the S174-convention `<b>` restore to special_intro's CSV cells). **Method: the detector's parent-only computed-style filter is ancestor-blind** (computed display does not inherit `none`) — every located hit was rect 0×0 — but that accident produced a hidden-DOM arm that found what S192's strict-visible collector provably missed; a reveal probe then separated stale-forever (real) from rebuilt-on-reveal (benign). Dismissed on grounding: trope pronunciation names (never-translate), "Alef" the font family, "Letter" the paper size, the pos badge (already queued). Logged: flash-cards Colors-tile labels (P4, gate), FM `precompName` raw composed-form names (P4, key census).
- [x] 2026-08-14 | b6d2dd5 | hebrew_dictionary.html | **The vowel-group collapse headers reach the 24px floor** (the haul's named next slice): the 7 clickable group headers measured 229×14 — a bare 0.68rem flex line with no min-height. `min-height:24px` in the builder's cssText; `align-items:center` keeps label + arrow centered. Verified 229×24 in EN/light/1280 + HE/dark/800, collapse/reopen intact, arrow Δ0.0px, no page overflow, 0 pageerrors.
- [x] 2026-08-14 | 3aabcea | torah_trainer.html + CLAUDE.md | **Every printed sheet carries the reference + clause key — the print band closes BOTH queued print P3s in one mechanism.** `#ttPrintBand` (range label + compact clause key) is populated at beforeprint (after the palette force, so dark boots print light) and re-stamped on every page by print's position:fixed repetition, sitting at top:-18mm inside a new `@page { margin-top:22mm }` — geometrically unable to overlap content on any sheet. Its swatches are born with the SOLID `--trope-*-line` ink the printed underlines use (absorbing the greyscale-collapse P3); the in-flow `.tt-ref-hdr` + legend card go print-hidden (screen behavior unchanged — the legend card still shows on screen). CLAUDE.md's Trope section rewritten: the S90 "legend prints" paragraph now documents the band, with the guard flipped to "do not restore the printed key to sheet-1-only". **Verified by PDF content-stream scan** (no pdftotext in the container — object-boundary splitting + declared-`/Length` slices + zlib, counting swatch rects above the content-top y per page): Letter **11/11**, A4 **10/10**, HE/dark **11/11** pages carry ≥6 band rects; trope-off prints label-only; afterprint restores dark + hides the band; 0 pageerrors. (The analyzer first reported a false 9/10 on A4 — `rstrip` had eaten a trailing 0x0A that was real deflate data; fixed by declared-length slicing. The parallel-layout column headers on pages 2+ (line-176a) are NOT covered by the band — still queued.)
- [x] 2026-08-14 | 2ba9646 (**MICRO-FEATURE — pre-approved at the S206 gate; built without a re-ask**) | flash_cards.html | **"Print the deck we just practiced" — the card sheet can match the drill.** An opt-in switch in the sheet menu makes printCardSheet take `savedCards` (the drilled deck Redo uses — same cards, same order) instead of a fresh generateDeck draw; the row appears only once a drill has run this visit (savedCards is in-memory), the default stays fresh-draw, and the preference rides the settings blob (getSettings/applySettings with `??`), so presets, `.ivrit` files and AllTools carry it with **zero new storage keys**. 20 lines + 2 keys (EN as gate-approved; HE drafted + REVIEW-marked like the menu title). **17 checks:** exact savedCards-sequence print with the count control deliberately mismatched (7 practiced vs 12 fresh — source-switch proof), duplex interplay (card page from savedCards + answer page), afterprint cleanup, toggle-off fresh draw at the live count, settings round-trip + reload persistence, pref-ON-with-no-drill safe fallback, fresh-profile (row hidden pre-practice, print unchanged), HE/dark/800 (label from the new key, menu inside the viewport), 0 pageerrors everywhere.
- [x] 2026-08-14 | 1684df2 | hebrew_blend_generator.html + Hebrew_Font_Maker.html + hebrew_dictionary.html + locales | **The Hebrew UI stops showing boot-time English in five spots — the Pass K fix slice** (one commit, one finding class): (1) generator vowel-lock tile names rebuild in applyI18n — the 2026-07-29 audit class's missed sibling (its own comment names the class); (2) the FM custom-glyph add-form retranslates — applyI18n now rebuilds the full panel past `renderGrids`' once-guard, accepting the documented open-Help trade (an unsent add-form resets on a language switch); (3) the kern-class dialog's Cancel wires its authored key; (4) the three composed-form tab intros wire `special/yiddish/ladino_intro` (+`<b>` restored to special_intro's CSV cells so EN renders identically — 2 `<b>` elements before and after); (5) the dictionary audio BETA badge localizes via new `dictionary.audio.beta_badge`, copying the ratified sibling בטא. Verified at the reveal pre/post: lock tiles קמץ/פתח/חטף פתח on HE boot AND after a live switch, the FM form fully Hebrew down the real setter path, badge בטא, EN unchanged incl. bold; 0 pageerrors.

- [x] 2026-08-14 | (S208 close-out) | branch/deploy note | **"Continue from where you left off"** → S208 **continued on `claude/improveloop-all-fixes-jdfkm4` and draft PR #167** (open, unmerged — now carrying S204–S208). **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `57a4541`, `FONT_MAKER_VERSION` unmoved at **5.12**, branch SW v489 was S207's own bump. **SW v489→v490** in the final commit; precached files changed: `classroom_dashboard.html`, `torah_trainer.html`, `hebrew_dictionary.html`, `Hebrew_Font_Maker.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** the FM change is i18n wiring of existing copy, not a feature (the S207 precedent). `check-i18n.js` clean (Check A clean + the same 14 baselined; Check B clean); `build-locales.js` run for the 1 new CSV row (4,277 keys); `update-sitemap.mjs` run **last** on a clone verified unshallow. **Gates: 1 asked, 1 answered, 0 deferred** — gate-1 (weekly-grid print seed, its first offer) → **maintainer chose SKIP**, so all 5 iterations went to fixes. Deploy `success` is verified after merge; the PR #167 watch stays armed.
- [x] 2026-08-14 | (S208 Pass G — print & export fidelity, `classroom_dashboard.html`: **its FIRST-EVER dedicated G and the per-tool history's last structural zero**) | 3 findings ALL fixed in-session (d08c124) + 1 P4 logged + the weekly-grid seed grounded | **The S195 starting condition reproduced: the page had NO `@media print` at all.** Measured (per-text-node paper-contrast census, print emulation, both themes × 1280/800): dark-mode Ctrl+P printed **55/67 text nodes below 1.5:1** on white paper — a near-blank sheet (hebDate 1.28:1, currentTime 1.39:1) — while light mode lost **every navy-plate string** (24 invisible, incl. the h1 and all col-titles, the Hebrew day-question rendering inside `#dowColTitle`); **24 interactive controls** printed as chrome; an **open settings drawer printed OVER the page** (position:fixed + full-opacity backdrop); a **160% SmartBoard zoom** (inline style on `.dashboard`) would print clipped; printBackground-on floods ~109%/19% of the sheet navy. Census exclusions that keep the numbers honest: `.spoiler` (color:transparent hiding IS the feature — paper-correct) and emoji-only marks. **Export arm clean:** `IVRIT_CFG.gather()` → presets/schedules/both folder trees/83-key liveState (the ivrit-gather-gap sweeps keep this verified). Grounding note: the weather ↺ Refresh hangs off `#weatherBody`, not `#weatherBlock` — the error state renders BESIDE the block.
- [x] 2026-08-14 | d08c124 | classroom_dashboard.html | **Ctrl+P yields a legible sheet instead of a blank or flooded one** — one designed `@media print` block (the trope_tutor S195 treatment): token remap converging both themes on the light palette **with `--gold-light`→text-gold** (safe: its only background uses are :hover states — one token line converged the whole `body.dark` accent-leak class: nn-val, holiday line, omer, weather-temp, text links), literal re-ink for the navy-plate text, an 18-selector chrome/overlay hide-list (drawer, first-run card, weekly editor, tour, picker, video, timer, zoom bar, fs strip), and `.dashboard{zoom:1!important}`. Verified: **0 invisible real-text nodes** (was 55 dark / 24 light) + **0 printing controls** (was 24) in light+dark × 1280/800, ink probes ≥4.5:1, screen computed styles untouched (navy header, white h1/col-title, zoom bar live), drawer + first-run card print-hidden but screen-live, zoom 160%→1 in print, Letter+A4 PDFs produced, 0 pageerrors.
- [x] 2026-08-14 | 4bc2c69 | torah_trainer.html | **The S181 print polish set ships** — (a) side-by-side column labels repeat on every sheet: `buildPrintBand` appends a `.pbnd-cols` row mirroring the reading grid (1fr count from `data-print-cols`, 20px gap, 24px `.tt-page` padding; h3 text/placement/direction cloned → **0 new strings, HE-RTL correct**). Verified **0px label/column center deltas** in EN-LTR and HE-RTL, and PDF content-stream analysis — **CTM-walked device coordinates, /Pages /Kids page order** — shows band-strip text spanning both page halves on **8/8 sheets** (page 1's 12 runs = exactly the band's own; pages 2+ add 8 clip-culled neighbour runs, confirming the interpretation). (b) `.tt-aliyah-divider{break-after:avoid}` — dividers staged real via a 2-chapter Bereshit fixture (index 0; Noach's starts match nothing — see method note); rule-level receipt. (c) drop cap prints `--gold-text` 6.22:1 (it is TEXT, not a mark), both themes, screen untouched.
- [x] 2026-08-14 | 6f058d3 | hebrew_dictionary.html | **Footer nav links (×7) + the word-modal Google link reach the 24px floor** (the haul's named 15px slice): inline-flex + min-height:24px on `footer nav a` and `.wm-google`; verified 24px in light/1280 + dark/800, no horizontal overflow, visuals unchanged.
- [x] 2026-08-14 | a7a11cd | Hebrew_Font_Maker.html + locales/ui-strings.csv | **The template-saved toast speaks Hebrew** — `status(spec.title + ' template saved.')` was a hardcoded English concat beside the keyed `building_template` sibling. New `fontmaker.status.template_saved` ({title} interpolation; HE drafted + REVIEW-marked), display title from `I18n.t(spec.titleKey)` per the titleKey model — filename stems keep English `spec.title`. Verified EN ("Hebrew Letters template saved.") and HE ("תבנית אותיות עבריות נשמרה.") resolution, literal grep 0, check-i18n clean.

## S209 (2026-08-14) — archived at S214 close-out

- [x] 2026-08-14 | (S209 close-out) | branch/deploy note | `/improveloop` → S209 **continued on `claude/improveloop-all-fixes-jdfkm4` and draft PR #167** (open, unmerged — now S204–S209). **Drift check: nothing landed outside the loop** — `origin/main` unmoved at `57a4541`, FM **5.12**, branch SW v490 was S208's own bump. **SW v490→v491** in the final commit; precached files changed: `hebrew_dictionary.html`, `classroom_dashboard.html`, `Hebrew_Font_Maker.html`, `torah_trainer.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** i18n wiring of existing copy, not a feature. `check-i18n.js` clean; `build-locales.js` run (4,277 keys; markup restoration only, no new rows); `update-sitemap.mjs` run **last** on the verified-unshallow clone. **Gates: 1 asked, 1 answered** — gate-1 (weekly-grid print seed, 2nd offer) → **maintainer chose SKIP again**; the S180 set's (b)/(d) were split as gate-2 candidates rather than asked mid-budget (not fundable this session — deferred to the next attended batch). Deploy `success` verified after merge; PR #167 watch armed.

- [x] 2026-08-14 | (S209 Pass D — performance snapshot, `classroom_dashboard.html`: the stalest D tool by ~92 sessions (S117), carrying the weekly-grid engine, calendar widgets, zone layout, TTS tap-to-read, and the new print block — none ever profiled) | **CLEAN — zero blocks >200ms at 1× anywhere** | Cold load 1×: DCL 149ms, FCP 128ms, top longtask 102ms (0 over 200). Cold 4×: ONE 446ms boot chunk — the FM-S196-class throttled baseline, logged as measured-baseline not defect. Interactions (1× and 4×): openSettings 9/36ms sync, weekly Schedule-Sync editor 14/43ms, closeAll 2/5ms, toggleDark with nikkud colorizing **1/6ms** (the S60-class CSS-var recolor architecture), zoom drag ×20 no jank, and the 28-name student-picker spin (roster seeded through the real textarea path) **0 longtasks at both rates** through the full 3.2s flicker+land animation. 0 pageerrors everywhere. Fixture: seeded colorCodeNikkud + default-on widgets; weather/hebcal fallbacks under aborted externals.

- [x] 2026-08-14 | 20a2ae8 | hebrew_dictionary.html | **The touch-target haul's sub-24px backlog CLOSES** — FAQ/How-to disclosure rows ×7 (19-21px; min-height rule beats nothing since the inline styles never set it; display stays list-item so the markers survive), .nav-btn incl. #tourBtn (22-23px), and both 16px-wide sidebar edge handles → 24px. Verified ≥24px light/1280 + dark/800, summaries still toggle their details, no horizontal overflow, 0 pageerrors.

- [x] 2026-08-14 | b7737ba | classroom_dashboard.html | **Print page-break protection** — .day-item + weather/omer/shabbat blocks gain break-inside:avoid in the print block (the S208 P4, split from the ink commit per the S195 shape). Rule-level receipt: computed avoid under print emulation; the baseline-vs-fixed PDF differential (rule force-disabled via injected override vs live) rendered identically with stable page counts — the fixture stages no crossing, the S208-divider receipt shape. The color-tagged CTM rect walker (day rows = the warm-gray fill) was built for it and disproved my first loose classifier (which counted 12 "day rects" that were mostly other wide blocks).

- [x] 2026-08-14 | 20be5d5 | Hebrew_Font_Maker.html + locales/ui-strings.csv | **The Place-Nikkud/Trop side panel's seven composed-English sites wire to their authored keys** (pos_title was literally rebuilding "{class} position — {name} only" from the raw ANCHOR_LABEL map beside the wired anchorLabelT helper). All seven via _pT with byte-identical fallbacks; 4 CSV rows regain their rendered markup (<b>, the keyboard-hint opacity span) per S174. Verified in-flow EN+HE down the real anchors paths — the trop branch needs setMarkEnabled('trop', true) first (the wizard fixture defaults trop OFF and gotoAnchors falls back silently), and the hints render only with an active trop mark; live language switch retranslates in place; 0 pageerrors; check-i18n clean.

- [x] 2026-08-14 | d55b80c | torah_trainer.html | **A verse loop now supersedes chant-all** (S180 set (a)): startLoop stopped read-all and TTS but never chant-all, leaving the ⏹ chant Stop lit after the loop AND the lingering chantAllState.active suppressing the verse-end stop for every later single-verse play (loadKaraoke's stopAt read). Fix = the mirror stopChantAll() in startLoop + the toolbar template's state-true .active class (verification caught the rebuilt button keeping state-true TEXT but dropping the lit class — check both signals on rebuilt DOM). Verified down the real state machine light+dark: engage → loop supersedes → stop leaves nothing stale; 0 pageerrors.

## S210 (2026-08-14) — archived at S215 close-out
_(includes the out-of-band Terms of Use landing, which belongs to the same block.)_

- [x] 2026-08-14 | (S210 close-out) | branch/deploy note | `/improveloop` → **the S204–S209 PR #167 MERGED**, so per protocol S210 cut a **fresh branch off latest `origin/main`** — `claude/improve-loop-k6dwi7` (the session's designated branch; it existed already and sat exactly at `origin/main` e435c04, so no restart was needed) → **new draft PR at close-out**. **Drift check: FOUR outside-loop landings** since the S209 close-out's `57a4541`, of which **two were never ledgered** — `6c869d9` terms.html + footers + sitemap (ledgered), `f875f98` torah Copy panel (ledgered), and the two **UNLEDGERED**: `7642fbc` dashboard one-char-per-line preset names + Rename, `e435c04` dashboard English font → toolbar dropdown. **SW v491→v495 outside the loop** (v492 terms, v493–v495 the three feature commits); **FM unmoved at 5.12**. Both unledgered dashboard landings were probed by this session's Pass I and are **i18n-clean on a fresh HE profile** (preset row reads טען/שכפול/שנה שם/מחק/העבר; the new `.ed-font-select` carries an explicit `aria-label`). **SW v495→v496** in the final commit; precached files changed: `Hebrew_Font_Maker.html`, `torah_trainer.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** both FM commits are i18n wiring of existing behaviour (drop zones, side-positions note, stage legend), zero new features and zero English wording changes, matching the S209 call. Scripts run: `build-locales.js` (×3), `check-i18n.js` (clean — no new blocking violations), `update-sitemap.mjs` **after `git fetch --unshallow`** (the container clones shallow; the script refuses outright, and the ledger's standing caution about date receipts from `git log` applied). **Decisions deferred to maintainer (session declared non-interactive — both gates took the unattended fallback):** **gate 1** — micro-feature SKIPPED this session, no seed built; **gate 2** — the terms.html Hebrew question (below) logged as a Candidate rather than acted on, because the two defensible answers lead to *opposite* code.

- [x] 2026-08-14 | (S210 Pass I — first-load & empty-state, 17th run, all 13 pages on genuinely fresh contexts) | 1 headline finding (half fixed in-pass, half gate-blocked) + 2 logged | **The mechanical gates are clean for the FIFTH consecutive I** — 26/26 fresh-profile loads (13 pages × EN/HE, `serviceWorkers:'block'`, local HTTP) with **0 pageerrors, 0 failed same-origin requests, 0 raw-key leakage, correct `lang`/`dir` on every page**, empty preset/profile/schedule lists all instructive ("No saved presets yet." + the generator's ready-made-worksheet card), and the FM wizard gate opening as designed. So the run's value came from a **new probe past the gates**, aimed at the largest never-I'd surface: **the brand-new `terms.html`**, plus the two unledgered dashboard landings. **The probe that paid: compare rendered body length EN vs HE per page.** Every localized page shrinks materially in Hebrew (privacy 7199→6112, index 2103→1805); `terms.html` measured **6199→6198 — a one-character delta**, which is the signature of a page that ignores the language switch entirely. Grounding confirmed it and sharpened it into a *rendering* defect, not merely translation debt: terms.html carries **31 keys of which 30 have an empty `he` cell** (its sibling privacy.html is 38/38 translated), so in the Hebrew UI **all 38 of its English paragraphs render under `direction: rtl`** — right-aligned, bidi-flipped trailing punctuation — while `privacy.html` renders 50/50 Hebrew and 0 English-under-RTL. The footer half of the same gap (`shared.footer.terms_link` empty → "מדיניות פרטיות · Terms of Use" side by side on all 5 carrier pages) **was fixed in-pass** (2065868) since it supplies missing Hebrew for an already-shipped label rather than changing wording. **The body half is gate-2 and was NOT touched** — see the Candidate. **Method notes:** (1) *an EN-vs-HE body-length ratio is a cheap, high-yield localization detector* — it needs no key census and catches a whole page at once; a page whose HE length matches EN within ~1% is either untranslated or content-only, and the two are trivially distinguished by counting Hebrew-bearing blocks. (2) *`data-i18n` coverage is not translation coverage* — terms.html has 35 `data-i18n` attributes and looked correctly wired by every structural check; only the CSV's empty `he` cells revealed it. Check B reports this as warn-only debt, so a green gate is not evidence a page speaks Hebrew.

- [x] 2026-08-14 | 0e0b4ec | Hebrew_Font_Maker.html + locales/ui-strings.csv | **The workspace stage legend speaks Hebrew** (the queued S209 P4, plus its two siblings in the same function — one class, one commit). `renderLegend`'s three non-draw branches were hardcoded English template literals sitting beside a fully-keyed draw-legend sibling; all three now route through `_pT` with byte-identical English fallbacks. The anchors banner also stops rebuilding its class name from the raw `ANCHOR_LABEL` map and uses the **translated `anchorLabelT` helper beside it** — the same raw-map-beside-wired-helper shape S209 found in `pos_title` — so "Below" reads "מתחת". | verified: EN+HE × light/dark × 1280/800 against **`#stageLegend`** — the first `.legend` selector matched the *grid* legend twin ("image/traced/anchored") and manufactured four false passes, the S209 distinctive-substring lesson recurring; align + anchors staged through the real work-mode paths, the wide-form branch through a stubbed `getPrecomp`. `<b>` and the coloured dot preserved, 0 pageerrors.

- [x] 2026-08-14 | 2947785 | torah_trainer.html | **A transliteration cold load builds the reading ONCE, not twice** (the S184 Pass D P3). `init()` fired `ensureLoaded()` fire-and-forget and then awaited `fetchAndRender()`, so the first `renderText` always ran while the esm.sh ESM was in flight — a full reading built with no translit rows **and a false "Transliteration unavailable" chip** — which `translitlibloaded` then discarded and rebuilt. Fix is the candidate's own prescription: race `ensureLoaded()` against a 2s timeout before the first render, only when `settings.showTranslit`. | verified by **baseline-vs-fixed differential** with the `#ttReading` innerHTML setter instrumented, 40-verse route-fulfilled Sefaria fixture + a **stub translit ESM with a controllable import delay** (so the race is real): fast CDN (150ms) baseline write 1 = **57,782 B, 0 rows, chip PRESENT** (the wasted full build) → fixed write 1 = **299 B (placeholder only)**, write 2 = 64,161 B / 40 rows / no chip. Slow CDN (3000ms, past the cap): baseline and fixed both fall through to the dead-path render then rebuild — **the blocked-CDN path is preserved**, just deferred to the 2s cap, and its chip is now truthful. Final state identical in both. EN/light/1280 + HE/dark/800; **translit OFF still renders exactly once**, so the new await never touches that path. 0 pageerrors.

- [x] 2026-08-14 | 9a30923 | Hebrew_Font_Maker.html + locales/ui-strings.csv | **The FM backlog's last three authored-but-unwired keys ship — the family is now fully closed.** `letter_drop`/`markctl_drop` were full English template literals built beside their own authored keys; both route through `_pT` with byte-identical fallbacks. The file-kind fragment gets its own pair (`drop_kind_svg`/`drop_kind_img`), and **"browse" became a `{browse}` param** so its clickable span and static `onclick` stay in code rather than migrating into the CSV — grounding showed `wireDrop` binds **drag events only**, so that span is the *sole* click path into the file input and could not be flattened to plain text. `side_positions_kept` splits `.one`/`.other`, transcribing the live possessive ternary (no new wording). | verified in flow EN+HE × light/dark × 1280/800: both drop zones render with `<b>` intact across all four image/SVG cells, **clicking the browse word still opens the right file input**, the plural pair renders at n=1 and n=2 and stays hidden at n=0, English byte-identical to the old ternary, 0 pageerrors. Fixture lessons: marks live at **`project.font.<kind>.custom`** (via `customMark`), not `project.<kind>`; a mark's drop zone needs **`designMode='upload'`** (the panel defaults to the Simple editor); letters key on **`codepoint`**, not `cp`.

- [x] 2026-08-14 | 2065868 | locales/ui-strings.csv (+ rebuilt en/he.json) | **The footer's "Terms of Use" link speaks Hebrew** (the fixable half of the Pass I headline). `shared.footer.terms_link` shipped with an empty `he` cell while its `privacy_link` sibling was translated, so the Hebrew footer on all five carrier pages read "מדיניות פרטיות · Terms of Use". The wiring was already correct on every carrier — only the CSV cell was missing, so this supplies Hebrew for already-shipped wording rather than changing copy (not a gate-2 call). HE REVIEW-marked per house convention. | verified: **40/40 cells** (5 pages × EN/HE × light/dark × 1280/800) — "תנאי שימוש" beside "מדיניות פרטיות" in HE, "Terms of Use" unchanged in EN, 0 pageerrors.

- [x] 2026-08-14 | (Terms of Use session — branch `claude/ivritsuite-terms-page-dt2vkj`) | terms.html (new) + index/privacy/contact/404 footers + sw.js + sitemap.xml + locales/ui-strings.csv (+ rebuilt en/he.json) + CLAUDE.md counts | **Added `terms.html` — 13-section Terms of Use as privacy.html's structural sibling** (output ownership, third-party license pass-through, AS-IS warranty disclaimer + $50 liability cap, GA governing law; §5 filled from the code-verified third-party inventory incl. YouTube; kokoro-TTS clause omitted — recon confirmed the Advanced-TTS removal at 43b64e6 left only browser Web Speech API). Fully i18n-wired privacy-style: 32 new CSV keys, `he` cells empty = tracked Check-B debt (only `terms.header.back_link` reuses privacy's `← חזרה`); §5 via `data-legal-block`, 11 `\n`-joined segments. Footer `Terms of Use` link on the 4 privacy-linking pages (byte-identical block kept). **SW v491→v492**, `/terms.html` precached. Follow-up candidates (flagged, NOT edited): privacy §4 omits YouTube embed, Sefaria runtime API, Hebcal, the Nominatim browser-geolocation path, and the CDN operators; privacy §6 "no submission form" contradicts the contact/suggest forms.

## S211 (2026-08-15) — archived at S216 close-out

- [x] 2026-08-15 | (S211 close-out) | branch/deploy note | `/improveloop` → **PR #170 was still open + unmerged**, so per protocol S211 **continued on `claude/improve-loop-k6dwi7` and the same draft PR #170** (no branch restart, no new PR; S210's 5 commits + this session's 5 accumulate on it). **Drift check: CLEAN — origin/main unmoved at e435c04, sw.js v496 and FM 5.12 both matching the S210 close-out, so nothing landed outside the loop this time** (the first drift-clean session since S208; S210 had found two unledgered dashboard landings). **Pass run: F (cross-tool consistency, 18th run)** — affordance: the settings/options DRAWER contract (full result in the rotation row + the Pass F entry below). **Gates: 0 asked, 2 deferred — the session was non-interactive.** Gate 1: micro-feature **skipped**; the Pass F convergence it would have built is logged as a Feature seed. Gate 2: the **terms.html Hebrew question carries forward UNANSWERED from S210** — it is now the oldest open gate item and blocks the single highest-value candidate in the file. **SW v496→v497** (precached files changed: torah_trainer.html, classroom_dashboard.html, Hebrew_Font_Maker.html). **FM: NO FONT_MAKER_VERSION bump** — the one FM commit is i18n wiring of existing behaviour (a raw name → `gName()`), zero new features and zero English wording changes, the same call S210 made. Scripts: check-i18n clean (no NEW blocking violations; Check B's `terms.*` debt is the deferred gate-2 item itself), **build-locales NOT run — no CSV keys were added or edited this session**, update-sitemap last (clone was already full at 2,054 commits, so the shallow-clone guard did not fire). Decisions deferred to maintainer: the two gates above.

- [x] 2026-08-15 | (S211 Pass F — cross-tool consistency, 18th run) | affordance: **the settings/options DRAWER contract** | **13 of 14 dimensions perfectly consistent; the single divergence is a shipped micro-feature, not a defect — and TWO of the pass's own findings were killed by grounding before either was logged.** Affordance picked per the S210 pointer's first suggestion (S180's open drawer P3), and deliberately NOT the tour (S198 measured that one as the most consistent affordance ever). **Contract fixed BEFORE measuring**, per the S198 method: 8 shell dimensions (Escape closes, backdrop click closes, `role=dialog`+`aria-modal`, non-empty accessible name, focus enters on open, focus returns to opener on close, Tab trapped, close button present) across the 3 `.settings-modal` carriers (dashboard, torah, trope), plus 6 panel dimensions (tabindex 0, `role=button`, `aria-expanded` present AND correct vs `.collapsed`, Enter toggles, Space toggles, state persists across reload) across the 6 `.panel-title` collapse carriers (those 3 + generator, flash_cards, dictionary). All cells EN/light/1280 + HE/dark/800, `serviceWorkers:'block'`, local HTTP. **Shell: 8/8 on 3/3 carriers, 0 pageerrors** — torah's and trope's implementations are literally identical and the dashboard's differs only by an extra in-place-edit guard; accessible names resolve in both languages (torah via `aria-labelledby`, the other two via `data-i18n-aria-label`). **Panels P1–P5: 6/6 carriers clean, 0 aria-expanded missing, 0 wrong, in both languages.** **The one divergence — P6:** only **torah_trainer** remembers which drawer panels you left open; dashboard, trope, generator, flash_cards and dictionary all reset to markup defaults on every reload. That is not drift to fix quietly: torah's `settings.panelsCollapsed` shipped as the **S202 MICRO-FEATURE** (a1320a0) against a P3 worded "the settings drawer forgets which panels you opened, every session" — so replicating it is itself micro-feature-shaped and belongs at gate 1, which was unattended. Logged as a Feature seed + candidate instead. | **2 findings KILLED by grounding, both by the same discipline:** (1) the first run reported **the dashboard drawer does not close on Escape** (✗ on both matrices, ✓ for torah/trope). Real cause: the dashboard's Escape handler checks `#frModal` FIRST and `return`s if the first-run card is open — and my fixture's `.overlay.open` sweep never dismissed it, because that card is `.fr-modal`, not `.overlay`. With the card in its normal dismissed state Escape closes 6/6, and the both-open state is **unreachable** (`elementFromPoint` on the gear returns the fr-backdrop, so a user can never open the drawer from there). (2) **P6 passed on all 12 rows in the first run** — because Enter-then-Space toggles a panel TWICE, returning it to its original state, so the reload check compared a value to itself. Re-run with a single toggle it failed 6/6; re-run again with a 1.2 s flush wait (torah's `saveSettings` is debounced 300 ms and `p.reload()` kills the pending timer) it correctly showed 1/6. **Three separate probe defects, each producing confident results, all caught before anything was logged or fixed.**

- [x] 2026-08-15 | b0c48cd | Hebrew_Font_Maker.html | **The mark drop zone translates its mark name like its three siblings** (the queued S210 P4, one token ×2). `markctl_drop` interpolated `esc(it.name)` — raw table English — while `markctl_box_hint`, `markctl_combined_hint` and `markctl_region_hint`, all built within ten lines of it in the same `renderControls` branch, each pass their name through `gName()`. Both occurrences on the line (the plain-literal fallback and the `{name}` param) now use it. Same raw-value-beside-wired-helper shape as S209's `pos_title` and S210's `ANCHOR_LABEL`/`anchorLabelT`. | verified: EN/HE × light/dark × 1280/800, 0 pageerrors. HE "שחררו תמונה של קמץ, או עיינו." (was `...של Qamats...`); **EN byte-identical — `gName` is the identity in English**; the `{browse}` span still carries its `onclick`, so the drop zone keeps its only click path into the file input

- [x] 2026-08-15 | 3023e5c | classroom_dashboard.html | **The Tuesday day plate stops failing contrast in both themes** (the S193 Pass A P4). Six of the seven fixed day plates carry white type comfortably; Tuesday's `#c8a000` did not, and because the plates never invert there was no theme in which it read better. The day colours are a convention students recognise, so the **plate keeps its hue and the ink flips** — dark type on the light plate, the trade the suite already makes for gold-background controls (`.tt-loop.active`); the badge deepens its scrim to 0.45 rather than going white, so the gold accent survives. Applied **by luminance, not by naming Tuesday**: `isLightPlate()` flips at L > 0.30, which is not a taste threshold but the exact point where white's `(1.05)/(L+0.05)` crosses the 3:1 large-text bar — so any future light plate is covered. | verified: all 7 plates × light/dark, badge forced onto Tuesday with a **fixed clock** (it renders only for yesterday/today/tomorrow). Tuesday day-name **2.48 → 7.54**, English **2.19 → 5.67**, badge **2.63/2.82 → 4.59/4.93** — all three now clear the 4.5:1 NORMAL-text bar, not just 3:1; **the other six days unchanged to the hundredth** (only one plate flipped). Screen-only, and the Letter PDF is byte-identical before/after (2 pages, 205,784 / 125,209 bytes). **The first cut used a 0.4 threshold and silently changed nothing — caught because the probe asserted the `ink-dark` CLASS as well as the ratio.**

- [x] 2026-08-15 | cef62eb | torah_trainer.html | **A render stops re-dirtying all 146 Loop buttons** (the S184 Pass D P4, shipped as prescribed). `renderLoopState()` runs after every `renderText()` and unconditionally rewrote every verse Loop button's label, aria-label and pressed state — work the `innerHTML` write three statements earlier had just done with those exact values. **Verified by reading both template sites rather than assuming**: they emit `torah.reading.verse_loop_aria{verseRef:numLabel}` + `torah.reading.verse_loop`, and `data-vk` IS that same `numLabel`, so with no loop running the whole walk was provably a no-op. Early-return it and `syncBulkClasses()` (same shape) when there is nothing to apply and nothing stale to clear. | verified at 146 verses by mutation-record attribution on `#ttReading`: **renderText() 301 → 9 records** (the 9 being the reading rebuild itself — exactly what the biblepage layout, which emits no Loop buttons, produces), **of which `.tt-loop` 292 → 0**; isolated cost `renderLoopState` 0.47 → 0.18 ms, `syncBulkClasses` 0.13 → 0.06 ms. Behaviour intact both languages: starting a loop applies `.active` + `aria-pressed` + the `.tt-looping` marker, clearing reverts all three, bulk select marks and unmarks its verse and checkbox, **0 nameless Loop buttons after a render**. The candidate's third sub-fix (guard `clearLoop()`'s call with its `was` flag) was **deliberately declined** — with the early return that call is already cheap, and skipping it would also skip the audio-bar refresh, which lives outside `#ttReading`

- [x] 2026-08-15 | ba9c650 | torah_trainer.html | **Colour/font/dark restyles stop walking the whole parsha** (the S184 Pass D P3, the highest-priority actionable candidate in the file). The JS was never the problem — S60/S66's var-swap design keeps `toggleDark` at ~0 mutations — but the browser still restyled ~4,300 `.nik` spans and ~1,000 trope-classed words, so the live preview that makes those controls usable ran at ~10–24 fps. One screen-only rule (`#ttReading .tt-verse { content-visibility:auto; contain-intrinsic-size:auto 140px }`) makes it a viewport-sized job. | verified on a deterministic 146-verse fixture (2,628 `.nik`, 1,022 trope words), dark-toggle restyle median of 7 samples: **en/light/1280 233.2 → 15.8 ms; he/dark/800 204.3 → 12.4 ms**. The two behaviours it could plausibly break were **measured, not assumed**: **print — the Letter PDF is byte-identical before and after** (19 pages, 193,207 B en / 191,090 B he), which is what screen-only buys; **scroll — `contain-intrinsic-size`'s `auto` keyword makes each verse remember its real height, so the reading converges from the 22,761 px estimate to the true 16,646 px after one pass, and a deep `scrollIntoView({block:'center'})` lands 0 px off-centre both before and after**, leaving the karaoke auto-scroll and jump-nav unaffected. 0 zero-height verses, 0 pageerrors. **NB the S184 entry predicted ~2–4× from a 90 ms measurement; this harness measured ~15×, so quote the method with the number**

<!-- Archived at the S219 close-out: S212–S214, moved so the working ledger keeps only the last 5 sessions. -->

- [x] 2026-08-15 | (S214 close-out) | branch/deploy note | `/improveloop` → **PR #170 still open + unmerged** (`state: open, draft: true, mergeable_state: clean`, 0 reviews; the only comment is the loop's own S213 summary), so per protocol S214 **continued on `claude/improve-loop-k6dwi7` and the same PR** — no branch restart, no new PR. **Drift check: NOTHING landed outside the loop** — `origin/main` unmoved at `e435c04` (still the branch's own merge-base), `FONT_MAKER_VERSION` **5.12**, branch SW **v500** was S213's own bump. **SW v500→v501** in the final commit; precached files changed: `hebrew_dictionary.html`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `torah_trainer.html`, `Hebrew_Font_Maker.html`, `locales/en.json` + `locales/he.json`. **No FONT_MAKER_VERSION bump — deliberate:** the FM change is an `aria-label` on an existing grid header (a fix, not a feature), and CLAUDE.md bumps FM only on release features. `check-i18n.js` clean (Check A clean, Check B clean, 14 baselined findings unchanged); `build-locales.js` run twice (4,349 keys both times — 4 rows rewritten + 1 HE cell, no new keys); `update-sitemap.mjs` run **last** on a verified-unshallow clone. **Gates: NONE asked — correctly.** The two copy changes shipped this session were already gate-2 **approved at S213**, and the micro-feature track was unfundable (the pass + 4 fixes spent the whole 5-iteration budget, and a micro-feature costs 2), so gate-1's lead (**the dashboard weekly-grid print seed, now on its FOURTH offer**) rolls to S215 rather than being asked with no budget behind it. One judgement call recorded for the maintainer: the reduced-motion convergence changes *default behaviour for reduce users* on four pages, and was shipped WITHOUT a gate on the reading that it enforces an existing CLAUDE.md rule ("every animation ... must be neutralized") by adopting an in-repo block verbatim — conformance, not a design decision. Deploy `success` is verified **after merge**.

- [x] 2026-08-15 | (S214 Pass A — recurring-pattern sweep, over `b0414e5..HEAD` = 58 commits / **1,747 added lines across 13 files**, the whole S204–S213 haul plus the out-of-band panel-collapse landing and the new terms.html) | 31 signatures over the delta's added lines, then runtime grounding for every class whose truth is not in the source | **2 REAL HITS, both fixed in-pass; 9 classes clean with receipts; 1 exclusion proven rather than assumed.** **Hit 1 — `animation-outside-its-reduced-motion-block`, and far bigger than the class had ever been measured**: emulating `prefers-reduced-motion: reduce` and counting elements whose COMPUTED transition/animation duration is still non-zero found **dictionary 666, generator 393, dashboard 135, torah 92** — the four pages enumerate selectors inside their reduce block, so every declaration added since escaped it. The other 9 pages were already 0 (four carry the universal `*, *::before, *::after` block; privacy/terms have no motion at all — control 0 both ways, so those two zeros are *blind*, not clean, and are recorded as such). Fixed by adopting the in-repo universal block verbatim (ffa2639): **1,286 → 0**, controls unchanged at 147/403/97/684. **Hit 2 — `decorative-glyph-carrier-exposed-to-assistive-tech` (S213's new class) swept OUTSIDE trope_tutor for the first time**, by the definition's own test ("carrier + a sibling name", never "carrier"): `Hebrew_Font_Maker.html` carries 5 U+25CC sites; **4 are the QA-grid column builders** whose `<th title="Sheva"><span>◌ְ</span></th>` shape makes ACCNAME take the carrier and strand the name in `title` — 78 columnheaders across the four QA tabs announced as "dotted circle" (17a115e). **The 5th is NOT a hit and was excluded by evidence:** `templateCellHTML` is rasterised by html2canvas for the printed tracing sheet and never enters the accessibility tree. **CLEAN with receipts:** the consequence-critical trio (localStorage-vs-AllTools — the only storage calls in the delta are terms.html reading the two shared no-flash keys, both long registered; unescaped-input/unsafe-parse — 0 attribute sinks, 0 network sinks, and all 10 HTML sinks + 1 `Object.assign` grounded by reading them: trusted `I18n.t()` output, an in-memory rekey, `esc()`'d tokens; destructive-bulk — 1 loop, a pure key-remap); `referenced-but-unauthored i18n key` (**2,947 strict literal references, 0 unresolved**; the first regex pass "found" 24 until the S209 helper-constructed-key warning was applied — those were `I18n.t('prefix.' + slug)` fragments); `sub-floor touch target` (**every rendered `.btn-xs` measures exactly 30.0px** on dashboard + torah — the S193 floor holding); `nameless-adjacent-text-labeled control` (0 of 13 visible `<select>`s nameless across dashboard/torah/FM); `blocking-alert-for-a-routine-path` (the 3 signature hits are all *comments* asserting the rule); `untrusted-shape-on-read` (all 4 reads go through `ivritSafeParse`); falsy-zero (A2 spot check, 0); plus 8 classes with no signature in the delta at all. **Method notes below.**

- [x] 2026-08-15 | fac77c6 | locales/ui-strings.csv + locales/he.json | **The Hebrew audio labels stop gluing a prefix onto a Latin word** (gate-2 answered S213: "add a hyphen"). `trope.learn.play_example_aria` is the `aria-label` on every Trope Tutor Learn card's play button, and the parsha name inside it stays Latin **by design** (the taught pronunciation names are not translated), so the Hebrew read `מBereshit`. Hebrew attaches such a prefix to a Latin word with a hyphen. **One HE cell, 0 new keys, 0 code.** Verified on the real Learn cards in the HE UI: 20 example labels, **0** remaining Hebrew-letter-touching-Latin joins, now `השמעת דוגמת Mercha מ-Bereshit 1:1`; **control run against the pre-fix locale with the identical detector flagged 20/20**, so the zero is a measurement.

- [x] 2026-08-15 | 3afad63 | locales/ui-strings.csv + locales/{en,he}.json (torah_trainer + flash_cards + hebrew_blend_generator surfaces) | **One failure, one voice: the four `data/`-corpus load messages converge on the dictionary's plain-language model** (the S212 Pass-B finding, gate-2 answered S213: "converge on the plain-language one"). Torah named raw file paths a teacher cannot act on (*"Failed to load static data files (data/parshiyot.json, data/pockettorah/*.json)…"*), flash cards was a bare *"Failed to load dictionary words."*, and the generator carried two different registers for one failure. All four now take the shape *cause → the one action a teacher on a school network can take*, keeping each tool's own noun — e.g. *"Couldn't load the Torah readings. Check your internet connection, or ask your school's IT to allow ivritsuite.com — then reload the page."* **CSV-only: no call site moved** — the maintainer's recorded answer converged the wording and did NOT ask for the generator's second message to be removed, so the pool line and the preview banner both survive. HE recombines the already-reviewed `dictionary.loading.failed` sentence with each tool's existing noun phrase, REVIEW-marked. Verified with each corpus answered **502** and `serviceWorkers:'block'` (a live worker serves `data/` from `DATA_CACHE` and no failure path runs at all — S165): **24/24** across flash cards / generator / torah × EN+HE — real sentence, not a raw key, no horizontal overflow, 0 pageerrors; the 4th register renders in the generator's preview banner and was measured there separately (670px, no overflow either language).

- [x] 2026-08-15 | 17a115e | Hebrew_Font_Maker.html | **The QA grid's columns announce the vowel instead of "dotted circle"** — the 2nd hit of the `decorative-glyph-carrier` class and its first outside trope_tutor. `<th title="Sheva"><span class="qa-colhead">◌ְ</span></th>`: ACCNAME takes name-from-content **before** `title`, so every column's accessible name was a U+25CC carrier plus an unpronounceable combining mark, and a screen-reader user auditing a font could not tell which vowel a flagged cell belonged to. `aria-label` names the column outright; `title` stays for sighted hover; the visible glyph is untouched. Measured with **Chromium's accessibility tree, not the DOM** (S213's method note), identical probe before and after, all four QA tabs with both mark families enabled: **78 carrier-named columnheaders → 0**, now reading "Sheva", "Etnahta", "Sheva + Munach", "Dagesh + Sheva". The one unnamed header per tab is the empty corner cell above the row labels. Matrix 8/8; no new strings; **no FM version bump — a fix, not a feature**.

- [x] 2026-08-15 | ffa2639 | hebrew_dictionary.html + hebrew_blend_generator.html + classroom_dashboard.html + torah_trainer.html | **The four biggest tools honour reduced motion completely.** Each enumerated selectors inside its `@media (prefers-reduced-motion: reduce)` block, so every CSS declaration added since silently escaped it — **1,286 elements still animating under `reduce`** (dictionary 666, generator 393, dashboard 135, torah 92) against 0 on the other nine pages. Fixed by adopting, verbatim, the `*, *::before, *::after` neutralizer that `index.html`, `contact.html`, `resources.html` and `flash_cards.html` already carry, at the top of each page's own block: the enumerated rules below it out-specify it (0,1,0 beats 0,0,0) and keep their **static replacement cues**, which is the failure mode that mattered — re-asserted under `reduce` and 6/6 held (`pulse-attn` ring ×4, `tt-looping` inset ring, `tt-flash` tint). `0.001ms` rather than `0` so `animationend` still fires; the generator's shuffle cells remove their class from that event. **After: 1,286 → 0, with the reduce-off control unchanged at 147/403/97/684 so the zero is provably a measurement.** Matrix 32/32 (4 pages × EN/HE × 1280/800 × light/dark, 0 pageerrors); the shared app-toast reduce one-liner untouched.

- [x] 2026-08-15 | (S213 close-out) | branch/deploy note | `/improveloop` → **PR #170 still open + unmerged** (`state: open, draft: true, mergeable_state: clean`, 0 reviews, 0 comments across the session's own hourly check-ins), so per protocol S213 **continued on `claude/improve-loop-k6dwi7` and the same PR** — no branch restart, no new PR. **Drift check: NOTHING landed outside the loop.** `origin/main` unmoved at `e435c04` (`sw.js` v495 there, exactly where S212 left it), `FONT_MAKER_VERSION` unmoved at **5.12**, so the whole S210+S211+S212+S213 haul is still unmerged branch work — this is now the **fourth** consecutive session with no outside-loop landing, and the drift note is starting to say more about the unmerged PR than about the maintainer. **`sw.js` v499→v500** in this close-out commit; precached files changed this session: `trope_tutor.html` (skip target + 2 decorative glyph marks), `torah_trainer.html` (copy-bar prerequisite notes), `locales/en.json` + `locales/he.json` (2 new keys). **FONT_MAKER_VERSION NOT bumped — correct**: nothing in `Hebrew_Font_Maker.html` was touched. Scripts run: `build-locales` (4347→**4349** keys, the only untranslated key still the lone pre-existing `worksheet.layout.title_default`), `check-i18n` (**Check A clean + 14 baselined; Check B clean** — the terms.html haul S212 shipped is holding), `update-sitemap.mjs` last. **Decisions ASKED and ANSWERED (3, all in one batch + 1 follow-up): gate 1** → build the **torah_trainer copy-bar prerequisite notes** seed (chosen over skip and over the twice-offered dashboard weekly-grid print seed, which stays unstruck); **gate 2** → **converge the four corpus-failure registers on the dictionary's plain-language model and drop torah's raw file path** (the S212 question, now answered — queued for S214, see Candidates); **gate 2** → **add a hyphen** to the Hebrew `מ-{ref}` audio labels (new this session, queued for S214 under the 2/tool cap). **Follow-up gate 2** → the micro-feature's wording, "shorter, plainer" of three drafted options. **Deploy: NOT verified in-session** — Pages runs on merge to `main`; the maintainer merges, then confirms the run concluded `success`.

- [x] 2026-08-15 | (S213 Pass C — accessibility, one tool: **`trope_tutor.html`**) | 7 probe arms, EN+HE × light/dark × 1280/800/400/320 | **Its 2nd dedicated C (S113 → S213, ~100 sessions) and the first over everything shipped since** — the S198 print-whole-chart micro-feature, the S205 slider row, the S211 drawer contract, and the panel-collapse memory. **Target re-derived per the S196 rule** rather than inherited: the S202 C row still said "next C: the dashboard", but the newer S213 pointer named trope-tutor/generator (untouched 7 and 6 sessions), and trope-tutor is the staler of the two by last-C. **Result: a11y-strong, with counts.** 180 distinct visible controls enumerated across every progressive-disclosure surface (all 6 families' Learn cards, drawer open with all 5 panels expanded, drill live) → **0 nameless / 0 title-only / 0 placeholder-only / 0 symbol-only in EN and HE**; **0 contrast misses below WCAG AA in all four language×theme cells**; **0 elements still animating under `prefers-reduced-motion: reduce`**; **0 aria-hidden containers holding focusables**; drawer contract exact (focus lands on Close, **0/40 Tab escapes**, Escape closes, focus restored to `#gearBtn`, in both languages); focus rings 6/6 visible in both themes; tablist APG-correct incl. the RTL arrow flip; **the drill's keyboard story is exemplary** — `#qPrompt` focused on each question, focus moved to `#qNextBtn` on answer, `#qFeedback` a `role=status`, `#resHeading` correctly `tabindex="-1"`, walked 3 full questions by keyboard with 0 focus loss. **Both clean sweeps were proven capable of failing** (S212 method note 1): an injected `#c9c9c9`-on-white chip measured 1.66:1 and would have been flagged; with reduce off, 44 elements animate. **Findings: 6, two fixed in-session** (1c16fa2, f207a34). **3 probe defects, all mine, all caught by their own controls** — see the method notes.

- [x] 2026-08-15 | f207a34 | trope_tutor.html | **The Learn chart and the mastery grid stop reading "dotted circle" before every trope name.** Glyph tiles render a combining mark on a U+25CC DOTTED CIRCLE carrier because the mark cannot stand alone; visually correct, but to a screen reader the carrier is the tile's only pronounceable part, so both surfaces announce "dotted circle" immediately before the name they illustrate — 26 times over. Both are decorative duplicates (the Learn card prints the name beside the glyph; each mastery cell carries the name in its `title` AND its `.m-name`), and the page already applies exactly this reasoning one step along — the drill's symbol choices carry `aria-label = tropeName(kt)` with a comment saying glyph shapes mean nothing to a screen reader — so this extends a decision the file had already made. Marked via a one-line `_decorative()` helper **at the call sites, not inside `tropeGlyphSpan`**: whether a glyph duplicates a name already on screen depends on where it sits. **The drill QUESTION glyph is deliberately left exposed** — there it is the question, and naming it would give away the answer. | **verified against Chromium's OWN accessibility tree, not by inspection**: nodes carrying the dotted circle **10 → 0**, nodes carrying a trope name unchanged at **36**; control census unchanged (180 controls / 0 nameless, EN+HE); matrix 8/8. The drill-question control's FIRST run was a vacuous pass — it reported "still exposed" on an Identify question, whose stem renders a word and has no tile at all; re-run against Hear/Melody it is a real check.

- [x] 2026-08-15 | 5f613c2 | torah_trainer.html + locales/ui-strings.csv + locales/{en,he}.json | **MICRO-FEATURE (gate-1 chosen at the S213 batch, on the seed's FIRST offer since S180 Pass H logged it): the copy bar says why a switched-on option produced nothing.** `verseCopyParts` silently omits a line it cannot build, so with all three format toggles lit a teacher could paste Hebrew-only text with nothing on the page explaining it. Two independent causes — the transliteration ESM never loaded (blocked school network / offline), and the reading's English never fetched — and while the reading header told half the story for transliteration via its `tt-notice-chip`, the translation case had **no notice anywhere**. Adds `#ttCopyPrereqNote`, populated by `_syncCopyPrereqNote` from the existing `updateCopyTextarea` chokepoint (every selection path AND `applyI18n` already funnel through it, so the note follows both the selection and the UI language with **no second listener**). A cause is claimed only when decidable from the current selection: `.every()` over the selected verses, the **same idiom and short-circuit `renderText` uses for `_translitDead`**, so the two surfaces can never disagree. `translitlibloaded` now also refreshes the copy bar, which incidentally fixes a latent staleness — a late library arrival left the preview without the transliteration it had just gained. **39 lines in one tool** (44 insertions total), **2 new CSV keys, ZERO new storage keys** — both toggles it reads are already persisted, so there is no AllTools or `.ivrit` wiring to add. Gate-2 wording approved as "shorter, plainer" of three drafted options; HE drafted + REVIEW-marked. | **17/17 truth-table checks**: both causes independently and in both directions (library stubbed ready + English present → note hidden AND both lines reaching the preview), toggles off, nothing selected (note steps aside for Copy All's own reason), Hebrew UI, live EN↔HE switch. **Live-region discipline proven with a SYNCHRONOUS `takeRecords()` count** — 3 no-op re-renders rewrite the note 0 times. Fresh profile (empty localStorage + IndexedDB): note present, hidden, silent, 0 pageerrors. Matrix 8/8; check-i18n A+B clean.

- [x] 2026-08-15 | 1c16fa2 | trope_tutor.html | **"Skip to the tutor" now actually moves focus.** `<main id="tuMain">` is not focusable by default, so activating the page's FIRST tab stop moved the scroll position but left `document.activeElement` on `<body>`; Chromium's sequential-focus fallback papered over it for plain Tab users, but a screen reader's cursor did not move at all — the link silently did nothing for the audience it exists for. **`torah_trainer.html`, the page this shell was copied from, already carries the house fix** (`<div id="ttReading" tabindex="-1">`); trope_tutor lost the attribute in the copy. One attribute, no copy, no CSS, no i18n. | **verified by differential against the twin**: before, torah `focusMoved=true` / trope `focusMoved=false`; after, both report `activeElement` = the skip target. Next Tab stop unchanged ("Torah Trainer →") so tab order is untouched; the UA focus ring the target now shows was confirmed **byte-identical to torah's long-standing behaviour** in both themes (checked before claiming it was not a regression). Matrix 8/8.

- [x] 2026-08-15 | (S212 close-out) | branch/deploy note | `/improveloop` → **PR #170 still open + unmerged**, so per protocol S212 **continued on `claude/improve-loop-k6dwi7` / PR #170** (no branch restart, no new PR) — that PR now carries S210 + S211 + the user-directed drawer work + this session. **Drift check: NOTHING landed outside the loop** — `origin/main` is unmoved at `e435c04` (the merge-base), `sw.js` was v498 and `FONT_MAKER_VERSION` 5.12, both exactly where the S211 close-out plus the 2026-08-15 out-of-band drawer commit left them; the whole delta B had to cover was loop work + that user-directed landing. **`sw.js` v498 → v499** (precached files changed: `flash_cards.html`, `locales/en.json`, `locales/he.json`). **FONT_MAKER_VERSION NOT bumped** — the Font Maker was not touched this session at all (it was the one tool Pass B cleared without a fix, and the pointer had asked for it to rest). Scripts run: `check-i18n.js` (**Check A clean, Check B now FULLY clean — 0 untranslated keys in any language column, a first**), `build-locales.js` (4347 keys ×2 after the 30 terms cells), `update-sitemap.mjs` (last step, non-shallow clone confirmed). **Gates: BOTH asked and BOTH answered** (see the metrics line). **Decisions deferred to maintainer: none this session** — the two-session-old terms question was finally answered and shipped. **Deploy `success` is verified after merge, not in-session.**

- [x] 2026-08-15 | (S212 Pass B — console & error audit, 18th run) | all 13 pages × EN/HE + 7 receipted tool interactions + 2 forced-failure arms | **Every mechanical arm clean, so — exactly as S199 predicted — the run's whole value came from the forced-failure arms.** Arm 1 (**26 loads**, `serviceWorkers:'block'`, local HTTP): **0 pageerrors, 0 same-origin failed requests, 0 unhandled rejections, 0 raw i18n keys, correct `dir` on all 26**; the only console.error is torah's Sefaria fetch under the harness's own route-abort, grounded to a graceful handler (`console.error(e)` + an i18n'd `reading_load_failed` + net hint). Arm 2 (**7/7 receipted interactions**) covered the S210+S211 delta deliberately — torah's two hot-path early-returns and `@media screen` restyle (driven by **stubbing the Sefaria response** so the real render pipeline runs offline: 21 words, trope+nikkud colour, dark, word click, HE), the dashboard's render-time luminance branch (1 `ink-dark` plate), FM, and the out-of-band panel-memory block on all 6 carriers. Arm 3 (**NEW — 56 corrupt-storage boots**: 7 pages × 8 hostile shapes incl. invalid JSON, arrays, scalars and `panelsCollapsed` of the wrong type): **56/56 survived**, every page still rendered, `panelMemSave()` still wrote, `Object.prototype` stayed clean — **with a control run proving the probe fires** (replacing the generator's `ivritSafeParse`+guard with a bare `JSON.parse` made exactly the malformed-JSON shape throw, 1/8, and the other 7 correctly still passed). Arm 4 (**corpus-fetch failure across 6 tools**) produced the session's finding. **4 self-caught probe defects** (see method notes) — 2 wrong function names, 1 wrong-typed codepoint, 1 English-only regex.

- [x] 2026-08-15 | 7191185 | locales/ui-strings.csv + locales/{en,he}.json | **The Hebrew UI stops rendering the whole Terms of Use page as right-aligned English** (the S210 Pass I P3 — the gate-2 question that had been open TWO sessions; **asked and answered this session: translate, don't pin LTR**). All **30** empty `terms.*` `he` cells authored and every one **REVIEW-marked**, with specific notes where a real judgment call was made: the ALL-CAPS emphasis in warranties/liability has no Hebrew equivalent (Hebrew has no case), and the paired headings lean on `כתב ויתור על אחריות` (warranties) vs `הגבלת חבות` (liability). Third-party NAMES and licence identifiers stay English per the content-vs-UI boundary; only connecting prose is translated. The `<title>` stays English — it is a plain literal, not a key, so the SEO boundary is untouched. **Verified: markup integrity exact across the 6 markup-bearing rows** (counts of `<a href>`, `</a>`, `<code>` and the literal `\n` separators all match EN; **zero URL drift**), rendered HE keeps **all 29 links, 0 dead hrefs, no leaked markup**, `dir=rtl`, 0 raw keys, EN byte-unchanged; matrix 16/16 (terms + privacy). Line-targeted CSV edit — **exactly 30 lines changed, 0 collateral** (a full csv rewrite would have reformatted one unrelated pre-quoted row, checked first). **This closes the suite's last translation debt: Check B is now fully clean.**

- [x] 2026-08-15 | 80695d9 | flash_cards.html | **A failed word-list load stops claiming it is still loading** — the **2nd ever hit** of the ACTIVE `error-status-clobbered-by-a-later-routine-write` pattern, found by doing exactly what S199's sweep log said to do next ("widen to non-init failure paths: a mid-session fetch failure followed by a routine re-render"). When `data/hebrew_words.json` fails, the catch wrote the message straight into the caption element; **the next routine re-render overwrote it** — a length tweak, a word-source change, or simply switching the UI language — leaving `alldict` users a permanent **"Loading dictionary…"** for a load that had already failed, and `mylists` users a false **"0 words in pool"**. A Hebrew-UI teacher could never see the real message at all, since `applyI18n` re-renders the caption on every switch. Fix adds the same distinction the **generator already draws with `_realWordsFailed`** (attempted-and-failed ≠ not-attempted-yet) and routes the failure through the single caption chokepoint instead of writing the element — which **also stops showing a dictionary error to My Word Lists users**, whose pool never touches `ALL_WORDS_DATA`. Verified with the corpus aborted: notice survives a filter re-render **and** a language switch in EN and HE; mylists reports its own honest count; happy path still loads 13072 words; matrix 8/8.

### Archived from the working ledger at the S223 close-out (S218 entries)

- [x] 2026-08-15 | (S218 close-out) | branch/deploy note | **PR #172 was still open and unmerged, so S218 continued on `claude/seo-improveloop-review-d25rtt` / PR #172** — no branch restart, no new PR, exactly as the S217 pointer directed. **Drift check clean:** `origin/main` unmoved at `bfc9000`, `sw.js` v504, `FONT_MAKER_VERSION` 5.12 — nothing landed outside the loop since S217. `sw.js` **v504 → v505**, one bump covering the three precached files this session edited (`hebrew_dictionary.html`, `Hebrew_Font_Maker.html`, `torah_trainer.html`). **No `FONT_MAKER_VERSION` bump** — `b229508` is a one-token wiring fix, not a feature, which CLAUDE.md exempts. Scripts: `check-i18n` clean (no new violations, 14 baselined) after every commit; `build-locales` NOT run — **this session authored zero CSV keys**; `update-sitemap` run last. **Budget: full 5** (1 pass + 3 fixes + 1 attempted-and-reverted). **Gates: 2 asked, 2 answered, 0 deferred** — gate 1 (owed from S217's interrupt): *skip the micro-feature, spend all four remaining iterations on the Pass K fixes*; gate 2 (flash-cards colour labels): *leave them in English*, recorded below as a CLOSED question. **Three check-ins fired during this session**; the first two found unpushed commits and pushed them, the third found the branch in sync.

- [x] 2026-08-15 | (S218 Pass K — i18n / localization audit, **14th run**) | 12 pages × Hebrew UI | **Both gates clean for the 7th consecutive K** (Check A clean + 14 baselined; Check B clean, no CSV debt), so the value was again entirely past the gates. **Ran the two-arm runtime reverse-lookup detector exactly as the S207 entry prescribed** — S207 found its hits by accident, because its visibility filter was parent-only and therefore ancestor-blind, and it ruled that the accident was worth keeping and that the next K should run both arms deliberately. Arm V (ancestor-aware `checkVisibility`) and arm H (present in the DOM but not visible), same detector split by one boolean: **103 raw hits (59 visible / 44 hidden) across 12 pages, 0 pageerrors → 3 real findings.** Reverse map built from 3,516 distinct English strings over 4,286 translatable keys. **The dismissals are all documented coincidental classes** and are re-confirmed here so a future K does not re-chase them: font-family **"Alef"** ×6 (every hit sits in a `.font-opt`/`#fontOptions`/`#templateFont` picker), preset name **"Default"**, curriculum brand **"TaL AM"** (element is wired, `i18nAttr=true`), paper size **"Letter"** (wired), and the ~30 **trope pronunciation names** in trope_tutor, which CLAUDE.md pins as never-translate. **The three real findings are the three fixes below** (two shipped, one closed by gate-2 as a deliberate non-defect). **The prescribed reveal probe was run and its first verdicts were UNEARNED** — see method note 1; both surviving findings rest on other evidence, not on it.

- [x] 2026-08-15 | ce75604 | hebrew_dictionary.html | **The dictionary's part-of-speech badges speak Hebrew, like the era badge beside them.** Pass K's headline: **52 visible hits**, every one `${w.pos}` rendered raw on a word card — while **one line above**, the era badge maps its corpus enum through `I18n.t()`. Adjacent badges, same card, same shape of data, one translated and one not. **Zero new copy:** the Part-of-Speech *filter* panel already maps every corpus value to an authored key (`value="proper noun"` → `data-i18n="dictionary.pos.proper_noun"`), so `posLabel()` reads the mapping out of that markup rather than duplicating it — a badge cannot disagree with the filter that produced the result, and adding a filter row wires its badge for free. Corpus enum measured, not assumed: **23 distinct values over 13,081 entries, none empty**; 19 have keys, the other 4 (proverb 19 entries, definite article 2, adjective/noun 1, adverbial phrase 1) fall back to the raw string exactly as the era badge falls back on an unknown era. **English output is unchanged** — `.badge` is `text-transform:uppercase`, so "noun" and "Noun" both paint NOUN. Both carriers fixed (word card + word-detail modal). **32/32** across he/en × light/dark × 1280/800 incl. a live language switch, the tail fallback and an unknown-value passthrough; **stashed control reproduced 99/100 Latin badges pre-fix vs 1 after.**

- [x] 2026-08-15 | b229508 | Hebrew_Font_Maker.html | **Composed-form tiles translate their glyph names like their three siblings.** `renderFormPanel`'s `.lt-name` was the one site in the file rendering a raw English name; the other three `.lt-name` sites already wrap theirs in `gName()`. Same "sibling translated in-range while this one stayed English" shape S177 and S211 recorded. **One token**, covering all three panels that share the builder (Specialized letters, Yiddish, Ladino). **Zero new copy:** the 10 names with authored keys now resolve; the 13 Yiddish/Ladino names with no key keep their English through `gName`'s existing fallback. **The detector UNDER-measured this one** — it reported only Shin dot / Sin dot, because Yiddish and Specialized render an "enable this tab" hint rather than tiles in a fresh profile; driving the real `setAdd*` setters exposed the other 8. **17/17** across en/he × light/dark × 1280/800, revealing each category through `setCatTab()` and reading only tiles that pass `checkVisibility`, so the result cannot come from hidden DOM; **stashed control reproduced 15/25 tiles resolved pre-fix vs 25/25 after.**

- [x] 2026-08-15 | c49b075 | torah_trainer.html | **The Torah Trainer's header buttons take the suite's 30px touch floor.** From the S217 Pass-H phone arm: ❓ Tour measured **66×29** at 390px, one pixel under. Fixed on the **shared `.hbtn` class rather than the instance**, exactly as the candidate advised — the `≤720px` block trims `.hbtn` padding to `6px 9px`, and that trim is what drops it under; `min-height:30px` on the base rule covers every current sibling and any future one, and `.hbtn` is already `inline-flex`/`align-items:center` so nothing shifts. **Measured all 5 `.hbtn` users across 7 cells** (1280/800/390/320 × light/dark × en/he) rather than the single instance the walkthrough caught: only `#tourBtn` was under, only at ≤720px, only on height. **14/14** after; the same probe scored **11/14** before, failing in exactly the three ≤720px cells, and every sibling's dimensions are byte-identical pre and post — so the class-level change is a *verified* no-op for them.

- [x] 2026-08-15 | (S218 iteration 5 — **ATTEMPTED, REVERTED**) | torah_trainer.html | **The `.hright` half of the reflow candidate resisted a one-tool slice, and the attempt bought sharp intelligence instead.** Tried to close torah's remaining 320px overflow with `header{flex-wrap:wrap}` + `.hright{flex-wrap:wrap;justify-content:flex-end}`. **Two failures, both real:** (1) the overflow only fell **451 → 396**, still over — because **`flex-wrap:wrap` on `.hright` is INERT while `flex-shrink:0` stands**: the container never shrinks below its 380px max-content, so its children never run out of room and never wrap. The entire 451→396 gain came from the *header* wrapping `.hright` onto its own line, not from `.hright` wrapping at all. (2) `header{flex-wrap:wrap}` **unscoped REGRESSED 800px** — the subtitle is hidden only at ≤720px, so at 800 the header content exceeds the width and wrap engages, growing the header **77 → 112px**; pre-fix, `.htitle{flex:1;min-width:0}` absorbed that squeeze by truncating instead. Reverted cleanly (`git checkout --`), and `c49b075` re-verified 14/14 afterwards. **What the next attempt must do:** scope any header wrap to ≤720px, AND establish what `flex-shrink:0` on `.hright` is protecting before touching it — that flag, not the wrap, is the actual blocker, and it is the cross-tool half. | verified: measured at 320/360/390 with `elementsFromPoint` + an overflow census before and after

## S219 (archived at the S224 close-out — Done keeps the last 5 sessions)

- [x] 2026-08-15 | (S219 close-out) | branch/deploy note | **PR #172 was still open and unmerged, so S219 continued on `claude/seo-improveloop-review-d25rtt` / PR #172** — no branch restart, no new PR, per the branch protocol. **Drift check: NOTHING landed outside the loop** — `origin/main` still at `bfc9000` (unmoved across S217, S218 and S219), `sw.js` at the v505 S218 left, `FONT_MAKER_VERSION` at 5.12. The branch now carries **14 commits** (S217 ×4, S218 ×4, S219 ×4 counting this ledger entry). **SW v505→v506**, bumped once in this final commit, covering the two precached files S219 edited: `flash_cards.html` (4bfdd55, 2cb4cd2) and `Hebrew_Font_Maker.html` (c2399f1). **No FONT_MAKER_VERSION bump** — `c2399f1` is an accessibility fix, not a feature, and the file's own rule bumps only on release features. **Scripts run:** `check-i18n` clean (Check A clean + the standing 14 baselined; Check B clean, no debt) — **zero CSV keys were authored this session, so `build-locales` was not needed and the maintainer's REVIEW queue is unchanged at 49**; `update-llms-txt --check` current on both files (no title/description/JSON-LD touched); `update-sitemap.mjs` run last on a verified NON-shallow clone. **MAINTAINER-DIRECTED MERGE:** at the end of this session the maintainer instructed "Push to main", so PR #172 was marked ready and merged rather than left open — the **2nd ledgered direct-to-main authorization** (precedent S78). It landed all three sessions on the branch (15 commits, `sw.js` v503→v506) and the Pages deploy was verified `success` after the merge. The standing branch-and-draft-PR protocol is unchanged for S220, which must cut a fresh branch off `main`. **Decisions deferred to maintainer: NONE — no gate was reached.** Gate 1 never arose: a micro-feature costs 2 of 5 iterations and the budget went to 1 pass + 3 fixes + 1 cross-page sweep, and the Feature-seeds section still has no fresh top pick (S217's H produced kills, S219's G produced defects). Gate 2 never arose: **no user-facing copy, title, description or JSON-LD was changed** — all four commits are CSS, a pool assignment, and one ARIA attribute. The two standing gate-2 splits (torah deferred-fetch flow P3, font-panel/size-slider cross-reference P4) and the new dictionary part-of-speech tail P4 remain queued for the next attended batch.

- [x] 2026-08-15 | (S219 Pass G — print & export fidelity, `flash_cards.html`) | 3 printable artifacts × Letter/A4 × light/dark × EN/HE | **The tool has grown from ONE printable artifact to three since its only prior dedicated G (S159), and the two new ones had never been print-audited.** Target re-derived per the S196 rule rather than taken from S208's "next: dictionary" note: the S218 pointer's variety note named flash-cards explicitly, and flash_cards has had **one** dedicated G ever against the dictionary's three. **Arm 1 (chrome / ink / contrast / overflow), 12 cells: 0 findings, and the zero is EARNED** — the same detector under `screen` media reports 42 visible chrome elements and 23 dark fills, so the print block genuinely neutralises all of it. **Arm 2 (silent truncation inside `.fcs-cell`'s `height:2.6in; overflow:hidden`), 7 card modes × EN/HE: 0 real hits** — the 24 `.fcs-emoji` spans reporting `dh=3` are `overflow:visible` on a span whose parent cell does not clip, so nothing is lost. **Arm 3 (pagination): 1 P2-shaped hit, fixed as `4bfdd55`.** **Arm 4 (the modes themselves): 1 hit, fixed as `2cb4cd2`.** **Export arm:** the `.ivrit` gather/apply shape and the AllTools registration were re-read and are complete. **2 candidates logged** (the certificate's hardcoded `@page`, and the deliberate blank-sheet measurement). **Two method traps caught in-pass, both the S218 "verify the manipulation landed" rule wearing new hats:** (1) `page.pdf()` fires `afterprint`, and all three teardowns wipe their host on it — the first run's 12 "clean" A4 sheets were simply EMPTY (1 KB each) because the artifact had been torn down by the preceding Letter render; re-fire before every `pdf()` call. (2) `page.pdf()` defaults to **zero margins**, which is how nobody prints — the pagination arm read 16/16 clean at margin 0 and 16/16 FAIL at the 1in margin the code's own comment budgets for. **A print measurement taken at margin 0 is not a print measurement.**

- [x] 2026-08-15 | (S219 pattern sweep — `print-trailing-dead-space`, 2nd sweep) | all 12 root pages | **The coverage gap this pattern has carried since S182 is CLOSED: 11 unswept pages are now swept.** Detector generalised beyond S182's container-bottom delta, because `4bfdd55` was *leading* dead space (an emptied wrapper above the flow), which a bottom-edge comparison cannot see: under print media, report any VISIBLE element ≥40px tall containing no visible text and no img/svg/canvas/hr anywhere beneath it — i.e. empty box on paper — outermost-first so a chain is not listed twice. **Result: 1 real hit (flash_cards, fixed as `4bfdd55`), 11 pages clean.** Three flags triaged as non-defects with reasons: `flash_cards` un-moded (the deliberately non-printable setup screen — logged as a candidate so it is not re-chased), `Hebrew_Font_Maker` (an 852px `.ws-split` — the glyph-editing canvas, never a paper surface; FM's G history is export paths, not print), `contact.html` (a 130px empty `<textarea>` — inherent to printing any form). `classroom_dashboard` showed two 45px boxes, below the 60px reporting bar. Streak reset to 0 by the hit; stays ACTIVE.

- [x] 2026-08-16 | 40b56a3 | flash_cards.html | **The completion certificate stops forcing Letter paper** (queued from S219 Pass G). `_certEnter` injected `@page { size: letter landscape }`, overriding the teacher's chosen paper; on A4 the dialog scaled a Letter sheet down, so the failure was a silently smaller certificate. Took the minimal shape — **drop the paper keyword, keep the orientation** — rather than adding a Letter/A4 setting to a tool that has none; the injection/removal pair stays (an `@page` rule can't be scoped by a body class). **Verified** with a genuinely ladder-complete profile driven through the real `_certEnter`, light/dark × 1280/800, 0 pageerrors, re-entering cert mode before every `pdf()` call (S219's `afterprint` teardown note): Letter → 11.00×8.50in, A4 → 11.71×8.28in, both landscape. **Stashed control reproduces S219's exact finding** — A4 came back 11.00×8.50, byte-identical to the Letter request. **⚑ Method note that nearly cost the fix: `page.pdf({format})` OVERRIDES the page's own `@page size` unless `preferCSSPageSize:true` is passed.** The first probe run showed defect and fix measuring identically and briefly looked like evidence S219 had been wrong; S219's number was right and the probe was wrong.

- [x] 2026-08-16 | (S220 close-out) | branch/deploy note | **PR #172 was MERGED at the S219 close-out, so S220 cut a FRESH branch off the latest `origin/main` and opens a NEW draft PR** — exactly as the S219 pointer directed; no commits were stacked on already-merged history. Branch `claude/improve-loop-2y96fr`, cut at `670df72` (the S217–S219 merge commit). **Drift check: NOTHING landed outside the loop** — `origin/main` moved `bfc9000` → `670df72`, but every commit in that range is the loop's own merged S217–S219 work (`git log origin/main --not b0bd728` returns only the merge and its ledger commit); `sw.js` at v506 and `FONT_MAKER_VERSION` at 5.12, both matching what S219 left. **`sw.js` v506 → v507**, one bump in the final commit covering the two precached HTML pages (`torah_trainer.html`, `Hebrew_Font_Maker.html`) plus `locales/en.json` + `locales/he.json` (both in `CORE_ASSETS`). **NO `FONT_MAKER_VERSION` bump** — the one Font Maker change is a localisation fix, not a feature, and CLAUDE.md's rule is that pure fixes/token swaps do not bump. **Scripts run:** `check-i18n.js` clean at every commit (Check A clean + 14 baselined, Check B clean); `build-locales.js` twice (7 new keys → 4,361); `update-sitemap.mjs` as the last step. **`update-llms-txt.mjs` deliberately NOT run** — no page `<title>`, `<meta name="description">` or JSON-LD block changed this session, and both files are generated. **Gates: 1 asked, 1 answered, 0 deferred** — gate 1 (owed since S217's interrupt) was answered *build*, choosing the trope-legend seed from three offered; no gate-2 work arose (the 7 new CSV keys are all newly authored tooltip/legend copy for controls that previously had none, offered and approved as part of the gate-1 seed, and the one Font Maker string was localised verbatim rather than rewritten). **Deploy verification is the maintainer's after merge.**

- [x] 2026-08-16 | (S220 Pass D — performance snapshot, `hebrew_blend_generator.html`) | the stalest D target by ~92 sessions (last dedicated D = **S128**, well before the chunked worksheet build landed) | **Result: essentially CLEAN — one throttled-only measurement, and TWO KILLS that were both my probe's fault rather than the product's.** Target re-derived per the S196 rule rather than taken from S209's row, and the re-derivation agreed with the S219 pointer's variety read (generator was both the stalest-touched tool and the one carrying the heaviest known main-thread work). **Cold load:** 1× → DCL 396 ms, FCP 212 ms, **0 blocks > 200 ms** (top longtask 154 ms); 4× → DCL 686 ms, top block **247 ms**, the same informational boot-chunk class S209 logged at 446 ms. **Interactions, all clean at 4×:** generate default, all-letters/all-vowels, letters All/None, vowels All, both vowel schemes, dark toggle over a live worksheet (max 150 ms). **The chunked class-set build is a POSITIVE RECEIPT** — 40 versions / 40 sheets / 7,000 elements / 452 KB of HTML with **zero longtasks ≥ 50 ms at 4×**; the mechanism does exactly what it was built for. **Corpus:** the 2.9 MB `hebrew_words.json` parses + flatMaps to 13,081 word objects in **65 ms** at 1×, no longtask. **All 12 real-word drills** generated at 4×: 8 clean, 3 with sub-100 ms blocks, 1 finding (bingo, logged as a candidate). **Leak arm CLEAN with GC forced** (`HeapProfiler.collectGarbage` before each snapshot, worksheet emptied at each measurement so the comparison is like-for-like): baseline → 40 generates → 80 generates gave nodes 5,495 / 5,453 / 5,453, listeners 866 / 873 / 873, heap 1.8 / 2.8 / 2.7 MB — flat. **Kill 1: "crossword renders an empty worksheet" (0 sheets, 2 elements) was a 1,200 ms settle window; crossword needs ~2.5 s to paint.** **Kill 2: "crossword is the slowest drill" — `generateCrossword` itself runs 3–8 ms at 1× and 10–27 ms at 4×, over 5 runs each; the wall time was my `waitForTimeout`, not the generator.**

- [x] 2026-08-16 | 3727d93 | Hebrew_Font_Maker.html (+ 1 CSV row, rebuilt en/he.json) | **The Trace-mode stage legend speaks Hebrew, like its three siblings — the fourth session running for this exact shape** (S177, S211, S218). `renderLegend`'s align, anchors and wideform branches all route through `_pT()`; the **trace** branch — its `else` fallback, and the mode a teacher sits in for the entire tracing step — interpolated a raw English sentence straight into `innerHTML`. Inside a template literal, i.e. **exactly check-i18n's documented Check-A blind spot**, so the gate was green over it the whole time. One token + one key (EN + REVIEW-marked HE draft reusing `trace_hint`'s own established terms, סף / מעקב); the English rendered text is unchanged, `&amp;` entity and all. **Found by grounding a stale candidate** — see the S209 anchors-banner line, now closed as already-fixed. | verified: 4 cells (EN/HE × light/dark × 1400/800 px), **with the work mode asserted to have actually landed in every cell** — `nodes` is refused without a traced letter and silently bounces back to `trace`, which is how the string surfaced at all — plus two live EN↔HE switches re-rendering the trace legend in place; 0 pageerrors throughout

- [x] 2026-08-16 | a2fe1ca | torah_trainer.html (+ 6 CSV rows, rebuilt en/he.json) | **MICRO-FEATURE — the trope legend explains itself** (gate-1 approved; the first gate-1 answer since S217's interrupt, and the seed was offered against two alternatives). A teacher who switched trope colouring on got six chips reading "Zakef Katon clause" with **no glyph and no explanation** — enough to label the colours, not enough to teach them. Each chip now carries the family's representative mark on its alef carrier (already in `TROPE_COLOR_DEFS.example`, so **no new data**) plus a tooltip naming what is actually in that family, drawn from the same taxonomy the Trope Tutor teaches. **The chip itself is the trigger**, so the legend gains no extra sub-30px targets — `bindTip` supplies the page's standard tap/keyboard contract, and **`aria-label` is set BEFORE binding** because `bindTip` only supplies its generic "More information" fallback when one is absent, and that fallback would otherwise have displaced the family name as the chip's accessible name. Chips take the ratified 30px floor + a focus ring; the glyph is `lang="he"` at the chokepoint. **No new state → no `getSettings()`/AllTools/`.ivrit` wiring** (checked, not assumed). 49 insertions, a third of the ~150-line fence. | verified: fresh contexts × light/dark × 1280/800 px × EN/HE — glyphs render, tooltips localise, **all 6 chips measure exactly 30px**, no horizontal overflow, 0 pageerrors; focus shows the tip and Enter toggles it off (the page's existing `bindTip` contract, re-probed after the first run's "Enter does nothing" turned out to be focus having already opened it); **three live EN↔HE switches rebuild and re-bind exactly 6 chips with no duplication**; the legend stays print-hidden and the per-sheet print band still builds its own 6 chips with no glyph leak

- [x] 2026-08-16 | 26b9e7e | torah_trainer.html | **Trope colour coding toggles without rebuilding the reading** — the S184 Pass-D candidate, shipped exactly as that entry prescribed. `tokenizeHebrew` guarded emission of the `trope-<fam>` classes on `settings.colorCodeTrope`, so the master toggle took a full `renderText()` — even though **every** trope rule is already gated on `body.trope-on` (and on `.trope-underline-fallback`, which `applyTropeColors` only ever sets alongside it), so the classes paint nothing while colouring is off. The live reading now emits them unconditionally and the handler calls `applyTropeColors()` instead of `renderText()`, making the toggle a pure body-class + CSS-var swap — **the same model the S60 nikkud layer already uses on this page**. **The COPY path keeps its `opts.colorCodeTrope` gating**, because `_inlineCopyStyles` reads these classes off the detached markup to resolve each word's colour; without that split a teacher with colouring off would have started getting coloured pastes. | verified: **baseline-vs-fixed differential at 4× over a 146-verse / 1,533-word reading, medians of 5** — handler JS **50 ms → 0–1 ms**, mutation records in `#ttReading` **3 → 0**, longtasks **50–55 ms on 3 of 5 runs → none**; rendered markup with colouring ON and BOTH copy payloads (on and off) **byte-identical** to the pre-fix build via a stashed-baseline diff; `data-twi` karaoke indices unchanged; the nikkud-Highlight collision fallback verified flipping background→underline→background; legend sync and 3× empty-state toggling clean across light/dark × 1280/800 px

- [x] 2026-08-15 | 4bfdd55 | flash_cards.html | **The card sheet stops printing its last row on a sheet of its own.** `print-trailing-dead-space`, 2nd instance (registered S182, first swept on torah only). `body.fc-sheet-mode` hid `.screen`, `.app-header` and `footer` — but **not `main.wrap`**, whose `padding:14px 14px 100px` survives its hidden children as **114px (1.19in) of empty box above the grid**. 7.8in of grid + 0.17in of head fits Letter at any sane margin; +1.19in needs 9.16in, which overflows Letter's 9in printable height at 1in margins — **exactly the budget this block's own comment assumes** ("clears the printable height of BOTH Letter (9in) and A4 (10.2in)"). The bottom row then printed alone on a second sheet, and in duplex that split a front grid from its mirrored back, **losing the two-sided alignment `fcsMirrorRows` exists to provide**. The fix is not a new mechanism: `fc-report-print` and `fc-cert-print` — both NEWER than the card sheet — already hide `.wrap` and zero `body { min-height }`, the report with a comment recording the identical discovery. **Verified: 31/31, control 11/31.** Largest margin still fitting one grid per sheet, Letter **0.75in → 1.25in** (the sweep's ceiling); duplex re-paired at ≥1in; A4 never hit it (taller paper) and is unchanged; report + certificate re-asserted at 1 sheet; screen matrix (1280/800 × light/dark × EN/HE) untouched — the diff is entirely inside `@media print`.

- [x] 2026-08-15 | 2cb4cd2 | flash_cards.html | **Words mode can print a card sheet without practising first.** `generateCard`'s words branch reads the module-level `WORDS_POOL`, and **`startPractice()` was its only writer** — while `printCardSheet()` calls `generateDeck()` directly. So a teacher who chose Words and went straight to the card sheet got an empty deck and the "no cards" toast **every time from a cold setup screen, under both sources**; the artifact was reachable only if a practice run happened to have filled the pool earlier in the same page load, which is why it presents as intermittent rather than broken. Resolved at **`buildDeckContext()`**, the shared deck-building chokepoint both callers already go through and which the file documents as the home for pools invariant across a deck loop — the emoji pool sits one line above. Words uses the module global rather than a `ctx` key **deliberately**: the need here is correctness, not the per-draw speed the emoji pool is threaded through `ctx` for, and `generateCard`'s contract is unchanged. `startPractice` keeps its own assignment because it needs the pool BEFORE `generateDeck` to validate and explain an empty deck. **Verified: 58/58, control 0 cells in all 8 Words cells.** Both sources × EN/HE × light/dark now print 36 duplex cells (18 Hebrew fronts, 18 answers), nothing clipped; practice still builds and prints; letters/blends/numbers/colors/emoji each still print a full 9-cell grid. **Method note:** the pass's first read of this was a FALSE zero in the other direction — setting `otherSubMode` by hand skips `syncOtherSubUI`, so the probe had to drive the real pills to see the true behaviour.

- [x] 2026-08-16 | (S221 close-out) | branch/deploy note | **PR #173 was still OPEN and UNMERGED, so S221 CONTINUED on the same branch `claude/improve-loop-2y96fr` and the same PR — no restart, no second PR**, exactly as the S220 pointer directed. **Drift check clean:** `origin/main` still at `670df72` (unmoved since S220 cut the branch), `sw.js` v507, `FONT_MAKER_VERSION` 5.12 — nothing landed outside the loop, as expected while the PR sits unmerged. **`sw.js` v507→v508**, one bump in the final commit; precached files changed: `index.html`, `flash_cards.html`, `classroom_dashboard.html`. **No `FONT_MAKER_VERSION` bump** — S221 shipped nothing in `Hebrew_Font_Maker.html`; the one FM-adjacent change is a key added to index.html's erase list, which is not an FM feature. **Scripts:** `check-i18n` clean at every commit (Check A clean + 14 baselined, Check B clean); **`build-locales` NOT run — zero CSV keys added this session** (the first such session since S215; the three changes are a storage-list entry, a CSS keyword, and a colour-assignment rule, none of which is user-facing copy); `update-sitemap.mjs` run last on a non-shallow clone; `update-llms-txt.mjs --check` clean (no title/description/JSON-LD touched). **Gates: 1 asked, 1 answered, 0 deferred** — gate 1 offered three freshest-tool seeds and the maintainer chose the dashboard auto-colour seed; **no gate-2 item arose, because all three changes are deliberately copy-free**. **Deploy verification is the maintainer's after merge.**

- [x] 2026-08-16 | (S221 Pass I — first-load & empty-state, all 13 pages) | **18th run; mechanical gates clean for the SIXTH consecutive I, so the budget went past them as the S220 pointer directed** | **Arm 1 (gates, kept deliberately cheap): 26/26** — 13 pages × EN/HE on genuinely fresh contexts (`serviceWorkers:'block'`, local HTTP), 0 pageerrors, 0 failed same-origin requests, 0 raw-key leakage, correct `lang`/`dir` on every run; torah's 2 console errors are the documented environmental Sefaria route-abort. **Arm 2 (NEW — boot-write census):** wrap `Storage.prototype.setItem`/`removeItem` + `indexedDB.open` in an init script and record what each page writes **before the user touches anything**. 13 pages wrote 14 distinct keys at boot (dashboard the most, 4 — its `DEFAULT_PRESET` seeding); the 5 chrome pages write nothing; **every boot-written key was already reconciled**, so arm 2 was a clean receipt rather than a finding. **Arm 3 (NEW — the erase↔fresh diff, where the value was):** a static census of every write site (51 keys, literal *and* variable-keyed — the 6 folder trees, 3 `*_panels`, and the FM constants only appear via variables) diffed against `eraseAllSettings`. **1 real hit: `hebrewFontMaker_uiPrefs`** (`6b04970`). **1 kill:** `hebrewFontMaker_recentProjects` looked like a second hit and is **correctly excluded** — it stores 6 full project payloads. **Method note: a probe that reads a resolver must assert the resolver saw what you seeded** — the certificate probe's `active`/`rule` receipt caught two bad seedings in a row (`_certEnter` refuses without a fully-passed ladder, and the ladder has **12** levels, not the 8 a truncated `grep -A 8` showed).

- [x] 2026-08-16 | 6b04970 | index.html | **Erase All Settings now clears the Font Maker's workspace prefs — the `localStorage-vs-AllTools` pattern's first hit in 4 sweeps, and its first ever on the ERASE side.** `hebrewFontMaker_uiPrefs` survived an erase while every sibling UI-state key was cleared: it is the other three tools' `*_panels` key under a different name (column widths, the three node-panel disclosure states, and the 14 Draw-mode tool settings), and CLAUDE.md directs *new* persistent FM UI prefs into it, so the gap would have grown with each release. All 19 of its fields have inline defaults, so clearing restores the factory workspace. **Verified:** 53 keys seeded, the real `eraseAllSettings` driven through both confirms, light/dark × 1280/800 → 8 survivors, all documented-intentional, **0 unexpected, 0 pageerrors**; a stashed-baseline control reproduces `hebrewFontMaker_uiPrefs` as the sole unexpected survivor, so the probe fires. Also recorded `_recentProjects`'s exclusion reason in the adjacent comment, which previously named only the autosave.

- [x] 2026-08-16 | d1c8661 | classroom_dashboard.html | **MICRO-FEATURE — presets that teach the same class share one colour** (gate-1 approved; chosen over two alternatives on variety, since torah_trainer holds the most seeds but had been worked in 2 of the last 3 sessions). The weekly grid is read as *"when do I have Grade 4?"*, but one class spread over three period presets consumed three palette slots and three legend rows, so the grid encoded **preset** identity where the teacher was reading **class** identity. `assignPresetColors` now groups by `presetClassName`: a new class takes the next palette colour, later siblings adopt it. Side benefit: it postpones the wrap point where two genuinely different classes collide on one colour. **Deliberate boundaries:** an unrostered preset still gets its own colour; a hand-picked colour **seeds** its class so later siblings adopt the explicit choice; and a hand pick never **retroactively** recolours already-assigned siblings. **No new string, no new setting, no new storage key** — `presetColors` already exists and already travels in the settings blob, and the palette chip already displays each preset's class name, so shared colours read correctly with no new copy (which is why this shipped ungated). **19 insertions — an eighth of the micro-feature fence.** Verified light/dark × 1280/800 on fresh contexts, 0 pageerrors, with the probe asserting `presetClassName` actually resolved the seeded rosters first: 6 presets across 2 classes + 1 unrostered → **3 distinct colours** (control: 6), both classes internally consistent, all hex valid, a new Grade 4 preset adopts the hand-picked `#123456` (control: takes a fresh palette colour), siblings untouched, and `.ivrit` `gather`→`apply` round-trips the map.
- [x] 2026-08-16 | 0969ba2 | (S222 post-close-out — **user-directed merge to `main`**) | **The maintainer asked for the work to go to `main` in-session, so PR #173 was taken out of draft and merged rather than left for later review.** This is the explicit in-session authorization the loop's branch protocol allows (the ledgered S78 precedent), not a change to the protocol: S223 still opens a NEW `claude/*` branch and a NEW draft PR. **Deploy verified in-session for once:** `pages build and deployment` run 1122 for `0969ba2` concluded **`success`** at 20:07:05Z, so `sw.js v509` and all twelve S220–S222 commits are live on ivritsuite.com. PR #173 is merged and closed; its self check-in was cancelled.

- [x] 2026-08-16 | c16c558 | torah_trainer.html | **A custom font chosen in Torah Trainer no longer reverts on the next load.** `setHebFont` fell back to `HEB_FONTS[0]` for a name it didn't recognise — then wrote that fallback into `settings.hebFont` and saved it. At init the saved name is a My Font the async IndexedDB read has not produced yet, so **every page load silently destroyed the choice**; built-in fonts were unaffected, which is why it survived this long. It also broke index.html's advertised **'Use everywhere'** button, which writes the chosen font into each tool's settings. **The fix was already authored in the repo** — `trope_tutor.html` carries the guard verbatim with a comment naming it 'the dictionary pattern', and trope's shell was copied FROM torah; the guard simply never travelled. | verified: 4 cells (light+dark × 1280/800), 0 pageerrors — choice survives reload, reset-all still returns to Frank Ruhl Libre, a built-in click still applies, a deleted font falls back with the picker usable and **measured identical to trope_tutor and hebrew_dictionary**

- [x] 2026-08-16 | f424ea6 | flash_cards.html + classroom_dashboard.html | **The other two carriers of the Pass F finding, fixed as one class.** **Flash Cards needed BOTH halves and this is the part worth remembering:** `applySettings` coerced an unknown id to the default *before* `refreshMyFonts` ran, and `refreshMyFonts` re-applies by matching `hebFontId` against `MY_FONTS` — so a coerced id can never match and the `setHebFont` guard **alone would not have helped**. Its comment claimed init was unaffected because `MY_FONTS` is empty then; that was exactly backwards and is rewritten. Its English picker carried the identical shape and is fixed with it; the dashboard's `applyFontChoice` is the shared setter for both its pickers, so one bail-on-unknown covers Hebrew and English there. | verified: 8 cells (light+dark × 1280/800), 0 pageerrors — the custom font survives reload, a `getSettings`/`applySettings` round trip AND an `.ivrit` gather/apply; an unknown name is inert rather than destructive; both English pickers still switch; dashboard widgets still render

- [x] 2026-08-16 | b707032 | classroom_dashboard.html | **MICRO-FEATURE (gate-1): hold a square, then drag to fill a row of the week grid.** The seed's own premise was correct and unchanged — v1 was tap-only because a naive `pointermove` capture steals the sideways pan the grid needs at ≤800px. A ~300 ms long press disambiguates: `touch-action` is suppressed **only once the press has fired**, and >10 px of movement before the timer cancels it as a pan. Pointer capture means `pointerover` never reaches the cells beneath, so each move hit-tests the cell under the finger; every write goes through the existing idempotent `paintCell`, so the synthetic post-`pointerup` click is a no-op and the gesture is ONE save, matching the mouse path. Mouse drag, arrow keys and Delete untouched. Hint shown only on `(pointer: coarse)` and only once periods exist. Ring is a static outline — nothing for `prefers-reduced-motion` to neutralize. | verified: 8 cells (light+dark × 1280/800 × coarse/fine), 0 pageerrors — quick drag pans and paints 0; held drag paints the full run of 3 in exactly 1 save; ring + `touch-action` both clear on release; hint only on touch; mouse drag still paints; **fresh profile with no schedule at all → gesture inert, hint hidden**

- [x] 2026-08-16 | (S222 Pass F — cross-tool consistency, 19th run) | **Affordance: the MY FONTS picker contract, across all 6 font-selector tools — never F-swept before (prior F affordances: drawer contract S211, busy/loading S189, destructive controls S176, transient feedback S155).** Contract fixed BEFORE measuring, S198-style: **12 dimensions × 6 tools × 3 arms (EN-light-1280 / HE-light-1280 / EN-dark-800) = 18 cells, 0 pageerrors**, every font seeded through the page's OWN `saveUserFont` path rather than a hand-written IDB record. **Result: 9 of 12 dimensions perfectly uniform** — the tile appears, the `accept` list is byte-identical, the upload label and its title translate, the control sits below the grid, the 'My Fonts' header translates, and selection applies, on all 6 tools in both languages. **ONE real divergence, with THREE carriers: a custom font is silently reverted on the next load in torah_trainer, flash_cards and the dashboard, while the generator, dictionary and trope_tutor keep it.** Isolating it needed a second arm — arm 1 conflated persistence with a deletion test — and a third arm (built-in font vs My Font) to prove the split is about *user* fonts only: **built-ins persist in 6 of 6, My Fonts in 3 of 6.** All three failures are one class (registered below) and all three are fixed this session. **The pass's real lesson was again methodological: FOUR confident probe results were killed before anything was logged** — (1) 'the dashboard has no upload label' was my probe taking the dashboard's FIRST `accept=".ttf"` input, which is the deliberately label-less English-font input (`aria-hidden`, `tabindex=-1`, driven from the toolbar; its Hebrew picker carries the shared label normally); (2) a proposed 'My Fonts cap meter' seed — index.html **already** renders `(N / 10)`; (3) a proposed 'the font delete is unguarded' candidate — `deleteMyFontFile` is **already** `confirm`-gated; (4) 'the fix makes Hebrew render in a Latin sans' — my probe measured `#ttReading`, a container, instead of a Hebrew leaf; the computed `--heb-font` falls back to Frank Ruhl Libre via `:root`, measured **identical to the two reference tools**. **S211's corollary holds and should now be treated as standing procedure: on an F run, a clean result AND a lone-laggard result both deserve a second look — but so does a *fix* that appears to regress something.**

- [x] 2026-08-16 | (S222 close-out) | branch/deploy note | **PR #173 was still OPEN and UNMERGED, so S222 CONTINUED on `claude/improve-loop-2y96fr` and PR #173 for the THIRD session running — no branch restart, no second PR, exactly as the S222 pointer instructed.** Drift check: `origin/main` unchanged at `670df72`, `sw.js` v508, `FONT_MAKER_VERSION` 5.12 — **nothing landed outside the loop since S221**, so Pass F swept only loop surface. `sw.js` **v508→v509** in this final commit; precached files changed: `torah_trainer.html`, `flash_cards.html`, `classroom_dashboard.html`, plus `locales/en.json`+`locales/he.json` (one new key). **FONT_MAKER_VERSION NOT bumped** — the Font Maker was not touched at all this session. Scripts run: `build-locales` (1 new key), `check-i18n` (**clean**, Check A clean + 14 baselined, Check B clean), `update-sitemap.mjs` last (clone confirmed NOT shallow first). `update-llms-txt.mjs` deliberately NOT run — no `<title>`/description/JSON-LD changed. **Gates: 1 asked, 1 answered, 0 deferred** (gate 1 → the dashboard drag-paint seed; **no gate-2 item arose** — the one new string is help copy for the approved feature, not a rewrite of existing wording). **Deploy verification is the maintainer's after merge.**

## S223 (2026-08-16) — archived at the S228 close-out (Done keeps the last 5 sessions)

- [x] 2026-08-16 | (S223 Pass B — console & error audit, **19th run**) | 13 pages × EN/HE + 7 tool interactions + **a NEW state-persistence arm** | **The S222 pointer's warning was right on both counts, and the state arm is now the pass's most valuable half.** Arm 1 (mechanical, 26 loads over the S213–S222 haul): **CLEAN** — 0 pageerrors, 0 same-origin request failures, 0 raw-key leakage, correct `dir` 26/26; every console error is the harness's own external-origin abort and torah's 2 are the documented Sefaria one. Arm 2 (7 interactions): clean, 0 pageerrors — **but 2 of 7 first reported a THROW and both were my probe's fault** (a wrong function name, `generateWorksheet` for `generate()`; and a wrong argument type — `project.letters` is ARRAY-indexed, so `_selectItem('letter', 0x05D0)` passed a codepoint where an index belongs and threw deep in `updateStepTicks`; driving the real controls gave 1 sheet and an intact `curCp`). Arm 3 (**NEW — set a persisted preference through its real control, reload, assert it came back**): 4 dimensions, **1 real hit** (`aeb7809`, below) out of 34 cells — dark mode 13/13, panel-collapse memory 6/6, language 13/13, widened scalar prefs on generator+dictionary 12/12, plus a dashboard save-on-close arm 2/2. Arm 4 (language) initially read **0/13** and was **entirely a selector bug** — the switcher is a `<select>`, not a button. | verified: local HTTP, `serviceWorkers:'block'`, fresh contexts

- [x] 2026-08-16 | aeb7809 | torah_trainer.html + trope_tutor.html | **A setting changed just before leaving the page is no longer lost — the Pass-B state arm's hit, and a NEW registered pattern.** Both tools debounce `saveSettings()` by 300 ms and neither had a flush, so a page discarded inside that window never ran its pending `setTimeout` and the **whole settings blob write** was lost: font, tradition, drill toggles, nikkud and trope colouring, playback rate, panel layout. Realistic on a tablet — change a setting, swipe to another app. **This is exactly the silent-failure shape the S222 pointer predicted B would otherwise miss: no throw, no console line, the setting simply gone.** Fix per S222 method note 3 — **it was already authored in the repo**: `Hebrew_Font_Maker.html` wires `visibilitychange`(hidden) + `pagehide` → `flushAutosave()`, so both files adopt that same pair rather than inventing one (`unload`/`beforeunload` are unreliable on mobile). The four synchronous-write tools are unaffected. torah's flush keeps `saveSettings()`'s own `_handoutActive` guard, so a flush mid-handout still cannot persist the transient print override. | verified: 2×2 matrix (leave-immediately vs 600 ms dwell, the dwell arm as CONTROL) × 2 tools = **4/8 before, 8/8 after**; then 8/8 over light/dark × 1280/800 driving real settings checkboxes (`optShowCantillation`, `optDrillIdentify`), 0 pageerrors; plus an explicit handout-guard assertion (disk kept nikkud ON through a flush mid-handout)

- [x] 2026-08-16 | 0201c95 | trope_tutor.html + hebrew_dictionary.html + flash_cards.html | **The header button row wraps instead of scrolling the page sideways at phone widths — the S213 reflow candidate closed for 3 of its pages, and its standing attribution OVERTURNED.** Attribution first, as that candidate demanded: at 320 px, **with off-canvas drawer content properly excluded**, the header button row IS the overflow on **five of six** pages — its right edge equals the page `scrollWidth` exactly (trope 394, torah 451, dashboard 408, dictionary 378, flash `.h-right` 336; index clean at 320/320). That supersedes the candidate's note that only the dictionary attributed to `.hright`: **the earlier reading was confounded by the closed settings drawer being measured as page content.** **The lever S218 was missing:** S218 concluded `flex-wrap` is inert against `flex-shrink:0` — true, and this is the other half. A flex row wraps once something **caps** its width, so `max-width:100%` does what `flex-wrap` alone could not, **without touching the `flex-shrink` flag S218 named as the blocker** (so the icon buttons still never squash). Scoped per file to that file's OWN narrow breakpoint (dictionary 680, flash cards 440, trope a new 430) because unscoped is inert on trope and flash cards but shifts the dictionary header at desktop widths. | verified: 3 pages × 9 widths (320→1280) × light/dark = **54 cells, overflow 0 in every one**, no clipped or sub-20px header buttons, and header heights at ≥768 px byte-identical to the pre-fix baseline

- [x] 2026-08-16 | 963c4b4 | hebrew_dictionary.html | **Two ReferenceErrors on EVERY phone load of the dictionary — Pass B's headline, and a coverage lesson about B itself.** Both fire on every load at **≤680 px** (the mobile layout — every phone and most tablets) and clean at ≥700 px, **which is exactly why 18 B runs never saw them: B has always loaded at desktop width.** Confirmed pre-existing against a stashed clean tree before any fix. (1) `updateMobileFiltersBtn` read bare `I18n`; `enterMobile()` runs at parse time when the media query already matches, before the deferred `js/i18n.js` has defined it — and `applyI18n()` calls the function again on `I18n.ready`, so the guard defers the label by a tick instead of throwing past the rest of mobile setup. (2) `applyI18n` read `_tourActive`, `let`-declared in a **later `<script>` block** — a temporal dead zone, so `typeof` is the only safe read from there. Same class both times (the registered **pre-ready-i18n** shape in its *parse-time* rather than its never-re-rendered form), and **not cosmetic**: a pageerror aborts the rest of the function. | verified: 7 widths × light/dark = 14 cells, **0 pageerrors everywhere (was 2 per load below 680 px)**; the guards do not disable what they guard — the Filters button still renders its localized label in EN ("⚙ Filters") and HE ("⚙ מסננים"), and a live language switch mid-tour still re-localizes the tour card (חיפוש → Search)

- [x] 2026-08-16 | (S223 close-out) | branch/deploy note | **PR #173 was MERGED before this session, so S223 cut a FRESH branch and opens a NEW draft PR — no commits stacked on merged history**, exactly as the S223 pointer directed. Branch `claude/improve-loop-rj85bu`, cut at `ef01b81`. **Drift check: nothing landed outside the loop** — `origin/main` at `ef01b81` (`0969ba2` + the loop's own pointer-correction commit), `sw.js` **v509**, `FONT_MAKER_VERSION` **5.12**, all matching the pointer; the v498→v509 jump it flagged is the loop's own merged work and was NOT re-swept. `sw.js` **v509→v510** in the final commit (precached files changed: `torah_trainer.html`, `trope_tutor.html`, `hebrew_dictionary.html`, `flash_cards.html`). **FONT_MAKER_VERSION NOT bumped — correct: no Font Maker file changed this session.** `node scripts/check-i18n.js` clean (Check A clean + 14 baselined; Check B clean); `build-locales.js` NOT run (**0 new CSV keys** — every fix was behavioural or CSS); `update-sitemap.mjs` run as the last step; `update-llms-txt.mjs` NOT run (no `<title>`/description/JSON-LD change, no page added or removed). **Decisions deferred to the maintainer (unattended session — no gate could be answered):** **gate 1** — micro-feature SKIPPED this session, and the seed bench is still skewed (torah ×6, Font Maker ×4, dashboard ×4; still zero for trope-tutor, flash-cards, dictionary, index/chrome); **gate 2** — no SEO/copy change was made, and the two standing gate-2 splits (torah deferred-fetch flow, dictionary part-of-speech tail) are untouched.

## S224 (2026-08-17) — archived at the S229 close-out

- [x] 2026-08-17 | (S224 Pass C — accessibility, `hebrew_blend_generator.html`, its **2nd dedicated C since S157** ~67 sessions) | 168 visible controls × EN/HE + 9 dimensions, each clean sweep proven capable of failing | **The generator is a11y-strong and the pass's value came from taking its CORRECTED detectors suite-wide.** Target RE-DERIVED per the S196 rule: `classroom_dashboard` is staler by last-C (S135 vs S157) but was touched ×2 AT THE CAP only two sessions ago (S222), while the generator had nine sessions of rest and was named by both the S213 C row and the S224 pointer — variety broke the tie. **CLEAN with receipts:** names 168 controls → 0 nameless / 0 title-only / 0 placeholder-only / 0 symbol-only in BOTH languages; 0 aria-hidden containers holding focusables; **14 selector groups all expose `aria-pressed`** (control: an injected stateless group fires the detector); reduce **0 animating / 403 with reduce off**; contrast light **0**; focus rings 6/6 light + 6/6 dark; the tour dialog opens, moves focus in, and restores focus to `#tourBtn` on BOTH exit routes (Escape and ✕); 1280px overflow 0; 0 pageerrors in every cell. **Touch targets resolve CLEAN under WCAG 2.5.8** once the exceptions are applied rather than assumed: 52 sub-30px controls → 30 pass the 24px SPACING exception, 9 are inline footer prose links (inline exception), 1 is the ledgered `.tip-icon` exemption, 0 genuine misses; 22 sit between 24 and 29px — WCAG-conformant, under the house 30px bar, recorded as a standing measurement. **2 real findings** (the 320px sidebar overflow, fixed `d04d373`; `.font-opt-attr` at 4.13:1 in dark, logged — the contrast class hit its 2/session cap first).

- [x] 2026-08-17 | e57ff6f | resources.html + contact.html | **A screen reader can now tell which filter or tab is switched on** — the registered `class-only-selected-state` pattern, found by taking Pass C's selector-state detector suite-wide. resources' category/age/font filter bars, its Links|Fonts view toggle and its three suggest-form pill rows, plus contact's PayPal|Venmo|Zelle tabs, marked the live choice with a CSS `.active` class and nothing else — every button in the group announced identically, so a screen-reader user could not tell which of the seven category filters was on or confirm a click had changed anything. **The fix was already authored in the repo** (S222 method note 3 again): the generator's `setVowelScheme`/ivrit-mode buttons mirror `.active` into `aria-pressed`, which is exactly why the generator measured clean. Adopted via a `setPressed(btn, on)` helper at every writer; initial state seeded from the markup's own `.active` rather than 46 hand-written attributes. **Verified** light+dark × 1280+800: 0 buttons without `aria-pressed`, attribute and class agree in every group before and after clicking, exactly one pressed per single-select row, two in the multi-select row and toggling back off works, contact's panel still switches, 0 pageerrors. **The first verification run FAILED and was right to** — 15 suggest-form pills were unseeded because their markup is parsed after the filter-bar script; seeding moved inside `wirePills`.

- [x] 2026-08-17 | 448254c | hebrew_dictionary.html | **Vowel sub-labels are readable in dark mode** — 10 of 13 chips sat at 3.28–4.49:1 (WCAG 1.4.3 AA wants 4.5 on 11.5px text) because `--muted` lands on the vowel groups' coloured tints, which are dark in dark mode. Light was always fine. **Fix already authored in the repo:** the generator renders the identical chip and carries `body.dark .vowel-chip-sub{color:#adb6c9}`. **The half that mattered:** the dictionary set the colour in an INLINE `style` attribute on the span, which out-specifies any class rule — adding the dark rule alone would have measured no change (the S222 "guard on the setter is worthless if the caller coerced the value first" shape, in CSS). Both declarations moved to CSS, inline style dropped. **Verified** light+dark × 1280+800: 13/13 ≥ 4.5 (dark min 4.91, light min 4.62), font-size still 11.52px, 0 inline colours, 0 empty labels, 0 pageerrors; control — forcing `--muted` back returns 10/13 under 4.5, so the detector still fails when it should.

- [x] 2026-08-17 | ed05a27 | classroom_dashboard.html | **Monday's day tile meets the contrast bar it is actually held to.** The projected day strip picks white vs dark ink from `isLightPlate()`, whose `0.30` threshold is documented — correctly — as the luminance where white crosses the **3:1 LARGE-text** bar. But the Hebrew day name renders at **21.6px / weight 500**, and WCAG large text starts at 24px (or 18.66px bold), so the **4.5:1 normal-text** bar applies and the constant was one bar too permissive. Exactly one plate sat in the gap: Monday's `#e65100` (L = 0.227) at **3.79:1** white, its badge label at 3.86 light / 4.14 dark. Threshold moved to **0.1833** — the 4.5:1 crossing, derived the same way the original was — flipping Monday to dark ink at 4.93:1 and its badge to 6.2:1. **Nothing else moves** (Tuesday was already above; the other five are ≤ 0.155). **Verified** light+dark × 1280+800: all 7 days clear their bar in every cell (min 4.59, was 3.79), ink-dark 1→2, the five conforming days unchanged, 0 pageerrors. **Two same-sweep hits deliberately NOT fixed and recorded as non-defects:** the badge emoji renders as a colour glyph so its computed `color` is not what a viewer sees, and `.spoiler` is transparent-on-black by design.

- [x] 2026-08-17 | d04d373 | hebrew_blend_generator.html | **The generator stops scrolling sideways on a phone — the LAST unattributed page of the S213 reflow candidate, carried unexplained for ten sessions.** Every other overflowing page traced to its header button row; this one did not. Cause: at ≤700px the sidebar is restyled `width:100%` with `padding:12px`, and the page sets **no global `box-sizing`**, so the content-box element renders **24px wider than its parent** — 344px in a 320px viewport, exactly the measured overflow. One `box-sizing:border-box` in that block makes 100% mean 100%. **Scoped to the ≤700px block on purpose:** the desktop rule's fixed 320 + 20 padding + 1 border is a deliberate 361px column that border-box would silently shrink by 40px. **Verified** light+dark × 320/390/700/800/1280 (10 cells): overflow 0 everywhere, 0 of 302 sidebar controls clipped, 0 pageerrors — and **measured against a stashed clean tree** to separate fix from baseline: 320px overflow **24 → 0** (sidebar 344 → 320), desktop **361.0 → 361.0** unchanged at both 800 and 1280.

- [x] 2026-08-17 | (S224 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-rj85bu` → the SAME draft PR #174**, per the branch protocol — S223's PR was still open and unmerged at session start, so no restart and no second PR; #174 now carries S223+S224 (8 commits). **Drift check: nothing landed outside the loop** — `origin/main` still at `ef01b81`, unmoved since the S223 close-out. **`sw.js` v510→v511**, one bump in the final commit; precached files changed: `resources.html`, `contact.html`, `hebrew_dictionary.html`, `classroom_dashboard.html`, `hebrew_blend_generator.html`. **`FONT_MAKER_VERSION` stays 5.12** — no Font Maker file was touched. Scripts: `check-i18n.js` clean (Check A clean + 14 baselined, Check B clean); `build-locales.js` NOT run (0 new CSV keys — all four fixes are ARIA/CSS, zero new user-facing strings); `update-llms-txt.mjs --check` current (no title/description/JSON-LD changed); `update-sitemap.mjs` run last. **Decisions deferred to maintainer: NONE — gate 1 was ASKED AND ANSWERED** ("fixes only, go wider than the queue"), which is why no micro-feature shipped and why all four fixes come from newly-hunted findings rather than the standing candidate list. **Gate 2 was never reached** — zero user-facing copy, title, description or JSON-LD text changed all session. Deploy verification (Pages run `success`) happens **after the maintainer merges #174**.

## S225 (2026-08-19) — archived at the S230 close-out

- [x] 2026-08-19 | (S225 Pass M — aesthetics & visual design, **FIRST EVER RUN**, `Hebrew_Font_Maker.html`) | 292 visible elements × light/dark × 1280/800px, 0 pageerrors; 8 design dimensions; every finding put to a control | **The Font Maker is visually disciplined and the pass's value was taking its row-alignment detector suite-wide.** Target chosen by the variety tie-breaker, since no surface has ever been M-audited: font-maker was the stalest tool by five sessions and the only tool untouched in the last two. **Clean with receipts:** dark-mode parity **perfect** — all 74 bordered elements flip tokens (59→59, 13→13, 1→1, 1→1), zero unthemed borders, radii and layout identical across all four arms; radius near-uniform (6px ×56 dominant, four outliers, only two unmotivated and both sub-pixel-invisible); spacing rhythm and border palette coherent. **Headline: a NEW recurring pattern, `row-siblings-with-mismatched-heights`** — controls sitting side by side in one flex row differ in height by 2–6px on **5 of 10 pages**, with **5 pages clean as a control** (index, flash_cards, trope_tutor, resources, contact), so the shape is fixable rather than inherent. Shared cause: one variant declares `border:none` where its siblings carry a border, so the boxes differ by exactly the border delta. **The convergence target is not the row mean but ≥30px** — the suite's own ratified touch floor, which `js/i18n.js` already applies to the switcher for this exact reason — because three of the five rows contain a control below it. **Three candidate findings were killed by a control**, none by inspection: the `.mode-toggle-stack`'s 1px raggedness is `border-top` acting as a separator (correct); the two "different" opacity sliders share identical CSS and differ only by fill level (35% vs 92%); and the page's apparent zero elevation is 15 box-shadows on modals the boot-state audit had dismissed. Also logged: the shared language-switcher divergence (**gate 3 asked → "leave as-is"**) and the 20-step type scale with 9 steps inside a 2.24px band (measured, deliberately not chased).

- [x] 2026-08-19 | b55b7d5 | Hebrew_Font_Maker.html | **The Font Maker's toolbar controls finally share one baseline** — first fix of the new pattern. The top bar carried **three heights in one row**: ghost buttons 31px, the export-format select 33px, and the gold **Export TTF button — the app's entire purpose — at 29px**, the smallest control in the bar and *below* the ratified 30px touch floor. Cause: `.tbtn` declared `border:none` while `.ghost`/`.tbtn-select` each carry 1.5px, so the filled variant was exactly the border delta shorter; a select's line box is a further 2px taller. Fix: a **transparent** 1.5px border on `.tbtn` + 6px block padding on `.tbtn-select`. **Verified:** all 9 controls at exactly 31px, 0.0px spread, none below 30, across light/dark × 1280/800, 0 pageerrors; the page's whole-row census went 1→0. The same class silently fixed `.tbtn` in the **wizard, help modal and tour card**, which were mixed 29/31 and are now uniformly 31. Gate 3 asked on whether Export should instead be made deliberately larger → **"match its siblings exactly."**

- [x] 2026-08-19 | 13de328 | hebrew_dictionary.html | **The dictionary's toolbar controls share a baseline and clear the touch floor** — second and last fix of the pattern this session (2/session cap). A 27px sort select sat beside three 25px tool buttons, and **both values are under the ratified 30px floor**, so *Copy link / Print / Word Lists* — the buttons a teacher taps to share or print a list — were the smallest targets on the page. Fix: `min-height:30px` + `box-sizing` on `.sort-sel` and `.dict-tool-btn`, the same lever and the same reasoning `js/i18n.js` records for the switcher ("padding alone left it at 26.2px"). **Verified** light/dark × 1280/800: all four at exactly 30px, 0.0px spread, button text centred within 0.5px, 0 pageerrors, page row census clean. **Method note:** the first run "failed" with the select 109px off-centre — a probe defect (`selectNodeContents()` on a `<select>` measures its `<option>` list, not the rendered label), not a fix defect.

- [x] 2026-08-19 | 4069b70 | hebrew_blend_generator.html | **The font picker's attribution line is readable on the selected row in dark mode** (queued P3 from S224 Pass C) — 4.13:1 → **6.18:1**. **Two corrections came out of it.** (1) The candidate's scope was wrong: it assumed every font row failed; with an alpha-compositing detector the five inactive rows measure **5.02:1 and pass**, and the lighter active-row surface `#2a3349` is the whole cause — one failing instance at a time, not one per font. (2) The pointer's "check for an inline style out-specifying the class" lead did not apply — there is no inline style — but a **specificity trap existed on the other side**: `body.dark .font-opt-attr` (0,2,1) outranks `.font-opt-attr:hover` (0,2,0), so the first attempt silently killed the link's gold hover in dark. **Verification caught it before commit**; the hover rule now carries a dark twin (5.89:1).

- [x] 2026-08-19 | 174995f | classroom_dashboard.html + torah_trainer.html | **The dashboard stops scrolling sideways on a phone, and Torah Trainer nearly does** — the second half of the S213/S223 reflow candidate (WCAG 1.4.10). Both pages needed the second lever S223 measured but did not ship: `.hright{flex-wrap;max-width:100%;justify-content:flex-end}` **plus** a narrow-scoped `header{flex-wrap:wrap}`, because capping `.hright` alone leaves the title block and the button row competing for one line. **Dashboard 88→0** at 320px (48→0 at 360). **Torah 131→11** at 320, 91→0 at 360. **Breakpoints are per-file and measured, not copied:** dashboard takes 430px like its siblings (its offender ends at 408px), but **torah needed 480** — its `.hright` ends at 451.4px, so a 430px scope left the page overflowing 20px across the whole **431–451 band**, which the S223 matrix could not see because it sampled 320 and 360 only. **Verified 6 pages × light/dark × 9 widths = 108 cells**: overflow 0 everywhere but torah's 320px residual, no squashed or clipped header controls, 0 pageerrors, and the four pages fixed in S223/S224 held at 0 as the control. Torah's header heights at 480px+ are identical with the rules forced on and off, so the scope is provably inert at desktop. **The 11px residual is now attributed** (see Candidates): the Layout `.radio-group`, a segmented control needing 330.7px whose labels carry `&nbsp;` and so have no internal wrap point.

- [x] 2026-08-19 | (S225 close-out) | branch/deploy note | **Branch/PR: FRESH branch `claude/improve-loop-bkos51`, cut at `0a1c5e8` (latest `origin/main`), with a NEW draft PR** — exactly as the S225 pointer directed, since PR #176 and PR #174 are both merged and neither may carry new work. **Drift check found an UNLEDGERED outside-loop landing:** `origin/main` moved `090b526` → `0a1c5e8` (5 commits), and one of them is a real code change nobody recorded in Done — **`0209ece`, a suite-wide fix for ten CSS rules that restated a slider-driven font-size as a literal and so beat the var the slider writes** (reported case: the dashboard's Hebrew-date size slider dead in fullscreen and under 600px). It carried the **`sw.js` v511→v512** bump, which is why the S224 pointer's "v511" is stale. The other four are the pass-M skill update, its ledger note, and a sitemap refresh. **`sw.js` v512→v513** in this session's final commit; precached files changed: `Hebrew_Font_Maker.html`, `hebrew_dictionary.html`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `torah_trainer.html`. **`FONT_MAKER_VERSION` stays 5.12** — the Font Maker change is a two-property CSS alignment fix, not a feature, and CLAUDE.md's rule bumps on features, not fixes. Scripts run: `check-i18n.js` (clean, no new violations, 14 baselined as before), `update-sitemap.mjs` (last step; clone was shallow at session start and `git fetch --unshallow` ran first, per the standing caution). `build-locales.js` NOT run — no CSV keys added, no new user-facing strings this session. **Decision gates: all three ASKED AND ANSWERED, none deferred** — gate 1 micro-feature → *fixes only*; gate 3 language switcher → *leave as-is, log it*; gate 3 Export TTF sizing → *match its siblings exactly*. **Protocol divergence flagged for the skill:** none — pass M ran as written, and its screenshot rule was honoured (before/after in the session scratchpad, delivered at close-out, never committed).

## S226 (2026-08-19) — archived at the S231 close-out

- [x] 2026-08-19 | (S226 Pass A — recurring-pattern sweep, over the `867b716..HEAD` delta = **69 commits / 528 added lines / 15 files**, the S215–S225 haul that no A had ever seen) | 28 diff signatures + 4 runtime detectors, every one run against a CONTROL | **2 classes HIT, 4 classes CLEAN with receipts, 3 candidate findings refuted.** The pointer named four classes and all four were worked. **HIT 1 — `debounced-persistence-with-no-page-hide-flush`, one NEW carrier, and the pass's headline:** `hebrew_dictionary.html` writes `hebrewDictionary_lastState` through a **1000 ms** debounce with no flush, so a teacher who changes a filter and closes the tab inside that window loses the whole saved view. Proven with the mandatory dwell control — dwell 0 → key `null` (LOST); dwell 1500 → restored (PASS); same-tick `getDictState()` → correct either way, which is exactly why a same-tick probe reports clean. **S223 swept this class suite-wide and classed the dictionary "not applicable, verified rather than assumed" on 12/12 scalar-pref round trips at dwell 0 — those prefs really are synchronous; this key is the one writer that is not.** Method lesson: *census the writers, then probe the key each writer owns — a per-tool verdict earned on a different key is not a verdict.* The census that found it is two greps: a debounce-timer signature (FM 5, torah 5, trope 3, dictionary 1) against a `pagehide`/`visibilitychange` signature (FM 2, dashboard 2, torah 2, trope 2, **dictionary 0**) — the one row where the two disagree is the hit. **HIT 2 — `row-siblings-with-mismatched-heights`:** the S225 detector re-run confirms 3 carriers (generator `header` 29/29/33/35 with two controls under the 30px floor; dashboard `.hright` 31/35/35/31/41; torah `.hright` 34.4/31/35/35/32) and, as the control, that S225's two fixes held and the 5 never-hit pages are still clean. **CLEAN 1 — `async-store-backed-choice-clobbered-by-a-sync-fallback`,** extended past fonts exactly as its "where else to look next" note directed: the census found two `find(…) || <default>` resolvers outside the font pickers (dashboard `applyFontChoice`, torah `renderText`'s font stack) and both are read-path only, and torah's five parsha-restore sites all **bail** rather than write back. The store-backed reload probe PASSES on all three carriers tested. **CLEAN 2 — `class-only-selected-state` beyond load-state:** 10 pages, 1 candidate, refuted on the control. **CLEAN 3 — `incomplete-print-token-reset` / `print-media-leak` on the dashboard's NEW week-grid print surface** (`body.swm-printing`, never print-swept): measured under `emulateMedia('print')` in BOTH themes, `--text` reads `#1a2744` in dark as in light and the ink measures an identical 14.81:1 on white — the `@media print` remap's `html, body, body.dark` selector reaches the new class. **CLEAN 4 — `theme-flipping-token-on-a-fixed-colour-plate`:** the dictionary's new `.sort-sel`/`.dict-tool-btn` put `var(--text)` on `var(--white)`; both tokens flip together, so it is correct by construction. Signature scan yielded 0 added `JSON.parse`/`Object.assign` (untrusted-shape), 0 `alert(` (blocking-alert), 0 SVG geometry, 0 tour edits, 0 new overlays, 0 new placeholders — the delta is overwhelmingly CSS and JSON-LD. | verified: diff-signature scan grounded by reading or measuring every hit (a signature hit is a candidate, never a finding) + 4 Playwright detectors over local HTTP, `serviceWorkers:'block'`, `resp.ok()` asserted, pageerror 0 in every cell

- [x] 2026-08-19 | 18c54ff | hebrew_dictionary.html | **A filter change followed by closing the tab no longer wipes the dictionary's saved view.** Pass A's HIT 1. `scheduleDictSessionSave` debounces the `hebrewDictionary_lastState` write by 1000 ms and nothing flushed it on page hide, so a discarded page simply never ran the callback — mode, sort, length, part-of-speech, themes, era, vowel/letter filters and gematria, gone, with no throw and no console line. Adopts the reference pair verbatim (Hebrew_Font_Maker, then torah/trope in S223): `visibilitychange`(hidden) + `pagehide` → `saveDictSessionFlush()`, wired at the same post-`panelMemApply()` point; `unload`/`beforeunload` are unreliable on mobile and deliberately not substituted. | verified: 4 arms × light/dark × 1280/800, pageerror 0 — dwell-0 pick→reload now restores (was `null`); the dwell-1500 CONTROL still restores; the flush is inert with no pending timer and pre-bootstrap; and the restored sort re-applies on the next load, not merely persists. **Two of the four arms failed on the first run and BOTH were assertion defects, fixed in the probe, not the code** — one asserted "no key after a fresh load" when a load legitimately writes one past the debounce, the other read the restored value on a fixed timer instead of waiting for the corpus-gated bootstrap.

- [x] 2026-08-19 | db37a54 | hebrew_blend_generator.html + classroom_dashboard.html + torah_trainer.html | **The last three header rows share one control height — and the `row-siblings-with-mismatched-heights` class is CLOSED.** Every control in all three rows now measures exactly 35px in both themes at 1280 and 800. One cause is shared across all three: icon buttons render at 1rem and text buttons at 0.82rem, so the line box alone decides the height and nothing declared a shared one. 35px is each row's own tallest control, so converging up shrinks no glyph and lifts the generator's Home and Tour buttons (29px) over the ratified 30px touch floor. **The dashboard's 41px outlier was entirely a specificity bug:** `#fsBtn` carries `class="hbtn icon-btn fs-btn"`, and `.hbtn.icon-btn` (0,2,0) outranks a bare `.fs-btn` (0,1,0), so the stacked button silently kept icon-btn's 7px block padding instead of its own 4px — scoping to `.hbtn.fs-btn` restores the authored intent and needed no redesign. The generator's four controls carry hand-rolled inline styles rather than one shared class (the Font Maker `.hbtn` and Flash Cards `.h-btn` rows are clean *because* they do), so its height goes on one `header > a, header > button` rule; `inline-flex` is what lets `min-height` reach the `<a>`, and the 6px gap replaces the source whitespace flex would otherwise collapse — the same gap both reference implementations use. **Gate 3 asked and answered: "shared height, keep stacking"** — the Full Screen buttons keep their stacked icon-over-label form. | verified: the S225 detector reports **0 groups on all 10 pages** with the 7 already-clean pages held as a control; full child census 35/35 across 3 pages × light/dark × 1280/800; no new sideways scroll at 320/360/400/430/440/451/480/520/700/720 (torah's known 11px at 320px is the ledgered Layout `.radio-group` residual, unchanged). Before/after screenshots light+dark at 1280 captured by stashing the diff — a faithful revert, not an injected approximation — and delivered at close-out, not committed. **The first verification run "failed" 4 cells on the decorative `✡︎` star span, which is not a row control: an assertion defect, corrected in the probe.**

- [x] 2026-08-19 | 1b8f211 | torah_trainer.html | **MICRO-FEATURE (gate-1 approved): the 17 holiday Torah readings are visible instead of buried.** The S180 Pass-H seed, on its first offer that got an answer. The readings already worked — as an `<optgroup>` 54 parshiyot down a **closed** `<select>`, with the word "holiday" appearing nowhere on the page, so a teacher setting up Yom Kippur or Shavuot had no way to know they existed. A **"🕎 Holiday Torah Readings"** button now sits beside "Custom range…" and opens a strip of the same 17 readings as buttons. **Adds no state and no storage key** (rides `settings.holidayKey`, which already travels in `hebrewTorahTrainer_settings`, so AllTools carries it with no index.html wiring) **and no i18n key** (button and strip `aria-label` reuse `torah.controls.holiday_group_label`, the optgroup's own translated header) — which is also why gate 2 never applied. Both entry points now call one `applyHolidayReading()` instead of two copies of the same four writes; the strip builds once on first open and thereafter flips a class + `aria-pressed` rather than rebuilding 17 buttons (honouring the ACTIVE `rebuild-where-a-class-swap-would-do` class this very session swept). Selected state uses the same gold treatment `.tt-speak`/`.tt-loop`/`.tt-jump-chip` already use. **Two pre-existing sync gaps surfaced and are fixed:** the `<select>`'s weekly-parsha branch and "jump to this week's parsha" both cleared `settings.holidayKey` without going through `syncParshaSelect()` — three of the five clear sites already did — and the second also set `parshaSelect.value` by hand; both now use the helper, making it genuinely the single chokepoint its own comment claims. | verified: **31 checks × light/dark × 1280/800 × EN/HE = 124/124**, covering lazy build, 17 options sharing one height at ≥30px, exactly-one selection, swap-not-accumulate, both entry points, clearing from the `<select>`, survival across reload, `aria-expanded`/`aria-pressed`; plus a fresh-profile pass (empty localStorage AND IndexedDB) proving no new key is written at boot or after use, and an AllTools export→erase→import round trip proving the pick restores live on the page. pageerror 0 in every cell. The one failing check on the first run — "picking a weekly parsha clears the strip" — was a real defect in the fix and is what surfaced the two sync gaps above.

- [x] 2026-08-19 | (S226 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177**, per the protocol's "if the previous loop branch's draft PR is still open and unmerged, continue on it" — checked rather than assumed (`pull_request_read` returned `state: open, draft: true, merged: false, mergeable_state: clean`). **Drift check: CLEAN, and verified by diff rather than by version note** — `origin/main` sat at `0a1c5e8` at both ends of the session, the same commit S225's close-out recorded, so nothing landed outside the loop between S225 and S226. This is the check the S225 lesson demanded, and it is the first time since that lesson that it has come back genuinely empty. **`sw.js` v513 → v514**, bumped once in the final commit; precached files changed: `hebrew_dictionary.html`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `torah_trainer.html`. **`FONT_MAKER_VERSION` stays 5.12** — nothing this session touched the Font Maker at all (it was S225's Pass-M target and is deliberately rested). **Scripts run:** `node scripts/check-i18n.js` → clean, Check A clean with the same 14 baselined findings, Check B clean with no CSV debt (the micro-feature added **zero** new keys, reusing `torah.controls.holiday_group_label`, so `build-locales.js` was correctly NOT needed); `node scripts/update-llms-txt.mjs --check` → both files current (no `<title>`, description or JSON-LD changed); `node scripts/update-sitemap.mjs` as the LAST step, on a non-shallow clone. **Iterations: 5 of 5** — 1 pass (A) + 2 fixes + 1 micro-feature (2 iterations). **Variety governor respected:** torah-trainer ×2 (at the 2/tool cap), dictionary/generator/dashboard ×1 each; `row-siblings-with-mismatched-heights` charged 1 of its 2 allowed pattern-iterations (all three remaining carriers fixed as one concern, one commit, closing the class). **Decision gates: 2 asked, 2 answered, 0 deferred** — gate 1 → "build one seed too" (the seed was then picked by the written rubric from six open S-size seeds, all single-audience, on value-per-line; the choice and its reasoning are in the seed's Done entry, and the maintainer can redirect it); gate 3 → "shared height, keep stacking", which also closes the S225 carry about unstacking the Full Screen buttons — **not taken, by decision**. Gate 2 never applied: the session added no user-facing wording, no title/description/JSON-LD change. **Screenshots (pass M rule, honoured because the header work is aesthetics-track):** before/after for all three headers, light + dark at 1280, produced by **stashing the diff and re-shooting** rather than injecting an approximate revert — the S225 lesson about a faithful before — plus two shots of the new holiday strip. Verified pairwise non-identical, held in the session scratchpad, delivered at close-out, **not committed**. **Deploy runs on merge to `main`; Pages `success` is verified after merge, not in-session.** **Decisions deferred to maintainer: none.**

## S227 (2026-08-19) — archived at the S232 close-out

- [x] 2026-08-19 | (S227 Pass E — freshness & site health, **20th run**, over `9b0a445..HEAD` = **69 commits / 1,809 added lines / 25 files**, the S216–S226 haul, untouched since 2026-08-15) | **12 dimensions run, every one CLEAN at the level E owns, ZERO real hits — and the run still produced the session's two best findings, because both came from OUTSIDE the dimensions rather than inside them.** D1 sitemap `lastmod` **12/12** exact per file (non-shallow clone verified first; control = an injected `2001-01-01` that the script detected and rewrote, so the clean is not vacuous). D2 **402** static internal refs, 0 broken, with `<script>`/`<style>` regions blanked so S215's JS-assignment FP class cannot recur. D3 SW precache BOTH directions: 26 entries, 0 listed-but-absent, `i18n-test.html` the only unlisted page (correct — non-production), locales 2/2, `js/` 1/1; `og-card.png` / `og-fontmaker.png` / `sw.js` correctly absent and the 18 splash PNGs runtime-cached by design, not a gap. D4 **21 CSP-allowlisted hosts, 21/21 accounted for** in `THIRD_PARTY_LICENSES.md` at the PRODUCT level (Google Analytics 4, jsDelivr, cdnjs, Google Fonts). D5 **all eight** shared `═══` blocks single-sha: `.ivrit` engine 3 identical + index's documented adapted superset, app-toast JS 4 / CSS 4, `ivritsuite-fonts` 9, My Fonts uploader 7, folder-tree CSS 3 + JS 4, panel-collapse 6, TROPES 2. D6 reduced motion **measured live** on all 13 pages — 0 animating under `reduce`, 11 with a firing `no-preference` control (30–684 animating) and privacy/terms the two documented blind zeros; 0 pageerrors on all 13. D7 the 18-tag apple block byte-identical **13/13** + mirror match + 0 dangling / 0 orphan PNGs. D8 8 `?v=`-busted data fetches, every file present and **none newer than its own token**. D9 counts 14 root `.html` = 13 production + `i18n-test`. D10 PocketTorah **432/432** labels case-exact via the manifest's own `labels` map, 0 unreferenced, both controls firing. CLAUDE.md accuracy (the pointer's specific ask) re-verified by grep against its own stated numbers: TROPES 26 ✓, panel-collapse 6 ✓, vowel-scheme 4 ✓, `showAppToast` 4 with index correctly absent ✓, tour engines 7 tools ✓, Font Maker 15,767 lines against its "~15,700" snapshot ✓, and **all 10** one-time UI flags present in `eraseAllSettings` ✓. `robots.txt` / `CNAME` / `.nojekyll` present and parsing; sitemap `<loc>` ↔ disk 12/12; `llms.txt` + `llms-full.txt` current; `check-i18n` clean. **The two findings both came from a dimension E does not have:** a first-ever suite-wide horizontal-overflow measurement at 320 CSS px (see the chrome-page fix below and the two new candidates), and a **six-strong checker-defect harvest** whose lesson sharpens S215's — see the method note candidate.

- [x] 2026-08-19 | e736a79 | hebrew_dictionary.html (`locales/ui-strings.csv`) | **The last four parts of speech stop showing English inside the Hebrew UI** — the S218 Pass-K tail, **gate-2 asked and answered this session ("author them as REVIEW drafts")**. The corpus carries 23 distinct `pos` values; 19 had a filter row and therefore a translated label, and proverb (19 entries), definite article (2), adjective/noun (1) and adverbial phrase (1) had none, so `posLabel` fell back to the raw corpus string. Shipped the entry's own "cheapest correct shape": the four become filter rows beside their families, so the badge inherits the label from the very markup the filter reads, **plus** the four values join `POS_KNOWN` — without that second half, unchecking "Proverb" would have been silently overridden by the catch-all "Other". **Grounding found a backward-compat hole the candidate did not predict and it is fixed in the same commit:** restore does `cb.checked = set.has(cb.value)`, so a `lastState` or `?s=` link written before these rows existed lists only the old 20 values and would have read that absence as "the teacher unchecked it", hiding those words on the next visit; a state mentioning none of the later-added values now leaves them at their markup default — the same rule the shared panel-collapse memory uses for a panel added in a later release. | verified: 60/60 checks over local HTTP with `serviceWorkers:'block'`, pageerror 0, across EN/HE × light/dark × 1280/800 (rows present + translated, `posLabel` resolving, `POS_KNOWN` membership, and unchecking Proverb actually emptying the result while "Other" stays checked), plus **three restore arms with two controls** — a pre-change state keeps all four on, a post-change state that deliberately unchecks Proverb is still honoured, and an ordinary omitted value (`noun`) is still unchecked, proving the guard is scoped to the new rows only

- [x] 2026-08-19 | b25421e | privacy.html + terms.html + contact.html + resources.html | **Four chrome pages stop scrolling sideways on a phone — a WCAG 1.4.10 (Reflow, AA) miss that no prior measurement had ever named.** Every earlier reflow measurement covered the tools; the chrome pages had never been swept, and Pass E's suite-wide arm found privacy/terms/contact overflowing **58px** at 320 CSS px and resources **70px**. One shared cause on all four: `header .hright` has neither `flex-wrap` nor a `max-width`, so Back + language switcher + dark toggle stay on one line and run past the viewport (`#darkBtn` ends at 377.9 on a 320px viewport). Fixed with the dashboard's and Torah Trainer's existing rule **verbatim** rather than a new one — `header{flex-wrap:wrap}` + `.hright{flex-wrap:wrap;max-width:100%;justify-content:flex-end}` at the dashboard's own 430px scope — which is the "read the passing files first" lesson supplying the fix for a fifth consecutive session. | verified: the 320–440 band in 10px steps goes 58/58/58/70 → **0 everywhere on all four**, and 0 at every width in EN and Hebrew RTL alike; the rule is **inert above its breakpoint** (header heights at 480/640/900/1280 identical with it forced on and forced off, both themes) and the same control fires below 430, where the unwrapped row had been making the header TALLER by forcing the title to wrap (privacy 172→122); before/after screenshots (light + dark, 320px, privacy + resources) produced by a faithful revert and held outside the repo

- [x] 2026-08-19 | 3f88db4 | Hebrew_Font_Maker.html | **MICRO-FEATURE (gate-1 approved): watch a letter being written.** The S180 seed, chosen over a dashboard housekeeping seed and over skip. Every drawn letter already stored its strokes in order in `l.draw`, and `drawStrokesSVG` already rendered each as its own `data-di`-keyed element — so stroke order was fully recorded and completely unwatchable. A **▶ Replay** button in the Draw panel hides those already-rendered elements and reveals them one at a time, in drawing order. **Deliberately render-only**: no `udDo`, no `markDirty`, no `renderStage`, so it cannot reach undo or autosave; the captured `<g>` doubles as a connectivity sentinel (the generator's chunked-build pattern) so any real re-render stops the replay instead of animating detached nodes, `drawPointerDown` stops it outright so a gesture can never land on hidden ink, and a second press stops and restores. Reduced motion is honoured the only honest way available — the reveal IS the animation, so under `reduce` nothing is hidden at all and the status line says why. 47 lines in one tool + 3 CSV keys, well inside the ~150-line fence; **no new storage key, so no AllTools wiring**; `FONT_MAKER_VERSION` 5.12 → 5.13 with a matching About-tab changelog entry. | verified: **32 checks on fresh profiles** (empty localStorage AND IndexedDB — every run walks the onboarding wizard) over local HTTP, pageerror 0, across EN/light/1280, HE/dark/800 and a `prefers-reduced-motion` context, with undo-stack and dirty-flag readings taken before and after every replay

- [x] 2026-08-19 | (S227 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177**, which was still OPEN and unmerged at session start (checked, not assumed — the pointer warns that "merged" has been the norm). **Drift check ran the diff the standing lesson demands, not the version note:** `origin/main` is still at `0a1c5e8`, byte-identical to S226's recorded position, so **nothing landed outside the loop** — and `sw.js` v514 + `FONT_MAKER_VERSION` 5.12 both matched the pointer independently. `sw.js` **v514 → v515**; precached files changed this session: `hebrew_dictionary.html`, `privacy.html`, `terms.html`, `contact.html`, `resources.html`, `Hebrew_Font_Maker.html`, plus `locales/en.json` + `locales/he.json`. **`FONT_MAKER_VERSION` 5.12 → 5.13 WITH an About-tab changelog entry** — the correct call this time because S227 shipped a Font Maker *feature*, not a token swap. Scripts run: `build-locales.js` (7 new keys), `check-i18n.js` clean (Check A clean + the same 14 baselined, Check B clean), `update-llms-txt.mjs --check` current, `update-sitemap.mjs` **last**, on a verified non-shallow clone. **Gates: two asked, two answered, none deferred** — gate 1 → *Font Maker stroke replay*; gate 2 → *author the four Hebrew part-of-speech terms as REVIEW drafts*. Deploy runs on merge to `main`; Pages `success` is verified after merge.
- [x] 2026-08-19 | (S228 Pass H — teacher walkthrough / paper-cuts, `trope_tutor.html`, its **first dedicated H since S140 (~88 sessions**, the stalest H target by a wide margin) and the first walk over the S198 print chart, the S202 sticky-panel micro-feature, the panel-collapse memory and the S51–S55 keyboard work) | **The tool walked very cleanly and the pass's headline is that SEVEN would-be findings were killed by grounding — every one a probe defect, not a product defect.** Role-played a b'nei-mitzvah tutor's prep end-to-end: set the tradition → teach one clause family with audio → print the take-home chart → set up a practice drill. **8 clicks to a running drill, 0 pageerrors throughout.** The seven kills: (1) the tradition radio "cannot be clicked" — `.radio-group input` is `opacity:0;width:1px;pointer-events:none` **by design**, the LABEL is the control; (2) tradition "not persisted" — `saveSettings()` is debounced 300 ms and the probe read at 250, and there is a `saveSettingsFlush()` on `visibilitychange`+`pagehide` whose own comment names this exact teacher scenario; (3) the print sheet "empty" — `printChart()` is `buildPrintChart(); window.print();` and headless `print()` fires `afterprint` immediately, so `clearPrintChart()` had correctly wiped it *before* the probe looked; (4) "the page header prints" — deliberate and documented, `.hright`/tabs/play buttons are hidden and the header is retained **as the chart's own title**; (5) play buttons "unlabelled" — each carries a distinct `aria-label` naming the trope and citation; (6) audio failure "silent" — the probe watched `#tuStatus`, but the tutor's toast is `#tuToast`; with a CONTROL proving the toast renders, the real path fires **in under 500 ms** with "Audio unavailable right now" after transparently trying an alternate example (`_badFiles` 1→2); (7) the drill "stalls at Q1" — answering builds `#qNextBtn` **inside `#qFeedback`** and focuses it, and the loop only clicked inside `#qChoices`. **Measured clean:** 0 document overflow at 320/360/390/800 in EN and HE/dark (trope_tutor is NOT part of the 320px family), 0 raw i18n keys, the "See it in the parsha →" handoff is a real deep link (`torah_trainer.html?parsha=Bereshit&v=1%3A1`), the print chart builds 6 families / 26 cards / 26 glyphs on Letter and A4, and a **full 10-question drill completes with audio blocked** — the never-dead-end contract holds end to end. **Yield: 2 candidates + the tool's first two feature seeds ever**, filling the bench gap five consecutive pointers had flagged. | verified: 4 Playwright probes over local HTTP, every finding grounded in the source before logging

- [x] 2026-08-19 | b3c4f52 | torah_trainer.html | **The settings segmented controls wrap instead of scrolling the page — closing the 320px reflow class.** `.radio-group` is `display:inline-flex` with **no `flex-wrap`**, and its labels are `flex:none` with several carrying a non-breaking space, so the track had no break point anywhere: the Layout control ("Side by side / Interlinear / Page view") laid out **265.4px wide from left 65.3 and ran 10.7px past a 320px viewport**. One declaration — `flex-wrap:wrap` — lets whole segments wrap rather than the page. **The defect is ENGLISH-ONLY at 320** (Hebrew's labels measure 246.5 and already fit), which is why earlier RTL-focused checks never saw it — a refinement of the attribution the ledger has carried since S225. | verified: the SAME probe that produced the original number, before and after, over 28 cells (EN/HE × light/dark × 320..1280, drawer closed and open) — before `en/light 320: 11/11`, group 265.4 wide, 1 row, `fitsViewport=false`; after `0/0`, 242.7 wide, **2 rows**, fits; all 27 other cells 0 both times, so the probe is demonstrably live rather than uniformly quiet. Inertness measured not assumed: with the rule forced off vs on, all ten `.radio-group` boxes report byte-identical width×height at 480/640/900/1280. 0 pageerrors

- [x] 2026-08-19 | 7e36893 | trope_tutor.html (`locales/ui-strings.csv`) | **MICRO-FEATURE (gate-1 approved on its first offer): review the marks you missed.** The drill already recorded a verdict per question — `answerQuestion` pushes `{key, ok}` into `drill.results` — and `showResults()` then cleared that list **without a single line of code ever reading it** (grep: three sites only, init / push / clear). So the tool knew exactly which marks the student had just missed and threw the answer away, leaving a bare "3 / 10 correct". The results screen now lists them: each missed mark's glyph, its name in the chosen tradition, and a ▶ that replays **the very clip that was playing when it was missed**. A mark missed twice is one row (first miss wins, and keeps its clip); a perfect round shows **no panel at all** rather than an empty one. Read-only by construction — renders from `_lastMissed`, touches no progress/mastery/drill state, so re-rendering on a language change cannot alter what the student earned. **87 combined lines, one tool, ZERO new storage keys, 3 new CSV keys** (Hebrew authored as REVIEW drafts). | verified: 22 checks, 0 failures, 0 pageerrors — fresh profile, light+dark, 1280+800. An all-wrong round lists 9 distinct marks for 10 misses, no duplicates, row count == `_lastMissed`; switching to Hebrew mid-results re-renders all 9 with no raw keys; replay surfaces "That clip wouldn't load." with the audio origin blocked. **The all-correct CONTROL now proves its own premise** (reports a genuine 10/10) before asserting the panel hides — its first version silently scored 5/10 because a timing-driven driver clicked a stale Next button and skipped questions, and **a question that is never answered never enters `drill.results` at all**, which understates the miss list rather than failing loudly

- [x] 2026-08-19 | a2aaa88 | contact.html | **The spam honeypot stops scrolling the Hebrew page 9999px sideways.** `.honey` parked the Web3Forms honeypot at `left:-9999px` — off-screen and unscrollable in LTR, but in RTL the same region is **real scrollable overflow**, so the Hebrew contact page scrolled 9999px at EVERY width, 1280 included. Clipped in place instead (`clip-path:inset(50%)` + legacy `clip`), which is direction-neutral. **A logical property is NOT the fix here** — `inset-inline-start:-9999px` would simply mirror the same bug into English; that is the general lesson (a physical `-9999px` parking spot is invisible in one direction and an overflow bug in the other). Deliberately does **not** reach for `display:none`: the element has to keep working as a honeypot, so it stays in the DOM, stays rendered, and keeps `aria-hidden`/`tabindex=-1`. | verified: before `en 0/0/0/0` vs `he 9999/9999/9999/9999` at 320/390/800/1280 (scrollWidth 11279 on a 1280 viewport); after 0 in all eight cells. 17 checks across en/he × light/dark: still in the DOM, still not `display:none`, clipped to zero area, `name="botcheck"` intact, `elementFromPoint` at its own centre returns something else, absent from the tabbable set, and the four visible fields unchanged. 0 pageerrors

- [x] 2026-08-19 | (S228 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → draft PR #177, which was still OPEN and unmerged (re-checked at 21:03Z: open/draft, `mergeable_state clean`, head `4ccd3b3`, 0 checks, 0 review threads).** No branch restart, no new PR. **Drift check: NOTHING landed outside the loop** — `origin/main` still at `0a1c5e8`, the exact commit the S227 close-out recorded, verified by diffing rather than trusting the version note (the standing S225 lesson, re-honoured). `sw.js` **v515 → v516**; precached files changed: `torah_trainer.html`, `trope_tutor.html`, `contact.html`, `locales/en.json`, `locales/he.json`. **`FONT_MAKER_VERSION` NOT bumped and correctly so** — nothing this session touched the Font Maker. Scripts run: `build-locales.js` (3 keys added, 4372 total), `check-i18n.js` (clean, no new violations), `update-llms-txt.mjs --check`, `update-sitemap.mjs` last. **Decisions deferred to maintainer: none** — gate 1 was asked and answered ("Review what you missed"); the one gate-2 item found (the unlabelled primary tradition name) was logged as a P4 candidate and deliberately NOT changed. Deploy runs on merge to `main`; Pages `success` verified after merge.

- [x] 2026-08-19 | (S229 Pass L — SEO & discoverability, **8th run**, over `9be12f2..HEAD` = **73 commits / 1,943 added lines / 25 files**, the S217–S228 haul no L had seen) | Ran the shape S216 prescribed. **Byte check as a five-minute gate: 11 of 14 pages byte-identical across the entire SEO surface**; the 3 that moved are the ledgered S217 `b0a6bb1` twitter-parity fix, additive only; visible twins 0 changed FAQ/details lines and the CSV 0 changed faq/seo rows — so S216's claims-vs-UI verification carries forward whole. **Mechanical clean at HEAD**: sitemap↔indexable 12/12 exact both directions, canonicals 12/12 with the bare-root homepage, `noindex` on both deliberate exclusions, 36/36 ld+json parse, unique titles+descriptions, one h1 on 12/12, img-alt clean, crawl graph 12/12 by static `<a href>`, robots→sitemap. **`llms.txt` + `llms-full.txt` audited for the first time** (they landed after S216, so L inherited a surface it had never seen): both current, and — the part that mattered — their coverage was verified INDEPENDENTLY of `--check`, since that flag only proves the file matches what the generator would emit today. Counting 38 HowTo steps and 47 FAQ questions against a per-page inventory of the source blocks is what actually catches a regressed page. **Widest arm, and the one that earned the run: closed enumerations.** 1,037 crawler-visible strings → 109 carrying a number or closed list → **10 falsifiable ones checked against the code, all true** (dictionary 7-step tour = 7 TOUR_STEPS; trope 26 marks = 26 TROPES; 10 questions = QUESTIONS_PER_SESSION; three drill kinds = identify/hear/melody; torah 54 parshiyot = 54 manifest keys; FM "up to 10, an 11th drops the oldest" = IV_FONTS_CAP; generator "over 13,000 words" = 13,081 summed; 0–9999 = the real input bounds; resources' 4 age groups + 6 categories = the real `data-filter` sets). **1 HIT, fixed in-session** (c0d413f). **Inverse arm:** 6 of 8 capabilities shipped since S216 are claimed; the 2 that are not are sub-affordances of claimed capabilities and are correctly unclaimed. **2 probe defects killed and 1 candidate refuted before any of them became findings** — an attribute reader that truncated at an apostrophe, an og/twitter arm that re-derived exact equality and reported 28 hits against a deliberate social-card convention (168/168 once rescoped to what must actually hold), and a resources age-group "mismatch" that turned out to be per-resource prose rather than the filter. | verified: every arm printed its own denominator; scripts in the session scratchpad

- [x] 2026-08-19 | c0d413f | Hebrew_Font_Maker.html (+ `llms-full.txt`) | **The structured-data summary admits you can draw, not only trace** — the S229 Pass-L hit, and the registered **(a) closed-enumeration** shape of the JSON-LD↔visible-content parity pattern. `WebApplication.description` enumerated the input paths as "from your handwriting — trace letters, place nikkud and trop, and export…" while the SAME page's HowTo step 1 and its "How do I make my own Hebrew font?" answer both already said a letter can be "drawn right in the browser": one of three parallel claims went stale while its siblings were kept current. **Twin rule honoured as a pre-condition this time**: the sentence was checked for an on-page twin and a CSV row before editing (it has neither — the page's visible tagline is a different, non-enumerating sentence), and `llms-full.txt`, which is GENERATED from this description, was regenerated in the same commit rather than left stale until an unrelated run. Gate-2 asked and approved. **No `FONT_MAKER_VERSION` bump — this is metadata copy, not a feature.** | verified: 4/4 ld+json still parse, the summary now mentions drawing, `update-llms-txt.mjs --check` reports both files current, page loads 0 pageerrors at 1280+800 in both themes

- [x] 2026-08-19 | 9e61489 | trope_tutor.html | **The Learn card says which tradition the big name is in** — closing the S228 Pass-H gate-2 copy candidate. On a cold profile the card showed "Mercha" in large type over "Sephardi: Ma'arich", so the only tradition named was the one the reader had NOT chosen and a first-time tutor could not tell what the headline was. Both lines now carry their tradition and the setting swaps which is which. **No new strings** — the headline reuses the two prefix keys the second line already used, so Hebrew came for free. The 11 marks whose two names are identical show no second line and nothing to disambiguate, so they stay bare. Gate-2 asked and approved. | verified: 17 assertions — 4/4 differing names labelled and the shared one bare; switching to Sephardi swaps both lines and the two never name the same tradition; Hebrew renders אשכנזי with no raw key; a runtime language switch re-renders it; 320/800/1280 × both themes × both languages hold 0 overflow, 0 clipped headline, 0 pageerrors

- [x] 2026-08-19 | e141dbf | torah_trainer.html (`locales/ui-strings.csv`) | **MICRO-FEATURE (gate-1: the recommended trope-tutor seed was declined and the maintainer chose from the bench — this one): step the Torah cycle a week at a time.** The S180 Pass-H seed, and the bench's most-wanted: "his reading is six weeks from now" was unanswerable from a 71-option dropdown with no dates, no marker for the current week, and a single jump that only ever went to THIS week. Two buttons flank the picker, walk the annual cycle one reading at a time, **and name their destination** (hovering › reads "Next reading: Noach"), so the neighbour can be read without being visited. **Deliberately not a second source of truth**: the buttons set the dropdown and dispatch its own `change`, leaving loop-clearing, settings, persistence and the re-render in the one handler that already owned them. A holiday or custom range steps off the weekly parsha the picker still remembers (selecting a holiday never clears `parshahKey`); the cycle wraps, so neither button dead-ends. Arrows reuse the page's existing `.dir-arrow` convention and mirror under RTL. **44 lines, one tool, no new state and NO new storage key** — so no AllTools wiring was needed, which was verified rather than assumed. | verified: 25 assertions across 7 arms (54 weekly readings / 71 options measured as the control; one step = exactly one reading; both ends wrap; holiday steps off the remembered parsha; destinations named in EN+HE and following a language switch with no raw key; glyph mirrors under RTL; Enter works; 30×30 touch floor; 0 overflow and 0 pageerrors at 1280/800/320 × both themes) + a separate fresh-profile arm: stepped reading survives a reload, picker restores to it, label rebuilds, no stray localStorage key

- [x] 2026-08-19 | (S229 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177**, per the protocol's "if the previous loop branch's draft PR is still open and unmerged, continue on that branch and PR" — checked, not assumed: PR #177 was open, draft, unmerged and `mergeable_state: clean` at session start. This is now the FIFTH session on that branch (S225–S229). **Drift check: NOTHING landed outside the loop.** `origin/main` is still exactly `0a1c5e8` — verified by diffing `origin/main` against the position the S228 close-out recorded (0 commits between), not by trusting the version note, per the standing S225 lesson. `sw.js` was **v516** and `FONT_MAKER_VERSION` **5.13** at entry, both matching what S228 claimed. **SW v516→v517** for the precached files this session changed: `Hebrew_Font_Maker.html`, `trope_tutor.html`, `torah_trainer.html`, `locales/en.json`, `locales/he.json`. (`llms-full.txt` is generated and NOT precached, so it needs no bump of its own — but it does need regenerating, which happened inside c0d413f rather than at close-out, because it is derived from the very sentence that commit edited.) **FONT_MAKER_VERSION stays 5.13 — deliberate.** The Font Maker change was its JSON-LD app summary: crawler-facing metadata, not a feature or even a UI string, so it fails the "a Font Maker feature requires a combined bump + changelog entry" test. No changelog `<li>` either. **Scripts run:** `build-locales.js` (4372→**4376** keys; the single pre-existing untranslated key `worksheet.layout.title_default` is unchanged and is NOT this session's debt), `check-i18n.js` clean with the same 14 baselined findings, `update-llms-txt.mjs` (in-commit) then `--check` current, and `update-sitemap.mjs` as the LAST step on a **non-shallow** clone (`git rev-parse --is-shallow-repository` = false, checked before running, per the standing caution about date-based receipts in this container). **Budget: 5 of 5** — 1 pass + 2 fixes + 1 micro-feature@2. **Variety governor: no tool at the cap** (font-maker ×1, trope-tutor ×1, torah-trainer ×1) and one pattern class touched once. **Decisions asked, all three answered — none deferred:** gate 1 (micro-feature) — the recommended trope-tutor "drill only what you just taught" seed was **declined**, and a follow-up question put the four S-sized bench seeds up; the maintainer chose torah_trainer's ‹/› week-step buttons, which shipped. Gate 2 (FM structured-data summary) — approved, shipped. Gate 2 (trope Learn-card tradition labels) — approved, shipped. **⚑ Protocol note for the skill, not a divergence in this session's conduct:** the S229 pointer carried an instruction to "amend the Retired-patterns line for JSON-LD parity when L first runs, since re-check ownership moves A2→L". That instruction is **stale and was correctly not acted on**: the pattern was UN-RETIRED at S190, already carries an ACTIVE Pattern-health line, and its Retired-patterns line already bears the `← UN-RETIRED` marker pointing there. Ownership has sat with L since S142 and nothing needed moving. **Deploy: verified after merge, not in-session** — Pages builds only on `main`. | human to-do: merge PR #177, then confirm the pages-build-and-deployment run concludes `success`

- [x] 2026-08-19 | (S230 Pass K — i18n / localization audit, **15th run**, over `1d57a2b..HEAD` = **66 commits / 721 added HTML+JS lines / 12 files**, the S219–S229 haul no K had seen) | **Both gates clean for the EIGHTH consecutive K** (Check A clean + 14 baselined; Check B clean — the one empty `he` cell is the allowlisted `worksheet.layout.title_default`), so the value was again entirely past the gates — and this run put it in two arms K had never had. **(1) NEW ARM — locale integrity across the whole corpus.** 4,376 rows / **4,375 en↔he pairs** checked for placeholder integrity, plural pairing, affix parity, identical-to-English and Hebrew-presence, with a **4-defect control that fired 4/4 before any verdict was read**. **Raw 236 → 1 real.** placeholder 21 → **0** (every one a `.one` plural idiomatically dropping `{count}` — “מילה אחת” *is* “one word”; nothing substitutes, nothing breaks); plural 3 → **0** (`dictionary.pos.other` and `worksheet.realwords.pos.other` are a part-of-speech VALUE named “Other”, and `worksheet.modes.practice.one` is the “1-Letter” MODE — the `.one`/`.other` suffix heuristic collides with legitimate key names); identical 67 → **0** (licence ids, brand names, bare numerals, pure `{a} ({b})` format strings, symbols — and `flashcards.header.home` = `←` is `.dir-arrow`-wrapped, so CSS mirrors it under RTL); affix 145 → 63 after normalising the classes proven correct (placeholder braces, geresh ׳ / gershayim ״ vs ASCII quotes, RTL arrow flips, bidi marks) → **0 real** (maqaf, sof pasuk, nikkud inside glyph names, ordinary translation variance). **(2) NEW ARM — inline-markup parity**: tag-set + `<a href>` count per row across the **69 rows carrying inline markup in either column**. **1 hit, and it is real** — `dictionary.translit.credit`, fixed this session (entry below). **(3) The documented blind spot (“Pass K run 2”, logged since S177 and never run): CLOSED with receipts.** 7 shapes (template-literal attribute, template-literal element text, `setAttribute`, property assign, `textContent`, `alert`/`confirm`/`prompt`, toast helpers), **control 7/7 fire** on synthetic samples before the real run. Over the K-unswept delta: **0**. Over **full HEAD — 65,384 lines / 15 files: 4 raw, and all 4 are the ledgered printed-output exemption** (`'Not placed: '` inside the crossword *sheet* builder, plus the QR answer-key banner / brand / caption). The ledger's “Font-Maker-heavy Pass-K-run-2 backlog” is **stale**: it was worked to zero across S177/S192/S207/S218 and nothing since re-introduced it. **(4) THE CONTRACT FINDING — the run's methodological headline, and it invalidates its own pointer.** The S230 pointer sent K after “seven un-reviewed Hebrew drafts”. Grepping the exact string `REVIEW: HE draft` gives 21; a case-insensitive `/REVIEW/` over the notes column gives **220 rows — 5.0% of the corpus — in 90+ distinct marker shapes**. An order-of-magnitude jump is exactly the S229 carry's trigger: **suspect the CONTRACT before the product** — and the contract was wrong. `REVIEW:` is a **provenance annotation** (“this Hebrew was drafted by the loop, not reviewed by a native speaker”), not a work queue: 45 rows ask “Hebrew letter/vowel name vs. keeping the transliteration”, a pinned-content question CLAUDE.md already settles; ~50 are trope pronunciation names CLAUDE.md says **never** translate; 23 are legal copy explicitly awaiting a human legal reviewer; and one literally reads `REVIEW: maintainer-approved HE`. **No future K should read the REVIEW count as a backlog, and no future pointer should schedule K on it.** | verified: both gates re-run; two new detectors each run against a control that exercises the counting step; denominators printed on every arm

- [x] 2026-08-19 | 63b15bf | hebrew_dictionary.html (`locales/ui-strings.csv`) | **The transliteration credit links to the library again in English** — the S230 Pass-K hit, and the only one across the 69 rows carrying inline markup. The attribution line is a `data-i18n-html` key, so `applyStaticI18n` overwrites the page's own markup with the CSV string on every load; the **`he` cell carries the `<a>` to the hebrew-transliteration repo and the `en` cell was plain text**, so in the default UI language the link was destroyed at load and an open-source attribution rendered as dead prose for nearly every visitor while Hebrew readers kept it. Measured before touching anything: **4/8 arms (en × light/dark × 1280/800) had no link, 4/8 (he) did; after, 8/8, 0 pageerrors**. The visible words are unchanged — only markup the page already ships was restored. The static fallback moved `--gold` → `--gold-text` on the same link so all three copies agree: that variable exists precisely because `--gold` is ≈2.5:1 on light and **fails WCAG for small text**, the Hebrew cell already used the accessible one, and the pre-JS path should not be the single contrast failure. | verified: 8-arm runtime probe before and after; markup-parity arm re-run to 0/69

- [x] 2026-08-19 | 3fc6bda | the four folder-tree carriers (generator, flash_cards, dashboard, index) + `locales/ui-strings.csv` | **The Hebrew empty state says which list is empty — gate-2 asked and approved ("name the list").** Every saved-item list shares one folder-tree component whose empty line was a single interpolated string, `“No saved {noun}s yet.”`. English filled it with preset / profile / schedule and read correctly; **Hebrew could not** — the noun must agree in gender and number with the adjective (הגדרות שמורות feminine, פרופילים שמורים masculine) — so the `he` cell dropped the token and said "no saved items yet" over all three lists. One interpolated row became **four keyed rows**, and the shared block now picks its key from `cfg.noun` **against a closed list with an `item` fallback**, so the lookup is total and an unknown noun can never render a raw key on screen. The Hebrew nouns are the ones the panels already use, not new coinages. **The line stays byte-identical across all four carriers of the `═══` shared block — hashed in each file before and after.** | verified: 24/24 runtime arms (3 tools × en/he × light/dark × 1280/800) render the right sentence, 0 raw keys, 0 pageerrors; gates clean

- [x] 2026-08-19 | 6eb47bb | Hebrew_Font_Maker.html | **The Font Maker stops scrolling sideways on a phone** — the queued no-gate candidate the S228 and S229 pointers both carried, and the last page in the suite that did it. **Attributed before proposing anything, exactly as the candidate demanded, and the attribution changed the shape of the fix**: 413px EN / 269px HE was NOT one unwrapped row but **three flex rows, measured one lever at a time**. (1) `.cat-tabs` was `nowrap`, so the category strip floored the whole workspace at its min-content width — **296px of the 413 on its own**. (2) `.app` becomes `flex-direction:column` at ≤820px but kept `align-items:flex-start`, so every child sized to its own content instead of the column — *that* is why `.workspace`'s `flex:1` + `min-width:0` were inert. (3) `.wtab-group` is `flex:0 0 auto`, i.e. explicitly refuses to shrink, so four step tabs held **417px open inside a 280px parent**. Result **413 → 8px EN, 269 → 0px HE**. **Inertness measured, not asserted** (the S228 standing carry): geometry of ten containers plus every step and category tab, captured with the three declarations forced back to their old values and with them live — **identical in 4/4 desktop arms at 1280px**; the 800px arms differ, which is the point, and the difference is also an improvement (two category tabs were being squeezed ≈14% below natural width to fit one row). **No `FONT_MAKER_VERSION` bump — a reflow fix, not a feature.** | verified: lever-by-lever attribution, 8-arm inertness proof, before/after screenshots at 320px

- [x] 2026-08-19 | e01ce03 | Hebrew_Font_Maker.html | **The footer stops holding the Font Maker 8px too wide — and the suite's 320px reflow thread is now CLOSED.** The footer lays out `grid-template-columns:1fr 1fr 1fr` with no responsive collapse, so at 320px each column gets ≈79px and the *Related Hebrew Tools* nav — whose links cannot break below 81px — pushed the document 8px past the viewport. `repeat(auto-fit,minmax(220px,1fr))` folds the row only when three columns genuinely stop fitting (below ≈730px) and is otherwise the same layout: **footer, nav and per-column geometry identical in 6/6 arms at 1280/1024/800 in both languages, still three equal columns, still mirrored under RTL**. **Four other pages carry the same inline footer grid and none of them overflow** — the Font Maker tips over because its footer padding is 18px a side rather than 14px; left alone on the one-concern rule and logged. **Suite-wide receipt: 13 pages × en/he = 26 arms, 0 horizontal overflow at 320px, 0 pageerrors** — closing the WCAG 1.4.10 (Reflow, AA) thread the loop has pulled since S213. | verified: suite-wide 26-arm sweep + 6-arm footer inertness proof

- [x] 2026-08-19 | (S230 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177** — checked, not assumed (`pull_request_read`: open, draft, `merged:false`, `mergeable_state: clean`, head `62d2d1d` = the S229 push). This is the **SIXTH** session on that branch. **Drift check ran the diff the standing lesson demands, not the version note:** `git diff 0a1c5e8 origin/main` is empty, `sw.js` was `v517` and `FONT_MAKER_VERSION` `5.13` exactly as the S229 pointer recorded — **nothing landed outside the loop.** **`sw.js` v517 → v518** in this final commit; precached files changed this session: `hebrew_dictionary.html`, `Hebrew_Font_Maker.html`, `flash_cards.html`, `hebrew_blend_generator.html`, `classroom_dashboard.html`, `index.html` (+ `locales/*.json`, `locales/ui-strings.csv`). **No `FONT_MAKER_VERSION` bump** — both Font Maker changes are CSS reflow fixes, not features (same call as S229). Scripts run: `check-i18n.js` (clean, 14 baselined) ×3, `build-locales.js` (4,376 → 4,379 keys), `update-llms-txt.mjs --check` (current — no title/description/JSON-LD changed), `update-sitemap.mjs` **last**, on a non-shallow clone. **Gates: 1 asked and answered — gate 1 “skip the micro-feature, three fixes instead”; gate 2 “name the list” (shipped as 3fc6bda).** **Decisions deferred to maintainer: none.** **Protocol divergence flagged for the skill: none — but see the S230 pointer's warning about the REVIEW-marker contract, which is a ledger-bookkeeping fault, not a skill fault.** **Date note for a future E pass:** the session ran on 2026-08-19 and every ledger entry carries that date, but the last two commits landed just after 00:00 UTC, so `sitemap.xml` correctly shows **2026-08-20** for `Hebrew_Font_Maker.html`. That mismatch is real history, not staleness — do not "fix" it. Deploy `success` is verified **after** the maintainer merges.

- [x] 2026-08-20 | (S231 Pass G — print & export fidelity, `classroom_dashboard.html`'s FIRST dedicated G; 55458c0) | **11 arms of real PDFs** (Playwright `page.pdf` → PyMuPDF fill/text/render extraction; Letter AND A4, EN+HE, light+dark source, populated/empty/14-period/forced-30-period weeks, plus the plain-Ctrl+P board ×3) | **The weekly Schedule Sync sheet's core promises all hold with receipts:** class colors survive the print dialog's drop-backgrounds default (all four seeded hexes present as fill rects at printBackground:false — `print-color-adjust:exact` earns its keep); **white cell text backed 10/10 by printed color fills, 0 unbacked** (span-bbox∩fill-rect, not assumption); 1 page on Letter AND A4 for realistic weeks; dark source prints light; the HE sheet fully mirrors (days RTL, times right); a forced 2-page sheet breaks rows WHOLE; an empty week prints heading+hint, not a blank page. **Two hits, both filed before fixing** (closed below). **Two probe faults caught before any number was believed:** `page.pdf()` honors an `emulateMedia('screen')` left active — the first 10 PDFs were silently screen-media renders, and the tell was print-hidden chrome's text in the extraction; and the placeholder verify's first run used a different fixture than its before-capture, making the check vacuous (`screen ::before = none` was the tell). | verified: every arm 0 pageerrors; denominators printed per arm; both detector faults fixed and re-run before any verdict

- [x] 2026-08-20 | 482047f | classroom_dashboard.html | **A missing-class cell no longer prints as a near-black slab from dark mode** — the S231 Pass-G P3, found by a fill-color diff between the dark and light print arms (`#0d1220` appeared in exactly one). Assigned cells carry inline colors that outrank the dark theme; a cell naming a deleted preset has none, so `body.dark .swm-cell`'s navy-deep won and `print-color-adjust:exact` faithfully shipped it to paper with remapped-navy text at 1.1:1 — the which-class-is-missing information was unreadable ink. One print-block rule re-asserts the hatch over a remapped light base (ties the dark rule's specificity, wins by order). | verified: dark-arm PDF fill set now IDENTICAL to the light arm's; all 58 unrelated fill rects byte-identical in light; dark SCREEN rendering untouched (computed bg unchanged — the rule lives under @media print); 0 pageerrors

- [x] 2026-08-20 | 1829b59 | classroom_dashboard.html | **The board print no longer carries the "Click to add a message…" edit placeholder** — an instruction to click, on paper; same family as the already-print-hidden `.dash-edit-pencil`, and the fullscreen (projected) view already suppresses it. The print rule mirrors the base rule's selector exactly (`:empty:not(.editing)::before`) so it ties on specificity and wins by order — **a first draft at lower specificity measurably did nothing, and only the re-verify caught it** (the base rule's `:not(.editing)` outweighs a bare `#id:empty::before`). | verified: placeholder present in the before-PDF / absent after, token multiset otherwise identical (order-insensitive), screen placeholder intact, 0 pageerrors

- [x] 2026-08-20 | 5c5422d | flash_cards.html + hebrew_blend_generator.html + hebrew_dictionary.html + resources.html | **The four remaining hard `1fr 1fr 1fr` footer grids converge on the Font Maker's proven `repeat(auto-fit,minmax(220px,1fr))`** — the S230 P4 queued as needing no gate. None overflowed at 320px, but only by ~8px of footer padding, and the columns squeezed to 75–90px slivers on phones. All five footer carriers now share one form. | verified: 24/24 wide arms (1280/1024/800 × en/he × 4 pages) pixel-identical before/after; at 320px the columns fold to full-width 288px rows, overflow 0→0 in both languages; 0 pageerrors across 64 captures

- [x] 2026-08-20 | eb8f078 | contact.html (+ resources.html measured) | **The S227 sub-element internal-overflow backlog is closed — half by a fix, half by a measurement.** Contact's `.cs-grid` +4px attributed lever-by-lever: the PayPal/Venmo/Zelle segmented capsule is the donate tile's min-content floor (238.1px with the 640px-media 16px pills — found by forcing the tile to 150px and seeing what refused), so the card painted 4px past its track, sitting closer to the screen edge than every other card. Two fewer px per pill side, scoped to a NEW `max-width:340px` block so nothing above the defect zone moves (wrapping was rejected: the capsule is a single 999px-radius segmented control and must not break onto two lines). **Resources' `<ol>`/`<li>` half was measured already-clean in both languages BEFORE any edit** — resolved by intervening work since S227; the receipt is the internal-scroller scan reporting zero on the page. | verified: 320px en+he internal-scroller scan 0 on both pages; capsule one row at 320/339/341/640/1280; padding 14px only at ≤340; 0 pageerrors

- [x] 2026-08-20 | (S231 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177** — checked, not assumed (`pull_request_read`: open, draft, merged:false, mergeable_state:clean, head `2364aba` = the S230 push). SEVENTH session on this branch. **Drift check ran the diff, not the version note:** `git diff 0a1c5e8 origin/main` empty, `sw.js` v518 and `FONT_MAKER_VERSION` 5.13 exactly as the S230 pointer recorded — **nothing landed outside the loop.** **`sw.js` v518 → v519** in this final commit; precached files changed this session: `classroom_dashboard.html`, `flash_cards.html`, `hebrew_blend_generator.html`, `hebrew_dictionary.html`, `resources.html`, `contact.html`. **No `FONT_MAKER_VERSION` bump — nothing touched the Font Maker at all.** Scripts run: `check-i18n.js` (clean, 14 baselined; zero new strings this session — CSS comments only), `build-locales.js` correctly NOT needed (no CSV change), `update-llms-txt.mjs --check` current (no title/description/JSON-LD changed), `update-sitemap.mjs` **last**, on a non-shallow clone. **Gates: 0 asked — and one PROTOCOL DIVERGENCE flagged for the maintainer:** gate 1 (micro-feature or skip) was not asked at the post-pass batch point; the session spent its budget on the pass's own two findings plus the two queued no-gate backlog items, defaulting to the S230 answer ("fixes instead") without confirming it. The unattended-fallback default is the same outcome, but an attended session should have asked. **Decisions deferred to maintainer: none.** Deploy `success` is verified **after** the maintainer merges.

- [x] 2026-08-20 | (S232 Pass D — performance snapshot, trope_tutor.html, its 3rd dedicated D; no perf code) | **Pass D on the stalest D target of all seven tools (last S138, ~94 sessions) — the S138 baseline HOLDS through everything shipped since (the S198 print chart, S202 panel, S228 missed-marks review, panel-collapse memory). Effectively CLEAN.** Cold (8 arms, 1×/4× × light/dark × 1280/800): ready 185–877ms, 0 longtasks >200ms anywhere (max 107 @1× / 159 @4×; S138: 76/147), 744 nodes (S138: 762), 0 pageerrors. Interactions: renderLearn 0.9–2.1ms/family @1× / 4.4–10.8 @4× (inside the S138 envelope); 20× family cycle longtask-max 67ms @4×; **buildPrintChart FIRST-EVER profile — 8.9ms @1× / ~50ms @4×, 1,375 print-host nodes, and the `_chartPrintBuild` mastery guard PROVEN (progress.families 6→6, live cards restored)**; identify-drill worst step 134ms @4× incl. harness IPC; tradition/language switches ≤9ms JS (worst follow-on re-render task 122ms @4×); idle 3s = 0 mutations / 0 node delta / 0 longtasks (projector-stable). **Failure path (audio route-aborted, default question types): NO dead-end** — 4×/EN and 1×/HE both reached the results screen with localized toasts (32–96/session is the documented substitute→regenerate design, bounded by regenDepth; 0 longtasks >200). **NEW METHOD LESSON: work driven via CDP `Runtime.evaluate` emits NO longtask entry** — the busy-loop probe control caught it (a 300ms loop invisible via evaluate; the same loop via setTimeout or a real click registers 300/303ms). Cold-load numbers stand (boot tasks are page-scheduled, 3–5 caught per arm); interaction longtask capture must drive REAL input events (page.click) — evaluate-timed performance.now() deltas remain valid wall time. | run: 2026-08-20, S232

- [x] 2026-08-20 | 0e8a8eb | trope_tutor.html (`locales/ui-strings.csv`) | **MICRO-FEATURE (gate-1 chosen on its THIRD offer — the offer the standing rule said must be its last): "Drill only what you just taught."** A "Drill which marks?" select on the drill start screen (All marks + the six families) scopes the ANSWER pool only — distractors stay global (already same-family/confusable-weighted), so a 3-mark family still fills four choice buttons. Rides the settings blob as `drillScope`, validated on every read (settings blobs travel through AllTools imports) — ZERO new storage keys, backup carries it for free; a scoped family that a future index rebuild empties disables Start with an inline note instead of dead-ending (unreachable today: every family has ≥3 drillable marks; geresh_muqdam(0) is the only example-less mark). 68 lines / 4 files / 3 CSV keys (HE REVIEW-marked). **23 checks:** fresh-profile defaults (light/1280 + dark/800), scoped drills 10/10 in-family for katon AND segol (asserted on the `.good` button's dataset.key), 4-choices-per-question guarantee, reload persistence, **the S228 pagehide flush surviving a scope change + IMMEDIATE reload inside the 300ms debounce**, bogus-stored-value → All fallback, **an injected-index gate arm** (segol emptied via route-fulfill → Start disabled with the scoped note; real-index control startable), EN↔HE relabel keeping the selection, 0 pageerrors. Two fixture faults caught by the standing probe-control carry: addInitScript re-seeds localStorage on RELOAD (a persistence test that re-seeds is vacuous — seed-once guard), and a post-setLang read raced the onChange re-render (settle wait). | fixed: 2026-08-20, S232

- [x] 2026-08-20 | 89c2d18 | classroom_dashboard.html | **The missing-class hatch shows on dark screens too** — the S231 Pass-G screen-half candidate, queued no-gate. `body.dark .swm-cell` (0,2,1) outranks bare `.swm-cell-missing` (0,1,0), so a dark screen showed a deleted-preset cell as solid navy-deep (dashed border + muted text the only cues). Re-asserted the hatch for `body.dark .swm-cell-missing` with the SAME tokens (theme-remapped: warm-gray stripes over a navy-deep base) — the screen sibling of the S231 print fix, same tie-and-win-by-order tactic; `body.dark .swm-cell-free` two lines up was the local precedent. Verified before/after: dark screen gains the gradient, light screen byte-identical, assigned + free cells untouched in both themes, and the @media print rules keep winning their tie by source order (hatch-over-cream on paper from BOTH themes, asserted under print emulation). 0 pageerrors. | fixed: 2026-08-20, S232

- [x] 2026-08-20 | (S232 measurement, no code) | suite-wide | **The S213 P3 "two-dimensional scrolling at 320px" (WCAG 1.4.10 Reflow AA) is CLOSED.** S213 measured trope_tutor 394/320, torah_trainer 477/320, hebrew_dictionary 378/320, flash_cards 336/320; the S222–S230 campaign fixed the class carrier-by-carrier (header-row wraps → chrome pages → torah's last 11px + the contact honeypot → the Font Maker's four-cause 413px), and the suite-wide 26/26 receipt has existed since S230 — the candidate simply outlived it. Re-receipted this session over the S231 footer convergence + the S232 feature: **13 pages × EN/HE = 26 arms at a 320px viewport, docOverflow 0px and 0 pageerrors on EVERY arm** — the horizontal-overflow pattern's second consecutive clean suite sweep (streak 2). | verified: 2026-08-20, S232 (reflow320.mjs)

- [x] 2026-08-20 | (S232 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improve-loop-bkos51` → the SAME draft PR #177** — checked at session start: open, draft, unmerged, mergeable_state clean, head c6f3b8a (S232 is the eighth session on this branch). **Drift: NONE** — `origin/main` still at `0a1c5e8` (the PR's base), sw v519, FM 5.13, all matching the S231 record. **Iters: 1 pass (D) + 1 micro-feature (2) + 2 fixes = 5 (full budget).** **Gate 1 ASKED at the post-pass point exactly as the S232 pointer directed — the S231 divergence is SETTLED**: the maintainer chose the trope-tutor drill-scope seed on its third offer (the offer-or-strike rule is discharged by SHIPPING, 0e8a8eb). No gate-2 (the feature's 3 labels are its own copy, HE REVIEW-marked per the shipped-seed convention; no existing copy changed) and no gate-3 (the stripe is a small refinement). **`sw.js` v519→v520** (precached files changed: trope_tutor.html, classroom_dashboard.html, locales/en.json, locales/he.json). **FONT_MAKER_VERSION stays 5.13** — no Font Maker change. **Scripts: build-locales (3 new keys), check-i18n clean (Check A gate + Check B), update-sitemap.mjs run LAST on a non-shallow clone**; llms.txt NOT regenerated (no title/description/JSON-LD change). **Stale-marker sweep — THREE candidates found already-fixed and flipped with receipts** (which-ones→7e36893 same-session; trope-toggle→26b9e7e, 4 sessions stale; S213 reflow→the campaign, receipt since S230): when grounding a candidate, `git log -S` its subject before coding. **Done overflow: the S227 block archived** to IMPROVEMENT_ARCHIVE.md. **Nothing deferred to the maintainer.** Deploy `success` verified after merge, per the branch protocol.

<!-- archived 2026-08-23 at S245 close-out: Done overflow, S240 and earlier -->
- [x] 2026-08-22 | (S240 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-mobile-support-pass-ifhznq` → the SAME draft PR #192** — checked at session start via the API before assuming: OPEN, draft, unmerged, `mergeable_state` clean at head 8731708. So no new branch and no new PR; **#192 now carries two sessions (S239 + S240, 10 commits).** **Drift check: NONE.** `origin/main` unmoved at `ba91468` since S239 started — the first quiet inter-session window in a while (S239's own entry records the opposite: 15 sw bumps and 9 FM releases outside the loop). Verified by fetch + `merge-base --is-ancestor`, not from the pointer's note; the clone arrived FULL this time (checked with `rev-parse --is-shallow-repository` before any date-based receipt). sw was v544, FM 5.24. **Maintainer direction this session: "focus on any queued work on fontmaker"** — honoured, and the FM actionable queue is now EMPTY (see the pointer). The pass was NOT skipped (no direction to); E is site-wide and covered the FM surface, and the run added two never-before-run dimensions on exactly that surface (Starting-Fonts, FM version↔changelog). **Variety governor respected in full and stated transparently:** font-maker took 2 iterations (AT CAP), `sub-floor touch target` took 2 (AT CAP), generator 1. The CLAUDE.md doc fix is counted as docs, not as a tool iteration — it edits prose about the Font Maker, not Font Maker code. **Decision gates: 1 asked, 1 answered, 0 deferred.** Gate 1 → the maintainer chose "clear the FM defect queue" over two seed slices, so no micro-feature ran. Gates 2/3/4 were never reached; notably the two fixes that could have tripped gate 3 (FM's 15x15 `.fld-help`, the generator's 16x52 sidebar tabs) were deliberately built as invisible hit-area expansions so that no visible chrome moved. **SW v544→v545** for the two precached files that changed (`Hebrew_Font_Maker.html`, `hebrew_blend_generator.html`); CLAUDE.md, the ledger and `sitemap.xml` are not precached. **FONT_MAKER_VERSION stays 5.24 — deliberate:** both FM commits are pure fixes (a touch-target CSS floor and an a11y announcement with zero new copy), and CLAUDE.md's rule bumps on a *feature*, not on fixes. **Scripts run:** `check-i18n` (clean, no NEW violations — 0 CSV keys added this session, so `build-locales` was correctly not needed), `check-inline-js` (62 inline + 36 JSON-LD blocks parse — run after every FM/generator edit, not just at the end), `update-llms-txt --check` (both files current), `update-sitemap.mjs` as the final step. **Deploy verification happens AFTER the maintainer merges** — Pages deploys on merge to `main`, not from this branch. | verified: PR state via API at session start; every fix carries its own multi-cell receipt above

- [x] 2026-08-22 | (S240 Pass E — freshness & site health, **21st run**, over `4ccd3b3..HEAD` = **121 commits / 33 files / +6,114 −569** — the whole S228–S239 haul PLUS the outside-loop surface no E had ever seen: the Starting-Fonts data layer, FM v5.16→v5.24, `scripts/check-inline-js.mjs`, resources.html's new entry. **12 dimensions, 11 CLEAN, 1 hit — and TWO dimensions that had never been run.**) | whole repo | D1 sitemap lastmod **12/12** exact (control: a fake 1999 date IS detected). D2 **402** internal refs 0 broken (identical to S227's count). D3 precache both directions — 26 entries, 0 absent, `i18n-test` correctly out. D4 **21/21** CSP hosts licensed at product level, extraction control printing per-page non-zero counts. D5 **all eight shared `═══` blocks single-sha** (app-toast JS 4, app-toast CSS 4, ivritsuite-fonts 9, uploader 7, panel-collapse 6, folder-tree JS 4, folder-tree CSS 3, TROPES 2) **plus the `.ivrit` engine's documented 3-identical-plus-adapted-index shape CONFIRMED** (4 carriers, 2 shas, the 3-set being gen/dash/flash) — each with a minimum-slice-length floor and a membership control. D6 **0 animating under `reduce` on 13/13** with a firing control on 11 (403/684/215/148 on the big four) and privacy/terms the documented blind zeros; 0 pageerrors over 26 loads; **trope_tutor, the page CLAUDE.md flags as risky because it enumerates rather than neutralizing universally, is still complete** (0 under reduce, 68 with it off). D7 apple splash 13/13 pages one sha, 18 tags each, mirror 18, 18 PNGs, 0 dangling / 0 orphan. D8 8 `?v=` fetches, 0 stale. D9 CLAUDE.md re-verified against its own numbers — panel-collapse 6, app-toast 4, ivritsuite-fonts 9, uploader 7, tours 7, root pages 14, reduced-motion carriers 11 all correct; **the FM line count is the run's one hit** (below). D10 pockettorah **432/432 case-exact, 0 unreferenced**, with a control proving case-sensitivity actually fires (`BERESHIT-1.TXT` rejected) on a corpus that really is mixed-case (`Bereshit-1.txt` vs `noach-1.txt`). **D11 (NEW — the Starting-Fonts data layer, never E-audited): schema 1, 2 fonts, both TTF + LICENSE.txt present and non-trivial, 2/2 ids carry a live `?start=` link in the generated LINKS.md, RFN recorded on 2/2 per the addOSFont rule, manifest network-first in sw.js and NOTHING under `starting-fonts/` precached — the documented contract holds exactly.** **D12 (NEW — FM release discipline): `FONT_MAKER_VERSION` 5.24 agrees with the newest changelog `<li>`, across 85 entries** — the one-combined-bump rule survived nine outside-loop releases. Repo gates all green: `check-i18n` clean, `check-inline-js` 62+36 blocks parse, `update-llms-txt --check` current. | verified: every dimension carries its own control; the checker-defect harvest is logged as a candidate

- [x] 2026-08-22 | f3e0b18 | Hebrew_Font_Maker.html | **Two sub-floor controls reach the 24px touch floor** (`sub-floor touch target`, the S238 Pass-A FM residue) — the vertical reference/image opacity sliders were 22px wide (width is the cross axis for a `writing-mode: vertical-lr` range, so this is the `3ae6891` pattern rotated), and `.fld-help`, the `?` that opens the licence and OS-lock modals at four sites, is a real 15x15 `<button>` that the hover-tip inline exception does not cover. **`.fld-help` got the box without being repainted** — a transparent `::after` at `inset:-4.5px` is the pointer target, so no visible chrome moved and no gate-3 question was needed. FM stays 5.24 (pure fixes). | verified 4 cells (light+dark x 1280+800): badge painted 15x15 unchanged in all four, 8/8 probe points at ±11px resolve to the button with a CONTROL at ±18px correctly resolving to nothing 4/4, both vsliders measured 24x150 **on the real path** (wizard completed, workspace rendered) rather than 0x0, 0 pageerrors. The first attempt's `::after` inherited `border-radius:50%`, making the target a circle whose corners failed 0/4 — caught by the corner probe, squared off

- [x] 2026-08-22 | 6f27e91 | Hebrew_Font_Maker.html | **The Draw move tool announces how many strokes it selected** (the S202 Pass-C candidate, closed exactly as it prescribed). Marquee-selecting updated a visible `.readout` and nothing else. That readout lives inside `renderDrawLeft`'s `ctxHtml`, rewritten by `innerHTML` every render, so `aria-live` on it would be destroyed and recreated each time — the very reason the node editor's `#nsSel` sits in static markup. Shipped as that idiom: a persistent `#drawSelLive` beside `#nsSel`, a `.a11y-live` clip-path helper (the file had none), and a diff-guarded `textContent` sync. **Zero new copy** — it reuses the already-translated `fontmaker.draw.sel_count.one/.other`. FM stays 5.24. | verified 5 cells (light+dark x 1280+800 + `?lang=he`) on the real path — wizard with the Draw source, two strokes drawn with actual pointer events, both caught by an actual marquee drag: "2 strokes selected" / "2 קווים נבחרו" announced in all 5; **the diff guard PROVEN by MutationObserver — 0 mutations across 3 no-op re-renders**; Esc empties it; exposed to AT (display block, visibility visible, no aria-hidden, aria-live=polite, painted 1x1); 0 pageerrors

- [x] 2026-08-22 | 48cd7d4 | CLAUDE.md | **Pass E's one hit: the Font Maker's growth claim was extrapolating ~9x low.** The doc said "~15,700 as of 2026-08" growing "roughly a thousand lines a month"; it measured **18,403**. **This is NOT a repeat of what `323cd25` forbade** — that commit deliberately replaced a bare count with a dated snapshot PLUS a growth rate so nobody would re-fix the number every few weeks. The number aged exactly as designed; **the extrapolation mechanism is what broke** (+2,700 lines in the nine days from 2026-08-13, ~9x the stated rate, so a reader following the doc's own instructions lands 2,400 lines wrong). Fixed by keeping that intent and dropping the part that cannot survive this file: three dated datapoints, an explicit "growth is uneven", and a pointer to measure rather than extrapolate. Not precached — no sw bump of its own. | verified by measurement (`wc -l` = 18,403 at that commit); the pass re-verified CLAUDE.md's seven other stated counts in the same run, all correct

- [x] 2026-08-22 | 06c50fb | hebrew_blend_generator.html | **The generator touch cluster is CLOSED — slice 2, the three JS-inline shapes** (`sub-floor touch target`, 2nd fix this session — AT THE 2/PATTERN CAP). `.vowel-group-header` x7 at **14px**, the smallest control in the suite, styled by `cssText` in `initVowels` with no CSS rule → `min-height:24px`; `.quick-tour-link` 19px, a standalone button so the 2.5.8 inline exception does not apply → the S102 declarations at 30px; and the 16x52 `.sidebar-toggle-btn` → **a transparent `::after` carries the target 8px inward to 24px with the sliver look untouched**. **The grounding found an unnamed twin:** `.sidebar-show-btn` is the identical shape, so fixing only the candidate's one would have left the other under the bar — both are covered by one rule. **24px not 30px for the header is deliberate**: the pattern's own definition scopes the 30px house bar to the `.btn-xs` family, and 30 would add 112px to one panel for 6px more than WCAG asks. **Inward-only + LOGICAL insets is load-bearing, not style** — show-btn is viewport-pinned, and the side it must grow toward flips in Hebrew. | verified 8 cells (light+dark x 1280+800 x EN+HE) with the candidate's three mandatory guards: 7/7 headers at 24px incl. Hebrew's longer labels (no wrap), quick-tour-link 30px, tab painted still 16px with the pointer registering at +4px and +7px inward and **a CONTROL at +12px correctly failing** (so the target is exactly 24), RTL mirroring measured rather than assumed, page overflow **0px in all 8**, 0 pageerrors; twin re-checked in both languages (x=0 in EN, x=1264 in HE, target fully on-screen in both)

- [x] 2026-08-22 | **⚑ Out-of-band feature request: ײַ (U+FB1F, pasekh tsvey yudn) joins the Font Maker's Yiddish tab** | branch `claude/fb1f-yiddish-pasekh-5uzv51`, `bcb4291` | Maintainer-supplied plan prompt, not an `/improveloop` session — so no `S<N>` per-session-log line. ײַ is the standard YIVO spelling of the /aj/ diphthong and a known OCR failure point, so historical Yiddish text routinely encodes it as **raw U+FB1F**; fonts built here had no glyph there and such text rendered as tofu. Before this, `FB1F` appeared **exactly once in the whole repo** — in the `SPECIAL_MORE` comment listing the six forms left out of the form tables. **Three verification questions the prompt posed, answered before any code moved.** (1) *Does `05F2+05B7` already work?* Position yes — `builtWide`'s FEA pass maps the donor anchor through `wideX` and the pre-change baseline emits `pos base uni05F2 <anchor 170 -45> mark @MC_BELOW`. Shape **no**: `builtPerLetter` iterates `project.letters` only and 05F2 is a *precomposed target*, never a letter, so a pasekh after ײ could only attach as the **global `uni05B7`**, silently dropping a per-letter custom pasekh — verbatim the gap `builtShinDagesh` closes for FB2C/FB2D. So the change is **not purely additive**, and the changelog says so. (2) *Normalization* — measured, not assumed: NFC(FB1F) = `05F2 05B7`, i.e. FB1F is on the **composition-exclusion list and NFC actively DECOMPOSES it**, never recomposing. Normalized text therefore never contains FB1F ⇒ the cmap entry (raw/legacy bytes) and the ccmp rule (typed/normalized sequence) are **both** load-bearing, neither sufficient alone. (3) *`UseMarkFilteringSet`?* **No** — pasekh is ccc18 and the only marks sorting before it (sheva, the three hatafs, hiriq, tsere, segol, ccc10–16) are all vowels, which ײַ never carries a second of; cantillation (220/230), dagesh (21), meteg (22) and rafe (23) all sort after, so the pair stays adjacent. A plain adjacent rule is correct; `ccmp_dagesh`/`ccmp_shinsin` need a filtering set precisely because dagesh ccc21 and the shin dots ccc24 sit **above** the vowel range. **Build:** `YID_PASEKH` declared beside `YIDDISH_FORMS` but deliberately kept **out of `project.precomposed`** (a `{bases,mark,wide}` entry there would be caught by `builtWide` and baked WITHOUT its mark) and out of the tables (a fourth entry shape would thread through `extraTargetsFor`, the tile renderer, the anchor editor and the Width slider); its own derived bake pass after `builtWide`; the wide-anchor mapping extracted into a shared **`wideDonorAnchors()`** beside `dropKeyPins`/`mapPinsX` so bake and FEA cannot drift — the house convention those two already follow. Tile on the Yiddish panel gated on `nikkud.enabled` (the same gate the bake uses, so the tab never promises a glyph that will not ship), clicking routes to ײ (untraced → jumps to yud; traced → Width slider + the read-only `legend_wideform`); QA build-check entry; the stale exclusion comment re-trued. **Two defects caught that the prompt did not name:** the ccmp block's gate keyed on `builtPrecomposed`/`builtPerLetter` alone, so with nikkud on and yud traced but no BeGeD-KeFeT base traced **both are empty and the FB1F rule would have silently vanished**; and the Hebrew changelog is `CHANGELOG_ORDER` + `CUR_KEY` driven, so an English-only `HELP_CONTENT.about` entry is invisible to non-English readers — the very trap that lost v5.10–v5.13 (the previous commit was a backfill of two of them). Both fixed. **Verified at export-spec level** (Pyodide/harfbuzz CDNs are unreachable in-container, so `buildFontSpec()`/`buildFEA()` were driven directly rather than compiling a TTF — stated plainly rather than claimed as a render test): the `wideDonorAnchors` extraction alone is a **byte-identical no-op** (FEA, glyphs, cmap, glyphOrder); **no pre-existing glyph changes at all** and `glyphOrder` grows by exactly `uniFB1F`; the baked glyph is **provably identical** to `uni05F2` + `uni05B7` translated to the GPOS anchor (170,−45), so raw FB1F and the typed sequence render the same; a custom pasekh on yud now carries into ײַ; FB1F emits 7 base-anchor lines (ײ's 8 minus the below class it bakes), all trop keys present, **none at x=0**; the FEA carries **no fractional coordinate** anywhere (the feaLib "Expected a number" trap). UI matrix light+dark × 1280/800 plus the Hebrew RTL locale, 0 `pageerror`s throughout. Gates: `check-inline-js` clean, `check-i18n` clean at exactly **14** baselined findings (unchanged), `build-locales` 4675 keys. One new i18n key (`fontmaker.glyphname.pasekh_tsvey_yudn`), the two form-enumerating hint strings re-trued in **both** language columns, `fontmaker.changelog.v5_24` in EN+HE. FONT_MAKER_VERSION 5.23→5.24, sw.js v541→v542. **Known limitation, consistent with the digraphs:** an imported font's own FB1F outline is not taken whole — wide forms and digraphs are deliberately never imported that way (see the `precompSrcOutline` comment), so ײַ is always rebuilt from the user's letters.

- [x] 2026-08-21 | **⚑ Out-of-band: maintainer-directed landing of PR #185 to `main`** | — | The Font Maker v5.19 branch `claude/imported-fonts-latin-letters-6o4gtn` (17 commits, `413f121..f06445d`, +891/−76 across 6 files) fast-forwards onto `main` at the maintainer's in-session "push to main" direction — the same branch-protocol carve-out as S78 / S191–S193 / S195 / PRs #179–#182 / #184. `merge-base --is-ancestor` was re-checked immediately before pushing (and that nothing sat on `main` outside the branch), so **no merge commit and no drift**; `main` moved `9e7845d → f06445d` and draft PR #185 auto-closed as **merged** at 22:07:21Z. **Landing, part 1 — the feature build (`413f121..5195a6b`):** an import now brings the whole font across, not just the letters. The typographic set (53 punct/symbol rows + 18 standalone Latin letters as `LATIN_EXTRA`), the masoretic marks and the rest of the presentation block (05AF/05C4/05C5 → `TROP_DEFS`; 05C6/FB29 → `LETTERS`; 18 composable dagesh/mapiq forms + yod+hiriq → `SPECIAL_MORE`), the source font's own sTypo line box and line gap (`nominalBox()`), and ready-made combined forms taken whole (`pc.src` / `precompSrcOutline`). Kerning was a **deliberate non-ship** with a HarfBuzz receipt (a legacy `kern` table is applied for LTR and ignored ENTIRELY for RTL, so Hebrew pairs are dormant data of unknowable order — LTR pairs import, Hebrew pairs are skipped and logged). **Landing, part 2 — the adversarial audit round (`311e913..f06445d`):** ten confirmed findings in that same build, each reproduced on `5195a6b` with a control before any code moved, each its own commit. The systemic one (universal form seeding leaking seeded-but-OFF forms into `precompFormFor`'s consumers — the Dagesh tile replaced on 17 letters, the Rafe tile deleted on 8), two shipped-bytes defects (Generate Bold shipping original-weight combined forms; `fontInkBounds` blind to traced-punct / custom-glyph / `pc.src` ink), the metrics undo-restore merge that resurrected a reset line box, the un-guarded live edit that could destroy it, the migration inversion + 18 fabricated QA rows, the QA/spacing surfaces showing ink the export doesn't ship, dead controls on imported forms, and two escapees shipped (NBSP → the space glyph; FB2C/FB2D taken whole after V2 measured a (59,41)-unit interior dagesh hiding inside a bbox-identical glyph). **SW v534→v535 and FM 5.18→5.19** carry all of it. | verified: "pages build and deployment" **run 1144 for `f06445d` concluded `success`** — so the whole v5.19 release AND its audit round are LIVE on ivritsuite.com (verifying the deploy is the one close-out step a branch-and-PR session cannot do in-session). Pre-flight before the push: `check-inline-js`, `check-i18n`, `update-llms-txt --check` and `update-sitemap` all clean, working tree clean, SW v535 / FM 5.19 confirmed in the file bytes. **Live-site spot-checks were NOT possible from this container** — the proxy denies outbound origins other than the allowlist, the same limitation recorded for the PR #180/#184 landings, so the browser check is the maintainer's. Drift baseline for the next session: post-landing `main` tip `f06445d` / SW v535 / FM 5.19; **the merged branch is not to be reused** — cut fresh off the latest `origin/main`. **Carry-forward for whoever next touches the Font Maker's import, export or fidelity work:** the harness that produced every receipt in both entries below — Playwright boot/import helpers, the production `PY_BUILDER` extracted verbatim and run under system fontTools 4.63, the npm copy of opentype.js used to route-fulfil the proxy-denied jsDelivr load, **`uharfbuzz` from pypi** (harfbuzzjs is proxy-denied) and `mkkern.py`, the synthetic kern/metrics probe generator — lives **only in the session scratchpad and does NOT survive the container**. The repo still has no test infrastructure, so those receipts are reproducible only by rebuilding them from the two Done entries below. Not a precached file, so no `sw.js` bump for this entry; `docs/` is not in `sitemap.xml`, so no lastmod run.

- [x] 2026-08-21 | branch `claude/imported-fonts-latin-letters-6o4gtn` (**adversarial audit round of the v5.19 build below** — maintainer-directed: *“let's do an adversarial audit and close look at anything that could have escaped the import”*) | Hebrew_Font_Maker.html + `locales/ui-strings.csv` | **Ten confirmed findings, ten fixes (`311e913..6d71b90` + changelog `d759858`), each reproduced on `5195a6b` with a control before any code moved.** Method: two Explore agents traced every consumer of the new mechanisms (`project.precomposed`, `pc.src`, the typo metrics, the widened LETTERS table), one Plan agent stress-tested every fix shape against the real code — and the stress-test itself surfaced a finding the explorers missed. **The systemic one (fix 1):** v5.19's universal form seeding exposed every `precompFormFor`-family consumer that filtered by base/mark alone — with all tabs OFF an import replaced the generic Dagesh tile on 17 letters with a dead form tile, deleted the Rafe tile on 8, offered editors for forms the export skips, and re-routed QA rafe cells. Gate hoisted into `precompFormFor` itself (the `fidelityPredictPair` pattern) + two `.filter(precompExportable)` sites; `renderEngAccentLeft` commented as deliberately ungated. **The stress-test's own catch (fix 4b):** `qaRows`' Specialized section ran `qaSynthWideRow` over ALL of `SPECIAL_TARGETS` — which v5.19 grew by 18 NON-wide forms — fabricating collision rows whose ink is the plain undotted base letter (`buildWideGlyph` degenerates to identity on a non-wide pc). One `isWideForm` gate, landed WITH fix 4a (migrate now re-seeds every enabled tab beside `ensureEnglishSeeded` — an old project with Specialized ON was silently missing all 18 SPECIAL_MORE forms forever, 0/18→18/18) because (a) alone would have handed every migrated project the 18 junk rows. **Shipped-bytes fixes:** boldGen never touched `precomposed`, so every `pc.src` form shipped ORIGINAL-weight in a font stamped bold — now dropped under its `udDo` with precomp scopes (ink area 209,690→332,252 on the rebaked שׁ; undo restores byte-identically). `fontInkBounds` had drifted from the bake on four counts (traced-punct ink, custom-glyph ink, `pc.src` ink all unmeasured; the precomp loop ungated on exportable and using global `markScale` where the bake uses `precompMarkScale`) — now mirrors the bake's gate order exactly, verified per-arm by raising each omitted source above all other ink with an ON/OFF control pair (a CENTER-class form cannot serve as the ON-control: bake and mirror both deliberately ignore its anchor override). **Undo/metrics:** the `'metrics'` undo RESTORE was an `Object.assign` merge that can never delete keys, so redo of Reset resurrected the imported line box invisibly (snapshot was always whole-object — only the restore was wrong; now delete-keys-then-assign, identity-preserving). `_clampMetric` had no range guard for the typo keys — a live-typed out-of-range value exported a degenerate box and the next load's pairing rule then silently deleted BOTH keys; now the import/sanitizer bounds (1..3000 / −1500..−1) hold on the live path too, reject-not-clamp. **Preview==QA==export restored for src forms (fix 8):** `qaSynthPrecompRow` and the spacing preview still rendered the base+dot reconstruction (QA row xMax 647 vs shipped 660); the QA row now ships the src ink whole (no dot layer, export's advance formula, holam-slot freeze kept, and it bypasses the dot-anchor bail exactly as the bake's src early-return does), and a typed שׁ/וֹ in the spacing sample substitutes the whole src cell with the layout total re-summed — full substitution, never skip-dot-alone (a bare vav for shuruk is worse than the approximation). Hand-traced control byte-identical. **Truth-telling controls (fix 9):** src forms' dead dot X/Y + size controls (writes the export never reads) replaced by the ready-made note + per-form Rebuild (which also gave CORE forms their first rebuild path); the shin holam-slot inputs stay live and the stage now renders the blue holam handle ALONE for src shins — `precompHolamAnchor` ships regardless of src. The first cut of this commit put a `//` comment mid-one-liner and swallowed the else-branch — **caught by `check-inline-js.mjs` pre-commit: the outage gate paying for itself within a day of being built.** **Escapees shipped (fixes 7+10):** `cmap[0xA0]='space'` — NBSP was the one keyboard-reachable character still escaping, and the `letterShips` ink gate makes it inexpressible as a punct row (verified: TTF many-to-one cmap, HarfBuzz shapes a<NBSP>b with 0 notdef, UFO glif carries both unicodes). And **V2's measurement forced fix 10**: the synthesized שּׁ was bbox-IDENTICAL to the source's own FB2C — hiding an interior dagesh sitting (59,41) units off. FB2C/FB2D now come whole as `pc.srcDagesh` on the FB2A/FB2B entries (no pc of their own — three components), gated on the dotted form itself being src (never mix ink families), with full lifecycle (sanitizer, undo snapshot, boldGen/Rebuild drops). Max centroid delta now 7 — resampling drift on the shin BODY; the dagesh matches at ≤1. | verified: **V1 compositions all green on the real binary** — גָּ (NEW dagesh form + vowel between) → `uni05B8+uniFB32` through the mark-filtered lookup, הּ/יִ compose, masora circle and lower punctum attach with real GPOS offsets, ﬩ and ׆ shape in RTL with 0 notdef; **import digest byte-identical to the v5.19 baseline** (252 keys, 0 changed) and the Taamey glyph SET byte-identical vs a `5195a6b` worktree (217=217 — an earlier '215' was a pre-Phase-B memory, killed by measuring); fresh project still 63 glyphs; UI matrices 1280/800 × light/dark × EN/HE clean for the punct picker, English grid and composed-forms panel; `check-i18n`, `check-inline-js`, `build-locales` clean; About renders v5.19 in both languages. **Refutations recorded so no future session re-derives them:** export nag / next-step hint / progress readouts count core-27 only (punct excluded by predicate); kern pickers don't enumerate LETTERS (free-text + classes; the class picker filters punct); `anchorClassMapForImport` derives from `TROP_DEFS` so the masoretic cps ride automatically; `a11yTiles` targets `.ltile` only — the `.punct-grp` headers get no tabindex and there is no roving-arrow nav to break (the ~104 sequential tab stops are a pre-existing pattern amplified — P4 candidate below); the metrics undo SNAPSHOT was always whole-object; `qaRows`' dotted-form section is double-gated; `_spacingCellMarks` hardcodes core targets (immune to seeding, but was src-blind → fix 8). **Deliberate skips, ledger-noted:** dagesh-form spacing cells (typed בּ) are un-routed and keep the class-anchor approximation; Latin accent-bake ink is absent from `fontInkBounds` (accents rarely exceed lamed+100); the spacing preview treats only ' ' as a space (a pasted NBSP shows a dashed box); U+25CC ◌ and the 2000-200A space family remain unmodelled by choice. No sw bump — v535/FM 5.19 are still unreleased on this branch; the audit additions ride the same combined changelog entry (`d759858`).

- [x] 2026-08-21 | branch `claude/imported-fonts-latin-letters-6o4gtn` (maintainer-directed follow-on: *“can we pick up with the other import marks you mentioned?”* → measured the remaining gaps, asked which one, answer was **“All of these”**) | Hebrew_Font_Maker.html + `locales/ui-strings.csv` + `sw.js` | **FM v5.19: four separate things an import still threw away, in four commits.** The premise was corrected first: an initial coverage pass reported 38/46 unmodelled Hebrew presentation forms because it forgot the app's own `PRECOMPOSED`/`WIDE_LETTERS`/`YIDDISH_FORMS`/`LADINO_FORMS` target sets — the true figure is 25, and **16/29 (Shlomo) / 24/29 (Taamey) of the forms the app builds are already drawn by the source font**. Do not quote a coverage number that was not diffed against every modelling table. **(1) `413f121` — the typographic set.** 53 `cat:'punct'` rows (Latin-1 symbols, General Punctuation, currency/math) + 18 standalone Latin letters with no base+mark decomposition (Æ Ø ß Œ Š Ž…) as a new `LATIN_EXTRA`, sharing the English tab's third case and deliberately kept OUT of `LATIN_PRECOMPOSED` so pass 11 never bakes one from parts it lacks. Zero new import code — every row rides the existing loop and v5.18's `letterShips()` ink gate. Shlomo 208→279 glyphs, Taamey 207→215 (exactly its cmap). The picker grew a band label per group (102 undifferentiated tiles is a wall) and its Hebrew-band test and its `lang="he"` test are now **one predicate**, so U+FB29 lands in the Hebrew band instead of being pinned LTR among the symbols. **(2) `fdcb140` — the masoretic marks and the rest of the presentation block**, split by what the model can express: U+05C6 nun hafukha and U+FB29 → `LETTERS` (real characters, not combining); the masora circle and the upper/lower puncta (U+05AF/05C4/05C5) → `TROP_DEFS`, because they attach at the same above/below class anchors and the vowel picker is the wrong home; and 18 composable forms (every letter+dagesh/mapiq beyond the four core BeGeD-KeFeT ones, plus yod+hiriq) → `SPECIAL_MORE` under the existing Specialized toggle, so a default project is unchanged. Six forms stay out because they are not base+mark pairs — FB2C/FB2D are already built by their own pass, FB1F is a digraph+mark, FB20/FB4F are their own designs, FB1E duplicates rafe. **yod+hiriq is the first BELOW-class mark to go through `precompAnchor`** and it bakes correctly (y from 94 against the yod's 253). **(3) `dd394aa` — line metrics.** The source font's sTypo box + line gap, scaled to 1000 UPM (hhea as fallback), behind a sanity gate. `nominalBox()` is the one source of truth, used at the three sites that MEAN “the exported line box” (export, its QA mirror, the spacing preview's line height); the other three viewBox users frame one glyph row and stay on the design measurements, and the spacing preview frames by the LARGER of the two so a tight imported box cannot clip it. **(4) `e2ff29e` — combined forms taken whole.** `pc.src` carries the source outline+advance and `precompSrcOutline()` is the single reader the export bake, the tile preview, the editor stage and the QA build-check all go through. The form stays a composed target — ccmp still substitutes base+mark to it, its base anchors are still emitted, and its advance still takes Letter-spacing. **Wide letterforms and digraphs are excluded on purpose:** `buildFEA` derives their exported mark anchors from the exact ink transform `buildWideGlyph` applied, so a foreign outline would leave every vowel typed after one attaching to coordinates that no longer describe the glyph. | verified (headless + local HTTP + `serviceWorkers:'block'`; production `PY_BUILDER` extracted and run under fontTools 4.63; **HarfBuzz via `uharfbuzz` from pypi**, since jsDelivr's harfbuzzjs is proxy-denied): **full-feature acceptance — Shlomo builds 349 glyphs and shapes Hebrew-with-nikkud, a full English sentence, a symbol line (© ₪ ½ × ÷ « » …) and Yiddish with ZERO notdef**; Taamey builds 285 and is clean on everything it actually contains. feaLib compiles both with `fellBack: []`, no duplicate cmap target, no unknown FEA glyph, no blank cmapped glyph. **A fresh project is unchanged at 63 glyphs** and ships none of the new rows. Shlomo's שׁ ships at bbox `[33,0,660,845]` against the bake's `[32,0,647,883]`. Shaped from the real binaries: שׁ→uniFB2A, בּ→uniFB31, וֹ→uniFB4B, שּׁ→uniFB2C, בָּ→uni05B8+uniFB31. Metrics land verbatim: Shlomo typo 1073/-404, Taamey 800/-200 gap 90, USE_TYPO_METRICS set, hhea/usWin still expanded over true ink; Reset clears them. Save→migrate→reload keeps 31 forms with the same outline; a **malformed `pc.src` drops back to the bake instead of reaching the exporter**; the Rebuild button restores the bake and **undo restores the import** (the precomp scope now carries `src`). Import digest of every previously-imported glyph **byte-identical** across all four commits. UI matrices 1280/800 × light/dark × EN/HE for the punctuation picker, the English accent tab and the composed-forms panel: 0 pageerrors, no overflow. About tab renders v5.19 at the top in both languages. `check-i18n`, `check-inline-js`, `update-llms-txt --check` and `update-sitemap` all clean. **⛑ The kerning half is a deliberate NON-ship, and the measurement is the reason.** Both partner fonts have **zero** kerning of either kind (no `kern` table, no GPOS `PairPos`), so this needed a synthetic fixture at all. Built one, then measured with HarfBuzz: **a legacy `kern` table is applied for LTR runs and IGNORED ENTIRELY for RTL ones** — the same probe font kerned Latin AV by -55, and its alef+bet by 0 in RTL and -80 only when the run was forced LTR. So a Hebrew font's `kern` pairs have never reached a reader through any modern shaper: they are dormant data whose order is unknowable (the table is visual-order by spec while most tools write logical), and reviving them would be **inventing** spacing rather than preserving it. LTR pairs import (round-tripped -55 through source and export with an un-kerned AB control at 0 in both); Hebrew pairs are skipped and the count is logged. **GPOS pair positioning is the unambiguous version and is the natural follow-up** — opentype.js does not expose it, so it needs the Pyodide path. | **Carry-forward:** the harness (Playwright + the extracted `PY_BUILDER` + the npm copy of opentype.js + `uharfbuzz` + `mkkern.py`, the synthetic kern/metrics probe generator) lives **only in the session scratchpad and does not survive the container**. FM 5.18→5.19, SW v534→v535, +110 CSV keys. The three “what carries over” claims now name the combined forms, punctuation, symbols and line metrics, and no longer promise that Yiddish forms start empty.

- [x] 2026-08-21 | **⚑ PRODUCTION OUTAGE + hotfix: a stray backtick in the v5.18 changelog killed the whole Font Maker** | `5df02bf` (fix) + `be8a229` (gate) | Hebrew_Font_Maker.html + `scripts/check-inline-js.mjs` + CLAUDE.md + `sw.js` | **The v5.18 changelog entry lists the 21 newly-added ASCII symbols — and one of them is the backtick.** `HELP_CONTENT.about` is authored HTML inside a **JS template literal**, so that character closed the string early and the next token (`~`) was a syntax error that took the **entire inline script block** with it. The static HTML still rendered, so the symptom read as broken *styling* rather than a dead page — but no JavaScript ran at all: `?start=<id>` never booted, the partner wizard never opened, and the page fell back to an empty untitled project with an **unlocked license**. Reported by the maintainer from the live `?start=shlomo-semistam` link. Fix: escape as `&#96;` (identical rendering, inert in source). The i18n copies in `ui-strings.csv` / `en.json` / `he.json` were never affected — those are data files where a backtick is an ordinary character, which is exactly why the i18n gates stayed green. SW v533→v534. | **Why nothing caught it — the real lesson.** Every gate this repo has was blind to it: `check-i18n` reads strings, `build-locales` reads the CSV, and the Playwright recipe **drove a page that had already failed to parse** — a `pageerror` count of 0 means nothing when the script never ran. Nothing ever asked *is this file still valid JavaScript?* And the verification I did run was on the shipped tree BEFORE the changelog edit: the changelog was written last, as a release chore, and never re-verified — **a "docs-only" edit inside a template literal is a code edit.** New gate `scripts/check-inline-js.mjs` (plain Node, no deps) parses — never executes — every inline `<script>` in every root HTML page, validates `application/ld+json` as JSON, ignores `<script src>`. Two traps it had to handle, both found by its own first run: several pages document their structure in HTML comments containing the literal text `<script>` (comments are blanked first, preserving byte offsets so line numbers stay true), and `type="module"` blocks need `vm.SourceTextModule`, so without `--experimental-vm-modules` they are reported as **skipped** rather than silently counted as checked. | verified: the gate **exits 1 on the real defect** (`Hebrew_Font_Maker.html:1709 inline script block #7 — Unexpected token '~'`) and 0 once fixed — 62 classic blocks + 36 JSON-LD across 14 pages; `?start=shlomo-semistam` boots the partner wizard with `_osStartCtx` populated and **0 pageerrors**; both wizard arms finish correctly (english on → 104 items / 52 in cmap, off → 0, license locked `os-passthrough` either way); a normal import unaffected. Deploys: **run 1141 for `5df02bf` concluded `success`** — the site is repaired. CLAUDE.md's definition of done now carries the gate plus the rule that authored HTML inside a template literal must escape `` ` `` → `&#96;`, `${`, and `</script>`.

- [x] 2026-08-21 | **⚑ Out-of-band: maintainer-directed landing of PR #184 to `main`** | — | The Font Maker v5.18 branch `claude/imported-fonts-latin-letters-6o4gtn` (5 commits, `e8b3670..e8094e7`, +412/−65 across 6 files) fast-forwards onto `main` at the maintainer's in-session "push to main" direction — the same branch-protocol carve-out as S78 / S191–S193 / S195 / PRs #179–#182. `merge-base --is-ancestor` was re-checked immediately before pushing (and again that nothing sat on `main` outside the branch), so **no merge commit and no drift**; `main` moved `e9de546 → e8094e7` and draft PR #184 auto-closed as **merged** at 19:31:00Z. Landing: **an imported font now brings its Latin across** — A–Z/a–z filled from the source font with the English tab auto-enabled, ready-made accented forms (á, é, ñ, ç…) taken whole with the base+accent bake as fallback, the font's own accent shapes via the new `project.font.accent` store, and a Replace/Keep-mine question for English the user drew — plus the **standalone data-loss fix** (`migrateProject` dropped every `.eng` item and re-seeded it blank, so a traced English letter did not survive save→reload, *Load recent*, or autosave restore) and the **blank-glyph fix** (export pass 1 emitted every `LETTERS` entry unconditionally, so untraced digits shipped as blank cmapped glyphs and `5` rendered as nothing instead of falling back; one shared `letterShips()` predicate now gates pass 1 and `buildFEA`'s base-anchor loop in lockstep, which is what let the 21 previously-unmodelled ASCII symbols be added safely). **SW v531→v533 and FM 5.17→5.18** are the bumps that carry all of it. | verified: "pages build and deployment" **run 1139 for `e8094e7` concluded `success`** — so the Latin import, the accent store, the two bug fixes, FM v5.18 and SW v533 are LIVE on ivritsuite.com (verifying the deploy is the one close-out step a branch-and-PR session cannot do in-session). **Live-site spot-checks were NOT possible from this container** — the proxy denies outbound origins other than the allowlist, the same limitation recorded for the PR #180 landing, so the browser check is the maintainer's. Drift baseline for the next session: post-landing `main` tip `e8094e7` / SW v533 / FM 5.18; **the merged branch is not to be reused** — cut fresh off the latest `origin/main`. **Carry-forward for whoever next touches the Font Maker's import or export:** the Playwright harness, the `PY_BUILDER` extracted verbatim from the page and run under system fontTools 4.63 (the technique that produced every real-TTF receipt in this build), and the npm copy of opentype.js used to route-fulfil the proxy-denied jsDelivr load all live **only in the session scratchpad and do NOT survive the container** — the repo still has no test infrastructure, so those receipts are reproducible only by rebuilding them from the two Done entries below. Not a precached file, so no `sw.js` bump for this entry; `docs/` is not in `sitemap.xml`, so no lastmod run.

- [x] 2026-08-21 | branch `claude/imported-fonts-latin-letters-6o4gtn` (maintainer-directed follow-on to the entry below — "do the punctuation fix too") | Hebrew_Font_Maker.html + `locales/ui-strings.csv` + `sw.js` | **An untraced digit no longer ships as a blank glyph, and 21 ASCII symbols join the punctuation tab — an imported font now covers all 94 printable ASCII characters.** Export pass 1 emitted **every** `LETTERS` entry unconditionally, so a project whose digits were never traced still shipped `uni0030`–`uni0039` with empty contours: typing `5` rendered **nothing** rather than substituting a fallback face (measured before the fix: 10 blank cmapped glyphs on a fresh project, each carrying 3 `pos base` lines — **30 FEA lines**, which is exactly the coupling that made the naive fix unsafe). One shared predicate `letterShips(L, l)` now gates **both** pass 1 and `buildFEA`'s base-anchor loop; splitting them would leave `pos base` lines on a glyph absent from `glyphOrder` and **feaLib rejects the whole file**. Hebrew letters are deliberately unaffected — an untraced one is a considered blank that `exportFont`'s own untraced-letters guard already covers; only `cat:'punct'` is gated on ink. With blank glyphs no longer shipping, the 21 symbols the app modelled nowhere (`# $ % & * + / < = > @ [ \ ] ^ _ ` { | } ~`) were added as `cat:'punct'` rows: they ride the **existing** import loop with zero new import code, and stay out of the font until a font actually fills them. Also: `renderPunctGrid` stamped `lang="he"` on every tile — wrong for ASCII, and it left `( [ {` bidi-mirroring in the Hebrew UI; now Hebrew-block-only (**the first range test used `>= 0x0590`, which caught U+2014 em dash — tightened to `0x0590–0x05FF` after the matrix showed 6 tagged tiles where 5 were expected**). Punctuation practice template 27→48 items = 1→2 pages, meta strings updated EN+HE. No version bump: v5.18 is unreleased, so this **extends** its changelog entry per the one-combined-bump rule; SW v532→v533. | verified (same harness; production `PY_BUILDER` extracted and run under fontTools 4.63): **fresh project 73→63 glyphs, digits 0/10 cmapped, `blankButShipped: []`, `pos base` digit lines 30→0**; **imported Shlomo 94/94 printable ASCII, 52/52 Latin, 27/27 Hebrew, 0 blank-cmapped glyphs, `fellBack: []`**; Taamey 94/94 likewise. Real feaLib compiled all three states — the lockstep holds, and a scan of every `pos base` operand against `glyphOrder` returns **zero unknown glyphs** in both the fresh and imported states. All 54 originally-modelled glyphs still import **byte-identically to the pre-change baseline**; all 21 new symbols import with ink. Punct grid matrix 1280/800 × light/dark × EN/HE: 48 tiles, exactly **5** `lang="he"`, `@` and `(` both `direction: ltr`, `At Sign` → `שטרודל`, no overflow, 0 pageerrors. Template paginates 48/30 = 2 pages, no duplicate codepoints, all cells build. Prior suite re-run green (migrate round-trip, sanitizer, both wizard arms, accent store). `check-i18n` clean. **Method note:** the fix was written only after measuring the failure — the 30 `pos base` lines are what proved pass 1 and `buildFEA` had to move together, and the em-dash mis-tag was caught by asserting an exact expected count rather than "some tiles are tagged".

- [x] 2026-08-21 | branch `claude/imported-fonts-latin-letters-6o4gtn` (plan-driven feature build — NOT an improveloop session) | Hebrew_Font_Maker.html + `locales/ui-strings.csv` + `sw.js` | **FM v5.18: an imported font now brings its Latin letters across — and a data-loss bug that would have eaten them is fixed first.** The Font Maker already had the whole English capability: `ENGLISH_LETTERS` seeds A–Z/a–z behind the *Add English letters* toggle, `buildFontSpec` pass 3 emits every `.eng` letter with a real cmap entry, and pass 11 bakes the 52 accented forms. The one missing link was the importer — `applyFontImport` walked only `LETTERS` (Hebrew + `cat:'punct'`, which is why digits and Latin punctuation already imported), so an uploaded TTF's A–z outlines were parsed by `pathToOutline` and discarded. Meanwhile OS/2 has declared **cp1252 since the Word-fallback fix**, so every export advertised a Latin alphabet it did not carry. **Commit 1 (`e8b3670`) is a standalone bug fix:** `migrateProject` rebuilds `data.letters = LETTERS.map(...)` and its preservation loop admitted only `.custom`, so `.eng` items were dropped and `ensureEnglishSeeded` re-added them **blank** — every hand-traced English letter was destroyed by save→reload, *Load recent*, AND the autosave-restore path. Without this the feature evaporates on reload, so it shipped first and separately. **Commit 2 (`6eaef9d`) is the feature.** A shared `importLetterOutline()` extracts one glyph's outline+advance; the Hebrew pass routes through it too so the two can't drift. The Latin pass fills A–Z/a–z and auto-enables the tab at `ENG_IMPORT_MIN` (≥20 of 52 — real fonts are bimodal; the one genuine partial is a case-only design, which clears 20 easily). Ready-made accented forms are taken **whole** as ordinary `.eng` items, so pass 3 emits them and pass 11's existing `if (glyphs[nm]) return;` skips the bake for free — a font with none keeps baking unchanged. A new `project.font.accent` store, read by `markGlyphContours` ahead of the built-in `LATIN_ACCENTS`, brings the font's own accent shapes; they are kept **out of `importedMarks`** (the fidelity-check domain) and take no GPOS `attachAnchor` — those classes are shared with rafe and would skew `hfm_read_anchors`' majority vote. An *Accented* case appears in the English grid only when such forms exist (a hand-tracer never sees 52 blank tiles), with a *Rebuild from my letters* escape hatch. Hand-made English gets its own Replace / Keep-mine question. **Design corrections caught in review, each load-bearing:** `ENGLISH_CPS` had to span both bands because its sole consumer `isEng()` is the routing predicate — an accented form outside it opens in the Hebrew grid with **nikkud anchor drag handles**; `markClassOf` needed an accent branch or an imported cedilla attaches by its TOP edge; and **`source.kind` is unusable as provenance** — `setInputMode` stamps every letter including `.eng`, so one mode switch would make hand-drawn letters look imported (provenance is now an OR of evidence — `handEdited`/`draw`/`source.dataUrl`/`engSource` — which can only ever cost an extra prompt, never a silent overwrite). Alongside: `.eng` codepoints validated on load (a junk one reaches `cmap[parseInt(cp,16)]` → `cmap[NaN]` → `int(k)` **fails the whole export** with no explanation), the accent store's key space closed and its `attachAnchor` rounded (a fractional one makes feaLib reject the entire FEA — the v5.16 lesson), Latin ink counted toward the clip box, **`ulUnicodeRange*` recalculated from the shipped cmap (it was never set at all — 0)**, the import toast wired to the `fontmaker.status.import_*` keys **that already existed in the CSV and had simply never been used**, `markDirty()` finally called by `applyFontImport` (it never was — a crash right after an import lost everything), and pass 3 stopped clobbering a custom glyph on the same codepoint. FM 5.17→5.18, SW v531→v532, +11 CSV keys, three now-false 'Latin doesn't come across' strings rewritten (EN+HE). | verified (headless, local HTTP, `serviceWorkers:'block'`, opentype.js route-fulfilled from npm since jsDelivr is proxy-denied): **a real TTF built by extracting the production `PY_BUILDER` verbatim and running it under system fontTools 4.63** — Shlomo 29,464 bytes, `fellBack: []`, **52/52 Latin + 52/52 accented + 27/27 Hebrew cmapped**, `ulUnicodeRange1 0x00000000 → 0x80000807` (BasicLatin+Latin-1+Hebrew), `g` bounds −217, GPOS/GSUB present; Taamey Frank (0 precomposed in the font) builds the **baked** path to the same coverage, and its `ä` (744) is measurably taller than its `á` (735) **because the diaeresis was imported and the acute is built-in** — the accent store proven end-to-end into the binary. Hebrew/punct import digest **byte-identical to the pre-change baseline** across the refactor and all later commits (54 keys deep-compared). Round-trip through `applyProjectData`: 52 bases + 52 accented + stamps + accent store + toggle + advances all survive. Sanitizer: junk codepoint dropped, junk accent key dropped, cedilla class/designMode coerced, non-finite `attachAnchor` dropped, spec still clean. Partner `?start=shlomo-semistam`: box pre-checked, ticked → 104 items / 52 in cmap, **un-ticked → 0** (the explicit choice is honoured), license lock intact. Modal chain drives Import → Replace-letters → Replace-English, and *Keep mine* preserved the hand-drawn A (adv 450) while filling all 103 others. Matrix 1280/800 × light/dark × EN/HE: 52 tiles, no overflow, Hebrew UI mirrors while glyph tiles stay LTR, **0 pageerrors in every arm**. `check-i18n` clean; `update-sitemap` a no-op (already current). **Scoped out, with evidence:** both partner fonts also carry all 21 ASCII symbols the app models nowhere (`# $ % & * + / < = > @ [ \ ] ^ _ ` { | } ~`), but export pass 1 emits **every** `LETTERS` entry unconditionally — blank ones included — so adding them as `cat:'punct'` would ship 21 blank cmapped glyphs and make `@` render as *nothing* instead of falling back. The correct version needs pass 1's emission rule and `emitBaseAnchors`' `LETTERS` loop changed in lockstep (it calls `seedAnchorsIfNeeded`, so a skipped glyph would still get `pos base` lines → feaLib rejects the file). **Same latent bug already ships for untraced digits.** Queued as a candidate, not smuggled into a letters change.

- [x] 2026-08-21 | **⚑ Out-of-band: maintainer-directed landing of PR #182 to `main`** | — | The Font Maker v5.16 branch `claude/nikkud-position-autofix-nan5bo` (10 commits, `2e2bf04..456d9ea`, +725/−145 across 8 files) fast-forwards onto `main` at the maintainer's in-session "Push to main" direction — the same branch-protocol carve-out as S78 / S191–S193 / S195 / PRs #179–#181. `merge-base --is-ancestor` was checked before pushing, so no merge commit and no drift; PR #182 auto-closed as **merged**. Landing: the **Fidelity Autofix v2** (the report's Apply button now fixes every measurable flagged combo via three vehicles — form / consensus anchor / per-(letter × mark) pin — then re-verifies against the original bytes), the **import progress modal** (`ipArm`, covering the cold-engine wait on the Open Siddur `?start=` path), and **15 defects from three adversarial review rounds**. `sw.js` v528→v529 and FM 5.15→5.16 are the bumps that carry all of it. | verified: "pages build and deployment" **run 1136 for `456d9ea` concluded `success`** — so the autofix, the progress modal and SW v529 are LIVE on ivritsuite.com (verifying the deploy is the one close-out step a branch-and-PR session cannot do in-session). Drift baseline for the next session: post-landing `main` tip `456d9ea` / SW v529 / FM 5.16; **the merged branch is not to be reused** — cut fresh off the latest `origin/main`. **Carry-forward for whoever next touches the fidelity work:** the regression harnesses and the `probe-noccmp.ttf` fixture (with its `build_probe_noccmp.py` builder) live only in the session scratchpad and do NOT survive the container — the repo still has no test infrastructure, so the round-3 receipts are reproducible only by rebuilding them from the ledger entry above. Not a precached file, so no `sw.js` bump for this entry; `docs/` is not in `sitemap.xml`, so no lastmod run.

- [x] 2026-08-21 | branch `claude/nikkud-position-autofix-nan5bo` (**round 3** — adversarial review of the round-1/2 FIX commits, which had only the author's own regressions behind them) | Hebrew_Font_Maker.html | **4 confirmed defects, all in the fix batch rather than the feature — the base rate held for a third round.** (1) **A font import discarded pins and applied form overrides on the strength of which CHECKBOXES were ticked, not what it actually read.** An anchors-only import whose GPOS read failed — offline engine, or a font with no GPOS — imported nothing, yet still deleted every fidelity pin in the project and every `auto` `pc.anchor` the autofix had written. Round 2's own provenance fix is what made this destructive: it added the clearing, and put it BEFORE `applyFontImport`'s `try`. Now gated on outcome (`opts.outlines || (imported && hadGpos)`; per letter on that letter's own `imp`) and moved inside the `try`. (2) **`fidelityMeasure` computed `dev` on a finer lattice than the planner's** — `implied()`/`row.fix` round, `dev` did not, so a row with a true deviation of 3.4 flagged at TOL 3 and then got **no vehicle at all**, because every vehicle computed it as already on-target: a flagged combo the Apply button silently cannot fix. Same rounding, same operand order, so `round(real − pred) === implied − cur` exactly. (3) **Clear pins vanished for a letter whose pins had all gone stale** — round 2 correctly made the COUNT live-only (an inert pin draws no badge and offers no Unpin), but gated the whole section on that count; a stale pin is still in the file, still rides autosave, and goes live again if the halign moves back, so it must never become unremovable. Section gates on stored, label counts live. (4) **Redo brought the fixes back without their report**, leaving the reopen button dead over a project that matched the report exactly; the report is stashed on the undo entry (stacks are runtime-only, so its `ctx` font bytes still never reach autosave or a `.hfm`). CLAUDE.md's fidelity invariants updated for all four. | verified: 4 new regressions, each **failing against the pre-fix commit b18e0b7** and passing after — and each carrying a CONTROL that passes pre-fix too, so none can pass for the wrong reason (the lattice test demonstrates BOTH rules in-page and pins the shipped one via `fidelityMeasure.toString()`; the import test asserts the class anchors were untouched before asserting the pins survived). Full suites re-run green: **15/15** fidelity (golden no-pin FEA still byte-identical to 2e2bf04; Shlomo 203→0 flagged, Taamey 96→0), **19/19** progress modal, 7/7 + 12/12 + 12/12 review regressions, both rounding arms. Acceptance re-run: the fixed Shlomo spec still compiles under **real feaLib** (`fellBack: []`) and **real HarfBuzz** puts **203/203** formerly-flagged combos exactly where the original font did, 203/203 ink-equivalent, mkmk stacking intact. `check-i18n` clean; 0 pageerrors. **Method note — the form vehicle had ZERO end-to-end coverage and nobody could see it**: neither vendored seed font can produce a `pc` row (both compose vav+holam through a GSUB ccmp ligature, so the pair lands in `skippedSubst` — measured `pcRows: 0`, `skippedSubst: 10` on both), so the round-2 rounding regression only ever exercised `implied()`, never `row.fix`. Closed with a purpose-built 3-glyph probe (vav + holam, real GPOS mark lookup, **no ccmp**, holam bbox x:[0,51] so `markAttachOrigin`'s bbox fallback returns a genuinely fractional 25.5). **A green suite over a fixture set that structurally cannot reach a code path is not coverage** — check what the fixtures can actually produce, not just what the tests assert.

- [x] 2026-08-21 | branch `claude/nikkud-position-autofix-nan5bo` (adversarial review of the two entries below) | Hebrew_Font_Maker.html | **Two review rounds over the v5.16 work: 11 confirmed defects fixed, each with a regression test proven to fail against the pre-fix code.** Round 1 (4 lenses × 2 refuters; 24 of 42 agents died on usage credits, so 3 lenses went unverified): stale `_fidelityLast` survived project switches and the new reopen button made it applyable cross-project (the re-verify then re-measured the NEW project against the OLD font's bytes, refreshing every guard so a second Apply would pin it wholesale); and `fidelityFixPlan` excluded green combined forms that still ride the class anchor via `precompAnchor`'s fallback, so fixing a flagged rafe on ש could silently shift an exact שׁ/שׂ dot while claiming to reduce off-rows. Round 2 finished the unverified findings + first review of the progress-modal commit (29/29 agents, 0 errors; 6/10 outstanding confirmed, 4 refuted **because round 1's fixes had already closed them**, 4/4 new-code findings confirmed). **The headline catch: `implied()`/`row.fix` were unrounded** — `markAttachOrigin`'s bbox fallback is fractional for any mark without an imported `attachAnchor` (12 built-ins are), and the value reached the FEA verbatim as `<anchor 250 -126.00288351272705>`, which **real feaLib rejects outright** — one such combo would have failed the WHOLE export. Both seed fonts happen to ship integer anchors, which is why the e2e acceptance had passed. Also fixed: the two QA synth bases (`qaSynthWideRow`, `qaSynthPrecompRow`) inherited pins by reference while transforming/relocating the anchors those pins refine (QA ≠ export — `dropKeyPins`/`mapPinsX` now shared by both surfaces); never-pin-center enforced at the reader (`markPinFor`) rather than trusted of every writer; the button-less progress modal let Tab walk the workspace behind it (`_focusables` returns [] → the trap bailed); a rider form fix landed as a permanent `pc.anchor` even when the anchor move that required it was skipped at apply time; `pc.anchor` gained `auto` provenance so one font's form fixes don't survive into the next import as user truth; `appliedTargets` unioned not replaced; the ss01 Sheva Na alternate joins the sheva's pin subclass; `buildUfoAnchors` mirrors the suffixed mkmk partner for a pinned centre trop; the pins row counts only live pins; and undoing the fixes clears the report that asserted them. | verified: 6 new regressions all fail against the prior commit and pass after (incl. the fractional `<anchor …>` in the FEA and the escaped focus landing on BODY); full suites re-run green — 15/15 fidelity (golden no-pin FEA still byte-identical, both seed fonts still 0 flagged), 19/19 progress modal, 7/7 + 12/12 targeted; `check-i18n` clean; 0 pageerrors

- [x] 2026-08-20 | branch `claude/nikkud-position-autofix-nan5bo` (plan-driven feature build — NOT an improveloop session) | Hebrew_Font_Maker.html | **Fidelity Autofix v2 (FM v5.16): the report's Apply button now fixes EVERY measurable flagged combo — three vehicles in one undoable batch, then an automatic re-verify against the original bytes.** `fidelityFixPlan` (pure) picks per flagged row: **form** (the v5.14 `pc.anchor` write, unchanged); **anchor** — the letter's shared class anchor moves to the component-wise median of the group's implied anchors (`real + attachOrigin − markNudge`, over ALL measured rows of that letter × key, greens included) only when that strictly reduces off-rows; **pin** — NEW per-(letter × mark) `l.markAnchors = {cp: {x,y,key}}` reproducing measured truth exactly (what finally fixes the D2 `classResidual` combos v5.14 could only report). Never pins center (dagesh keeps one home, mirroring `precompAnchor`); `baseAnchorPosFor` is the only sanctioned reader (predict/stage/QA `qaAnchorFor`/spacing/inkBounds/`builtPerLetter` bake/FEA/UFO all flow through it, key-mismatch strands stale pins). Export: `buildFEA` splits each pinned mark into its own `@MC_<KEY>_<cp>` subclass with per-base lines (pin ?? shared — partition at emission only; `byClass`/`tropGroups` stay whole for GDEF/slide/ss01), extra mkmk lines keep pinned center trops stacking, wide forms map pins through `wideX`, shin/sin holam-slot synthetics drop above-pins, `buildUfoAnchors` mirrors the naming. Lifecycle: manual anchor edits translate same-key pins (`moveClassAnchor`; the autofix writes raw absolutes), the anchor editor edits the PIN when the previewed mark is pinned (gold ✎ badges, Unpin/Clear-pins, `writeActiveAnchor`), import clears pins, boldGen rescales them, inherit skips pinned letters, `sanitizeMarkAnchors` guards loads. Re-verify: `_fidelityLast.ctx` stashes bytes/upm/coverage RUNTIME-ONLY; `fidelityRerun()` re-measures, `appliedTargets` lets this session's own form fixes re-verify instead of skipping as user-set; apply-time D3 guards skip (and count) anything the user touched since measurement. Modal: fix-type cells + legend + post-fix recheck banner; **Fidelity report** reopen button in the Nikkud under-strip (the report was one-shot before). CSV: −2 obsolete keys, +16 `fontmaker.fidelity.*` (EN+HE) + `changelog.v5_16`; CHANGELOG_ORDER/CUR_KEY advanced; SW v528→v529; FM 5.15→5.16; CLAUDE.md fidelity section extended. | verified (offline harness: real page code with local harfbuzzjs route-fulfilled, GPOS read precomputed via the page's own extracted `hfm_read_anchors` + system fontTools 4.63): **Shlomo 913 combos, 203 flagged (max 342, mean 23.2) → plan 1 anchor + 202 pins → re-measure 0 flagged, max 0, mean 0**; **Taamey 940 combos, 96 flagged (max 398) → 3 anchors + 73 pins → 0 flagged, max 3, mean 0.1**; ONE undo restores the full pre-fix state (deep-compare) and redo re-applies; golden compare — no-pin project's FEA **byte-identical** to pre-change (2e2bf04); END-TO-END: the fixed Shlomo spec compiled with REAL feaLib (fellBack []) and the BUILT bytes shaped with REAL HarfBuzz land **all 203 previously-flagged combos EXACTLY (dev 0)** at their pins, 203/203 ink-equivalent to the original font, pinned-trop mkmk stacking intact, `harfbuzzGate(built)` ok; the real Apply-button click flow reopens the fresh modal ("Re-checked after fixes: max deviation 0 … 0 combos still flagged", Apply gone); pin ✎/Unpin/Clear round-trip under undo; EN/HE × light/dark × 1280/800 screenshots eyeballed; `check-i18n` clean; 0 pageerrors in every arm

<!-- archived 2026-08-23 at S246 close-out: Done overflow, S241 -->

- [x] 2026-08-22 | (S241 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-mobile-support-pass-ifhznq` → draft PR #192**, which was still OPEN and unmerged at session start (checked, per the protocol, not assumed) — so no fresh branch and no new PR. The PR now carries **THREE sessions (S239+S240+S241, 14 commits)**. **Drift check: `origin/main` sat unmoved at `ba91468` for the THIRD consecutive session** — nothing landed outside the loop; branch `sw.js` v545 vs main's v543, `FONT_MAKER_VERSION` 5.24 on both at start. **SW v545→v546** _(→ **v547** on 2026-08-22 by the maintainer-directed v5.25 finish, e8d664e, in this same unmerged PR — v546 never deployed)_ (one precached file changed: `Hebrew_Font_Maker.html`, in all three commits). **FONT_MAKER_VERSION 5.24→5.25 — bumped, correctly**: the session shipped a genuine feature (the fullscreen draw canvas), so one combined bump + one changelog entry per CLAUDE.md, written into all four required places (constant, authored `HELP_CONTENT.about` `<li>`, `fontmaker.changelog.v5_25` CSV row EN+HE, `CHANGELOG_ORDER` + `CUR_KEY`) and verified rendering in both languages. The two pure fixes did NOT bump. **Scripts run:** `check-inline-js` (clean, 62+36 blocks), `check-i18n` (clean, no new violations), `build-locales` (4677 keys), `update-sitemap.mjs` as the last step. **⚑ PROTOCOL DIVERGENCE, flagged deliberately:** the variety governor caps a tool at 2 iterations/session and this session charged **font-maker 3** (two fixes + the micro-feature). The maintainer's explicit "focus on fontmaker" direction plus a gate-1 answer selecting a *Font Maker* seed is the override — the governor exists so one hot spot cannot eat a session unasked, and here it was asked for. Recorded so the pattern is visible rather than silent. **Decisions deferred to maintainer:** none — both gates were asked and answered in-session. **Deploy verification happens after merge** (Pages builds on `main`, not on the branch). | verified: gates clean, 66 headless assertions across three commits

- [x] 2026-08-22 | 205f4e1 | Hebrew_Font_Maker.html + `locales/` | **Fullscreen gives the drawing canvas the screen (micro-feature, FM v5.25)** — the gate-1-selected "tablet-optimized fullscreen canvas" seed, shipped WHOLE rather than as the slice the question offered. Fullscreen already dropped header+footer, but the canvas kept its desktop caps (720px / 74vh) and shared the screen with every settings panel, so the stylus-first classroom device got a small box with the pen tools below the fold. `body.fullscreen.mode-draw` (one derived class, synced at renderControls' existing self-guarding chrome point — no stored state, no AllTools wiring) folds the settings column and uncaps the canvas; ≥1181px the tools move beside it. **The design was REVERSED mid-iteration by its own verification:** the first cut forced two columns at every width and measured **720px → 408px on an iPad in portrait**, i.e. narrower than shipping nothing — so the two-column rule was gated on the workspace's existing 1181px stacking boundary (converging, not inventing a breakpoint) and stacked widths take only the taller canvas. | verified: 40 assertions over `iPad (gen 7)` portrait+landscape descriptors, 1280 light+dark, 800 HE — canvas wider in every cell (720→770, 720→1040, 620→878), Save on-screen everywhere, 0 overflow, 0 pageerrors; RTL mirrors the tools column (measured 1400 HE); controls: leaving the Draw step while STILL fullscreen restores the column and the 740px canvas, exiting fullscreen restores exactly, fresh profile (empty localStorage+IndexedDB) clean; changelog renders EN+HE

- [x] 2026-08-22 | feb839a | Hebrew_Font_Maker.html + `locales/` | **The Help finally documents Drawing mode** (S241 Pass H finding 2, gate-2 approved as "both, minimal"). The changelog announced Draw mode at v5.7 ("now draw your letters right on screen"); the instructions never absorbed it — **every "Draw" mention in the 331-line help corpus was in the CHANGELOG, none in the instructional tabs**. Help > Getting started still opened its "whole journey, start to finish" with *"Upload a clear picture of each letter"*, and Help > Uploading listed **3 of the 4** ways to add letters. Three minimal edits: journey step 1 now covers both sources (still five steps), the Add Letters list gains its missing fourth option and the intro sentence stops promising a picture of every letter, and the tour's step-1 title gets a draw variant (it read **"Step 1 — Align"** while spotlighting a tab labelled **"1 · Draw"**). One new key (`fontmaker.tour.step_2_title_draw`); its Hebrew reuses the word the shipped `fontmaker.tabs.step_draw` already uses, so not a fresh translation. **Method note:** the first CSV pass went through `csv.writer` and silently re-quoted **17 untouched rows** (21+/20− for a 3-row change) — reverted and redone as a line-level text edit (3+/2−). Never round-trip `ui-strings.csv` through a CSV writer. | verified: 26 assertions — Help journey + source list + tour title in light/dark × EN/HE × 1280/800, 0 pageerrors, 0 overflow, no raw i18n keys rendered; CONTROL: a non-draw project still gets "Step 1 — Align", so the variant does not leak

- [x] 2026-08-22 | f4f9baa | Hebrew_Font_Maker.html | **Drawing mode's "what's next" hint finally reaches the screen** (S241 Pass H finding 1). `renderNextStepHint()` branches on `project.inputMode` and its draw branch even carries the comment *"upload-flavored copy is wrong for a draw project"* — but the hint only ever re-renders from `renderLetterGrid()`, and **`setInputMode()`, the one function that changes the mode it branches on, re-renders controls and stage and never the grid**. So the draw copy had **never once rendered for a user**: both the wizard's Drawing-mode path and a mid-project switch to "Draw letters on screen" left *"Start by uploading and tracing your first letter"* on screen, one line under a step tab reading **"1 · Draw"**. One line, at the mode-change chokepoint. The wrong window is exactly project-creation → first committed letter, i.e. the moment a new teacher has no other guidance. **The siblings were all measured firing correctly** (tour `bodyKeyDraw`, the `step_draw` tab label, the mode-button state), which is what bounded this to a single-line fix. | verified: 8 assertions — wizard→draw and mid-project switch both show the draw copy in light/dark × EN/HE × 1280/800, 0 pageerrors, 0 overflow; switching back to Individual pictures restores the upload copy (tracks the mode both ways); the same assertions measured MISMATCH on the pre-fix bytes

<!-- archived 2026-08-23 at S247 close-out: Done overflow, S242 -->

- [x] 2026-08-22 | (S242 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-mobile-support-pass-ifhznq` → draft PR #192**, still OPEN and unmerged at session start (verified via the API, not assumed: open/draft, `mergeable_state: clean`, 0 reviews, 0 comments, 0 check runs — the repo has no CI). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `ba91468`, exactly where the S241 close-out recorded it, now unmoved through FOUR sessions (S239–S242); `sw.js` was v547 and `FONT_MAKER_VERSION` 5.25, both of which are the loop's own ledgered S241-follow-up work, not maintainer drift. Verified by diffing against the recorded position rather than trusting the note (the standing S225 lesson). **`sw.js` v547 → v548** — precached files changed: `Hebrew_Font_Maker.html` (all three fixes) and `locales/en.json`+`he.json`. **`FONT_MAKER_VERSION` 5.25 → 5.26**, ONE combined bump for the one new feature (Tidy strokes); the fullscreen panels control was deliberately folded into the UNRELEASED v5.25 entry instead of taking its own number, because it completes that feature and PR #192 has never merged, so no user has seen v5.25. **Scripts run:** `build-locales.js` (4677 → 4686 keys, 9 added), `check-i18n.js` (Check A + Check B clean), `check-inline-js.mjs` (62 JS + 36 JSON-LD blocks parse), `update-llms-txt.mjs` (regenerated in dd92327, 47→48 questions; `--check` current), `update-sitemap.mjs` as the last step. **⚑ PROTOCOL DIVERGENCE, flagged deliberately (second session running):** S242 charged the Font Maker **4 iterations** against the variety governor's 2-per-tool cap. The override is the maintainer's `on fontmaker` direction PLUS two gate answers that both independently named the Font Maker (gate 1 chose the FM auto-beautify seed over "skip"; gate 4 asked for an FM collapse button). Recorded here rather than left silent — and the S243 pointer explicitly hands the next session back to the non-FM backlog. **Decisions deferred to maintainer: NONE** — all three gates were asked and answered in-session. One candidate was created BY a gate answer (the "minimal" scope left the `<title>`/meta-description/ItemList blurb handwriting-only) and is logged as the top Candidate, not a deferral. **Deploy:** Pages deploys on merge to `main`, so deploy `success` is verified AFTER the maintainer merges — not in-session.

- [x] 2026-08-22 | e5c94c1 | Hebrew_Font_Maker.html + `locales/` | **MICRO-FEATURE: "Tidy strokes" — clean up a drawn letter without losing the hand that drew it (FM v5.26)** — the gate-1-selected M-size Draw-mode seed from 2026-08-06, chosen over "skip" on the maintainer's "on fontmaker" direction. The seed's own guard rail ("a child's handwriting isn't sterilized") chose the shape: **not** live snapping, but one button → one `udDo` entry → Ctrl+Z restores byte-for-byte. Straightens a stroke whose every point lies within 4.5% of its length of the chord; snaps an endpoint within 24 FU of a metric line or user guide. Plans on COPIES first, so a no-op press pushes no undo entry. | **verified** headless local HTTP, 1400 + 800px × light/dark = 60 checks all pass, on fixtures chosen so each pass has something it must LEAVE ALONE (near-straight → 2 pts BUT a real arc untouched; end 10 FU above baseline snaps BUT one 200 FU above does not); honest counts; exactly one undo entry; a second press pushes none; undo restores byte-exactly; **a non-vacuous `drawSig` control** (set to a real value first so "unchanged" could fail) proving Save re-arms; fresh-profile run; autosave+reload round-trip. 84 lines, 0 new storage keys.

- [x] 2026-08-22 | 5529836 | Hebrew_Font_Maker.html + `locales/` | **Fullscreen drawing can hand the panels back (finishes v5.25; gate-4 answer)** — the maintainer answered the gate-4(a) question the S242 pointer had flagged with an option not on the menu: *keep it at every width, but offer a collapse button*. Every rule in the fullscreen-draw block is now scoped `:not(.fs-panels)` (10 selectors, mechanically), and a **"Show Panels"** button — reusing `#fsExitBtn`'s chrome by joining its selectors rather than inventing a second floating-button style — is the only thing that sets it. Shown only in fullscreen AND on the Draw step. Choice remembered per device via the existing `wsSavePref` → `hebrewFontMaker_uiPrefs`. Pure UI state: no `udDo`, no `markDirty`. | **verified** headless at desktop 1400 / tablet 1024×768 / stacked 800 × light+dark = 60 checks all pass, **including the control that proves the scoping** (hidden in fullscreen but OUTSIDE the Draw step) and the canvas cap returning with the panels (none→100% desktop, none→720px stacked); EN+HE render real text not raw keys, `dir=rtl`; preference survives reload; 0 pageerrors. Changelog folded into the UNRELEASED v5.25 entry rather than bumping — PR #192 has not merged, so v5.25 has never reached a user.

- [x] 2026-08-22 | dd92327 | Hebrew_Font_Maker.html + `llms-full.txt` | **The structured data admits you can edit an existing font (Pass L's one hit)** — JSON-LD ↔ shipped-capability parity, the same shape as S229's fix to this page, one layer out. Since the last L the FM became a font EDITOR too (v5.14 per-mark attachment positions, v5.18 English A–Z, v5.19 punctuation/currency/symbols), yet **all five crawler-facing surfaces still framed it as handwriting-only** and **none of the 8 FAQ questions mentioned import at all** — one clause in HowTo step 1 was the sole acknowledgement anywhere. Gate 2 answered "minimal — structured data only", so exactly two things moved: the `WebApplication` summary and one new FAQ question (now 9). | **verified** every claim in the new answer grounded in code BEFORE publishing (per the closed-enumeration rule this pass applies to others' copy): `hfm_read_anchors`→`m.attachAnchor` (23 refs), the `ENGLISH_LETTERS` + `LATIN_IMPORT_ONLY` loops, `nPunct` in the same import counter, the 3 export formats. Visible twin checked first (the wizard's "Existing font file" card — already accurate). 4/4 ld+json parse in the served DOM, 0 pageerrors at 1280/800 × light/dark; `llms-full.txt` regenerated in the SAME commit (47→48 questions) and `llms.txt` correctly unchanged.

- [x] 2026-08-22 | e8d664e | Hebrew_Font_Maker.html + `locales/` | **Fullscreen Draw: on a tablet the Save actions now stay with the drawing (finishes the v5.25 micro-feature; maintainer-directed follow-up to S241)** — v5.25 shipped the layout half but delivered its own headline promise ("stop scrolling between a stroke and its Save") only above the workspace's 1181px boundary, i.e. **on desktop only**. Measured on real tablet geometries, `#stageBelow` sat at **y=926..1048 in viewports 768..1024 tall** — off-screen on every iPad but the two largest in portrait. **Root cause is upstream of the canvas cap and no cap could have fixed it:** the toolbar (112px) + category tabs + letter grid + step tabs (277px) eat **~389px, 51% of a landscape iPad**, before the canvas starts. Fix is `position:sticky; bottom:0` on `#stageBelow`, scoped to `body.fullscreen.mode-draw` inside the existing `≤1180px` arm — nothing moved, hidden or reordered. **Verified:** Save-reachable-at-scroll-top flips false→true on iPad Mini/10.2"/Air landscape + Mini portrait, stays true on Air portrait (no-op); **controls that can fail** — desktop 1400 and 1280 stay non-sticky (so **no gate-4(a) desktop change**), and leaving the Draw step or leaving fullscreen both return `position:static`; light + dark, 0 pageerrors; before/after screenshots at 1024×768 both themes. **A measured NEGATIVE result is half this entry's value:** extending the two-column layout down to landscape tablets — the obvious "finish" — makes the canvas **worse** on the smaller ones (1024×768 −37px, 1080×810 −41px on the binding dimension), because the 240px tool column costs more width than the freed height gains; **S241's 1181px boundary stands, now on 10 measured geometries rather than one.** Changelog folded into the **unreleased** v5.25 entry (EN+HE) rather than bumping to v5.26 — PR #192 has not merged, so v5.25 has never reached a user. **SW v546→v547.** | verified: Playwright, 8 geometries × light/dark + 3-state reversibility control

<!-- archived 2026-08-23 at S248 close-out: Done overflow, S243 -->
- [x] 2026-08-22 | (S243 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-mobile-support-pass-ifhznq` → draft PR #192**, verified OPEN/draft/unmerged via the API at session start rather than assumed (`mergeable_state: clean`, 21 commits, 0 reviews, 0 check runs). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `ba91468`, unmoved through **five** sessions now (S239–S243); `sw.js` was v548 and `FONT_MAKER_VERSION` 5.26 exactly as S242 recorded, and both were re-measured, not trusted. **SW v548→v549** (precached files changed: `Hebrew_Font_Maker.html`, `locales/en.json`, `locales/he.json`). **FONT_MAKER_VERSION NOT bumped, deliberately** — all four iterations are i18n *defect repairs* (English leaking into the Hebrew UI), and the house rule bumps for features, not fixes; no changelog entry either, since there is no capability to announce. Scripts run: `check-i18n` (clean, 14 baselined, Check B clean), `build-locales` (4686→4808 keys), `check-inline-js` (62 JS + 36 JSON-LD blocks), `update-llms-txt --check` (current — no title/description/JSON-LD moved), `update-sitemap` last. **Gates: 3 asked, 3 answered, 0 deferred** — (2) undo toast scope → "Localize all 56 + prefix"; (2) FM `<title>`/description/tile blurb → "Still minimal — leave the copy" (**asked twice now, same answer both times — treat this gate as CLOSED, not open**); (1) micro-feature → "Skip it — spend the budget on fixes". **⚑ Protocol divergence, third session running: the Font Maker took 4 of 5 iterations against the 2-per-tool cap**, on the explicit `on the fontmaker` direction — which the S243 pointer had itself pre-authorized ("Unless the maintainer again says 'on fontmaker'…"). Deploy `success` is verified after the maintainer merges; `origin/main` has not moved in five sessions, so nothing from S239–S243 is live.
- [x] 2026-08-22 | 81f8727 | Hebrew_Font_Maker.html | **Three stage strings had Hebrew waiting and were showing English** — `fontmaker.panels.not_traced`, `fontmaker.me.baseline_label` and `fontmaker.me.no_pieces` are all authored AND translated; three render sites printed the English literal anyway. Pure wiring: no new copy, no new keys, each site copying the idiom used one or two lines away in the same function. Surfaced by running the blind-spot detector over the WHOLE file instead of the session delta, then filtering out strings sitting inside an `I18n.t`/`_pT` fallback — which were **40 of the 50 raw template-literal hits**, i.e. the filter is what made the signal readable. | **verified**: 8 arms (he/en × 1280/800 × light/dark), each branch proven REACHED before its text was read — the first attempt reported PASS while rendering a *sibling* branch, so every assertion now checks a marker only that branch emits (its own `y="520"`, the baseline guide's `stroke-dasharray="5 4"`, the `.hint` node). Hebrew arms match the Hebrew string specifically, so a still-hardcoded site fails.
- [x] 2026-08-22 | 93cb3a3 | Hebrew_Font_Maker.html + `locales/` | **Finish the undo toast — `udDo` was not the only label producer.** Re-running the detector over the whole file (not the delta) found three more feeders into the same toast: `udCapture` (drag handles), `udBurstBegin` (slider + typed-field bursts) and `udNudgeTick` (arrow-key nudges) — i.e. **every continuous edit in the tool**, which is the most-used half of the undo surface. 38 more keys, same display-side lookup, still zero call sites touched. **120 stems total.** Three extraction artifacts dropped rather than keyed (`''` and `' points'` are tail halves of a concatenation; `'rot'` is a ternary discriminant, `kind === 'rot'`), and the three axis labels (`'anchor ' + axis`) keyed as their six real runtime strings since `axis` is only ever `'x'`/`'y'`. | **verified**: 120/120 stems extracted FROM SOURCE across all four producers, both languages, stem-only assertion, keyless-stem control still fires, sentinel intact, 0 pageerrors.
- [x] 2026-08-22 | 4ebd846 | Hebrew_Font_Maker.html + `locales/` | **Ctrl+Z speaks Hebrew now — Pass K's headline finding.** In the Hebrew UI every undo/redo toast was fully English (`"Undo: reset width"`) on a page whose other **259** elements were Hebrew. **Fifteen previous K runs missed it, and the reason is structural, not sloppiness:** K's delta probe only reads newly-changed lines (this code is old) and its runtime detector reads the English left ON SCREEN (this string lives 4 seconds, and only after you press Ctrl+Z) — it sat precisely in the gap between the two arms. **The labels are STORED raw and stay that way**: `undo()` compares `e.label` against `'fidelity fixes'` to decide whether to stash the fidelity report, so the stored string is a *sentinel*, not display text. Translation happens at the one place the label is shown — the same split `gName()` already documents in its own comment ("used only at DISPLAY sites; internal undo labels keep the raw name"), which is what vindicated the architecture before it was written. `undoLabelText()` splits stem from glyph-name tail, translates the stem via `fontmaker.undolabel.*`, and runs the tail back through `gName()` so "Alef" reads "אלף" too; counted labels ("apply alignment to 12 letters") normalize onto one `{n}` key; **a stem with no key falls back to its own English**, so a `udDo()` label added later degrades to English rather than printing a raw key at a teacher. 84 keys (82 action names + the two toast frames), Hebrew authored here and marked `REVIEW:` for the maintainer. **Not one `udDo()` call site changed.** | **verified**: all 82 stems extracted from the source (not hand-listed), asserted in both languages; **the assertion was rewritten after the first version passed on the strength of the Hebrew prefix and glyph name alone** (an unearned verdict — the S218 trap), and a deliberately keyless stem now proves it can fail; sentinel re-checked live after a real edit + Ctrl+Z; matrix he/en × 1280/800 × light/dark 8/8.
- [x] 2026-08-22 | f50cd23 | Hebrew_Font_Maker.html | **The QA grid says אלף in Hebrew, like every other grid does.** The letter, mark and composed-form grids all route names through `gName()`; the QA grid rendered `esc(row.name)` raw, so the same letter read **אלף** in the workspace and **"Alef"** in the quality-check table one row apart. One line, both the visible span and its `title`. **This is the FOURTH instance of the "a sibling one line away is already translated" shape** (S177 → S211 → S218 → S243), which the S218 entry said to register as a pattern on the fourth — done below. | **verified**: 8 arms (he/en × 1280/800 × light/dark), 27 rows titled in every arm, 0 wrong-language labels, 0 pageerrors; Hebrew "א אלף", English "א Alef".

<!-- archived 2026-08-23 at S249 close-out: Done overflow, S244 -->
- [x] 2026-08-22 | (S244 close-out) | branch/deploy note | **Branch/PR: NEITHER of the protocol's two options — the maintainer was ASKED and chose to continue on `claude/font-maker-gridlines-snap-ihixmk` → draft PR #193, this conversation's own non-loop task branch.** The protocol's rule would have cut a FRESH branch: PR #192 (S239–S243, 26 commits) **MERGED** as `5d89020` between sessions, so there was no open loop branch to continue. The question was put because this session carries an operator instruction naming that branch and forbidding pushes elsewhere without permission; the answer makes PR #193 a **two-concern PR** (Font Maker grid defaults + S244 mobile fixes) and its title/body were rewritten to say so. **⚑ Protocol divergence flagged for the skill:** a session may arrive pre-assigned a branch that is not the loop's — the branch rule needs an "unless the session is pinned to a branch, in which case ask" clause. **Drift check, done by diffing rather than trusting the S244 pointer:** the pointer said `origin/main` sat at `ba91468` — it did NOT; it is at `5d89020` (PR #192 merged). Also landed outside the loop, earlier in this same conversation: the Font Maker grid-defaults change (maintainer request "gridlines on by default, snap off by default"), which moved **sw.js v549→v550** and **FONT_MAKER_VERSION 5.26→5.27** before the loop began. **This session: sw.js v550→v551** (final commit) for two precached edits — `flash_cards.html` and `classroom_dashboard.html`. **No FM bump for the loop's own work** (it touched neither the Font Maker nor any feature — the 5.27 bump belongs to the grid-defaults task). Scripts: `check-i18n.js` clean (no new strings added — the fixes are CSS + HTML attributes only), `check-inline-js.mjs` clean, `update-sitemap.mjs` run last. **Decisions asked and answered (2):** gate 1 micro-feature → **skip, spend the budget on mobile** (maintainer's choice, matching the session's "focus on the mobile support pass" direction); branch → **same branch/PR #193**. **Decisions deferred: none.** Deploy verifies after the maintainer merges PR #193.

- [x] 2026-08-22 | a46fd60 | classroom_dashboard.html | **The YouTube URL box asked a phone for a sentence keyboard** (S244 Pass N arm 5, 2nd tool of the `mobile-input-hints` class this session, and the closing half of the S239 candidate's named actionable set). `#videoUrlInput` was `type="text"` with no hints → sentence keyboard, capitalised first character, spell-check squiggles on a URL. Now `type="url"` (a keyboard carrying `/` and `.com`) + the `autocapitalize/autocorrect/spellcheck` triplet. **Retyping the field was grounded as safe before the edit, not assumed:** the page has **0 `<form>` elements** (so URL validation can never block a submission) and **0 `:invalid` CSS rules** (so nothing paints red while typing); both consumers touch only `.value` — `updateVideoEmbed()` reads it, `syncFormToSettings()` writes it. | **verified** iPhone SE / iPhone 13 / 1280 / 800 × light+dark = 8 cells: a pasted URL reaches `settings.videoEmbedUrl` through the real handler and returns to the field on re-sync, in all 8; 0 pageerrors.

- [x] 2026-08-22 | 46d1000 | flash_cards.html | **Phone keyboards mangled the share-code and backup paste boxes** (S244 Pass N arm 5, closing the candidate S239 logged from the DICTIONARY's audit — the arm-5 control that measured a field the previous session did not touch). `#shareCodeInput` takes a case- and character-exact teacher share code and `#backupArea` takes exported preset JSON; both carried no typing hints, so a phone capitalised the first character, offered autocorrect and drew spell-check squiggles under a 2,300-character code. Both now carry `autocapitalize="off" autocorrect="off" spellcheck="false"` — the same triplet and placement as the dictionary fix (b0a9a10). **`#backupArea` was a NEW find of the same class**, surfaced by re-running the census on the audited surface rather than trusting the logged list. `#presetName` deliberately left alone (a human name wants a capital) — the control proving this is targeted, not a blanket sweep. | **verified** attributes present on iPhone SE / iPhone 13 / 1280 / 800 × light+dark = 8 cells with `#presetName` untouched in all 8, plus a functional share-code round trip on a phone descriptor: generate at 3-Letter → reset to 2-Letter → paste back → mode restored (2,323-char code); 0 pageerrors.

- [x] 2026-08-22 | 8f80eb3 | flash_cards.html | **The Mode pills were CLIPPED by their own panel on a 320px phone** (S244 Pass N arm 1 — the run's one reflow finding). `.pill-group` is `inline-flex` with `flex-wrap: nowrap`, so the four Mode pills laid out **295px wide inside a 258px row**; nothing scrolled it away because `.panel` is `overflow: hidden` (it clips `.panel-title` to the rounded corners), so the group ran **20px past the panel edge and was cut at any scroll position** — "Other" lost the tail of its label (measured: text right edge 308 vs clip edge 306) and the pill track lost its right end, while every label broke onto two lines in the squeeze. Fixed by letting pill groups wrap inside the page's existing `@media (max-width: 420px)` block — **converging on what `#timerModePills` and `#timeLimitPills` already do inline**, and robust where padding-tuning is not: wrapping cannot clip whatever a label translates to, and EN/HE differ here (EN 69px pills, HE 55px). **EN-only and 320-only before the fix** — Hebrew's shorter labels fit, and 390/412/landscape had room. | **verified** on real device descriptors (never a resized window): iPhone SE / iPhone 13 / Pixel 7 / 13-landscape × light+dark × EN+HE = 16 cells, clipping **20px → 0** in every one, label text no longer clipped, 0 pageerrors; desktop 1280 + 800 unchanged (computed `flex-wrap` stays `nowrap` above 420px). BEFORE/AFTER screenshots at 320 and 390 delivered at close-out.

---

## Archived at the S251 close-out (2026-08-25) — S245 + S246 Done entries

- [x] 2026-08-23 | (S246 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-m-n-passes-uy07ou` → draft PR #196**, verified OPEN / draft / unmerged / `mergeable_state: clean` via the API at session start rather than assumed (head `8490f50`, 8 commits, 0 reviews, 0 check runs — the repo has no CI). The PR now carries **two sessions (S245 + S246, 13 commits)**. **Drift check: NOTHING landed outside the loop** — `origin/main` still at `b745745`, exactly where the S245 close-out recorded it, and the branch was 8 ahead / 0 behind; `sw.js` was v555 on the branch (S245's bump) against v554 on main, and `FONT_MAKER_VERSION` sat at 5.28 unchanged. Diffed `origin/main` myself per the standing S225 lesson rather than trusting the note. **`sw.js` v555→v556**, once, in the final commit — **7 precached pages changed** (index, 404, resources, contact, privacy, terms, Hebrew_Font_Maker). **`FONT_MAKER_VERSION` deliberately NOT bumped** and no changelog entry: the Font Maker's only change is three typing-hint attributes on one input, which is a pure fix, not a feature (CLAUDE.md: "pure fixes/token swaps don't bump"). This also **ends the Font Maker's six-session run of being the session's main tool** — it was touched here only as the second carrier of a shared-component defect. **Scripts:** `check-inline-js` clean (62 inline blocks + 36 JSON-LD across 14 pages), `check-i18n` clean (Check A no new violations, Check B clean), `build-locales` correctly NOT run (zero CSV keys added — every change is CSS or an HTML attribute), `update-llms-txt --check` reports both files current (no title / meta-description / JSON-LD changed), `update-sitemap.mjs` run LAST. **Decision gates: 0 asked, 0 deferred.** Gate 3 was considered and not tripped — every visual change is a convergence onto a rule four sibling pages already carry, which the gate names as explicitly autonomous. Gate 4 was considered and not tripped — no change is scoped inside a phone media query, nothing was restructured or hidden at phone widths, and the desktop effect was measured, not assumed. **Deploy `success` is verified after the maintainer merges** (Pages deploys on merge to `main`, not from the branch).

- [x] 2026-08-23 | (S246 Pass M — aesthetics & visual design, **4th-ever run**, `index.html`; target re-derived per the M row's own instruction, and it agreed with the S246 pointer: index/chrome was the stalest coverage line at **S238**, ~8 sessions, and index.html had never been M-audited — the site's front door and the one page every teacher and every crawler sees first) | index.html | **Full matrix light/dark × 1280/800 × EN/HE (8 cells), 70 visible elements, 0 pageerrors, `scrollWidth == innerWidth` in every cell, `dir` correct 8/8. VERDICT: a well-kept page with one real defect — and the defect was a same-day rollout miss, not drift.** `.hright` (the header control group) never got `display:flex`, so the language switcher and the Dark button **stacked**, 89×60, against the single 30px row on resources / contact / privacy / terms. Traced to the 2026-07-12 i18n rollout: `243f0f4` ("page 8/11") gave index the container without the rule, and `051440e` — *the same commit that gave contact and privacy theirs* — missed 404 the same way. The two pages whose `.hright` holds exactly two children were the two that missed it. Fixed `2ad8301`. **The narrow-width control is what made the fix correct rather than half-correct:** flexing alone overflowed 320px to 376 and pushed Dark to `left:-52` in Hebrew, on pages that previously had none — the other four carry a `@media (max-width:430px)` header-wrap block, comment and all, and these two were missed for that too; contact.html measured clean at 320 throughout as the control, which localised the gap to the missing block rather than to the flex. **Contrast: 0 misses in BOTH themes** — and this run's probe **composites alpha**, the exact defect that made S245's contrast arm produce 24 false hits, so the zero is earned rather than blind. **Dark parity: 1 element** identical in both themes (`header`, navy on `#fff`), grounded as deliberate — `--navy` is not among the tokens dark mode overrides, and CLAUDE.md prescribes literal `#fff` on navy surfaces. **Grid discipline perfect** once the detector was corrected (see the two method candidates). Standing measurements logged: a four-size 2.08px type band (4th page with this shape) and five radii. **No gate 3 — nothing dramatic to propose.** Screenshot rule honoured: 12 BEFORE + 12 AFTER header crops across index / 404 / contact × EN/HE × light/dark, plus 8 full-page matrix frames, delivered at close-out, never committed. Surfaces now M-audited: font-maker (S225), dictionary (S237), trope-tutor (S245), **index (S246)**. **Next M: another chrome page (resources / contact — resources now carries two brand-new shared components) or the stalest remaining tool; re-derive by staleness.**

- [x] 2026-08-23 | (S246 Pass N — mobile & touch-device, **4th-ever run**, `resources.html`; the S246 pointer's named prime target — never N-audited, and the carrier of BOTH shared components that landed outside the loop, neither ever touch-tested) | resources.html | **7 arms on REAL device descriptors (`iPhone SE` 320 / `iPhone 13` 390 / `Pixel 7` 412 / 13-landscape 750; flags verified `coarse:true, hoverNone:true, maxTouchPoints:1`, dpr 2/3/2.625/3). 6 of 7 arms CLEAN, 1 hit, fixed.** **The headline is a clean result, and it is the one the pass existed to get:** the **on-screen Hebrew keyboard works correctly under real touch** — the component whose entire purpose is typing Hebrew on a device with no Hebrew keyboard, i.e. a phone, and which had never once been tested with a finger. Its keys fire on `click` behind a `pointerdown`+`preventDefault()`, a combination that can suppress `click` on touch; measured instead of reasoned about: toggle opens by tap (`aria-expanded=true`, 35 keys, label flips), **3 key taps → `אבג`**, the preview **re-renders** (so the documented `onChange` contract fires — `setRangeText` emits no `input` event), the Nikkud tab attaches a mark correctly (`U+05D0 U+05D1 U+05D2 U+05B8`, decomposed), and a test-phrase chip fills and re-renders. Identical in EN and HE, 0 pageerrors. **Arm 1: 16/16 cells clean** (4 devices × EN/HE × light/dark) with the keyboard OPEN inside the preview modal — the panel's `max-height:min(46vh,340px)` resolves to 261 / 305 / 340 / 157px, the modal bottom stays on screen in every cell, and the ✕ is hit-testable in every cell (grounded with `elementFromPoint`, per S244's occlusion lesson). **Arm 2 clean** (`width=device-width, initial-scale=1`; no `user-scalable=no`, no `maximum-scale` — the control that proves the detector can fire). **Arm 3 clean by static audit**: one `100vh` use, `body{min-height:100vh}` — a floor, not a full-height sheet with a bottom-anchored control, so no `dvh` case. **Arm 6 clean**: three independent tap escape routes from the modal (backdrop, ✕, "‹ Back") — no standalone dead end. **Arm 7 clean**: at 4× CPU throttle the font gallery renders in 357ms and the modal+keyboard build in 297ms. **Arm 5 was the hit** (`mobile-input-hints`, fixed `ee89dd1`). Screenshot rule honoured — and this run shipped the sheet it would have shipped clean: **40 phone frames** (24 full-page: 3 devices × EN/HE × light/dark × both views; 16 modal-with-keyboard), delivered at close-out, never committed. Surfaces now N-audited: dictionary (S239), flash-cards (S244), torah-trainer (S245), **resources (S246)**. **Next N: the generator (most controls, never audited) or the Font Maker (never audited, and the third keyboard carrier); re-derive by staleness.**

- [x] 2026-08-23 | 91c57ab | resources.html + contact.html + privacy.html + terms.html | **The chrome header buttons meet the 30px floor** (`row-siblings-with-mismatched-heights`, the class S245 re-opened — found by this session's Pass-M census and then measured across the whole chrome set). `.hbtn`, the header nav link ("← Back", "Fonts"), painted **27px** beside a 30px language switcher and a 30px `.dark-btn` in the same row: a 3px spread on all four pages in both languages, and under the suite's ratified 30px control floor on the one control that leaves the page. Converged on **`.dark-btn`'s own idiom** — the floor chosen is the one its two row siblings already have — and deliberately NOT the tools' 35px, which torah_trainer and trope_tutor set for a heavier header and which would have restyled chrome rather than closed a gap. **`min-height` alone would have been half a fix on three of the four:** `.hbtn` is an `<a>` with no display rule, so a 30px box would have held the label against the top edge; `.hright`'s flex blockifies it (which is why the probe reported `display:block` and why the box binds at all), so `inline-flex` + centring ships with the floor. Verified: row spread **3 → 0** on all four pages in both languages, and a **96-cell matrix** (6 chrome pages × EN/HE × light/dark × 320/390/800/1280) with 0 overflow, 0 pageerrors, no `.hbtn` under 30px.

- [x] 2026-08-23 | ee89dd1 | resources.html + Hebrew_Font_Maker.html | **The two new Hebrew-keyboard hosts stop fighting the phone keyboard** (`mobile-input-hints`, **3rd tool of the class** after the dictionary at S239 and flash-cards + dashboard at S244; the S246 Pass-N arm-5 hit). The on-screen keyboard was adopted onto two more fields out-of-band last week — resources' `#fontPreviewInput` and the Font Maker's `#spacingSample` — and **neither host got the typing hints its first host already has**: both are `dir="rtl"` fields that exist to hold Hebrew and both were bare, so a phone capitalised the first character, offered English autocorrect over Hebrew, and drew spell-check squiggles through the specimen. The dictionary's `#searchInput` has carried `autocapitalize/autocorrect/spellcheck` since S239; these two now match it byte-for-byte. **The sharp edge: this is the field the keyboard exists FOR** — mounted for people with no Hebrew keyboard installed, i.e. exactly the phone users the missing hints hurt. **Census, not assumption:** three `dir="rtl"` text fields suite-wide lacked hints; the third, torah_trainer's `#ttCopyTextarea`, is `readonly` — no keyboard ever opens for it — so it is excluded **by evidence** rather than skipped. **No `inputmode`**: there is no Hebrew inputmode and forcing one would be the wrong fix (the same trap logged against `#ttCopyRange` at S245); the phone should still offer a Hebrew layout to users who have one. Verified on real device descriptors: both fields report the attributes **at runtime** — the Font Maker's through `renderSpacingPanel()`'s template literal, which is the edit that could have broken — and the keyboard still types into the preview field by tap in both EN and HE, 0 pageerrors.

- [x] 2026-08-23 | 2ad8301 | index.html + 404.html | **The home page and 404 lay their header controls in one row** (the S246 Pass-M hit). The 2026-07-12 i18n rollout gave every chrome page a `.hright` control group but only four of six the rule that lays it out, so on index and 404 the language switcher and the Dark button **stacked vertically** (89×60) against the other four pages' single 30px row — on the site's front door. Converged on the majority form byte-for-byte (`gap:10px`; resources uses 8px). **The flex rule is only half of it, and the narrow-width control proved it:** one row overflowed 320px to **376** and pushed the Dark button to `left:-52` in Hebrew, on two pages that had no overflow before — the other four carry a `@media (max-width:430px)` header-wrap block for exactly this, comment and all, and these two were missed for that too. **contact.html measured clean at 320 throughout as the control**, which is what localised the gap to the missing block rather than to the flex rule. After: `.hright` is one 30px row at 320/360/390/800/1280 in both languages, zero overflow, correct RTL mirror.

- [x] 2026-08-23 | (S245 close-out) | branch/deploy note | **Branch/PR: a FRESH branch, per the protocol's own rule — `claude/improveloop-m-n-passes-uy07ou`, cut off the latest `origin/main`.** The previous loop branch is gone: PR #192 (S239–S243) merged before S244, and S244's pinned branch → PR #193 **merged** on 2026-08-22, along with #194 and #195 — so there was no open loop branch to continue. A **new draft PR** is opened at close-out. **Drift check — the busiest inter-session window in a while, and NONE of it was the loop's:** `origin/main` moved **`5d89020` → `b745745`** (three maintainer-directed direct-to-main landings closing PRs #193/#194/#195), `sw.js` **v551 → v554**, `FONT_MAKER_VERSION` **5.27 → 5.28**, and the Font Maker grew to **19,008 lines**. The landed work is the on-screen Hebrew keyboard adopted into resources.html and the Font Maker's Spacing field, plus the test-phrase chips on both font pages — i.e. **two brand-new shared components (`hebrew-keyboard`, `test-phrases`) entered the repo outside the loop and neither has been swept by any pass yet.** Verified by fetching and diffing against S244's recorded position rather than trusting the pointer, per the standing lesson. **⚑ The clone arrived SHALLOW (77 commits) and was unshallowed to 2,311 before any date-based receipt was read** — `git log -S` had been returning the boundary commit `dd394aa` for every query, which would have dated the `#fsBtn` regression and the `db37a54` closure to the same meaningless commit and buried this session's headline finding. CLAUDE.md warns about this for `update-sitemap.mjs`; it bites `git log -S` archaeology just as hard. **Maintainer direction: "M and N passes only for now"** — honoured exactly: both ran, no third pass, and **G (S231) remains the stalest and is still owed the next un-directed slot** (D S232 and I S233 behind it). **Variety governor respected and stated: torah-trainer ×2 (AT CAP), trope-tutor ×1**; no tool exceeded, and the Font Maker's five-session run (S239–S243) ended — it was not touched at all. **Decision gates: 0 asked, 0 deferred, and that is a deliberate result, not an omission.** Every shipped change was an alignment/clearance fix whose desktop rendering was measured byte-identical (so no gate 3 and no gate 4(a)), and the two findings that WOULD be gated were logged as candidates instead of shipped: the shared 30px language switcher (one rule in `js/i18n.js` moves 13 pages, and the correct height differs per host) and the drawer's `100vh`→`dvh`. **No micro-feature** — two passes consume 2 of the 5 iterations, leaving 3 fixes; no P1 existed. **SW v554→v555** for the two precached files that changed (`trope_tutor.html`, `torah_trainer.html`); the ledger and `sitemap.xml` are not precached. **FONT_MAKER_VERSION stays 5.28 — correct by the rule: the Font Maker was not touched this session.** **Scripts run:** `check-inline-js` (62 inline + 36 JSON-LD blocks parse — run after each edit, though all three fixes were CSS-only), `check-i18n` (clean, no NEW violations; **0 CSV keys added, so `build-locales` was correctly not run** — no user-facing string changed anywhere this session), `update-llms-txt --check` (both files current — no `<head>`, title, description or JSON-LD change), `update-sitemap.mjs` as the final step, on the now-unshallowed clone. **Deploy verification happens AFTER the maintainer merges** — Pages deploys on merge to `main`, not from this branch. | verified: every fix carries its own multi-cell receipt above; drift confirmed by fetch + diff, not from the pointer's note

- [x] 2026-08-23 | (S245 Pass M — aesthetics & visual design, **3rd-ever run**, `trope_tutor.html`; target re-derived per the M row's own instruction ("re-derive by tool staleness") — trope-tutor was the stalest tool at S237, ~8 sessions, and had never been M-audited) | trope_tutor.html | **Full matrix light/dark × 1280/800 × EN/HE (8 cells) + the Learn and Drill views, 292 visible elements, 0 pageerrors, `scrollWidth == innerWidth` in every cell, `dir` correct 8/8. VERDICT: a well-kept page with ONE real defect — and the defect turned out to be about the loop's own bookkeeping rather than about this page.** **(1) THE HIT — `row-siblings-with-mismatched-heights`, on a carrier the class was CLOSED without.** The `.hright` header row ran five heights across six controls (Home 34.38, switcher 30, Tour 31, fullscreen 30, dark 35, gear 35 — the SVG-bearing `#fsBtn` being the short one), identical in EN and HE and at both widths. torah_trainer and the dashboard took `min-height:35px` in `db37a54` (S226) and the class was declared closed on that census; **trope_tutor was IN that census and was recorded "Clean" — a false negative**, mechanism reproduced and written up as its own candidate. Fixed `57d5e68` by copying torah's exact declaration and its reasoning; the row now measures 35/35/35/35 byte-identically to torah's, the detector returns 0 groups (was 1 in all 8 cells), and at 390px it also lifts Tour off **29px**, under the suite's 30px floor — which is the other half of why the reference pages set a min-height rather than a height. Comment corrected in `2a2306c` after the sweep-status line disproved my first account of the miss. **(2) Dark-mode parity CLEAN** — 7 apparent "unflipped" elements, all 7 correct by design (translucent white overlays and `.skip-link` on the navy header, plus the drawer scrim). **(3) The contrast arm was INVALID as run** — no alpha compositing, 24 false hits including `.mark-hit` at a fabricated 1.99:1; killed by CSS + screenshot. **(4) One logical-property finding** (`.tu-chip`'s `border-left` colour bar does not mirror in Hebrew) and **standing measurements** (a 1.28px five-size type band — the third page with this shape; radius strays 5px×2) logged as candidates. **No gate 3: nothing dramatic to propose.** Screenshot rule honoured — 12 BEFORE/12 AFTER header crops at 1280/800/390 × light/dark × EN/HE plus 16 full-page BEFORE frames of both views, in the session scratchpad, delivered at close-out, never committed. | verified: 8-cell matrix re-run after the fix, 0 rowMismatch groups, 0 pageerrors, element count unchanged at 292

- [x] 2026-08-23 | (S245 Pass N — mobile & touch-device, **3rd-ever run**, `torah_trainer.html`; the most phone-plausible never-N-audited surface — a b'nei-mitzvah student practising with audio in hand — picked over the generator the S244 pointer had suggested, and the pick paid for itself) | torah_trainer.html | **7 arms on REAL device descriptors (`iPhone SE` 320 / `iPhone 13` 390 / `Pixel 7` 412 / 13-landscape 750; flags verified `coarse:true, hoverNone:true, dpr:3, maxTouchPoints:1`). VERDICT: 5 of 7 arms clean, 2 real findings, both fixed in-pass — plus a method finding that explains why one of them survived 20 sessions of sweeps.** **⚑ THE METHOD FINDING FIRST, because it conditions everything else: this page cannot be measured with its Sefaria fetch aborted.** Verse text comes from an external API the CLAUDE.md recipe route-aborts, and every per-verse control is rendered from that response — **28 visible controls blocked vs 58 stubbed**, same page, same load. Every prior census (incl. the S238 all-button sweep over ~835 controls) has been reading torah_trainer with half its controls absent. Stubbing `**sefaria.org/api/v3/texts**` with 5 real verses — validated against the repo's OWN `data/trope/trope_index.json` — is what surfaced both findings. **Arm 1 (reflow, 16 cells): HIT.** `.tt-verse-buttons` is `inline-flex` with the `nowrap` default, so at 320px the four per-verse actions laid out wider than the row and `.tt-verse` CLIPS rather than scrolls — Copy was cut mid-label on **every verse**, reading as a bare "C" (18px off in EN, 30px in HE where labels are longer), while `scrollWidth` stayed 320, which is why no overflow sweep ever caught it. Fixed `0da7574`; arm 1 now 16/16, was 12/16. **Arm 2 CLEAN:** three viewport-meta spellings across 14 files (`initial-scale=1` ×8, `1.0` ×5, +`viewport-fit=cover` ×1) — functionally identical, **unchanged from S239's reading, deliberately NOT re-filed**; no `user-scalable=no`, no `maximum-scale`; `viewport-fit`/`env()` correctly paired on the one page that has either. **Arm 3:** two `100vh` sites — `body{min-height:100vh}` is the benign form S239 settled, and `.settings-modal{height:100vh}` strands the drawer's last panel (Reset) under an expanded iOS URL bar → candidate with the one-site `100dvh` fix named. **Arm 4 CLEAN:** 0 draggable elements, 0 hover-only reveals (the single candidate, `#fsExitBtn:hover{opacity:1}`, REFUTED — base opacity is 0.45, not 0, the same shape as S239's dictionary refutation), and tap-to-chant fires under real touch with a graceful failure message when the audio CDN is blocked. **Arm 5:** `#ttCopyRange` is the one field with no typing hints (`mobile-input-hints`, 4th carrier) → candidate, WITH the trap that `inputmode="numeric"` would be the wrong fix; the three `type=number` fields held as the control. **Arm 6 CLEAN, after a PROBE FAULT was caught:** `.settings-close` matches **2** elements — `#drawerDarkBtn` (`toggleDark()`) sorts first — so "tap the ✕" tapped the dark toggle and the drawer appeared unescapable; tapping the real ✕ closes it, and so does the 31px backdrop strip. **Arm 7 CLEAN** at 4× CPU: FCP 132 / DCL 161 / load 192 ms, 0 longtasks. **Contact sheet: 20 frames** (reading × 320/390/412 and drawer × 320/390, each light+dark × EN+HE) plus 4 BEFORE frames, delivered at close-out, never committed. | verified: per-arm receipts above; every finding grounded against a control or a screenshot before filing

- [x] 2026-08-23 | b86c788 | torah_trainer.html | **The per-verse Read / Chant / Loop / Copy chips reach the 24px touch floor** (`sub-floor touch target`). All four paint at **19px** and repeat 4× per verse down a whole parsha — on a phone they are the tool's primary interaction, and every one was under WCAG 2.5.8. Invisible to 20 sessions of sweeps because they only exist once a reading has loaded (see the Pass-N method finding). **Vehicle: the transparent `::after` sanctioned at S240**, since repainting deliberate chrome down an entire reading would be a gate-3 change for zero accessibility gain — paint stays 19px, target reaches 25px, block axis only (siblings sit 4px apart on the inline axis). **Two details the S240 verification recipe caught, both of which would have shipped a broken fix:** insets resolve against the **padding** box, so with a 1px border `-3px` measured a **23px** target — one short of the floor — and `-4px` is the correct value; and once the row wraps at 320px the two lines' targets overlapped by 2px, so `row-gap` goes to 6px while `column-gap` stays 4px. | verified: the S240 recipe WITH its mandatory failing control — centre/−3/+3 past the paint all hit the button, +12 does not; 0 target overlaps at 320 and 390 in both languages; all 8 visible Hebrew words still hit-test to themselves so tap-a-word-to-chant is intact; desktop byte-identical (rows=1, group 21×264 EN / 21×282 HE at 1280 and 800); arm 1 stays 16/16

- [x] 2026-08-23 | 0da7574 | torah_trainer.html | **The per-verse actions fit a 320px phone** (S245 Pass N arm 1 — the run's one reflow hit). `.tt-verse-buttons` is `inline-flex` and `flex-wrap` defaults to `nowrap`, so Read / Chant / Loop / Copy laid out wider than the row on an iPhone SE; `.tt-verse` clips rather than scrolls, so **Copy was cut mid-label on every verse** — 18px off in EN, 30px in HE — while `scrollWidth` stayed at 320, which is exactly why the registered `horizontal-overflow-at-narrow-widths` sweeps never saw it. `flex-wrap:wrap`, which engages only where there is no room; `margin-left` → `margin-inline-start` in the same declaration so the gap sits after the verse number in Hebrew too. | verified: arm 1 16/16 cells (320/390/412/landscape × light/dark × EN/HE), was 12/16 — all four failures were 320px; desktop unchanged in 8 cells (one line, 21px, same 264/282px group widths), so no gate-4 question arose; 0 pageerrors; confirmed in the RTL screenshot, where `העתקה` now drops to a second line intact

- [x] 2026-08-23 | 57d5e68 + 2a2306c | trope_tutor.html | **One shared control height in the header row** (`row-siblings-with-mismatched-heights` — the S245 Pass-M hit, on a carrier the class was closed without). Six controls, five heights: Home 34.38, switcher 30, Tour 31, fullscreen 30, dark 35, gear 35 — the odd one being `#fsBtn`, whose 14×14 SVG makes a shorter line box than its neighbours' 1rem emoji. `min-height:35px` on `.hbtn`, the same declaration torah_trainer and the dashboard took in `db37a54`, with torah's reasoning restated for this file. At 390px it also lifts Tour off **29px**, under the ratified 30px floor, which is why the reference pages set a min-height rather than a height. **`2a2306c` corrects my own account of the miss:** 57d5e68's message said this page was outside that sweep's census; the S225 sweep-status line lists it under "Clean", so it was a **false negative** — the `round(top/6)` grouping split one row into two internally-consistent buckets. | verified: all five `.hbtn` controls read 35 at 1280/800/390 × light/dark × EN/HE, byte-identical to torah's row; the detector that flagged it returns 0 groups (was 1 in all 8 cells); 0 pageerrors; element count unchanged at 292; the remaining 30px switcher is the shared component, logged as its own candidate

- [x] 2026-08-22 | **⚑ Out-of-band feature request (4th in the thread): one-click test-phrase chips on the two font pages** | branch `claude/hebrew-keyboard-nikkud-trop-n166be`, `72277fe` | Maintainer supplied the Open Siddur catalogue's "Test your text in every font" popup as a reference and asked for **the samples row from it** — "buttons for test phrases… Kind of like in this code snippet" — on **ONLY the Font Maker and the Font download page**, plus a CLAUDE.md entry "(only if asked for)". **Read as a scope instruction, not a port:** the reference is a 250-line modal that harvests a `.cfd-font-row` catalogue and previews text in every font at once; the piece asked for is its `data-osft-sample` chip row, and building the whole popup would have been a large uninvited feature on top of a page that already previews one font at a time. **"Font download page" resolved by evidence, not by guess** — `resources.html` carries `#fontGrid`, a preview modal and `downloadFont(i)` shipping a .zip with a generated LICENSE.txt, so it is the download page; the Font Maker is the other target, and the dictionary — which had just gained the keyboard on its search box — was deliberately left out because a search box has no specimen to show. **Six phrases, each isolating ONE thing** so a builder can localize a fault (letters / vowels / cantillation / mixed script) instead of reading one sentence that mixes them: Shalom (the suite's own `FONT_SAMPLE`), the classic pangram `דג סקרן שט בים…`, all 22 letters + the 5 finals, every vowel on an alef, Esther 1:1 cantillated, and Hebrew + digits + Latin. Two of the reference's five chips were **not** carried over: its "Pangram" chip only re-seeded from the host page's own textarea (no pangram of its own), and its Shema sample was dropped — the trop chip already covers pointed liturgical text, so nothing is lost in coverage, and it keeps the Tetragrammaton out of a field that exists to be overwritten. **Flagged to the maintainer as their call, not settled silently.** Shipped as a `═══`-marked shared block pair (`TP_PHRASES` + `mountTestPhrases`, `.tp-*` CSS) beside the keyboard's, byte-identical across both carriers (CSS `af63ba62f1de`, JS `f1b5d2d53acb`); where a page has both components they pass the **same `getInput`/`onChange`**, so the chips inherit the keyboard's contracts for free — the required `onChange` (a scripted `input.value =` fires no `input` event) and, on the Font Maker, the burst-closing callback that makes one chip one undoable step. 13 `shared.phrases.*` rows (label + 6 names + 6 title tips) in **both** languages → no new debt; **the phrases themselves are Hebrew content and are never translated**. CLAUDE.md gains a `##` section + Contents entry stating the requested rule explicitly: **add only when asked** — a field must feed a type specimen to earn them, unlike the `.ivrit` engine or panel-collapse memory, which every new tool adopts. **Folded into the still-unshipped v5.28 changelog rather than opening v5.29** (both halves land in one deploy, per the "one combined bump per release" rule) — entry extended in BOTH places the two-place system needs. | **verified** — 33-assertion headless run across both pages. **The phrase strings are checked by codepoint class, not by eyeball**, because a tester whose "trop" sample carries no trop is worse than useless: all 22 bases + 5 finals present in the alef-bet chip, the pangram covering the alef-bet (missing only the נ that appears as ן — the standard reading of a Hebrew pangram, asserted rather than hand-waved), the nikkud chip vowel-only with **no** trop mixed in, the trop chip carrying both classes, the mixed chip carrying all three scripts, and **zero FB-block precomposed codepoints** anywhere. Behavior: chips fill the field and re-render the host preview; a second chip replaces rather than appends; caret parks at the end; the keyboard's backspace then edits the chip-filled text (the two components cooperating on one field); Hebrew UI relabels the chips while the phrase bytes stay identical; on the Font Maker the model receives it, the burst closes, one undo entry per chip, undo restores the previous sample, and the chips re-mount through `renderGrids()` writing to the NEW input; 24px touch floor held; 0 pageerrors. Gates: `check-inline-js` clean, `check-i18n` clean at the unchanged 14 baselined findings, `build-locales` 4922 keys. **The two pre-existing suites were re-run, not assumed** — the resources keyboard suite and the Font Maker keyboard suite both still pass with the chips inserted above them.
- [x] 2026-08-22 | **⚑ Out-of-band feature request (3rd in the thread): the on-screen Hebrew keyboard adopted into the Font Maker's Spacing sample-text field** | branch `claude/hebrew-keyboard-nikkud-trop-n166be`, `1bccc04` (+313/−3 in `Hebrew_Font_Maker.html`, +28/−5 CLAUDE.md, 1 CSV row, sw.js) | Maintainer screenshot circling the empty space on the **Tricky combos** row under `#spacingSample` ("Can we add it here in the fontmaker too?"). **This is the first carrier whose host markup is NOT static, and that was the whole engineering problem:** `renderSpacingPanel()` replaces `#spacingBody.innerHTML` wholesale and `renderGrids()` calls it on nearly every state change (select a glyph, undo, import, language switch), so the toggle, the panel AND the target input are all discarded — a single `DOMContentLoaded` mount, the pattern both existing carriers use, would have been stranded on the first re-render with a controller pointing at dead nodes. Solved **without forking the shared block**: the page reads `{open, tab}` before the rebuild (`hkSampleState()`) and re-mounts at the tail of the render (`mountSampleKeyboard(kbd)`), re-asserting state via the controller's own `setOpen`. Re-mounting is cheap because `renderPanel()` is lazy — the ~80 key buttons only build while the panel is open. **A consequence worth recording: this page needs NO `_hk.applyI18n()` hook** (the one step of the CLAUDE.md recipe it skips) — `setOpen()` re-runs the toggle label through I18n, and `applyI18n()` → `renderGrids()` → `renderSpacingPanel()` already reaches the re-mount. **Undo/dirty contract, decided from the code rather than by analogy:** `onChange` calls `setSpacingSample(v)` then `udBurstCommit()` — one undo entry per key, matching the `insertSampleCombo` chips in the same row. Leaving the burst open (the coalescing behavior physical typing gets) was **rejected on evidence**: the undo handler bails on `_burst` ("don't undo mid-interaction"), and a programmatic `setRangeText` never fires the input's `change`, so the burst would never close on its own — Ctrl+Z would have been dead for the rest of the session and `markDirty()`/autosave would never fire for the typed sample. `getFontFlags` reads the **project** (`font.nikkud.enabled`/`font.trop.enabled`, named by `familyName`), so a font with marks switched off says so on the matching tab instead of silently previewing glyphs it will never ship. Zero new i18n rows (the ~99 `shared.kbd.*` were built to be shared); zero CSP change; **both shared blocks still byte-identical across all three carriers** (CSS `55752f78ee2c`, JS `c0ba70fd6036` — carrier list re-trued in every copy, and the JS header gained the re-mount note). **FONT_MAKER_VERSION 5.27→5.28** with a changelog entry in BOTH places the two-place system needs (`HELP_CONTENT.about` English HTML + the `fontmaker.changelog.v5_28` CSV row, `v5_27` demoted into `CHANGELOG_ORDER`, `CUR_KEY` advanced) — en **and** he, so Check B stays clean. **sw.js v553→v554.** | **verified** — 30-assertion headless Playwright run (localhost HTTP, `serviceWorkers:'block'`, external origins route-aborted, real project created via `newProject()` after clearing the onboarding gate): typing reaches `fontSpacing().sample` AND re-renders `#spacingPreviewSvg` (proving `onChange` substitutes for the `oninput` that `setRangeText` never triggers); **the panel survives an explicit `renderGrids()` — still open, still on the Trop tab, and the re-mounted keyboard types into the NEW input**; one undo entry per key with `_burst === null` after each; vav-holam inserts decomposed (U+05D5 U+05B9, not the FB codepoint); one-mark backspace; the capability hint appears only with nikkud switched off and names the font; layout persists and QWERTY rows stay LTR; Hebrew UI relabels the toggle through the re-mount path with the panel still open; dark keys take `rgb(30,37,53)`; 24px touch floor held at 800px (28.9px in a 343px column) with no sideways page scroll; **0 pageerrors** in both viewports. Gates: `check-inline-js` clean (62 JS + 36 JSON-LD), `check-i18n` clean at the unchanged 14 baselined findings, `build-locales` 4909 keys with the pre-existing single debt row unchanged, `update-llms-txt --check` current, `update-sitemap` last (no-op — both pages already carried today's date). **One assertion failed first and was chased before being believed:** "Ctrl+Z undoes the last typed key" — not a product bug but a test bug, since `udShortcutBlocked()` deliberately yields Ctrl+Z to the browser's own field undo whenever focus is in an INPUT, exactly as physical typing in that field behaves; blurring first proves the committed entry is real and reachable.
- [x] 2026-08-22 | **⚑ Out-of-band feature request: an on-screen Hebrew keyboard for the resources font-preview modal** | branch `claude/hebrew-keyboard-nikkud-trop-n166be`, `9f74ba9..7167622` (6 commits, +669/−3 across 8 files) | Maintainer-supplied prompt with a screenshot circling the spot under the preview input ("a button that says Show Hebrew Keyboard… full Hebrew keyboard with nikkud and trop support, so people can type in the hebrew even if they don't have a hebrew keyboard installed"), plus "write a guide to making an onscreen hebrew keyboard down in claude.md" — **not an `/improveloop` session, so no `S<N>` per-session-log line.** The prompt said "Ask followup questions", so **three design decisions were asked BEFORE any code moved and all three came back non-default**: letter order → **BOTH** alef-bet and Israeli SI-1452, with a toggle (not the recommended single order); physical-key capture → **no**, on-screen clicks only; trop coverage → the **full Unicode set**, not the 26-mark Torah taxonomy. **The component is a genuinely new primitive for this repo, not an assembly of existing parts** — `setRangeText`/`selectionStart`/`selectionEnd` had **0 matches repo-wide** and no clickable character-input UI existed anywhere (the four Hebrew glyph grids in the suite — generator letter/vowel tiles, dictionary `.lpt`, trope-tutor `.tu-choice`, Font Maker tiles — are all *selection/filter* grids, so none could be reused); the nearest precedent was the Font Maker's append-to-end `insertSampleCombo`. **Shipped as a shared component** (`═══ shared: hebrew-keyboard ═══` JS + a matching CSS block, per-page wiring below the end marker) so other tools can adopt it, with the recipe and behavior contract written into CLAUDE.md as a new Component-reference section + Contents entry. **Data provenance — every table sourced, none invented:** trop from trope_tutor's `TROPES` (family order + keys), vowels from the generator's `VOWEL_GROUPS` with the dictionary's raw-insert strings, the extras (dagesh/shin dot/sin dot/kamatz katan/holam haser/rafe) from the Font Maker's `MARK_NAMES` — the only place in the suite that had them. **Three domain decisions worth not re-litigating:** (1) **no sof-pasuk trop key** — Unicode unifies siluk with METEG U+05BD, so there is no distinct mark to type; the `׃` terminator lives in the punctuation row instead. (2) **Vav-holam/shuruk insert DECOMPOSED** two-char sequences, never the FB-block precomposed codepoints (the Font Maker's "stored decomposed" rule). (3) **The SI-1452 rows are pinned `direction:ltr` even under `dir=rtl`** — a physical layout is a coordinate system and must not mirror; the same documented exception as the Font-Maker stage, commented in place so an RTL consistency sweep does not "fix" it. **Interaction contract:** delegated `pointerdown` → `preventDefault()` so keys never steal the caret; one `setRangeText` edit path that ends with refocus **plus an explicit `onChange()`**, because `setRangeText` fires **no `input` event** and the host's `oninput` would otherwise never run; backspace deletes one UTF-16 unit, which for Hebrew is exactly one mark (surrogate-pair guard for emoji). i18n: **99 new `shared.kbd.*` rows** with Hebrew mirrored from the already-translated flashcards/dictionary/fontmaker/trope glyph-name sets → **zero new translation debt** (Check B clean); adopting pages add no rows. `hebrewBlender_kbdLayout` registered at **all five** AllTools sites. **sw.js v551→v552.** | **verified** — data tables machine-checked codepoint-by-codepoint (83 keys; U+0591–U+05AF each present exactly once across the two trop tables; QWERTY rows ≡ the 27-letter set; every slug resolves to a CSV row), then a headless Playwright matrix (localhost HTTP, `serviceWorkers:'block'`, external origins route-aborted): expand/collapse + `aria-expanded` + label swap, caret-position insert updating input **and** preview, kamatz/etnachta/vav-holam inserts, one-mark backspace, space/clear, layout toggle persisting across reload, the per-font capability hint on/off, Escape + reopen state, the Hebrew UI switch (chrome translates, rows stay LTR, typing still works), light+dark, 1280 + 800px, **0 pageerrors**. Gates: `check-inline-js` clean (62 JS + 36 JSON-LD blocks), `check-i18n` clean at exactly **14** baselined findings (unchanged), `build-locales` 4908 keys, `update-llms-txt --check` current (no title/description/JSON-LD moved), `update-sitemap` last. **A dark-mode screenshot initially looked broken (white keys, invisible glyphs) and was chased before being believed** — it was the screenshot racing the 0.15s token transition, not a bug: the key background measured `rgb(255,255,255)` at t=0 and the correct `rgb(30,37,53)` by t=400ms, confirmed by re-shooting after settle.

## Archived at the S252 close-out (2026-08-25)

- [x] 2026-08-23 | (S247 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-m-n-passes-uy07ou` → draft PR #196**, verified OPEN/draft/unmerged via the API at session start rather than assumed (`mergeable_state: clean`, 13 commits, 0 reviews, 0 check runs — the repo has no CI). The PR now carries **three** sessions (S245+S246+S247). **Drift check: NOTHING landed outside the loop** — `origin/main` still at `b745745`, now unmoved through THREE consecutive sessions; `sw.js` was v556 and `FONT_MAKER_VERSION` 5.28 exactly as the S247 pointer recorded, both re-measured rather than trusted (the standing S225 lesson). Clone arrived FULL (2,324 commits), so no unshallow was needed. **SW v556→v557** (precached files changed: `hebrew_blend_generator.html`, `classroom_dashboard.html`). **FONT_MAKER_VERSION stays 5.28** — the Font Maker was not touched at all this session, ending its involvement entirely. Scripts run: `check-inline-js.mjs` (58 JS + 36 JSON-LD blocks parse), `check-i18n.js` (Check A clean, 14 baselined, Check B clean), `update-sitemap.mjs` last. `build-locales.js` correctly NOT run — zero CSV keys added (all three fixes are CSS/JS behaviour, no new user-facing strings). **Gates: 0 asked, 0 deferred** — one gate-4 situation AROSE and was engineered away rather than asked: the `.radio-group` wrap was desktop-affecting when set outright, so it was scoped to the page's existing 430px breakpoint, which returns it to the pass's explicitly-autonomous category. **Decisions deferred to maintainer: NONE.** Deploy `success` is verified AFTER the maintainer merges; `origin/main` has not moved in three sessions, so nothing from S245–S247 is live.

- [x] 2026-08-23 | (S247 Pass M — aesthetics & visual design, **5th-ever run**, `hebrew_blend_generator.html`; the S247 pointer's named target — never M-audited, and the tool with the most controls in the suite) | **Full matrix light/dark × 1280/800 × EN/HE, run POST-Generate** (the worksheet surface only exists after the click, so an M that never generates audits half the tool — 2,245 elements vs 2,094). **0 pageerrors, 0 overflow, `dir` 8/8, 1 real hit.** **The hit: `.btn-primary:hover` inverted the contrast the button has at rest** — white on #b8821e measured **3.36:1** at the pixel level against the 4.5:1 its 15.2px bold label needs, while the same button at rest is 5.43:1. Light mode only, because `var(--white)` is #1e2535 in dark (CLAUDE.md's documented pitfall), so dark hover was accidentally near-black on gold at 4.59:1 — which is exactly why it survived. Unique to this file (`#b8821e` appears once suite-wide; one `:hover` flips to `var(--white)` anywhere). Fixed at 3421074. **TWO probe defects found and corrected in-pass, both of which had produced false findings:** (1) the contrast probe **did not exclude disabled controls**, so it fired on `#regenBtn`/`#printBtn`/`#pdfBtn` at 1.84:1 — all three carry `disabled` + `aria-disabled=true`, and **WCAG 1.4.3 explicitly exempts inactive components**; the dark disabled treatment (explicit `#3d4f6a` at opacity 1) is deliberate and MORE legible than light's opacity-0.35 dimming. (2) the probe did not composite cumulative element **opacity** into the foreground. **And a third, method-level: `getComputedStyle` cannot see a `filter`, and `brightness()` brightens the TEXT as well as the background** — the arithmetic prediction (6.04) was wrong because it brightened only the background; the fix had to be measured from **rendered pixels** (5.86 actual). **Standing measurements** (recorded so no future M re-derives them): **23 distinct font sizes** on one page, with 12.8/12.48/12 and 11.84/11.52/11.2/10.88 crowding two tight bands — the FIFTH page with the type-scale micro-fragmentation shape; **seven distinct non-pill radii** (3/4/5/6/8/10/22px) — also the fifth page; **4 box-shadows, all purposeful — clean.** Contrast otherwise 0 misses in all 8 cells with alpha compositing on. | verified: 8/8 matrix cells post-fix, pixel-sampled contrast table

- [x] 2026-08-23 | (S247 Pass N — mobile & touch-device, **5th-ever run**, `classroom_dashboard.html`, its FIRST-EVER mobile audit) | **Target RE-DERIVED per the S196 rule and deliberately AGAINST the S247 pointer's suggestion** (which named the generator or the Font Maker): re-derived on phone-plausibility, the S239 precedent — the dashboard is the one tool a teacher operates **standing up in a classroom**, and its 380px drawer against a 390px phone was the likeliest arm-1 shape in the suite. Real device descriptors throughout (`coarse:true, hoverNone:true, dpr:3, maxTouchPoints:1`; a plain resized context reports `false,false,1,0`). **7 arms run, 2 REAL HITS — both of them controls a teacher simply could not operate on a phone.** **Hit 1 (arm 1): the "Long" date format was unreachable at 320px** — 0% visible, `elementFromPoint` returned null, identical in Hebrew, and "Both" on the Hebrew-date row was cut too; `.radio-group` is inline-flex with no wrap, `.form-row` does not wrap, and `.panel` is `overflow:hidden`, so the excess is clipped with no scrollbar. Fixed at 1bdc5f5. **Hit 2 (arm 4): none of the drawer's three tooltips could be opened by tapping** — the arm's own named check, failing on a device where `(hover:none)` is true and there is no other way in. Fixed at 3c842ae. **Arms clean:** 2 (viewport contract `width=device-width, initial-scale=1.0`, no `user-scalable=no`, no `maximum-scale`; must-fail control fires), 6 (every overlay carries ≥1 dismiss control; drawer ✕ on-screen and 32×32 in all 12 cells), plus the **first-run setup card measured on phones before dismissal** — fits at 320/390/412 in both themes and languages, scrolls internally, ✕ is 30×30, 0 overflow. **Arm 3 (static, as the pass prescribes): `.settings-modal` is `height:100vh`** — the THIRD carrier of the shape S245 logged for torah_trainer and trope_tutor; the ✕ is at the TOP so nothing is stranded, but the scroll container's tail sits under expanded iOS chrome. Logged, not fixed — a page-wide unit migration is gate 4. **Arm 4 also confirms the sanctioned touch paths are present** (1 `draggable` element alongside 2 Move-menu affordances). **Method note — THREE of this pass's own probe readings were wrong before they were right,** each caught by re-running the census the other way (the S246 standing lesson, earned again): a pre-scroll rect reported a phantom 67.3px overflow that vanished on hit-test; a zero-width collapsed group reported a phantom overflow (zero-size rects sit at 0,0); and the tooltip ARIA inventory queried `.tip-wrap` when the attributes live on `.tip-icon`, briefly contradicting CLAUDE.md — **CLAUDE.md was right, the probe was wrong (3/3 icons carry all four attributes).** | verified: 20/20 phone cells + 8/8 desktop control cells, contact sheet of 34 frames

- [x] 2026-08-23 | 3c842ae | classroom_dashboard.html | **The settings tooltips can be opened by tapping them** (S247 Pass N arm 4 — the arm's named check). On a phone none of the drawer's three tooltips could be opened AT ALL, and `(hover:none)` is true there, so there was no other way in. CLAUDE.md's shared-UX contract §2 promises tap/click toggles the bubble; the **ARIA half is honoured exactly** (3/3 icons carry `tabindex`, `role=button`, `aria-expanded`, `aria-describedby`) — only the pointer half was wrong, **in two stacked layers, both traced with an event log rather than inferred**: (1) the synthetic `mouseenter` a tap fires ran `show()`, then `click` saw `openWrap === wrap` and ran `hide()`; (2) guarding hover on `pointerType === 'mouse'` — the form `hebrew_dictionary.html`'s `bindTip` **already uses**, so this is convergence on an existing in-suite implementation, not invention — exposed the SAME race one layer down, because a press focuses the icon before its own click lands (`focus` → show, `click` → hide). Both layers gated; keyboard Tab-to-icon deliberately preserved. | verified: all three input paths — touch (tap opens, `aria-expanded=true`, second tap closes), mouse (hover opens, unhover hides, desktop unchanged), keyboard (focus opens); 8/8 standard matrix cells, 0 pageerrors

- [x] 2026-08-23 | 1bdc5f5 | classroom_dashboard.html | **A phone teacher can finally pick the "Long" date format** (S247 Pass N arm 1 — the run's reflow hit). At 320px the option measured **0% visible and NOT tappable** (`elementFromPoint` → null); "DD/MM/YYYY" was clipped to 77%, and in Hebrew "ארוך" was likewise 0% and "Both" on the Hebrew-date row was cut. `.radio-group` is `inline-flex` with no wrap, `.form-row` does not wrap, and `.panel` is `overflow:hidden` (needed to clip the title to the rounded corners) — so anything past the drawer edge is **clipped with no scrollbar and no way to reach it**, an overflow shape the registered `docW <= innW` detector is blind to by construction. **The fix is one declaration, but WHERE it lives was the whole decision:** set on `.radio-group` outright it ALSO wraps the date-format group on **desktop** (277×29 → 265×55), because allowing wrap lets flex-shrink take the group below its max-content width — a desktop change in service of phones, i.e. **gate 4(a)**. Caught by a desktop no-op control that FAILED, then engineered away by scoping to the page's existing 430px phone breakpoint (which covers 320/390/412 and no desktop width), returning the change to the pass's explicitly-autonomous category. **A gate avoided by measurement rather than by asking.** | verified: 20/20 phone cells (320/390/412 × EN/HE × light/dark — all 8 groups, all 20 options 100% visible and tappable) + 8/8 desktop control cells reporting 0 groups wrapping and 0 clipped; before/after frames both themes

- [x] 2026-08-23 | 3421074 | hebrew_blend_generator.html | **The Generate button stops getting harder to read on hover** (S247 Pass M — the run's one hit, on the tool's primary action). `.btn-primary:hover` darkened the gold to `#b8821e` AND flipped the label to `var(--white)`: in light mode that is literal white on mid-gold, **3.36:1 measured from rendered pixels** against the 4.5:1 its 15.2px bold text needs, while the SAME button at rest is 5.43:1 — hovering the suite's most-used button made it **less** legible. Light-only, because `var(--white)` is `#1e2535` in dark (CLAUDE.md's documented `--white` pitfall), so dark hover was accidentally near-black on gold at 4.59:1 and passed — which is why this survived. **Converged on the idiom this same file already uses for its own tour buttons** (`.tbtn:hover { filter: brightness(1.06) }`) and six other gold-button carriers suite-wide, deleting a one-off magic hex AND a one-off colour flip; leaving the text colour alone means contrast can only improve. **Had to be measured from pixels, not computed style** — a `filter` is invisible to `getComputedStyle`, and the before-hover case has no dark ink to sample at all. light rest 5.43→5.43, light hover **3.36→5.86**, dark rest 6.96→6.96, dark hover 4.59→7.55; hover is now better than rest in both themes rather than worse. | verified: 8/8 matrix cells post-Generate, 0 pageerrors, pixel-sampled before/after table, before/after frames both themes

## Archived at the S253 close-out (2026-08-25)

- [x] 2026-08-23 | (S248 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-m-n-passes-uy07ou` → draft PR #196** — verified via the API before assuming (open / draft / unmerged / `mergeable_state: clean`, head `5fb788d`, 18 commits) exactly as the S248 pointer required. The PR now carries **FOUR sessions (S245 + S246 + S247 + S248)**. **Drift check: NOTHING landed outside the loop for a FOURTH consecutive session** — `origin/main` still at **`b745745`**, and the branch was 0 behind / 18 ahead at session start. Clone arrived **FULL (2,329 commits, no `.git/shallow`)**, so every date-based receipt in this entry is sound. **`sw.js` v557 → v558** in the final commit; precached files changed: `classroom_dashboard.html`, `trope_tutor.html`, `torah_trainer.html`. **`FONT_MAKER_VERSION` stays 5.28** — S248 did not touch the Font Maker at all, a THIRD consecutive session clear of it, so no bump and no changelog entry. Scripts run: `check-i18n.js` clean (Check A clean, 14 baselined; Check B clean) ×3, `check-inline-js.mjs` clean (62 inline blocks + 36 JSON-LD across 14 pages) ×2, `update-sitemap.mjs` LAST. `build-locales.js` correctly NOT run — **zero CSV keys added** (both fixes were CSS/JS-behaviour only, no new user-facing strings). `llms.txt`/`llms-full.txt` untouched — no `<title>`, description or JSON-LD changed. **Decisions deferred to maintainer: NONE. Decision gates asked: NONE** — a fourth consecutive zero-gate session; the one gate-3 candidate this run surfaced (the dark day-of-week palette) was deliberately logged rather than proposed, because the rendered frame did not support filing it. **Deploy: GitHub Pages runs on merge to `main`, so nothing here is live until the maintainer merges PR #196.**

- [x] 2026-08-23 | (S248 Pass M — aesthetics & visual design, **6th-ever run**, `classroom_dashboard.html`, its FIRST-EVER aesthetics audit) | **Target re-derived per the S196 rule and the re-derivation moved the surface, not the page.** The S248 pointer named the dashboard, and that stood — but the pointer's reason was "S247's N pass already mapped its DRAWER," which is the argument for auditing the *other half*. So the run went at the **student-facing projected board**, the one surface in the suite read by 25 people from across a room, and the drawer was left to its S247 map. **Method point worth keeping, and it paid immediately: the run STUBBED the Open-Meteo / Hebcal endpoints instead of aborting them** (the S245 torah_trainer lesson). With them aborted the weather card reads "Weather unavailable" and one of the run's two real hits — `.weather-attr` — does not exist in the DOM at all. **Second method point: the pass ran `body.fullscreen` as well as windowed** — fullscreen is how a SmartBoard actually shows this tool, and it is where the headline hit lives. Matrix: 8 windowed cells + 8 fullscreen cells (light/dark × EN/HE × 1280/800, plus 1920×1080 and 1280×720 for fullscreen). **0 pageerrors, 0 horizontal overflow in all 16, `dir` correct 8/8, card shell perfectly consistent across all five columns (12px radius, one shadow, one border, `.col-title` identical at 9px 14px / Libre Baskerville 12.48px / 1.248px tracking), 0 homogeneous row-height spreads under the S246-corrected vertical-overlap detector.** **2 hits, both fixed (`d31788c`, `57f379f`); 7 measurements logged; 2 readings REFUTED before they could be filed.**

- [x] 2026-08-23 | (S248 Pass N — mobile & touch-device, **6th-ever run**, `trope_tutor.html`, its FIRST-EVER mobile audit) | **Target re-derived per the S196 rule on phone-plausibility** (the S239/S247 precedent), from the pointer's three candidates. The Font Maker already *declares* itself phone-hostile (`hebrewFontMaker_mobileWarnDismissed`), so an N there mostly re-confirms documentation; nobody builds a worksheet on a phone, so the generator's phone case is weak. The trope tutor is the one tool in the suite with a **native** phone form factor: a b'nei-mitzvah student drilling one-handed with headphones. It also completes the sibling pair — S245 ran M here and N on `torah_trainer.html`. **Real device descriptors verified live: `coarse:true, hoverNone:true, maxTouchPoints:1`, dpr 2 / 3 / 2.625 (iPhone SE 320 / iPhone 13 390 / Pixel 7 412 / 13-landscape 750).** **6 of 7 arms CLEAN; 1 hit, fixed (`2168a8f`) — and it turned out to sit on TWO pages, not one.** **Arm 1** (16 cells: 4 devices × light/dark × EN/HE): 0 overflow, 0 pageerrors, `dir` correct, 5 cards + 6 family chips render everywhere. **Arm 2 CLEAN with its must-fail control fired** — `width=device-width, initial-scale=1.0`; no `user-scalable=no`, no `maximum-scale`; `viewport-fit` absent AND 0 `env(safe-area-inset-*)` rules, i.e. consistent by the "neither" branch; the control injected `user-scalable=no` into the live meta and the detector caught it, then restored it. **Arm 3** re-measured the `100vh` drawer live (see the candidate). **Arm 4** is the hit — see below; the rest of the walk was clean **taps only**: a family chip switches family (Mercha → Kadma, 5 → 7 cards), a `▶` example plays real audio on one tap (`loadedmetadata → seeked → play → playing`, `currentTime 2.12`, `paused:false`, against a stubbed 30 s WAV so the seek/rAF path actually ran), the Drill tab + Start button start a live 4-choice question, an answer tap grades it (2 graded, all 4 disabled), and the drawer opens and closes by tap in **all 8 device × language cells** (✕ 40×40, on-screen, hit-tests to itself). **0 hover-only reveal rules on the whole page** — enumerated from the live CSSOM, not grepped. **Arm 5 CLEAN and notably so: the first N-audited surface with ZERO `mobile-input-hints` debt** — the page has no text or number input at all (one `<select>`, two radios, one `range`), and zero `prompt()` calls, so there is nothing to give `inputmode`/`autocapitalize` to and no prompt flow to tap through. **Arm 6:** both overlays carry a dismiss (`#tourCard` → tourClose/tourBack; `#settingsModal` → ✕). **Arm 7** (4× CPU + 400 kbps vs unthrottled): interaction stays snappy under throttling — `printChart` build of all 26 marks **251 → 335 ms**, drill start **198 → 280 ms**, i.e. no main-thread block over 200 ms in anything the page controls; cold load is transfer-bound (**DCL 101 → 4,554 ms, first cards 172 → 8,425 ms** on 183 KB over 400 kbps). Handed to **D** per the documented boundary. **Contact sheet: 48 frames** (3 phones × light/dark × EN/HE × Learn / Drill-start / live Drill question / settings drawer) — shipped per the arm's binding rule even though 6 arms came back clean.

- [x] 2026-08-23 | 57f379f | classroom_dashboard.html | **The weather attribution line is readable** (S248 Pass M, 2nd hit — the one that only exists when the weather API is stubbed rather than aborted). `.weather-attr` was dimmed TWICE: `color: var(--muted)` — already the suite's quiet-text token — plus `opacity: 0.85` on top, at `0.6rem`. Measured **4.35:1 light / 4.04:1 dark**, both under the 4.5 floor, on the legally-required Open-Meteo + OpenStreetMap credit: "quiet" is right, "unreadable" is not. Dropped the redundant second dimming and kept the intended one — same shape as the Exit-Full-Screen fix in the same run. **6.15:1 light, 5.02:1 dark**, still `--muted`, still `0.6rem`. Verified across 16 cells with the APIs stubbed: 0 pageerrors, 0 overflow, `dir` correct, `.weather-attr` gone from every contrast-fail list. (Here the arithmetic and the probe agreed to two decimals — no `filter` in the path, unlike S247.)

- [x] 2026-08-23 | 2168a8f | trope_tutor.html + torah_trainer.html | **Settings tooltips open when you tap them** (S248 Pass N arm 4 — the arm's named target, and the pass's only hit). Both pages carry a **byte-identical** copy of the `bindTip` IIFE (`diff` of the two blocks: IDENTICAL), and both had the exact layer-2 race S247 found on the dashboard: `bindTip` sets `tabindex="0"` on the `.tip-wrap` itself, so a pointer press focuses it BEFORE its own click lands. **Traced with an event log under a device descriptor rather than inferred** — one tap ran `pointerenter:touch` (correctly ignored, the layer-1 `pointerType === 'mouse'` guard was already right here) … `focus → show(wrap)` [bubble `display:block`, `aria-expanded="true"`] … `click → isShown(wrap) → hide(wrap)` [`display:none`, `aria-expanded="false"`]. Net: the tip could not be opened by tapping, on a device where `(hover:none)` is true and tapping is the only way in. Fixed with the `ptrFocus` flag the dashboard adopted in S247, so **all three carriers now agree**; Tab-to-tip deliberately preserved. Verified on iPhone 13 with **the S247-fixed dashboard as a must-pass control that stayed green throughout**: touch tap opens / second tap closes (was: tap did nothing), keyboard focus opens / Escape closes, mouse hover shows / leave hides / click-after-hover toggles off — plus 16 desktop cells across both pages (light/dark × EN/HE × 1280/800), 0 pageerrors, 0 overflow, `dir` correct, every `.tip-wrap` bound. `hebrew_dictionary.html` carries the same shape but its five tips could not be surfaced headless, so it was **logged, not changed**.

- [x] 2026-08-23 | d31788c | classroom_dashboard.html | **The Exit Full Screen button is legible on a projected light board** (S248 Pass M, 1st hit — found only because the pass ran `body.fullscreen`, which is how a SmartBoard actually shows this tool). `#fsExitBtn` dimmed itself with a **group `opacity: 0.45`**, which fades the white glyph toward whatever is BEHIND the button rather than toward its own plate — so on the cream board it rendered **lighter than the page it sits on**. Measured from rendered pixels with a dependency-free PNG decoder: ink `(253,248,239)` on field `(178,173,165)` = **2.11:1** (the CSS-side probe independently said 2.12). It is the only visible way out of a fullscreen lesson, and it was effectively invisible; dark mode fades toward dark and so passed at 4.46:1 — **the same light-fails / dark-passes inversion the S247 Generate-button hover had, on the same page's opposite theme assumption.** Converged on **`.fs-strip-inner`**, this same file's own fullscreen overlay, which does the identical job over the identical two backdrops with a `0.72` plate and **no group opacity at all**: same ink, same border and blur idiom, restraint carried by the plate instead of by fading the text. Pixel-verified **2.11 → 8.59 light, 4.46 → 17.90 dark** (the arithmetic predicted 7.77 — the `backdrop-filter: blur(6px)` and the 1.5px border are why the pixels win again). Verified across 16 cells windowed + fullscreen; `#fsExitBtn` absent from every contrast-fail list.

- [x] 2026-08-23 | (S249 close-out) | branch/deploy note | **Branch/PR: CONTINUED on `claude/improveloop-m-n-passes-uy07ou` → draft PR #196** — verified via the API, not assumed: still OPEN / draft / unmerged / `mergeable_state: clean` at session start (head `96b23de`, 22 commits, base `b745745`, 0 reviews, 0 check runs — this repo has no CI, so 0 is the healthy result). The PR now carries **FIVE sessions (S245 + S246 + S247 + S248 + S249)**. **Drift check: NOTHING landed outside the loop for a FIFTH consecutive session** — `origin/main` still at **`b745745`** (now the longest such run in the ledger by two), branch `sw.js` **v558→v559** (bumped once in the final commit; precached files changed: `flash_cards.html`, `index.html`, `hebrew_dictionary.html`), `FONT_MAKER_VERSION` **5.28 unchanged** — **no Font Maker surface was touched at all this session (fourth session running clear of it)**, so no bump and no changelog entry was due. **Maintainer direction: "one more loop m/n" — the FIFTH consecutive directed M+N session**, so the strict stalest-first rotation stays set aside by explicit instruction and **G (print & export fidelity, last run S231) is now owed for SEVEN sessions running**; D (S232) and I (S233) follow it. Scripts run: `check-inline-js.mjs` (clean — 62 inline + 36 JSON-LD blocks across 14 pages parse), `check-i18n.js` (clean — Check A no new violations, 14 baselined; Check B fully translated), `update-sitemap.mjs` (last step). **`build-locales.js` NOT run and not needed — this session added zero CSV keys** (all three fixes are CSS/JS behaviour, no new or changed user-facing strings). **`update-llms-txt.mjs` NOT run and not needed** — no `<title>`, `<meta name="description">` or JSON-LD block was touched. **Decisions deferred to maintainer: NONE. Gates asked: NONE** — all three fixes are alignment/operability repairs that gate 3 and gate 4 explicitly leave autonomous (gate 3 names "a spacing/alignment fix"; gate 4 scopes the crush fix inside phone behaviour with the desktop result measured unchanged, and the desktop case it also repairs was a pre-existing bug at short window heights, not a desktop-facing change made in service of phones). **⚑ LANDED DIRECTLY ON `main` ON MAINTAINER DIRECTION ("push to main"), 2026-08-23 21:33Z — the THIRD direct-to-main landing of this thread and the in-session authorization the branch/PR protocol allows for (precedent S78).** Clean **fast-forward** `b745745..bcb6b82` (27 commits, no merge commit — `origin/main` was an ancestor, verified before pushing); GitHub then auto-closed **PR #196 as MERGED** (`merged_at` 21:33:00Z, `merged_by` Hbleiberg). **Deploy VERIFIED IN-SESSION for once, not deferred:** the `pages build and deployment` run for `bcb6b82` concluded **`success`** at 21:33:25Z — so `sw.js` v559 and all three fixes are live on ivritsuite.com. The PR-watch check-in was cancelled at the same time, per its own stop condition.

- [x] 2026-08-23 | (S249 Pass M — aesthetics & visual design, **7th-ever run**, `flash_cards.html`, its FIRST-EVER aesthetics audit) | **Target = exactly what the S249 pointer named** — the strongest of M's four never-audited surfaces, because flash cards is projected like the dashboard AND S244's N had already mapped its phone layout, so an M lands on mapped ground the way S248's did. **Method carried from S248 and it is again what found the hit: ask which mode the tool is actually USED in first.** The flash-card product is not the windowed page — it is `body.fc-fullscreen`, the projector "Present" mode behind `#fsBtn`. **HIT (1, fixed `b8213c9`): the projected card overflows its own column instead of centring.** `body.fc-fullscreen #screenCard.active .fc-perspective` overrides only `width` to `min(80vw, 96vh, 1000px)`; the base rule (`.fc-perspective { … margin: 0 auto }`) cannot centre a box wider than its 732px parent — over-constrained, both auto margins collapse to 0 and the box overflows to the inline-end. Measured, not eyeballed: **1920×1080 card 594→1594 vs everything else 594→1326 (268px overhang, centre 134px left of screen centre); 1280×900 274→1138 vs 274→1006 (132px); 1366×768 5px.** The progress bar, stats bar, "Card N of M", nav row and End-Practice-Early are all centred on the viewport, so the single biggest object on a classroom projector was the only thing off-centre — with the nav row centred under a left-shifted card, compounding it. **CLEAN (and this is the pass's other half): contrast is spotless suite-surface-wide on this tool.** 0 sub-AA text hits across **20 probe cells** — card front AND back × light/dark × 1280/800 × windowed/fullscreen (16), plus the results screen (2) and the setup screen (2, every `.panel` and `<details>` force-expanded) — all with **cumulative `opacity` composited** per the S248 `legibility-destroyed-by-a-second-dimming-layer` pattern and disabled components exempted. 0 pageerrors in every cell. **Logged, not fixed:** the card screen carries **5 distinct radii** (16px card/faces · 12px stats bar · 10px nav buttons · 8px `#endEarlyBtn` via `.btn` · 999px progress pill) — the 8/10/12 trio reads as three near-identical values rather than a scale (P4); and the results screen stacks two gold buttons separated by one navy, giving Save-Results and Practice-Mistakes equal weight for different jobs (P4, and a hierarchy judgement = gate 3 if ever acted on). **⚑ One incidental behaviour confirmed correct, NOT a bug:** pressing Next on an ungraded card re-queues it once (`showSkippedAgain`), so a 10-card deck legitimately needs ~20+ Next presses to reach results — bounded by the `isSkippedReshow` flag, documented in the code, and it is why an early probe wrongly reported "the results screen is unreachable".

- [x] 2026-08-23 | (S249 Pass N — mobile & touch-device, **7th-ever run**, `index.html`, its FIRST-EVER mobile audit) | **Target = the S249 pointer's re-ranked prime target.** After S248 came back 6-of-7 arms clean on the trope tutor, the pointer's own conclusion was that the residual phone risk sits in the pages nobody *drills* on a phone but everybody *opens* on one — and `index.html` is the hub, the PWA `start_url`, and the home of the AllTools backup modal. Real device descriptors confirmed in all 20 arm-1 cells (`coarse:true, hoverNone:true, mtp:1`). **Arm 1 (reflow/overflow) CLEAN in 20/20 cells** — iPhone SE 320 / iPhone 13 390 / Pixel 7 412 / iPhone 13 landscape 750 / iPad gen 7 810, light + dark, EN + HE: `scrollWidth == innerWidth` everywhere, zero elements past the viewport, 0 pageerrors. **Arm 2 (viewport contract) CLEAN** — `width=device-width, initial-scale=1`, no `user-scalable=no`, no `maximum-scale`. **Arm 3 (dynamic chrome): one observation, not a fix** — the page's only `vh` uses are `body{min-height:100vh}` (a floor, strands nothing) and `.ie-modal{max-height:90vh}`; on an iOS large-viewport reading the modal can exceed the visible area by ~11px, but its body scrolls and its ✕ is pinned in the header, so there is no dead end. **Arm 4 (touch parity) — THE HIT (fixed `2453788`): the Automatic/Manual Input toggle was completely un-tappable on a phone.** `.ie-modal-body` is a flex column with `overflow-y:auto`; every child defaults to `flex-shrink:1`, so when the content overflowed the column **crushed its rows instead of scrolling**, and all of the compression landed on `.ivrit-mode-toggle` — measured as the ONE child whose min-content height can collapse — squeezing it **33px → 2px**. Its `overflow:hidden` then clipped both buttons away: a **vertical hit-test scan across the toggle's entire band returned `.ivrit-mode-toggle` or `.ie-modal-body` at every single point and the buttons at none**, on iPhone SE (637px of content in 457px) and iPhone 13 (603 in 544). **Manual Input — the copy-and-paste restore path — could not be reached by finger at all.** Pixel 7 (634/634) and iPad (568/568) fit their content and were unaffected, which is exactly why this needed a real device descriptor rather than a resized window. The drop zone itself is fine: `#ivritDrop` carries the sanctioned **Browse…** fallback, so HTML5 DnD is never the only path. **Arms 5/6 CLEAN** — index has no text inputs beyond the hidden file input (arm 5 largely N/A); the modal fits the screen on all five descriptors with its ✕ in view and hit-testing to itself in 20/20 cells, and both ✕ and backdrop-tap close it. **⚑ Probe artifacts correctly NOT filed (the standing lesson, earned again):** the broken Sefaria logos in the phone frames are `files.readme.io` images my own route-abort blocked, not a page defect; and `inView=false` on the gear and erase buttons was my probe measuring before Playwright's scroll-into-view, not an off-screen control.

- [x] 2026-08-23 | b8213c9 | flash_cards.html | **The projected flash card sits in the middle of the board** (S249 Pass M) | The `body.fc-fullscreen` rule overrode only `width`, leaving the base `margin: 0 auto` unable to centre a box wider than its parent, so the card overflowed inline-end by up to 268px while every other element on the projected board stayed centred. Fixed with symmetric negative inline margins driven by a `--fc-fs-w` custom property — no size change, and they go positive to behave exactly like `auto` whenever the card fits. **Verified over local HTTP across 40 cells** — 1920×1080 / 1366×768 / 1280×900 / 1024×600 / 800×900, light and dark, EN and HE, windowed and fullscreen: **card centre == viewport centre to the pixel in every one**, `scrollWidth == innerWidth` everywhere, 0 pageerrors. `margin-inline` keeps the RTL result identical (verified: `dir=rtl` cells measure `off=0` too), and `80vw` caps the width so the overhang can never reach a screen edge. Before/after 1920×1080 frames in the session scratchpad.

- [x] 2026-08-23 | 2453788 | index.html | **Automatic / Manual Input is tappable on a phone** (S249 Pass N arm 4) | A scrolling flex column was crushing its own rows: `.ie-modal-body>*{flex-shrink:0}` lets the body scroll, as it already could, instead of collapsing `.ivrit-mode-toggle` to a 2px clipped hairline. **Verified across 32 cells** — iPhone SE / iPhone 13 / Pixel 7 / iPhone 13 landscape / iPad gen 7 **by tap**, plus 1280×900, 800×900 and a deliberately short **1280×620** desktop window **by click** (`page.tap()` requires `hasTouch`, so a desktop context needs `click` — 12 first-run "failures" were that harness detail, not the page), each in light and dark, EN and HE: toggle 33px and both buttons hit-testing to themselves in all of them, Manual reached and Automatic reached back in all of them, `scrollWidth == innerWidth`, 0 pageerrors. **The short desktop window was crushing the same row, so this was never only a phone bug.** The shared `.ivrit-*` CSS is deliberately untouched — the fix sits on `.ie-modal-body`, which exists only on this page, so the four `.ivrit` carriers stay convention-identical.

- [x] 2026-08-23 | 383aa2a | hebrew_dictionary.html | **Dictionary settings tooltips open when you tap them** (S249 Pass N arm 4) | **This closes the open thread S248 could not.** Third and last carrier of the `hover-only-affordance-under-a-synthetic-mouse-event` focus-race: `bindTip` sets `tabindex="0"`, so a pointer press focuses the wrap BEFORE its own click lands — `focus → show`, then `click → isShown → hide` — leaving the tip closed after one tap, on a device where `(hover:none)` makes tapping the only way in. **Why S248 measured zero-width, now on the record:** the five `.tip-wrap`s sit behind **TWO independent collapse layers** — the `.panel` AND the inner `.adv-section` — and **both rules are `display:none !important`**, so no inline-style override reveals them; removing the `.collapsed` class from both is what does, after which all five measure 20×15 and the race reproduces on iPhone 13 and iPad. **Adapted, not pasted** — this file's `hide()` takes no argument, unlike the torah/trope twins. **Verified across 16 touch cells** (iPhone SE / iPhone 13 / Pixel 7 / iPad gen 7 × light/dark × EN/HE): first tap opens (`#tipFloat` `display:block`, `aria-expanded="true"`), second tap closes, **and Tab-focus still opens it**, in every one. **Desktop proven unchanged by measuring the PRE-fix file the same way** (`git stash` → re-run → restore): 1280×900 and 800×900 produce the identical hover-then-click sequence before and after, because a synthetic mouse click fires `pointerenter` first — pre-existing behaviour this does not touch.

- [x] 2026-08-25 | (S250 close-out) | branch/deploy note | **Branch/PR: STARTED FRESH on `claude/torah-trainer-m-feature-u4wdpa`, cut from `origin/main` — exactly as the S250 pointer required.** Verified rather than assumed: `list_pull_requests` returned **zero open PRs** (PR #196 is merged and closed, as the pointer said), and the new branch was at `0	0` against `origin/main` at session start. **⚑ Drift since the S249 close-out: `origin/main` MOVED** — head is `5cb1eee`, not the recorded `bcb6b82`, carrying **three Font Maker landings outside the loop** (`0e463dc` stage growth, `9b95e6e` kerning/spacing/dagesh import, `5cb1eee` Sculpt eraser). Consequently `sw.js` was **v562 on arrival, not the v559 the pointer recorded**, and `FONT_MAKER_VERSION` is **5.31, not 5.28** — the standing drift-check lesson paid again; the version note would have been wrong on both counts. **This session: `sw.js` v562 → v563**, bumped once in this final commit; precached files changed = `torah_trainer.html` + `locales/en.json` + `locales/he.json`. **`FONT_MAKER_VERSION` NOT bumped** — nothing in this session touched the Font Maker. **Not a rotation session:** the maintainer directed one specific build, so **no discovery pass ran** and the A–N rotation is untouched (G still stalest, owed for eight sessions now). **Scripts run:** `check-inline-js.mjs` (62 blocks clean), `build-locales.js`, `check-i18n.js` (clean, no new Check-A violations), `update-sitemap.mjs` (after `git fetch --unshallow` — the clone arrived shallow at 50 commits and the script correctly refuses on one). `update-llms-txt.mjs` deliberately NOT run: no `<title>`, `<meta name="description">` or JSON-LD changed. **⚑ LANDED DIRECT TO MAIN on maintainer direction** ("Push to main", given after the draft PR was open) — a clean fast-forward `5cb1eee..ad08dc7`, 3 commits, verified `0	0` against `origin/main` immediately before the push so nothing was reconciled blind. **This is the FOURTH direct-to-main landing in this thread and the pattern now holds at four-for-four, always on explicit in-session direction — still never assume it**; the default remains a `claude/*` branch feeding a draft PR, which is exactly how this session started (PR #199, opened draft, clean, 0 check runs — this repo has no CI — 0 reviews, 0 comments, auto-closed as merged by the push). Deploy for `ad08dc7` verified `success`, so the toolbar is live.

- [x] 2026-08-25 | 30dc995 | torah_trainer.html | **A presentation toolbar on the projected board** — the S180 Pass H **M-sized seed**, built on maintainer direction. Full detail on the ticked seed line in Feature seeds; the short version: `body.fullscreen` hid the header, `#gearBtn` is the only `openSettings()` caller in the file, so the drawer and every font-size slider were unreachable in the exact mode the page sells as SmartBoard projection. `#ttFsBar` carries A−/A+, aliyah ‹/›, dark and ⚙, auto-hiding after 4 s idle on the `#fsStrip` model. Zero new storage keys, zero new settings fields, no CSP/index.html/`data/` change. **117 checks, 0 pageerrors**, light+dark × EN+HE × 1920+1280, with native fullscreen actually granted in the harness.

- [x] 2026-08-25 | 206a0e6 | torah_trainer.html | **Exit Full Screen is legible on a projected light board** — an unfiled **second carrier of the registered ACTIVE `legibility-destroyed-by-a-second-dimming-layer` pattern** (S248), found while exploring the surface the toolbar sits on. `#fsExitBtn` still carried the group `opacity: 0.45` over an `rgba(13,18,32,0.55)` plate that `d31788c` had removed from `classroom_dashboard.html`; torah was not in that commit. A group opacity composites the white glyph toward the **page** rather than toward its own plate, so on the cream board it measured **1.78:1** — and in fullscreen this button is the only visible way out of a projected lesson. **Dark passed at 4.55:1, which is exactly what kept it hidden** — the same light-fails/dark-passes inversion S248 and S249 both hit. Fixed with the sibling's ratified remedy (drop the group opacity, 0.72 plate, hover 0.88): **7.75:1 light / 18.24:1 dark**, measured both ways, no other declaration touched. Shipped as its own commit, before the feature, so the two are independently revertable.

- [x] 2026-08-25 | (S251 close-out) | branch/deploy note | **Branch/PR: STARTED FRESH on `claude/feature-seed-queue-refill-ee958o`, cut from `origin/main` — exactly as the S251 pointer required, and verified rather than assumed:** `list_pull_requests` returned **zero open PRs** (PR #199 merged and closed as the pointer said), and the branch measured `0	0` against `origin/main` at session start. **⚑ Drift check: NOTHING landed outside the loop.** `origin/main` is at **`eec957d`** — the S250 close-out's `ad08dc7` plus the one ledger commit that rode the same direct-to-main push — and **both version figures in the S251 pointer proved accurate for once**: `sw.js` **v563** and `FONT_MAKER_VERSION` **5.31**, exactly as recorded. The clone arrived **shallow** and was `git fetch --unshallow`ed before anything date-based ran (the ledger's standing caution about `git log` receipts in this container). **Session shape: maintainer-directed — "refill the feature seed queue".** H is the loop's only seed intake and had not run since S241, so H ran as the session's pass; the A–N rotation is otherwise untouched and **⚑ G — print & export fidelity — is now owed for NINE sessions (last run S231)**, followed by D (S232) and I (S233). **`sw.js` v563 → v564** (precached file changed: `trope_tutor.html` ×2 commits). **`FONT_MAKER_VERSION` NOT bumped** — the Font Maker was not touched this session. **Scripts run:** `check-i18n.js` (Check A clean, Check B clean), `update-sitemap.mjs` (last step). `build-locales.js` **not** run — **zero CSV keys were added**, because the one fix that would have needed a new string was refuted before any code was written. **Decisions asked (2, both answered):** gate 1 → **skip the micro-feature**, keep all 7 seeds banked; gate 2 → approved the drafted zero-state copy, which then became **moot** when the underlying candidate was refuted. **Decisions deferred to maintainer: none.** Deploy `success` is verified after merge, not in-session.
- [x] 2026-08-25 | 1414f59 | trope_tutor.html | **The mastery-grid family bar mirrors too — the logical-property breach is now CLOSED in this file** | Second and last carrier of the shape fixed in `33fad20`. `.tu-mfam-label`'s 4px family colour bar faces the mastery cells it labels, but was pinned with physical `border-right` + `padding-right` + `text-align:right` (and the same shape again in the narrow stacked override), so when the flex row mirrored in Hebrew **the bar sat on the far edge, away from the cells it names**. Converted to `border-inline-end` / `padding-inline-end` / `text-align:end` (+ `:start` in the stacked override) plus the six `fam-*` colour rules. **`trope_tutor.html` now contains ZERO physical `border-left`/`border-right` declarations.** Verified 12 cells (EN/HE × light/dark × 1280/800/420, **drill view open**): EN byte-identical, HE now `inked=left / facingCells=left`, 420px stacked correctly drops the bar in both languages, 0 pageerrors; the six chips from `33fad20` re-verified 8/8 unchanged. **⚑ Method note — the probe that nearly banked a false clean:** the first run of this measurement reported **all 12 cells OK** because the mastery grid lives inside a hidden `#drillView` and every rect was **0×0**; the degenerate geometry made "which edge faces the cells" answerable and wrong. `setMode('drill')` must run before measuring. **This is the S214 blind-zero trap in a new costume, and the second probe defect this one session** — see the refuted P3 below.
- [x] 2026-08-25 | 33fad20 | trope_tutor.html | **The clause-family colour bar mirrors in Hebrew** (S245 Pass M candidate, closed) | CLAUDE.md i18n §5. The six family chips (Sof Pasuk / Zakef Katon / Segol / Revia / Geresh / Rare) carried their identifying colour on the physical **left** edge in both directions, so in the RTL UI the colour that names the family sat on the **trailing** edge. `border-left` → `border-inline-start` on the base rule + `border-left-color` → `border-inline-start-color` on all six `fam-*` rules; source order unchanged, so the `fam-*` rules still win over `:hover`'s `border-color` exactly as before. Verified across the full 8-cell matrix (EN/HE × light/dark × 1280/800): **EN byte-identical** (thick edge left, same computed colours — a proper no-op), **HE now thick=right/start=right in all four cells**, hover and active re-checked in both directions (family colour kept on the start edge, other three sides gold), 0 pageerrors. **Suite grep first, per the S250 lesson:** `torah_trainer.html` carries no byte-similar rule — its legend swatches read the body CSS vars instead — so the shape was confined to this one file.
- [x] 2026-08-25 | bddce8b | docs/IMPROVEMENT_LOG.md | **Both S251 candidates WITHDRAWN — refuted by re-grounding, no code shipped** | The pass's own P3 ("Generate is a dead click with zero letters") was a **probe defect, not a product defect**. The generator already implements the house inline-validation contract **more completely than the `_setPresetNameNote` flow the candidate proposed converging on**: `_setGenerateNote(key)` writes into **every** `.gen-note` node and sets `aria-disabled` — deliberately not `disabled`, with an in-file comment saying why — on **every** `[onclick="generate()"]`, and `updateCombosCount()` auto-retires the note the instant the teacher re-selects. Re-measured: `select_more_letters` fires, `aria-disabled="true"`, note visible in 2 of 3 nodes, retires on fix, 0 pageerrors. The paired P4 (blank combos count) was withdrawn on the same grounding — `''` is the file's own `nikud`-branch idiom for "no count applies". **Durable rule recorded in the struck candidate: read the note AFTER the action, and query the page's own note class before calling a control silent — `alert()`-absence is not evidence of silence in a suite whose stated direction is inline validation.**
- [x] 2026-08-25 | 811d0c5 | (S251 Pass H — teacher walkthrough / paper-cuts, `hebrew_blend_generator.html`) | **The generator's first dedicated H since S130 (~121 sessions) and its FIRST SEEDS EVER — bench 4 → 7, and no longer a one-tool bench.** **Target RE-DERIVED per S196 against BOTH the pointer and pure staleness:** the pointer named flash-cards or index, and `classroom_dashboard` is the stalest H (S107) — but the dashboard already owned **all four** remaining seeds, so an H there would only have deepened a bench that was already 100% one tool. The generator was 2nd-stalest, **zero-seeded**, the flagship, and S247's M had already mapped its post-Generate surface. Walked the H prompt's own example lesson end-to-end — "Tuesday's aleph-bet worksheet **and a matching flashcard deck**" — and **the seeds are what the lesson's SECOND HALF ran into**. **Positive receipts:** **3 clicks cold-load → printable tracing sheet** (1-Letter → Tracing → Generate; 4 sheets, build queue drained, 0 pageerrors), a genuinely instructive empty state (5 ready-made recipes + tour entry), and Print / Export PDF / Copy-shareable-link / Regenerate / Save-preset all present on the result. **Yield: 3 feature seeds + 3 would-be findings killed by grounding** (dictionary→flash-cards already ships via `useFcLink`; the two candidates above). **Headline seed measured, not inferred:** flash_cards' `?s=` boot handler already calls `applySettings(payload.data || payload)` on key names **byte-identical** to the generator's `getSettings()`, so the forward handshake needs **ZERO consumer-side code** (the 781f3df precedent) — proven end-to-end, `['בּ','ג','ד'] + ['a','patah']` → 159-char code → fresh context restored both sets exactly with the drill live. **Second seed measured across all six tools:** the generator is the **only** preset-bearing tool with no live-state blob — it writes only `hideZoomBar` + `inputMode`, both per-device and both export-exempt — so a reload resets letters, vowels, title, count, practice type and worksheet type to defaults.
