# IvritSuite Improvement Log

The memory of the continuous-improvement loop. **Read this first every session.** One concern per
iteration, one commit per iteration. Prioritization: P1 (data loss/security/broken core/export
corruption) > P2 (silently wrong output/undo holes/a11y blockers) > P3 (perf/dead UI/confusing
copy/consistency) > P4 (polish). Tie-breakers: (1) affects teachers' saved work, (2) affects the
printed/exported artifact a student receives, (3) dual-audience (Hebrew + secular) wins, (4) smallest diff.

## Candidates (prioritized, top = next)

- [ ] P2 | classroom_dashboard.html | **39 settings-drawer toggle switches have no accessible name** (S135 Pass C — first-ever dedicated C on the dashboard). Each toggle row is `<div class="form-row"><label data-i18n>Text</label><label class="toggle"><input type="checkbox" id=X><span class="slider"></span></label></div>` — the descriptive `<label>` has NO `for=`, and the `.toggle` wrapper wraps the input but carries no text, so the checkbox is nameless. Confirmed ground-truth in the Chromium AX tree: **33/33 visible checkbox nodes nameless** (the other 6 sit in still-collapsed panels; same pattern). An SR user hears only "checkbox, not checked" across the whole settings surface of the projected classroom tool. | found: 2026-07-22, S135 Pass C
- [ ] P3 | classroom_dashboard.html | **2 range sliders nameless** — `#zoomSlider` (zoom bar; preceding `<span data-i18n="dashboard.display.zoom_label">🔍 Zoom</span>` is unassociated) and `#ttsRateSlider` (TTS-speed column; its `<label>` has no `for`). Same class as the S125 FM slider fix — resolve with `aria-label`/`aria-labelledby` reusing existing visible text, no new keys. | found: 2026-07-22, S135 Pass C
- [ ] P4 | classroom_dashboard.html | **No custom `:focus-visible` indicators** (0 rules in the file) — the UA default outline IS present on buttons/inputs (only `.editor` sets `outline:none`, and it has a `.editor:focus` gold-border replacement), so keyboard focus is visible and this is polish, not a blocker. A high-contrast focus ring styled for both themes would be a nicety; restyling focus across the 7k-line file risks regressions — parked. Also minor: the settings text inputs (`#locationInput`/`#videoUrlInput`/`#presetName`/`#timerCustomInput`) rely on their localized `data-i18n-placeholder` as the sole accessible name (a real `<label>` would be sturdier, but placeholder names are localized and present). | found: 2026-07-22, S135 Pass C
- [x] ✅ DONE S125, f5d4080 + 18d9a5d | Hebrew_Font_Maker.html | **Range sliders nameless to screen readers** (S125 Pass C — first-ever dedicated C on FM). FM's `vSlider()` helper + the spacing/kern panels + `#zoomRange` rendered slider labels as unassociated adjacent text, so every affected `input[type=range]` had no accessible name. **FIXED S125:** f5d4080 = `vSlider` `aria-label="${esc(label)}"` (6 opacity sliders, localized for free) + `#zoomRange` mirrors `nodeZoomRange` (reuse `fontmaker.node_editor.zoom_title`); 18d9a5d = the 5 spacing/kern sliders (`setLetter/Word/LineSpacing`, `setKernValue/Target`) `aria-label` reusing their existing `fontmaker.panels.*` label keys. No new keys, no FM version bump. Verified headless: FM visible nameless controls 3→0; all sliders expose localized names EN/HE, light/dark, pageerror==0. | found: 2026-07-20, S125 Pass C
- [x] ✅ DONE (maintainer design-call, c1591a8, 2026-07-22) | Hebrew_Font_Maker.html | **Interactive editing stages given an accessible name** (S125 Pass C). **RESOLVED — a guarded `aria-label` naming the current glyph (`labelStage`) on `#stage`/`#meStage`/`#adStage`; NO role change (the `#nodeBar` keyboard path stays authoritative, so `role="application"`/`"img"` deliberately avoided); refreshed from `applyI18n` (renderStage isn't re-run on a live switch) with an English fallback on a pre-locale key-return. Verified EN+HE headless.** _Original analysis:_ The glyph-edit `#stage` / `#meStage` (SVG) and `#adStage` (div) are pointer-driven editing canvases with no `role`/`aria-label`/`tabindex` — an SR user gets no context for what they are. Nuanced: they're editing surfaces, not images, and the *keyboard* path is the separate `#nodeBar` toolbar (roving tabindex, already accessible), so `role="application"` risks trapping SR users and `role="img"` mislabels an interactive surface. A safe slice would be an `aria-label` describing the current glyph (e.g. "Glyph editing canvas — {glyph}") without a role change, but wants a deliberate design call. Otherwise FM audited **a11y-strong** (S125): 0 nameless visible controls after the slider fix, icon buttons carry titles/aria, modal focus-trap via `aOpenModal`, `.me-coord`/`.ad-box` focus-visible indicators present, wrapping-`<label>` coordinate inputs. | found: 2026-07-20, S125 Pass C
- [x] ✅ DONE S123, d57108a | hebrew_blend_generator.html + hebrew_dictionary.html | **Fullscreen `#fsBtn` title reset to hardcoded English on `fullscreenchange`** (S123 Pass F — fullscreen-toggle affordance audit). Both tools hid the header in fullscreen and reset `btn.title` to the literal `'Exit full screen'`/`'Full screen'` in JS, clobbering the button's localized `data-i18n-title` after one fullscreen round-trip → Hebrew UI then showed an **English** tooltip on the fullscreen button (and EN was inconsistent: static i18n resolves `shared.fullscreen.enter_title` = "Present fullscreen"). Check-A i18n blind spot (`.title` set to a plain JS literal, invisible to the gate). **FIXED S123 (d57108a):** converged both to flash_cards' canonical `_syncFsBtn` pattern — set the state-appropriate `shared.fullscreen.{enter,exit}_title` key + resolve title via `I18n.t()`. Reused existing keys (no CSV/build). Verified headless EN+HE × light/dark × 1280/800, pageerror==0. | found: 2026-07-20, S123 Pass F
- [x] ✅ DONE S124, 9dc2662 | classroom_dashboard.html | **Student-picker chip `.title` tooltips hardcoded English** (S123 Pass F sweep, ~L4900). `chip.title = absent.includes(name) ? 'Absent today — tap to include' : …` — JS-built `.title`, hardcoded English on picker chips. **FIXED S124 (9dc2662, Pass K):** the keys `dashboard.picker.chip_absent`/`chip_present` **already existed** (translated) but were never wired — pure wiring gap. Wired both via `I18n.t()` (matching adjacent picker render code); `applyI18n` re-renders the picker so a live switch re-localizes. No new keys. Verified headless (seeded roster, one absent → both branches) EN/HE + switch, light/dark × 1280/800, pageerror==0. **NOTE — verified NOT a bug:** flash_cards' `~L3476` `setAttribute('aria-label','More information')` is correctly wired (`data-i18n-aria-label='shared.tip.aria_more_info'`) — false positive; don't re-log. | found: 2026-07-20, S123 Pass F
- [ ] P4 | Hebrew_Font_Maker.html | **Template-literal tooltip backlog — remaining ~9 (the pure-wiring 9 DONE S126, 1984167)** (S124 Pass K sweep — the tracked FM-heavy blind-spot backlog). ~~~9 have **exact orphaned existing keys** (pure wiring): the `guidesPanelHTML()` cluster (`guide_collapse_title`/`guide_color_title`/`guide_name_title`/`guide_delete_title`/`guide_style_title`), plus `me.delete_piece_title`, `panels.custom_del_title`, `panels.kern_click_edit`, `panels.combined_remove_title`~~ **DONE S126 (1984167 — inline guarded `${esc(window.I18n ? I18n.t('key') : 'fallback')}`, 0 new keys, verified EN↔HE)**. **Remaining (P4, all need new/reconciled Hebrew keys → maintainer-authored):** ~3 need new keys ("Copy this letter", "Redesign this mark", "Convert to a main glyph (gives it its own adjustable width)"). ~6 are interpolated ("Trace ${…}", "Place the ${…} for ${…}", "Add ${…}", "Assign box ${b.id}", "Sample text at ${px} pixels", "You added a custom glyph at U+${t}…") → param keys. **MECHANISM (important):** FM's `T()` helper is scoped to the About tab only, and `renderControls()` rebuilds these panels frequently **without** re-running `applyStaticI18n` (the only `applyStaticI18n` is on language switch, L13842) — so `data-i18n-title` **alone is insufficient** (fresh re-renders would show the English fallback until a switch). Correct fix = **inline guarded `${esc(window.I18n ? I18n.t('key') : 'fallback')}`** in each panel builder (+ `data-i18n-title` belt-and-braces). Pure i18n fix → **no `FONT_MAKER_VERSION` bump** (per S112/S114). Best done as its own focused FM effort. | found: 2026-07-20, S124 Pass K
- [x] ✅ DONE S124, 5dd4f94 | hebrew_dictionary.html | **Emoji-card gender forms + speak button hardcoded English** (S124 Pass K sweep). The `addForm` template literal hardcoded "♂ Masculine"/"♀ Feminine"/"…(only form)" + the 🔊 button title/aria "Hear pronunciation" — a consistency gap (the word *modal* localized these; the emoji *card* didn't). **FIXED S124 (5dd4f94):** reused existing `dictionary.modal.form_*` + `dictionary.card.hear_title`; added 2 keys for the sole-form variants; wired via `data-i18n*` + inline `I18n.t()` (renderEmojiResults isn't re-run by applyI18n). Verified headless EN/HE + switch, light/dark × 1280/800, pageerror==0. | found: 2026-07-20, S124 Pass K
- [x] ✅ DONE S109, 74c8f76 | classroom_dashboard.html | **Schedule Sync is a soft dead-end for a brand-new teacher** (S107 Pass H). **FIXED S109 (74c8f76, maintainer-directed):** `renderScheduleValidation` now shows a muted `.sched-hint` ("Each block switches to a saved preset — create a couple in Presets first.") when `≤1 preset` AND `≥1 schedule row`; new key `dashboard.schedule.need_presets_hint` (EN+HE). Original: each schedule block switches to a saved *preset*, but a new teacher has only the one seeded "Default" preset — so `addScheduleRow` (L5730) offers a single-option preset dropdown, while the panel's own explainer (L2174) walks through "Morning → Ivrit → Recess" presets that don't exist, and `renderScheduleValidation` (L5928) gives no "create a couple of presets first" guidance. Fix sketch: when `Object.keys(presets).length <= 1`, render a one-line hint into `#scheduleValidation` pointing at the Presets panel (1 new i18n key). Design-call on wording → log, don't force. | found: 2026-07-19, S107 Pass H
- [x] ✅ DONE S109, 74c8f76 | classroom_dashboard.html | **'Hebrew classroom' starter over-promises "Everything on"** (S107 Pass H). **FIXED S109 (74c8f76):** dropped the "Everything on —" prefix (EN+HE) so the description accurately enumerates. Original: `dashboard.setup.starter_hebrew_desc` = "Everything on — Hebrew headers & date, Shabbat, Omer, holidays, weather." but no `STARTERS` diff (L2643) enables the Student Picker / video / dash-text (opt-in by design), so "Everything on" is inaccurate. Trivial copy fix: drop the "Everything on —" prefix (the desc already enumerates). | found: 2026-07-19, S107 Pass H
- [x] ✅ DONE S118, 969e293 | classroom_dashboard.html | **Settings-drawer power-user panels now ship collapsed** (S107 Pass H candidate; user-directed S118). **FIXED S118 (969e293):** added `collapsed` to `#presetsPanel` + `#schedulePanel` (backup lives inside Presets) so the daily controls sit higher — matching the pre-existing `collapsed` About & FAQ panel; the panel-title click still expands them. Pure default change: no new copy, no persistence change (collapse stays transient by design, per `expandAllMenus`/`collapseAllMenus`). The maintainer chose "collapse the dashboard power-user panels" (S118 AskUserQuestion) over persisting per-panel state. Verified headless (light/dark × desktop/800px): both start collapsed (bodies hidden), all daily panels stay expanded, About unchanged, title-toggle works, 0 pageerrors. | found: 2026-07-19, S107 Pass H
- [x] ✅ DONE S103, 439bb47 | hebrew_dictionary.html | **Word Lists workflow is buried + under-signposted** (S68 Pass H). The flagship "save a lesson's vocabulary as a Word List" flow lived two collapse levels deep (`#panelAdvanced` → `#copyOptsSec`) behind **"Bulk Copy Mode"** — no top-level entry point. **FIXED S103 (439bb47):** a **"📚 Word Lists" button** now sits in the always-visible results `.toolbar` (next to 🖨️ Print, mirroring the S102 Print-button pattern), opening the existing `wlOpenManager()` — view / load / rename / delete lists or send one to Flash Cards. When empty, the manager's instructive empty-state ("Turn on Bulk Copy Mode, select words, and use 'Save as Word List'") guides first-time creation, so it's a signpost not a dead-end. Reuses the existing `dictionary.wordlists.manager_button` label key + 1 new toolbar-title key (en+he); no new state/localStorage. (Prior halves: stale-onboarding-copy FIXED S85 b711347; per-word Add-to-Word-List S98 1e1e9c3.) | found: 2026-07-11, S68 Pass H; done S103
- [x] 🚫 WON'T-FIX (maintainer decision S118) | torah_trainer.html | **Reading-selection paper-cut — Diaspora/Israel toggle stays in the drawer.** Jump-to-this-week half FIXED S94 (7fd0ab3, toolbar chip); custom-range-resets-to-Genesis half FIXED S93 (ed47aba). **Surfacing half CLOSED S118 — the maintainer explicitly chose to keep the Diaspora/Israel schedule toggle (`#schedDiaspora`/`#schedIsrael`) buried in the settings drawer** (S118 AskUserQuestion: *"I like … the Israel/diaspora toggle being buried"*). Do NOT re-log or re-propose surfacing it in future Pass H runs. | found: 2026-07-13, S77 Pass H
- [x] ✅ DONE S106, 8f26ea9 | torah_trainer.html | **Audio-unavailable / status messages land on the low-visibility karaoke status line** (S77 Pass H). **Visibility half FIXED S98, deb2e28** — the two teacher-facing messages ("Audio for aliyah {n} is not available…", "Chanting isn't available for this reading.") also fire the visible `showHintToast` (role="status", auto-dismiss). **Localization half FIXED S106, 8f26ea9:** the 11 sibling hardcoded-English `setKaraokeStatus`/`setStatus` calls (No-PocketTorah-manifest, Audio-basename-missing, Failed-to-load-timings, Loaded-N-timings, word-count-drift note, Pick-a-parsha-to-chant, Sefaria this-week / cant-determine-parsha, translation-version fallback — all Check-A blind spots: template literals + plain function args) were wired to their **already-existing** `torah.status.*` keys (EN+HE both filled since the 07-12 rollout; 0 new keys, no build-locales, EN byte-identical). | found: 2026-07-13, S77 Pass H; visibility S98; localization S106
- [x] ✅ DONE S105, 59ed584 | hebrew_dictionary.html | **Word Lists polish** (S68 Pass H): ~~dup names allowed (warned only after saving)~~ **FIXED S98, a2c1159**. ~~gate labeled "Bulk Copy Mode"~~ **MITIGATED S103, 439bb47** (top-level "📚 Word Lists" toolbar button). ~~(a) `wlRenameList` had no dup-name guard + (b) Rename used raw `prompt()`~~ **FIXED S105, 59ed584 (maintainer-directed):** the manager's Rename is now a **styled inline editor** (pre-filled input + Save/Cancel + aria-live status + Enter-to-commit) reusing the Save flow's dup guard (blocks renaming onto another list's name, excludes self) — replaces the raw `prompt()`, no more dup-name asymmetry. Delete's `confirm()` intentionally **stays** (destructive-action guard, Shared-UX §5). 2 new keys (rename_save/cancel). Verified light+dark×1280/800: 0 native prompts, dup/empty blocked, Cancel/Enter/same-name all correct, pageerror=0. | found: 2026-07-11, S68 Pass H; dup-name-save S98; mislabel-gate S103; rename+dup-guard S105
- [x] ✅ DONE S103, 62be610 | classroom_dashboard.html | **`resetPanelLayout` has no confirm** (S93 Pass A observation, 639fcf8). "↺ Restore default layout" wiped the teacher's custom panel arrangement with no confirm/undo, unlike the suite's other reset buttons. Logged "log, don't force" (maintainer shipped no-confirm by choice); **DONE S103 (62be610) under explicit maintainer direction** — added `if (!confirm(I18n.t('dashboard.layout.reset_confirm'))) return;` (canonical dashboard confirm pattern) + 1 new localized key (en+he). Verified headless: dismiss preserves the custom layout, accept still resets to `DEFAULT_PANEL_LAYOUT`, localized dialog, pageerror=0. | found: 2026-07-17, S93 Pass A; done S103
- [ ] P4 (informational/future — no near-term fix) | torah_trainer.html | **Pass G print residuals** (S53; items 1–2 of the original tail fixed S55 in fb38cec): (3) informational: nikkud letter-mode colors print but collapse to similar grays on B/W — no secondary channel exists for vowels (the trope layer now has its print underline); (4) future refinement: side-by-side print is collapsed to one column — a verse-row-aligned two-column print (e.g. `display:contents`) would restore parallel text on paper. | found: 2026-07-10, S53 Pass G; reduced S55
- [x] 🚫 WON'T-FIX (maintainer decision S118) | flash_cards.html | **Teacher-critical panels stay collapsed on first run.** `#panelTimer`/`#panelTeacherTools`/`#panelAdvanced` ship `class="panel collapsed"`. **CLOSED S118 — the maintainer explicitly chose to keep the flash_cards teacher panels collapsed** (S118 AskUserQuestion: *"I like the flash card teacher panels being collapsed"*). Consistent with S39's finding that dropping `collapsed` would lengthen setup scroll for **every** user (collapse doesn't persist) and that the tour (steps 4–5) + first-run pulse already surface these panels for new teachers. Do NOT re-log. | found: 2026-07-08, S34 Pass H; re-scoped S39 Pass F
- [ ] P4 | flash_cards.html | **"My Word Lists" dead-ends to the Dictionary** (Pass H). Words mode's My-Word-Lists source shows "Create word lists in the Dictionary, then come back to drill them here" — no in-app authoring, a hard context switch mid-setup. Cross-tool fix — log, don't force. | found: 2026-07-08, S34 Pass H
- [x] ✅ DONE (maintainer design-call, c1591a8, 2026-07-22) | flash_cards.html | **Per-card results breakdown auto-expands on mistakes** (S120 Pass H). **RESOLVED — `showResults` toggles `#panelResults` open when `mistakeIndices.length > 0`; a perfect score stays collapsed. Both paths verified headless.** _Original:_ After a drill, `#panelResults` (the ✓/✗ per-card list — the "what did the student miss" review) is `class="panel collapsed"` (~L1824); a teacher reviewing what to reteach must expand it. Candidate fix: auto-expand it on the results screen **when `mistakeIndices.length > 0`** (a perfect score stays collapsed/clean). Borderline design (collapsed default keeps the student's score-hero view clean) → log, don't force. | found: 2026-07-20, S120 Pass H
- [x] ✅ DONE S123, 8225e07 | flash_cards.html | **Words mode: no default list selected even when lists exist** (S120 Pass H). `selectedWordListIds = new Set()` starts empty, so a teacher with saved lists had to tick one before Start (else `err_select_wordlist`). **FIXED S123 (8225e07):** `renderWordListPicker` now auto-checks the sole list when exactly ONE exists and none is selected (runtime-only; multi-list left ambiguous; an in-session deselect still holds until re-render). No new state/key — `selectedWordListIds` already persists via `selectedWordListNames`. Verified headless fresh-profile (seeded lists): 1 list → auto-selected + Start reaches the card screen with no error (light/dark × 1280/800); 2 lists → neither auto-selected; pageerror==0. | found: 2026-07-20, S120 Pass H
- [~] ✅ RENAME DONE (maintainer design-call, c1591a8, 2026-07-22); residual mode-label copy still open (P4) | flash_cards.html | **Two near-identically-named "practice the hard ones" paths** (S120 Pass H). **DONE — `#mistakesBtn` "Practice My Mistakes"→"Practice This Drill's Mistakes"; `#weaknessesBtn` "🎯 Practice My Weaknesses"→"🎯 Practice My Weak Cards" (EN+HE). STILL OPEN (maintainer chose rename-only): the "Other" mode pill + "Emoji" sub-mode label copy calls below.** `#mistakesBtn` "Practice My Mistakes" (results — THIS drill's wrong cards, `practiceMistakes`) vs `#weaknessesBtn` "🎯 Practice My Weaknesses" (Teacher Tools — cross-session profile weak items) read as the same feature. Disambiguating copy (e.g. "Practice This Drill's Mistakes" / "Practice My Weak Cards") would help but needs new Hebrew + a wording call → maintainer. Also logged: the **"Other" mode pill** is opaque (hides Numbers/Colors/Emoji/Words) and the **"Emoji" sub-mode** label over-promises (scope is Animal+Food only, `EMOJI_VISIBLE_CATEGORIES`) — both copy/design calls. | found: 2026-07-20, S120 Pass H
- [ ] P4 | flash_cards.html | Letters/syllables path rebuilds `buildPool` ×3 per card (+ `pickVowel`/`allVowelsList`) inside `generateDeck`/`sampleChoices` — could thread the base pools + vowel list through `buildDeckContext` (L5291) once, like the emoji pool already does, but the payoff is tiny. **Re-measured S75 Pass D (2nd D pass) — still negligible, NO regression:** ~7.6 µs/card; `generateDeck(20)` 0.4 ms @1× / 1.55 ms @4× (≤ the S15 1.3–5.9 ms), `generateDeck(100)` ≤6.2 ms @4× (~30× under the 200 ms bar). Mechanically hoistable (per-position lock logic only *overrides* the base pool) but ~1–5 ms/deck → **not worth an edit; parked.** | found: 2026-07-06, S15 Pass D; re-measured 2026-07-13, S75 Pass D
- [ ] P4 (informational — parked, throttled-only) | classroom_dashboard.html | Cold-load: one main-thread longtask **only at 4× CPU throttle** — the ~60-call `DOMContentLoaded` boot block (near L6690). **STALE half corrected S117:** the "render-blocking 5-family Google Fonts stylesheet" is no longer render-blocking — the maintainer's ~07-17 change loads it `media="print" onload="this.media='all'"` (+ `<noscript>` fallback + a local `@font-face` for Frank Ruhl), so the boot block is now the sole cause. **Re-measured S117 Pass D (3rd dedicated D):** boot **@1× max 195 ms (UNDER the 200 ms bar — clean at real desktop speed) / @4× 561 ms max, 796 ms sum**; nav→settled 1.9 s @1× / 2.4 s @4×. All interactions <200 ms even @4× (openSettings 65, colorCode+rerender 31, layoutEdit+3moves 145, darkToggle 153). **Idle/all-day churn CLEAN** — the S14 preview guard held, the S117 nn-block guard (b5d4261) removed the last per-second innerHTML rebuild, and 40 open/close·dark·lang·re-render cycles grew DOM 1104→1104 (Δ0) and `setInterval` 3→3 (Δ0) (no listener/interval/DOM accumulation). Only a deferred/idle-time boot-panel build would improve @4× — a larger refactor with init order-deps, not a small-diff win. | found: 2026-07-06, S13 Pass D → S14 → S35 → **S117 (fonts-fixed correction + re-measure)**
- [ ] P4 (informational — parked) | hebrew_dictionary.html | **Cold-load parse + broad-sort, both inherent** (S105 Pass D, first dedicated D on the dictionary since the S14 multi-tool pass). **Effectively CLEAN — well-architected** (results paginated `PAGE=100` via `loadMore`; search + gematria debounced 150ms). Measured: cold-load (nav→13,081-word corpus built + first render) **1036 ms @1× / 1170 ms @4×** with one **517 ms @1× boot longtask** (the 2.9 MB `data/hebrew_words.json` fetch+parse+build — `data/` corpus is out of improvement-loop scope; already progress-barred via `fetchJsonWithProgress`). Per-interaction all fine @1× (filter-all 70 ms, search 62 ms, one 100-card page 28 ms); only the **broadest** filter/search cross 200 ms **@4× only** (229 / 205 ms), and both are debounced. That cost is **~all `Intl.Collator.compare` × ~180k on the 13k sort** (measured 127 ms @4×; filter-only just 4.4 ms). **Sort-branch hoist empirically ruled out** (0 ms @1× / −4.7 ms @4× — V8 already JITs the constant branch). A precomputed collation key *could* cut it but risks getting Hebrew ordering subtly wrong → not a clean win. Full-render-all (13,081 cards, ~4 s @1×) is pathological (needs ~130 "Load more" clicks) — pagination protects normal use. Same parked category as the dashboard/FM/trope/flash_cards cold-load longtasks. Emoji mode (457 KB corpus, lighter) not separately deep-profiled this pass. | found: 2026-07-19, S105 Pass D
- [ ] P4 (informational) | Hebrew_Font_Maker.html | **Cold-init boot longtask** (S85 Pass D, first-ever FM D). `init()`→`newProject()`→`renderGrids()` (8+ panel builders over 54 letters) + the `applyI18n` re-render fans into one boot longtask **438 ms @4× / 104 ms @1×** — one-time, throttled-only, desktop-fine; same category as the dashboard cold-load P4 above. Only a deferred/idle-time panel build would improve it (not a small-diff win). Everything else measured clean (heaviest Generate Bold render ~92 ms @4×, apply ≤150 ms @4×; the redundant per-frame warning scan was fixed S85, 2f59215). | found: 2026-07-16, S85 Pass D
- [ ] P4 (informational — parked, throttled-only) | trope_tutor.html | **Cold-init boot longtask** (S95 Pass D, first-ever dedicated D on the Trope Tutor). Desktop @1× is fully clean (0 longtasks, ~176 ms load); only under 4× CPU throttle does cold init fan into boot longtasks (max ~385 ms / ~1146 ms total across 7 tasks) — one-time, throttled-only, desktop-fine, same category as the parked dashboard cold-load + FM cold-init siblings. All interactions stay under the 200 ms bar even @4× (full 10-question drill session 94 ms, `renderLearn` all 6 families 21 ms, `startDrill` build 1.7 ms). Only a deferred/idle-time init would improve it (`init`→`loadData`→`renderLearn` + the i18n-ready re-render) — not a small-diff win. | found: 2026-07-18, S95 Pass D
- [ ] P4 | hebrew_blend_generator.html | `exportPDF` (near L8139): `html2canvas(scale:2)` per sheet + per-pixel `getImageData` cut-line scan, worst on a 200-card bingo — largest absolute stall but user-initiated and already chunked across `await`ed pages. Not measured (needs the export libs, which are CDN — **re-confirmed unreachable through the proxy at S128**, so still un-profilable headless). | found: 2026-07-06, S13 Pass D
- [x] ✅ SHIPPED (user-directed, 37125a9) | hebrew_blend_generator.html | **First-generate reachability** (S130 Pass H, generator's 2nd H [last S56]) — **RESOLVED: the maintainer greenlit the frozen Generate footer.** Added a fixed "⬡ Generate" footer pinned to the bottom-left of the viewport (sidebar width) on desktop; an IntersectionObserver hides it while the inline `#generateBtn` is on screen (no double buttons); reuses the `generate_mob`/`generate` keys (0 new); desktop-only (mobile has `.mobile-fab`). Verified light/dark × 1280/800 + mobile, pageerror==0. The original friction:  the primary `#generateBtn` sits at the **bottom** of a ~2693px sidebar (top ~2263 @900px viewport) with **no sticky/floating variant**. Well-mitigated: the empty state offers 5 one-click ready-made starters (scroll-free) + the clear instruction "…then click Generate Worksheet", the selectors flow top-to-bottom so customizing letters/vowels lands you right at Generate, and after the first generate **live-preview auto-updates on every tweak** (verified: blendCount 40→20 re-rendered with no click). So the only real friction is the rare "change one top control, then generate" path. A sticky/floating Generate button OR an empty-state "Generate Worksheet" button would remove it — but both are UI-design choices the maintainer should own (placement vs the fullscreen/zoom chrome; the deliberate starters-first empty state). Otherwise the generator is mature/well-onboarded (sane defaults, live-preview, Real-Words built-in word bank = no dead-end, tour). | found: 2026-07-21, S130 Pass H
- [ ] P4 (informational — parked, throttled-only) | hebrew_blend_generator.html | **Class-Set chunked-build per-chunk variance at the extreme** (S128 Pass D). The S61 per-version chunking **resolved** the pre-S61 S44 monolithic longtask (1,305 ms @4×); typical builds now stream at <100 ms/chunk. Only the **absolute-extreme config** (blendCount 100 × Class-Set 40 × answer key = 80 sheets / 33,040 nodes) shows occasional per-chunk variance spikes to ~200–224 ms @4× (median still ~90–100 ms; @1× trivial). Reducing it further would mean splitting a version's worksheet+answer-key into separate ticks — a change to the strict Worksheet-Chunked-Build contract for a variance-only, throttled-only, extreme, user-initiated case. Parked (same category as the cold-load longtasks). | found: 2026-07-21, S128 Pass D
- [ ] P4 (informational — large-refactor-only) | torah_trainer.html | **Full-parsha `renderText` one-shot block** (S16 Pass D observation, now measured S66). The longest portions (~176 verses → ~2,300 `.tt-word` × ~4,600 `.nik` spans) rebuild in a single `innerHTML` pass: **~26–37 ms JS @1× / ~128–194 ms @4×**, plus a browser layout/paint longtask (~60–130 ms @1× / ~470–560 ms @4×). Fires **only on content change** (new reading / layout / translit·translation / nikkud·cantillation on-off) — **not** idle, and **not** on color/mode/scheme/dark tweaks (those are S60 var-swaps, re-verified 0-rebuild in S66). Only a chunked/streamed render (à la the generator's S61 Class-Set build) would improve it — not a small-diff win → parked, exactly like the generator Class-Set candidate was pre-S61. | found: 2026-07-06, S16 Pass D; measured 2026-07-11, S66 Pass D
- [x] ✅ CLEARED S101 | multiple tools | **Untranslated tooltip attributes — Check-B advisory backlog** (S81 Pass K; burned down over S88–S91 — dashboard 11 "Restore default" (S88), flash_cards nav (S88), **FM Undo/Redo ×4 (S91, 93a31eb)**, **torah settings-close ×2 (S91, 3fa7fbc)**). **Burned down through S99.** Cleared S99: hebrew_blend_generator (17 activity-panel input aria-labels + 2 reset titles via `worksheet.aria.*`/reused `reset_default`; transliteration placeholder `i18n-ignore` — 6f1c5dc), classroom_dashboard (Finish-editing title `dashboard.editor.finish_editing`; roster placeholder `i18n-ignore` — 9a4892a), pwa.js (redundant static Dismiss literal removed — already localized via `t('pwa.aria.dismiss')` — 04348bb). **Cleared S101 (f257019):** Hebrew_Font_Maker **7** static `aria-label`s (Help / Workspace step / Glyph category / Point-editing-tools / Editing-mode / 2 panel-resizers) → `data-i18n-aria-label` + 7 new `fontmaker.*_aria` keys — **the untranslated-tooltip-attribute advisory backlog is now empty (0) across the whole suite** (check-i18n warn-only section prints nothing). | found: 2026-07-14, S81 Pass K; gen/dash/pwa done S99; FM done S101
- [ ] P4 | Hebrew_Font_Maker.html | **Residual template-literal i18n blind-spot** (S101 Pass K). **S112 wired 11 orphaned `status()` toasts** (b112c0b — existing EN+HE keys, byte-identical EN). **S114 wired the 2 `assigned_letters` + `autoassigned_svg` sites** (c788498 — through the maintainer-authored `.one`/`.other` plural keys + the `box_others` fragment key, 0 new keys; only EN change is proper "letter(s)"→"letters"/"letter" pluralization — user approved the keep-or-improve call via AskUserQuestion). **Remaining (~6):** the two "Rounded … point" sites (text differs from the `smoothed_*` keys → need reconciled/new keys), the **guides-panel prose** (needs new keys), and — added S115 — the **3 pre-v5.5 `harfbuzzGate` failure messages** (vowel-slide, dotted-shin count, dotted-shin x=0; hardcoded template literals — the 5 NEW v5.5 gate probes are I18n'd, these older siblings are not). All need new/reconciled keys + Hebrew, so no longer a byte-identical clean wire. Size **S–M**. | found: 2026-07-19, S101 Pass K; sliced S112, S114; +3 gate sites S115
- [x] ✅ DONE S110, 1bace42 | THIRD_PARTY_LICENSES.md + resources.html + fonts/ | **`fonts/LakhishBold.ttf` license stated (SIL OFL 1.1)** (S99 Pass E). **FIXED S110 (1bace42, maintainer-confirmed: OFL 1.1, © 2026 Baruch Sienna, Reserved Font Name "Lakhish"):** created `fonts/LakhishBold-LICENSE.txt` (canonical OFL body + Lakhish header), wired the resources.html gallery entry to `license:'OFL'` + `licenseFile` + `madeWithIvritSuite:true` (so the download zips the real OFL text and the OFL filter includes it), and stated OFL 1.1 + the license-file ref on the THIRD_PARTY_LICENSES.md line (matching the TzviScript sibling). Verified via the produced download artifact (LakhishBold.zip = LakhishBold.ttf byte-identical + LICENSE.txt OFL w/ Lakhish header, CRC-valid). | found: 2026-07-19, S99 Pass E
- [x] ✅ DONE S102, 1a37213 | hebrew_dictionary.html | **Print button — dictionary is the lone laggard** (S100 Pass F). The dictionary ships the most elaborate `@media print` CSS but had **no print UI / no `window.print()`** — Ctrl+P only, unlike generator (`#printBtn`) + torah (`#ttPrintBtn`, S96). **FIXED S102 (1a37213):** a visible 🖨️ Print button in the results `.toolbar` (next to Copy link / Reset filters) on torah's `printReading()` canonical — a **click-time guard** (the dictionary has no visible toast, so an empty search routes through the existing SR-only `announce()` rather than a blank print dialog); the toolbar is in the `@media print` `display:none` list so it never leaks; 3 new `dictionary.toolbar.print_button`/`_title`/`_empty` keys. | found: 2026-07-19, S100 Pass F; done S102
- [x] ✅ DONE S102, 6e26a4d | generator + dictionary + torah | **Font-size control a11y — 3 laggards vs trope canonical** (S100 Pass F). trope_tutor's `#optFontSize` associates its `<label for>` + its reset `.sreset` carries `aria-label`+`data-i18n-aria-label`; the 3 laggards lacked an accessible name on the slider + `aria-label` on the ↺ reset. **FIXED S102 (6e26a4d):** associated each laggard's **existing visible label** with its slider (`<span class="fs-label">`→`<label for="hebFontSizeSlider">` on generator/dictionary; `for=` added to torah's 3 `<label>`s) so the accessible name == the visible text (WCAG 2.5.3) + `aria-label`+`data-i18n-aria-label` on each reset, reusing each tool's existing title key (annotated "aria-label only") → **0 new keys**. | found: 2026-07-19, S100 Pass F; done S102
- [x] ✅ DONE (card-face announce shipped in the S131 design-call batch c1591a8; language-aware voicing S132 4af7b78) | flash_cards.html | **a11y polish batch — tail** (S82 Pass C). **RESOLVED — card faces are now announced to screen readers via `announce()`/`#a11yStatus` (prompt on render, revealed answer on flip), with `lang="he"` set for Hebrew content so glyphs are voiced in a Hebrew voice rather than mis-voiced by an English SR (chose the suite's lang="he" convention over the deferred transliteration-only approach — no maintainer copy call needed). Verified headless: Hebrew face→lang="he", translit face→cleared.** FIXED so far: pill `aria-pressed` (S86, 88cab3a); results-score `aria-live` (S88, 52c60c8); profile+preset-delete ✕ `aria-label` + nav localization (S88, 0319d64); **`.tour-x` (26→30px, + coupled `.tour-card h4` clearance) + `.btn-xs` (~20→30px min) sub-30px touch targets → 30px min (S102 Pass C, 0e83432).** **Remaining (P4):** the **card faces** have no `aria-live` (announce the front/current card — a design call, could be noisy on flip). **S102 survey mapped the concrete path** (reuse the existing `announce()`/`#a11yStatus` — currently share-copy-only; announce card **position on render** + the **pronounceable transliteration on flip**, since the raw Hebrew glyphs would be mis-voiced by an English SR); deferred to the maintainer as a design call. | found: 2026-07-14, S82 Pass C; touch-targets done S102
- [x] 🚫 WON'T-FIX (maintainer decision, 2026-07-22 — accept as-is) | Hebrew_Font_Maker.html | **Non-dagesh per-letter shapes share the ccmp adjacency fragility** (S115, found during the shin-dagesh fix). **DECISION: documented known limitation — no reported symptom, and the full cure is a combinatorial pl-on-form glyph blowup; not worth the glyph-count cost without a real report. (The mechanical per-mark-filtered-lookup slice was explicitly NOT taken.)** The v5.5 `ccmp_dagesh` mark-filtered lookup fixed the dagesh class, but every `builtPerLetter` rule for a NON-dagesh mark (a per-letter *vowel* shape) still emits as a plain unfiltered `sub base mark by pl_…` — unmatchable when a canonically-earlier mark intervenes, AND unreachable when the base ligates first (a per-letter qamats on bet can never fire once בּ→FB31 — would need combinatorial pl-on-form glyphs). Same disease, wider cure: per-mark filtered lookups are mechanical, but the pl-on-form combinatorics are a design call (glyph-count blowup vs. accepting the global shape on dotted letters). No reported symptom. | found: 2026-07-20, S115
- [ ] P4 (pre-existing limitation, documented) | Hebrew_Font_Maker.html | **Kern pairs never cover precomposed targets** (S115 observation). Kern sides resolve stored letter cps via `gname` (`kernSide`), so a pair against ש/ב never applies to the ligated uniFB2A/uniFB31 — true before v5.5 (FB2A) and inherited unchanged by FB2C/FB2D. A fix would mirror base-letter kern classes onto their form glyphs at FEA-emit time. Cosmetic-spacing scope. | found: 2026-07-20, S115

## Feature seeds (micro-features only; see the Micro-feature track in the session prompt)

- [x] ✅ SHIPPED S126, 9a2a2c6 | trope_tutor.html | **Fullscreen "project" button** (S123 Pass F seed — the only projected tool without one). **SHIPPED S126 (9a2a2c6, micro-feature):** header `#fsBtn` icon-button (styled `.hbtn icon-btn`) using the dictionary single-button model — `toggleFullscreen()` + a `fullscreenchange` listener swapping glyph/title, reusing the `shared.fullscreen.*` keys (0 new keys), no persistent state, no CSP change; `header .hright` already print-hidden. 24-line diff; kept minimal (no `body.fullscreen` chrome-hiding — native fullscreen is the deliverable). Verified light/dark × desktop/800px, fresh empty-storage context, pageerror==0, EN↔HE. | found: 2026-07-20, S123 Pass F
- [x] ✅ SHIPPED S119, 6e75907 | classroom_dashboard.html | **Empty-state placeholder for the dashboard message surface** (S107 Pass H seed; user-directed micro-feature S119). **SHIPPED S119 (6e75907):** `#dashTextDisplay:empty:not(.editing)::before { content: attr(placeholder) }` renders a **"Click to add a message…"** hint (new key `dashboard.text.empty_placeholder`, EN+HE, via `data-i18n-placeholder`) whenever Dashboard Text is on but blank (e.g. right after a starter clears the intro text); the empty strip is also **click-to-edit** (mouse/touch; the ✎ pencil still covers keyboard/SR). Hidden **while editing** and **in fullscreen/projected view** (students never see an edit hint). Also collapses "effectively empty" content (a lone `<br>`/empty `<div>` from contenteditable) to `''` in `sanitizeDashHTML` + `commitEditorToDisplay` so the placeholder reliably reappears after a teacher clears the text — reset behavior unchanged. Verified headless (light/dark × desktop/800px, fresh profile): shows-when-empty / hides-with-content·editing·fullscreen / click-to-edit / clear→'' / lang→Hebrew / persistence round-trip; 0 pageerrors. ~25-line diff, dashboard-only. | found: 2026-07-19, S107 Pass H
- [x] ~~S | Hebrew_Font_Maker.html | **Standalone "My IvritSuite Fonts" opener**~~ — **SHIPPED S111, b006dbd** (a "★ My IvritSuite Fonts…" item added to the **Save Project ▾** menu → calls the existing `openMyFonts()`; the manager was previously reachable ONLY from the post-export success screen). +1 separator +1 button +1 i18n key `fontmaker.savemenu.my_fonts` (EN+HE); FM v5.1→5.2 + changelog; **no new state** (opens an existing window). Verified headless fresh-profile EN+HE × light/dark × 1280/800, pageerror==0. | found: 2026-07-18, S97 Pass H
- [x] ✅ SHIPPED S121, 1d43e48 | hebrew_dictionary.html + hebrew_blend_generator.html | **"Use in Worksheet Generator →" deep-link on saved Word Lists** — the second half of the S72 handshake. **SHIPPED S121 (1d43e48, micro-feature):** the Dictionary's Word List manager + save-success panel now offer a **"Use in Worksheet Generator →"** link (new key `dictionary.wordlist.use_in_generator`, EN+HE, RTL-mirrored arrow like its Flash Cards sibling) emitting `hebrew_blend_generator.html?wl=<id>`; the generator consumes `?wl=<id>` in `restoreFromURL` — validates the id against the shared `ivritSuite_wordLists` store (object-key/Set only, never interpolated), enables **Real Words → Lists** source, selects the list, and generates a worksheet. Invalid/absent ids degrade silently; a link only sets the live view (never touches saved presets). ~27-line diff, dictionary+generator only. Verified headless (light/dark × desktop/800px, fresh profile): consume → real-words+lists+worksheet (2 sheets/6 cards); invalid `?wl=` no-op; both buttons present with correct href in EN + HE; check-i18n clean; 0 pageerrors. | found: 2026-07-13, S72 (deferred half of the S68 seed)
- [x] ~~S | hebrew_dictionary.html | **Per-card "Add to Word List"**~~ — **SHIPPED S98, 1e1e9c3** (word-detail modal → "➕ Add to Word List…" → picker of existing lists → `wlMergeIntoList([wlWordFromDict(w)])`; shown only when `wlListCount() > 0`; 2 new localized keys). | found: 2026-07-11, S68 Pass H
- [ ] M — **GREENLIT 2026-07-22 (maintainer); a dedicated 2-phase project, NOT a loop iteration (Phase 1 is a `data/` corpus edit, fenced off from iterations)** | hebrew_dictionary.html | **Word theme/category filter** (body parts, food, colors…) for the *word* dictionary — thematic browsing exists only in Emoji mode today, so "build a body-parts set" forces Emoji mode or free-text guessing. Dual-audience-friendly (theme-based vocab is generic). **SCOPE (measured 2026-07-22):** `data/hebrew_words.json` = 13,081 entries / **6,810 nouns**, carrying `pos`+`era` but **no semantic theme tag**. **Phase 1 (gating data project):** offline-tag the nouns (+ colour adjectives) into a new optional `themes:[]` field via a ~12–16 kid-relevant taxonomy (body/food/animals/colors/clothing/home/family/nature/weather/time/school/feelings/places/holidays), LLM-assisted draft → **maintainer spot-check** → bump `hebrew_words.json ?v=`. **Phase 2 (micro-feature-sized):** a Theme filter panel mirroring the existing `posFilter` panel + emoji-category chips; predicate in `onFilter()` (keep words whose `themes` ∩ selection); serialize into `getDictState()`/`hebrewDictionary_lastState` + `?s=`; i18n theme names (EN+HE, dual-audience). | found: 2026-07-11, S68 Pass H; scoped + greenlit 2026-07-22
- [x] ✅ SHIPPED S129, 6ee266c | hebrew_dictionary.html | ~~**Reorder words within a saved list**~~ **SHIPPED S129 (6ee266c, micro-feature):** ↑/↓ buttons per word row in the Word List manager's "View words" expansion (disabled at ends), via a new `wlMoveWord(id, word, dir)` helper; reuses the `ivritSuite_wordLists` store (0 new persistence, 2 new i18n keys reusing established Hebrew); verified reorder + persistence + EN↔HE. **Remaining half (not done):** "surface Word Lists at top level" was already MITIGATED S103 (439bb47 top-level "📚 Word Lists" toolbar button). | found: 2026-07-11, S68 Pass H
- [ ] S–M | torah_trainer.html | **Student-handout mode** — a one-click "handout" preset producing a clean student worksheet: Hebrew-only (hide translation + translit), larger font, optionally verse-numbers-only / hide-nikkud for practice. Today a teacher can approximate it by toggling several controls, but there's no preset or discoverability. Pairs with the Print-button seed. | found: 2026-07-13, S77 Pass H

_(Struck seeds — S55, 2026-07-10: the maintainer **struck both remaining seeds** (flash_cards hands-free
auto-advance/loop mode; random student-picker/name spinner, both from S34 Pass H) by explicit
direction. Do NOT re-add them; future Pass H runs should not re-log these two ideas.)_

## In progress

_(none)_

## Done

- [x] 2026-07-22 | (S134 close-out) | branch/deploy note | S134 ran **Pass K** (i18n/localization audit, stalest since S124) — **Check A + B clean, 0 fixes**: the automated gate is green (14 baselined), and a JS-template-literal blind-spot probe over the recently-changed surface (the design-call batch + S131–133 + the merged frozen-footer generator work) found it fully localized — the frozen-footer `#genFab` button carries `data-i18n` + `data-i18n-title` + a guarded fallback; the one `.textContent='CARD'` hit is a `visibility:hidden`/`aria-hidden` print-alignment **spacer**, never visible (false positive). Broad re-sweep: **0 bare-English `alert`/`confirm` across all 6 non-FM tools** (S124 claim holds); dashboard TTS-drift surface has 0 bare-English chrome. The only i18n backlog left is the FM template-literal tooltips that need **new maintainer-authored Hebrew** — explicitly deferred in the design-call review, so no non-gated fix. **Iters 1/5.** **Drift:** none since S133 (main `e9234cc`; PR #123 unmerged). No sw bump / no code (ledger-only). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123. (4th consecutive clean pass — B/E/F/K.)
- [x] 2026-07-22 | (S133 close-out) | branch/deploy note | S133 ran **Pass F** (cross-tool consistency, stalest since S123) on the **share / shareable-config affordance** (first dedicated F sweep of it) — **CONSISTENT, 0 fixes**: generator + dictionary have **converged** `?s=` URL-share (identical pattern — `serialize`→`history.replaceState`→clipboard w/ execCommand fallback→button-flip-to-"copied"→`announce()` SR→1500 ms restore, both localized + keyboard-operable); flash_cards' paste-in share-code is the grandfathered coexistence the Shared-UX rule allows; FM shares a font file (a distinct affordance); dashboard/torah/trope carry **no** shareable-config state (device-local/content-driven) so no share affordance is appropriate — no laggard. `#shareNotice` confirmed live (auto-restore banner, L12379), not vestigial. Only a cosmetic icon/wording diff (📋 "Copy shareable link" vs 🔗 "Copy link") — a maintainer wording call, not a defect. **Iters 1/5**, stopped — no non-gated candidate (backlog all P4 gated; the one actionable-with-drafted-Hebrew item, FM tooltips, was explicitly deferred by the maintainer in the design-call review). **Drift:** none since S132 (main `e9234cc`; PR #123 unmerged). No sw bump / no code (ledger-only). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123.
- [x] 2026-07-22 | (S132 close-out) | branch/deploy note | S132 ran **Pass E** (freshness & site-health, stalest since S122) — **mechanical CLEAN**: `update-sitemap` no-op (all `<lastmod>` == git); SW `CORE_ASSETS` 25/25 resolve + all 12 production pages precached; **0 broken internal links** (12 pages scanned; the only regex hits were `data-rwsrc` attrs + JS template literals); `THIRD_PARTY_LICENSES.md` covers every CDN dep + all 5 bundled fonts (Frank Ruhl, Lakhish, Reuben, TzviScript, TzviScriptStrokeGuide, all used in resources.html); sitemap `<loc>` = 11 (all prod pages bar 404; i18n-test/404 correctly excluded); 3/3 manifest icons resolve. **1 fix (4af7b78):** completed the flash-cards a11y-tail candidate — `announce()` now sets `lang="he"` for Hebrew content so the card-face SR announcement is voiced in a Hebrew voice (verified headless: Hebrew face→`lang="he"`, translit→cleared, pageerror 0 light/dark × desktop/800px). **Iters:** 1 pass (E) + 1 fix = 2/5. **Drift:** none since S131 (main `e9234cc`; PR #123 unmerged). **SW v384→v385** (flash_cards.html precached). `check-i18n` clean (no new strings). **Branch:** directed `claude/improvement-loop-decisions-3p7c9z` / draft PR #123. Deploy verified after merge.
- [x] 2026-07-22 | (S131 close-out) | branch/deploy note | S131 ran **Pass B** (console & error audit, stalest since S121) — **11th run, comprehensively CLEAN**: 24 load runs (12 pages × light/dark) + one interaction per tool (index gear→modal, generator `generate()`+frozen-footer button, dashboard drawer, flash_cards Start→flip, dictionary search, torah settings, trope drill, FM `.ltile`→Alef) = **0 pageerror / 0 `[i18n] miss` / 0 failed-local / 0 non-noise console** on load AND after interaction, including the drift surface (merged frozen-Generate-footer generator work + the out-of-band FM/flash_cards design-call batch). **0 new candidates, 0 fixes** — backlog fully gated (all open candidates P4: needs-Hebrew / parked-perf / design-calls; both feature seeds blocked — dictionary theme = `data/` project, torah handout = Sefaria-unverifiable; flash_cards+FM at the variety cap from the same-session batch). **Drift check:** PR #122 (S126–S130) + the user-directed frozen-Generate-footer work **merged to `origin/main`** since S130 (main `e9234cc`; sw `v383`, FM `5.5` unchanged). No new `sw.js` bump this pass (ledger-only; the design-call batch already bumped v383→v384). **Branch:** runs on directed `claude/improvement-loop-decisions-3p7c9z` / draft **PR #123** (diverges from the merged loop branch `claude/improve-loop-5xuuei`/#122 — a task-directive branch, flagged). `check-i18n` clean. Deploy verified after merge.
- [x] 2026-07-22 | c1591a8 (+ sw v383→v384 in the ledger/sw commit) | Hebrew_Font_Maker.html + flash_cards.html (+ locales) | **Maintainer design-call batch (out-of-band — a directed review of gated candidates, NOT a numbered rotation session; no discovery pass, session counter untouched).** Resolved 3 gated a11y/copy candidates + accepted 1 WON'T-FIX: (1) FM edit-canvas accessible names (`labelStage` aria-label on `#stage`/`#meStage`/`#adStage`, no role change); (2) FC screen-reader card announce (wired the idle `#a11yStatus` region in `renderCurrentCard`/`flipCard`); (3) FC per-card results auto-expand on mistakes; (4) FC practice-button rename ("This Drill's Mistakes" / "Weak Cards", EN+HE); (5) FM ccmp non-dagesh adjacency → accept-as-is (documented limitation). Dictionary theme-filter seed **greenlit + scoped** (deferred — Phase-1 corpus tagging is a `data/` project). Verified headless: EN+HE, light+dark, desktop+800px, `pageerror==0`, both results paths; `check-i18n` clean, `build-locales` run. | branch `claude/improvement-loop-decisions-3p7c9z`
- [x] 2026-07-21 | 37125a9 | hebrew_blend_generator.html (+ sw.js) | **Frozen "Generate" footer button (user-directed — "Let's do that floating generate button on the bottom, frozen cell style"; resolves the S130 Pass-H P4).** The primary `#generateBtn` sat at the bottom of the ~2693px controls sidebar with no always-visible variant. Added a **`position: fixed`** "⬡ Generate" footer pinned to the viewport bottom-left (sidebar width, 320px) on desktop, so the primary action is always one click away; the sidebar gets `padding-bottom` so no content hides behind it. An **IntersectionObserver** hides the frozen footer whenever the inline `#generateBtn` is on screen (the two never show at once). Desktop-only (mobile keeps the existing `.mobile-fab` bar); reuses `worksheet.actions.generate_mob`/`.generate` keys (**0 new keys**); no persistent state (no preset wiring); opacity transition neutralized in the reduced-motion block. **Verified headless (light/dark × 1280/800, mobile 600, `pageerror==0`):** visible at page-top, clicking it builds a worksheet, hides when the inline Generate scrolls into view + reappears on scroll-up, `display:none` on mobile. `sw.js` v379→**v380**. **Landed on branch `claude/improve-loop-5xuuei` / PR #122.**
- [x] 2026-07-21 | (S130 close-out) | branch/deploy note | "Rerun" → S130 **continues on branch `claude/improve-loop-5xuuei`, adding to the still-open draft PR #122** (S126–S129; open + unmerged, verified via the self-check-ins → no branch restart, no new PR). **Drift check: no outside-loop change** since S129 — branch head @ `96eba05`, `sw.js` **v379**, `FONT_MAKER_VERSION` **5.5**; PR #122 open/mergeable, no CI checks, 0 review comments. **Pass run: H** (teacher walkthrough / paper-cuts — stalest since S120; **hebrew_blend_generator.html, its 2nd H [last S56, 74 sessions ago = the stalest-H tool]**). **Measured mature/well-onboarded, 0 clean fixes.** Role-played a teacher building a worksheet end-to-end (headless, fresh context, light/dark): excellent empty state (clear instruction + 5 one-click ready-made starters + tour), sane defaults (2-letter, 40 blends, Letter), logical top-to-bottom selector flow with Generate at the natural end, **live-preview auto-updates after the first generate** (verified blendCount 40→20 re-render, no click), Real-Words has a built-in word bank (no dead-end), `pageerror==0`. **1 minor friction logged (P4, design-call):** the first-generate `#generateBtn` sits at the bottom of the ~2693px sidebar with no sticky variant — well-mitigated (starters + instruction + live-preview + selectors-end-at-Generate), and any fix (sticky/floating or empty-state Generate button) is a maintainer UI-design choice → **log, don't force**. Budget **1 pass = 1 of 5**; **stopped early** — tool mature, and the rest of the fix-backlog is gated (design-calls / needs-new-Hebrew-keys / parked-perf / unverifiable-headless) with no clean variety-appropriate win. **`sw.js` NOT bumped (stays v379) — no precached file changed** (ledger-only session). **No `FONT_MAKER_VERSION` bump.** `check-i18n` clean (no UI strings); no `build-locales`; `update-sitemap` no-op. Deploy `success` verified after the maintainer merges PR #122.
- [x] 2026-07-21 | (S130 Pass H — teacher walkthrough / paper-cuts, one tool) | hebrew_blend_generator.html (stalest pass, last H S120 flash_cards; generator's 2nd H, last S56) | **Pass H — generator mature/well-onboarded; 0 clean fixes, 1 P4 design-call logged.** Role-played a fresh teacher building a worksheet end-to-end (headless over local HTTP, light 1280 / dark, fresh context). **Excellent onboarding:** "Ready to Generate" empty state with a clear instruction + **5 one-click ready-made starters** (Kamatz & Patach drill, Class Bingo, Alef-Bet, Fill-in-the-Vowels, Numbers 0–20) + tour link; sane defaults; logical top→bottom selector flow (Mode → Layout → Letters → Vowels → Generate at the natural end). **Live-preview confirmed:** after the first generate, every tweak auto-updates (blendCount 40→20 → 20 cells, no click); Real-Words mode has a built-in word bank (generated "Hebrew Word Practice" immediately — no saved-list dead-end); `pageerror==0`. **1 friction (P4, design-call, logged):** first-generate `#generateBtn` at the bottom of a ~2693px sidebar, no sticky variant — well-mitigated, any fix is a UI-design choice for the maintainer. Since the S56 first-H the generator gained the chunked build (S61), print work (S30–S34/S116), and the `?wl=` deep-link (S121); the UX held up excellently. All 7 tools now H'd ≥1×: flash_cards S34/S120, dashboard S46/S107, generator S56/S130, dictionary S68, torah S77, trope S87, FM S97.

_Older entries (S129 and earlier) archived to [`docs/IMPROVEMENT_ARCHIVE.md`](IMPROVEMENT_ARCHIVE.md)._

## Metrics

### Per-session log (one line per session)
- 2026-07-08 | S24 | iters: 1 | tools: 10 root pages (dark-mode toggle convergence) | patterns fixed: — | pass run: F | SW: v222→v223
- 2026-07-08 | S25 | iters: 2 | tools: font-maker, torah-trainer | patterns fixed: — | pass run: — (backlog) | SW: v223→v224
- 2026-07-08 | S26 | iters: 1 | tools: font-maker | patterns fixed: — | pass run: — (backlog) | SW: v224→v225
- 2026-07-08 | S27 | iters: 1 | tools: generator | patterns fixed: — | pass run: C | SW: v225→v226
- 2026-07-08 | S28 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v226→v227
- 2026-07-08 | S29 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v227→v228
- 2026-07-08 | S30 | iters: 2 (dark-print-bleed, page-break protection) + ledger v2 bootstrap | tools: generator | patterns fixed: — (Pass G is a discovery pass, not a recurring-pattern class) | pass run: G (FIRST RUN) | SW: v228→v229
- 2026-07-08 | S31 | iters: 2 (Letter/A4 page-size control, crossword/word-search block-break) | tools: generator | patterns fixed: — | pass run: G (human-directed continuation) | SW: v229→v230
- 2026-07-08 | S32 | iters: 2 (landscape window.print() wiring P2, landscape PDF for all types P3) | tools: generator | patterns fixed: — | pass run: G (human-directed, orientation focus) | SW: v230→v231
- 2026-07-08 | S33 | iters: 1 (discovery only — CLEAN, no fix) | tools: all 8 (audit, no edits) | patterns fixed: — | pass run: I (FIRST RUN, CLEAN) | SW: v231 (no bump)
- 2026-07-08 | S34 | iters: 2 (gold answer-ink print P3, dead `@page` reconcile P3) | tools: generator (fixes); flash cards (Pass H audit, no code) | patterns fixed: — (Pass G/H are discovery passes, not recurring-pattern classes) | pass run: H (FIRST RUN — teacher walkthrough; seeded the micro-feature track: 3 candidates + 5 seeds) | SW: v231→v232
- 2026-07-08 | S35 | iters: 2 (**custom card-count MICRO-FEATURE — the loop's first**, from an S34 Pass H seed) | tools: flash cards (feature); dashboard (Pass D audit, no code) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (2nd pass — dashboard, CLEAN: S14 per-second fix held; cold-load P4 re-measured) | SW: v232→v233 | note: reverted the S34 gender-default candidate (maintainer flagged intentional)
- 2026-07-08 | S36 | iters: 1 (clamp `cardCount` on restore — P3 robustness, hardens the S35 feature) | tools: flash cards | patterns fixed: Pass A recurring-pattern sweep of all 8 classes over the S23–S35 additions — **CLEAN, no P1/P2**; 1 adjacent robustness finding fixed (unclamped restore → tab-freeze). Retired undo-wiring + slider-commit (3 clean). | pass run: A (recurring-pattern sweep) | SW: v233→v234
- 2026-07-08 | S37 | iters: 1 (remove dead FM `.panel-title` collapse handler — P4, fresh-tool pick) | tools: font-maker | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, all 11 pages — **CLEAN**, 0 pageerrors; the only console errors were environmental `file://` data-fetch failures, gone over HTTP) | SW: v234→v235
- 2026-07-08 | S38 | iters: 3 (Pass E doc reconcile P4; rw-card clip **measured→closed** P3; inverted bingo spinner P4) | tools: generator (bingo fix; rw-card measured, no-code); docs (README + CLAUDE toast cross-ref) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health — mechanical **CLEAN**; 2 doc-drift P4 found + fixed) | SW: v235→v236
- 2026-07-08 | S39 | iters: 2 (Pass F toast convergence P4 — flash_cards + dashboard; transliteration wayfinding hint P3 — flash_cards) | tools: flash cards, dashboard | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency — non-blocking toast/success-feedback; converged the twin `alert('Imported successfully.')` on the portable `showAppToast`) | SW: v236→v237
- 2026-07-08 | S40 | iters: 4 (**P1 user-reported torah "esc is not a function" de-shadow** — d88fa99; dictionary SR announcements P3 — dd21b3c; dictionary focus-visible + RM carets P4 — 85f37ab; dictionary Pass C completeness P3+P4 — e6c0128, adversarial-verify follow-ups) | tools: torah-trainer (P1), dictionary (Pass C ×3 — deliberate cap exception, completing the shipped concerns incl. a Level-A P3) | patterns fixed: **new "shadowed-global helper" pattern registered + swept — 2 hits (both torah, fixed), rest of suite CLEAN** | pass run: C (accessibility — dictionary 2nd pass, strong shape; 5-agent adversarial verify + suite shadowing sweep) | SW: v237→v238
- 2026-07-09 | S41 | iters: 3 (Pass A sweep of new FM code — **CLEAN, no code**; torah error-copy P3 — 609f433; dictionary gold-contrast P3 — 8215f24) | tools: font-maker (read-only sweep), torah-trainer, dictionary | patterns fixed: **retired undo-wiring + slider-commit re-verified CLEAN over the never-swept FM v3.9–v4.2 code (auto-detect, node-editor, pen, fillet, waterfall)** — stayed dead | pass run: A (recurring-pattern sweep — user-directed deviation from strictly-stalest G, to cover the un-swept FM feature work) | SW: v241→v242 | note: branch started at v241 (FM v3.9–v4.2 landed outside the loop, v238→v241)
- 2026-07-09 | S42 | iters: 2 (**Pass G on FM's font-file export** — spec-legal export identity strings P1+P2, cd84087; version-bump-only-on-success P3, c796158) | tools: font-maker (2 iters — the 2/tool cap) | patterns fixed: — (Pass G is a discovery pass, not a recurring-pattern class) | pass run: G (print & export fidelity — **first-ever audit of the Font Maker export path**; TTF/WOFF2/zip/UFO; 2 fixed + 7 logged, 5 confirmed-safe) | SW: v242→v243 | FM: 4.2→4.3
- 2026-07-09 | S43 | iters: 1 (**FM LICENSE-delivery P2** — reliable one-click ⬇ LICENSE.txt button on the "Exported ✓" modal, replacing the blockable deferred 2nd download; also closes the P4 generic-filename collision, 2155ef1) | tools: font-maker | patterns fixed: — (backlog-fix session, no discovery pass) | pass run: — (backlog; user-directed) | SW: v243→v244 | FM: 4.3→4.4
- 2026-07-09 | S44 | iters: 1 (**duplicate real-word CSS consolidation** — P4 pure cleanup, pixel-identical light+dark, 51a2e97) | tools: generator | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance, generator 2nd pass — re-quantified the Class-Set 40 build: **13,840 nodes / 353 ms desktop / 1,305 ms @4×**, worse than the S13 estimate; idle CLEAN; no cheap win → parked P3 refactor confirmed) | SW: v244→v245
- 2026-07-09 | S45 | iters: 0 (discovery-only — Pass I **CLEAN**, no fix forced) | tools: all 11 root pages (read-only audit) | patterns fixed: — (Pass I is a discovery pass) | pass run: I (first-load & empty-state, 2nd run — static absent-key audit CLEAN; **22/22** fresh-context empty-storage loads clean (0 pageerrors, OS-aware dark, FM welcome shows); all 6 tour engines' static targets resolve incl. the reorganized FM tour, only the 3 documented content-dependent steps skip offline) | SW: v245 (no bump — no code)
- 2026-07-09 | S46 | iters: 1 fix (**FM silent Cu2Qu fallback P2** — aefe739, warn which letters shipped faceted) + 1 discovery pass | tools: font-maker (fix); dashboard (Pass H audit, no code) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — first-ever on the **dashboard**; strong new-teacher shape (starter card, quick-start, NY location default, empty states); 1 P3 paper-cut logged (empty schedule-row time)) | SW: v245→v246 | FM: 4.4→4.5 | note: new branch `claude/session-10-hqd501` on merged `origin/main` (PR #73 merged) → new draft PR
- 2026-07-09 | S47 | iters: 1 discovery pass (B) + 2 **MICRO-FEATURE** = 3 | tools: flash cards (Present/fullscreen feature — 507c543); all 11 pages (Pass B audit, no code) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, 3rd run — **CLEAN**, 20/22 page-runs clean; the 2 torah flags are the documented env local-`data/`/remote-Sefaria fetch noise, confirmed gone over HTTP) | SW: v246→v247 | note: loop's **2nd micro-feature** (from S34 Pass H seed); continues on draft PR #74
- 2026-07-09 | S48 | iters: 1 (Pass E doc-drift — 3 P4 fixes, e4c2d9c) | tools: docs only (README + CLAUDE — no tool code) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health — mechanical **CLEAN** (sitemap/SW-precache both directions/links/licenses/robots/CNAME/manifest + all CLAUDE.md shared-UX counts); 3 doc-drift fixed: README FM v3.8→v4.5, README flash-cards fullscreen bullet, CLAUDE FM ~10,350→~12,300) | SW: v247 (no bump — README/CLAUDE not precached) | note: docs-only (like S45); continues on draft PR #74
- 2026-07-09 | S49 | iters: 2 **MICRO-FEATURE** | tools: flash cards (English/Latin font picker — db0b112) | patterns fixed: — (no discovery pass — user-directed micro-feature focus) | pass run: — (skipped; **F stays stalest**) | SW: v247→v248 | note: loop's **3rd micro-feature** (from S34 Pass H seed); `--eng-font` picker, no new localStorage key; continues on draft PR #74
- 2026-07-10 | S50 | iters: 1 pass (F) + 4 fixes = 5 (budget) | tools: generator (Pass F convergence — 30e0251), font-maker ×2 (word-spacing floor dd5f84c + gate-skip warning 2c584e4 — the 2/tool cap), dashboard (schedule-time prefill 08c4b28) | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency — toast/status feedback across all **7** tools incl. the new trope tutor; a11y CLEAN, 1 laggard converged, `.ivrit`-engine restore-alert finding folded into the deferred extraction candidate) | SW: v251→v252 | FM: 4.6→4.7 | note: **v248→v251 + FM 4.5→4.6 landed outside the loop** (Trope Tutor + Torah-Trainer trope coloring — reconciled, like S41's FM note); PR #74 merged pre-session → new branch `claude/session-11-uh5dj0`, new draft PR
- 2026-07-10 | S51 | iters: 1 pass (C) + 4 fixes = 5 (budget) | tools: font-maker (partial-anchors backfill d209ab0, v4.8 — held from S50 for its dedicated iteration), trope-tutor ×2 (drawer tab-order P2 681e840 + drill focus P3 0fea475 — the 2/tool cap; first loop code on this tool), index (autosave-omission comment dece726 — coldest tool) | patterns fixed: — (Pass C is a discovery pass) | pass run: C (accessibility — **first-ever on the trope tutor**, full drill flow over HTTP; strong shape, 1 P2 + 3 P3 + P4 batch found, P2 + 1 P3 fixed, drawer-shell P2-verify logged for torah/dashboard) | SW: v252→v253 | FM: 4.7→4.8 | note: continues on draft PR #77
- 2026-07-10 | S52 | iters: 1 pass (A) + 3 fixes = 4 | tools: torah-trainer (drawer P2 port 7da3916), dashboard (drawer P2 port b0d8cce), font-maker (version-integrity P4 d343b1b, v4.9) | patterns fixed: **falsy-zero 1 hit** (FM `parseFloat ‖ 1.0` version promotion — fixed; streak resets to 0), other 7 active classes CLEAN over the biggest never-swept scope (full trope_tutor + torah trope-coloring + FM v4.3–4.8 + S50/S51 diffs); **workMode/step reachability RETIRED** (3rd consecutive clean sweep) | pass run: A (recurring-pattern sweep — cleanest scope yet; trope_tutor's documented safety patterns verified genuinely implemented, all 5 AllTools registration sites confirmed) | SW: v253→v254 | FM: 4.8→4.9 | note: continues on draft PR #77
- 2026-07-10 | S53 | iters: 1 pass (G) + 4 fixes = 5 (budget) | tools: trope-tutor ×2 (gold-contrast P3 42f1958 + touch-targets P3 8ea040f — the 2/tool cap), torah-trainer ×2 (print P2-tier 60277fe + print P3-tier 070e0e0 — the 2/tool cap) | patterns fixed: — (Pass G is a discovery pass) | pass run: G (print & export fidelity — **first-ever on the torah trainer**, the suite's most-printed tool; its whole print treatment was 3 lines; **3 P2 + 3 P3 found, all 6 fixed same-session**; 4 P4s logged; real Letter/A4 PDFs inspected) | SW: v254→v255 | note: continues on draft PR #77
- 2026-07-10 | S54 | iters: 1 pass (D) + 2 fixes = 3 | tools: 7 uploader carriers (suite-wide keyboard-operable font upload P3 — e292a34, single cross-tool commit per the S24 precedent), dictionary (collator P4 — fb38fda) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance — dictionary 2nd pass: effectively CLEAN, both S12 fixes held, idle 0/0, typing 0 longtasks; 1 P4 found + fixed same-session, measured 155→79 ms @4×) | SW: v255→v256 | note: continues on draft PR #77
- 2026-07-10 | S55 | iters: 1 pass (I) + 4 fixes = 5 (budget) | tools: trope-tutor (a11y polish batch 160c8fe), torah-trainer (print tail fb38cec), font-maker (UFO xHeight 27d1b82, v4.10), generator (gematria print blank 5523bc7) — 4 different tools | patterns fixed: — (Pass I is a discovery pass) | pass run: I (first-load & empty-state, 3rd run — **CLEAN**: 24/24 fresh-profile page-runs incl. the never-I'd trope tutor + 10 depth probes on the S50–S54 diffs) | SW: v256→v257 | FM: 4.9→4.10 | note: PR #77 merged pre-session → new branch `claude/session-12-dvb8xp`, new draft PR; **maintainer struck both flash_cards feature seeds** (micro-feature track skipped by direction)
- 2026-07-10 | S56 | iters: 1 pass (H) + 2 fixes = 3 | tools: generator (Pass H target + crossword RTL 9384202 — the 2/tool cap), 4 picker carriers (unnesting 845bb90 — generator/dictionary/torah/trope, single mechanical cross-tool commit) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — **first-ever on the generator; CLEAN**, one-click recipes + live preview + inline validation all verified; no paper-cuts, no seeds; struck-seeds rule honored) | SW: v257→v258 | note: continues on draft PR #78
- 2026-07-10 | S57 | iters: 1 pass (B) + 1 fix = 2 | tools: generator (contrast tail dd466e9) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, 4th run — **CLEAN**, 24/24 page-runs + interactions over the never-B'd S50–S56 surface; trope-tutor noise histogram = 100% route-aborted externals) | SW: v258→v259 | note: continues on draft PR #78
- 2026-07-10 | S58 | iters: 1 pass (E) + 1 doc-freshness batch = ~1 | tools: docs only (README + THIRD_PARTY_LICENSES + CLAUDE.md — no tool HTML) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health, 5th run — site-health **CLEAN**; doc-drift fixed incl. the missing Kaikki license entry + corrected `.ivrit`-engine byte-identity claim after re-sha) | SW: v261 (no bump — docs not precached; v259→v261 = 2 outside-loop trope commits) | note: new branch `claude/session-13-5-auf4mh` on merged `origin/main` (PR #78 merged) → new draft PR
- 2026-07-10 | S59 | iters: 1 refactor (showAppToast shared-block extraction + `.ivrit` restore-alert fold) | tools: generator, dashboard, flash cards (+ CLAUDE.md) | patterns fixed: — (no discovery pass — user-directed backlog refactor) | pass run: — (no pass; **F stays stalest**) | SW: v261→v262 | note: continues on draft PR #81; **re-unified the `.ivrit` engine byte-identity** broken since S39 (S58 finding); top open large-refactor candidate now DONE
- 2026-07-10 | S60 | iters: 1 refactor (torah nikkud coloring → CSS-var decouple from `renderText`) | tools: torah-trainer (+ CLAUDE.md) | patterns fixed: — (no discovery pass — user-directed backlog refactor) | pass run: — (no pass; **F stays stalest**) | SW: v262→v263 | note: continues on draft PR #81; closes the **S16 Pass D** torah full-render candidate (settings-driven rebuilds eliminated; content/text/layout changes still rebuild, by design)
- 2026-07-10 | S61 | iters: 1 refactor (generator Class-Set chunked build + flush contract) + 1 full-session regression sweep | tools: generator (+ CLAUDE.md) | patterns fixed: — (no discovery pass — user-directed backlog refactor) | pass run: — (no pass; **F stays stalest**) | SW: v263→v264 | note: continues on draft PR #81; closes the **S13 Pass D** generator candidate — **the last parked large refactor is now done**; regression sweep re-ran S59+S60 suites + shared-block shas + 24-page loads, all green
- 2026-07-10 | S62 | iters: 1 pass (F) + 1 fix = 2 | tools: font-maker (glyph-editor `alert()`→`askModal()` §5 convergence, v4.15) | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency, 4th run — surveyed error-surface/font-picker/empty-state affordances; converged FM's 3 error alerts onto its own `askModal`; font-picker+contrast deferred to a linked Pass C) | SW: v271→v272 | FM: 4.14→4.15 | note: new branch `claude/ivrit-improvement-loop-x8vpn4`; **outside-loop pre-session reconcile** SW v264→v271 + FM 4.10→4.14 (7 FM Firefox-box debug commits); new draft PR
- 2026-07-10 | S63 | iters: 1 pass (C) + 1 cross-tool fix = 2 | tools: generator, flash_cards, dashboard, dictionary, torah, trope (upload-pill WCAG contrast + trope pill structural convergence — single mechanical cross-tool commit) | patterns fixed: — (Pass C is a discovery pass) | pass run: C (accessibility — focused font-upload affordance audit across carriers; fixed the top S62 P3) | SW: v272→v273 | note: continues on draft PR #82; the `--gold-text` readable-gold token is now suite-wide (all 6 tool pages)
- 2026-07-10 | S64 | iters: 1 pass (A) + 1 fix = 2 | tools: font-maker (UFO version falsy-zero guard, b9c1aa3) | patterns fixed: **falsy-zero 1 hit** (FM `buildUfoFontInfo` `spec.version \|\|` → `Number.isFinite`, matching the S52 siblings); 6/7 classes CLEAN over the S53–S63 surface; **retired listener/interval + JSON-LD parity** (3-clean); kept 3 consequence-critical classes ACTIVE past 3-clean | pass run: A (recurring-pattern sweep — 3 agents, incl. the never-swept FM Firefox-box debug code, CLEAN) | SW: v273→v274 | note: continues on draft PR #82; no `FONT_MAKER_VERSION` bump (unreachable-today internal hardening, no user-facing change)
- 2026-07-10 | S65 | iters: 1 pass (G) + 1 fix = 2 | tools: dictionary (emoji selections now export to Anki/Quizlet/Word-List, 6082a7e) | patterns fixed: — (Pass G is a discovery pass) | pass run: G (print & export fidelity — **first-ever on the dictionary export surface**; word-export builders CLEAN, 1 emoji-export drop fixed, dark-print reset deferred P4) | SW: v274→v275 | note: continues on draft PR #82; all 3 export paths (Anki/Quizlet/Word-List) share `exportSelectedWords` so one fix covered them
- 2026-07-11 | S66 | iters: 1 pass (D) + 0 fixes | tools: torah-trainer (read-only profiling) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance — **2nd-pass on torah_trainer; effectively CLEAN**; empirically CONFIRMED the S60 nikkud CSS-var decouple's no-rebuild win held — color/mode/scheme/dark = 0 reading mutations / 0.1–2.7 ms vs renderText 354–356 mutations / 26–194 ms; idle CLEAN; worst-case one-shot render logged P4) | SW: v278 (no bump — docs-only, ledger not precached) | note: new branch `claude/new-session-4scgct` on merged `origin/main`; **outside-loop pre-session reconcile** SW v275→v278 + FM 4.15→4.17 (FM mark-editor moves + trope SEO + sitemap); new draft PR
- 2026-07-11 | S67 | iters: 1 pass (I) + 0 fixes | tools: all 12 root pages (read-only fresh-profile sweep) | patterns fixed: — (Pass I is a discovery pass) | pass run: I (first-load & empty-state, **4th run — CLEAN**; 24/24 fresh-context empty-storage page-runs 0 pageerrors, OS-aware dark + `lang="he"` + first-run affordances + no-h-scroll all pass; 3 depth-probes CLEAN incl. the outside-loop **FM mark-editor relocation** tile→modal on an empty project + **trope SEO** JSON-LD↔DOM parity) | SW: v278 (no bump — docs-only, ledger not precached) | note: continues on **draft PR #86** / `claude/new-session-4scgct` (reached via "Rerun" after S66)
- 2026-07-11 | S68 | iters: 1 pass (H) + 1 fix = 2 | tools: hebrew_dictionary (teacher walkthrough + shoresh-select fix, 0de2408) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — **first-ever on the dictionary**; "find words → save a Word List → carry to Flash Cards/Generator"; **1 silently-wrong correctness bug fixed** — shoresh "Select all matching" saved the stale Words-mode set instead of the root family; 3 P3 + 1 P4 friction candidates + **4 Feature seeds** logged, H = seed intake) | SW: v278→v279 (precached file changed: hebrew_dictionary.html — **first S66-series session to ship tool code**) | note: continues on **draft PR #86** / `claude/new-session-4scgct` (reached via "Rerun" after S67)
- 2026-07-11 | S69 | iters: 1 pass (B) + 0 fixes | tools: all 12 root pages (headless console/error audit) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, **5th run — CLEAN**; 24/24 page-runs, 0 pageerrors, 0 non-environmental console errors, 0 local request failures; interactions verified to fire incl. the S68 dictionary shoresh path; noise = 100% route-aborted externals) | SW: v279 (no bump — CLEAN audit, docs-only) | note: continues on **draft PR #86** / `claude/new-session-4scgct` (reached via "Rerun" after S68)
- 2026-07-11 | S70 | iters: 1 pass (E) + 1 doc-freshness batch | tools: docs only (README + CLAUDE.md — no tool HTML) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health, **6th run** — mechanical site-health **CLEAN**: SW CORE_ASSETS 22 both directions @v279, sitemap 11 locs all lastmod-current (update-sitemap no-op), licenses/robots/CNAME/manifest, shared app-toast block re-verified byte-identical; **2 P4 doc-drift fixed** (5bac85c): README FM v4.10→v4.17, CLAUDE FM line-count ~12,550→~12,650) | SW: v279 (no bump — README/CLAUDE not precached) | note: continues on **draft PR #86** / `claude/new-session-4scgct` (reached via "Rerun" after S69)
- 2026-07-11 | S71 | iters: 1 pass (F) + 1 cross-tool fix = 2 | tools: flash_cards + generator + dashboard (empty-state link a11y converge, 2e61fa3) | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency, **5th run** — empty states; converged the "No word lists yet" link onto the shared `--gold-text` token: flash_cards 2.6→6.22:1 light + underline, generator token-ized, dead dashboard `.preset-empty` CSS deleted; survey confirmed no other laggard → clears the S62/S68 empty-state debt) | SW: v279→v280 (precached files changed: flash_cards/generator/dashboard) | note: continues on **draft PR #86** / `claude/new-session-4scgct` (reached via "Rerun" after S70)
- 2026-07-11/12 | (outside-loop i18n rollout + Pass K — reconcile) | tools: all 12 pages | **full-site Hebrew RTL UI localization** (PR #87 pages 2–11 + Font Maker, PR #88 language-switcher → `<select>`) + **Pass K** i18n/localization audit (first run) — these are NOT S-numbered loop sessions; they are recorded in **Done** (the i18n-rollout entries) and the **Discovery-pass rotation** (K). `sw.js` **v280→v294**; `FONT_MAKER_VERSION` **4.17→4.18**; `check-i18n` gate + column-driven N-language locale pipeline added. (This line closes the drift between the S71-ending Per-session table and the 2026-07-12 Done/rotation work.)
- 2026-07-13 | S72 | iters: 1 pass (C) + 2 fixes + 1 micro-feature (2 iters) = 5 (budget) | tools: torah-trainer (Pass C G2 reduced-motion + G1 aria-label localize), hebrew_dictionary + flash_cards (deep-link micro-feature) | patterns fixed: — (Pass C is a discovery pass) | pass run: C (accessibility — **torah_trainer 2nd dedicated pass, last S18**; heavily hardened, no P2 blocker; 2 real fixes + G3/G4 logged) | SW: v294→v295 | note: new branch `claude/new-session-nv4854` on merged `origin/main` (PR #86 + i18n PRs #87/#88 merged); new draft PR; micro-feature pulled by user direction
- 2026-07-13 | S73 | iters: 1 pass (A) + 1 fix = 2 | tools: Hebrew_Font_Maker (P2 stored-XSS hardening, a87745e) | patterns fixed: **unescaped-input/unsafe-parse — 1 P2 (+1 P3) FIXED** (FM imported-project glyph/mark names → innerHTML, 10 name sinks + 3 precomp spans); other 4 active classes CLEAN; **shadowed-global RETIRED** (3-clean) | pass run: A (recurring-pattern sweep over the never-A-swept site-wide i18n rollout + Pass K + S68/S71/S72; 2 recon agents; the recon undercounted the sink set, the end-to-end exploit test caught 5 more) | SW: v295→v296 | note: continues on **draft PR #89** / `claude/new-session-nv4854` (reached via "Rerun" after S72)
- 2026-07-13 | S74 | iters: 1 pass (G) + 1 fix = 2 | tools: hebrew_blend_generator (self-check print-chrome hide, e57c06d) | patterns fixed: — (Pass G is a discovery pass) | pass run: G (print & export fidelity — **2nd-pass on the generator**, last S30–S32; substantially CLEAN over the S56/S61/i18n changes — dark-reset/break-protection/page-size/chunked-flush/i18n-boundary all held; **1 P3 fixed** (self-check chrome + dark-ink bleed printed), 2 P4 candidates logged) | SW: v296→v297 | note: continues on **draft PR #89** / `claude/new-session-nv4854` (reached via "Rerun" after S73)
- 2026-07-13 | S75 | iters: 1 pass (D) + 0 fixes | tools: flash_cards (read-only profiling) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance — **2nd-pass on flash_cards, first + only prior D was S15; measured effectively CLEAN** — no >200 ms interaction block, `pageerror==0` @1×+4×; the S15 `buildPool`×3 re-confirmed negligible (deck20 0.4/1.55 ms, deck100 ≤6.2 ms @4×), no regression; emoji/words/idle/present-mode all clean with numbers) | SW: v297 (no bump — read-only audit, docs-only) | note: continues on **draft PR #89** / `claude/new-session-nv4854` (reached via "Rerun" after S74)
- 2026-07-13 | S76 | iters: 1 pass (I) + 1 fix = 2 | tools: trope_tutor (first-load status i18n, 8845e7f) | patterns fixed: — (Pass I is a discovery pass) | pass run: I (first-load & empty-state, **5th run — first over the site-wide i18n rollout**; **48 fresh-profile runs CLEAN** — `pageerror==0`, no raw-key leaks, absent-key-safe, EN/`dir=ltr` default + switcher mounts, first-run affordances fire; **1 i18n leak fixed** — trope index-load status messages were hardcoded English → Hebrew UI showed English) | SW: v297→v298 (precached: trope_tutor.html) | note: **new harness-assigned branch `claude/new-session-s17scs`** (same tip as the prior nv4854) → **new draft PR**; the S72–S75 PR #89 left for maintainer reconcile
- 2026-07-13 | S77 | iters: 1 pass (H) + 2 fixes = 3 | tools: torah_trainer (Reading-dropdown label P3 + gearBtn-aria/audio-status-i18n P4, 8aa6691) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — **first-ever on torah_trainer**, un-H'd + lesson-shaped; effectively CLEAN flow, settings persist, `pageerror==0`; 1 P3 + 2 P4 fixed, 2 P3 + 2 P4 candidates + **2 Feature seeds** logged — H is seed intake) | SW: v298→v299 (precached: torah_trainer.html + locales) | note: continues on **draft PR #90** / `claude/new-session-s17scs` (reached via "Rerun" after S76)
- 2026-07-13 | S78 | iters: 1 pass (B) + 1 fix = 2 | tools: js/i18n.js shared runtime (suite-wide pre-ready i18n console-flood fix, 0f72e1d) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, **6th run — first over the i18n rollout**; `pageerror==0` on all 12 pages; the ONLY non-env output was a suite-wide pre-ready `[i18n] missing key` flood — 239 unique keys all present in en.json, pure timing — fixed in js/i18n.js, re-run → 0) | SW: v300→v301 (precached: js/i18n.js) | note: **maintainer authorized direct push to `main`** (option B) — S76–S78 + CTA land on main via FF; PRs #89/#90 closed
- 2026-07-14 | S79 | iters: 1 pass (E) + 2 doc-freshness = ~2 | tools: docs only (README + CLAUDE.md + THIRD_PARTY_LICENSES.md — no tool HTML) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health, **7th run** — mechanical site-health **CLEAN** over the S71→S78 + FM v4.18→v4.26 surface: SW CORE_ASSETS 25 both directions @v305 all resolve + every root HTML precached, sitemap 11 locs all `lastmod`-current (update-sitemap no-op), internal links + 18 splash hrefs, licenses vs deps; **2 doc-drift fixed** (2f89745 + c878a1a): README FM v4.17→v4.26, CLAUDE FM line-count ~12,650→~12,900, + THIRD_PARTY_LICENSES 5 undocumented runtime services (Hebcal / Open-Meteo CC BY / Nominatim ODbL / Web3Forms / hCaptcha)) | SW: v305 (no bump — README/CLAUDE/licenses not precached) | note: new branch `claude/new-session-vhfp4g` off updated `main` (`5666f91`); new draft PR; bonus off-rotation Pass A recon logged 3 low-signal candidates (no P1/P2)
- 2026-07-14 | S80 | iters: 1 pass (F) + 1 cross-tool fix = 2 | tools: hebrew_dictionary + trope_tutor + torah_trainer + classroom_dashboard (font-picker i18n convergence, single cross-tool commit d08f02a) | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency, **6th run** — Hebrew font-picker i18n, the S62-deferred affordance; 3 of 6 pickers hardcoded section headers + `[DEFAULT]` → converged onto flash_cards/generator reusing existing `shared.fonts.*` keys (no CSV/build-locales), torah upload label localized + `initFontSelector()` added to its `applyI18n`; 16/16 headless EN+HE clean) | SW: v305→v306 (precached: dashboard/dictionary/torah/trope) | note: continues on draft PR #94; logged 3 P3 candidates (gear aria-label, dashboard tour-pulse, share-copy announce)
- 2026-07-14 | S81 | iters: 1 pass (K) + 1 fix = 2 | tools: index.html (`.ivrit` save/restore UI i18n, 6d4d1a1) | patterns fixed: — (Pass K is a discovery pass) | pass run: K (i18n / localization audit, **2nd run** — Check A CLEAN; Check B 52-attr advisory backlog logged; a blind-spot probe found + fixed index's hardcoded-English `.ivrit` restore modal + status lines by reusing existing `shared.ivrit.*` keys, no CSV/build-locales; 4/4 headless EN+HE clean) | SW: v306→v307 (precached: index.html) | note: continues on draft PR #94; **index gets its first dedicated loop touch since S54** (was the coldest tool); logged the 52-attr backlog + "Sound" header + 122 REVIEW rows as candidates
- 2026-07-14 | S82 | iters: 1 pass (C) + 1 fix = 2 | tools: flash_cards.html (gold-on-light contrast → `--gold-text`, 9c10a65) | patterns fixed: — (Pass C is a discovery pass) | pass run: C (accessibility — **flash_cards, first dedicated a11y pass since S8**, the oldest of any tool; reduced-motion / `lang` / keyboard-operability all CLEAN; fixed gold-on-light contrast on the footer + 7 nav links + streak score + skip pill → 6.22:1 light, dark unchanged) | SW: v307→v308 (precached: flash_cards.html) | note: continues on draft PR #94; logged P2 overlay-modal a11y (top), generator contrast twin + translit CSV, pill-aria/live-regions/labels/touch-targets
- 2026-07-15 | S83 | iters: 1 pass (A) + 1 fix = 2 | tools: hebrew_blend_generator (footer/nav gold-on-light contrast → `--gold-text`, 449b587) | patterns fixed: Pass A recurring-pattern sweep of the 4 active classes over the never-A-swept **FM v4.18→v4.26 + S74–S82** surface — **CLEAN, 0 hits** (streaks: falsy-zero →2; localStorage →5, unescaped →1, destructive →5; no retirements) | pass run: A (recurring-pattern sweep, 2 agents; per the S41 precedent the clean sweep was paired with the S82-logged generator contrast twin) | SW: v308→v309 (precached: hebrew_blend_generator.html) | note: continues on draft PR #94; completes S82's footer/nav contrast convergence (generator = last laggard); translit-credit CSV still logged
- 2026-07-15 | S84 | iters: 1 pass (G) + 1 fix = 2 | tools: torah_trainer.html (2 dark-mode/highlight print-fidelity fixes, ce5ab3c) | patterns fixed: — (Pass G is a discovery pass) | pass run: G (print & export fidelity — **2nd-pass on torah_trainer**, the most-printed tool, last S53; flash_cards confirmed to have NO print/export surface; S53/S55 treatment holds; **2 P2 fixed** — dark-mode invisible reference header + silently-dropped nikkud Highlight coloring → per-vowel underline) | SW: v309→v310 (precached: torah_trainer.html) | note: continues on draft PR #94; logged the dark-palette-reprint P2 + print-legend P3
- 2026-07-16 | S85 | iters: 1 pass (D) + 4 fixes = 5 (full budget, user-directed) | tools: Hebrew_Font_Maker (Generate Bold warn-scan debounce 2f59215), classroom_dashboard (gear aria-label 2b11c02 + colorize esc af99b55), hebrew_dictionary (tour copy b711347 + colorize esc af99b55) | patterns fixed: — (Pass D is a discovery pass); the af99b55 fix is a cross-tool `esc()` **alignment** in the unescaped-input class (a hygiene fix, not a formal A-sweep — sink was latent/trusted) | pass run: D (performance — **first-ever dedicated D on Font Maker**; own-JS render pipeline + the new pure-JS Generate Bold; measured **no >200 ms in-scope block**, cold-init 438 ms @4× logged P4; **1 P3 drag-responsiveness fix** — redundant per-frame all-letters warning scan → debounced, 67→24 ms/frame @4×) | SW: v314→v315 (precached: FM + dashboard + dictionary + locales; **v310→v314 landed outside the loop** = 4 FM commits Generate Bold/Style-dropdown/deferred-restore) | note: new branch `claude/new-session-ienue4` on merged `origin/main` (PR #94 merged) → new draft PR; no `FONT_MAKER_VERSION` bump (FM change internal-only); variety: FM 1 + dashboard 2 + dictionary 2 (2/tool cap), 3 tools
- 2026-07-16 | S86 | iters: 1 pass (I) + 4 fixes = 5 (full budget, user-directed) | tools: classroom_dashboard (weather status i18n self-heal e458161), flash_cards (pill aria-pressed 88cab3a + dead empty-state removal 2dd3dd8), hebrew_blend_generator (PDF-export alert→notice 9640a11) | patterns fixed: — (Pass I is a discovery pass) | pass run: I (first-load & empty-state, **6th run**, first over the S77→S85 surface — 48 fresh-context runs (12 pages × light/dark × 1280/800) **`pageerror==0`**, EN/`dir=ltr` default + switcher + `lang="he"` + no h-scroll all pass; **1 fresh-user i18n leak found + fixed** — dashboard weather-unavailable raw key before I18n ready) | SW: v315→v316 (precached: dashboard + flash_cards + generator + locales) | note: "Rerun" → continues on branch `claude/new-session-ienue4` / **draft PR #97** (still open, not merged); no `FONT_MAKER_VERSION` bump; variety: dashboard 1 + flash_cards 2 + generator 1 (2/tool cap), 3 tools
- 2026-07-16 | S87 | iters: 1 pass (H) + 4 fixes = 5 (full budget, user-directed) | tools: trope_tutor (family-nav i18n leak d47e5ca), torah_trainer (resume-link contrast ee33666 + audio-collapse aria-expanded e8ccfb8), index (AllTools close-button aria-label 9fafa40) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — **first-ever H on trope_tutor**; Learn + Drill complete end-to-end, `pageerror==0`, audio-failure degradation verified never-dead-end; **effectively CLEAN, 1 P3 i18n leak fixed** — hardcoded-English family-nav button) | SW: v316→v317 (precached: trope + torah + index + locales) | note: "Rerun" → continues on branch `claude/new-session-ienue4` / **draft PR #97** (still open, not merged); no `FONT_MAKER_VERSION` bump; variety: trope 1 + torah 2 + index 1 (2/tool cap), 3 tools
- 2026-07-16 | S88 | iters: 1 pass (B) + 4 fixes = 5 (full budget, user-directed) | tools: classroom_dashboard ("Restore default" ×11 i18n 1661e93), flash_cards (results-score aria-live 52c60c8 + nav/delete accessible names 0319d64), hebrew_blend_generator (print flip-back relight 6b392d1) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, **7th run**, first over the S79→S87 surface — 24 page-runs (12 pages × light/dark) **`pageerror==0`, 0 local-request failures, 0 i18n-key warnings**; only non-abort output = torah's documented offline Sefaria/ESM-CDN artifacts; **CLEAN, no fix forced**) | SW: v317→v318 (precached: dashboard + flash_cards + generator + locales) | note: "Rerun" → continues on branch `claude/new-session-ienue4` / **draft PR #97** (still open, not merged); no `FONT_MAKER_VERSION` bump; variety: dashboard 1 + flash_cards 2 + generator 1 (2/tool cap), 3 tools
- 2026-07-16 | S89 | iters: 1 pass (E) + 4 fixes = 5 (full budget, user-directed) | tools: torah_trainer (dark-mode print palette 2e69d0f), flash_cards (overlay dialog semantics 98be114 + share announce ffa0e6e), classroom_dashboard (weather attribution 256a364), hebrew_blend_generator (share announce ffa0e6e) | patterns fixed: — (Pass E is a discovery/site-health pass) | pass run: E (freshness & site-health, **8th run** — CORE_ASSETS/sitemap/links/licenses CLEAN, no new CDN/API; reconciled the outside-loop PR #98 drift sw v318→v319 / FM 4.28→4.29; **2 doc-drift fixed** — README FM v4.26→v4.29, CLAUDE FM line-count) | SW: v319→v320 (precached: torah_trainer + flash_cards + dashboard + generator) | note: new branch `claude/new-session-2z7kfq` off merged `origin/main` (PR #97 merged + #98 merged) → new draft PR; no `FONT_MAKER_VERSION` bump; **2 P2s cleared** (torah print palette + flash_cards local-overlay dialog semantics; shared-engine confirm-modal split to a new cross-tool P2); variety: torah 1 + flash_cards 2 + dashboard 1 + generator 1 (2/tool cap), 4 tools; a11y class 2 (#24+#21)
- 2026-07-16 | S90 | iters: 1 pass (F) + 4 fixes = 5 (full budget, "Rerun 100") | tools: classroom_dashboard (Pass F tour-pulse 7053f7a + ivritAskMode 9fe5d43), hebrew_blend_generator + flash_cards (ivritAskMode 9fe5d43), torah_trainer (print trope legend db099e6), locales (translit-credit --gold-text c4020e0), hebrew_dictionary (a11yStatus role=status 458ef64) | patterns fixed: — (Pass F is a discovery/consistency pass) | pass run: F (cross-tool consistency, **7th run** — converged the dashboard tour-pulse onto the shared `.pulse-attn` + fallback ring; font-picker aria-pressed = enhancement-to-all deferred, share buttons already converged) | SW: v320→v321 (precached: dashboard + generator + flash_cards + torah + dictionary + locales) | note: continues on branch `claude/new-session-2z7kfq` / **draft PR #99** (still open, not merged); no `FONT_MAKER_VERSION` bump; **the last open P2 cleared** (cross-tool `.ivrit` confirm-modal dialog semantics, byte-identity preserved sha 7dc8e620); variety: dashboard 2 + gen 1 + flash_cards 1 + torah 1 + dictionary 1 (2/tool cap); a11y class 2 (ivritAskMode + dict role=status)
- 2026-07-16 | S91 | iters: 1 pass (K) + 4 fixes = 5 (full budget, "Rerun") | tools: generator+flash_cards ("Sound" header i18n 77e99fa + ftRead safe-parse 59b8733), hebrew_dictionary ("Sound" header 77e99fa), Hebrew_Font_Maker (Undo/Redo aria 93a31eb + "Editing:" header aa7b1c6), index+classroom_dashboard (ftRead safe-parse 59b8733), torah_trainer (settings-close i18n 3fa7fbc) | patterns fixed: — (Pass K is a discovery/i18n pass) | pass run: K (i18n / localization audit, **3rd run** — localized the visible "Sound" group-header leak across 3 tools; burned down FM Undo/Redo + torah close Check-B slices; logged the generator-24 / FM-prose backlog) | SW: v321→v322 (precached: generator + flash_cards + dictionary + FM + torah + index + dashboard + locales) | note: continues on branch `claude/new-session-2z7kfq` / **draft PR #99** (still open, not merged); no `FONT_MAKER_VERSION` bump; variety: gen 2 + fc 2 + FM 2 + dict 1 + index 1 + dashboard 1 + torah 1 (2/tool cap, 7 tools); i18n theme + 1 security fix (ftRead)
- 2026-07-16 | S92 | iters: 1 pass (C) + 4 fixes = 5 (full budget, "rerun the loop") | tools: resources.html (Pass C keyboard blocker 806d23a + lang/modal polish 6de9915), index.html (gear-modal dialog 4c59f00 + main/aria polish d94aa5e), + cross-page contrast convergence b00428f (index/resources/contact/privacy/404/torah_trainer/Hebrew_Font_Maker) | patterns fixed: — (Pass C is a discovery pass) | pass run: C (accessibility — **first-ever dedicated C on resources.html** + index, the two never-C'd chrome pages; 2 P2 keyboard/focus blockers fixed + a suite-wide gold-on-light contrast convergence, the S63 pattern) | SW: v322→v323 (precached: index + resources + contact + privacy + 404 + torah_trainer + Hebrew_Font_Maker) | note: continues on branch `claude/new-session-2z7kfq` / **draft PR #99** (still open, not merged); no `FONT_MAKER_VERSION` bump; a11y theme; variety: resources 2 + index 2 (2/tool cap) + 1 cross-tool convergence
- 2026-07-17 | S93 | iters: 1 pass (A) + 4 fixes = 5 (full budget, "/improveloop") | tools: torah_trainer (gold-on-light contrast 307849b + custom-range prefill ed47aba), hebrew_dictionary (wlSetStatus success contrast 2c670de), Hebrew_Font_Maker (#addebug scaffolding prune 707cd80) | patterns fixed: gold-on-light contrast ×2 (torah + dict, at the 2/pattern-class cap); Pass A itself found 0 hits | pass run: A (recurring-pattern sweep, stalest since S83 — swept the 4 active classes over the outside-loop dashboard movable-panels feature + S84–S92 + the 07-17 maintainer commits; CLEAN; **falsy-zero RETIRED** at its 3rd clean sweep, the 3 consequence-critical classes stay ACTIVE) | SW: v327→v328 (precached: torah_trainer + hebrew_dictionary + Hebrew_Font_Maker) | note: fresh branch `claude/improve-loop-7ugf93` off latest main (prior PR #99 merged) → new draft PR; no `FONT_MAKER_VERSION` bump (FM = dead-code prune); variety: torah 2 + dict 1 + FM 1 (2/tool cap), 3 tools
- 2026-07-18 | S94 | iters: 1 pass (G) + 4 fixes = 5 (full budget, "/improveloop") | tools: hebrew_dictionary (@media print block 8dc3505 + Quizlet CSV BOM cc09372), torah_trainer (this-week toolbar chip 7fd0ab3 + FAQ contrast f1cc90e) | patterns fixed: — (Pass G is a discovery pass; contrast ×1 torah + print/export fidelity ×2 dict) | pass run: G (print & export fidelity, **stalest since S84** — the 2nd/3rd-pass rotation lands on hebrew_dictionary, the stalest print tool (last G S65); found+fixed 2 in-pass — no `@media print` block + Quizlet CSV no-BOM; Anki tab/newline parity logged P4-informational, not reachable) | SW: v329→v330 (precached: hebrew_dictionary + torah_trainer + locales) | note: fresh branch `claude/improve-loop-y54736` off latest main (S93 direct-pushed to main) → new draft PR; outside-loop a22bfc0 FM "Save as a copy" v328→v329 / FM 4.29→4.30 (unswept — noted for next Pass A); no `FONT_MAKER_VERSION` bump; variety: dict 2 (pass-found, cap-exempt) + torah 2 (2/tool cap)
- 2026-07-18 | S95 | iters: 1 pass (D) + 3 fixes = 4 (stopped at 4 — no clean 5th under the variety governor) | tools: hebrew_dictionary (Load-as-filter banner 5112e65 + Anki TSV field sanitize df464f7), torah_trainer (tour reading-step hold adae250) | patterns fixed: — (Pass D is a discovery pass; P3 ×2 + P4 ×1) | pass run: D (performance, one tool, **stalest since S85** — trope_tutor.html, **first-ever dedicated D** (only never-D’d tool, coldest by loop-touch S87); effectively CLEAN — desktop @1× 0 longtasks, only throttled-only cold-init boot longtasks @4× (max 385 ms), all interactions <200 ms; logged 1 P4-informational parked candidate) | SW: v337→v338 (precached: hebrew_dictionary + torah_trainer + locales) | note: branch `claude/improve-loop-jjtbjk` off latest main (f89f7b0) → new draft PR (S94 branch merged); **outside-loop drift v330→v337 / FM 4.30→5.1** (FM Beta→v5.0→v5.1 + Resources fonts) unswept → next Pass A; no `FONT_MAKER_VERSION` bump; variety: dict 2 (cap) + torah 1 + trope-tutor pass (cap-exempt)
- 2026-07-18 | S96 | iters: 1 pass (I) + 2 fixes + 1 micro-feature (2 iters) = 5 (full budget) | tools: Hebrew_Font_Maker (mark-editor/auto-detect i18n 394d1ea), hebrew_blend_generator (Layout aria-labels ba640c0), torah_trainer (Print button micro-feature 48e0e42) | patterns fixed: — (Pass I is a discovery pass; P3 + P4 + micro-feature) | pass run: I (first-load & empty-state, **stalest since S86** — 7th run over the S87→S95 + outside-loop FM v5.1 / Resources-fonts surface; **effectively CLEAN** — 48 fresh-context runs `pageerror==0`, every empty state instructive, all demo paths produce output) | SW: v338→v339 (precached: FM + generator + torah + locales) | note: fresh branch `claude/improve-loop-h22l25` off latest main (3a4ceee) → new draft PR (S95 branch merged); no outside-loop app drift; no `FONT_MAKER_VERSION` bump (FM change = i18n wiring, not a feature); variety: FM 1 + gen 1 + torah 1 (2/tool cap), i18n class 2 (cap) + micro-feature 1
- 2026-07-18 | S97 | iters: 1 pass (H) + 1 fix = 2 (stopped early — no clean variety-appropriate win left) | tools: Hebrew_Font_Maker (Help-copy fix 2564beb) | patterns fixed: — (Pass H is a discovery pass; P3) | pass run: H (teacher walkthrough — **font-maker, first-ever H on the only never-H'd tool + freshest surface post-v5.1**; effectively CLEAN onboarding — welcome/next-step-hint/export-guard all sound; **1 P3 fixed** (stale Help copy → removed export-picker option) **+ 1 Feature seed** (standalone My-Fonts opener)) | SW: v339→v340 (precached: FM + locales) | note: continues on branch `claude/improve-loop-h22l25` / **draft PR #110** (S96, still open, not merged); no `FONT_MAKER_VERSION` bump (Help-copy correction, not a feature); variety: FM 1 (cap); H completes the 7-tool first-ever-H set
- 2026-07-19 | S98 | iters: 1 pass (B) + dup-guard + micro-feature (2 iters) + torah toast = 5 (full budget) | tools: hebrew_dictionary (dup-name guard a2c1159 + Add-to-Word-List micro-feature 1e1e9c3), torah_trainer (audio-unavailable toast deb2e28) | patterns fixed: — (Pass B is a discovery pass; P4 ×2 + 1 micro-feature) | pass run: B (console & error audit, **stalest since S88** — 12 pages × light/dark = 24 runs; **effectively CLEAN** — pageerror==0 / 0 failed-local / 0 i18n-miss; only torah's documented external offline artifacts) | SW: v340→v341 (precached: hebrew_dictionary + torah_trainer + locales) | note: fresh branch `claude/improve-loop-fhrbxq` off latest main (33a1d3c; S96+S97 direct-pushed) → new draft PR; **plan-mode interrupt** — interactive approval gate failed in this non-interactive run, fixes applied after plan-mode exit; no `FONT_MAKER_VERSION` bump (FM untouched); variety: dict 2 (cap — dup-guard + micro-feature) + torah 1
- 2026-07-19 | S99 | iters: 1 pass (E) + 3 Check-B i18n fixes = 4 (stopped at 4 — no clean caveat-free 5th on an untouched tool) | tools: hebrew_blend_generator (activity-panel aria-labels + reset titles 6f1c5dc), classroom_dashboard (Finish-editing title 9a4892a), pwa.js/chrome (Dismiss literal 04348bb) | patterns fixed: — (Pass E is a discovery pass; Check-B i18n ×3) | pass run: E (freshness & site-health, **stalest since S89** — mechanical audit CLEAN: CORE_ASSETS/precache/sitemap/links/splash/manifest/licenses/referenced-scripts all resolve; 1 tiny license nit logged) | SW: v341→v342 (precached: generator + dashboard + pwa.js + locales) | note: continues on branch `claude/improve-loop-fhrbxq` / **draft PR #111** (S98, still open, not merged); **plan-mode interrupt** (as S98) — approval gate failed, fixes applied after plan-mode exit; no `FONT_MAKER_VERSION` bump (FM untouched); variety: generator 1 + dashboard 1 + pwa/chrome 1 (all untouched-last-2, Check-B burndown)
- 2026-07-19 | S100 | iters: 1 pass (F) + 2 font-picker a11y convergences = 3 (stopped at 3 — dict/gen/dash/torah at the 2/tool cap) | tools: generator + dictionary + torah_trainer + dashboard + flash_cards + trope_tutor (aria-pressed 7c9b571; lang=he on 4 laggards 90c0e48) | patterns fixed: — (Pass F is a discovery pass; 2 a11y convergences) | pass run: F (cross-tool consistency, **stalest since S90** — font-picker a11y: aria-pressed on all 6 pickers + lang=he on 4 previews; surveyed via 2 Explore agents; Print-button→dictionary + font-size a11y logged for next F) | SW: v342→v343 (precached: all 6 picker HTML) | note: continues on branch `claude/improve-loop-fhrbxq` / **draft PR #111** (S98/S99, still open); **plan-mode interrupt** (as S98/S99); no `FONT_MAKER_VERSION` bump (FM has no `.font-opt` picker); attribute-only → no CSV/build-locales; variety: 6 pickers via 2 suite-wide convergences (dict/gen/dash/torah at cap 2, flash/trope 1)
- 2026-07-19 | S101 | iters: 1 pass (K) + 4 i18n fixes = 5 (full budget) | tools: Hebrew_Font_Maker (7 aria-labels f257019 + Contours panel 11e9880), classroom_dashboard (class-switcher b049741), hebrew_dictionary (emoji-card aria 20def99) | patterns fixed: — (Pass K is a discovery pass; tooltip-attribute advisory backlog cleared + 3 Check-A blind-spot slices) | pass run: K (i18n / localization audit, **stalest since S91** — Check-A clean; tooltip-attribute advisory backlog was 7 FM aria-labels → **now empty (0) suite-wide**; blind-spot probe found 3 clean template-literal slices, all fixed) | SW: v343→v344 (precached: FM + dashboard + dictionary + locales) | note: continues on branch `claude/improve-loop-fhrbxq` / **draft PR #111** (S98/S99/S100, still open); no `FONT_MAKER_VERSION` bump (i18n fixes, not a FM feature); 7 new `fontmaker.*_aria` keys + 3 reused orphaned keys (`contour_*` / `switch_class` / `aria_view_details`); variety: FM 2 (cap — aria + contours) + dashboard 1 + dictionary 1; **ExitPlanMode gate succeeded normally** (unlike S98–S100)
- 2026-07-19 | S102 | iters: 1 pass (C) + 3 a11y fixes = 4 | tools: generator + dictionary + torah (font-size a11y convergence 6e26a4d), flash_cards (touch targets 0e83432), hebrew_dictionary (Print button 1a37213) | patterns fixed: — (Pass C is a discovery pass; closes 2 logged S100 Pass-F P3s + the S82 touch-target tail) | pass run: C (accessibility, **stalest since S92** — form-control a11y: associate the 3 laggard font-size labels + reset aria [0 new keys], flash_cards sub-30px targets → 30px min, dictionary visible Print button; scoped via 2 Explore surveys; card-face aria-live deferred) | SW: v344→v345 (precached: generator + dictionary + torah + flash_cards + locales) | note: continues on branch `claude/improve-loop-fhrbxq` / **draft PR #111** (S98–S101, still open) → **maintainer-authorized direct push to `main`** ("Push to main after this"); 3 new `dictionary.toolbar.print_*` keys (font-size + touch-targets add 0); no `FONT_MAKER_VERSION` bump (FM untouched); variety: dictionary 2 (cap — font-size + print) + generator 1 + torah 1 + flash_cards 1; **ExitPlanMode gate failed again** (S98–S100 pattern; S101 was the exception)
- 2026-07-19 | S103 | iters: 1 pass (A) + 3 fixes | tools: hebrew_dictionary (Word Lists top-level toolbar button 439bb47, P3), classroom_dashboard (`resetPanelLayout` confirm 62be610, P4 — maintainer-directed) | patterns fixed: **Pass A CLEAN, 0 hits** — swept the 3 active consequence-critical classes (unescaped-input/unsafe-parse, localStorage-vs-AllTools, destructive-bulk) over the outside-loop FM v5.0→v5.1 + Resources font-gallery + S94–S102 surface (`a22bfc0..HEAD`); streaks 7/3/7, all KEPT ACTIVE | pass run: A (recurring-pattern sweep, stalest since S93) | SW: v345→v347
- 2026-07-19 | S104 | iters: 1 pass (G) + 1 fix = 2 (stopped early — backlog maintainer-flagged / other-pass (K) / parked-perf / balloon-risk) | tools: resources.html (font-download-zip DOS-timestamp fix d317e71, P4) | patterns fixed: — (Pass G is a discovery pass) | pass run: G (print & export fidelity — **first dedicated G on the resources font-download export**; all 4 fonts byte-accurate + zips CRC-valid + license delivery correct; flash_cards has no print surface → corrected in-rotation) | SW: v347→v348
- 2026-07-19 | S105 | iters: 1 pass (D) + 1 maintainer-directed fix | tools: hebrew_dictionary (Pass D profiled read-only; then Word-Lists styled inline rename + dup-name guard 59ed584) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance — **first dedicated D on the dictionary since S14**; 13,081-word corpus; effectively CLEAN — paginated+debounced, every real interaction <200 ms @1×; only a 517 ms cold-load boot longtask + throttled-only broad-sort @4×, both parked/inherent; sort-branch hoist empirically ruled out at 0 ms). **Pass D forced 0 fixes; the fix came from the maintainer picking the logged Word-Lists Rename balloon-risk item** (styled inline editor replacing the raw `prompt()` + blocks renaming onto another list's name; 2 new keys; no new state) | SW: v348→v349 (dictionary + locales precached)
- 2026-07-19 | S106 | iters: 1 pass (I) + 1 fix = 2 | tools: torah-trainer (localize 11 hardcoded status/karaoke messages via existing keys 8f26ea9) | patterns fixed: — (Pass I is a discovery pass; the fix is a Pass-K-class orphaned-key wiring) | pass run: I (first-load & empty-state, **stalest since S96** — 8th run over the S96→S105 surface; 48 fresh-context runs pageerror==0 + 8/8 targeted empty-state checks, all CLEAN; 0 new candidates) | SW: v349→v350 (torah_trainer precached)
- 2026-07-19 | S107 | iters: 1 pass (H) + 1 fix = 2 | tools: classroom_dashboard (two copy paper-cuts — misleading first-run skip link + ambiguous "Text Font"→"English Font" label db3221d) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough / paper-cuts, **stalest since S97** — 2nd dashboard H, first productive one [S46 was a no-code audit]; onboarding strong; fixed 2 copy labels + logged 3 candidates + 1 seed) | SW: v350→v351 (classroom_dashboard precached)
- 2026-07-19 | S108 | iters: 1 pass (B) + 0 fixes = 1 (discovery-only — no clean variety-favorable fix; backlog worked down to maintainer-judgment/parked-perf) | tools: — (Pass B is a read-only console audit; no file changed) | patterns fixed: — | pass run: B (console & error audit, **stalest since S98** — 24 runs load+interaction, CLEAN: 0 pageerror / 0 [i18n] miss / 0 failed-local / 0 non-noise console; unfiltered capture = only expected external-abort noise) | SW: v351 (no bump — no precached file changed)
- 2026-07-19 | S109 | iters: 1 pass (E) + 1 maintainer-directed fix = 2 | tools: classroom_dashboard (accurate 'Hebrew classroom' starter copy + new-teacher schedule preset hint 74c8f76) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health, **stalest since S99** — mechanical CLEAN: sitemap no-op, CORE_ASSETS 27/27 resolve, 0 broken internal links [Node scan 344 refs], 5 fonts licensed; then the maintainer picked dashboard copy polish, closing 2 S107 Pass-H candidates) | SW: v351→v352 (classroom_dashboard precached)
- 2026-07-19 | S110 | iters: 1 maintainer-directed fix (pass skipped) = 1 | tools: resources.html + THIRD_PARTY_LICENSES.md + fonts/ (Lakhish Bold OFL 1.1 license stated end-to-end 1bace42) | patterns fixed: — | pass run: — (skipped; the maintainer supplied the Lakhish OFL license → directed fix; **Pass F stays stalest**) | SW: v352→v353 (resources.html precached)
- 2026-07-19 | S111 | iters: 1 pass (F) + 2 micro-feature = 3 (budget 5; stopped early — remaining backlog all maintainer-design-calls, AskUserQuestion undeliverable/non-interactive) | tools: font-maker ("My IvritSuite Fonts" opener b006dbd, v5.2) | patterns fixed: — (Pass F is discovery; toast affordance consistent, adoption = standing large item) | pass run: F | SW: v353→v354 | FM: 5.1→5.2
- 2026-07-19 | S112 | iters: 1 pass (K) + 1 fix = 2 (budget 5; stopped early — remaining backlog maintainer-design-calls + parked; avoided over-concentrating on FM after S111) | tools: font-maker (11 orphaned status() toasts localized via existing keys b112c0b) | patterns fixed: template-literal-i18n blind-spot (11 sites, byte-identical EN) | pass run: K | SW: v354→v355 | FM: 5.2 (no bump — pure i18n wiring)
- 2026-07-19 | S113 | iters: 1 pass (C) + 1 fix = 2 (budget 5; stopped early — tool a11y-clean, remaining backlog maintainer-design-calls) | tools: trope-tutor (complete prefers-reduced-motion coverage d04b45c) | patterns fixed: reduced-motion completeness (2 selectors) | pass run: C | SW: v355→v356 | FM: 5.2 (no bump)
- 2026-07-20 | S114 | iters: 1 pass (A) + 1 fix = 2 (budget 5; stopped early — Pass A CLEAN, remaining backlog all maintainer-design-calls; user directed the one actionable fix via AskUserQuestion) | tools: font-maker (2 template-literal status() toasts localized via existing plural keys c788498) | patterns fixed: template-literal-i18n (2 sites, Pass-K-class) | pass run: A | SW: v358→v359 | FM: 5.4 (no bump)
- 2026-07-20 | S115 | iters: 1 maintainer-directed release (pass skipped; full plan-mode investigation: 2 explorations + design + maintainer approval) | tools: font-maker (v5.5 — shin custom-dagesh export via FB2C/FB2D + ccmp_dagesh filtered lookup + lookupflag-leak reset + shaping gate resurrected, d05d5f1) | patterns fixed: — (3-bug export chain discovered + fixed; 2 new candidates logged) | pass run: — (skipped; G stays stalest) | SW: v359→v360 | FM: 5.4→5.5 (release rule)
- 2026-07-20 | S116 | iters: 1 pass (G) + 1 fix = 2 | tools: hebrew_blend_generator (dark-print answer-key banner → light/PDF navy 07b6549) | patterns fixed: — (Pass G is a discovery pass; cross-tool print-reset sweep clean) | pass run: G (print & export fidelity — generator, substantially clean) | SW: v360→v361
- 2026-07-20 | S117 | iters: 1 pass (D) + 1 fix = 2 (budget 5; stopped early — dashboard thoroughly clean, remaining backlog all maintainer-design-calls + parked-perf; the one clean burndown source (template-literal i18n) is FM-concentrated and needs new Hebrew keys) | tools: classroom_dashboard (Now/Next per-second innerHTML rebuild guard b5d4261) | patterns fixed: — (Pass D is a discovery pass; retired listener/interval-accum re-confirmed dead under the perf lens — DOM/interval Δ0 over 40 cycles) | pass run: D (performance — classroom_dashboard, 3rd dedicated D, thoroughly clean) | SW: v362→v363
- 2026-07-20 | S118 | iters: 1 pass (I) + 1 fix = 2 (budget 5; stopped early — user directed "Dashboard fix only") | tools: classroom_dashboard (ship power-user settings panels Presets+Schedule Sync collapsed 969e293, user-directed) | patterns fixed: — (Pass I is a discovery pass; comprehensively CLEAN, 24 fresh-context runs 0 pageerror/0 i18n-miss/0 failed-local + 7-tool empty-state probes) | pass run: I (first-load & empty-state — 9th run, clean); AskUserQuestion resolved 3 design-calls (1 fix + 2 WON'T-FIX: flash_cards panels stay collapsed, torah Diaspora/Israel stays buried) | SW: v363→v364
- 2026-07-20 | S119 | iters: 1 micro-feature = 2 (budget 5; user-directed single deliverable) | tools: classroom_dashboard (empty-state placeholder for the message surface 6e75907 — S107 Pass-H seed) | patterns fixed: — (micro-feature, not a pattern) | pass run: — (SKIPPED; user-directed micro-feature → Pass H stays stalest) | SW: v364→v365
- 2026-07-20 | S120 | iters: 1 pass (H) + 1 fix = 2 (budget 5; stopped early — flash_cards mature/well-built, remaining findings copy/design-calls or minor) | tools: flash_cards (emoji-data-still-loading Start no longer errors — waits + retries 203a0b9) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — flash_cards, stalest H target [last H S34]; non-dashboard per the S119 variety note; core flow solid, 1 P3 paper-cut fixed + candidates logged) | SW: v365→v366
- 2026-07-20 | S121 | iters: 1 pass (B) + 1 micro-feature = 3 (budget 5; micro-feature = 2 iters; stopped — the session's 1 allowed micro-feature shipped, backlog otherwise design-gated) | tools: hebrew_dictionary + hebrew_blend_generator (Dictionary→Generator ?wl= deep-link 1d43e48 — completes the S72 handshake) | patterns fixed: — (Pass B is a discovery pass; CLEAN 10th run) | pass run: B (console & error audit — site-wide, 24 runs + 7 interactions, 0 errors) | SW: v366→v367
- 2026-07-20 | S122 | iters: 1 pass (E) + 1 fix = 2 (budget 5; stopped early — site-health clean, variety-favorable fix-backlog design-gated/parked) | tools: README.md (doc-accuracy: FM v4.29→5.5 + add Lakhish Bold to fonts list 717c53b) | patterns fixed: — (Pass E is a discovery pass; the README FM-version drift is the recurring Pass-E doc-drift class, last bumped S89) | pass run: E (freshness & site health — 11th run, mechanical CLEAN) | SW: v367 (NO bump — no precached file changed; README + ledger only)
- 2026-07-20 | S123 | iters: 1 pass (F) + 3 fixes = 4 (budget 5; stopped early — 2/session i18n pattern cap reached, no clean non-i18n candidate left) | tools: generator + dictionary (fullscreen title i18n d57108a), flash_cards (sole-word-list default-select 8225e07), torah_trainer (scope holiday tooltip i18n faedfb4) | patterns fixed: hardcoded-English-in-JS `.title` (the Check-A blind spot) ×2 sites-classes (fullscreen gen/dict + torah scope) | pass run: F (cross-tool consistency — fullscreen-toggle affordance; dark-mode spot-checked CONSISTENT) | SW: v367→v368
- 2026-07-20 | S124 | iters: 1 pass (K) + 2 fixes = 3 (budget 5; stopped early — remaining K work is the FM 24-tooltip backlog, best as its own focused effort) | tools: hebrew_dictionary (emoji-card gender forms + speak button i18n 5dd4f94), classroom_dashboard (student-picker chip tooltips i18n 9dc2662) | patterns fixed: hardcoded-English-in-JS template-literal tooltips (Check-A blind spot) — dict emoji-card + dash picker (both reused existing/orphaned keys; +2 new dict keys) | pass run: K (i18n/localization audit, 6th run — Check A+B clean; blind-spot sweep → suite excellent, FM backlog mapped) | SW: v368→v369
- 2026-07-20 | S125 | iters: 1 pass (C) + 2 fixes = 3 (budget 5; stopped early — FM audited a11y-strong, only the slider-label gap was a clean fix; the stage finding is a design-call, Pass C is one-tool) | tools: Hebrew_Font_Maker ×2 (all range sliders get accessible names — vSlider helper + #zoomRange f5d4080; spacing/kern sliders 18d9a5d) | patterns fixed: nameless-slider (unassociated label spans) — systemic via the shared vSlider helper + spacing/kern .inline divs; reused existing label keys (0 new keys) | pass run: C (accessibility, one tool — **first-ever dedicated C on FM**; 247 visible controls, only 3 nameless, all sliders) | SW: v369→v370
- 2026-07-21 | S126 | iters: 1 pass (A) + 1 fix + 1 micro-feature (2 iters) = 4 (budget 5; stopped — 1 micro-feature quota used, remaining fix-backlog design-gated/needs-Hebrew) | tools: Hebrew_Font_Maker (9 template-literal tooltips localized 1984167), trope_tutor (fullscreen "project" button micro-feature 9a2a2c6) | patterns fixed: hardcoded-English-in-JS template-literal tooltips (Check-A blind spot) — 9 orphaned-key sites wired | pass run: A (recurring-pattern sweep, 3 consequence-critical classes — stalest since S114; CLEAN 0 hits over fd0fd11..HEAD incl. outside-loop Advanced-TTS-added-then-removed/Basic-TTS drift) | SW: v376→v377
- 2026-07-21 | S127 | iters: 1 pass (G) + 2 fixes = 3 (budget 5; stopped — all 6 Pass-G findings addressed, remaining cross-tool backlog design-gated) | tools: hebrew_dictionary ×2 (print pagination completeness 912d1ea; print @media chrome-leaks + bg-off/dark legibility 9c8b0f3) | patterns fixed: print-fidelity (silently-truncated printout P2 + 5 print-CSS defects P3/P4) | pass run: G (print & export fidelity, one tool — stalest since S116; ran as a 6-config adversarially-verified workflow on the dictionary; torah_trainer G deferred — Sefaria unreachable headless; flash_cards has no print surface) | SW: v377→v378
- 2026-07-21 | S128 | iters: 1 pass (D) = 1 (budget 5; stopped early — build measured clean and the fix-candidate backlog is fully gated: design-calls / needs-Hebrew / parked-perf / maintainer-preference / unverifiable-headless; no clean non-dictionary win, and a dictionary micro-feature would be a 3rd consecutive dictionary touch vs the variety governor) | tools: hebrew_blend_generator (Pass D measure-only, 0 code) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (performance, one tool — stalest since S117; generator's 3rd D [last S44]; the S44 Class-Set monolithic longtask CONFIRMED resolved by the S61 chunked build under the perf lens; 0 fixes) | SW: v378 (no bump — ledger-only session)
- 2026-07-21 | S129 | iters: 1 pass (I) + 1 micro-feature (2 iters) = 3 (budget 5; stopped — rest of the fix-backlog gated, this was the one clean/verifiable actionable win) | tools: hebrew_dictionary (Word-List reorder micro-feature 6ee266c) | patterns fixed: — (Pass I is a discovery pass; micro-feature) | pass run: I (first-load & empty-state — stalest since S118; 10th run, comprehensively CLEAN: 24 fresh-context runs 0 pageerror/0 i18n-miss; torah Sefaria-unreachable gracefully handled with a red error banner) | SW: v378→v379
- 2026-07-21 | S130 | iters: 1 pass (H) = 1 (budget 5; stopped early — generator mature/well-onboarded, 0 clean fixes; rest of the fix-backlog gated, no clean variety-appropriate win) | tools: hebrew_blend_generator (Pass H walkthrough, 0 code) | patterns fixed: — (Pass H is a discovery pass) | pass run: H (teacher walkthrough — stalest since S120; generator's 2nd H [last S56], the stalest-H tool; excellent onboarding (starters + live-preview + Real-Words word bank), 1 P4 first-generate-reachability design-call logged) | SW: v379 (no bump — ledger-only session)
- 2026-07-22 | S131 | iters: 1 pass (B) = 1 (budget 5; stopped — Pass B fully CLEAN and the fix-backlog is comprehensively gated: all open candidates P4 [needs-Hebrew / parked-perf / design-calls], both feature seeds blocked [dictionary theme = `data/` project; torah handout = Sefaria-unverifiable], flash_cards+FM at the variety cap from the same-session out-of-band design-call batch) | tools: none (audit only; the out-of-band design-call batch separately touched FM+flash_cards, c1591a8) | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit — stalest since S121; 11th run, CLEAN incl. drift surface) | SW: v384 (no bump this pass — the design-call batch already bumped v383→v384)
- 2026-07-22 | S132 | iters: 1 pass (E) + 1 fix = 2 (budget 5; stopped — Pass E mechanically CLEAN; fixed the one clean, non-gated, variety-appropriate candidate [flash-cards SR-announce lang-awareness], rest of backlog gated) | tools: flash_cards (announce() sets lang="he" for correct Hebrew SR voicing, 4af7b78) | patterns fixed: — (a11y refinement) | pass run: E (freshness & site-health — stalest since S122; sitemap no-op, CORE_ASSETS 25/25 resolve + all pages precached, 0 broken links, licenses complete, manifest/sitemap coverage OK) | SW: v384→v385
- 2026-07-22 | S133 | iters: 1 pass (F) = 1 (budget 5; stopped — share affordance CONSISTENT, 0 laggard; no non-gated candidate [backlog all P4 gated, FM tooltips maintainer-deferred]) | tools: none (audit only) | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency — share/shareable-config affordance; gen+dict converged on one ?s= pattern, flash_cards paste-code grandfathered, FM font-share, dash/torah/trope no shareable state = appropriate) | SW: v385 (no bump — ledger-only)
- 2026-07-22 | S134 | iters: 1 pass (K) = 1 (budget 5; stopped — Check A+B clean, recently-changed surface fully localized, 0 bare-English in the 6 non-FM tools; the only i18n backlog left needs new maintainer-authored Hebrew [FM tooltips, deferred]) | tools: none (audit only) | patterns fixed: — (Pass K is a discovery pass) | pass run: K (i18n/localization — Check A+B clean; template-literal blind-spot probe on the design-batch + S131–133 + frozen-footer surface = clean; 'CARD' hit is a hidden print spacer) | SW: v385 (no bump — ledger-only)

### Tool coverage (last-touched date per tool)
_(Every page was touched 2026-07-12 by the site-wide i18n rollout; dates below fold that in. "Loop-last" = last dedicated loop iteration.)_
- torah-trainer: **2026-07-20 (S123 — Pass F recurring-shape sweep: scope holiday tooltip i18n faedfb4, new key)**; loop-prior S106 localize 11 status/karaoke messages 8f26ea9 + S102 font-size a11y; i18n page 4 (07-12) | dictionary: **2026-07-21 (S129 — Word-List reorder micro-feature 6ee266c)**; loop-prior S127 Pass-G print fidelity (pagination 912d1ea + @media chrome-leaks/legibility 9c8b0f3) + S124 Pass-K emoji-card i18n 5dd4f94 + S123 Pass-F fullscreen title i18n d57108a + S121 ?wl= deep-link emit 1d43e48; i18n page 2 (07-11) | flashcards: **2026-07-22 (S132 — SR-announce `lang="he"` voicing 4af7b78; + same-day out-of-band design-call batch: SR card announce + results auto-expand + practice-button rename c1591a8)**; loop-prior S123 Words-mode sole-word-list default-select 8225e07; loop-prior S120 emoji-data-loading Start waits+retries 203a0b9 + S102 touch targets 0e83432; i18n page (07-12) | generator/worksheets: **2026-07-21 (S130 — Pass H teacher walkthrough, 2nd H [S56/S130]: mature/well-onboarded, 0 fixes, 1 P4 design-call; measure-only)**; loop-prior S128 Pass D perf 3rd D [S13/S44/S128]: worksheet build measured CLEAN, S61 chunking confirmed to have resolved the S44 Class-Set monolithic longtask; measure-only, 0 code)**; loop-last-code S123 Pass-F fullscreen title i18n d57108a + S121 Dictionary→Generator ?wl= consume 1d43e48 + S116 Pass-G dark-print banner 07b6549; i18n page 3 (07-12) | font-maker: **2026-07-22 (out-of-band design-call batch — glyph-edit canvas aria-labels, c1591a8)**; loop-prior S126 Pass A 9 template-literal tooltips localized 1984167; loop-prior S125 Pass-C a11y all range sliders get accessible names f5d4080/18d9a5d + S115 v5.5 export-chain release d05d5f1 (shin custom-dagesh FB2C/FB2D + ccmp_dagesh + shaping gate) + S114 2 status() toasts c788498; loop-prior S114 2 status() toasts c788498 + S112 11 status() toasts b112c0b + S111 "My Fonts" opener b006dbd; i18n page 7 (07-12) + S101 Pass K (f257019 + 11e9880) + S97 Pass H + Help-copy fix 2564beb + S96 mark-editor/auto-detect i18n | dashboard: **2026-07-20 (S124 — Pass K: student-picker chip tooltips i18n 9dc2662)**; loop-prior S119 empty-state placeholder 6e75907 + S118 power-user panels collapsed 969e293; i18n page 6 (07-12) | trope-tutor: **2026-07-21 (S126 — fullscreen "project" button micro-feature 9a2a2c6, S123 Pass-F seed)**; loop-prior S113 Pass-C a11y complete reduced-motion coverage d04b45c (tool a11y-strong); i18n page 5 (07-12); loop-prior S100 font-picker aria-pressed + S95 Pass D + S87 Pass H | index/chrome: **2026-07-19 (S99 — shared pwa.js Dismiss aria-label 04348bb)**; i18n page 8 (07-12); loop-prior S92 Pass C gear-modal/landmark/aria + S91 ftRead safe-parse + S87 AllTools close-button aria | resources: **2026-07-19 (S110 — Lakhish Bold OFL 1.1 license stated end-to-end 1bace42)**; loop-prior S104 Pass G font-download-zip DOS-timestamp fix + S92 first-ever dedicated C (Fonts-view keyboard blocker + lang=he + modal dialog + gold-on-light contrast) | contact / privacy / 404: **2026-07-16 (S92 — first loop touch: gold-on-light footer/body contrast → --gold-text)** | (torah_trainer + Font Maker footers also converged to --gold-text in S92's contrast commit)

### Pattern health (per recurring pattern: last swept, hits that sweep, consecutive clean sweeps; detail in the sweep log below)
- falsy-zero: swept 2026-07-17 (S93), hits: 0 (the dashboard movable-panels feature 639fcf8/53e50fc uses array-order `panelLayout` with no numeric restores; zone reorder is array-index splicing; its guards use `=== undefined`, not `||`), clean streak: **3 → RETIRED S93** (3 consecutive clean sweeps S73/S83/S93; correctness-scoped, not consequence-critical → auto-retired per the rule; re-checked only in Pass A2). Prior S83: 0 (streak 2); S73: 0 (streak 1); S64: 1 hit (FM `spec.version||1.0`, b9c1aa3).
- localStorage-vs-AllTools: swept 2026-07-21 (S126), hits: 0 (over `fd0fd11..HEAD`: the outside-loop **Advanced-TTS-added-then-removed / Basic-TTS** drift + S115–S125 loop changes add **0 new localStorage keys** — Basic-TTS reuses the already-registered `hebrewDictionary_ttsRate` (`dictTtsRate`) and its `_heVoiceWarned` is a JS var; the S126 FM tooltip + trope fullscreen fixes add no state), clean streak: **9** — **KEPT ACTIVE** (consequence-critical carve-out: backup completeness / data-loss-on-new-machine). Prior S114: 0 (streak 8); S103: 0 (streak 7); S93: 0 (streak 6).
- unescaped-input / unsafe-parse: swept 2026-07-21 (S126), hits: 0 (over `fd0fd11..HEAD`: the dashboard Now/Next guard `esc()`-wraps every dynamic value — only trusted `I18n.t` goes in raw; the message-surface `innerHTML` is pre-existing teacher-local contenteditable (the new `_dashTextEmpty` uses an inert detached `<template>` only to read `textContent`); FM's one new interpolation is a trusted anchor `Object.assign` clone; Basic-TTS uses `SpeechSynthesisUtterance(text)` — not an HTML sink — + localized strings; new fetches are same-origin `data/*.json`; the removed Advanced-TTS cleanly reverted its CSP; S126's own fixes route through `I18n.t()`/reuse keys), clean streak: **5** — **KEPT ACTIVE** (consequence-critical carve-out: XSS — never auto-retire a security sweep). Prior S114: 0 (streak 4); S103: 0 (streak 3); S73: **1 P2 (+1 P3) FIXED** (FM imported-project glyph/mark `.name` → `innerHTML` unescaped, a87745e).
- destructive-bulk: swept 2026-07-21 (S126), hits: 0 (over `fd0fd11..HEAD`: the FM v5.5 export-chain `emitBaseAnchors` uses `Object.assign({}, base, …)` shallow clones — never mutates a stored anchor; the removed Advanced-TTS + Basic-TTS notice touch no per-item stored data; no loop change overwrites per-item customizations unconditionally), clean streak: **9** — **KEPT ACTIVE** (consequence-critical carve-out: teacher data-loss). Prior S114: 0 (streak 8); S103: 0 (streak 7); S93: 0 (streak 6).
- _(**shadowed-global helper RETIRED at S73** — 3 consecutive clean sweeps (registered S40 w/ 2 torah hits fixed, then clean S64 + S73 over the i18n rollout: one `esc`/`applyI18n` per file; FM `t()` + `pwa.js` `t()` are intentional in-scope locals); correctness-scoped, not consequence-critical → auto-retired. Re-checked only in Pass A2. Also **listener/interval accumulation** + **JSON-LD ↔ visible-content parity** RETIRED at S64; workMode/step reachability RETIRED at S52; undo-wiring + slider-commit RETIRED at S36 — see Retired patterns.)_
- _**Retirement rule (as applied):** a pattern retires after 3 consecutive clean sweeps **UNLESS it is consequence-critical** (security or data-loss). The three that hit 3-clean at S64 — unescaped-input/unsafe-parse (XSS), localStorage-vs-AllTools (backup), destructive-bulk (teacher data) — are **deliberately kept ACTIVE**: new code reintroduces them anytime and they re-sweep cheaply, so the safety margin outweighs the noise. Only structural/cosmetic classes auto-retire. Retired patterns are re-checked only in pass A2._

### Retired patterns

- falsy-zero (`s.field || default` silently discarding a stored `0`/`''`/`false` in a numeric/boolean restore) | retired 2026-07-17 (S93) | 3 consecutive clean sweeps: 1 hit S64 (FM `spec.version||1.0`, b9c1aa3), then clean S73, S83 (FM v4.18→v4.26 slider/geometry guards), S93 (dashboard movable-panels — array-order `panelLayout`, no numeric restores). Correctness-scoped (a wrong restored value, not data-loss) → not a consequence-critical carve-out → auto-retired. Re-checked only in Pass A2. **Watch:** any new tool with numeric/boolean `applySettings`/size-restore should re-register it if `|| default` reappears on a count/size/index/toggle.
- shadowed-global helper (a top-level helper — `esc`/`status`/`applyI18n`/`t`/… — shadowed by an inner decl so a global-expecting call site gets the wrong one) | retired 2026-07-13 (S73) | 3 consecutive clean sweeps: registered S40 (2 torah `esc`-shadow hits fixed, d88fa99), clean S64, clean S73 over the site-wide i18n rollout (one `esc`/`applyI18n` per file; FM's local `t()` in `shortcutGroups()` and `pwa.js`'s self-contained `t(key,fallback)` are intentional in-scope locals, never reach a global-`t` call site). Correctness-scoped, not consequence-critical → auto-retired. Re-checked only in Pass A2.
- listener/interval accumulation (a `setInterval`/`setTimeout`/`addEventListener` attached repeatedly without clear/remove) | retired 2026-07-10 (S64) | 3 consecutive clean sweeps (S52, and S64 over the S53–S63 surface — generator chunked-build timer single/self-chaining + `cancelWorksheetBuild` clears on re-entry, `beforeprint` attached once, torah color fns add no listeners, `_appToastTimer` clear-guarded). Structural/perf-scoped → auto-retired. Re-checked only in Pass A2.
- JSON-LD ↔ visible-content parity (an `application/ld+json` FAQ/HowTo/ItemList claim drifting from the visible UI or code constants) | retired 2026-07-10 (S64) | 3 consecutive clean sweeps (S52, and S64 — no `ld+json` block changed across S53–S63; spot-checked torah/generator/trope/FM claims still accurate). SEO/cosmetic-scoped → auto-retired. Re-checked only in Pass A2.
- workMode/step reachability (controls reachable in only one workMode/step while the workflow steers users past it) | retired 2026-07-10 (S52) | 3 consecutive clean sweeps (S36, S41-scoped, S52 — trope_tutor's drawer/tabs/tour-skip/mid-drill-return all reachable-by-design). Re-checked only in Pass A2.
- undo-wiring (Font Maker: `markDirty()` without `udDo`/`udBurstBegin`/`udNudgeTick`) | retired 2026-07-08 (S36) | 3 consecutive clean sweeps; the S25–26 keyboard additions (node-insert, crop) verified as routing through `udDo` or deliberately non-undoable. Re-checked only in Pass A2. **Re-verified CLEAN 2026-07-09 (S41)** over the un-swept FM v3.9–v4.2 additions (auto-detect Apply via `udDo(...withSource)`, pen contour, node delete/paste/transform/specks/fillet all `udDo`; `_hiddenContours`/opacity/snapGuides are documented view-only).
- slider-commit (Font Maker: `oninput` range sliders lacking `onchange="udBurstCommit()"`) | retired 2026-07-08 (S36) | 3 consecutive clean sweeps; every project-data slider commits, and no new range slider has been added. Re-checked only in Pass A2. **Re-verified CLEAN 2026-07-09 (S41)** over the FM v3.9–v4.2 sliders (fillet `filletLiveInput`/`filletCommit` burst; size setters commit; mark-editor `meSetDotSize` uses its own `meBeginEdit`/`meCommitEdit` stack; `adSep`/`adTh` are detection-only until Apply).

## Recurring-pattern sweep status

- **S126 (Pass A, 2026-07-21): CLEAN, 0 hits** across the 3 active consequence-critical classes over `fd0fd11..HEAD` (S115 FM v5.5 export chain + S116–S125 loop changes + the substantial **outside-loop TTS drift**: Advanced TTS/Kokoro-ONNX added 4032210, iterated, then **fully removed** 43b64e6, leaving only a Basic-TTS "no Hebrew voice" notice 349c2c9). **unescaped-input:** dashboard Now/Next guard `esc()`-wraps all dynamic values; message-surface `innerHTML` is pre-existing teacher-local contenteditable (`_dashTextEmpty` reads `textContent` off an inert `<template>`); FM's new interpolation is a trusted anchor clone; TTS `SpeechSynthesisUtterance(text)` is not an HTML sink; new fetches same-origin `data/*.json`. **localStorage-vs-AllTools:** 0 new keys (TTS reuses `hebrewDictionary_ttsRate`). **destructive-bulk:** FM anchor emits are shallow clones. Also verified the Advanced-TTS removal **cleanly reverted its CSP** (no stale `wasm-unsafe-eval`/Hugging Face) and left no orphaned CDN refs. The FM 9-tooltip pure-wiring fix (1984167) was the top actionable candidate found; no new pattern registered.
- **S114 (Pass A, 2026-07-20): CLEAN, 0 hits** across the 3 active consequence-critical classes over `36161c0..fd0fd11` (S104–S113 loop changes + the substantial **outside-loop FM v5.2→v5.4 drift** — combined-form mark-grid tiles that replace the redundant generic tiles, faithful שׁ/שׂ dot-stacking checks, the וֹ ligature shaping-guard at export, and the v5.4 Holam-Haser-on-vav split). **unescaped-input:** the new FM tile/stacking code interpolates only trusted static codepoints (`pc.target`), `esc()`-wrapped form names, and trusted i18n strings — 0 untrusted sinks, no new `JSON.parse`/`Object.assign`; **localStorage-vs-AllTools:** 0 new keys (all new state is `project.*` → IndexedDB autosave, FM backup-exempt by design); **destructive-bulk:** the v5.4 `cholamScale`/`holamScale` migration is `== null`-seeded (from a prior FB4B dot size, so existing saves look unchanged) and the `precomposed` backfill stays `=== undefined`-guarded — never clobbers a customized anchor. All 3 KEPT ACTIVE (streaks 4/8/8).
- **S103 (Pass A, 2026-07-19): CLEAN, 0 hits** across the 3 active consequence-critical classes over the surface changed since S93's A sweep — the **outside-loop FM v5.0→v5.1** (precomposed שׁ/שׂ+ḥolam anchor/stacking, spacing-preview nikkud, Save-as-a-copy, help formatting, og-share), the **Resources font-gallery** (Lakhish Bold + `fontSampleFor` mark-stripping preview), and the **S94–S102 loop changes** (`a22bfc0..HEAD`, 21 files, +790/−206). **unescaped-input/unsafe-parse ✓** (new user-data `innerHTML` = FM `meItem.name`/`lname`, both `esc()`; `buildAboutBody`/`HELP_CONTENT` trusted-static with `esc()`'d i18n values; new `JSON.parse` = `JSON.stringify` clones; resources font-preview `txt` → `textContent`). **localStorage-vs-AllTools ✓** (0 new keys). **destructive-bulk ✓** (FM precomp reset single-target `udDo`-wrapped; `data.precomposed` backfill null-guarded). Maintainer-suggested **falsy-zero** spot-check on the new FM coordinate code = clean (`!= null`/`Number.isFinite` throughout — retired, stays retired). Streaks: localStorage 6→7, unescaped-input 2→3, destructive-bulk 6→7 (all KEPT ACTIVE).
- **S93 (Pass A, 2026-07-17): CLEAN, 0 hits** across the 4 active classes over the surface changed since S83 — the outside-loop **dashboard movable-widget-panels feature** (639fcf8 + review-fixes 53e50fc), the S84–S92 loop changes, and the 07-17 maintainer commits (Referrer-Policy meta / non-render-blocking fonts / network-first locales). Dashboard feature verified: `settings.panelLayout` rides in the AllTools-registered `hebrewDashboard_settings` blob (**localStorage-vs-AllTools ✓**); loaded via `ivritSafeParse` + a `normalizePanelLayout` validator at every ingress (**unsafe-parse ✓**); zone reorder is pure array-index splicing, guards use `=== undefined` not `||` (**falsy-zero ✓**); **0** new `innerHTML`/`insertAdjacentHTML`, panels reparented by known ID (**unescaped-input ✓**); `resetPanelLayout` is a reversible, clearly-labeled view-pref touching no saved data (**destructive-bulk** — below the bar, logged P4 not a hit). **falsy-zero hit its 3rd consecutive clean sweep → RETIRED**; the 3 consequence-critical classes stay ACTIVE (carve-out).
- falsy-zero: re-swept 2026-07-15 **S83** over the FM v4.18→v4.26 + S74–S82 surface — **CLEAN, 0 hits.** The new FM geometry/slider code guards a legal 0 correctly: vowel-pair `dx` read via `typeof(c.dx)==='number'` (preserves stored 0); guide-weight (`+w||1`, slider `min=0.25`), zoom (`+z||1`, slider `min=1`→0.01 floor), and fillet-radius (`+v||25`, slider `min=5`) all clamp with slider floors where 0 is unreachable, or use a default equal to the falsy value (`setGuidePos +v||0`, `setVowelPairDx +v||0`). The one genuinely 0-reachable new field (vowel-slide `dx`) is preserved by the `typeof`-guarded reader; the near-miss `setVowelPairMode Math.abs(c.dx)||70` is an intentional re-seed of a meaningless 0-magnitude direction click. Streak →2, kept ACTIVE. Prior: re-swept 2026-07-10 **S64** over the S53–S63 surface (generator chunked build, torah color layer, FM v4.10–v4.15 incl. the outside-loop Firefox-box code) — **1 hit fixed** (FM `buildUfoFontInfo` L10899 `spec.version || 1.0`, the sibling the S52 sweep missed — one screen from its own `Number.isFinite` guards at L10699/L11347; normalized b9c1aa3; unreachable-with-0 today because UFO export bumps the version to ≥0.001 upstream at L11347 first, but the pattern was present and copy-prone). FM `adState.sep = +v || 0` and `spec.lineGap || 0` verified legal (`||0` where the default equals the falsy value). Prior: re-swept 2026-07-10 **S52** over everything since S41 — **1 hit fixed** (FM `parseFloat(v) || 1.0` promoted a legal user-entered 0.x font version to 1.x at the doExport tick + the buildFontSpec head-table bake; `Number.isFinite`-guarded, d343b1b — bundled with the adjacent out-of-class find: the version bump wasn't rolled back on gate-Cancel/WOFF2-failure, now rolled back via `finishDownload`'s success boolean). The trope index's documented `s`/`e`-can-be-0 clip bounds verified safe at every read (direct assignments, `=== null` stop-guards, `== null` builder checks); dashboard prefill comparison-based (midnight 0 flows correctly); FM `ensureNikkudAnchors`/autosave-restore `!= null` throughout. Prior: re-swept 2026-07-08 **S36** over the S23–S35 additions — grep CLEAN (the S35 custom card-count uses `??`/`Math.min/max` clamps; only the 2 vetted `||` exceptions remain). **BUT** the same sweep surfaced an adjacent numeric-safety gap — `cardCount` restored (`applySettings` `?? 10`) + `setCardCount` were **unclamped**, so a corrupt/shared blob with a huge `cardCount` could freeze the tab on Start; fixed by clamping every setter + the restore through `_clampCardCount` (952edcd, P3). So falsy-zero is at 3 clean but **kept ACTIVE** (numeric-restore family still warrants watch). Earlier: re-swept 2026-07-07 **S22** — CLEAN (nullish sweep thorough). Earlier: last swept 2026-07-06 (all 11 root HTML) — 2 hits, both legal (opentype `advanceWidth || 0` missing-value fallback; dashboard `offsetWidth || 320` layout fallback). The 2026-07-05 remainder was fixed same-day (ba33911).
- undo-wiring (Font Maker mutations that `markDirty()` without `udDo`/`udBurstBegin`): re-verified clean 2026-07-06 — all 12 `markDirty()` sites are undo-plumbing, burst-adjacent, or documented-exempt (custom-glyph CRUD, `toggleGuideCollapse` pure-UI, `savePlacement` snapshot baseline). Aside logged as P4 Candidate: opacity view-pref setters don't markDirty at all.
- slider-commit (Font Maker `oninput` sliders lacking `onchange="udBurstCommit()"`): re-verified clean 2026-07-06 — every project-data slider commits; the kern-value slider (staging draft) and opacity view-prefs are correctly outside the burst model.
- workMode/step reachability (controls reachable in only one workMode while the workflow steers past it): swept 2026-07-06 — CLEAN. Letter Metrics now renders in Align, Trace, AND Nodes; the Trace tab (with the SVG-only Edit-points/Copy-outline controls) stays visible for all letter glyphs; Anchors/Trop tabs hide only when marks are disabled.
- localStorage-vs-AllTools (keys written by a tool but missing from index.html gather/import/erase or the owning `.ivrit`): re-swept 2026-07-06 **S17** — all cross-machine data keys registered; `hebrewFontMaker_inputMode` (UI-flag class) was missing from `eraseAllSettings` and was added (b37cc42). Earlier: swept 2026-07-06 — found + fixed `hebrewBlender_hebFont`/`_hebFontSize`/`_livePreview` (de3424d). `hebrewFontMaker_inputMode` matches the exempt inputMode class (now erase-listed). The last known gap — `hebrewDictionary_lastState` (erase-only) — was registered as `dictLastState` 2026-07-06 (5d33e76). S10 re-swept all 33 written keys: export/import registry complete; the erase-side gap (8 one-time flags missing from `eraseAllSettings`) fixed (0d63af0). No known gaps remain.
- unescaped-input / unsafe-parse (innerHTML interpolation bypassing `esc()`; untrusted `JSON.parse` bypassing `ivritSafeParse`): re-swept 2026-07-07 **S22** over the hardening-pass + Word Lists additions — found + fixed a **P1** stored/reflected XSS: the `ea03110` Word Lists feature routed untrusted words (`?s=` auto-`generate()`, imported `ivritSuite_wordLists`, paste-in lists) into every Real Words drill `innerHTML` unescaped, and the generator's `colorizeHebrew` didn't escape (46fec2f — 10 drills, incl. 4 renamed-variable sinks only the payload test caught). Flash Cards hardened defensively (22efcc1); its reachable words-mode path was already escaped. Parse side CLEAN: `wlReadStoreLocal`/`wlMergeIntoStorage` use `ivritSafeParse`+`ivritSafeAssign`, `?s=`/`?ak=` decode via `ivritSafeParse`. Earlier: re-swept 2026-07-06 **S17** over the S12–S16 additions — found + fixed a real gap: `nikudColorOverrides` color values flowed unvalidated into `style="color:…"` spans in all 4 vowel-color tools; a malicious imported override was a stored-XSS vector (6bd1b7b). All other interpolations of imported/user data confirmed routed through `esc()`/`escapeHtml`/`textContent`; every untrusted `JSON.parse` uses `ivritSafeParse`, every import merge uses `ivritSafeAssign`. Earlier: swept 2026-07-06 incl. all session-1/2 additions (tooltip engine, tile a11y, tour) — clean; all render via `textContent`/`setAttribute`; the 8 `JSON.parse(localStorage…)` sites are trusted own-writes or shape-guarded `ftRead`; import paths use `ivritSafeParse`/`ivritSafeAssign`. S10 re-swept the nine S8/S9 a11y commits — clean (numeric-index interpolation only; user data through `esc()`; no new JSON.parse).
- listener/interval accumulation (S10, new pattern): all `setInterval`s in the 6 tools are `clearInterval`-guarded or one-shot (dashboard's unassigned `checkSchedule` interval is inside one-shot DOMContentLoaded — fine, but it's the only one with no clearable handle); no render/init/apply function attaches listeners to elements it doesn't recreate; all document/window listeners are named + removeEventListener-paired (tour keys, drawer trap, pointer drags, menu closers); Font Maker's `qaGrid` uses an explicit `_wired` guard. CLEAN — no accumulation risk.
- JSON-LD ↔ visible-content parity: swept in depth 2026-07-06 (all 11 files) — every FAQ Q&A visible on-page, HowTo steps match real UI (incl. flash_cards post-drawer-removal + post-tour), dictionary's "7-step walkthrough" claim matches its 7 TOUR_STEPS. One cosmetic hit fixed (index ItemList privacy entry, 73cfb7d).
- destructive-bulk (loops over all items/letters overwriting per-item customizations unconditionally): swept 2026-07-06 — `applyOutlineToAll` confirm-gated this session (94b1d14); `resetTropSideColumns`/`resetAllAnchors` confirm+undo, apply-to-all modals opt-in per letter, `migrateProject` backfills are null-guarded (never clobbers).
- console/error audit (Pass B): re-run 2026-07-19 **S108** (9th run — 24 runs [12 pages × light/dark] load + 1 interaction/tool over the S98→S107 surface = **CLEAN**: 0 pageerror / 0 `[i18n] miss` / 0 failed-local / 0 non-noise console; an unfiltered distinct-message capture confirmed the only output is expected external-abort noise — Google-Fonts/gtag `net::ERR_FAILED` on every page + torah_trainer's documented offline Sefaria `fetchSefariaText`/esm.sh/jsdelivr ESM artifacts). Prior re-run 2026-07-19 **S98** — all 12 root pages over local HTTP, light+dark = 24 runs, load + one interaction per tool: **CLEAN — pageerror==0, 0 failed-local requests, 0 `[i18n] missing key`** (only torah_trainer's documented external Sefaria/ESM offline noise, route-aborted). Prior 2026-07-16 **S88** (24 runs, CLEAN). Prior re-run 2026-07-08 **S37** — all 11 root pages headless (file://), light-1280 + dark-800, load + one representative interaction each (index gear, generator letter tile, dashboard drawer, flash cards mode-pill + custom-count, dictionary search, FM letter tile, dark-toggle everywhere): **CLEAN — 22/22 page-runs, 0 pageerrors**. The only console errors were 10× `URL scheme "file" is not supported` (dictionary `hebrew_words.json`, torah `parshiyot/pockettorah/*`) — a file:// `fetch()` artifact; re-checked those over a local HTTP server → **0 pageerrors / 0 real console errors**, purely environmental. No console regressions from the S21–S36 additions. Prior: last run 2026-07-07 **S20** — all 11 root pages over local http, light-1280 + dark-800, load PLUS one representative interaction each (incl. index gear modal; Font Maker letter select works via `#letterGrid .ltile`): CLEAN — 22/22 page-runs, 0 pageerrors, zero non-environmental console errors/warnings (same expected torah Sefaria/ESM noise). Prior full runs S4 and S11 also clean.
- freshness/site-health (Pass E): re-run 2026-07-08 **S38** — mechanical checks (sitemap `<loc>`+`<lastmod>` vs git = all 10 pages @ today; sw `CORE_ASSETS` both directions @ v235 = 21 entries all resolve, only intentional omissions og-card/zelle-qr; all internal `href`/`src` + 18 splash hrefs; THIRD_PARTY_LICENSES vs bundled fonts + all CDNs + 22 Google Fonts; robots/CNAME/manifest) **all CLEAN — nothing broke since S23**; docs audit found + fixed 2 P4 drift lines (README FM v3.6→v3.8; CLAUDE page-level toast cross-ref) and verified the ledger's other flagged doc items (S36 retirements, S35 flash-cards feature, shared-block/tour/reduced-motion/lang="he" counts) **NOT drifted**; harmless P4 note logged (duplicate root+`/icons/` icon precache). Prior: re-run 2026-07-08 **S23** — mechanical checks (sitemap `<loc>`+`<lastmod>` vs git, sw `CORE_ASSETS` both directions @ v222, all internal links + 18 splash hrefs + data fetches, THIRD_PARTY_LICENSES vs 6 bundled TTFs + CDNs, robots/CNAME/manifest) **all CLEAN — nothing broke since S19**; docs audit found + fixed 2 drift lines from the S21 FM a11y work (README v3.5→v3.6; CLAUDE `lang="he"` list + Font Maker). Earlier: re-run 2026-07-06 **S19** (full: sitemap `<loc>`+`<lastmod>` vs git, sw CORE_ASSETS both directions, all internal links + 19 splash hrefs, THIRD_PARTY_LICENSES vs bundled fonts/CDNs, CLAUDE.md shared-block counts + AllTools key table, README, robots/CNAME/manifest). **Almost entirely CLEAN** — only 2 doc-drift lines found + fixed (README Font Maker v2.0→v3.5, CLAUDE.md line count; c7f5da1). Sitemap/sw/links/licenses/robots/manifest and all CLAUDE.md counts verified accurate. Earlier: last run 2026-07-06 (full: sitemap `<loc>`+`<lastmod>` vs git, internal links/assets incl. all 18 splash hrefs, sw CORE_ASSETS both directions, THIRD_PARTY_LICENSES vs bundled content, README + CLAUDE.md accuracy, robots/CNAME/manifest). Sitemap/links/SW/misc all CLEAN; the doc gaps found (font-license claim, README tour list + fonts list, CLAUDE.md AllTools table + DATA_CACHE) were all fixed this session (0e9070d, 3afca1f, d02fbde). Remaining: Reuben license-file parity (P4 Candidate).
- cross-tool consistency (Pass F): 2026-07-22 **S133** — **share / shareable-config affordance** (first dedicated F sweep): **CONSISTENT** — the two `?s=` tools (generator, dictionary) have converged on one pattern (`serialize`→`history.replaceState`→clipboard w/ execCommand fallback→button-flip-"copied"→`announce()`→1500 ms restore; both localized + keyboard-operable); flash_cards' paste-code is the grandfathered coexistence the Shared-UX rule allows; FM shares a font file; dashboard/torah/trope have **no** shareable-config state (device-local/content-driven) so no affordance is appropriate. 0 laggard / 0 fix; the only diff is cosmetic (📋 "Copy shareable link" vs 🔗 "Copy link") = a maintainer wording call. `#shareNotice` confirmed live (auto-restore banner), not vestigial. Prior 2026-07-20 **S123** — **fullscreen-toggle affordance** across the 6/7 tools that have it (trope_tutor has NONE → feature seed): two legitimate models — two-button enter + overlay-exit with header-hidden-in-fullscreen (dashboard/torah/FM/generator) vs single-button-stays-visible toggle (flash_cards, the `aria-pressed`-on-`fullscreenchange` canonical); accessible names all present. **Bug fixed (d57108a):** generator + dictionary reset `#fsBtn.title` to hardcoded English on `fullscreenchange`, clobbering the localized `data-i18n-title` after a round-trip → Hebrew UI showed English; converged both to flash_cards' `_syncFsBtn` (reuse existing `shared.fullscreen.*` keys, no CSV). **Dark-mode toggle re-confirmed CONSISTENT** across all 12 pages (the S24 convergence holds: OS-aware no-flash IIFE + `aria-pressed` synced in toggle & init + correct accessible names + `shared.darkmode.*` localization). **Recurring-shape mini-sweep** (hardcoded English `.title`/`aria-label`/`textContent` assigned in JS — the Check-A blind spot — across all 7 tools): fixed torah scope holiday tooltip (faedfb4, new key); logged dashboard student-picker chip titles (deferred — 2/session i18n pattern cap); flash_cards `aria_more_info` confirmed correctly wired (false positive, don't re-log); generator's remaining hits are intentional printed worksheet/answer-key/crossword output (baselined). Prior 2026-07-19 **S111** — **toast/status affordance CONSISTENT** (every tool's toast carries `role="status"` + a `prefers-reduced-motion` neutralizer + a `body.dark` variant; the only gap is full toast *adoption*, a tracked large refactor). Prior 2026-07-14 **S80** — **Hebrew font-picker i18n** (the S62-deferred affordance). `flash_cards`+`generator` localize the picker's section headers + `[DEFAULT]` badge via `I18n.t()`, but `hebrew_dictionary`/`trope_tutor`/`torah_trainer` hardcoded them and torah's upload label was unlocalized → converged all onto flash_cards' pattern reusing existing `shared.fonts.*` keys (no CSV/build-locales), + `initFontSelector()` added to torah's `applyI18n` so a live language switch re-localizes the picker (d08f02a, 16/16 headless EN+HE clean; `[DEFAULT]` lives in a single-quoted string inside the template `${}`, so the fix uses `' + esc/ivEsc(I18n.t(…)) + '` concat). Shared `ivritsuite-fonts`+uploader blocks re-verified byte-identical; the `.font-opt` `aria-pressed` gap is uniform (enhancement-to-all, logged as candidate not laggard). 3 P3 candidates logged (dashboard gear aria-label, dashboard tour-pulse, share-copy announce). Prior re-run 2026-07-10 **S50** — audited the **toast/status-feedback** affordance across all **7** tools (first F to include the trope tutor). Every tool's mechanism cataloged (generator/dashboard/flash_cards `showAppToast` 2600 ms; torah `showHintToast` 8000 ms; trope tutor `showToast` 6000 ms, built to the house pattern; FM `status()` 4000 ms + `askModal`; dictionary inline `wlSetStatus` + SR-only `announce` — no visual toast, but no success-alerts either → toast would be net-new, skipped); **`role="status"` present on every feedback element in all 7 tools (a11y CLEAN)**. 1 laggard converged (generator preset-import `alert` → `showAppToast`, 30e0251 — completes the S39 convergence); the `.ivrit` engine's triplicate `'Restored successfully'` alert folded into the deferred shared-block extraction candidate (it lives inside the byte-identical engine block — a per-tool fix would break byte-identity). The `showAppToast`/`showHintToast`/`showToast` triplication re-confirmed → stays with the deferred extraction. Prior 2026-07-08 **S39** — audited the **non-blocking toast / success-feedback** affordance across all tools; the 3 laggards (flash_cards, dashboard, dictionary) had no floating toast while the generator has the portable `showAppToast`. Reframe: copies were **already** non-blocking (label swaps) everywhere, so only the twin blocking `alert('Imported successfully.')` (flash_cards L3999, dashboard L4898) were in scope → converged both on `showAppToast` (c2b0274); dictionary skipped (no success alert), the shared `.ivrit` restore alert + all error/validation alerts left per CLAUDE.md §5. Logged the `showAppToast` shared-block extraction follow-up; corrected the C1 panels candidate to a maintainer design call. Prior **S24** — audited the **dark-mode toggle** across all 11 pages and converged it on the Font Maker model (aed9072): aria-pressed on all toggles, aria-label on icon-only, ☀️ glyph, and an OS-aware first-visit default suite-wide (user opt-in). Also surveyed toast/empty-state (CLEAN aria-live; logged 3 coverage candidates — torah empty-state P3, showAppToast adoption P4, CLAUDE toast-doc P4). Earlier: last run 2026-07-06 — shared-block byte-identity audit (all 4 marked JS blocks + 2 CSS blocks, sha-compared across every carrier). ivritsuite-fonts (8 copies), folder-tree JS (4) and CSS (3) were already identical; the `.ivrit` engine drift (comments/indent) and the uploader block boundary were fixed this session (cfae257, 4236b7f); index.html's engine/uploader/`.ivrit`-CSS variants are documented-deliberate. All shared-block invariants now hold and are machine-checkable by extracting the marked spans and sha-comparing.
- performance (Pass D): 2026-07-20 — **S117 — classroom_dashboard.html, 3rd dedicated D (S13→S35→S117); thoroughly CLEAN, 1 per-second-churn fix (b5d4261).** Cold-load boot longtask **@1× max 195 ms (UNDER the 200 ms bar — clean at real desktop) / @4× 561 ms max, 796 ms sum** — the parked candidate; its "render-blocking Google Fonts" half is now STALE (maintainer ~07-17 made the stylesheet `media=print`-swap non-render-blocking, so the JS boot block is the sole cause). Movable-panels feature (never profiled): button-driven `insertBefore` over 5 panels, enterEdit+3moves 145 ms @4×, no >200 ms LT. All interactions <200 ms even @4× (openSettings 65, colorCode+full-rerender 31, darkToggle 153). **All-day-projector stability:** 40 open/close·dark·lang·re-render cycles → DOM 1104→1104 (Δ0), `setInterval` 3→3 (Δ0) — retired listener/interval-accum re-confirmed dead under the perf lens. Fixed the one per-second waste found: `#nowNextBlock` rebuilt `innerHTML` every clock tick when Schedule Sync is active though its content is minute-granularity (~21,600 needless rebuilds/day) → `_lastNowNextKey` guard mirroring the S14 `_lastPreviewKey` sibling (verified: 20 stable ticks → 0 rebuilds, change → rebuild, lang-switch → translated re-render). Prior 2026-07-18 — **S95 — trope_tutor.html, FIRST-EVER dedicated D; effectively CLEAN baseline** (desktop @1× 0 longtasks ~176 ms load; only @4× cold-init boot longtasks max ~385 ms/~1146 ms total, throttled-only; all interactions <200 ms @4× — drill session 94 ms, renderLearn 6 fam 21 ms, startDrill 1.7 ms; 0 pageerrors light/dark×desktop/800; logged P4-informational parked). Prior 2026-07-11 — **S66 — torah_trainer.html 2nd pass (first-ever D was S16 on this tool); effectively CLEAN, no code.** Purpose: verify the **S60** nikkud CSS-var decouple's no-rebuild win held. Headless over local HTTP, worst-case injected fixture (176 verses → **2,317 `.tt-word` / 4,636 `.nik` spans**, nikkud color + trope + translit + translation all on), light/dark × 1280/800 × 1×/4×, with a `MutationObserver` on `#ttReading` + a `longtask` observer. **S60 confirmed HELD:** `toggleDark` / color-mode radios / `setVowelScheme` / both color pickers → **0 reading mutations, 0.1–2.7 ms JS** every scenario; the control (`renderText`/layout) → **354–356 mutations, 26–194 ms** — proof the color/mode/scheme/dark paths are pure `<body>` var+class swaps, no reading rebuild. **Idle CLEAN** (0 mutations/3 s; no `setInterval`, rAF one-shot). No pageerrors, no h-scroll @800px. Karaoke `onAudioTimeUpdate` re-confirmed (static) as binary-search + `idx`-unchanged early-out + O(1) DOM/word-change (live audio env-blocked — remote PocketTorah MP3s route-aborted). **Two costs, no fix:** (1) worst-case full-parsha `renderText` = one-shot content-change block (~128–194 ms @4× / 26–37 ms @1× JS + ~470–560/60–130 ms browser layout) — large-refactor-only → **new P4** (like the generator Class-Set pre-S61); (2) changing body color vars restyles all 4,636 `.nik` spans (~0.8–1.6 s @4× / ~0.1–0.5 s @1× style-recalc) — inherent to the CSS-var model, `saveSettings` debounced, still cheaper than a rebuild → informational. Prior: 2026-07-08 — **S35** 2nd pass on classroom_dashboard.html (all-day projector focus): re-profiled cold load + a ~4.2 s idle recurring-churn probe (desktop + 4× throttle). **CLEAN** — idle DOM churn ~4 nodes over ~4 clock ticks (just the clock time text), 0 idle longtasks; the **S14 per-second schedule-preview rebuild fix HELD**. Cold-load one 321 ms longtask @4× / 89 ms desktop = the already-logged P4 (boot block + 5-family Google Fonts), re-measured, no new action. Earlier: 2026-07-06 — **S16** on torah_trainer.html: the audio-highlight loop is already index-tracked + O(log n)/tick (not a hotspot); no setInterval/continuous rAF. Fixed the per-syllable dark-mode DOM read in the colorize hot loop (6ce8b3c). `aliyah.json` parse trivial (1.2–5.5 ms). Logged: full-reading render (nested-span innerHTML + no incremental re-render on every fetchAndRender) as the likely biggest one-shot block. **This completes the Pass-D cycle — all six tools profiled** (dictionary → generator → dashboard → flash_cards → torah_trainer; Font Maker's heavy work is lazy CDN/WASM and was surveyed as exempt). **S15** on flash_cards.html: measured largely CLEAN (no >200 ms blocks; emoji/letter deck builds single-digit to low-double-digit ms even throttled; no rAF/confetti loops; 1 Hz timer is one textContent write). Fixed the per-draw emoji-pool rebuild (4a72aaa). `hebrew_words.json` parse re-confirmed cheap (12–48 ms). **S14** on classroom_dashboard.html (long-running projector focus: quantified per-second recurring cost, not just >200 ms blocks). Found + fixed the per-second schedule-preview innerHTML rebuild (3bf4bbd — 299/300 writes redundant). `applySettings` measured fast (3.3/20.9 ms — survey overestimated, no action); cold-load one 452 ms throttled-only longtask logged P4; clock tick confirmed clean (colorize/renderDaysList short-circuit on day-key, per survey). **S13** on hebrew_blend_generator.html: fixed render-path full-corpus translit rebuild (a3507c0); cold-load corpus parse+flatMap CLEAN (17–21/98 ms); logged Class-Set 40 Generate (~8,000 cells, 210/368 ms) + exportPDF raster. **S-dictionary** (first run): desktop CLEAN (worst 113 ms); 4× throttled 457 ms load flatten+gematria + 270 ms onFilter (fixed 7ef380c, b7cfadd).
- accessibility (Pass C): **S125 — Hebrew_Font_Maker.html, first-ever dedicated C on FM: a11y-strong** (247 visible controls, only 3 nameless = all range sliders; modal focus-trap / focus-visible / wrapping-label coord inputs all fine). 2 fixes: all FM sliders get accessible names — the shared `vSlider()` helper rendered unassociated label spans (systemic), fixed via `aria-label` reusing existing keys (f5d4080 + 18d9a5d), nameless 3→0. Deferred: editing-stage `role`/`aria-label` (design-call). Prior **S113 — trope_tutor.html, first dedicated C on the tutor: a11y-strong (APG keyboard / drill `<button>`s w/ aria-label+aria-pressed / settings-drawer Escape+focus-restore / all `<label for>` / contrast clean); completed reduced-motion coverage (d04b45c). Prior **S82 — flash_cards.html, first dedicated a11y pass since S8** (over its S35/S47/S49/S71/S72 growth): reduced-motion / `lang="he"` / keyboard-operability / `:focus-visible` all CLEAN; fixed gold-on-light contrast (footer + 7 nav links + streak score → `--gold-text`, navy skip pill; 9c10a65, 6.22:1 light, dark unchanged, `pageerror==0`); logged P2 overlay-modal dialog semantics + generator contrast twin + pill-`aria-pressed` + card/score `aria-live` + labels + touch targets. Prior **S51 — first-ever pass on trope_tutor.html (over local HTTP; drill flow exercised end-to-end): strong shape, 1 P2 + 3 P3 + a P4 batch.** CLEAN: full keyboard reachability/operability (no mouse-only handlers), gold `:focus-visible` rings + zero `outline:none`, drawer-open trap/Escape/restore, `#qFeedback` role=status (text, never color-only), aria state sync throughout, tour dialog semantics, 68 `lang="he"` nodes, reduced-motion complete, dark-mode contrast all-pass, 800px no-h-scroll, audio-failure never dead-ends. FIXED: closed-drawer phantom tab stops P2 (681e840 — CSS visibility, ~13 stops → 0 in a 60-Tab walk) + drill focus-drop P3 (0fea475 — `#qPrompt`/`#resHeading` tabindex=-1 + focus). LOGGED: gold-on-light contrast P3 (2.1–2.75:1; `#8a6216` precedent in-file), 30px touch-target P3, suite-wide uploader-keyboard P3 (shared markup, 7 carriers), P4 polish batch, and the **drawer-shell P2-verify for torah_trainer + dashboard** (same copied shell). Prior: **S40 — 2nd pass on hebrew_dictionary.html (cold since S24; a11y last at S12): strong shape.** Word-card `role=button`+Enter/Space, the word-detail modal (trap+Escape+focus-restore), `bindHdrA11y` collapse headers, `aria-expanded`/`aria-pressed` sync, and `lang="he"` all correct; **both S12 leftovers closed** (`_scrollBehavior()` gates smooth scrolls; `.shoresh-pat.filled` keyboard-operable). Fixed 5 of 6 findings: `#resultCount` live region + `#searchInput` aria-label + shared `#a11yStatus`/`announce()` copy announcements (dd21b3c P3); `:focus-visible` ring on the 2 search inputs + RM caret coverage (85f37ab P4). Logged the gold-on-light contrast (footer `a` ≈2.7:1, `.shoresh-pat.filled .pat-match` ≈2.5:1) as a candidate. An **adversarial verification workflow (5 read-only agents)** confirmed the P1 + Pass C fixes correct/regression-free and drove a completeness commit (e6c0128 — `#shoreshInput` aria-label = the P3 twin the audit missed, `copyBulkText` announce, `.pos-arrow` RM), plus upgraded the torah error-copy candidate to **P3** (over-broad `fetchAndRender` try). The suite-wide "shadowed-global helper" grep sweep was CLEAN. **S27 ran the first 2nd-pass — accessibility Pass C on hebrew_blend_generator.html** (the flagship, biggest recent churn). Mostly CLEAN: no keyboard traps (only 7 `.rw-section-header` divs are click-only, all `_wireCollapseHdr`-wired), zero `outline:none`. Fixed icon-button names (`🔊`, bingo `▲`/`▼`) + 3 reduced-motion escapes (cardFlip, infinite timer blink, chevron); logged 4 fresh Candidates. **S28 then fixed** accessible names (`aria-label`) for all 22 unnamed config `<select>`/`<input>`/`<textarea>` controls (WCAG 2.5.3), and **S29 fixed** the segmented-control `aria-pressed` sweep (all 5 pill classes, via one `_syncPillAria` helper + delegated listener + `updateModeVisibility`/`applySettings` hooks). Remaining from this Pass C: inverted bingo spinner **P4**, contrast suspects **P4** (small tail). **S25–S26 closed BOTH S21 SVG-stage keyboard gaps** — Font Maker Edit-points mode now supports keyboard node selection (`,`/`.` cycle, `i` insert; S25), and the combined-sheet crop marquee is now keyboard-drivable (arrows move, +/− resize, Enter crop, Esc cancel; S26). The Font Maker SVG stage (node editing + cropping) is now fully keyboard-operable. S25 also added the torah empty-state (S24 Pass F backlog). **S21 ran Pass C on Hebrew_Font_Maker — the last un-audited tool; accessibility now covers ALL tools.** Font Maker was in strong shape (grid tiles, tablists, all modals' focus-trap, universal-selector RM block all already correct per the audit); fixed 3 small P3s (shared gold focus-ring, `lang="he"` on Hebrew glyph tiles, `aria-pressed` on toggles — 0487d47) and logged 2 P2 SVG-stage keyboard gaps (crop-box, node selection) + 1 P4 dead handler as Candidates. The S18 torah_trainer backlog was **closed out in S20** — verse-word roving tabindex (9790276, P2), radio-group contrast (0a5d491; the other muted suspects measured already-passing), and the grouped small items: main/skip link, lang="he", karaoke aria-current, drawer/caret RM (ddb6230). The dictionary's S12 `.shoresh-pat.filled` leftover also closed (315fea9). Last full-tool run 2026-07-06 **S18** on torah_trainer.html (keyboard-only + contrast survey). Fixed then: the settings-drawer focus management (0362ed9, P2) and the 8 panel-title collapse headers (205fa11, P3). Confirmed good already: `input:focus-visible + .slider`, tour keyboard+focus, tip-wrap keyboard pattern, audio-button aria-labels, native pickers/toggles. Still un-audited: Font Maker. Prior S12 on hebrew_dictionary.html (keyboard-only + measured contrast, light/dark × 1280/800). All 5 findings fixed same-session (4157ecc, 6d83604, 7168b74, b532e86, 373b455); 2 P4 leftovers logged (RM scrollIntoView gaps, `.shoresh-pat.filled` tiles). Confirmed clean: cards, word modal (trap/restore), bindTip source engine, tour focus handling, native-checkbox filters; no `.slider` switches. Prior: flash_cards → dashboard S8 → generator S9. Still un-audited: torah trainer, Font Maker.

### Discovery-pass rotation (run one per session, stalest first)
- B console/error audit: 2026-07-22 (**S131**: 11th run — first over the S121→now surface (the merged frozen-Generate-footer generator work + the out-of-band FM/flash_cards design-call batch c1591a8); **CLEAN** — 24 load runs (12 pages × light/dark) + 1 interaction/tool (index gear→modal, generator `generate()`+frozen-footer button, dashboard drawer, flash_cards Start→flip, dictionary search, torah settings, trope drill, FM `.ltile`→Alef) = **0 pageerror / 0 real console errors-or-warnings / 0 failed-local / 0 `[i18n] miss`** on load AND after interaction. 0 new candidates.) Prior 2026-07-20 (**S121**: 10th run — first over the S108→S120 surface (S109–S120 loop + the S117–S120 branch changes); **CLEAN** — 24 runs (12 pages × light/dark) load + 1 interaction/tool (index gear→modal, generator Generate, dashboard settings, flash_cards Start→card, dictionary search, torah gear→drawer, trope drill tab, FM `#letterGrid .ltile`→Alef, resources filter, contact input) = **0 pageerror / 0 real console errors-or-warnings / 0 failed-local / 0 `[i18n] miss`** on load AND after interaction; interaction firing independently confirmed. 0 new candidates.) Prior 2026-07-19 (**S108**: 9th run — first over the S98→S107 surface; **CLEAN** — 24 runs (12 pages × light/dark, load + 1 interaction/tool: index gear, generator Generate, dashboard settings, flash_cards start, dictionary search, torah settings, trope drill, FM tile-click, resources) 0 pageerror / 0 `[i18n] miss` / 0 failed-local / 0 non-noise console; an **unfiltered** distinct-message capture confirmed the only output is expected external-abort noise (Google-Fonts/gtag `net::ERR_FAILED` everywhere + torah's documented offline Sefaria/ESM artifacts). 0 new candidates, nothing to fix. Prior 2026-07-19 (**S98**: 8th run — first over the S89→S97 loop surface; **effectively CLEAN** — 12 pages × light/dark = 24 runs over local HTTP, load + one interaction per tool; **pageerror==0 / 0 failed-local requests / 0 `[i18n] missing key`**; the only non-abort console output = torah_trainer's documented offline artifacts (Sefaria `fetchSefariaText` "Failed to fetch" + hebrew-transliteration esm.sh/jsdelivr ESM-CDN warnings — all external, route-aborted, gone over a real network). No regression from S89→S97; no fix forced.) Prior 2026-07-16 (**S88**: 7th run — first over the S79→S87 surface; **CLEAN**. All 12 root pages × light/dark = **24 page-runs over local HTTP**, load + one interaction per tool (index gear, generator letter-tile, dashboard drawer, flash_cards mode-pill, dictionary search, trope drill, FM letter-tile). **`pageerror==0` on every run; 0 local-request failures; 0 `[i18n] missing key` warnings** (the S78 pre-ready-flood fix held). Console-error counts == the route-aborted external count on 11 of 12 pages; the only non-abort output = **torah_trainer**'s documented offline artifacts (Sefaria API `Failed to fetch` via `fetchSefariaText` + the hebrew-transliteration `esm.sh`/`jsdelivr` ESM-CDN load warnings) — all external, gone over a real network. No regression from S79–S87 (S85 FM debounce, S86 dashboard weather-heal / flash_cards pills, S87 trope/torah/index). No fix forced. Prior 2026-07-13 (**S78**: 6th run — first over the site-wide i18n rollout + S70→S77; **`pageerror==0` on all 12 pages**, and the only non-env console output was a **suite-wide pre-ready `[i18n] missing key` flood** — every page's init calls `I18n.t()` before `/locales/*.json` loads, so the empty `dict` makes all **239 unique keys** look missing (verified every one present in en.json → pure timing, **0 genuine gaps**). Fixed in the shared runtime (js/i18n.js `dictLoaded` gate, 0f72e1d): pre-ready warning suppressed, post-ready genuine miss still warns; re-run → **0** missing-key warnings, detection preserved. `i18n-test.html` excluded (intentional harness miss). Prior 2026-07-11 (**S69**: 5th run, first over the S58–S68 surface — all **12** pages over local HTTP, light-1280 + dark-800, load + one interaction per tool with the interactions **verified to actually fire** (dictionary → shoresh mode active + 14-word family = the S68 change; generator → worksheet built; flash_cards → deck started; FM → alef selected via the relocated mark grid; trope → drill started; + index gear, dashboard drawer, torah parsha-select) — **CLEAN: 24/24 page-runs, 0 pageerrors, 0 non-environmental console errors, 0 local request failures**; noise histogram = 100% route-aborted externals (every page's console-error count == its external `requestfailed` count; torah's +2 are the documented offline "load data files"/Sefaria messages). No regression from the S58–S68 changes (shoresh-select, FM mark-editor relocation, trope SEO, S59–S61 refactors). Prior **S57**: 4th run, first over the S50–S56 surface — all **12** pages (trope tutor included) over local HTTP, light-1280 + dark-800, load + one interaction per tool (incl. trope drill start+answer, torah parsha select, generator Generate) — **CLEAN: 24/24 page-runs, 0 pageerrors, 0 non-environmental console errors**; the trope tutor's 77–82 noise lines/run histogrammed to 100% `net::ERR_FAILED` route-aborted externals (fonts/gtag/PocketTorah MP3 retries — the documented degradation path). Prior **S47**: load + one interaction, all 11 pages, light-1280 + dark-800 — **CLEAN**, 20/22 page-runs clean; the only flags were torah_trainer's "Failed to load data files" + hebrew-transliteration ESM-CDN warnings — the documented env artifact (local `data/parshiyot` fails over `file://`; ESM CDN route-aborted), **confirmed gone over a local HTTP server** (page renders; only the remote Sefaria API fetch remains, expected offline). No regression from S38–S46. Prior **S37**: all 11 pages, CLEAN, 0 pageerrors across 22 page-runs; same env noise confirmed gone over HTTP. Prior **S20** also CLEAN.)
- A recurring-pattern sweep: 2026-07-21 (**S126**: swept the 3 active consequence-critical classes over `fd0fd11..HEAD` — S115 FM v5.5 export chain + S116–S125 loop changes + the substantial outside-loop **Advanced-TTS-added-then-removed / Basic-TTS** drift (Kokoro-ONNX added 4032210, iterated, **fully removed** 43b64e6, leaving only a Basic-TTS "no Hebrew voice" notice 349c2c9). **All 3 CLEAN, 0 hits:** unescaped-input (dashboard Now/Next guard `esc()`-wraps every dynamic value — only trusted `I18n.t` raw; message-surface `innerHTML` is pre-existing teacher-local contenteditable, `_dashTextEmpty` reads `textContent` off an inert `<template>`; FM's one new interpolation is a trusted anchor `Object.assign` clone; TTS `SpeechSynthesisUtterance(text)` is not an HTML sink; new fetches same-origin `data/*.json`), localStorage-vs-AllTools (0 new keys — Basic-TTS reuses the registered `hebrewDictionary_ttsRate`, `_heVoiceWarned` is a JS var), destructive-bulk (FM v5.5 `emitBaseAnchors` shallow-clones, never mutates a stored anchor). Also verified the Advanced-TTS removal **cleanly reverted its CSP** (no stale `wasm-unsafe-eval`/Hugging Face) + left no orphaned CDN refs. Streaks: localStorage 8→9, unescaped 4→5, destructive 8→9 (all KEPT ACTIVE). Paired the clean sweep with the FM 9-tooltip pure-wiring i18n fix (1984167) per the S41/S83/S103/S114 precedent.) Prior 2026-07-20 (**S114**: swept the 3 active consequence-critical classes over `36161c0..fd0fd11` — the substantial outside-loop **FM v5.2→v5.4** (combined-form mark-grid tiles that replace the redundant generic tiles, faithful שׁ/שׂ dot-stacking checks, the וֹ ligature export shaping-guard, the v5.4 Holam-Haser-on-vav split) + S104–S113 loop changes — inline. **All 3 CLEAN, 0 hits:** unescaped-input (new FM tile/stacking interpolations are trusted static codepoints `pc.target` / `esc()`-wrapped form names / trusted i18n strings; no new untrusted `JSON.parse`/`Object.assign`), localStorage-vs-AllTools (0 new keys — all new state is `project.*` → IndexedDB autosave, FM backup-exempt), destructive-bulk (v5.4 `cholamScale`/`holamScale` migration `== null`-seeded from a prior FB4B dot size so old saves look unchanged; `precomposed` backfill `=== undefined`-guarded — never clobbers a customized anchor). Streaks: localStorage 7→8, unescaped 3→4, destructive 7→8 (all KEPT ACTIVE). Paired the clean sweep with the user-directed FM template-literal i18n wiring (c788498) per the S41/S83/S103 precedent.) Prior 2026-07-19 (**S103**: swept the 3 active consequence-critical classes over the surface changed since S93 — the outside-loop **FM v5.0→v5.1** (precomposed שׁ/שׂ+ḥolam anchors, spacing-preview nikkud, Save-as-a-copy) + **Resources font-gallery** (Lakhish Bold + `fontSampleFor` mark-strip) + **S94–S102 loop changes** (`a22bfc0..HEAD`, 21 files) — inline. **All 3 CLEAN, 0 hits:** unescaped-input (new user-data `innerHTML` = FM `meItem.name`/`lname`, both `esc()`; else trusted-static `HELP_CONTENT`/`buildAboutBody` with `esc()`'d i18n; `JSON.parse`=clones; resources preview→`textContent`), localStorage-vs-AllTools (0 new keys), destructive-bulk (FM precomp reset single-target `udDo`-wrapped, `data.precomposed` backfill null-guarded). Maintainer-suggested falsy-zero spot-check on new FM coords = clean (`!= null`/`Number.isFinite`; stays retired). Streaks: localStorage 6→7, unescaped 2→3, destructive 6→7 (all KEPT ACTIVE). Paired the clean sweep with the top logged P3 (dictionary Word Lists top-level button, 439bb47) per the S41/S83 precedent.) Prior 2026-07-17 (**S93**: swept the 4 active classes over the surface changed since S83 — the outside-loop **dashboard movable-widget-panels feature** (639fcf8 + 53e50fc), the S84–S92 loop changes, and the 07-17 maintainer commits (Referrer-Policy/fonts/locales) — inline (the feature was the sole substantive new code). **All 4 CLEAN, 0 hits:** `settings.panelLayout` rides in the registered `hebrewDashboard_settings` blob (localStorage), load via `ivritSafeParse`+`normalizePanelLayout` (unsafe-parse), array-index zone reorder with `===undefined` guards (falsy-zero), 0 new `innerHTML` (unescaped-input), `resetPanelLayout` a reversible clearly-labeled view-pref below the bar (destructive-bulk — logged P4). **falsy-zero → RETIRED** at its 3rd consecutive clean sweep (S73/S83/S93; correctness-scoped, not consequence-critical); the 3 consequence-critical classes stay ACTIVE). Prior 2026-07-15 (**S83**: swept the 4 active classes over the never-A-swept surface since S73 — the outside-loop **Font Maker v4.18→v4.26 feature work** (zoom slider, copy/clear outline on Align, steadier handle rounding, straighten, round corners, guide thickness, fit margins, vowel+trop pairing, edit-points toggle) + the loop's S74–S82 changes — via 2 recon agents. **All 4 CLEAN, 0 hits:** falsy-zero (new FM geometry guards a legal 0 via `typeof`/slider-floors/default==0; near-miss `setVowelPairMode Math.abs(dx)||70` = intentional re-seed), localStorage-vs-AllTools (**0 new keys** — FM pairing is `project.font.trop.vowelPair` project-file data), unescaped-input/unsafe-parse (new innerHTML = trusted `I18n.t`/consts/`esc()`-ed names; parses = clones or `ivritSafeParse`), destructive-bulk (new FM Align/round-corners/copy-outline/straighten per-item + `udDo`-undoable, `applyI18n` hooks read-only). Streaks: falsy-zero →2, localStorage →5, unescaped →1, destructive →5; **no retirements** (falsy-zero at 2; the 3 consequence-critical stay ACTIVE). Per the **S41 precedent**, paired the clean sweep with the S82-logged generator footer/nav contrast fix (449b587). Prior 2026-07-13 (**S73**: swept the 5 active classes over the large never-A-swept surface since S64 — the **whole 2026-07-12 site-wide i18n rollout (all 12 pages) + Pass K + S68/S71/S72 diffs** — via 2 recon agents. **4 CLEAN** (falsy-zero: `substitute()` + `===1` plurals preserve 0; localStorage: sole new key `hebrewBlender_lang` = per-device pref, exempt like darkMode; destructive-bulk: `?wl=` select-only + `applyI18n` read-only; shadowed-global: one `esc`/`applyI18n` per file → **RETIRED at 3-clean**). **unescaped-input/unsafe-parse: 1 P2 stored-XSS + 1 P3, FIXED** (a87745e) — FM imported-project glyph/mark `.name` → `innerHTML` unescaped; **the recon flagged 5 `gName` sites but UNDERCOUNTED — the end-to-end exploit test (malicious `.name` via `applyProjectData`) caught 5 more direct `${l.name}`/`${it.name}` sinks**, all 10 name sinks + 3 precomp spans now `esc()`'d, whole-file grep clean. Parse surface uniformly `ivritSafeParse`/`ivritSafeAssign`. Prior 2026-07-10 (**S64**: swept the 7 active classes over the 26-commit S52→HEAD surface (generator chunked build, torah nikkud/trope CSS-var decouple, `showAppToast` block, `--gold-text` pill, and the never-loop-swept **FM v4.10–v4.15 Firefox auto-detect-box debug code**) via 3 agents — **6 of 7 CLEAN; falsy-zero 1 hit** (FM `buildUfoFontInfo` `spec.version || 1.0`, the S52-missed sibling, fixed b9c1aa3). **Retired listener/interval + JSON-LD parity** (3-clean, structural/cosmetic); **kept unescaped/unsafe-parse + localStorage-vs-AllTools + destructive-bulk ACTIVE past 3-clean** (consequence-critical carve-out — security/data-loss, re-sweeps cheaply). FM `#addebug` deep-dive CLEAN (URL-hash gate, dynamically-built `#adDbg`, never user-visible; `.ad-box !important` is the intentional Firefox fix); logged the obsolete debug scaffolding as a P4 prune. Prior 2026-07-10 (**S52**: all 8 active classes over everything since S41 — the never-swept full trope_tutor.html, torah trope-coloring, FM v4.3–v4.8, S50/S51 diffs, build-trope-index.mjs. **Cleanest scope yet: 1 P4 hit** (FM version `|| 1.0` promotion, fixed d343b1b with the adjacent rollback-gap note), 7 classes CLEAN; trope_tutor's documented safety patterns verified genuinely implemented (all 5 AllTools sites, safe-parse everywhere, DOM-built rendering, leak-free audio, JSON-LD↔code parity, confirm-gated resets); **workMode retired** (3 clean). Prior 2026-07-09 (**S41**: user-directed sweep of the never-swept FM v3.9–v4.2 feature code (auto-detect, full node-editor, pen, corner-fillet, waterfall) across 6 in-scope classes — **CLEAN, no P1/P2/P3**; retired undo-wiring + slider-commit stayed dead over the largest new-code surface since retirement. Ran in place of the strictly-stalest G, by user direction; G remains stalest.) Prior 2026-07-08 (**S36**: all 8 classes re-swept over the S23–S35 additions (flash_cards custom card-count, Font Maker keyboard crop/node-insert, generator a11y + print CSS, suite-wide dark-mode `aria-pressed`) — **no P1/P2**; 1 adjacent robustness finding fixed (unclamped `cardCount` on restore → tab-freeze, 952edcd P3). All 8 classes CLEAN; **retired undo-wiring + slider-commit** (3 consecutive clean). Prior **S22**: all 8 classes re-swept over the hardening-pass + Word Lists + S21 additions — found + fixed a **P1** Real Words XSS (46fec2f) + defensive Flash Cards hardening (22efcc1); localStorage-registry, Font Maker undo/slider/bulk, falsy-zero all CLEAN; one likely-intentional aside logged (`hebrewFontMaker_autosave` erase omission). Prior **S17**: all 8 bug classes re-swept over the S12–S16 additions — found + fixed a real P2 stored-XSS gap (nikud color-override → style attribute, all 4 color tools, 6bd1b7b) and a P4 erase-list gap (b37cc42); the other 6 classes CLEAN incl. every named recent addition. Prior S10: the nine S8/S9 a11y commits — CLEAN; listener/interval accumulation swept — CLEAN; localStorage registry — `eraseAllSettings` flag gap fixed 0d63af0)
- C accessibility (one tool): 2026-07-20 (**S125 — Hebrew_Font_Maker.html, first-ever dedicated C on FM: **a11y-strong**. Headless enumeration of 247 visible interactive controls → only **3 nameless** (all range sliders) + 0 weak icon buttons; modal focus-trap, focus-visible indicators (`.ad-box:focus-visible`, `.me-coord:focus-within`), wrapping-`<label>` coordinate inputs, `#nodeEditToggle` dynamically labeled — all fine. **2 fixes:** every FM range slider now has an accessible name — the shared `vSlider()` helper rendered labels as unassociated spans (systemic), fixed via `aria-label` reusing existing label keys (f5d4080 vSlider+#zoomRange; 18d9a5d spacing/kern) → nameless 3→0. **Deferred:** interactive editing-stage `role`/`aria-label` (design-call). `sw.js` v369→v370; no FM version bump. Prior **S113 — trope_tutor.html, first dedicated C on the tutor: **a11y-strong** — APG keyboard / roving-tablist / radiogroup, drill `<button>`s w/ aria-label + aria-pressed, settings-drawer Escape + focus-restore, all form controls `<label for>`, contrast clean both themes; **1 fix** = completed reduced-motion coverage (`.settings-close` + `.radio-group label`, d04b45c). `sw.js` v355→v356. Prior **S102 — form-control a11y convergence (generator/dictionary/torah + flash_cards)** — the two logged S100 Pass-F P3s + the S82 touch-target tail, scoped via 2 read-only Explore surveys. **Font-size slider a11y (6e26a4d):** the 3 laggard sliders had no accessible name + resets no aria → associated each existing visible label with its slider (`<label for>`; WCAG-correct 2.5.3, name==visible text) + `aria-label`/`data-i18n-aria-label` on each reset reusing existing "aria-label only" title keys → **0 new keys**. **flash_cards touch targets (0e83432):** `.tour-x` 26→30px (+ coupled `h4` clearance) + `.btn-xs` ~20→30px min (WCAG 2.5.5/2.5.8). **Dictionary Print button (1a37213):** the print-CSS-but-no-button laggard got a visible 🖨️ Print in the results toolbar (torah `printReading()` canonical, click-time guard via SR `announce()`, print-hidden; 3 new keys). **Deferred:** flash_cards card-face `aria-live` (S82 design call — survey mapped the path). Prior 2026-07-16 (**S92 — first-ever dedicated C on `resources.html` + `index.html`, the two never-C'd chrome pages (the freshest-for-C). 2 parallel Explore audits verified against live code; reduced-motion / focus-rings / i18n-wiring already clean on both. **2 P2 keyboard/focus blockers fixed:** resources Fonts-view actions were mouse-only `<a onclick>` → `<button>` (806d23a); the index AllTools gear modal had no focus management → `role=dialog` + move-in/restore + Tab-trap (4c59f00). Plus resources `lang=he` + modal dialog semantics (6de9915), index `main` landmark + mode-toggle `aria-pressed` (d94aa5e), and a **suite-wide gold-on-light contrast convergence** (b00428f, the S63 pattern): the chrome pages (index/resources/contact/privacy/404) never got the `--gold-text` token the 6 tool pages carry, so their always-visible body/footer links failed WCAG 1.4.3 → converged onto `--gold-text` (light #7f5a13 ~6.2:1), also clearing the S72 torah footer gold-tail + the FM footer. Logged P4s: resources Suggest-modal focus-management, view-toggle/filter/pill `aria-pressed`, the `target=_blank` cue. Prior 2026-07-14 (**S82 — flash_cards.html, first dedicated a11y pass since S8** (the oldest of any tool), over its S35/S47/S49/S71/S72 growth. **CLEAN:** reduced-motion (global `*` neutralizer covers every animation), `lang="he"` (all chokepoints), keyboard operability (card flip via global handler, `bindTileA11y` tiles, accordions w/ synced `aria-expanded`, tooltips w/ Escape, the guided tour dialog + focus save/restore, Present/fullscreen native-FS + `aria-pressed` sync), no `outline:none`, real `:focus-visible` rings. **1 fix:** gold-on-light contrast → `--gold-text` (footer + 7 nav links + streak score + navy skip pill; 9c10a65, 6.22:1 light, **dark unchanged**, `pageerror==0`). **Logged:** P2 overlay-modal dialog semantics (5 JS builders, shared-helper — likely cross-tool); generator footer/nav contrast twin + the translit-credit CSV; ~18 pill groups no `aria-pressed`; card/score no `aria-live`; profile-delete/nav labels; sub-30px touch targets. Prior 2026-07-13 (**S72 — torah_trainer.html, 2nd dedicated pass (last S18); over local HTTP, both themes + Hebrew RTL. Heavily hardened → NO P2 keyboard/focus blocker remained** (drawer trap+return+Escape, roving-tabindex reading, panel-title button semantics, labeled audio bar+region, tour dialog+keys, `lang="he"` chokepoints, RTL drawer-flip all already correct). **2 fixes:** G2 (471e765, P4-but-free) — reduced-motion block missed `.tt-word` karaoke + audio-caret transitions, one-line neutralizer, no keys; G1 (ecf9faf, P3, the RTL-rollout regression) — 5 `setAttribute('aria-label')` sites hardcoded English (invisible to `check-i18n`), now `I18n.t()` (4 reuse existing keys, 1 new `torah.settings.color_for_aria`). Logged G3 gold-on-light contrast sweep (P3, design call) + G4 gearBtn aria polish (P4). Prior **S63**: focused audit of the **font-upload affordance across all 7 carriers** (cross-tool, not one tool — the top S62 P3 was suite-wide). Found the shared upload pill's `var(--gold)` text **failing WCAG 1.4.3 on light** (~2.5–2.9:1) in 6 pills + trope's `.font-upload-label` structural drift; fixed via a suite-wide **`--gold-text`** token (light `#7f5a13` / dark `var(--gold)`) + trope chip convergence (5797ca0, measured 4.93–6.22:1 light after). `index.html`'s My-Fonts manager label passes (`color:var(--text)`), out of scope. Prior **S51: trope_tutor.html — FIRST pass, full drill over HTTP; 1 P2 fixed (drawer tab-order), 1 P3 fixed (drill focus), 3 P3 + P4 batch logged; accessibility now covers ALL 8 tools at least once.** Prior **S40: hebrew_dictionary.html — 2nd pass, strong shape** — `#resultCount` live region + `#searchInput` aria-label + shared `#a11yStatus`/`announce()` copy announcements (dd21b3c P3), `:focus-visible` ring + RM carets (85f37ab P4); gold-on-light contrast logged; also registered + swept the new "shadowed-global helper" pattern. Prior **S27**: hebrew_blend_generator.html — first 2nd-pass; icon-button names + 3 reduced-motion escapes fixed, 4 fresh candidates logged (P2 unnamed selects, P3 segmented aria-pressed, P4 inverted spinner, P4 contrast)**. History: flash_cards → dashboard S8 → generator S9 → dictionary S12 → torah_trainer S18 → Font Maker S21 = all tools Pass-C'd once; **generator 2nd pass S27**.)
- D performance (one tool): 2026-07-21 (**S128 — hebrew_blend_generator.html**, its 3rd dedicated D [S13/S44/S128], last S44 ~84 sessions ago = by far the stalest-profiled. **Measured effectively CLEAN, 0 fixes** — the long-parked **S44 Class-Set monolithic longtask** (1,305 ms @4× for the whole 40-version build) is **confirmed resolved by the S61 chunked build**: a max-stress 33,040-node build (blendCount 100 × Class-Set 40 × answer key) streams at ~90–100 ms/chunk @4× (variance to ~200–224 ms, 0 systematic >200), a typical 1,600-cell build has 0 longtasks >50 ms even @4×, @1× trivial. Boot has the standard @4×-only cold-load longtask (~225 ms, parked). `exportPDF` un-profilable (CDN unreachable). Logged the @4×-extreme per-chunk variance as informational-parked. No `sw.js` bump (ledger-only). Prior 2026-07-20 (**S117 — classroom_dashboard.html, 3rd dedicated D (last S35), the stalest-profiled tool + its movable-panels feature was never D-profiled. Thoroughly CLEAN; 1 fix (b5d4261).** Cold-load boot @1× max 195 ms (UNDER bar) / @4× 561 ms (parked; the "render-blocking Google Fonts" candidate half is now STALE — non-render-blocking since ~07-17). All interactions <200 ms @4×; movable-panels efficient (145 ms @4× for enter+3 moves). All-day stability: DOM/interval Δ0 over 40 cycles. Fixed the Now/Next per-second `innerHTML` rebuild via a `_lastNowNextKey` guard mirroring the S14 preview guard.) Prior 2026-07-19 (**S105 — hebrew_dictionary.html, first dedicated D since the S14 multi-tool pass** — the stalest-profiled tool. **Effectively CLEAN, 0 fixes:** 13,081-word corpus (2.9 MB); paginated `PAGE=100` + debounced search/gematria (150 ms). Cold-load 1036 ms @1× / 1170 ms @4× (one 517 ms @1× boot longtask = fetch+parse+build, progress-barred, `data/` out of scope). Per-interaction all <200 ms @1× (filter-all 70, search 62, one page 28); broadest filter/search cross 200 ms @4×-only (229/205 ms), both debounced — cost is ~all `Intl.Collator` × ~180k on the 13k sort (127 ms @4×; filter 4.4 ms). **Sort-branch hoist measured 0 ms → ruled out; collation-key precompute risks wrong Hebrew order → not clean.** Full-render-all pathological. `pageerror==0`; emoji mode not deep-profiled. Parked/informational, same category as the other cold-load longtasks.) Prior 2026-07-18 (**S95 — trope_tutor.html, FIRST-EVER dedicated D** (the only never-D’d tool, coldest by loop-touch S87) — effectively CLEAN baseline: desktop @1× 0 longtasks, only throttled-only cold-init boot longtasks @4× (max ~385 ms/~1146 ms total, same category as parked dashboard/FM cold-load), all interactions <200 ms @4× (drill 94 ms, renderLearn 6 fam 21 ms, startDrill 1.7 ms); 0 pageerrors; logged 1 P4-informational parked candidate. Prior 2026-07-16 (**S85 — Hebrew_Font_Maker.html, FIRST-EVER dedicated D** (FM's heavy work is lazy CDN/WASM + surveyed-exempt; its own-JS render pipeline + the new **pure-JS** Generate Bold had never been profiled). Headless over **local HTTP** (the page's root-absolute `/js/i18n.js` needs a web root — `file://` leaves `I18n` undefined and render paths throw), buffered-longtask + `MutationObserver` + CPU-throttle rig, light/dark × 1280/800 × 1×/4×, seeded a full alphabet of 300+140-pt traced contours (triggering the O(n²) self-intersection branch). **No in-scope block >200 ms:** heaviest Generate Bold live render **~92 ms @4× / ~23 ms @1×**; `boldGenApply` ≤150 ms @4× / ≤32 ms @1×; cold-init one boot longtask **438 ms @4× / 104 ms @1×** (one-time, throttled-only → **new P4**, same category as the dashboard cold-load). `pageerror==0` across the matrix. **1 P3 drag-responsiveness fix same-session** (2f59215): the Bold preview re-ran the deep-clone+offset+O(n²) warning scan over EVERY traced letter each slider frame (~65 ms/frame @4×) — debounced the full-corpus scan so each frame re-renders only the ≤5 preview glyphs (67→24 ms/frame @4× / 18→4 ms @1×; preview byte-identical; warnings settle ~130 ms after the slider stops). Font Maker is now Pass-D'd once (its geometry/render own-JS, the only in-scope surface). Prior 2026-07-13 (**S75 — flash_cards 2nd pass** (first + only prior D was S15; the last tool to get a 2nd D → **all 6 non-FM tools Pass-D'd twice**): **measured effectively CLEAN — no main-thread block >200 ms, `pageerror==0` @1×+4×, 0 fixes.** Headless over local HTTP, `PerformanceObserver('longtask')` + `MutationObserver`, 1× **and** 4× throttle, decks of 10/20/100. **S15 `buildPool`×3 re-measured negligible / no regression:** ~7.6 µs/card, `generateDeck(20)` 0.4/1.55 ms, `generateDeck(100)` ≤6.2 ms @4× (~30× under threshold) — hoistable via `buildDeckContext` but ~1–5 ms/deck payoff, parked P4. Cold load TTI 1410 ms @1× (one 68 ms load block <200 ms; @4× one one-time 245 ms init, throttled-only P4). Emoji `fetchEmojiData` 46/75 ms one-time (pool ctx-cached); words `JSON.parse` 17/74 ms sync (the 2.9 MB load is async network wait, `resolveWordsPool` runs on setup not the loop). **Idle CLEAN** — running timer swaps one `#timerDisplay` text node/tick (5 mutations/3.6 s), present/fullscreen pure CSS (0 rAF, only `setInterval` is the timer). No code — 2nd-pass measured clean like S35/S44/S66. Prior **S66 — torah_trainer 2nd pass** (first-ever D was S16 on this tool): effectively CLEAN — **empirically confirmed the S60 nikkud CSS-var decouple held** (color/mode/scheme/dark = 0 reading mutations / 0.1–2.7 ms vs renderText 354–356 mutations / 26–194 ms, across light/dark × 1280/800 × 1×/4×); idle 0 mutations/3 s (no `setInterval`); karaoke O(log n) + early-out (static, audio env-blocked); worst-case one-shot full-parsha render (~128–194 ms @4×) logged P4 (large-refactor-only); CSS-var restyle cost informational. No code. History: dictionary → generator → dashboard → flash_cards → torah_trainer = all 6 Pass-D'd once; 2nd passes: dictionary S54, generator S44, dashboard S35, **torah_trainer S66**. **flash_cards is the only tool without a 2nd D pass** (last S15). Prior **S54 — dictionary 2nd pass** (first-ever D was on this tool): effectively CLEAN — desktop worst block 55 ms; typing 0 longtasks (150 ms debounce load-bearing + intact); filter toggles @4× 182–197 ms decomposed to the 'he' sort (155 ms → fixed with a cached Intl.Collator, fb38fda — measured 79 ms after); Word Lists capped/denormalized, no full-corpus work; modal ≤18 ms; **idle 0 mutations/0 longtasks**; both S12 fixes HELD; no new full-corpus chains since S12. Prior **S44 — 2nd pass on hebrew_blend_generator.html**: re-quantified the known Class-Set 40 build — 13,840 DOM nodes in one **353 ms desktop / 1,305 ms @4×** longtask (worse than the S13 210/368 ms estimate; tool grew); **idle CLEAN** (0 mutations/2.5 s); cells build detached with cheap per-cell work → **no small win**, the fix is the parked chunked-render refactor (contract-changing, large). One P4 rw-CSS cleanup shipped alongside (51a2e97). Prior **S35 — 2nd pass on classroom_dashboard.html, CLEAN**: idle recurring churn minimal (~4 nodes/4 ticks — clock text only), 0 idle longtasks — the S14 per-second schedule-preview rebuild fix held; cold-load 321 ms @4× / 89 ms desktop, the already-logged P4, re-measured. History: dictionary → generator → dashboard → flash_cards → torah_trainer = all 6 tools Pass-D'd once (cycle complete S16); dashboard 2nd pass S35.) **measured effectively CLEAN — no main-thread block >200 ms, `pageerror==0` @1×+4×, 0 fixes.** Headless over local HTTP, `PerformanceObserver('longtask')` + `MutationObserver`, 1× **and** 4× throttle, decks of 10/20/100. **S15 `buildPool`×3 re-measured negligible / no regression:** ~7.6 µs/card, `generateDeck(20)` 0.4/1.55 ms, `generateDeck(100)` ≤6.2 ms @4× (~30× under threshold) — hoistable via `buildDeckContext` but ~1–5 ms/deck payoff, parked P4. Cold load TTI 1410 ms @1× (one 68 ms load block <200 ms; @4× one one-time 245 ms init, throttled-only P4). Emoji `fetchEmojiData` 46/75 ms one-time (pool ctx-cached); words `JSON.parse` 17/74 ms sync (the 2.9 MB load is async network wait, `resolveWordsPool` runs on setup not the loop). **Idle CLEAN** — running timer swaps one `#timerDisplay` text node/tick (5 mutations/3.6 s), present/fullscreen pure CSS (0 rAF, only `setInterval` is the timer). No code — 2nd-pass measured clean like S35/S44/S66. Prior **S66 — torah_trainer 2nd pass** (first-ever D was S16 on this tool): effectively CLEAN — **empirically confirmed the S60 nikkud CSS-var decouple held** (color/mode/scheme/dark = 0 reading mutations / 0.1–2.7 ms vs renderText 354–356 mutations / 26–194 ms, across light/dark × 1280/800 × 1×/4×); idle 0 mutations/3 s (no `setInterval`); karaoke O(log n) + early-out (static, audio env-blocked); worst-case one-shot full-parsha render (~128–194 ms @4×) logged P4 (large-refactor-only); CSS-var restyle cost informational. No code. History: dictionary → generator → dashboard → flash_cards → torah_trainer = all 6 Pass-D'd once; 2nd passes: dictionary S54, generator S44, dashboard S35, **torah_trainer S66**. **flash_cards is the only tool without a 2nd D pass** (last S15). Prior **S54 — dictionary 2nd pass** (first-ever D was on this tool): effectively CLEAN — desktop worst block 55 ms; typing 0 longtasks (150 ms debounce load-bearing + intact); filter toggles @4× 182–197 ms decomposed to the 'he' sort (155 ms → fixed with a cached Intl.Collator, fb38fda — measured 79 ms after); Word Lists capped/denormalized, no full-corpus work; modal ≤18 ms; **idle 0 mutations/0 longtasks**; both S12 fixes HELD; no new full-corpus chains since S12. Prior **S44 — 2nd pass on hebrew_blend_generator.html**: re-quantified the known Class-Set 40 build — 13,840 DOM nodes in one **353 ms desktop / 1,305 ms @4×** longtask (worse than the S13 210/368 ms estimate; tool grew); **idle CLEAN** (0 mutations/2.5 s); cells build detached with cheap per-cell work → **no small win**, the fix is the parked chunked-render refactor (contract-changing, large). One P4 rw-CSS cleanup shipped alongside (51a2e97). Prior **S35 — 2nd pass on classroom_dashboard.html, CLEAN**: idle recurring churn minimal (~4 nodes/4 ticks — clock text only), 0 idle longtasks — the S14 per-second schedule-preview rebuild fix held; cold-load 321 ms @4× / 89 ms desktop, the already-logged P4, re-measured. History: dictionary → generator → dashboard → flash_cards → torah_trainer = all 6 tools Pass-D'd once (cycle complete S16); dashboard 2nd pass S35.)
- E freshness/site-health: 2026-07-22 (**S132**: 12th run — first over the S122→now surface (S123–S130 loop + the merged frozen-footer generator work + the out-of-band design-call batch); **mechanical CLEAN** — `update-sitemap` no-op (all lastmod==git); SW `CORE_ASSETS` 25/25 resolve + all 12 production pages precached; 0 broken internal links (12 pages); `THIRD_PARTY_LICENSES.md` covers every CDN dep + all 5 bundled fonts; sitemap `<loc>`=11 (404/i18n-test correctly excluded); 3/3 manifest icons resolve. 0 new candidates; the session's 1 fix was the flash-cards a11y-tail (SR-announce lang-awareness, 4af7b78).) Prior 2026-07-20 (**S122**: 11th run — first over the S109→S121 surface; **mechanical CLEAN** — `update-sitemap` no-op (all lastmod==git); SW `CORE_ASSETS` **25/25 resolve** + every root HTML precached bar the intentional i18n-test.html (12/13) + **locales `/locales/{en,he}.json` precached** (no offline i18n gap); **0 real broken links** (Node scan of **410** local `href`/`src`/`url()` refs across 12 pages; 5 regex false-positives from `data-rwsrc="all|lists|imported"` matching `src=`); all **5** bundled fonts (Frank Ruhl Libre, Lakhish Bold, Reuben, TzviScript, TzviScript Stroke Guide) licensed; every CLAUDE.md-referenced `docs/*.md`+`scripts/*` exists; CLAUDE.md has no `?wl=` claim to drift. **1 fix (717c53b):** README doc-drift — FM **v4.29→5.5** (the recurring Pass-E FM-version drift, last bumped S89) + added the missing **Lakhish Bold** to the `fonts/` list. Other README version-claims spot-checked accurate (flash_cards present mode now exists, dashboard 13-font, 54 parshiyot, 13,081 entries). `sw.js` NOT bumped (README not precached). Prior 2026-07-19 (**S109**: 10th run — first over the S99→S108 surface; **mechanical CLEAN** — `update-sitemap` no-op (all lastmod==git), SW `CORE_ASSETS` 27/27 resolve + every root HTML precached bar the intentional i18n-test.html, **0 broken internal links** (Node scan of 344 local `href`/`src`/`url()` refs across 12 pages; only JS template-literal/code false-positives), all 5 bundled `fonts/*.ttf` licensed. Then the maintainer picked **dashboard copy polish** (74c8f76): accurate 'Hebrew classroom' starter + a new-teacher schedule preset hint, closing 2 S107 Pass-H candidates. Prior 2026-07-19 (**S99**: 9th run — over the S89→S98 loop surface; **mechanical site-health CLEAN** — 25 SW `CORE_ASSETS` resolve + every root HTML precached (only i18n-test.html excluded); 11 sitemap `<loc>`→files + all `<lastmod>` == git (update-sitemap a no-op); **0 broken internal `href`/`src`**; 18 apple-touch splash PNGs + manifest icons + og/twitter images resolve; `THIRD_PARTY_LICENSES.md` comprehensive (all CDN libs/APIs/fonts); README/CLAUDE-referenced scripts+data exist. **1 tiny nit logged** (maintainer legal call, not fixed): `fonts/LakhishBold.ttf` has no explicit license line. No outside-loop drift since S98 → no reconciliation needed. Prior 2026-07-16 (**S89**: 8th run — over the S79→S88 + outside-loop PR #98 surface — mechanical site-health **CLEAN** (SW `CORE_ASSETS` **25** entries both directions @v320 all resolve + every root HTML precached, only intentional omissions i18n-test.html + splash/og-card runtime-cached; sitemap 11 `<loc>` map to files + every `<lastmod>` == git commit date so `update-sitemap.mjs` was a no-op; all loaded external origins match the S79-documented 5 services — **no new CDN/API** since; THIRD_PARTY_LICENSES covers every dep); **reconciled the outside-loop PR #98 drift** (`sw.js` v318→v319, `FONT_MAKER_VERSION` 4.28→4.29 — HTML+locales+sitemap+sw only, no CORE_ASSETS/precache/data change, About changelog already done); **2 doc-drift fixed** (175d3c5): README FM **v4.26→v4.29**, CLAUDE.md FM line-count **~12,900→~13,400** (actual 13,439). Prior 2026-07-14 (**S79**: 7th run, over the S71→S78 + FM v4.18→v4.26 surface — mechanical site-health **CLEAN** (SW `CORE_ASSETS` **25** entries both directions @v305 all resolve + every root HTML precached, only intentional omissions i18n-test.html + splash/og-card runtime-cached; sitemap 11 `<loc>` all map to files + every `<lastmod>` == git commit date so `update-sitemap.mjs` was a no-op; all static internal `href`/`src` + 18 splash hrefs + manifest/sw paths resolve; THIRD_PARTY_LICENSES documents every loaded lib); **2 doc-drift fixed** (2f89745 + c878a1a): README FM **v4.17→v4.26** (trailed FONT_MAKER_VERSION), CLAUDE.md FM line-count **~12,650→~12,900** (actual 12,911), + a new THIRD_PARTY_LICENSES **"Third-Party Web Services & APIs"** section for the 5 undocumented runtime services (Hebcal, Open-Meteo **CC BY 4.0**, Nominatim/OpenStreetMap **ODbL 1.0**, Web3Forms, hCaptcha — 2 with attribution duties). A bonus off-rotation Pass A grep recon (not a formal A run) found **no P1/P2** and logged 3 low-signal candidates (dashboard visible-attribution P3; `ftRead` plain-parse P4; dictionary/dashboard `colorizeHebrew` non-esc P4). Prior 2026-07-11 (**S70**: 6th run, over the S59–S69 surface — mechanical site-health **CLEAN** (SW `CORE_ASSETS` 22 entries both directions @v279 all resolve + every root HTML precached; sitemap 11 `<loc>` all map to files + every `<lastmod>` == git commit date so `update-sitemap.mjs` reported "already current — no changes"; THIRD_PARTY_LICENSES no new dep since S58; robots/CNAME/manifest; **shared app-toast JS block re-verified byte-identical across the 3 tool pages, sha `9944abcb3943`**); **2 P4 doc-drift fixed (5bac85c):** README FM **v4.10→v4.17** (trailed FONT_MAKER_VERSION), CLAUDE.md FM line-count **~12,550→~12,650** (actual 12,634). No feature-list gaps — S59–S69 were refactors + a bug fix. Prior **S58**: 5th run — mechanical site-health **CLEAN** (SW `CORE_ASSETS` 22 entries both directions, sitemap 11 locs, links + 18 splash hrefs, all 6 bundled-font + 3 FM-lib licenses, robots/CNAME/manifest); doc-drift fixed (83991f4): README FM v4.5→v4.10 + 2 flash-cards features, THIRD_PARTY_LICENSES **Kaikki/Wiktionary entry added** (the one missing data-source), CLAUDE.md sha-dates 07-09→07-10 + **corrected `.ivrit`-engine byte-identity** (re-sha: fonts 9/9 + uploader 7/7 identical; engine flash==dash, generator differs only by the S39 `showAppToast`) + FM line-count + conv-7 a11y note. Prior 2026-07-09 (**S48**: mechanical all CLEAN (sitemap 10 locs/SW-precache both directions 21 entries/links + 18 splash hrefs/licenses (no new dep from S46–S47)/robots/CNAME/manifest + **all CLAUDE.md shared-UX counts** still hold — the flash_cards `#fsBtn` invalidated none); **3 doc-drift P4 fixed** (e4c2d9c): README FM v3.8→v4.5, README flash-cards Present/fullscreen bullet, CLAUDE FM ~10,350→~12,300 lines. Prior **S38**: mechanical all CLEAN; 2 doc-drift P4 fixed (README FM v3.6→v3.8, CLAUDE toast cross-ref). Prior **S23**: mechanical CLEAN; 2 doc-drift fixed. Prior **S19**: almost entirely clean)
- F cross-tool consistency: 2026-07-22 (**S133**: 11th run — **share / shareable-config affordance** (first dedicated sweep): **CONSISTENT, 0 fixes** — generator + dictionary converged on one `?s=` pattern (serialize→replaceState→clipboard+execCommand fallback→button-flip-"copied"→announce→1500ms restore, localized + keyboard-operable); flash_cards paste-code grandfathered; FM font-share; dashboard/torah/trope have no shareable-config state (appropriate). No laggard; only a cosmetic 📋-vs-🔗 icon/wording diff = maintainer wording call.) Prior 2026-07-20 (**S123**: 10th run — **fullscreen-toggle affordance**: present in 6/7 tools (trope_tutor has NONE → feature seed); two legitimate models (two-button+overlay-exit vs single-button toggle); dark-mode re-confirmed CONSISTENT across all 12 pages. Fixed generator + dictionary hardcoded `#fsBtn` title on `fullscreenchange` (d57108a) + torah scope holiday tooltip (faedfb4) via a recurring-shape mini-sweep for hardcoded-English `.title`/`aria-label`/`textContent` in JS; dashboard student-picker chip titles logged/deferred by the i18n pattern cap; flash_cards `aria_more_info` false positive). Prior **S111**: 9th run — the **toast/status-feedback** affordance across all 7 tools = **CONSISTENT** (every toast carries `role="status"` + a `prefers-reduced-motion` neutralizer + a `body.dark` variant); the only gap is full toast *adoption* (dictionary none; gen/dash/cards ~17/13/16 `alert()`) — a tracked large refactor (CLAUDE.md's "Inline validation, never alert()" rule), no small fix. Also spot-verified dark-toggle / tour-buttons / empty-state = consistent. Prior S100: Hebrew font-picker a11y (aria-pressed ×6, 7c9b571).
- G print & export fidelity (one tool): 2026-07-21 (**S127 — hebrew_dictionary.html**, hermetic real-data target chosen over torah_trainer (Sefaria unreachable through the proxy → no reading loads headless; needs a synthetic-fixture G, logged as a future target) and flash_cards (NO print surface at all). Ran a **6-config adversarially-verified workflow** (words/emojis/shoresh × light/dark): validated Playwright print-emulation harness per config + screenshot inspection + independent refutation. **6 findings CONFIRMED, 2 fixes.** **P2:** print silently truncates to the loaded page (100 of 13,081 words / 100 of 1,581 emojis; "Show more"+counts print-hidden) → `beforeprint` expand-all + `afterprint` restore (912d1ea). **P3/P4:** shoresh `#shoreshControls` + per-card `.wc-*` chrome leaks; `.b-len`/`.ec-tag.cat` badge + `.shoresh-patterns-title` illegible under "background graphics off"; `.ec-gender` dark-theme low-contrast → all fixed in the `@media print` block (9c8b0f3). **Clean:** card layout Letter+A4 (0 overflow, break-inside honored, RTL), dark→light reset, main chrome hide-list. `sw.js` v377→v378. Prior 2026-07-20 (**S116 — hebrew_blend_generator.html, the flagship worksheet printable and the stalest tool with a real print surface (last generator G S74; resources S104 has no worksheet)**. Drove real `page.pdf`/print-media emulation at the true Letter (749px) & A4 (727px) printable widths across **7 modes** (blend·highlight·gematria·tracing·word-search·crossword·bingo) × light/dark (fluid layout, **0 overflow either width**, `pageerror==0`, each visually inspected). **SUBSTANTIALLY CLEAN** — every S30–S32/S74 protection HELD (dark→light token reset flawless incl. grids, gematria blanks-as-borders + darkened `#6b4e12` values, word-search tinted→gold outline, crossword black-blockers deliberate, bingo 4-up dashed cuts + caller sheet, tracing fade + 4-page pagination). **1 fidelity divergence fixed (07b6549):** the dark-print-reset block relights every dark surface to its LIGHT appearance, but the answer-key banner alone relit to a beige (#f6efdd/#6b4e12) present in no other mode → the same key printed navy from light mode + exportPDF but beige from dark-mode `window.print()`; relit to the light navy/#f0d080 (computed-style verified light==dark, screen #0d1220 untouched). **Cross-tool print-reset sweep CLEAN** (torah/dictionary/dashboard/flash_cards all relight-to-light; torah even forces `.tt-ref-hdr`→dark ink) — the beige was a one-off, not a recurring pattern. Chose the generator over the S115-pointer's suggested FM font-export target (FM `.ttf` can't be produced headless — the recipe stubs Pyodide — and S115 already deep-verified it; the worksheet is the student-held, headless-verifiable artifact). Prior 2026-07-19 (**S104 — resources.html font-download-zip, the first-ever dedicated G on that export surface**. `downloadFont`→`buildLicenseNotice`/committed `licenseFile`→`makeZip`→`.zip`. Export **CLEAN**: all 4 fonts (Reuben CC0, TzviScript + Stroke Guide OFL, outside-loop Lakhish Bold) byte-accurate `.ttf` round-trip, zips CRC-valid (`unzip -t` + Python `zipfile.testzip()`), committed OFL/CC0 `LICENSE.txt` verbatim, Lakhish Bold's honest "License: Other" generated notice (actual license = the logged S99 P4 maintainer legal call). **1 P4 fixed:** malformed MS-DOS zip timestamp (date field 0 → month/day 0; now fixed 1980-01-01, d317e71). **Re-confirmed flash_cards has NO print/export surface** (the S104-pointer's suggested target) — 0 `@media print`, no `window.print`/PDF/CSV, only the `.ivrit` backup; do not re-pick it for G. Prior 2026-07-18 (**S94 — dictionary print+export, the stalest print tool (last G S65)** — no `@media print` block at all → dark-mode Ctrl+P ink-bleed + chrome leak (fixed 8dc3505); Quizlet CSV lacked a UTF-8 BOM → Excel mojibake (fixed cc09372); Anki/Quizlet nikkud + `csvEscape` confirmed sound; Anki tab/newline parity logged P4-informational (not reachable — clean corpus, no free-text entry). Prior **S84 — 2nd-pass on `torah_trainer.html`** (last S53), the suite's most-printed tool. First confirmed **flash_cards has NO print/export surface** (screen-only — only `.ivrit` backups + a paste share-code + live projection — like dashboard/trope), so the only un-G'd tool has no G surface → 2nd-pass on the stalest print tool. S53/S55 treatment substantially **HOLDS** (chrome-hide, dark→light reset, verse break-inside, trope→underline print, side-by-side single-column, RTL). **2 P2 fixed** (ce5ab3c): dark-mode reference header printed near-invisible light-gold → dark ink; nikkud **Highlight** mode (a bg tint stripped by "background graphics off") silently dropped → converted to the per-vowel underline (like Underline mode); + corrected the stale S53 comment. **Logged:** dark-mode washed-out nikkud/trope palettes P2 (`beforeprint` re-resolve) + print-legend P3. Prior 2026-07-13 (**S74 — 2nd-pass on `hebrew_blend_generator.html`** (last G was S30–S32; re-audited over the S56 crossword-RTL + S61 chunked-build + i18n changes). Headless print-emulation, all 8 modes × Letter/A4 × light/dark + PDF code-audit. **Substantially CLEAN:** every S30–S32 protection held (dark-print light-reset, per-item + whole-grid `break-inside:avoid`, Letter/A4 `@page` + landscape, gold answer-ink `#6b4e12`, gematria B6/B7 both fixed), S56 crossword RTL held, **the S61 chunked-build × print contract INTACT** (`beforeprint`→`flushWorksheetBuild` L8981 = 30/30 sheets, no truncation), **i18n boundary held** (`.worksheet{direction:ltr}` + `headerLang` decoupled → Hebrew UI prints unchanged), no nikkud clipping, `pageerror==0`. **1 P3 fixed** (e57c06d — self-check `.score-bar`/`.self-check-controls`/`.timer-bar` printed as chrome-junk + dark-mode `.btn-xs` ink-bleed; added to the print `display:none` list). **2 P4 logged** (the S62 PDF-`alert()`, still present; a pre-print reveal-all light-back navy edge case). Prior **S65 — first-ever G on `hebrew_dictionary.html`'s EXPORT surface** (it has no print stylesheet; its artifacts are downloads/clipboard, not paper — 2 surveys picked it as the only remaining tool with real un-audited human-facing exports; flash_cards/dashboard/trope are screen/JSON-only). **Word-export builders audited CLEAN** (Bulk-Copy `\n`-join, **Anki `.txt`** TSV with `#html:false` so nikkud isn't escaped, **Quizlet `.csv`** with `csvEscape` quoting `" , \n`, Word Lists denormalized store — all preserve `w.word` nikkud + logical order + round-trip). **1 fix:** emoji-mode selections were silently dropped from all 3 export paths (`exportSelectedWords` filtered `.w`); now expanded via `emojiVisibleForms` into word-shaped records (6082a7e, empirically verified — nikkud round-trips byte-for-byte). Deferred P4: no `@media print` → dark-mode Ctrl+P bleed (dictionary is a browse/export tool, not a paper deliverable). Prior **S53 — first-ever print audit of torah_trainer.html**, the suite's most-printed tool (teachers print readings for students): fixture verses through the real render path over HTTP; print-emulation computed styles + REAL Letter/A4 PDFs (12 runs) inspected via content-stream analysis. The tool's entire print treatment was 3 lines — every generator S30 pattern absent. **3 P2 fixed** (dark-navy sheets → token reset; trope tints stripped silently by background-graphics:off → clause-underline on paper; side-by-side printing WRONG verse pairings ~15 verses out of sync → single-column collapse) **+ 3 P3 fixed** (chrome incl. footer-spill + every position:fixed surface print-hidden; verse `break-inside:avoid`; print block moved to stylesheet END so equal-specificity layout overrides win by source order) in 60277fe + 070e0e0. CLEAN: legend/jumpnav/audio-chrome already print-hidden as documented, RTL + nikkud integrity in the PDF text layer, no mark clipping, interlinear/page-view sound. 4 P4s logged. Prior **S42 — first-ever audit of Hebrew_Font_Maker.html's font-file export path** (TTF/WOFF2/zip/UFO): mapped `exportFont`→`doExport`→`buildFontSpec`/`buildFEA`→fontTools→`finishDownload`; fixed the top **P1** spec-illegal PostScript name + paired **P2** empty filename (cd84087, one `fontSafeBase` helper covering psName + filename + UFO name) and a **P3** version-bump-on-failure (c796158); logged 7 more (P2 license-second-download, P2 silent Cu2Qu, P3 partial-anchor crash / zero word-spacing / shaping-skip, P4 LICENSE filename collision / UFO xHeight); confirmed 5 safe (dropped-glyph kern filter, no export falsy-zero, single UPM, FEA-error surfaced, license generated per format). Prior **S30 FIRST RUN + S31 continuation**, hebrew_blend_generator.html. S30 mapped the two output paths and fixed dark-mode print bleed **P2** + per-item page-break protection **P2/P3**. **S31 (human-directed print loop)** fixed the A4-vs-Letter divergence — added a Letter/A4 page-size control wired to both `@page` and jsPDF (1d0088d) — and block-break protection for whole crossword/word-search grids + clue/word-bank items (299a558, closes B1 crossword-split P2). Fresh P3/P4 findings logged: rw-card overflow (re-scoped), dead duplicate `@page` B5, gold answer-ink on B/W B6, gematria blank-line B7, crossword clue RTL B9. **S32 (human-directed, orientation focus)** fixed the headline **P2** — "Landscape Print" now actually rotates `window.print()` via `@page{size:<page> landscape}` wired to the toggle (cae2fbb) — and aligned the PDF export to honor the toggle for all worksheet types (266e8d6, P3))
- H teacher walkthrough / paper-cuts (one tool): 2026-07-21 (**S130 — hebrew_blend_generator.html**, its 2nd H [last S56, 74 sessions ago = the stalest-H tool]. **Mature/well-onboarded, 0 clean fixes.** Role-played a teacher building a worksheet end-to-end: excellent empty state (instruction + 5 one-click starters + tour), sane defaults, logical top→bottom selector flow with Generate at the natural end, **live-preview auto-updates after the first generate** (blendCount 40→20 verified, no click), Real-Words built-in word bank (no dead-end), `pageerror==0`. **1 P4 design-call logged:** first-generate `#generateBtn` at the bottom of the ~2693px sidebar, no sticky variant — well-mitigated (starters + instruction + live-preview + selectors-end-at-Generate); any fix (sticky/floating or empty-state Generate) is a maintainer UI-design choice → log, don't force. All 7 tools now H'd ≥1× (flash_cards S34/S120, dashboard S46/S107, generator S56/S130, dictionary S68, torah S77, trope S87, FM S97). No `sw.js` bump (ledger-only). Prior 2026-07-20 (**S120 — flash_cards.html, the stalest H target [last H S34, 86 sessions ago]; non-dashboard per the S119 variety note. Core flow mature/well-built — sane defaults (2-letter, 26 letters + 9 main vowels, count 10), first-run Tour pulse, aleph-bet deck → flip → mark ✓/✗ → coherent dual-metric results, `pageerror==0`. 1 P3 paper-cut fixed (203a0b9 — emoji-data-still-loading Start now waits + retries with a toast instead of a blocking alert; promise-cached so the 450KB file loads once; failure-fallback preserved). Candidates logged (all copy/design/minor): per-card results panel collapsed; words-mode no-default-list; mistakes-vs-weaknesses naming + opaque "Other" pill + "Emoji"-label-over-promises. Intentional/struck (not logged): per-sub-mode gender defaults (S34), hands-free loop + student-picker (struck S55). No new seeds.** Prior 2026-07-19 (**S107 — classroom_dashboard.html, 2nd dashboard H [S46 was a no-code audit → the first PRODUCTIVE dashboard H]; onboarding strong, 2 copy paper-cuts fixed + 3 candidates + 1 seed.** Role-played a fresh teacher setting up today's class board (headless fresh-context + an Explore code-map): Welcome modal sets location + 3 clear starter layouts, board not empty behind it (DEFAULT_PRESET intro text), `setupSeen` once, schedule "until" auto-prefill (the S46-logged empty-`<input type=time>` P3 is now RESOLVED), validation covers unknown-preset/missing/dup/out-of-order, timer ~2 clicks, picker no-crash/no-alert, dark/lang/inputMode shared cross-tool. **Fixed (db3221d):** the first-run skip link "Skip — use New York for now" was false once a city was typed (`frSkip`→`frCaptureLocation` keeps it) → "Skip for now"; the "Text Font" sub-header drives the Latin/`engFont` picker only → "English Font" to parallel "Hebrew Font". **Logged:** P3 Schedule-Sync 1-preset soft dead-end (explainer cites non-existent presets); P4 'Hebrew classroom' starter over-promises "Everything on"; P4 settings-drawer 12/13 panels expanded (design-call); Seed `.dash-text-display:empty::before` placeholder. Prior 2026-07-18 (**S97 — Hebrew_Font_Maker.html, first-ever H on the Font Maker (the only never-H'd tool + freshest surface post-outside-loop v5.1); effectively CLEAN onboarding, 1 P3 fixed + 1 seed.** Role-played a fresh teacher ("turn my handwriting into a classroom font") headless over HTTP: welcome modal + next-step hint, 3 input methods, workflow tabs, reference-glyph guide, export guarded ("Trace at least one letter first" — not a dead-end). **Fixed same session (2564beb, P3):** the Help `export`/`ivritsuite` tabs misdirected teachers to a removed "★ Use this Font in IvritSuite" export-picker option for the core save-to-My-Fonts payoff → corrected both (EN template + HE CSV) to the real export → Exported ✓ → ★ My IvritSuite Fonts → ★ Save current font path. **Logged 1 Feature seed:** My Fonts window only reachable post-export (no standalone opener). **H now run on all 7 tools** (flash_cards S34, dashboard S46, generator S56, dictionary S68, torah S77, trope S87, font-maker S97) — index/resources are chrome without a lesson-prep workflow. Prior 2026-07-16 (**S87 — trope_tutor.html, first-ever H on the Trope Tutor; effectively CLEAN flow, 1 P3 i18n leak fixed.** Role-played a teacher preparing a lesson end-to-end (fresh profile, HTTP, light/dark × 1280/800): **Learn** (6 family chips, cards, `nextFamily` cycles all 6 families + hands off to Drill on the last, intro links to Torah Trainer) and **Drill** (start screen w/ types-summary + mastery grid + first-run tour pulse → full 10-question session → results 80–100% + per-trope mastery grid) both complete, `pageerror==0`. **Audio-failure degradation verified robust** (all MP3s env-blocked → all 4 toast kinds fired: clip-switched / audio-unavailable / new-question / skipped — the drill still reached results, **never dead-ended**, exactly as documented). **1 fix same session** (d47e5ca): `renderLearn()` hardcoded the family-nav button text in English ('Next family →' / 'Ready? Try a drill →'), overwriting its `data-i18n` — both keys already existed + were translated (authored but never wired), so routed the ternary through `I18n.t()` (no CSV). A broad blind-spot scan for sibling hardcoded literals found none; mastery grid well-labeled. **No other paper-cuts / no Feature seeds** — the tool is feature-complete for its scope. H history (first-ever runs): flash_cards S34, dashboard S46, generator S56, dictionary S68, torah S77, **trope S87** → **index + resources remain un-H'd** (both are chrome/landing pages without a real lesson-prep workflow). Prior 2026-07-13 (**S77 — torah_trainer.html, first-ever H on the Torah trainer; effectively CLEAN flow, 1 P3 + 2 P4 fixed, rich inventory + 2 seeds.** Role-played a teacher preparing a lesson end-to-end (fresh profile, HTTP, light+dark, 1280+800): auto-loads this week's parsha, scope/display/audio/karaoke all work, settings persist (debounced `saveSettings` verified), `pageerror==0`. **Fixed same session (8aa6691):** a visible `Reading:` label on the scope dropdown (P3 — it was aria-label-only vs its labeled `Parsha:` sibling, and the tour/How-To call it "the Reading dropdown"), a `#gearBtn` aria-label (P4, closes the gearBtn half of S72 G4), and one hardcoded audio-status literal → `I18n.t` (P4); verified 35/35 headless EN+HE × light/dark × 1280/800. **Logged:** Print-button + student-handout **Feature seeds** (the core "print readings for students" gap — no print affordance today), plus P3s (buried "Jump to this week"/Diaspora toggle; custom-range never prefills; tour skips its 2 key steps on a slow first load) and P4s (low-visibility audio-unavailable status; audio-collapse `aria-expanded`). Prior 2026-07-11 (**S68 — hebrew_dictionary.html, first-ever H on the dictionary; 1 correctness fix + rich friction inventory.** Role-played a teacher building a lesson's vocabulary end-to-end (fresh profile, HTTP, light-1280 + dark-800): the core "find → save a Word List → export/carry to Flash Cards/Generator" flow works (one-box search, a solid save modal, persistence to the shared `ivritSuite_wordLists`, Anki/Quizlet incl. the S65 emoji fix). **Found + fixed a silently-wrong bug** (0de2408): in shoresh mode "Select all matching" selected the **stale Words-mode `filtered`** (63 unrelated "hand" results) instead of the visible root family (14 words) — DOM checkboxes said one thing, the saved/exported list held another; fixed via a `shoreshFam` module var + `shoreshMode` branch, regression-clean (Words 63→63, Emoji 52→52). **Logged 3 P3** (Word Lists buried 2 collapse levels + "Bulk Copy Mode" label; stale tour copy claiming a nonexistent Worksheet-Generator export + never mentioning Word Lists; save-success dead-end + "Load as filter" silently discarded) **+ 1 P4** (dup names / raw prompt-confirm) **+ 4 Feature seeds** (cross-tool "Use in Flash Cards →" deep-link = the biggest gap; per-card "Add to Word List"; word theme/category filter; reorder-in-list / surface-at-top-level). Struck-seeds rule honored. Prior **S56 — hebrew_blend_generator.html, first-ever H on the flagship worksheet tool: CLEAN.** Role-played a brand-new teacher building "Tuesday's aleph-bet worksheet" end-to-end (empty localStorage, headless over HTTP, light-1280 + dark-800): instructive empty state with 5 TRUE one-click Quick-Start recipes (one click → complete 40-cell worksheet + guiding toast); sane defaults (28 letters + 9 main vowels pre-selected, live combos count); 9 clicks manual path to a 20-cell sheet; editable worksheet title with live preview; inline preset-name validation (no alert); Print/Export-PDF/share-link/.ivrit all discoverable; only genuinely-advanced panels ship collapsed; 0 pageerrors. **No paper-cuts, no seeds; the S55 struck-seeds rule honored.** Prior **S46 — classroom_dashboard.html, first-ever H on the dashboard**: role-played a brand-new teacher (empty localStorage, headless) building a class — enabled Schedule Sync, added rows, inspected defaults/empty-states. **Strong shape:** `#frStarters` first-run starter card shows, Presets panel surfaces ✨ Quick-start layouts, `settings.location` defaults to New York (weather + Shabbat work out-of-box), instructive saved-schedule empty state, fullscreen + quick-controls present, 0 pageerrors. **1 P3 paper-cut logged:** each new schedule row's `<input type="time">` starts empty (no prefill/auto-increment, no per-row Duplicate) → typing every block-time from scratch. No trivial P1 → no fix forced (FM was the code tool per variety). Prior **S34 — FIRST RUN**, flash_cards.html: role-played a new teacher building a Tuesday aleph-bet deck end-to-end, headless, empty localStorage, light-1280 + dark-800. **0 pageerrors**, defaults sane. Confirmed live: 3 teacher panels ship collapsed; no fullscreen/present mode; no deck print/export; no custom card count; no hands-free loop/shuffle; Hebrew-only font grid (the Numbers/Colors gender-default difference is intentional per the maintainer — not logged). Logged 2 P3 + 1 P4 Candidates and **5 dual-audience Feature seeds** — the micro-feature track is now seeded. Primary intake for the still-empty→now-seeded micro-feature track.)
- I first-load & empty-state: 2026-07-21 (**S129 — 10th run; first over the S118→S128 surface; comprehensively CLEAN.** 12 pages × light/dark = 24 fresh-context runs (empty localStorage + IndexedDB): 0 pageerrors / 0 `[i18n] miss` / 0 real console errors on load + one new-user interaction/tool. New-surface probes all pass: S119 dashboard message placeholder, **S127 dictionary `beforeprint`/`afterprint` safe on fresh load** (globals array-init), S126 trope fullscreen, FM tooltips, Basic-TTS notice; empty states instructive (generator `rwListEmptyState`, FM grids, trope Learn). Lone torah console error = Sefaria unreachable-through-proxy, gracefully handled with a prominent red error banner (screenshot-verified), not a silent failure. No new candidates; paired with the Word-List reorder seed (6ee266c). Prior 2026-07-20 (**S118 — 9th run; first over the S106→S117 surface; comprehensively CLEAN.** All 12 pages in a fresh context (empty localStorage + IndexedDB) × light/dark = 24 runs `pageerror==0` / 0 `[i18n] miss` / 0 failed-local. 7-tool empty-state / absent-key probes all pass: FM fresh newProject renders grids; generator fresh Generate→1 sheet **and** `rwSource='lists'`→instructive `#rwListEmptyState`; flash_cards `startPractice` cold + `renderWordListPicker`→`#wordsEmptyState`; dictionary "📚 Word Lists"→instructive empty manager; trope `renderLearn` cold; torah reading toolbar present; the S117 Now/Next guard else-branch confirmed (nn hidden, no schedule). Onboarding copy accurate (dictionary "Bulk Copy Mode" reference still matches L886). **0 new candidates.** Pass clean + backlog design-gated → AskUserQuestion → 1 fix (969e293, dashboard power-user panels collapsed) + 2 candidates closed WON'T-FIX (flash_cards panels, torah Diaspora/Israel — maintainer decisions). Prior 2026-07-19 (**S106 — 8th run; first over the S96→S105 surface; effectively CLEAN** — 48 fresh-context runs (12 pages × light/dark × 1280/800) `pageerror==0` / 0 `[i18n] miss` / 0 failed-local + **8/8** targeted empty-state checks (the flagship S103 📚 Word Lists toolbar button opens an instructive empty manager "No word lists yet…"; the S98 Add-to-Word-List action gated for 0 lists (no dead button); generator fresh Generate→worksheet; dashboard/resources/FM/flash-cards/trope all render for a new user); 0 new candidates, nothing to fix. Prior 2026-07-18 (**S96 — 7th run; first over the S87→S95 loop surface + the outside-loop FM Beta→v5.0→v5.1 / Resources font-gallery drift; effectively CLEAN** — headless over local HTTP, a fresh browser context per run (empty localStorage **+ IndexedDB**), all 12 pages × light/dark × 1280/800 = **48 runs `pageerror==0`, 0 local-request failures, 0 `[i18n] miss`**. Every empty state instructive (FM welcome + "0/27 traced", generator "Ready to Generate" + recipes, dashboard setup starter, flash-cards "Start Practicing", dictionary "13,081 words", trope Learn intro, torah auto-load / informative Sefaria error, resources 42 links); demo paths all produce output (generator recipe→worksheet, dashboard starter, flash-cards deck→card, trope drill→"Question 1 of 10"); font-picker My-Fonts reads empty IndexedDB with no crash. Nothing to fix → worked the backlog (FM mark-editor i18n P3, generator Layout aria-labels P4, torah Print-button micro-feature). Prior 2026-07-16 (**S86 — 6th run; first over the S77→S85 surface; effectively CLEAN, 1 fresh-user i18n leak fixed** — headless over local HTTP, a fresh browser context per run (empty localStorage **+ IndexedDB**), all 12 pages × 1280/800 × light/dark = **48 runs `pageerror==0`**. Brand-new user defaults to **EN / `dir=ltr`** with the `<select>` switcher mounted, **no raw `page.feature.x` keys leaking** on 11 of 12, `lang="he"` present on all 7 Hebrew pages, OS-aware dark honored, no h-scroll, no dir/switcher issues. **Fixed the one leak** (e458161): the dashboard weather widget painted the raw key `dashboard.weather.unavailable` for a fresh user — `fetchWeather` renders loading/not_found/unavailable via `I18n.t()` which returns the key before `/locales` loads, and `applyI18n`→`renderWeather()` early-returns without a `weatherCache` so it never re-translated → routed all three status messages through a `renderWeatherStatus()` helper (`_weatherStatus`) that `renderWeather()` now re-renders, so the existing `applyI18n` path heals it (mirrors the Shabbat widget). Depth probes on the S79–S85 diffs (FM Generate Bold on an empty project, dictionary corrected tour copy, dashboard colorize on a fresh profile) all clean. Prior 2026-07-13 (**S76 — 5th run; first over the 2026-07-12 site-wide i18n rollout; effectively CLEAN, 1 i18n leak fixed** — headless over local HTTP, a fresh browser context per run (empty localStorage), all 12 pages × 1280/800 × light/dark = **48 runs `pageerror==0`, 0 local console errors, 0 failed local requests**. A brand-new user (no `hebrewBlender_lang`) defaults to **English / `dir=ltr`** with the `<select>` switcher mounted and **no raw `page.feature.x` keys leaking**; absent-key safety solid (every `JSON.parse(getItem)` guarded, incl. the no-`||` `ivritSafeParse(getItem)` settings loaders in trope/torah — static recon + runtime agree); first-run affordances fire (dashboard `frModal` seeds settings/presets, FM `welcomeOverlay`). **Fixed** the one leak: trope_tutor's four index-load `setStatus` sites were hardcoded English (Hebrew UI saw English, worst on a persisting offline-load error) → wired to the already-present `trope.status.*` keys via `setStatusKey` + a pre-ready English fallback + `applyI18n` re-translate (8845e7f, 19/19 headless). Logged flash_cards' dead `#presetEmpty`/`#profileEmpty` copy (P4). Prior 2026-07-11 (**S67 — 4th run, CLEAN** — first over the S56–S66 surface incl. the outside-loop **FM mark-editor relocation** + **trope SEO polish**: 24/24 fresh-context page-runs (12 pages × light/dark, empty localStorage **+ empty IndexedDB** per context) 0 pageerrors, OS-aware dark honored everywhere, `lang="he"` on all 7 Hebrew pages, first-run affordances fire (FM welcome / dashboard `#frStarters` / 6 tour pulses), no h-scroll @1280 or @800. **3 depth-probes CLEAN:** FM tile→Edit-shape modal (`openMarkEdit(cat,cp,{global:true})`) opens on a truly empty project (`.open`/`display:flex`/visible, `#meApplyCur` hidden, `meGlobal`, `meItem` seeded from static tables, Apply-to-all persists 0→1 — `newProject()` runs unconditionally at init so `project` is never null); trope 4 JSON-LD blocks (incl. the new HowTo) 0 parse-errors + JSON-LD↔DOM parity all pass (head-only, no render impact); torah color-code default renders (`--nikv-a=#0099bb`) + generator presets empty-state. Static: S60/S61/S65/FM commits added **zero** new `getItem` reads; every read fallback-guarded. A read-only pre-audit agent independently found no unguarded first-load crash surface. Nothing to fix. Prior **S55 — 3rd run, CLEAN**: 24/24 fresh-context empty-storage loads (now **12** pages × light/dark — first I to include trope_tutor.html), 0 pageerrors, OS-aware dark honored everywhere, FM welcome auto-opens, dashboard `frModal` starter + `frStarters` grid show, tour pulses fire on all 7 tool pages, `lang="he"` present on every Hebrew page; **10 depth probes on the never-I'd surface**: trope tutor brand-new-user Learn + drill end-to-end incl. the audio-blocked melody skip-with-toast degradation (works exactly as documented, progress key shape correct), dashboard S50 schedule prefill correctly leaves un-inferable rows empty, S54 `.vh-file` uploader focusable within its opened container on a fresh profile (collapsed panel correctly excludes it), FM fresh welcome→interact + `#fontVersion` "1.000" default, torah offline-Sefaria status message instructive (documented env artifact), 800px dark no-h-scroll on trope tutor/flash cards/dashboard. Nothing to fix. Prior **S45 — 2nd run, CLEAN**: static absent-key audit clean (all `localStorage` reads fallback-guarded; `ivritSafeParse(null)`→null guarded by every settings loader; the 3 no-fallback loaders — dashboard/torah/flash_cards — are safe); empirical **22/22** fresh-context empty-storage loads clean (11 pages × light/dark, 0 pageerrors, OS-aware dark honored, FM welcome auto-opens correctly); tour-drift: all 6 `TOUR_STEPS` engines' static targets resolve incl. the reorganized **FM v3.9–v4.4 tour (7/7)**, only the 3 documented content-dependent steps skip offline. New-user experience solid across everything added since S33. Prior **S33 — FIRST RUN, CLEAN**: static recon + 32/32 empty-storage sweep + tour-drift audit; nothing to fix)
- J metrics-informed: never run — SKIP in rotation until the impact-metrics dashboard/Worker is live (not live)
- K i18n / localization audit: 2026-07-22 (**S134**: 7th run — first over the S124→now surface (design-call batch + S131–133 + merged frozen-footer generator work); **Check A + B CLEAN, 0 fixes**: recently-changed surface fully localized (frozen-footer `#genFab` button has `data-i18n`+`data-i18n-title`+guarded fallback; the `.textContent='CARD'` hit is a `visibility:hidden`/`aria-hidden` print-alignment spacer = false positive); **0 bare-English alert/confirm across all 6 non-FM tools**; dashboard TTS-drift surface 0 bare-English chrome. The only backlog left = FM template-literal tooltips needing new maintainer-authored Hebrew (deferred). No non-gated fix.) Prior 2026-07-20 (**S124**: 6th run — Check A + B **clean**; a systematic JS-built-English blind-spot sweep found the suite in excellent shape (alert/confirm/toast **0 bare-English** across the 6 non-FM tools; dashboard `.innerHTML` widget titles + Omer line correctly follow `headerLang` = projected content, not UI). **2 fixes:** dictionary emoji-card gender forms + speak button (5dd4f94 — reused `dictionary.modal.form_*` + `dictionary.card.hear_title`, +2 new sole-form keys) + dashboard student-picker chip tooltips (9dc2662 — wired the orphaned `dashboard.picker.chip_absent`/`chip_present` keys). **FM 24-tooltip backlog logged** with a mechanism note: `renderControls` rebuilds the panels frequently **without** re-running `applyStaticI18n`, so `data-i18n-title` alone is insufficient → the correct fix is **inline guarded `I18n.t()`** in each panel builder; ~9 are pure-wiring orphaned keys (incl. the `guidesPanelHTML()` guide cluster). `sw.js` v368→v369. Prior **S112**: 5th run — Check A + B clean; the template-literal blind-spot probe found the non-FM surface clean (content/data) and wired **11 of ~17 orphaned FM `status()` toasts** through `I18n.t()` with existing EN+HE keys — b112c0b, 0 new keys, EN byte-identical, ~6 sites left. `sw.js` v354→v355. Prior **S101**: 4th run, first over the S92→S100 surface — Check-A clean (14 baselined); the tooltip-attribute **advisory backlog** (7 FM static aria-labels) → **cleared to 0 suite-wide** (f257019: `data-i18n-aria-label` + 7 new `fontmaker.*_aria` keys); a Check-A template-literal blind-spot probe found + fixed 3 slices with **0 new keys** by wiring already-orphaned CSV keys — FM `contoursPanelHTML()` (14 `contour_*` keys, 11e9880), the dashboard picker `Switch class` (b049741), the dictionary emoji-card `View details for …` (20def99). Logged the residual FM `status()`-toast + guides-panel template-literal backlog (size L). `sw.js` v343→v344. Prior 2026-07-16 (**S91**: 3rd run, first over the S82→S90 surface — localized the visible **"Sound" vowel-group header** leak (hardcoded `${sound} Sound – ${color}` template literal, invisible to Check A) at 5 sites across generator/flash_cards/dictionary via the existing `worksheet.vowels.group_header`/`flashcards.vowelgroup.header` keys (0 new keys; `window.I18n`-guarded + `applyI18n` re-render). **Check-B burndown:** FM Undo/Redo ×4 (93a31eb) + torah settings-close ×2 (3fa7fbc) wired to existing keys; FM "Editing:" header (aa7b1c6, 2 new `fontmaker.workspace.*` keys). **Remaining logged:** generator 24 numeric-aria (~22 new keys), FM 7 chrome + a fresh FM `meScopeNote` prose cluster (P3), dashboard 2, pwa 1. Also hardened `ftRead`→`ivritSafeParse` (security, off-theme). `sw.js` v321→v322. Prior 2026-07-14 (**S81**: 2nd run, first over the S72→S80 surface — `check-i18n` Check A **CLEAN** (no NEW literals; 14 baselined printed-output items), Check B = **52** untranslated `title`/`aria-label`/`placeholder` attributes lacking a `data-i18n-*` sibling (generator 24 / dashboard 13 mostly repeated "Restore default" / Font Maker 11 / torah 2 / flash_cards 1 / pwa.js 1) + empty-`he` debt CLEAN. A blind-spot probe (English in template literals / `innerHTML`, invisible to Check A) found the **index `.ivrit` save/restore UI** still hardcoded English — the last un-localized copy of the shared engine, on the home page — and localized it by reusing existing `shared.ivrit.*` keys (6d4d1a1; no CSV/build-locales; 4/4 headless EN+HE clean; `sw.js` v306→v307). Logged the 52-attr backlog + the shared "Sound" group-header (1 new key) + 122 REVIEW rows. Prior 2026-07-12 (**first run** — added the guardrail (`CLAUDE.md` § Internationalization + `scripts/check-i18n.js`), then burned down the **entire Group-B backlog**: **~47 untranslated UI strings** the rollout missed — the shared folder-tree prompts/confirms/titles (index copy), the shared My-Fonts uploader alerts (dictionary/torah/trope, restoring cross-carrier consistency), Font-Maker confirms (guides/kern-class/replace-cp/delete-glyph/convert-mark/apply-outline/reset-trop) + My-Fonts button titles + empty-state + confirm-placement, profile-rename prompts (flash cards), reset confirms (torah/trope), "✓ Copied!" (dictionary/flash cards), the QR "too many items" note (generator), and the tooltip/dismiss-resume aria-labels — all wired through `I18n.t()` (3 new keys authored: `fontmaker.panels.tip_finer_move`/`upload_ref_font`, `worksheet.qr.too_many`; everything else reused existing `shared.folders.*`/`shared.fonts.*`/`fontmaker.modals.*`/`torah.confirm.*`/`trope.confirm.*` keys). The checker also gained a paired-`data-i18n-*`-fallback filter (flash cards' dual-attribute tooltip is compliant). `check-i18n` now **0 NEW**; the baseline holds only the 6 permanent Group-A printed-output signatures. `sw.js` v290→v291. **Pre-ship audit follow-up (same day, before the PR #87 merge):** two read-only audits (localization debt + multi-language extensibility) found and fixed the remaining *visible* leaks — the torah scope dropdown's static "Aliyah 1–7" fallbacks (7 keys; the runtime labels were already localized via `refreshScopeLabels`), the torah jump-bar "Aliyah"/"Current verse" labels + the orphaned `torah.jumpnav.aria`/`torah.reading.jump_chip_aria` keys now actually wired, the "Holiday Torah Readings" optgroup header, the dashboard rich-text-editor toolbar (11 button titles + the 5 visible size-option labels + the "Spoiler" button face; rebuilt from `applyI18n` on language switch), and the "Move folder" tooltip (new `shared.folders.move_folder`, all 4 carriers; index's `ftPaths` labels re-unified so the folder-tree block is byte-identical across carriers again) — 28 new keys, `sw.js` v291→**v292**. The locale pipeline (`build-locales.js` + `check-i18n.js` Check B) was generalized to a **column-driven N-language schema** (byte-identical en/he output verified; a test `fr` column round-trips), and CLAUDE.md gained **§8 "Adding a new UI language"**, the tab-title/meta-description boundary ruling, and the Check-A template-literal blind-spot note. **Advisory backlog still open** (not blocking, a future K run): 53 static `title`/`aria-label`/`placeholder` tooltip attributes lacking a `data-i18n-*` sibling, **plus** the Check-A-invisible pocket of English titles built inside JS template literals (Font-Maker-heavy, ~20), plus the 122 `REVIEW`-flagged Hebrew rows awaiting fluent review (one self-flagged suspect: `letter_sav`→תָו, `ui-strings.csv`). A code fix here touches precached files → **bump `sw.js`**.)

**⚑ Out-of-band batch (2026-07-22, branch `claude/improvement-loop-decisions-3p7c9z`, c1591a8; `sw.js` v383→v384):** a maintainer-directed design-call review (no discovery pass; session counter untouched) landed FM edit-canvas aria-labels, FC screen-reader card announce, FC results auto-expand-on-mistakes, and the FC practice-button rename; accepted FM ccmp non-dagesh adjacency as **WON'T-FIX**; **greenlit + scoped** the dictionary theme filter (Phase-1 corpus tagging = deferred data project). This **clears** two of the "top remaining candidates" the pointer below still lists — FM editing-stage `role`/`aria-label` and flash_cards card-face `aria-live` (the announce covers it). **S131 then ran Pass B** (11th run, CLEAN — see the S132 pointer below), folding in this batch. Branch note: this work is on `claude/improvement-loop-decisions-3p7c9z`; the S126–S130 loop branch `claude/improve-loop-5xuuei` (PR #122) has since **merged** to `origin/main`.

**Next session (S135):** S134 ran **Pass K** (i18n/localization, stalest since S124) — **Check A + B clean, 0 fixes**: the recently-changed surface (design-call batch + S131–133 + the merged frozen-footer generator work) is fully localized (the frozen-footer `#genFab` button carries `data-i18n` + `data-i18n-title` + a guarded fallback; the `.textContent='CARD'` hit is a `visibility:hidden`/`aria-hidden` print-alignment **spacer** = false positive); **0 bare-English `alert`/`confirm` across all 6 non-FM tools** (S124 claim holds); dashboard TTS-drift surface has 0 bare-English chrome. Only remaining i18n backlog = the FM template-literal tooltips needing **new maintainer-authored Hebrew** (explicitly deferred in the design-call review). **Iters 1/5.** **Drift:** none since S133 (main `e9234cc`; PR #123 unmerged; sw v385, FM 5.5). **Stalest next: Pass C** (accessibility, one tool; last S125 was **Font Maker** → pick a **NON-FM** tool now — dashboard / torah_trainer / trope_tutor are the stalest-C candidates; rotation after: A S126, G S127, D S128, I S129, H S130, B S131, E S132, F S133, K S134): keyboard-only walkthrough — focus order + visibility, Escape/Enter on modals, `aria-` on interactive SVG/canvas, `prefers-reduced-motion`, contrast in both themes. **⚠ PLATEAU — 4th consecutive clean pass (B/E/F/K)** since the design-call batch: the suite is verified healthy across errors, site-health, cross-tool consistency, AND i18n. The entire remaining fix-backlog is gated on **maintainer decisions** — author the FM-tooltip Hebrew, greenlight dictionary theme-filter Phase 1, make the "Other"/"Emoji" copy call. Reruns keep confirming health and occasionally finish a small non-gated item (like S132's a11y-tail); the highest-leverage move now is unblocking the gated work, not more passes. **Branch/PR:** directed `claude/improvement-loop-decisions-3p7c9z` / open draft PR #123 (carries the design-call batch + S131–S134 loop work); the S126–S130 loop branch `claude/improve-loop-5xuuei`/#122 has **merged** — future sessions should reconcile to a single loop branch cut off latest `main`. Deploy `success` verified after merge. _(Prior pointers S130–S133 in git history + the Done log.)_
