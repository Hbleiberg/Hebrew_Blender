# IvritSuite Improvement Log

The memory of the continuous-improvement loop. **Read this first every session.** One concern per
iteration, one commit per iteration. Prioritization: P1 (data loss/security/broken core/export
corruption) > P2 (silently wrong output/undo holes/a11y blockers) > P3 (perf/dead UI/confusing
copy/consistency) > P4 (polish). Tie-breakers: (1) affects teachers' saved work, (2) affects the
printed/exported artifact a student receives, (3) dual-audience (Hebrew + secular) wins, (4) smallest diff.

## Candidates (prioritized, top = next)

- [ ] P3 | hebrew_blend_generator.html | **A4-vs-Letter print divergence** (Pass G). `@page` (~L1996) sets `margin` only, no `size:`; `exportPDF` hardcodes jsPDF `format:'letter'` (~L8104). A4-locale teachers get pagination that differs between the browser-Print path (browser default page size) and the PDF export (always US Letter). A real fix needs a page-size setting (or locale detection) applied to **both** paths — larger change; log, don't force. | found: 2026-07-08, S30 Pass G
- [ ] P3 | hebrew_blend_generator.html | **Real-word card nikkud clip risk** (Pass G). `.rw-card { overflow:hidden }` (~L1233) can clip tall below-baseline marks (holam/dagesh/shin-sin dots) on real-word flashcards in **both** the print and PDF paths. Verify layout-safety before switching to `overflow:visible` (tracing rows already use `overflow:visible` for exactly this reason). | found: 2026-07-08, S30 Pass G
- [ ] P4 | hebrew_blend_generator.html | Bingo **Cards spinner arrows are visually inverted**: `▲` calls `adjustBingoCards(-1)` (decreases) and `▼` calls `adjustBingoCards(1)` (increases) — up arrow should increase. Swap the two deltas (2-char correctness fix; the S27 aria-labels already describe the real action). | found: 2026-07-08, S27 Pass C
- [ ] P4 | hebrew_blend_generator.html | Contrast to measure (both themes): `.rw-section-header` (`var(--muted)` 0.7rem uppercase), `#rwListEmptyState` (`var(--muted)` 0.75rem), word-list picker count `<span>` (`var(--muted)` 0.72rem on `--white`) — small NEW text on the panel bg; measure alpha-composited and pin a dedicated literal if any is <4.5:1 (like the prior drill-button fix). | found: 2026-07-08, S27 Pass C
- [ ] P3 | torah_trainer.html | Full-reading render (`renderText` near L1969) builds thousands of nested color spans in one `innerHTML` string and re-runs the whole tokenize+colorize+innerHTML pass on **every** settings/nav change (`fetchAndRender`, 13 call sites, no incremental update). Colorize alone measured 6–38 ms desktop / 28–38 ms throttled for ~2,000 words; the innerHTML build + post-render re-scans (updateKaraokeWordRefs/setupVerseObserver/buildJumpNav) add more but weren't measurable offline (Sefaria fetch is remote). Likely the app's biggest one-shot block on the largest parshiyot; a real fix (incremental/partial re-render) is a large, risky refactor. Log, don't force. | found: 2026-07-06, S16 Pass D
- [ ] P4 | flash_cards.html | Letters/syllables path rebuilds `buildPool` ×3 per card (+ `pickVowel` rebuilds the vowel list) inside the same `generateDeck`/`sampleChoices` loops — negligible measured cost (letter generateDeck(20) 1.3–5.9 ms) and more entangled with per-position lock logic than the emoji pool, so left as-is in S15. Could thread the same ctx (firstPool/midPool/lastPool + vowel list) if ever worth it. | found: 2026-07-06, S15 Pass D
- [ ] P4 | classroom_dashboard.html | Cold-load: one 452 ms main-thread longtask at 4× CPU throttle (desktop clean — max 109 ms) — the ~55-call boot block (near L6071) + render-blocking 5-family Google Fonts stylesheet (near L125). One-time cost, desktop-fine; log (throttled-only, like the dictionary/generator load blocks). | found: 2026-07-06, S13 Pass D → measured S14
- [ ] P3 | hebrew_blend_generator.html | Class-Set 40 + max blendCount + answer-key Generate builds ~8,000 DOM cells synchronously in `renderWorksheet`→`appendStandardBlendSheets` (near L9037): measured 210 ms desktop first-run / 368 ms at 4× throttle (66 ms warm). Higher-risk to fix (intentional heavy config, synchronous-render contract) — needs chunked/deferred build without changing output. Log; large diff. | found: 2026-07-06, S13 Pass D
- [ ] P4 | hebrew_blend_generator.html | `exportPDF` (near L8139): `html2canvas(scale:2)` per sheet + per-pixel `getImageData` cut-line scan, worst on a 200-card bingo — largest absolute stall but user-initiated and already chunked across `await`ed pages. Not measured this session (needs the export libs, which are CDN). | found: 2026-07-06, S13 Pass D
- [ ] P4 | Hebrew_Font_Maker.html | Vestigial click-only `.panel-title` collapse handler (~L10151) is effectively dead code: the only layout ever applied is `layout-tabbed`, which `display:none`s every `.cat-panel .panel-title` (category switching goes through the keyboard-operable `#catTabs`). Delete the handler, or a11y-wire it (role/tabindex/aria-expanded/Enter-Space) if titles ever become visible again. | found: 2026-07-07, S21 Pass C
- [ ] P4 | flash_cards.html + classroom_dashboard.html + hebrew_dictionary.html | These three tools have **no reusable non-blocking toast** (they rely on `alert()`), while the generator has a clean portable one — `showAppToast(msg, ms)` (hebrew_blend_generator.html L5644, lazily creates `#appToast` role="status"). Paste it into the three laggards for **new/opportunistic** non-blocking confirmations ("Copied!", "Saved") — do NOT mass-migrate the ~15-18 existing `alert()` validation sites (CLAUDE.md §5 rule stands). | found: 2026-07-08, S24 Pass F
- [ ] P4 | CLAUDE.md | Toast inventory (§Shared UX / §engine) lists `showPreviewNotice`, Font Maker `status()`, and `ivritStatus` but omits the generator's `showAppToast` (L5644) and torah's `showHintToast` (L4514) — add them so the toast-adoption candidate above is discoverable. | found: 2026-07-08, S24 Pass F
- [ ] P4 | index.html + Hebrew_Font_Maker.html | `hebrewFontMaker_autosave` is absent from `eraseAllSettings` — **likely intentional** (don't wipe an in-progress FM project on a settings erase; FM projects aren't in the AllTools payload and the `.ivrit` file is the canonical save). Recommended action is to **document the deliberate omission with a one-line comment**, not to change behavior. Low priority. | found: 2026-07-07, S22 Pass A

## Feature seeds (micro-features only; see the Micro-feature track in the session prompt)

_(none yet — the primary intake is Pass H (teacher walkthrough), which has not run. Add dual-audience
seeds here as they surface; never invent a feature mid-session.)_

## In progress

_(none)_

## Done

- [x] 2026-07-08 | (S30 close-out) | branch/deploy note | S30 runs on branch `claude/session-6-jli5do` (per session instructions; carries all prior S-work on top of a stale/diverged local `main`). Ships as a **new draft PR** for this branch. `sw.js` VERSION v228→v229 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | fc10cc1 | hebrew_blend_generator.html | **P2/P3 (S30 Pass G — page-break protection)** — in the browser Print path, `.blend-cell`, `.ws-cell`, `.cw-cell`, and `.rw-card` had no break protection, so an item could split mid-cell/mid-card across a page boundary (unlike `.bingo-card`/`.caller-item`/`.match-item`/`.trace-row`, which already do). Added `break-inside:avoid; page-break-inside:avoid` for those four inside `@media print`, scoped to single items only — grids still flow across pages at cell boundaries. The PDF export rasterizes per sheet, so it was already unaffected. | Verified headless (print media, 1280): computed `break-inside === 'avoid'` on all four; the reading worksheet still renders its 40 cells; 0 pageerrors. | fixed: 2026-07-08, S30
- [x] 2026-07-08 | 3a658e0 | hebrew_blend_generator.html | **P2 (S30 Pass G top finding — dark-mode ink bleed on the printed worksheet)** — the browser Print path (`window.print()`) renders the live DOM through `@media print`, which reset only `body{background:white}` and never neutralized dark mode; a teacher printing with dark mode ON got dark slate worksheet backgrounds + light-on-dark Hebrew bleeding onto paper (`.cw-block`/word-search even force `print-color-adjust:exact`). `exportPDF()` was already immune (strips `.dark` on its html2canvas clone) — only `window.print()` bled. Added a dark-mode print reset inside `@media print`: reset the dark custom properties to light (fixes every `var(--*)`-based surface — bingo/caller cells, borders) + re-state the literal-color `body.dark` worksheet rules to light. All non-`!important`, after the dark rules → win by source order, while **intentional inline nikkud color-coding still wins over them (inline > stylesheet)**. `.cw-block` (black crossword cell) and the already-print-handled `.ws-cell.ws-word` deliberately left alone. | Verified headless (light+dark × 1280/800, print media, 58 checks): every worksheet surface (reading/bingo/tracing/matching/gematria/nametheletter + synthetic `.ws-cell`) → light bg + dark text under print+dark; `.cw-block` stays dark; inline nikkud span keeps `#0099bb`; screen dark mode unchanged (fix is print-scoped); light-print unregressed; 0 pageerrors. | fixed: 2026-07-08, S30
- [x] 2026-07-08 | aa18f3c | docs/IMPROVEMENT_LOG.md + docs/IMPROVEMENT_ARCHIVE.md | (S30 ledger v2 bootstrap) — added the mandated v2 sections in the session's first commit: `## Metrics` (Per-session log, Tool coverage, Pattern health, Retired patterns), `## Feature seeds` (empty), and extended the discovery-pass rotation to **A–J**. Archived the Done overflow (S24 and earlier, 89 lines) to the new `docs/IMPROVEMENT_ARCHIVE.md` so the working ledger stays cheap to read. | Verified: heading order + rotation A–J + archive head/tail by grep; log 156→~110 lines.
- [x] 2026-07-08 | (S29 close-out) | branch/deploy note | S29 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v227→v228 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S29, this session) | hebrew_blend_generator.html | **P3 (S27 Pass-C — `aria-pressed` on segmented pill button groups)** — the `.rw-drill-btn` / `.blend-type-btn` / `.color-mode-btn` / `.ivrit-mode-btn` / `.sc-cols-btn` single-select groups tracked the choice only via an `active` class, so AT couldn't announce which option was selected. Added `aria-pressed` mirroring `.active` via **one idempotent `_syncPillAria()` helper** (models `_syncTileAria`) rather than editing the ~16 `.active`-toggle sites: a **delegated document click listener** (`if (e.target.closest(PILL_SEL))`) covers every user toggle across all 5 classes, and `updateModeVisibility()` — which is also `applySettings()`'s final call — covers the programmatic re-syncs (preset / share-link / `.ivrit` restore + checkbox-driven mode changes); an init call stamps the static groups, and the 4 `.sc-cols-btn` template literals carry `aria-pressed` inline (they're rebuilt via `innerHTML`). The color-panel **Reset** action button is marked `data-noaria` and excluded from `PILL_SEL`. Single-select groups → `aria-pressed` (not `role=radiogroup`, which would need roving-tabindex/arrow-key work — left as a possible future upgrade). Purely additive; no behavior change. | Verified headless (light+dark × 1280): the contract `aria-pressed === String(.active)` holds at init, after clicks on all 5 classes, after the broad `.rw-drill-btn` clear (L9912) + `updateModeVisibility`, after `applySettings` restore (correct **without a click**), and for a `.sc-cols-btn` rebuild+click; Reset excluded + unlabeled; exactly one pressed per main group; 0 pageerrors. | fixed: 2026-07-08, S29
- [x] 2026-07-08 | (S28 close-out) | branch/deploy note | S28 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v226→v227 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S28, this session) | hebrew_blend_generator.html | **P2 (S27 Pass-C top candidate — accessible names for 22 unnamed config controls)** — the worksheet/drill config `<select>`/`<input>`/`<textarea>` controls relied on a visual-only sibling `<span>`/`<div>` (no `<label for>`, no wrapping `<label>`, no `aria-label`), so a screen reader announced only "combo box"/"edit". Added a disambiguating `aria-label` to each of the 22: `rwCount`, `wsWordCount`, `wsGridSize`, `cwWordCountSel`, `cwGridSize`, `matchCount`, `matchVersions`, `rwBingoGrid`, `rwBingoCards`, `rwBingoPerPage`, `cwImportTextarea`, `bingoGrid`, `bingoCards`, `bingoPerPage`, `npCount`, `npMin`, `npMax`, `timerDuration`, `blendCount`, `classSetCount`, `wsTitle`, `wsSubtitle`. Reused visible labels ("Number of Words" ×3, "Grid size" ×4, "Cards"/"Per page" ×2) are qualified by context (word search / crossword / bingo — hence `aria-label`, not `aria-labelledby` at the ambiguous span); `npMin`/`npMax` split "Number range" into Minimum/Maximum number; `cwImportTextarea` (no visible label) got an accurate invented name. Every name **contains the visible label text (WCAG 2.5.3 Label in Name)**. Purely additive attributes — no markup restructure, no behavior change. | Verified headless (light+dark × 1280): all 22 present with the exact expected `aria-label`, zero duplicate ids, 2.5.3 containment holds on the spot-checked set, worksheet still generates (no template-string corruption); 0 pageerrors. | fixed: 2026-07-08, S28
- [x] 2026-07-08 | (S27 close-out) | branch/deploy note | S27 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v225→v226 (precached file changed: hebrew_blend_generator). GitHub Pages deploy runs on PR merge — verify `success` after merge.
- [x] 2026-07-08 | (S27, this session) | hebrew_blend_generator.html | **P3/P4 (S27 Pass C — accessibility 2nd pass on the generator, the discovery pass this session)** — audited the flagship tool afresh (esp. the ~S22 Word Lists / real-words drill UI). **No keyboard traps** — the only click-only non-`<button>` elements (7 `.rw-section-header` divs) are fully keyboard-wired by `_wireCollapseHdr`, and `outline:none` appears zero times. Fixed the two smallest, highest-confidence classes: (a) **icon-only button names** — the flip-card `🔊` audio buttons (real-words + self-check drills, 2 sinks) + the bingo Cards spinner `▲`/`▼` gained `title`/`aria-label` ("Play audio"; "Fewer cards"/"More cards", labelled by real action); (b) **reduced-motion** — the drill `cardFlip`, the **infinite** `.timer-display.urgent` blink (WCAG 2.2.2 + vestibular), and the `.rw-section-header::before` chevron rotate now sit in the `@media (prefers-reduced-motion: reduce)` block (CLAUDE.md §4). Logged the larger findings as fresh Candidates (unnamed `<select>`/`<input>` rows **P2**, segmented-control `aria-pressed` **P3**, inverted spinner arrows **P4**, contrast suspects **P4**). | Verified headless (light+dark): normal-mode animations intact (timer/shuffle 0.5s, chevron 0.2s), all → `none`/`0s` under reduced-motion; both `🔊` carry `aria-label="Play audio"`; spinner labels "Fewer cards"/"More cards"; 0 pageerrors.
- [x] 2026-07-08 | (S26 close-out) | branch/deploy note | S26 continued on branch `claude/session-5-w827m0`, adding to the already-open **draft PR #66** (not merged → no branch restart). `sw.js` VERSION v224→v225 (precached file changed: Hebrew_Font_Maker). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | (S26 iter 1) | Hebrew_Font_Maker.html | **P2 (S21 Pass C backlog — the last SVG-stage keyboard gap; both now closed)** — the combined-sheet crop marquee (cut a letter/mark out of a "one image, all letters" or combined mark-sheet upload) was pointer-only: `startCrop` bound via `pointerdown` on `#cropImg`, sized by pointermove, committed by pointerup→`finalizeCrop`. Added a dedicated crop-mode keydown handler (`workMode==='align' && isCropSelect(curLetter())`): arrows **move** a draft box (Shift = fine, 2 vs 10 stage px), `+`/`−` **resize** symmetrically (±12, clamped to the sheet rect + min size), **Enter/Space** crop, **Esc** cancel; the first key seeds a centred ~40% box. Draft lives in `cropDrag.kbd` (no `start` field) and renders through the existing `cropSelectSVG` `cropDrag.box` path — so the pointer move/up crop branches now require `cropDrag.start` to ignore it; `_setWorkMode` clears the draft on step change. An early `isCropSelect` guard keeps the align image-nudge handler from acting in crop mode. **A crop is non-undoable by design** — `l.source` is in `ITEM_SKIP` (excluded from undo snapshots, preserved live on restore), so `reselectArea()` is the re-crop affordance and the keyboard path matches the pointer path (no `udDo`; the initial plan's "add undo" step was dropped after discovering ITEM_SKIP). Hints (stage legend + letter/mark align panels) and the `?` cheat sheet ("Crop a letter from a sheet") document the keys. FONT_MAKER_VERSION 3.7→3.8 + changelog. | Verified headless (light+dark × 1280/800, real combined-sheet fixture → `isCropSelect` true): ArrowRight seeds the draft + renders the gold marquee `<rect>`; move +10 / Shift +2 / left-edge clamp to `f.ox`; `+`/`−` grow/shrink w by ±12; Esc clears (`cropDrag===null`); Enter → `finalizeCrop` sets `source.dataUrl`+`cropRect`, `isCropSelect` now false; `reselectArea()` reverts it (crop mode returns); the **pointer** path (`cropDrag` with `start` → `onStagePointerUp`) still finalizes — no regression from the guards; cheat sheet renders the crop group (Plus/Minus kbds); 0 pageerrors.
- [x] 2026-07-08 | (S25 close-out) | branch/deploy note | S25 ran on branch `claude/session-5-w827m0` (per session instructions; origin/main already had the merged S24 work). Ships as a **draft PR**, not a direct push to `main`. `sw.js` VERSION v223→v224 (precached files changed: Hebrew_Font_Maker, torah_trainer). GitHub Pages deploy runs on PR merge, not on the branch push — verify the deploy concludes `success` after merge.
- [x] 2026-07-08 | (S25 iter 2) | torah_trainer.html | **P3 (S24 Pass F backlog — the last main content area without a friendly empty-state)** — `#ttReading` was blanked to `''` when no parsha/reading is loaded (both `renderText()`'s no-verses branch and `fetchAndRender()`'s `!r`/no-ref branch, which previously set only a status-bar line). Added a shared `renderEmptyReading()` card (mirrors the generator's `.empty-state`: decorative `aria-hidden` ✡︎ + "Pick a parsha to start" + a pointer at the dropdown), called from both sites; the `!r` branch also nulls `currentVerses` first. Static card, no animation → reduced-motion block unchanged. Dark mode recolors the heading `--navy`→`--gold-light`, like the generator. (Rarely seen — the page auto-defaults to the current week's parsha — but the initial pre-fetch and any no-ref state now show it.) | Verified headless (light+dark × 1280/800): card present + visible (offsetHeight>100) in both themes, h2 = "Pick a parsha to start", star `aria-hidden`+opacity 0.4, h2 color `--navy` light (rgb 26,39,68) / `--gold-light` dark (rgb 245,217,122), and the card returns when re-emptied; 0 pageerrors. (The unchanged non-empty render branch overwrites the card when verses load.)
- [x] 2026-07-08 | (S25 iter 1) | Hebrew_Font_Maker.html | **P2 (S21 Pass C backlog — keyboard node selection in Edit-points mode)** — points could only be *selected* by pointer (`.node-pt` pointerdown / background marquee), so the existing arrow-nudge / Delete / smooth-corner support in `nodes` mode was unreachable keyboard-only. Added `,`/`.` to cycle the single selection through every node in flat contour order (wrapping; picks first/last from an empty selection) and `i` to split the segment after the selected node at its on-curve midpoint (`_bezEval` t=0.5 → `insertNodeAt`, the keyboard twin of Alt-click). Both route through the existing `udDo`/`insertNodeAt` path, so undo + dirty + autosave are intact; no new project-mutation path. Registered in the `?` cheat sheet (Point-editing group) and the Edit-points hint line. FONT_MAKER_VERSION 3.6→3.7 + changelog. (Crop-box keyboard gap still open — separate P2.) | Verified headless (light+dark × 1280/800, square-polygon fixture → 4-node model): `.`/`,` cycle & wrap the selection (empty→0:0, →0:1, ,→0:0, ,→wrap 0:3), ArrowRight then nudges the keyboard-selected node by +5 (the whole point — previously impossible), `i` inserts a point (4→5, new node selected); 0 pageerrors.
_Older entries (S24 and earlier) archived to [`docs/IMPROVEMENT_ARCHIVE.md`](IMPROVEMENT_ARCHIVE.md)._

## Metrics

### Per-session log (one line per session)
- 2026-07-08 | S24 | iters: 1 | tools: 10 root pages (dark-mode toggle convergence) | patterns fixed: — | pass run: F | SW: v222→v223
- 2026-07-08 | S25 | iters: 2 | tools: font-maker, torah-trainer | patterns fixed: — | pass run: — (backlog) | SW: v223→v224
- 2026-07-08 | S26 | iters: 1 | tools: font-maker | patterns fixed: — | pass run: — (backlog) | SW: v224→v225
- 2026-07-08 | S27 | iters: 1 | tools: generator | patterns fixed: — | pass run: C | SW: v225→v226
- 2026-07-08 | S28 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v226→v227
- 2026-07-08 | S29 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v227→v228
- 2026-07-08 | S30 | iters: 2 (dark-print-bleed, page-break protection) + ledger v2 bootstrap | tools: generator | patterns fixed: — (Pass G is a discovery pass, not a recurring-pattern class) | pass run: G (FIRST RUN) | SW: v228→v229

### Tool coverage (last-touched date per tool)
- generator/worksheets: 2026-07-08 (S30) | font-maker: 2026-07-08 (S26) | torah-trainer: 2026-07-08 (S25) | dashboard: 2026-07-08 (S24) | flashcards: 2026-07-08 (S24) | dictionary: 2026-07-08 (S24) | index: 2026-07-08 (S24)

### Pattern health (per recurring pattern: last swept, hits that sweep, consecutive clean sweeps; detail in the sweep log below)
- falsy-zero: swept 2026-07-07 (S22), hits: 0, clean streak: 2
- undo-wiring (Font Maker): swept 2026-07-06, hits: 0, clean streak: 2
- slider-commit (Font Maker): swept 2026-07-06, hits: 0, clean streak: 2
- workMode/step reachability: swept 2026-07-06, hits: 0, clean streak: 1
- localStorage-vs-AllTools: swept 2026-07-06 (S17), hits: 1 (fixed), clean streak: 0
- unescaped-input / unsafe-parse: swept 2026-07-07 (S22), hits: 1 P1 (fixed), clean streak: 0
- listener/interval accumulation: swept 2026-07-06 (S10), hits: 0, clean streak: 1
- JSON-LD ↔ visible-content parity: swept 2026-07-06, hits: 1 cosmetic (fixed), clean streak: 0
- destructive-bulk: swept 2026-07-06, hits: 1 (fixed), clean streak: 0
- _(RETIRE a pattern after 3 consecutive clean sweeps → Retired list; retired patterns are re-checked only in pass A2)_

### Retired patterns

- _(none yet)_

## Recurring-pattern sweep status

- falsy-zero: re-swept 2026-07-07 **S22** over the hardening-pass + Word Lists + inline-validation additions — CLEAN (the `ddb3444` nullish sweep was thorough; new word-source restore fields use `??`/array/String fallbacks with no falsy-zero risk). Earlier: last swept 2026-07-06 (all 11 root HTML) — 2 hits, both legal (opentype `advanceWidth || 0` missing-value fallback; dashboard `offsetWidth || 320` layout fallback). The 2026-07-05 remainder was fixed same-day (ba33911).
- undo-wiring (Font Maker mutations that `markDirty()` without `udDo`/`udBurstBegin`): re-verified clean 2026-07-06 — all 12 `markDirty()` sites are undo-plumbing, burst-adjacent, or documented-exempt (custom-glyph CRUD, `toggleGuideCollapse` pure-UI, `savePlacement` snapshot baseline). Aside logged as P4 Candidate: opacity view-pref setters don't markDirty at all.
- slider-commit (Font Maker `oninput` sliders lacking `onchange="udBurstCommit()"`): re-verified clean 2026-07-06 — every project-data slider commits; the kern-value slider (staging draft) and opacity view-prefs are correctly outside the burst model.
- workMode/step reachability (controls reachable in only one workMode while the workflow steers past it): swept 2026-07-06 — CLEAN. Letter Metrics now renders in Align, Trace, AND Nodes; the Trace tab (with the SVG-only Edit-points/Copy-outline controls) stays visible for all letter glyphs; Anchors/Trop tabs hide only when marks are disabled.
- localStorage-vs-AllTools (keys written by a tool but missing from index.html gather/import/erase or the owning `.ivrit`): re-swept 2026-07-06 **S17** — all cross-machine data keys registered; `hebrewFontMaker_inputMode` (UI-flag class) was missing from `eraseAllSettings` and was added (b37cc42). Earlier: swept 2026-07-06 — found + fixed `hebrewBlender_hebFont`/`_hebFontSize`/`_livePreview` (de3424d). `hebrewFontMaker_inputMode` matches the exempt inputMode class (now erase-listed). The last known gap — `hebrewDictionary_lastState` (erase-only) — was registered as `dictLastState` 2026-07-06 (5d33e76). S10 re-swept all 33 written keys: export/import registry complete; the erase-side gap (8 one-time flags missing from `eraseAllSettings`) fixed (0d63af0). No known gaps remain.
- unescaped-input / unsafe-parse (innerHTML interpolation bypassing `esc()`; untrusted `JSON.parse` bypassing `ivritSafeParse`): re-swept 2026-07-07 **S22** over the hardening-pass + Word Lists additions — found + fixed a **P1** stored/reflected XSS: the `ea03110` Word Lists feature routed untrusted words (`?s=` auto-`generate()`, imported `ivritSuite_wordLists`, paste-in lists) into every Real Words drill `innerHTML` unescaped, and the generator's `colorizeHebrew` didn't escape (46fec2f — 10 drills, incl. 4 renamed-variable sinks only the payload test caught). Flash Cards hardened defensively (22efcc1); its reachable words-mode path was already escaped. Parse side CLEAN: `wlReadStoreLocal`/`wlMergeIntoStorage` use `ivritSafeParse`+`ivritSafeAssign`, `?s=`/`?ak=` decode via `ivritSafeParse`. Earlier: re-swept 2026-07-06 **S17** over the S12–S16 additions — found + fixed a real gap: `nikudColorOverrides` color values flowed unvalidated into `style="color:…"` spans in all 4 vowel-color tools; a malicious imported override was a stored-XSS vector (6bd1b7b). All other interpolations of imported/user data confirmed routed through `esc()`/`escapeHtml`/`textContent`; every untrusted `JSON.parse` uses `ivritSafeParse`, every import merge uses `ivritSafeAssign`. Earlier: swept 2026-07-06 incl. all session-1/2 additions (tooltip engine, tile a11y, tour) — clean; all render via `textContent`/`setAttribute`; the 8 `JSON.parse(localStorage…)` sites are trusted own-writes or shape-guarded `ftRead`; import paths use `ivritSafeParse`/`ivritSafeAssign`. S10 re-swept the nine S8/S9 a11y commits — clean (numeric-index interpolation only; user data through `esc()`; no new JSON.parse).
- listener/interval accumulation (S10, new pattern): all `setInterval`s in the 6 tools are `clearInterval`-guarded or one-shot (dashboard's unassigned `checkSchedule` interval is inside one-shot DOMContentLoaded — fine, but it's the only one with no clearable handle); no render/init/apply function attaches listeners to elements it doesn't recreate; all document/window listeners are named + removeEventListener-paired (tour keys, drawer trap, pointer drags, menu closers); Font Maker's `qaGrid` uses an explicit `_wired` guard. CLEAN — no accumulation risk.
- JSON-LD ↔ visible-content parity: swept in depth 2026-07-06 (all 11 files) — every FAQ Q&A visible on-page, HowTo steps match real UI (incl. flash_cards post-drawer-removal + post-tour), dictionary's "7-step walkthrough" claim matches its 7 TOUR_STEPS. One cosmetic hit fixed (index ItemList privacy entry, 73cfb7d).
- destructive-bulk (loops over all items/letters overwriting per-item customizations unconditionally): swept 2026-07-06 — `applyOutlineToAll` confirm-gated this session (94b1d14); `resetTropSideColumns`/`resetAllAnchors` confirm+undo, apply-to-all modals opt-in per letter, `migrateProject` backfills are null-guarded (never clobbers).
- console/error audit (Pass B): last run 2026-07-07 **S20** — all 11 root pages over local http, light-1280 + dark-800, load PLUS one representative interaction each (incl. index gear modal; Font Maker letter select works via `#letterGrid .ltile`): CLEAN — 22/22 page-runs, 0 pageerrors, zero non-environmental console errors/warnings (same expected torah Sefaria/ESM noise). Prior full runs S4 and S11 also clean.
- freshness/site-health (Pass E): re-run 2026-07-08 **S23** — mechanical checks (sitemap `<loc>`+`<lastmod>` vs git, sw `CORE_ASSETS` both directions @ v222, all internal links + 18 splash hrefs + data fetches, THIRD_PARTY_LICENSES vs 6 bundled TTFs + CDNs, robots/CNAME/manifest) **all CLEAN — nothing broke since S19**; docs audit found + fixed 2 drift lines from the S21 FM a11y work (README v3.5→v3.6; CLAUDE `lang="he"` list + Font Maker). Earlier: re-run 2026-07-06 **S19** (full: sitemap `<loc>`+`<lastmod>` vs git, sw CORE_ASSETS both directions, all internal links + 19 splash hrefs, THIRD_PARTY_LICENSES vs bundled fonts/CDNs, CLAUDE.md shared-block counts + AllTools key table, README, robots/CNAME/manifest). **Almost entirely CLEAN** — only 2 doc-drift lines found + fixed (README Font Maker v2.0→v3.5, CLAUDE.md line count; c7f5da1). Sitemap/sw/links/licenses/robots/manifest and all CLAUDE.md counts verified accurate. Earlier: last run 2026-07-06 (full: sitemap `<loc>`+`<lastmod>` vs git, internal links/assets incl. all 18 splash hrefs, sw CORE_ASSETS both directions, THIRD_PARTY_LICENSES vs bundled content, README + CLAUDE.md accuracy, robots/CNAME/manifest). Sitemap/links/SW/misc all CLEAN; the doc gaps found (font-license claim, README tour list + fonts list, CLAUDE.md AllTools table + DATA_CACHE) were all fixed this session (0e9070d, 3afca1f, d02fbde). Remaining: Reuben license-file parity (P4 Candidate).
- cross-tool consistency (Pass F): re-run 2026-07-08 **S24** — audited the **dark-mode toggle** across all 11 pages and converged it on the Font Maker model (aed9072): aria-pressed on all toggles, aria-label on icon-only, ☀️ glyph, and an OS-aware first-visit default suite-wide (user opt-in). Also surveyed toast/empty-state (CLEAN aria-live; logged 3 coverage candidates — torah empty-state P3, showAppToast adoption P4, CLAUDE toast-doc P4). Earlier: last run 2026-07-06 — shared-block byte-identity audit (all 4 marked JS blocks + 2 CSS blocks, sha-compared across every carrier). ivritsuite-fonts (8 copies), folder-tree JS (4) and CSS (3) were already identical; the `.ivrit` engine drift (comments/indent) and the uploader block boundary were fixed this session (cfae257, 4236b7f); index.html's engine/uploader/`.ivrit`-CSS variants are documented-deliberate. All shared-block invariants now hold and are machine-checkable by extracting the marked spans and sha-comparing.
- performance (Pass D): 2026-07-06 — **S16** on torah_trainer.html: the audio-highlight loop is already index-tracked + O(log n)/tick (not a hotspot); no setInterval/continuous rAF. Fixed the per-syllable dark-mode DOM read in the colorize hot loop (6ce8b3c). `aliyah.json` parse trivial (1.2–5.5 ms). Logged: full-reading render (nested-span innerHTML + no incremental re-render on every fetchAndRender) as the likely biggest one-shot block. **This completes the Pass-D cycle — all six tools profiled** (dictionary → generator → dashboard → flash_cards → torah_trainer; Font Maker's heavy work is lazy CDN/WASM and was surveyed as exempt). **S15** on flash_cards.html: measured largely CLEAN (no >200 ms blocks; emoji/letter deck builds single-digit to low-double-digit ms even throttled; no rAF/confetti loops; 1 Hz timer is one textContent write). Fixed the per-draw emoji-pool rebuild (4a72aaa). `hebrew_words.json` parse re-confirmed cheap (12–48 ms). **S14** on classroom_dashboard.html (long-running projector focus: quantified per-second recurring cost, not just >200 ms blocks). Found + fixed the per-second schedule-preview innerHTML rebuild (3bf4bbd — 299/300 writes redundant). `applySettings` measured fast (3.3/20.9 ms — survey overestimated, no action); cold-load one 452 ms throttled-only longtask logged P4; clock tick confirmed clean (colorize/renderDaysList short-circuit on day-key, per survey). **S13** on hebrew_blend_generator.html: fixed render-path full-corpus translit rebuild (a3507c0); cold-load corpus parse+flatMap CLEAN (17–21/98 ms); logged Class-Set 40 Generate (~8,000 cells, 210/368 ms) + exportPDF raster. **S-dictionary** (first run): desktop CLEAN (worst 113 ms); 4× throttled 457 ms load flatten+gematria + 270 ms onFilter (fixed 7ef380c, b7cfadd).
- accessibility (Pass C): **S27 ran the first 2nd-pass — accessibility Pass C on hebrew_blend_generator.html** (the flagship, biggest recent churn). Mostly CLEAN: no keyboard traps (only 7 `.rw-section-header` divs are click-only, all `_wireCollapseHdr`-wired), zero `outline:none`. Fixed icon-button names (`🔊`, bingo `▲`/`▼`) + 3 reduced-motion escapes (cardFlip, infinite timer blink, chevron); logged 4 fresh Candidates. **S28 then fixed** accessible names (`aria-label`) for all 22 unnamed config `<select>`/`<input>`/`<textarea>` controls (WCAG 2.5.3), and **S29 fixed** the segmented-control `aria-pressed` sweep (all 5 pill classes, via one `_syncPillAria` helper + delegated listener + `updateModeVisibility`/`applySettings` hooks). Remaining from this Pass C: inverted bingo spinner **P4**, contrast suspects **P4** (small tail). **S25–S26 closed BOTH S21 SVG-stage keyboard gaps** — Font Maker Edit-points mode now supports keyboard node selection (`,`/`.` cycle, `i` insert; S25), and the combined-sheet crop marquee is now keyboard-drivable (arrows move, +/− resize, Enter crop, Esc cancel; S26). The Font Maker SVG stage (node editing + cropping) is now fully keyboard-operable. S25 also added the torah empty-state (S24 Pass F backlog). **S21 ran Pass C on Hebrew_Font_Maker — the last un-audited tool; accessibility now covers ALL tools.** Font Maker was in strong shape (grid tiles, tablists, all modals' focus-trap, universal-selector RM block all already correct per the audit); fixed 3 small P3s (shared gold focus-ring, `lang="he"` on Hebrew glyph tiles, `aria-pressed` on toggles — 0487d47) and logged 2 P2 SVG-stage keyboard gaps (crop-box, node selection) + 1 P4 dead handler as Candidates. The S18 torah_trainer backlog was **closed out in S20** — verse-word roving tabindex (9790276, P2), radio-group contrast (0a5d491; the other muted suspects measured already-passing), and the grouped small items: main/skip link, lang="he", karaoke aria-current, drawer/caret RM (ddb6230). The dictionary's S12 `.shoresh-pat.filled` leftover also closed (315fea9). Last full-tool run 2026-07-06 **S18** on torah_trainer.html (keyboard-only + contrast survey). Fixed then: the settings-drawer focus management (0362ed9, P2) and the 8 panel-title collapse headers (205fa11, P3). Confirmed good already: `input:focus-visible + .slider`, tour keyboard+focus, tip-wrap keyboard pattern, audio-button aria-labels, native pickers/toggles. Still un-audited: Font Maker. Prior S12 on hebrew_dictionary.html (keyboard-only + measured contrast, light/dark × 1280/800). All 5 findings fixed same-session (4157ecc, 6d83604, 7168b74, b532e86, 373b455); 2 P4 leftovers logged (RM scrollIntoView gaps, `.shoresh-pat.filled` tiles). Confirmed clean: cards, word modal (trap/restore), bindTip source engine, tour focus handling, native-checkbox filters; no `.slider` switches. Prior: flash_cards → dashboard S8 → generator S9. Still un-audited: torah trainer, Font Maker.

### Discovery-pass rotation (run one per session, stalest first)
- B console/error audit: 2026-07-07 (**S20**: load + interaction, all 11 pages, light-1280 + dark-800 — CLEAN; see sweep-status line)
- A recurring-pattern sweep: 2026-07-07 (**S22**: all 8 classes re-swept over the hardening-pass + Word Lists + S21 additions — found + fixed a **P1** Real Words XSS (46fec2f) + defensive Flash Cards hardening (22efcc1); localStorage-registry, Font Maker undo/slider/bulk, falsy-zero all CLEAN; one likely-intentional aside logged (`hebrewFontMaker_autosave` erase omission). Prior **S17**: all 8 bug classes re-swept over the S12–S16 additions — found + fixed a real P2 stored-XSS gap (nikud color-override → style attribute, all 4 color tools, 6bd1b7b) and a P4 erase-list gap (b37cc42); the other 6 classes CLEAN incl. every named recent addition. Prior S10: the nine S8/S9 a11y commits — CLEAN; listener/interval accumulation swept — CLEAN; localStorage registry — `eraseAllSettings` flag gap fixed 0d63af0)
- C accessibility (one tool): 2026-07-08 (**S27: hebrew_blend_generator.html — first 2nd-pass; icon-button names + 3 reduced-motion escapes fixed, 4 fresh candidates logged (P2 unnamed selects, P3 segmented aria-pressed, P4 inverted spinner, P4 contrast)**. History: flash_cards → dashboard S8 → generator S9 → dictionary S12 → torah_trainer S18 → Font Maker S21 = all tools Pass-C'd once; **generator 2nd pass S27**.)
- D performance (one tool): 2026-07-06 (S16: torah_trainer — fixed the per-syllable colorize DOM read. History: dictionary → generator → dashboard → flash_cards → torah_trainer = **all 6 tools Pass-D'd**. Font Maker's heavy paths are lazy CDN/WASM, surveyed exempt.) ← Pass-D cycle COMPLETE
- E freshness/site-health: 2026-07-08 (**S23**: mechanical all CLEAN; 2 doc-drift lines fixed (README FM v3.6, CLAUDE lang="he" list). Prior **S19**: re-audit — almost entirely clean, 2 doc-drift lines fixed)
- F cross-tool consistency: 2026-07-08 (**S24**: dark-mode toggle convergence — aria-pressed/aria-label/glyph + OS-aware default, all 11 pages; toast/empty-state surveyed, 3 candidates logged. Prior 2026-07-06: shared-block byte-identity)
- G print & export fidelity (one tool): 2026-07-08 (**S30 — FIRST RUN**: hebrew_blend_generator.html — mapped the two output paths (`window.print()` = live DOM + `@media print` CSS; `exportPDF()` = per-sheet html2canvas raster). Findings: dark-mode print bleed **P2** (fixed this session), missing page-break protection **P2/P3** (fixed this session), A4-vs-Letter divergence **P3** (logged), `.rw-card{overflow:hidden}` nikkud clip **P3** (logged))
- H teacher walkthrough / paper-cuts (one tool): never run (primary intake for the micro-feature track)
- I first-load & empty-state: never run
- J metrics-informed: never run — SKIP in rotation until the impact-metrics dashboard/Worker is live (not live)

**Next session:** S30 ran **Pass G (print & export fidelity — FIRST RUN)** on the generator and shipped the two P2/P3 print fixes (dark-mode ink bleed on `window.print()`; page-break protection for split-prone items). It also brought the ledger up to full v2 structure (Metrics / Feature seeds / rotation A–J) and archived the Done overflow. **Pass rotation now: the never-run passes H (teacher walkthrough) and I (first-load & empty-state) are the stalest** — H is also the primary intake for the currently-empty micro-feature track, so running it would let a future session ship its first micro-feature. Remaining generator Pass-G backlog is **P3 "log, don't force"**: A4-vs-Letter divergence, `.rw-card{overflow:hidden}` clip. Other tractable quick-wins still parked: inverted bingo spinner **P4**, generator contrast suspects **P4**, `showAppToast` adoption **P4**, CLAUDE toast-doc **P4**, FM autosave doc-comment **P4**. Parked large items: torah full-render **P3**, generator Class-Set 40 **P3**. (Note: S30 runs on branch `claude/session-6-jli5do` as a **new draft PR**, not a direct push to `main`; confirm the Pages deploy succeeds after merge.)
