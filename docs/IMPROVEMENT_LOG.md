# IvritSuite Improvement Log

The memory of the continuous-improvement loop. **Read this first every session.** One concern per
iteration, one commit per iteration. Prioritization: P1 (data loss/security/broken core/export
corruption) > P2 (silently wrong output/undo holes/a11y blockers) > P3 (perf/dead UI/confusing
copy/consistency) > P4 (polish). Tie-breakers: (1) affects teachers' saved work, (2) affects the
printed/exported artifact a student receives, (3) dual-audience (Hebrew + secular) wins, (4) smallest diff.

## Candidates (prioritized, top = next)

- [ ] P3 | hebrew_dictionary.html | **Gold accent text < 4.5:1 in light theme** (S40 Pass C). `--gold` light = `#c9922a`: footer `a` (~L548) on white ≈ **2.7:1**, and `.shoresh-pat.filled .pat-match` (~L653, 1.15rem) ≈ **2.5:1** — both fail WCAG 1.4.3. Dark theme is fine (`--gold` `#e0a832` ≈ 8:1). Fix = a **targeted** darker-gold literal for just those two text selectors in light theme (NOT the suite-wide `--gold` token — that has app-wide blast radius). Brand-shade judgment; may be suite-wide (the generator has a parallel gold-contrast candidate). | found: 2026-07-08, S40 Pass C
- [ ] P3 | torah_trainer.html | **`fetchAndRender` over-broad try misattributes render/code errors to a Sefaria outage** (surfaced by the S40 P1; adversarial-verify confirmed). The try (L2242–2293) wraps the **whole render path** (`renderText` L2269 + post-render helpers `maybeLoadKaraoke`/`maybeShowResumeChip`/`maybeShowClickHint`/`populateVersionDropdown`), not just the awaited fetch, so the lone catch (L2293) unconditionally appends "Sefaria API may be unreachable." (L2296) to **every** error — incl. code/render throws (exactly how the S40 `esc`-bug manifested; the Hebrew is often already on screen) and a fault in the esm.sh transliteration lib. Fix: narrow the try to the network calls (`fetchSefariaText`/`pickFallbackVersion`), or branch the message so the "unreachable" hint is appended only for genuine fetch/network errors. **Natural companion to the S40 P1 — strong next-session pick.** | found: 2026-07-08, S40; upgraded P4→P3 by S40 adversarial-verify
- [ ] P3 (maintainer design call — deferred) | flash_cards.html | **Teacher-critical panels collapsed on first run** (Pass H). `#panelTimer`/`#panelTeacherTools`/`#panelAdvanced` ship `class="panel collapsed"`. **S39 correction of the S34 premise:** collapse state does **not** persist (no localStorage, not in `getSettings()`), so dropping `collapsed` changes the default for **every** user on **every** load (longer setup scroll), not just new users; AND a first-run `.pulse-attn` on `#tourBtn` (gated by `hebrewFlashCards_tourSeen`) + the tour (`TOUR_STEPS` steps 4–5 open Teacher Tools & Advanced) **already** surface these panels for a new teacher. So this is a UX judgment call, not a clean mechanical fix — **defer to the maintainer**. (Aside: `.ivrit` backup lives in `#panelAdvanced`, not Teacher Tools.) | found: 2026-07-08, S34 Pass H; re-scoped S39 Pass F
- [ ] P4 | flash_cards.html | **"My Word Lists" dead-ends to the Dictionary** (Pass H). Words mode's My-Word-Lists source shows "Create word lists in the Dictionary, then come back to drill them here" — no in-app authoring, a hard context switch mid-setup. Cross-tool fix — log, don't force. | found: 2026-07-08, S34 Pass H
- [ ] P4 | hebrew_blend_generator.html | **Gematria write-on line is a faint tint** (Pass G, B7). `.gematria-blank { height:1px; background: var(--border); }` (~L766) prints as a 1px `#c8bfa8` tan line — light on B/W. Consider a darker rule for print. | found: 2026-07-08, S31 Pass G
- [ ] P4 | hebrew_blend_generator.html | **Crossword clue RTL** (Pass G, B9). `.cw-clue` (~L1329) inherits `direction:ltr` from `.worksheet`; when the clue-language option yields Hebrew clue text, punctuation/number ordering renders LTR (on screen and paper). Not print-specific but manifests on the printed clue list. | found: 2026-07-08, S31 Pass G
- [ ] P4 | hebrew_blend_generator.html | Contrast to measure (both themes): `.rw-section-header` (`var(--muted)` 0.7rem uppercase), `#rwListEmptyState` (`var(--muted)` 0.75rem), word-list picker count `<span>` (`var(--muted)` 0.72rem on `--white`) — small NEW text on the panel bg; measure alpha-composited and pin a dedicated literal if any is <4.5:1 (like the prior drill-button fix). | found: 2026-07-08, S27 Pass C
- [ ] P3 | torah_trainer.html | Full-reading render (`renderText` near L1969) builds thousands of nested color spans in one `innerHTML` string and re-runs the whole tokenize+colorize+innerHTML pass on **every** settings/nav change (`fetchAndRender`, 13 call sites, no incremental update). Colorize alone measured 6–38 ms desktop / 28–38 ms throttled for ~2,000 words; the innerHTML build + post-render re-scans (updateKaraokeWordRefs/setupVerseObserver/buildJumpNav) add more but weren't measurable offline (Sefaria fetch is remote). Likely the app's biggest one-shot block on the largest parshiyot; a real fix (incremental/partial re-render) is a large, risky refactor. Log, don't force. | found: 2026-07-06, S16 Pass D
- [ ] P4 | flash_cards.html | Letters/syllables path rebuilds `buildPool` ×3 per card (+ `pickVowel` rebuilds the vowel list) inside the same `generateDeck`/`sampleChoices` loops — negligible measured cost (letter generateDeck(20) 1.3–5.9 ms) and more entangled with per-position lock logic than the emoji pool, so left as-is in S15. Could thread the same ctx (firstPool/midPool/lastPool + vowel list) if ever worth it. | found: 2026-07-06, S15 Pass D
- [ ] P4 | classroom_dashboard.html | Cold-load: one main-thread longtask at 4× CPU throttle (desktop fine) — the ~55-call boot block (near L6071) + render-blocking 5-family Google Fonts stylesheet (near L125). One-time cost, desktop-fine; log (throttled-only, like the dictionary/generator load blocks). Re-measured S35 Pass D (2nd pass): **321 ms @4× / 89 ms desktop** (was 452/109 in S14); **idle recurring churn CLEAN** — the S14 per-second schedule-preview rebuild fix held. | found: 2026-07-06, S13 Pass D → measured S14 → re-measured S35
- [ ] P3 | hebrew_blend_generator.html | Class-Set 40 + max blendCount + answer-key Generate builds ~8,000 DOM cells synchronously in `renderWorksheet`→`appendStandardBlendSheets` (near L9037): measured 210 ms desktop first-run / 368 ms at 4× throttle (66 ms warm). Higher-risk to fix (intentional heavy config, synchronous-render contract) — needs chunked/deferred build without changing output. Log; large diff. | found: 2026-07-06, S13 Pass D
- [ ] P4 | hebrew_blend_generator.html | `exportPDF` (near L8139): `html2canvas(scale:2)` per sheet + per-pixel `getImageData` cut-line scan, worst on a 200-card bingo — largest absolute stall but user-initiated and already chunked across `await`ed pages. Not measured this session (needs the export libs, which are CDN). | found: 2026-07-06, S13 Pass D
- [ ] P4 | hebrew_blend_generator.html + flash_cards.html + classroom_dashboard.html | **Extract `showAppToast` into a shared `═══`-marked block.** After S39, `showAppToast` (+ the `#appToast` CSS) is duplicated in the generator and the two laggards (torah keeps its own `showHintToast` variant). Promote it to a byte-identical `/* ═══ shared: app-toast ═══ */ … /* ═══ end shared: app-toast ═══ */` block (like ivritsuite-fonts / folder-tree) so the three copies can't drift. Separate refactor — deferred (would re-touch the S38-hot generator + a 3rd tool). | found: 2026-07-08, S39 Pass F
- [ ] P4 | index.html + Hebrew_Font_Maker.html | `hebrewFontMaker_autosave` is absent from `eraseAllSettings` — **likely intentional** (don't wipe an in-progress FM project on a settings erase; FM projects aren't in the AllTools payload and the `.ivrit` file is the canonical save). Recommended action is to **document the deliberate omission with a one-line comment**, not to change behavior. Low priority. | found: 2026-07-07, S22 Pass A
- [ ] P4 | hebrew_blend_generator.html | **Duplicate real-word CSS rule blocks** (surfaced S38 rw-card investigation). `.rw-card` (~L1221 & ~L1482), `.rw-heb` (~L1236 & ~L1497), and `.rw-grid` (~L1261 & ~L1502) each appear **twice**; the later of each pair wins (equal specificity), so the earlier block is dead/overridden and the two `.rw-card` blocks differ only in `padding`/`gap` (silent divergence). Consolidate to one block per selector to remove the dead CSS + drift risk (a pure cleanup — verify pixel-identical render first). | found: 2026-07-08, S38 Iter 2

## Feature seeds (micro-features only; see the Micro-feature track in the session prompt)

- [ ] S | flash_cards.html | **Fullscreen / "Present" mode** for the running deck (`#screenCard`) — no `requestFullscreen` exists anywhere in the tool; add a projector present button (enter/exit fullscreen, optionally hide chrome). Dual-audience (any flashcard use); highest classroom value; likely a self-contained ~1-button diff. | est. S | found: 2026-07-08, S34 Pass H
- [ ] S | flash_cards.html | **Hands-free auto-advance / loop mode** — there's a post-grade `autoAdvance` but no untended projection loop (advance every N seconds, optionally loop the deck). Dual-audience. | est. M | found: 2026-07-08, S34 Pass H
- [ ] S | flash_cards.html | **Random student-picker / name spinner** — draw a random name to answer; net-new, on the loop's dual-audience wishlist. Needs a small name-list store (register in AllTools + `.ivrit` if persisted — same-commit rule). | est. M | found: 2026-07-08, S34 Pass H
- [ ] S | flash_cards.html | **English/Latin display-font selector** — `#fontGrid` is Hebrew-only; the English-front word/number/color faces can't be restyled. My Fonts uploader infra already exists suite-wide (partial reuse). Dual-audience. | est. M | found: 2026-07-08, S34 Pass H

## In progress

_(none)_

## Done

- [x] 2026-07-08 | (S40 close-out) | branch/deploy note | S40 continued on `claude/ivrit-improvement-loop-v2-3nam8d`, adding to the open **draft PR #69** (open, `mergeable_state:clean`, not merged → no restart, no new PR). The **P1** (d88fa99) was committed + pushed **first**. `sw.js` VERSION **v237→v238** (precached files changed: torah_trainer + hebrew_dictionary). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | d88fa99 | torah_trainer.html | **P1 (user-reported live bug) — "esc is not a function"** — `renderInterlinear` (L2070) + `renderSideBySide` (L2133) each did `const esc = cssEscape(numLabel)`, **shadowing the global `esc()` HTML-escaper** (L1858) with a string; the next lines call `esc(numLabel)` (data-vk + verse number) → TypeError on every render, **breaking the Interlinear + Side-by-side layouts entirely** (Page view, no shadow, worked). The load catch surfaced it with a misleading "Sefaria API may be unreachable" suffix (logged separately as a candidate). Fix = pure de-shadowing: renamed the local to `vkEsc` at both sites and pointed the 3 inline `onclick` args at it; `esc(numLabel)` now resolves to the escaper. `cssEscape` only escapes `'`, so for a `chapter:verse` label the output is unchanged — data-vk, onclick args, and the `[data-vk]` verse lookups stay consistent. | Verified headless (all 3 layouts render without throwing; `data-vk="44:31"` + `onclick="speakVerse('44:31')"` correct; `esc()` is a function again; 0 pageerrors, light+dark × 1280/800). | fixed: 2026-07-08, S40
- [x] 2026-07-08 | (S40 Pass C audit + shadowing sweep) | hebrew_dictionary.html + suite-wide | **Pass C (accessibility, dictionary 2nd pass) — strong shape.** Word-card `role=button`+Enter/Space, the word-detail modal (trap+Escape+focus-restore), `bindHdrA11y` collapse headers, `aria-expanded`/`aria-pressed` sync, and the `lang="he"` chokepoint are all correct; **both S12 leftovers are now closed** (`_scrollBehavior()` gates the smooth scrolls; `.shoresh-pat.filled` is keyboard-operable). 6 small findings — 5 fixed this session (dd21b3c + 85f37ab), the gold-on-light contrast logged as a candidate. **Shadowing sweep (new pattern, prompted by the P1):** grepped every tool for `const/let/var esc =` and other locals named like suite-globals (announce/hebDisplay/colorizeHebrew/getSettings/applySettings) — **CLEAN; the 2 torah sites were the only ones** (fixed above). | Verified as described (read-only audit + grep sweep).
- [x] 2026-07-08 | dd21b3c | hebrew_dictionary.html | **P3 (S40 Pass C — SR announcements & naming)** — made `#resultCount` a live region (`role="status" aria-live="polite" aria-atomic="true"`) so the result count + "no results" empty state are announced; gave `#searchInput` an `aria-label`; added a shared visually-hidden `#a11yStatus` live region + `announce()` helper, poked in the 3 copy handlers (`copyWord`, `copyDictShareLink`, `_wmCopyFlash`). | Verified headless (light+dark × 1280/800, 0 pageerrors): live region + aria-label present; `announce()` writes `#a11yStatus`; `copyWord` announces "Copied to clipboard". | fixed: 2026-07-08, S40
- [x] 2026-07-08 | 85f37ab | hebrew_dictionary.html | **P4 (S40 Pass C — focus-visible ring + reduced-motion carets)** — `.search-wrap input` + `#shoreshInput` had `outline:none` + a border-color-only `:focus`; added a `:focus-visible` gold outline ring (both keep the border change). Added `.shoresh-patterns-title::after` + `.emoji-cat-arrow` to the reduced-motion `transition:none` list. | Verified headless (light+dark × 1280/800, 0 pageerrors): `:focus-visible` rule present for both inputs; under reduced motion both carets' `transition` computes to 0s. | fixed: 2026-07-08, S40
- [x] 2026-07-08 | e6c0128 | hebrew_dictionary.html | **P3+P4 (S40 Pass C completeness — adversarial-verify follow-ups)** — the S40 adversarial workflow confirmed the 3 fixes above correct + regression-free, and surfaced 3 in-tool gaps, all closed here: **(P3)** `#shoreshInput` (the root-lookup input, the structural twin of `#searchInput`) had **no** programmatic name — added `aria-label="Enter a Hebrew root"` (WCAG 4.1.2/3.3.2, Level A; the audit + iter-2 missed the twin); **(P4)** `copyBulkText()` ("Copy All") gave SR users no feedback — now `announce()`s on success + fallback; **(P4)** `.pos-arrow` collapse caret was missing from the reduced-motion list — added. **Note:** this is dictionary touch #3 this session (nominal 2-per-tool cap) — a deliberate exception, as it *completes* the two already-shipped Pass C concerns (naming + RM) rather than opening a new one, and includes a Level-A P3. | Verified headless (light+dark, 0 pageerrors): shoresh aria-label present; copyBulkText announces "Copied to clipboard"; `.pos-arrow` transition 0s under reduced motion. | fixed: 2026-07-08, S40
- [x] 2026-07-08 | (S39 close-out) | branch/deploy note | S39 continued on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, adding to the already-open **draft PR #69** (open, `mergeable_state:clean`, not merged → no branch restart, no new PR). `sw.js` VERSION **v236→v237** (precached files changed: flash_cards + classroom_dashboard). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 21d294d | flash_cards.html | **P3 (S39 Pass F — transliteration wayfinding hint)** — the global romanization-style `#translitStyleSelect` (Advanced → Transliteration) and the per-mode "Show transliteration" toggles (Numbers/Colors/Emoji/Words, Mode panel) are **orthogonal** (style vs. visibility) but split across panels with no signposting. Added a one-line static muted hint under the style select pointing to the per-mode visibility toggles. No JS, no state, no `getSettings()`/`.ivrit` interaction. | Verified headless (light+dark × 1280/800, 0 pageerrors): hint renders in the Transliteration sub-panel next to `#translitStyleSelect`; no behavior change. | fixed: 2026-07-08, S39
- [x] 2026-07-08 | c2b0274 | flash_cards.html + classroom_dashboard.html | **P4 (S39 Pass F convergence — non-blocking toast for import success)** — the two laggards had no reusable floating toast (unlike the generator's portable `showAppToast`) and used a **blocking** `alert('Imported successfully.')` for preset-backup import. Ported `showAppToast` (JS + `#appToast` CSS — self-contained: own element, `textContent`, `role="status"`, reduced-motion safe; flash_cards' universal RM reset already covers it, dashboard got `#appToast` added to its targeted RM list) into both and converted the two alerts. **Reframed from the S24 candidate:** every clipboard/copy path in the 3 laggards was **already** non-blocking (button-label swaps), so there was no copy-with-alert to fix; dictionary has no success alert (skipped — would be dead code); the shared `.ivrit` "Restored successfully" alert (+ its `ivritStatus` companion) stays (touching it = every embedding page). 2 tools, one concern. | Verified headless (light+dark × 1280/800, 0 pageerrors): `showAppToast` callable on both, creates `#appToast` role="status", `.show` toggles, light bg `--navy` / dark `#0a0f1c`, auto-hides; the two call sites no longer `alert`. | shipped: 2026-07-08, S39
- [x] 2026-07-08 | (S39 Pass F audit) | 6 tools + index | **Pass F (cross-tool consistency) — non-blocking toast / success-feedback affordance.** Inventoried toast helpers across all tools: generator `showAppToast` (L5751), torah `showHintToast` (L4535), Font Maker `status()` (L2036), the `.ivrit` engine `ivritStatus`; the 3 "laggards" (flash_cards, dashboard, dictionary) had **no floating toast**. Key reframe of the S24 candidate: **copies are already non-blocking** in all 3 (✓ Copied! label swaps), so the only in-scope blocking *success* alerts were the twin `alert('Imported successfully.')` (flash_cards L3999, dashboard L4898) — converged this session (c2b0274 above). Out of scope (verified): dictionary (no success alert), the shared-engine restore alert, all error/validation alerts + `confirm()` guards (CLAUDE.md §5). Logged a follow-up (extract `showAppToast` to a shared block) + corrected the C1 panels-collapsed candidate to a maintainer design call. | Verified as described (read-only audit + the one convergence fix).
- [x] 2026-07-08 | (S38 close-out) | branch/deploy note | S38 is fresh work on the designated branch `claude/ivrit-improvement-loop-v2-3nam8d`, which sits **cleanly on `origin/main`@65f9b9d** — the merged S30–S37 history. A `git fetch origin main` **force-updated the stale local ref** (`3da0582→65f9b9d`, the exact staleness the S34 note diagnosed), confirming `HEAD==origin/main` (ahead 0 / behind 0), no divergence, and **no open PR** (the prior PR #68 lineage is merged/closed). So S38 stacks on the merged base and opens a **new draft PR**. `sw.js` VERSION **v235→v236** (precached file changed: hebrew_blend_generator; README/CLAUDE aren't precached). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | a65220f | hebrew_blend_generator.html | **P4 (S27 Pass C candidate — inverted bingo Cards spinner)** — the Cards number-spinner was inverted: top ▲ decreased (`adjustBingoCards(-1)`, "Fewer cards") and bottom ▼ increased. **Corrected the ledger's proposed fix**: swapping only the deltas would have left each aria-label contradicting its new action (the S27 labels describe the *current* action, not the intended one), so a delta swap = an a11y regression. Instead swapped each button's whole action (delta + title + aria-label), keeping glyphs/positions → conventional spinner: top ▲ = "More cards" = increase, bottom ▼ = "Fewer cards" = decrease, all mutually consistent. `adjustBingoCards` unchanged; the 2 buttons are its only callers. | Verified headless (light+dark × 1280/800, 0 pageerrors): ▲ raises 30→31 & clamps at 200; ▼ lowers 31→30 & clamps at 1; each button's onclick/title/aria-label agree. | fixed: 2026-07-08, S38
- [x] 2026-07-08 | (S38 Iter 2, no code) | hebrew_blend_generator.html | **P3 rw-card nikkud clip risk — MEASURED, NOT REPRODUCIBLE → candidate CLOSED.** The premise (tall marks — holam/shin-sin dots — clipped by `.rw-card { overflow:hidden }`) is structurally unreachable: `.rw-card` has `min-height:80px` but **no fixed/max height** (flex column auto-grows), and `.rw-grid` cells stretch, so there is **no vertical overflow** for `overflow:hidden` to clip. Faithful headless measurement (8 worst-case top-mark words incl. `לֹא` = tall lamed + holam, × light/dark × 1280/800): **overflowY=0 on every card**, `.rw-heb` line-box top sits **18.8px below** the card's clip edge (gapBelow 46.8px), **no top-clip, 0 pageerrors**. Kept `overflow:hidden` → no horizontal-bleed regression either. A speculative padding/line-height nudge to the html2canvas-rasterized export path was **not** warranted. (Aside logged as P4 below: duplicate `.rw-card`/`.rw-heb`/`.rw-grid` rule blocks.) | Verified via the faithful reading-drill-card measurement. | closed: 2026-07-08, S38
- [x] 2026-07-08 | d2718ca | README.md + CLAUDE.md | **P4 (Pass E doc reconciliation)** — README Font Maker "(Currently v3.6.)" → **v3.8** to match `FONT_MAKER_VERSION`; CLAUDE.md now cross-references the page-level toast helpers (generator `showAppToast` L5751, torah `showHintToast` L4535, generator `showPreviewNotice` L5783, engine `ivritStatus`) under the Font Maker Toasts primitive — closing the pending "CLAUDE toast-doc" candidate (whose own refs were stale: showAppToast L5644→5751, showHintToast L4514→4535, and it wrongly claimed CLAUDE.md "lists showPreviewNotice"). | Verified: referenced symbols re-grepped at the cited lines; doc-only, no behavior touched. | fixed: 2026-07-08, S38
- [x] 2026-07-08 | (S38 Pass E audit) | site-wide | **Pass E (freshness & site-health) — mechanical checks CLEAN.** sitemap `<lastmod>` vs git (all 10 pages = today's commit date), `sw.js` CORE_ASSETS both directions (21 entries @ v235, all resolve; only intentional omissions og-card/zelle-qr), all internal `href`/`src` + 18 splash hrefs resolve, THIRD_PARTY_LICENSES covers every bundled font + CDN (html2canvas/jspdf/opentype/pyodide/harfbuzz/transliteration + 22 Google Fonts + Ezra SIL/Culmus + GA4/PocketTorah/Sefaria), robots/CNAME/manifest consistent. Doc-drift: 2 P4 found + fixed (d2718ca above). Ledger's other flagged doc items (S36 retirements, S35 flash-cards feature, shared-block counts "8 files"/"11 pages"/tour "6 tools", reduced-motion "10 files", lang="he"/tooltip "Implemented on" lines) all verified **NOT drifted**. Harmless P4 note: CORE_ASSETS precaches duplicate root + `/icons/` icon copies (redundant, not a defect). | Verified as described (read-only audit).
- [x] 2026-07-08 | (S37 close-out) | branch/deploy note | S37 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v234→v235 (precached file changed: Hebrew_Font_Maker). **No `FONT_MAKER_VERSION` bump** — dead-code removal, not a user-facing change. GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 60d4092 | Hebrew_Font_Maker.html | **P4 (S21 Pass C candidate — remove the dead `.panel-title` collapse handler)** — `initPanels()` bound a click-to-collapse handler to every `.panel-title`, but the workspace always runs `layout-tabbed` (added unconditionally at init, never removed) with `#catPanelGroup` nested in `#catHost`, so `#catHost .cat-panel .panel-title { display:none }` hides all 12 titles — never visible/clickable (category switching goes through the keyboard-operable `#catTabs`). Confirmed dead **empirically** (0 visible `.panel-title` at 1280 & 760 px). Removed the one-line function + its `init()` call; no a11y-wiring (titles never render). Fresh-tool pick (font-maker untouched since S26). | Verified headless (light+dark × 1280/800): `initPanels` gone (typeof undefined), init completes (56 letter tiles), titles stay hidden, category-tab switching intact (letter→fontdetails), 0 pageerrors, no reference errors. | fixed: 2026-07-08, S37
- [x] 2026-07-08 | (S37 Pass B audit, no code) | all 11 root pages | **Pass B (console & error audit) — CLEAN.** Loaded every root page headless (light-1280 + dark-800, load + one representative interaction each: index gear, generator letter tile, dashboard settings drawer, flash cards mode-pill + custom-count, dictionary search, Font Maker letter tile, dark-toggle everywhere). **0 pageerrors across all 22 page-runs.** The only console errors were 10× `URL scheme "file" is not supported` from the dictionary/torah `fetch('data/*.json')` — a `file://`-harness artifact (Chromium blocks `fetch()` of `file:` URLs); re-checked those corpus-loading pages over a local **HTTP** server → **0 pageerrors / 0 real console errors**, confirming the errors are purely environmental. No console-error regressions from the S21–S36 additions (dark-mode aria, FM keyboard, generator a11y/print, the flash-cards feature). Matches the S20 result. | Verified as described (file:// + HTTP re-check); ledger-only.
- [x] 2026-07-08 | (S36 close-out) | branch/deploy note | S36 continued on branch `claude/ivrit-improvement-loop-v2-kbwrfu`, adding to the already-open **draft PR #68** (open, not merged → no branch restart). `sw.js` VERSION v233→v234 (precached file changed: flash_cards). GitHub Pages deploy runs on merge — verify `success` after merge.
- [x] 2026-07-08 | 952edcd | flash_cards.html | **P3 (S36 Pass A finding — clamp `cardCount` on restore; hardens the S35 feature)** — the S35 custom card-count clamped the custom *input* but the restore path (`applySettings`) + `setCardCount()` were unbounded, so a shared `.ivrit`/preset or hand-edited `hebrewFlashCards_settings` with e.g. `cardCount:500000` (shape-valid → `ivritSafeParse` passes it) would drive `generateDeck`'s `attempts<n*30` loop to ~15M iterations and **freeze the tab on Start**. Added a shared `_clampCardCount(n)` (NaN/garbage→10, else clamp to [1..200]) and routed `setCardCount`, `setCustomCardCount`, and the `applySettings` restore assignment through it — every value reaching `generateDeck` is now bounded + saved back (self-healing). | Verified headless (light+dark, fresh localStorage, 0 pageerrors): restore 500000→200, 0→1, -5→1, "abc"/missing→10, valid 12→12 / 75→75; chokepoint 999999→200; the freeze scenario (restore 999999 → generate) yields a bounded 200-card deck in ~10 ms; the S35 custom-input + preset behavior is unregressed. | fixed: 2026-07-08, S36
- [x] 2026-07-08 | (S36 Pass A sweep) | recurring-pattern sweep | **Pass A — re-swept all 8 ACTIVE classes over the S23–S35 additions** (flash_cards custom card-count, Font Maker keyboard crop/node-insert, generator a11y + print CSS, suite-wide dark-mode `aria-pressed`). **No P1/P2.** Falsy-zero CLEAN (new code uses `??`/clamps; only the 2 vetted `||` exceptions remain); unescaped-input/unsafe-parse CLEAN (count-grid `innerHTML` interpolates only constants + a clamped Number; page-size `@page` allowlists `a4|letter`; no bare `JSON.parse`/`Object.assign`); localStorage-vs-AllTools CLEAN (the feature added **no** key — `cardCount` rides `hebrewFlashCards_settings`); FM undo-wiring + slider-commit CLEAN (node-insert routes through `udDo`; keyboard crop reuses the deliberately-non-undoable `finalizeCrop`, like the pointer path); JSON-LD parity, destructive-bulk, workMode/step all CLEAN. **One actionable finding → fixed this session (952edcd above):** the unclamped-`cardCount`-on-restore robustness gap. | Verified via the sweep + my own falsy-zero/localStorage cross-greps; the finding drove the one fix.
_Older entries (S35 and earlier) archived to [`docs/IMPROVEMENT_ARCHIVE.md`](IMPROVEMENT_ARCHIVE.md)._

## Metrics

### Per-session log (one line per session)
- 2026-07-08 | S24 | iters: 1 | tools: 10 root pages (dark-mode toggle convergence) | patterns fixed: — | pass run: F | SW: v222→v223
- 2026-07-08 | S25 | iters: 2 | tools: font-maker, torah-trainer | patterns fixed: — | pass run: — (backlog) | SW: v223→v224
- 2026-07-08 | S26 | iters: 1 | tools: font-maker | patterns fixed: — | pass run: — (backlog) | SW: v224→v225
- 2026-07-08 | S27 | iters: 1 | tools: generator | patterns fixed: — | pass run: C | SW: v225→v226
- 2026-07-08 | S28 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v226→v227
- 2026-07-08 | S29 | iters: 1 | tools: generator | patterns fixed: — | pass run: — (backlog) | SW: v227→v228
- 2026-07-08 | S30 | iters: 2 (dark-print-bleed, page-break protection) + ledger v2 bootstrap | tools: generator | patterns fixed: — (Pass G is a discovery pass, not a recurring-pattern class) | pass run: G (FIRST RUN) | SW: v228→v229
- 2026-07-08 | S31 | iters: 2 (Letter/A4 page-size control, crossword/word-search block-break) | tools: generator | patterns fixed: — | pass run: G (human-directed continuation) | SW: v229→v230
- 2026-07-08 | S32 | iters: 2 (landscape window.print() wiring P2, landscape PDF for all types P3) | tools: generator | patterns fixed: — | pass run: G (human-directed, orientation focus) | SW: v230→v231
- 2026-07-08 | S33 | iters: 1 (discovery only — CLEAN, no fix) | tools: all 8 (audit, no edits) | patterns fixed: — | pass run: I (FIRST RUN, CLEAN) | SW: v231 (no bump)
- 2026-07-08 | S34 | iters: 2 (gold answer-ink print P3, dead `@page` reconcile P3) | tools: generator (fixes); flash cards (Pass H audit, no code) | patterns fixed: — (Pass G/H are discovery passes, not recurring-pattern classes) | pass run: H (FIRST RUN — teacher walkthrough; seeded the micro-feature track: 3 candidates + 5 seeds) | SW: v231→v232
- 2026-07-08 | S35 | iters: 2 (**custom card-count MICRO-FEATURE — the loop's first**, from an S34 Pass H seed) | tools: flash cards (feature); dashboard (Pass D audit, no code) | patterns fixed: — (Pass D is a discovery pass) | pass run: D (2nd pass — dashboard, CLEAN: S14 per-second fix held; cold-load P4 re-measured) | SW: v232→v233 | note: reverted the S34 gender-default candidate (maintainer flagged intentional)
- 2026-07-08 | S36 | iters: 1 (clamp `cardCount` on restore — P3 robustness, hardens the S35 feature) | tools: flash cards | patterns fixed: Pass A recurring-pattern sweep of all 8 classes over the S23–S35 additions — **CLEAN, no P1/P2**; 1 adjacent robustness finding fixed (unclamped restore → tab-freeze). Retired undo-wiring + slider-commit (3 clean). | pass run: A (recurring-pattern sweep) | SW: v233→v234
- 2026-07-08 | S37 | iters: 1 (remove dead FM `.panel-title` collapse handler — P4, fresh-tool pick) | tools: font-maker | patterns fixed: — (Pass B is a discovery pass) | pass run: B (console & error audit, all 11 pages — **CLEAN**, 0 pageerrors; the only console errors were environmental `file://` data-fetch failures, gone over HTTP) | SW: v234→v235
- 2026-07-08 | S38 | iters: 3 (Pass E doc reconcile P4; rw-card clip **measured→closed** P3; inverted bingo spinner P4) | tools: generator (bingo fix; rw-card measured, no-code); docs (README + CLAUDE toast cross-ref) | patterns fixed: — (Pass E is a discovery pass) | pass run: E (freshness & site-health — mechanical **CLEAN**; 2 doc-drift P4 found + fixed) | SW: v235→v236
- 2026-07-08 | S39 | iters: 2 (Pass F toast convergence P4 — flash_cards + dashboard; transliteration wayfinding hint P3 — flash_cards) | tools: flash cards, dashboard | patterns fixed: — (Pass F is a discovery pass) | pass run: F (cross-tool consistency — non-blocking toast/success-feedback; converged the twin `alert('Imported successfully.')` on the portable `showAppToast`) | SW: v236→v237
- 2026-07-08 | S40 | iters: 4 (**P1 user-reported torah "esc is not a function" de-shadow** — d88fa99; dictionary SR announcements P3 — dd21b3c; dictionary focus-visible + RM carets P4 — 85f37ab; dictionary Pass C completeness P3+P4 — e6c0128, adversarial-verify follow-ups) | tools: torah-trainer (P1), dictionary (Pass C ×3 — deliberate cap exception, completing the shipped concerns incl. a Level-A P3) | patterns fixed: **new "shadowed-global helper" pattern registered + swept — 2 hits (both torah, fixed), rest of suite CLEAN** | pass run: C (accessibility — dictionary 2nd pass, strong shape; 5-agent adversarial verify + suite shadowing sweep) | SW: v237→v238

### Tool coverage (last-touched date per tool)
- generator/worksheets: 2026-07-08 (S38) | font-maker: 2026-07-08 (S37) | torah-trainer: 2026-07-08 (S40) | dashboard: 2026-07-08 (S39) | flashcards: 2026-07-08 (S39) | dictionary: 2026-07-08 (S40) | index: 2026-07-08 (S24)

### Pattern health (per recurring pattern: last swept, hits that sweep, consecutive clean sweeps; detail in the sweep log below)
- falsy-zero: swept 2026-07-08 (S36), hits: 0 (grep clean — only the 2 vetted `||` exceptions), clean streak: 3 — **kept ACTIVE** (not retired) because the S36 sweep found an adjacent numeric-value-restore gap in the same family (`cardCount` unclamped on restore, fixed 952edcd); keep numeric-safety under watch.
- workMode/step reachability: swept 2026-07-08 (S36), hits: 0, clean streak: 2
- localStorage-vs-AllTools: swept 2026-07-08 (S36), hits: 0 (S35 feature added no key), clean streak: 1
- unescaped-input / unsafe-parse: swept 2026-07-08 (S36), hits: 0, clean streak: 1
- listener/interval accumulation: swept 2026-07-06 (S10), hits: 0, clean streak: 1
- JSON-LD ↔ visible-content parity: swept 2026-07-08 (S36), hits: 0, clean streak: 1
- destructive-bulk: swept 2026-07-08 (S36), hits: 0, clean streak: 1
- shadowed-global helper (a local `const`/`let`/`var` shadowing a suite-global helper — e.g. `esc` — then calling it as a function): **NEW, registered S40** (prompted by the P1). Swept 2026-07-08 (S40), hits: 2 (both torah `renderInterlinear`/`renderSideBySide`, fixed d88fa99), rest of suite CLEAN. clean streak: 0 (first sweep found hits) — **ACTIVE**, keep under watch.
- _(undo-wiring + slider-commit **RETIRED at S36** — 3 consecutive clean sweeps; see Retired patterns. RETIRE a pattern after 3 consecutive clean sweeps → Retired list; retired patterns are re-checked only in pass A2)_

### Retired patterns

- undo-wiring (Font Maker: `markDirty()` without `udDo`/`udBurstBegin`/`udNudgeTick`) | retired 2026-07-08 (S36) | 3 consecutive clean sweeps; the S25–26 keyboard additions (node-insert, crop) verified as routing through `udDo` or deliberately non-undoable. Re-checked only in Pass A2.
- slider-commit (Font Maker: `oninput` range sliders lacking `onchange="udBurstCommit()"`) | retired 2026-07-08 (S36) | 3 consecutive clean sweeps; every project-data slider commits, and no new range slider has been added. Re-checked only in Pass A2.

## Recurring-pattern sweep status

- falsy-zero: re-swept 2026-07-08 **S36** over the S23–S35 additions — grep CLEAN (the S35 custom card-count uses `??`/`Math.min/max` clamps; only the 2 vetted `||` exceptions remain). **BUT** the same sweep surfaced an adjacent numeric-safety gap — `cardCount` restored (`applySettings` `?? 10`) + `setCardCount` were **unclamped**, so a corrupt/shared blob with a huge `cardCount` could freeze the tab on Start; fixed by clamping every setter + the restore through `_clampCardCount` (952edcd, P3). So falsy-zero is at 3 clean but **kept ACTIVE** (numeric-restore family still warrants watch). Earlier: re-swept 2026-07-07 **S22** — CLEAN (nullish sweep thorough). Earlier: last swept 2026-07-06 (all 11 root HTML) — 2 hits, both legal (opentype `advanceWidth || 0` missing-value fallback; dashboard `offsetWidth || 320` layout fallback). The 2026-07-05 remainder was fixed same-day (ba33911).
- undo-wiring (Font Maker mutations that `markDirty()` without `udDo`/`udBurstBegin`): re-verified clean 2026-07-06 — all 12 `markDirty()` sites are undo-plumbing, burst-adjacent, or documented-exempt (custom-glyph CRUD, `toggleGuideCollapse` pure-UI, `savePlacement` snapshot baseline). Aside logged as P4 Candidate: opacity view-pref setters don't markDirty at all.
- slider-commit (Font Maker `oninput` sliders lacking `onchange="udBurstCommit()"`): re-verified clean 2026-07-06 — every project-data slider commits; the kern-value slider (staging draft) and opacity view-prefs are correctly outside the burst model.
- workMode/step reachability (controls reachable in only one workMode while the workflow steers past it): swept 2026-07-06 — CLEAN. Letter Metrics now renders in Align, Trace, AND Nodes; the Trace tab (with the SVG-only Edit-points/Copy-outline controls) stays visible for all letter glyphs; Anchors/Trop tabs hide only when marks are disabled.
- localStorage-vs-AllTools (keys written by a tool but missing from index.html gather/import/erase or the owning `.ivrit`): re-swept 2026-07-06 **S17** — all cross-machine data keys registered; `hebrewFontMaker_inputMode` (UI-flag class) was missing from `eraseAllSettings` and was added (b37cc42). Earlier: swept 2026-07-06 — found + fixed `hebrewBlender_hebFont`/`_hebFontSize`/`_livePreview` (de3424d). `hebrewFontMaker_inputMode` matches the exempt inputMode class (now erase-listed). The last known gap — `hebrewDictionary_lastState` (erase-only) — was registered as `dictLastState` 2026-07-06 (5d33e76). S10 re-swept all 33 written keys: export/import registry complete; the erase-side gap (8 one-time flags missing from `eraseAllSettings`) fixed (0d63af0). No known gaps remain.
- unescaped-input / unsafe-parse (innerHTML interpolation bypassing `esc()`; untrusted `JSON.parse` bypassing `ivritSafeParse`): re-swept 2026-07-07 **S22** over the hardening-pass + Word Lists additions — found + fixed a **P1** stored/reflected XSS: the `ea03110` Word Lists feature routed untrusted words (`?s=` auto-`generate()`, imported `ivritSuite_wordLists`, paste-in lists) into every Real Words drill `innerHTML` unescaped, and the generator's `colorizeHebrew` didn't escape (46fec2f — 10 drills, incl. 4 renamed-variable sinks only the payload test caught). Flash Cards hardened defensively (22efcc1); its reachable words-mode path was already escaped. Parse side CLEAN: `wlReadStoreLocal`/`wlMergeIntoStorage` use `ivritSafeParse`+`ivritSafeAssign`, `?s=`/`?ak=` decode via `ivritSafeParse`. Earlier: re-swept 2026-07-06 **S17** over the S12–S16 additions — found + fixed a real gap: `nikudColorOverrides` color values flowed unvalidated into `style="color:…"` spans in all 4 vowel-color tools; a malicious imported override was a stored-XSS vector (6bd1b7b). All other interpolations of imported/user data confirmed routed through `esc()`/`escapeHtml`/`textContent`; every untrusted `JSON.parse` uses `ivritSafeParse`, every import merge uses `ivritSafeAssign`. Earlier: swept 2026-07-06 incl. all session-1/2 additions (tooltip engine, tile a11y, tour) — clean; all render via `textContent`/`setAttribute`; the 8 `JSON.parse(localStorage…)` sites are trusted own-writes or shape-guarded `ftRead`; import paths use `ivritSafeParse`/`ivritSafeAssign`. S10 re-swept the nine S8/S9 a11y commits — clean (numeric-index interpolation only; user data through `esc()`; no new JSON.parse).
- listener/interval accumulation (S10, new pattern): all `setInterval`s in the 6 tools are `clearInterval`-guarded or one-shot (dashboard's unassigned `checkSchedule` interval is inside one-shot DOMContentLoaded — fine, but it's the only one with no clearable handle); no render/init/apply function attaches listeners to elements it doesn't recreate; all document/window listeners are named + removeEventListener-paired (tour keys, drawer trap, pointer drags, menu closers); Font Maker's `qaGrid` uses an explicit `_wired` guard. CLEAN — no accumulation risk.
- JSON-LD ↔ visible-content parity: swept in depth 2026-07-06 (all 11 files) — every FAQ Q&A visible on-page, HowTo steps match real UI (incl. flash_cards post-drawer-removal + post-tour), dictionary's "7-step walkthrough" claim matches its 7 TOUR_STEPS. One cosmetic hit fixed (index ItemList privacy entry, 73cfb7d).
- destructive-bulk (loops over all items/letters overwriting per-item customizations unconditionally): swept 2026-07-06 — `applyOutlineToAll` confirm-gated this session (94b1d14); `resetTropSideColumns`/`resetAllAnchors` confirm+undo, apply-to-all modals opt-in per letter, `migrateProject` backfills are null-guarded (never clobbers).
- console/error audit (Pass B): re-run 2026-07-08 **S37** — all 11 root pages headless (file://), light-1280 + dark-800, load + one representative interaction each (index gear, generator letter tile, dashboard drawer, flash cards mode-pill + custom-count, dictionary search, FM letter tile, dark-toggle everywhere): **CLEAN — 22/22 page-runs, 0 pageerrors**. The only console errors were 10× `URL scheme "file" is not supported` (dictionary `hebrew_words.json`, torah `parshiyot/pockettorah/*`) — a file:// `fetch()` artifact; re-checked those over a local HTTP server → **0 pageerrors / 0 real console errors**, purely environmental. No console regressions from the S21–S36 additions. Prior: last run 2026-07-07 **S20** — all 11 root pages over local http, light-1280 + dark-800, load PLUS one representative interaction each (incl. index gear modal; Font Maker letter select works via `#letterGrid .ltile`): CLEAN — 22/22 page-runs, 0 pageerrors, zero non-environmental console errors/warnings (same expected torah Sefaria/ESM noise). Prior full runs S4 and S11 also clean.
- freshness/site-health (Pass E): re-run 2026-07-08 **S38** — mechanical checks (sitemap `<loc>`+`<lastmod>` vs git = all 10 pages @ today; sw `CORE_ASSETS` both directions @ v235 = 21 entries all resolve, only intentional omissions og-card/zelle-qr; all internal `href`/`src` + 18 splash hrefs; THIRD_PARTY_LICENSES vs bundled fonts + all CDNs + 22 Google Fonts; robots/CNAME/manifest) **all CLEAN — nothing broke since S23**; docs audit found + fixed 2 P4 drift lines (README FM v3.6→v3.8; CLAUDE page-level toast cross-ref) and verified the ledger's other flagged doc items (S36 retirements, S35 flash-cards feature, shared-block/tour/reduced-motion/lang="he" counts) **NOT drifted**; harmless P4 note logged (duplicate root+`/icons/` icon precache). Prior: re-run 2026-07-08 **S23** — mechanical checks (sitemap `<loc>`+`<lastmod>` vs git, sw `CORE_ASSETS` both directions @ v222, all internal links + 18 splash hrefs + data fetches, THIRD_PARTY_LICENSES vs 6 bundled TTFs + CDNs, robots/CNAME/manifest) **all CLEAN — nothing broke since S19**; docs audit found + fixed 2 drift lines from the S21 FM a11y work (README v3.5→v3.6; CLAUDE `lang="he"` list + Font Maker). Earlier: re-run 2026-07-06 **S19** (full: sitemap `<loc>`+`<lastmod>` vs git, sw CORE_ASSETS both directions, all internal links + 19 splash hrefs, THIRD_PARTY_LICENSES vs bundled fonts/CDNs, CLAUDE.md shared-block counts + AllTools key table, README, robots/CNAME/manifest). **Almost entirely CLEAN** — only 2 doc-drift lines found + fixed (README Font Maker v2.0→v3.5, CLAUDE.md line count; c7f5da1). Sitemap/sw/links/licenses/robots/manifest and all CLAUDE.md counts verified accurate. Earlier: last run 2026-07-06 (full: sitemap `<loc>`+`<lastmod>` vs git, internal links/assets incl. all 18 splash hrefs, sw CORE_ASSETS both directions, THIRD_PARTY_LICENSES vs bundled content, README + CLAUDE.md accuracy, robots/CNAME/manifest). Sitemap/links/SW/misc all CLEAN; the doc gaps found (font-license claim, README tour list + fonts list, CLAUDE.md AllTools table + DATA_CACHE) were all fixed this session (0e9070d, 3afca1f, d02fbde). Remaining: Reuben license-file parity (P4 Candidate).
- cross-tool consistency (Pass F): re-run 2026-07-08 **S39** — audited the **non-blocking toast / success-feedback** affordance across all tools; the 3 laggards (flash_cards, dashboard, dictionary) had no floating toast while the generator has the portable `showAppToast`. Reframe: copies were **already** non-blocking (label swaps) everywhere, so only the twin blocking `alert('Imported successfully.')` (flash_cards L3999, dashboard L4898) were in scope → converged both on `showAppToast` (c2b0274); dictionary skipped (no success alert), the shared `.ivrit` restore alert + all error/validation alerts left per CLAUDE.md §5. Logged the `showAppToast` shared-block extraction follow-up; corrected the C1 panels candidate to a maintainer design call. Prior **S24** — audited the **dark-mode toggle** across all 11 pages and converged it on the Font Maker model (aed9072): aria-pressed on all toggles, aria-label on icon-only, ☀️ glyph, and an OS-aware first-visit default suite-wide (user opt-in). Also surveyed toast/empty-state (CLEAN aria-live; logged 3 coverage candidates — torah empty-state P3, showAppToast adoption P4, CLAUDE toast-doc P4). Earlier: last run 2026-07-06 — shared-block byte-identity audit (all 4 marked JS blocks + 2 CSS blocks, sha-compared across every carrier). ivritsuite-fonts (8 copies), folder-tree JS (4) and CSS (3) were already identical; the `.ivrit` engine drift (comments/indent) and the uploader block boundary were fixed this session (cfae257, 4236b7f); index.html's engine/uploader/`.ivrit`-CSS variants are documented-deliberate. All shared-block invariants now hold and are machine-checkable by extracting the marked spans and sha-comparing.
- performance (Pass D): 2026-07-08 — **S35** 2nd pass on classroom_dashboard.html (all-day projector focus): re-profiled cold load + a ~4.2 s idle recurring-churn probe (desktop + 4× throttle). **CLEAN** — idle DOM churn ~4 nodes over ~4 clock ticks (just the clock time text), 0 idle longtasks; the **S14 per-second schedule-preview rebuild fix HELD**. Cold-load one 321 ms longtask @4× / 89 ms desktop = the already-logged P4 (boot block + 5-family Google Fonts), re-measured, no new action. Earlier: 2026-07-06 — **S16** on torah_trainer.html: the audio-highlight loop is already index-tracked + O(log n)/tick (not a hotspot); no setInterval/continuous rAF. Fixed the per-syllable dark-mode DOM read in the colorize hot loop (6ce8b3c). `aliyah.json` parse trivial (1.2–5.5 ms). Logged: full-reading render (nested-span innerHTML + no incremental re-render on every fetchAndRender) as the likely biggest one-shot block. **This completes the Pass-D cycle — all six tools profiled** (dictionary → generator → dashboard → flash_cards → torah_trainer; Font Maker's heavy work is lazy CDN/WASM and was surveyed as exempt). **S15** on flash_cards.html: measured largely CLEAN (no >200 ms blocks; emoji/letter deck builds single-digit to low-double-digit ms even throttled; no rAF/confetti loops; 1 Hz timer is one textContent write). Fixed the per-draw emoji-pool rebuild (4a72aaa). `hebrew_words.json` parse re-confirmed cheap (12–48 ms). **S14** on classroom_dashboard.html (long-running projector focus: quantified per-second recurring cost, not just >200 ms blocks). Found + fixed the per-second schedule-preview innerHTML rebuild (3bf4bbd — 299/300 writes redundant). `applySettings` measured fast (3.3/20.9 ms — survey overestimated, no action); cold-load one 452 ms throttled-only longtask logged P4; clock tick confirmed clean (colorize/renderDaysList short-circuit on day-key, per survey). **S13** on hebrew_blend_generator.html: fixed render-path full-corpus translit rebuild (a3507c0); cold-load corpus parse+flatMap CLEAN (17–21/98 ms); logged Class-Set 40 Generate (~8,000 cells, 210/368 ms) + exportPDF raster. **S-dictionary** (first run): desktop CLEAN (worst 113 ms); 4× throttled 457 ms load flatten+gematria + 270 ms onFilter (fixed 7ef380c, b7cfadd).
- accessibility (Pass C): **S40 — 2nd pass on hebrew_dictionary.html (cold since S24; a11y last at S12): strong shape.** Word-card `role=button`+Enter/Space, the word-detail modal (trap+Escape+focus-restore), `bindHdrA11y` collapse headers, `aria-expanded`/`aria-pressed` sync, and `lang="he"` all correct; **both S12 leftovers closed** (`_scrollBehavior()` gates smooth scrolls; `.shoresh-pat.filled` keyboard-operable). Fixed 5 of 6 findings: `#resultCount` live region + `#searchInput` aria-label + shared `#a11yStatus`/`announce()` copy announcements (dd21b3c P3); `:focus-visible` ring on the 2 search inputs + RM caret coverage (85f37ab P4). Logged the gold-on-light contrast (footer `a` ≈2.7:1, `.shoresh-pat.filled .pat-match` ≈2.5:1) as a candidate. An **adversarial verification workflow (5 read-only agents)** confirmed the P1 + Pass C fixes correct/regression-free and drove a completeness commit (e6c0128 — `#shoreshInput` aria-label = the P3 twin the audit missed, `copyBulkText` announce, `.pos-arrow` RM), plus upgraded the torah error-copy candidate to **P3** (over-broad `fetchAndRender` try). The suite-wide "shadowed-global helper" grep sweep was CLEAN. **S27 ran the first 2nd-pass — accessibility Pass C on hebrew_blend_generator.html** (the flagship, biggest recent churn). Mostly CLEAN: no keyboard traps (only 7 `.rw-section-header` divs are click-only, all `_wireCollapseHdr`-wired), zero `outline:none`. Fixed icon-button names (`🔊`, bingo `▲`/`▼`) + 3 reduced-motion escapes (cardFlip, infinite timer blink, chevron); logged 4 fresh Candidates. **S28 then fixed** accessible names (`aria-label`) for all 22 unnamed config `<select>`/`<input>`/`<textarea>` controls (WCAG 2.5.3), and **S29 fixed** the segmented-control `aria-pressed` sweep (all 5 pill classes, via one `_syncPillAria` helper + delegated listener + `updateModeVisibility`/`applySettings` hooks). Remaining from this Pass C: inverted bingo spinner **P4**, contrast suspects **P4** (small tail). **S25–S26 closed BOTH S21 SVG-stage keyboard gaps** — Font Maker Edit-points mode now supports keyboard node selection (`,`/`.` cycle, `i` insert; S25), and the combined-sheet crop marquee is now keyboard-drivable (arrows move, +/− resize, Enter crop, Esc cancel; S26). The Font Maker SVG stage (node editing + cropping) is now fully keyboard-operable. S25 also added the torah empty-state (S24 Pass F backlog). **S21 ran Pass C on Hebrew_Font_Maker — the last un-audited tool; accessibility now covers ALL tools.** Font Maker was in strong shape (grid tiles, tablists, all modals' focus-trap, universal-selector RM block all already correct per the audit); fixed 3 small P3s (shared gold focus-ring, `lang="he"` on Hebrew glyph tiles, `aria-pressed` on toggles — 0487d47) and logged 2 P2 SVG-stage keyboard gaps (crop-box, node selection) + 1 P4 dead handler as Candidates. The S18 torah_trainer backlog was **closed out in S20** — verse-word roving tabindex (9790276, P2), radio-group contrast (0a5d491; the other muted suspects measured already-passing), and the grouped small items: main/skip link, lang="he", karaoke aria-current, drawer/caret RM (ddb6230). The dictionary's S12 `.shoresh-pat.filled` leftover also closed (315fea9). Last full-tool run 2026-07-06 **S18** on torah_trainer.html (keyboard-only + contrast survey). Fixed then: the settings-drawer focus management (0362ed9, P2) and the 8 panel-title collapse headers (205fa11, P3). Confirmed good already: `input:focus-visible + .slider`, tour keyboard+focus, tip-wrap keyboard pattern, audio-button aria-labels, native pickers/toggles. Still un-audited: Font Maker. Prior S12 on hebrew_dictionary.html (keyboard-only + measured contrast, light/dark × 1280/800). All 5 findings fixed same-session (4157ecc, 6d83604, 7168b74, b532e86, 373b455); 2 P4 leftovers logged (RM scrollIntoView gaps, `.shoresh-pat.filled` tiles). Confirmed clean: cards, word modal (trap/restore), bindTip source engine, tour focus handling, native-checkbox filters; no `.slider` switches. Prior: flash_cards → dashboard S8 → generator S9. Still un-audited: torah trainer, Font Maker.

### Discovery-pass rotation (run one per session, stalest first)
- B console/error audit: 2026-07-08 (**S37**: load + interaction, all 11 pages, light-1280 + dark-800 — CLEAN, 0 pageerrors across 22 page-runs; the only console errors were environmental `file://` data-fetch failures (dictionary/torah), confirmed gone over a local HTTP server. Prior **S20** also CLEAN.)
- A recurring-pattern sweep: 2026-07-08 (**S36**: all 8 classes re-swept over the S23–S35 additions (flash_cards custom card-count, Font Maker keyboard crop/node-insert, generator a11y + print CSS, suite-wide dark-mode `aria-pressed`) — **no P1/P2**; 1 adjacent robustness finding fixed (unclamped `cardCount` on restore → tab-freeze, 952edcd P3). All 8 classes CLEAN; **retired undo-wiring + slider-commit** (3 consecutive clean). Prior **S22**: all 8 classes re-swept over the hardening-pass + Word Lists + S21 additions — found + fixed a **P1** Real Words XSS (46fec2f) + defensive Flash Cards hardening (22efcc1); localStorage-registry, Font Maker undo/slider/bulk, falsy-zero all CLEAN; one likely-intentional aside logged (`hebrewFontMaker_autosave` erase omission). Prior **S17**: all 8 bug classes re-swept over the S12–S16 additions — found + fixed a real P2 stored-XSS gap (nikud color-override → style attribute, all 4 color tools, 6bd1b7b) and a P4 erase-list gap (b37cc42); the other 6 classes CLEAN incl. every named recent addition. Prior S10: the nine S8/S9 a11y commits — CLEAN; listener/interval accumulation swept — CLEAN; localStorage registry — `eraseAllSettings` flag gap fixed 0d63af0)
- C accessibility (one tool): 2026-07-08 (**S40: hebrew_dictionary.html — 2nd pass, strong shape** — `#resultCount` live region + `#searchInput` aria-label + shared `#a11yStatus`/`announce()` copy announcements (dd21b3c P3), `:focus-visible` ring + RM carets (85f37ab P4); gold-on-light contrast logged; also registered + swept the new "shadowed-global helper" pattern. Prior **S27**: hebrew_blend_generator.html — first 2nd-pass; icon-button names + 3 reduced-motion escapes fixed, 4 fresh candidates logged (P2 unnamed selects, P3 segmented aria-pressed, P4 inverted spinner, P4 contrast)**. History: flash_cards → dashboard S8 → generator S9 → dictionary S12 → torah_trainer S18 → Font Maker S21 = all tools Pass-C'd once; **generator 2nd pass S27**.)
- D performance (one tool): 2026-07-08 (**S35 — 2nd pass on classroom_dashboard.html, CLEAN**: idle recurring churn minimal (~4 nodes/4 ticks — clock text only), 0 idle longtasks — the S14 per-second schedule-preview rebuild fix held; cold-load 321 ms @4× / 89 ms desktop, the already-logged P4, re-measured. History: dictionary → generator → dashboard → flash_cards → torah_trainer = all 6 tools Pass-D'd once (cycle complete S16); dashboard 2nd pass S35.)
- E freshness/site-health: 2026-07-08 (**S38**: mechanical all CLEAN (sitemap/SW-precache/links/licenses/robots/manifest); 2 doc-drift P4 fixed (README FM v3.6→v3.8, CLAUDE page-level toast cross-ref); ledger's other flagged doc items verified NOT drifted. Prior **S23**: mechanical all CLEAN; 2 doc-drift lines fixed. Prior **S19**: re-audit — almost entirely clean)
- F cross-tool consistency: 2026-07-08 (**S39**: non-blocking toast / success-feedback — ported the portable `showAppToast` into flash_cards + dashboard and converged the twin import-success alerts (c2b0274); copies were already non-blocking (reframe), dictionary skipped (dead code), shared-engine + error alerts left per §5; logged the shared-block extraction follow-up; added the flash-cards translit wayfinding hint (21d294d). Prior **S24**: dark-mode toggle convergence, all 11 pages; toast/empty-state surveyed. Earlier 2026-07-06: shared-block byte-identity)
- G print & export fidelity (one tool): 2026-07-08 (**S30 FIRST RUN + S31 continuation**, hebrew_blend_generator.html. S30 mapped the two output paths and fixed dark-mode print bleed **P2** + per-item page-break protection **P2/P3**. **S31 (human-directed print loop)** fixed the A4-vs-Letter divergence — added a Letter/A4 page-size control wired to both `@page` and jsPDF (1d0088d) — and block-break protection for whole crossword/word-search grids + clue/word-bank items (299a558, closes B1 crossword-split P2). Fresh P3/P4 findings logged: rw-card overflow (re-scoped), dead duplicate `@page` B5, gold answer-ink on B/W B6, gematria blank-line B7, crossword clue RTL B9. **S32 (human-directed, orientation focus)** fixed the headline **P2** — "Landscape Print" now actually rotates `window.print()` via `@page{size:<page> landscape}` wired to the toggle (cae2fbb) — and aligned the PDF export to honor the toggle for all worksheet types (266e8d6, P3))
- H teacher walkthrough / paper-cuts (one tool): 2026-07-08 (**S34 — FIRST RUN**, flash_cards.html: role-played a new teacher building a Tuesday aleph-bet deck end-to-end, headless, empty localStorage, light-1280 + dark-800. **0 pageerrors**, defaults sane. Confirmed live: 3 teacher panels ship collapsed; no fullscreen/present mode; no deck print/export; no custom card count; no hands-free loop/shuffle; Hebrew-only font grid (the Numbers/Colors gender-default difference is intentional per the maintainer — not logged). Logged 2 P3 + 1 P4 Candidates and **5 dual-audience Feature seeds** — the micro-feature track is now seeded. Primary intake for the still-empty→now-seeded micro-feature track.)
- I first-load & empty-state: 2026-07-08 (**S33 — FIRST RUN, CLEAN**: static recon (no absent-key crashes — all reads fallback-guarded/try-caught, `JSON.parse(null)`→null) + empirical headless empty-storage sweep (all 8 pages × light/dark, 32/32 checks, 0 pageerrors, every empty-state/first-run renders) + tour-drift audit (all 6 `TOUR_STEPS` engines' targets resolve; the 3 skips are content-dependent, offline-only). New-user experience solid suite-wide; nothing to fix)
- J metrics-informed: never run — SKIP in rotation until the impact-metrics dashboard/Worker is live (not live)

**Next session:** S40 fixed a **user-reported P1** — the Torah Trainer "esc is not a function" (a local `const esc = cssEscape(...)` shadowed the global `esc()` escaper in `renderInterlinear` + `renderSideBySide`, breaking Interlinear + Side-by-side entirely; renamed to `vkEsc`, d88fa99) — and ran **Pass C** on the cold dictionary (strong shape; result-count + copy announcements dd21b3c P3, focus-visible ring + RM carets 85f37ab P4; gold-on-light contrast logged). Registered a **new recurring pattern "shadowed-global helper"** and swept the suite CLEAN (torah was the only hit). **Stalest pass is now G** (print & export fidelity, last run **S32** — older than I/S33, H/S34, D/S35). G is one-tool: it ran S30–S32 on the generator's print/export, so the natural next target is **Hebrew_Font_Maker.html** — its real font-file export path (TTF/OTF generation + download) has never been G'd, and it's variety-OK (last code S37). (J stays skipped — no impact-metrics dashboard.) **Variety:** index (S24) + font-maker (S37) are coldest; torah + dictionary (S40), flash_cards + dashboard (S39), generator (S38) are hot. **Top candidates by priority:** two fresh items — dictionary gold-on-light contrast **P3** (targeted literal; may be suite-wide — the generator has a parallel one) and the torah misleading-error copy **P4**; torah full-render **P3** + generator Class-Set 40 **P3** stay parked (large). Other P4 backlog: generator print tail, duplicate `.rw-*` CSS, `showAppToast` shared-block extraction, FM autosave doc-comment. **Micro-feature track:** 4 flash_cards seeds remain (flash_cards hot — wait). (Note: S38–S40 ride **draft PR #69** on `claude/ivrit-improvement-loop-v2-3nam8d`, open + `mergeable_state:clean`; confirm the Pages deploy succeeds after merge.)
